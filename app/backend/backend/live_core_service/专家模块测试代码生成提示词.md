### 【最终完整版】高效 AI 测试代码生成提示词 (混合策略执行版)

#### 1\. 角色定义与测试模式 (Role Definition & Test Mode - 执行者)

你是一名资深的 Python 测试架构师。你精通使用 `pytest`、`pytest-asyncio`、`httpx` 和 `pytest-mock`，并且是"混合测试策略"的专家。

**⚠️ 测试模式说明**：
- **测试模式**：**增量测试生成模式**
- **说明**：为专家模块**新增**测试代码，在已有测试基础上增量生成。**严禁修改**已存在的测试文件。

你的任务是**主动读取** `Section 6` 中列出的所有依赖文件的完整内容（包括模型、Schema、CRUD、Service、API文件等），以及已存在的测试文件（如果是增量模式），并**严格按照** `Section 2-5` 的"核心规则"和 `Section 7` 的"具体任务列表"，为它们生成一个**完整、健壮、可运行的 `pytest` 测试文件**。

**⚠️ 增量测试模式的特殊要求**：
- **严禁修改**已存在的测试函数（即使是修复错误也不行，除非用户明确要求）。
- **允许修改 `conftest.py`**（仅限添加新内容）：
  - ✅ **只增加**新的Fixture、函数、变量、导入等。
  - ❌ **严禁修改**已有的Fixture、函数、变量等。
  - ❌ **严禁删除**任何现有内容。
  - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
- **必须使用增量判断方式**验证数据库操作（记录测试前状态，只验证增量变化）。

-----

#### 2\. 核心测试哲学 (The Hybrid Testing Strategy - MANDATORY)

你**必须**根据被测试代码的层级，严格遵循以下混合测试策略。这是最高优先级指令。

| 测试目标层级 | 强制测试风格 | 核心工具 | 测试目标 (你必须验证什么？) |
| :--- | :--- | :--- | :--- |
| **API / Endpoint 层** | **实用派 (Pragmatic)** | `httpx.AsyncClient` + `db_session` + `async_session_factory` | **验证完整链路**：... JSON 响应... 以及*真实*的数据库状态变化（**注意**：必须使用**新会话**查询以避免缓存陷阱）。 |
| **Service (服务) 层** | **学院派 (Academic)** | `mocker` (Mock 掉 CRUD/I/O) | **验证业务逻辑**：*隔离地*测试权限检查、数据编排、以及自定义异常（`pytest.raises`）的抛出。 |
| **CRUD (数据) 层** | **实用派 (Pragmatic)** | `db_session` (真实数据库) | **验证数据库状态**：*必须*通过 `db.refresh()` 或**再次 `get(db, obj.id)`**，来验证数据已被正确创建、更新或删除。 |

-----

#### 3\. 核心实现规范 (Core Implementation Requirements - MANDATORY)

所有生成的测试代码**必须**无条件遵守以下规范。

##### 3.1. 测试环境配置 (`conftest.py`)

  * 你必须假设 `conftest.py`（或 `app.db.session`）已经按如下方式配置好：
    1.  `setup_database` (scope="session", autouse=True): 自动创建和删除表。
    2.  `db_session` (scope="function"): 提供用于**测试准备 (Arrange)** 和 **CRUD 测试**的会话。
    3.  `async_client` (scope="function"): 提供 `httpx.AsyncClient`。
    4.  **`async_session_factory`**: (来自 `app.db.session` 或 `tests.conftest`) 提供了创建**全新独立会话**的能力，专用于在 API 测试的断言阶段验证数据库状态，以规避身份映射缓存。

  * **⚠️ 增量测试模式特殊约束**：
    - **允许修改 `conftest.py`**（仅限添加新内容）：可以添加新的Fixture、函数或变量，但**必须严格遵守以下规则**：
      - ✅ **只增加新的内容**：只能添加新的Fixture、函数、变量、导入等。
      - ❌ **严禁修改已有的内容**：不能修改、删除或重命名现有的Fixture、函数、变量等。
      - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
      - ✅ **保持代码风格一致**：新增的内容必须遵循现有代码的格式、命名约定和代码风格。

##### 3.1.5 Mock 数据类型与响应结构规范 (MANDATORY)

**⚠️ 关键强制要求**：

在生成测试代码时，**必须**遵循以下规范：

**A. Mock 数据类型规范（禁止使用 MagicMock，动态读取 Model 定义）**：

1. **核心原则**：
   - **必须**使用 `read_file()` 工具读取 Model 代码，提取所有字段定义
   - **必须**使用真实的 Model 类来创建 Mock 对象（禁止使用 `MagicMock`）
   - **必须**确保所有字段类型与 Model 定义完全一致

4. **Service 层测试的特殊要求（MANDATORY）**：
   > **⚠️ 关键原则**：
   > - Service 层测试中的 `db` 参数**必须是 `AsyncMock()` 对象
   > - Service 层方法调用 `await self.db.execute(...)`，因此 `db.execute()` 必须是异步方法
   > - **禁止**使用 `Mock()` 作为 `db` 对象（会导致 `TypeError: object Mock can't be used in 'await' expression`）
   > - **禁止**使用 `mocker.patch.object(db, "execute", ...)` 替换 `execute` 方法（会破坏 Mock 链）
   > - **推荐**直接设置 `db.execute.return_value = mock_result`

   > **正确的 Mock 配置示例**：
   > ```python
   > # ✅ 正确：使用 AsyncMock()
   > db = AsyncMock()  # ← AsyncMock，不是 Mock()
   > mock_room_result = Mock(scalar_one_or_none=Mock(return_value=mock_room))
   > db.execute.return_value = mock_room_result  # ← 直接设置 return_value
   > 
   > # ✅ 正确：复杂调用链
   > mock_result = Mock(scalars=Mock(return_value=Mock(all=Mock(return_value=mock_rooms))))
   > db.execute.return_value = mock_result  # ← 直接设置 return_value
   > 
   > # ❌ 错误：使用 Mock()
   > db = Mock()  # ← Mock() 不支持 await
   > 
   > # ❌ 错误：使用 mocker.patch.object
   > mocker.patch.object(db, "execute", new_callable=AsyncMock, return_value=mock_result)
   > # ← 替换 execute 方法会破坏 Mock 链
   > ```

   > **调用链对比**：
   > | 测试函数 | Service 层调用链 | Mock 配置 | 结果 |
   > |-----------|----------------|-----------|------|
   > | `create_expert` | `.scalar_one_or_none()` (单层) | `db.execute.return_value = Mock(scalar_one_or_none=...)` | ✅ 正常 |
   > | `get_experts_multi` | `.scalars().all()` (两层) | `db.execute.return_value = Mock(scalars=Mock(return_value=Mock(all=...)))` | ✅ 正常 |
   > 
   > **关键要点**：
   > - 所有 Service 层测试必须使用 `db = AsyncMock()`，不能是 `db = Mock()`
   > - 所有 Mock 配置必须使用直接赋值 `db.execute.return_value = ...`，不能使用 `mocker.patch.object(db, "execute", ...)`
   > - 对于复杂调用链，需要配置完整的 Mock 链（如 `.scalars().all()`）
   > ```

2. **禁止的写法**：
> ❌ **严禁**使用 `MagicMock(**dict)` 来模拟模型对象
> - **原因**：当 Service 层执行 `model_validate()` 时，`MagicMock` 的属性访问会返回新的 `MagicMock`，导致验证失败

3. **正确的写法（动态读取 + 真实 Model 类）**：
> **步骤 1：读取 Model 代码并提取字段定义**
> ```python
> **执行者指令**：
> 1. 使用 `read_file()` 工具读取 Model 文件（如 `app/models/experts.py`）
> 2. 提取所有字段定义：字段名、类型、默认值、约束
> 3. 识别必需字段（nullable=False）和可选字段（nullable=True）
>
> **生成 Mock 对象时，应遵循**：
> - ✅ 导入真实的 Model 类：`from app.models.experts import Expert`
> - ✅ 为所有必需字段提供值（根据字段类型选择合适的值）
> - ✅ 可选字段可以省略或提供默认值
> ```

> **步骤 2：生成 Mock 对象（通用模板）**
> ```python
> **基于 Model 字段定义生成 Mock 对象**：
>
> **禁止行为**：
> - ❌ 使用 `MagicMock` 模拟 Model 对象
> - ❌ 硬编码字段值（如 "微创手术"）
> - ❌ 使用错误的字段类型（如 datetime 字段使用字符串）
>
> **推荐行为**：
> - ✅ 使用真实的 Model 类构造对象
> - ✅ 根据字段类型选择正确的值（如 UUID 字段用 `uuid.uuid4()`，datetime 字段用 `datetime.now()`）
> - ✅ 对于有验证器的字段（如 `slug`），遵循验证规则
> - ✅ 对于 unique=True 字段，使用唯一值（推荐：Faker 或 UUID）
>
> **通用模板（示例）**：
> ```python
> from app.models.experts import Expert  # 动态从读取的 Model 文件中导入
> from datetime import datetime
> from faker import Faker
>
> fake = Faker()
> # ✅ 正确：使用真实的 Model 类
> mock_expert = Expert(
>     id=uuid.uuid4(),  # UUID 类型字段
>     name=fake.name(),  # String 类型字段（使用 Faker 生成唯一值）
>     title=fake.job(),  # String 类型字段
>     hospital=fake.company(),  # String 类型字段
>     department=fake.word(),  # String 类型字段
>     expertise_areas=fake.text(max_nb_chars=200),  # Text 类型字段
>     bio=fake.text(max_nb_chars=500),  # Text 类型字段
>     avatar_url=fake.image_url(),  # String 类型字段（可选）
>     is_featured=True,  # Boolean 类型字段
>     sort_order=0,  # Integer 类型字段
>     contact_info={"phone": fake.phone_number()},  # JSONB 类型字段
>     created_at=datetime.now(),  # DateTime 类型字段
>     updated_at=datetime.now()   # DateTime 类型字段
> )
> ```
>
> **适用范围**：所有 Model 对象，特别是涉及到 `model_validate()` 的测试场景

**B. Schema 字段验证规则（动态读取验证器）**：

1. **核心原则**：
   - **必须**使用 `read_file()` 工具读取 Schema 代码，提取字段验证规则
   - **必须**根据实际验证规则生成有效的测试数据
   - **禁止**硬编码具体的验证规则（如 "slug 只能包含小写字母"）

2. **动态读取验证器的通用指导**：
> **步骤 1：读取 Schema 代码并提取验证规则**
> ```python
> **执行者指令**：
> 1. 使用 `read_file()` 工具读取 Schema 文件（如 `app/schemas/experts.py`）
> 2. 提取所有字段的验证器（如 `@field_validator`）
> 3. 记录验证规则（如正则表达式、长度限制、格式要求）
> ```

> **步骤 2：根据验证规则生成测试数据**
> ```python
> **基于 Schema 验证规则生成测试数据**：
>
> **禁止行为**：
> - ❌ 违反验证规则生成测试数据（如验证规则要求连字符却使用下划线）
> - ❌ 硬编码验证规则到母版中（母版不应包含具体的验证规则）
>
> **推荐行为**：
> - ✅ 严格遵循从 Schema 代码中提取的验证规则
> - ✅ 根据验证规则生成有效的测试数据
> - ✅ 对于正则表达式验证，生成符合模式的值
> - ✅ 对于长度限制，生成符合范围的值
>
> **通用模板（示例）**：
> ```python
> # 假设 Schema 中有以下验证规则（通过读取代码获取）：
> # class ExpertCreate(BaseModel):
> #     name: str = Field(..., min_length=1, max_length=120, description="专家姓名")
> #     title: Optional[str] = Field(None, max_length=120, description="职称")
> #     hospital: Optional[str] = Field(None, max_length=200, description="所属医院")
>
> # ✅ 正确：遵循验证规则
> expert_create = ExpertCreate(
>     name=fake.name()[:120],  # 符合 min_length=1, max_length=120（动态生成）
>     title=fake.job()[:120],  # 符合 max_length=120（可选字段）
>     hospital=fake.company()[:200],  # 符合 max_length=200（可选字段）
> )
>
> # ❌ 错误：违反验证规则
> expert_create = ExpertCreate(
>     name="",  # ❌ 违反 min_length=1
>     title="a" * 150,  # ❌ 违反 max_length=120
> )
> ```
>
> **关键要点**：
> - ✅ 动态读取：必须读取 Schema 代码，提取验证规则
> - ✅ 遵循规则：根据实际验证规则生成测试数据
> - ✅ 禁止硬编码：母版中不得硬编码具体的验证规则

**C. 响应结构断言规范（动态读取 Schema 定义）**：

1. **核心原则**：
   - **必须**使用 `read_file()` 工具读取 Schema 代码，提取响应结构定义
   - **必须**根据实际 Schema 定义构造断言
   - **禁止**硬编码具体的响应结构（如 `ExpertFollowResponse` 的具体字段）

2. **动态读取响应结构的通用指导**：
> **步骤 1：读取 Schema 代码并提取响应结构**
> ```python
> **执行者指令**：
> 1. 使用 `read_file()` 工具读取 Schema 文件（如 `app/schemas/experts.py`）
> 2. 提取响应 Schema 的字段定义
> 3. 识别嵌套结构（如 `data` 字典包含业务数据）
> 4. 记录顶层字段和嵌套字段
> ```

> **步骤 2：根据响应结构构造断言**
> ```python
> **基于 Schema 响应结构生成断言**：
>
> **禁止行为**：
> - ❌ 直接访问业务数据字段（如 `result["name"]`）如果 Schema 定义是嵌套结构
> - ❌ 硬编码具体的响应结构到母版中
> - ❌ 使用 Schema 中不存在的字段进行断言
>
> **推荐行为**：
> - ✅ 严格遵循从 Schema 代码中提取的响应结构
> - ✅ 对于嵌套结构，通过嵌套方式访问（如 `result["data"]["name"]`）
> - ✅ 断言所有标准字段（code, message, data, timestamp）
>
> **通用模板（示例）**：
> ```python
> # 假设 Schema 中有以下响应结构（通过读取代码获取）：
> # class ExpertFollowResponse(BaseModel):
> #     code: int = Field(200, description="状态码")
> #     message: str = Field("关注成功", description="响应消息")
> #     data: dict
> #     timestamp: datetime
>
> # ✅ 正确：遵循嵌套结构访问
> result = response.json()
> assert result["code"] == 200
> assert result["message"] == "关注成功"
> assert result["data"]["followed"] == True  # 通过 data 字典访问业务数据
> assert result["timestamp"] is not None
>
> # ❌ 错误：直接访问业务数据字段（如果 Schema 定义是嵌套结构）
> result = response.json()
> assert result["followed"] == True  # KeyError: 'followed'
> ```
>
> **关键要点**：
> - ✅ 动态读取：必须读取 Schema 代码，提取响应结构
> - ✅ 遵循结构：根据实际 Schema 定义构造断言
> - ✅ 禁止硬编码：母版中不得硬编码具体的响应结构

**D. 增量测试模式规范（自动读取依赖）**：

1. **自动发现依赖**：
> - **必须**使用 `read_file()` 工具读取 `app.crud`、`app.models`、`app.schemas`、`app.services`、`app.api` 等依赖文件
> - **必须**使用 `read_file()` 工具读取 `tests/conftest.py`，了解现有 Fixture 定义
> - **必须**使用 `read_file()` 工具读取已存在的测试文件，了解现有测试风格和命名约定

**E. 禁止行为**：

❌ **严禁**以下行为：

> 1. **严禁**在生成的测试提示词中硬编码具体的 Schema 定义或 Model 字段
> 2. **严禁**直接复制设计文档中的 Schema 定义到提示词中
> 3. **严禁**为每个具体模块硬编码详细的约束信息或验证规则
> 4. **严禁**使用 `MagicMock` 来模拟 Model 对象（强制使用真实 Model 类）
> 5. **严禁**硬编码具体的响应结构（如 `ExpertFollowResponse` 的具体字段）

**F. 推荐做法**：

> 通过抽象指导和示例来说明"方法论"，而不是"模板"
> 让 AI 在生成具体提示词文档时自行读取代码并提取关键信息
> 提供通用的指导原则和示例，而不是具体内容

**关键要点**：

 - ✅ 动态读取：必须使用 `read_file()` 工具读取实际代码，而非猜测或硬编码
 - ✅ 提取摘要：提示词文档包含"关键信息摘要"（字段、验证规则、响应结构），而非完整定义
 - ✅ 一致性保证：确保字段名、函数名、变量名、返回类型结构与实际代码完全一致
 - ✅ 通用性：保持母版的通用性和抽象性，适用于所有模块
 - ✅ 禁止硬编码：母版中不得硬编码任何具体 Model、Schema 或验证规则的完整定义

-----
    - **必须复用现有Fixture**：优先使用 `conftest.py` 中已定义的Fixture，避免重复创建。
    - **新增Fixture的位置**：
      - **优先**：在 `conftest.py` 中添加新Fixture（如果没有命名冲突或功能重复）。
      - **备选**：如果存在命名冲突或功能重复，在测试文件内部定义（使用 `@pytest.fixture`）。

##### 3.2. 结构化测试 (Arrange, Act, Assert)

  * 所有测试用例**必须**遵循"准备 (Arrange)"、"执行 (Act)"、"断言 (Assert)"的逻辑结构，并使用注释将其清晰分开。

##### 3.3. 严格的异步测试语法 (MANDATORY `async for` Syntax)

  * 所有使用异步 `pytest` Fixture (如 `db_session`, `async_client`) 的测试函数**必须**使用 `async for` 语法来解包。
  * **⚠️ 禁止的写法**：
      * 不得在 `async for` 外部调用数据库操作。
      * 不得直接使用 fixture 参数而不解包。
      * 不得在函数参数中添加类型提示（`db_session: AsyncSession`）。

<!-- end list -->

```python
# ✅ 正确的 CRUD 测试模板
@pytest.mark.asyncio
async def test_crud_operation(self, db_session):
    """测试描述"""
    async for db in db_session:
        # ...所有数据库逻辑必须在此块内...
        pass
```

# ✅ 正确的 API + DB 组合测试模板

@pytest.mark.asyncio
async def test\_api\_with\_db(self, async\_client, db\_session):
"""API 和数据库组合测试"""
async for client in async\_client:
async for db in db\_session:
\# ...所有组合测试逻辑必须在此嵌套块内...
pass

```` 
##### 3.4. 测试数据隔离 (MANDATORY for Pragmatic Tests)

  * **指令**: 为了防止测试之间因违反数据库 `UNIQUE` 约束而产生冲突，所有唯一性字段（如 `username`, `email`, `product_code`）**必须**是随机生成的。
  * **实现**: **必须**导入 `Faker` 和 `uuid` (以及 `random` 和 `string`，如果需要)。
  * **具体要求 (Faker 范例)**:
      * **`name`**: `fake.name()`
      * **`title`**: `fake.job()`
      * **`hospital`**: `fake.company()`
      * **`email`**: `fake.email()`
      * **`product_code`**: `f"PROD_{''.join(random.choices(string.ascii_uppercase + string.digits, k=10))}"`
  * **禁止硬编码**: **严禁**使用硬编码的字符串（如 `'usera'`）作为唯一性字段的值，除非测试目的就是为了验证重复性。

  * **🚨 [关键] API 层测试的用户身份一致性规范**：
      * **问题场景**：测试中创建数据时使用随机 UUID（如 `user_id_from_token = uuid.uuid4()`），但 API 请求时使用 JWT token 中的真实 user_id，导致两个 ID 不匹配，查询返回 0 条记录。
      * **禁止行为**：
          - ❌ **严禁**在 API 层测试中使用 `user_id_from_token = uuid.uuid4()` 创建数据
          - ❌ **严禁**将 UUID 对象用作 JWT token（如 `headers = {"Authorization": f"Bearer {user_id_from_token}"}`）
          - ❌ **严禁**使用不稳定的 JWT 解析方式（如 `regular_user_token.split("-")[0] + ...`）
      * **正确做法**：
          - ✅ **从 JWT token 解析真实 user_id**（用于创建数据）：
            ```python
            import jwt
            from app.core.config import settings

            # 从 JWT token 解析 user_id
            payload = jwt.decode(regular_user_token, settings.JWT_SECRET_KEY, algorithms=["HS256"])
            user_id_from_token = payload.get("user_id")
            user_id_from_token = uuid.UUID(user_id_from_token) if isinstance(user_id_from_token, str) else user_id_from_token
            ```
          - ✅ **在 headers 中使用原始 JWT token**（用于 API 请求）：
            ```python
            headers = {"Authorization": f"Bearer {regular_user_token}"}
            ```
          - ✅ **确保测试场景一致性**：创建数据时使用的 `user_id` 和请求数据时使用的 `user_id` 必须是同一个（都来自 JWT token）
      * **关键原则**：
          - 创建数据：从 JWT token 解析真实 `user_id`
          - API 请求：使用原始 JWT token 字符串
          - 避免随机：不要使用 `uuid.uuid4()` 作为 `user_id`，除非测试明确需要验证用户隔离

  * **🚨 [关键] 数据隔离与用户身份的一致性原则**：
      * **场景区分**：数据库隔离规范和 API 层测试的用户身份一致性规范针对不同的使用场景
      * **数据库隔离**（第389-397行）：
          - **适用场景**：防止测试之间因违反数据库 `UNIQUE` 约束而产生冲突
          - **应用对象**：唯一性字段（如 `name`, `email`, `product_code`）
          - **随机生成**：**必须**使用 Faker 或 UUID 生成唯一值（如 `fake.name()`, `fake.email()`）
      * **API 层测试的用户身份一致性**（第399-424行）：
          - **适用场景**：API 层测试中，创建数据和请求数据必须使用同一个 `user_id`（都来自 JWT token）
          - **应用对象**：`user_id` 字段（用于创建测试数据）
          - **避免随机**：不要使用 `uuid.uuid4()` 作为 `user_id`，除非测试明确需要验证用户隔离
      * **不冲突**：这两个规范**不冲突**，因为它们针对不同的字段和场景
          - 数据库隔离：唯一性字段（如 `name`）**必须**随机生成
          - 用户身份一致性：`user_id` 字段**必须**从 JWT token 解析，不随机生成
      * **例外场景**：
          - ✅ Service 层测试：可以使用 `uuid.uuid4()` 作为 `user_id`（因为不涉及 JWT token）
          - ✅ 用户隔离测试：可以明确说明"测试用户隔离"时使用随机 `user_id`

  * **🚨 [关键] API 层测试的 URL 参数匹配规范**：
      * **问题场景**：测试使用数据库主键（如 `favorite_id`）作为 URL 参数，但后端 API 使用其他字段（如 `room_id`），导致查询不到记录。
      * **禁止行为**：
          - ❌ **严禁**假设使用数据库主键（如 `favorite.id`）作为 URL 参数
          - ❌ **严禁**根据模型名称猜测 URL 参数（如"删除关注就用 favorite_id"）
      * **正确做法**：
          - ✅ **必须查看后端 API 定义**（如 `@router.delete("/{expert_id}/follow")`）
          - ✅ **使用与 Path 参数匹配的变量**（后端定义什么参数，测试就传什么）
          - ✅ **根据 API 端点选择正确的字段**：
            ```python
            # 示例：关注专家
            # 后端：@router.post("/{expert_id}/follow")
            # 测试：使用 expert_id
            response = await client.post(f"/api/v1/experts/{expert_id}/follow", headers=headers)
            
            # 示例：取消关注专家
            # 后端：@router.delete("/{expert_id}/follow")
            # 测试：使用 expert_id
            response = await client.delete(f"/api/v1/experts/{expert_id}/follow", headers=headers)
            ```
      * **关键原则**：
          - 查看后端 API 定义（`@router.delete("/{param}")`）
          - 使用匹配的参数（后端定义什么参数，测试就传什么）
          - 不要假设（不要假设使用数据库主键）

##### 3.5. [关键] 统一的用户身份解析规范 (MANDATORY - V5.1 修复)

**🚨（新增）API 层用户提取规范（JWT→UUID+Enum）**
**`current_user` 永远是一个 JWT payload `dict`，而不是 ORM `User` 模型**。

API 层必须使用以下模式解析：

```python
current_user: Dict = Depends(get_current_user)
````
user\_id = uuid.UUID(current\_user.get("user\_id") or current\_user.get("sub"))
role\_str = current_user.get("role", "REGULAR").upper()
user\_role = LiveRoomMessageUserRole.get(role\_str, LiveRoomMessageUserRole.REGULAR)

````
**🚨（新增）Service 层用户参数规范**
Service 层方法**不得**接受实体类 `User`，**必须**统一为：

```python
async def some_action(
    self,
    user_id: UUID,
    user_role: str,
    ...
):
````
-----

#### 4\. 按层级生成的断言规范 (Assertion Rules by Layer)

你必须根据"核心测试哲学"（第 2 节）生成对应的断言逻辑。

##### 4.1. 实用派测试 (Pragmatic: CRUD & API)

  * **A. API 响应验证 (针对 API 测试)**:

      * **必须**同时验证 HTTP 状态码和完整的 JSON 响应结构（`code`, `message`, `data`）。
      * **必须**验证业务状态码（`response.json()["code"]`）和 `"data"` 内部的关键字段。
      * 对于失败响应，**必须**验证业务错误码（例如 `assert response.json()["code"] == 2001`）。

  * **B. 数据库状态验证 [关键修改]**

      * **(针对 CRUD 层测试)**:

          * 在**同一个** `db_session` 中执行的 `create()`, `update()`, `remove()` 操作，**必须**在操作后立即使用 `await db.refresh(obj)` 或**再次 `get(db, obj.id)`**，来验证数据已正确持久化。
          * **[增量测试模式]**：对于涉及记录数量或列表查询的测试，**必须使用增量判断方式**（见 `3.6.2` 节）。在测试前记录基线状态（记录数、ID集合等），测试后只验证增量变化。
          * **[基于实际代码]**：**必须**根据实际模型的字段名来验证数据，**严禁**猜测字段名。

      * **(针对 API / Endpoint 层测试)**:

          * **[关键禁令]**：在 `await client.patch(...)` 或 `client.post(...)`（它们使用*独立会话*）之后，**严禁**使用*原始的* `db_session`（用于 Arrange 阶段）来 `get()` 或查询对象。这样做**必定**会读取到 SQLAlchemy 身份映射（Identity Map）中的**陈旧缓存**，导致测试失败。

      * **[API 层正确验证方案]**: 要验证 API 调用后的数据库*真实*状态，**必须**选择以下两种方法之一：

        1.  **(首选 - 验证响应)**: **只断言** API 返回的 `response.json()["data"]` 中的内容是否正确（例如 `assert response.json()["data"]["name"] == "新标题"`）。
        2.  **(次选 - 验证数据库)**: **必须**导入 `async_session_factory`（见 3.1 节），创建一个**全新的、独立的会话**（`async with async_session_factory() as new_db:`），并使用这个 `new_db` 会话执行显式的 `select()` 语句来查询数据库，以获取**最新**数据。
         - **[增量测试模式]**：如果涉及记录数量验证，**必须使用增量判断方式**。在API调用前记录基线状态，调用后验证增量变化。

##### 4.2. 学院派测试 (Academic: Service & I/O)

  * **A. 异常逻辑验证**:

      * **必须**使用 `with pytest.raises(TopicPermissionDeniedException):` 来验证在特定条件下是否正确抛出了**自定义业务异常**。

  * **B. 行为验证 (Mocks)**:

      * **必须**使用 `mock_crud.assert_called_once_with(...)` 来验证依赖是否被正确调用。
      * **[关键] Mock 路径规范 (MANDATORY)**:
          * 假设 Service 层使用别名导入 (`from app.crud import experts as crud_experts`)。
          * 测试代码在 `mocker.patch()` 中**必须**使用**原始模块路径** (`"app.crud.experts.create_expert"`)。
          * **必须**为异步函数使用 `new_callable=AsyncMock`。
      * **[关键] Mock 断言参数格式规范 (MANDATORY)**:
          * **断言参数必须与实际调用方式完全一致**：
            * 如果 Service 层调用时使用**位置参数**（如 `await crud_experts.create_expert(...)`），则断言必须使用**位置参数**。
            * 如果 Service 层调用时使用**关键字参数**（如 `await crud_experts.create_expert(db=db, expert_create=...)`），则断言可以使用**关键字参数**。
          * **⚠️ 常见错误**：使用关键字参数断言（`db=db`），但实际调用使用位置参数（`db`），会导致断言失败。
          * **正确做法**：**必须**先检查实际Service层代码的调用方式，然后使用相同格式的断言。

      * **示例**:
        ```python
        # Service 层 (app/services/expert_service.py)
        # from app.crud import experts as crud_experts
        # await crud_experts.create_expert(...)
        ```

````
    # 测试层 (tests/unit/test_service_experts.py)
    @pytest.mark.asyncio
    async def test_create_expert(mocker):
        # 1. 准备 (Arrange)
        # [关键] Mock 原始路径 "app.crud.experts.create_expert"
        mock_create = mocker.patch(
            "app.crud.experts.create_expert",
            new_callable=AsyncMock,
            return_value=mock_expert_object
        )
        # ...
        # 3. 断言 (Assert)
        mock_create.assert_called_once_with(...)
    ```
````

##### 4.3. [关键] 异步 SQLAlchemy 会话的测试规范 (V5.2 修复)

**🚨（新增）异步 SQLAlchemy Session 的禁止操作**
在所有测试中：

**严禁使用**：

  * `db.expire_all()`
  * `db.expire(obj)`

因为这会触发 lazy loading，并在异步环境中引发：

`sqlalchemy.exc.MissingGreenlet`

**🚨（新增）避免 Identity Map 缓存污染**
若测试创建了对象（会话 A），但 API 在另一会话（B）修改了该对象：

  * 会话 A 再查询该 ID → 会直接命中缓存，无法看到最新值

**⭐ 正确验证最新数据库值的两种模式**

  * **最推荐方式**：完全依赖 API 返回 JSON，测试不访问数据库（见 `4.1.A`）。
  * **若必须访问数据库**：
      * 必须使用新的独立会话（见 `4.1.B`）。
      * `async with async_session_factory() as new_db:`
      * `stmt = select(Model).where(Model.id == id)`
      * `result = await new_db.execute(stmt)`
      * `obj = result.scalar_one_or_none()`
  * **⚠️ 警告**：不能在**同一个**测试会话中二次查询同一主键，否则会命中身份映射缓存。

-----

#### 5\. 工程与风格规范 (Engineering & Style Norms)

  * **RESTful 路径规范 (MANDATORY)**:
      * 在 `APIRouter` 中定义 `prefix` 时，**严禁**在末尾添加斜杠 (例如：`prefix="/experts"`)。
      * 对于集合端点（`GET /experts`, `POST /experts`），**必须**在装饰器中使用 `path="/"`。

-----

#### 6\. [动态生成] 核心上下文与被测试代码 (Context & Code to Test)

**⚠️ 关键规则（MUST READ）**：
- **本节不包含完整的依赖代码**，仅列出依赖文件清单
- **生成测试代码时，必须主动读取以下依赖文件的完整内容**
- **必须根据实际代码的字段、方法、约束来生成测试，而不是猜测**
- **对于增量测试模式：必须读取最新代码，确保测试与当前代码一致**

### 6.1 依赖文件清单

**⚠️ 必须读取的文件（CRITICAL）**：
- `app/core/deps.py` - 读取依赖注入函数（如 `get_current_user`）
- `app/models/experts.py` - **必须**完整读取，包含所有模型类的完整定义（字段、约束、索引、关系）
- `app/schemas/experts.py` - **必须**完整读取，包含所有 Pydantic 模型定义
- `app/crud/experts.py` - **必须**完整读取，包含所有 CRUD 函数定义
- `app/services/expert_service.py` - **必须**完整读取，包含所有 Service 方法定义
- `app/api/v1/endpoints/experts.py` - **必须**完整读取，包含所有 API 端点定义
- `tests/conftest.py` - **必须**完整读取（增量模式下），了解现有 Fixture 定义

### 6.2 读取文件的规范

**⚠️ 必须使用工具读取文件**：
- 使用 `read_file()` 工具读取依赖文件
- **必须读取完整的文件内容**，不得只读取部分或摘要
- **必须**记录每个文件中：
  - 模型类的所有字段（包括字段名、类型、约束、注释）
  - 所有函数的签名（包括参数名、类型、默认值）
  - 所有端点的路径、方法、依赖注入
  - 所有现有 Fixture 的定义（增量模式下）

### 6.3 生成测试前的关键检查点

**在生成测试代码之前，必须检查**：

1. **模型字段检查**：
   - ✅ 确认模型中有哪些字段（如 `Expert.name`, `Expert.title`, `Expert.hospital`）
   - ✅ 确认字段的类型（如 `String(120)`, `Boolean`, `UUID`）
   - ✅ 确认字段的约束（如 `unique=True`, `nullable=False`）
   - ✅ 确认字段注释（如 `comment='专家姓名'`）
   - ❌ **严禁**猜测不存在的字段

2. **主键和外键检查**：
   - ✅ 确认主键字段（如 `id`）
   - ✅ 确认外键关系（如 `UserExpertSubscription.expert_id` 引用 `Expert.id`）
   - ✅ 确认复合主键（如 `LiveSessionExpert` 的 `(session_id, expert_id, role)`）
   - ❌ **严禁**假设有 `id` 字段（如果模型使用复合主键）

3. **约束检查**：
   - ✅ 确认唯一约束（如 `Expert.user_id` 的 `unique=True`）
   - ✅ 确认非空约束（如 `Expert.name` 的 `nullable=False`）
   - ✅ 确认其他约束（如 `ForeignKey` 的 `ondelete="CASCADE"`）
   - ✅ 在测试中必须**使用 Faker/UUID 生成唯一值**以满足唯一约束

4. **函数签名检查**：
   - ✅ 确认 CRUD 函数的参数（如 `create_expert(db, expert_create)`）
   - ✅ 确认 Service 函数的参数（如 `create_expert(self, expert_create, user_id, user_role)`）
   - ✅ 确认 API 端点的参数（如 `@router.post("/{expert_id}/follow")`）
   - ❌ **严禁**猜测参数名或类型

5. **Schema 检查**：
   - ✅ 确认 Pydantic 模型的字段（如 `ExpertCreate(name, title, hospital)`）
   - ✅ 确认哪些字段是必需的（如 `name` 是必需的）
   - ✅ 确认哪些字段是可选的（如 `title` 是可选的）
   - ❌ **严禁**在测试中传递 Schema 中不存在的字段

**示例（伪代码）**：
```python
# 在生成测试代码之前，必须先读取依赖文件
models_content = read_file("app/models/experts.py")
schemas_content = read_file("app/schemas/experts.py")
crud_content = read_file("app/crud/experts.py")
# ... 读取其他依赖文件 ...

# 然后基于实际代码生成测试
# 例如：从 Expert 模型中读取字段名和约束
# Expert.name 是 nullable=False，所以在测试中必须提供值
expert_name = fake.name()
```

### 6.4 增量测试模式的特殊要求

**⚠️ 增量测试模式必须额外检查**：
- **必须**读取 `tests/conftest.py`，了解现有 Fixture 定义
- **必须**读取已存在的测试文件（如 `tests/unit/test_crud_experts.py`、`tests/unit/test_service_experts.py`），了解现有测试风格
- **必须**确保新生成的测试与现有测试风格一致
- **必须**避免命名冲突（函数名、变量名、Fixture名等）

**示例**：
```python
# 读取现有 conftest.py
conftest = read_file("tests/conftest.py")

# 检查是否存在某个 Fixture
if "created_expert" in conftest:
    # 复用现有 Fixture
    pass
else:
    # 创建新的 Fixture（在测试文件内部或 conftest.py 中）
    pass
```

### 6.5 已存在的测试文件清单

**⚠️ 增量测试模式**：以下测试文件已存在，**严禁修改**已存在的测试函数：

- `tests/unit/test_crud_experts.py` - CRUD 层测试（已存在，只能补充，不能修改）
- `tests/unit/test_service_experts.py` - Service 层测试（已存在，只能补充，不能修改）

**需要新增的测试文件**：

- `tests/integration/test_api_experts.py` - API 层测试（需要生成）

-----

#### 7\. [动态生成] 具体测试任务列表 (Dynamic-Generated Specific Test Tasks)

**⚠️ 关键要求**：
- **必须**基于**实际读取的代码**生成测试，而不是猜测
- **必须**使用实际的模型字段名、函数签名、端点路径
- **必须**考虑实际的约束（唯一性、非空等）
- **严禁**使用硬编码的字段名或函数签名

**⚠️ 增量测试模式说明**：

**已存在的测试文件**：
- `tests/unit/test_crud_experts.py` - CRUD 层测试（已存在）
- `tests/unit/test_service_experts.py` - Service 层测试（已存在）

**需要新增的测试文件**：
- `tests/integration/test_api_experts.py` - API 层测试（**必须生成完整的新文件**）

**关键约束**：
- **严禁修改**已存在的测试文件中的任何测试函数
- **可以补充**新的测试函数到现有测试文件中（如果测试覆盖不完整）
- **必须生成完整**的 API 层测试文件

**你必须为 `Section 6` 中列出的依赖文件严格生成以下所有测试用例**：

> 
> **A. 针对 `app/crud/experts.py` (实用派 CRUD 测试) - [可选补充]**

>   * **说明**：`tests/unit/test_crud_experts.py` 已存在，可以根据实际测试覆盖情况补充缺失的测试。
>   * **禁止**：**严禁修改**已存在的测试函数，只能补充新的测试函数。
>   * **如果需要补充**，请检查以下函数是否有测试：
>     * `create_expert(db, expert_create)`
>     * `update_expert(db, expert_id, expert_update)`
>     * `delete_expert(db, expert_id)`
>     * `get_expert(db, expert_id)`
>     * `get_experts_multi(db, skip, limit, filters)`
>     * `get_experts_multi_and_total(db, skip, limit, filters)`
>     * `get_featured_experts(db)`
>     * `create_expert_follow(db, user_id, expert_id)`
>     * `delete_expert_follow(db, user_id, expert_id)`
>     * `is_expert_followed(db, user_id, expert_id)`
>     * `get_followed_experts_multi(db, user_id, skip, limit)`
>     * `get_followed_experts_multi_and_total(db, user_id, skip, limit)`
>     * `get_followed_experts_with_live_status(db, user_id, expert_ids)`
>     * `set_session_experts(db, session_id, expert_items)`
>     * `get_session_experts(db, session_id)`
>
> **B. 针对 `app/services/expert_service.py` (学院派 Service 测试) - [可选补充]**

>   * **说明**：`tests/unit/test_service_experts.py` 已存在，可以根据实际测试覆盖情况补充缺失的测试。
>   * **禁止**：**严禁修改**已存在的测试函数，只能补充新的测试函数。
>   * **如果需要补充**，请检查以下函数是否有测试：
>     * `create_expert(expert_create, user_id, user_role)` - 权限检查（仅管理员）
>     * `update_expert(expert_id, expert_update, user_id, user_role)` - 权限检查
>     * `delete_expert(expert_id, user_id, user_role)` - 权限检查
>     * `get_expert_detail(expert_id, user_id, user_role)` - 公开接口
>     * `get_expert_with_sessions(expert_id, user_id, user_role)` - 公开接口
>     * `get_experts_multi(page, size, is_featured, user_id, user_role)` - 分页
>     * `get_featured_experts()` - 公开接口
>     * `set_expert_featured(expert_id, is_featured, user_id, user_role)` - 权限检查
>     * `follow_expert(expert_id, user_id)` - 需要登录
>     * `unfollow_expert(expert_id, user_id)` - 需要登录
>     * `is_expert_followed(expert_id, user_id)` - 需要登录
>     * `get_followed_experts_multi(user_id, page, size, include_live_status)` - 需要登录
>     * `get_followed_experts_with_live_status_batch(user_id, expert_ids)` - 批量查询
>     * `set_session_experts(session_id, expert_items, user_id, user_role)` - 权限检查
>     * `get_session_experts(session_id)` - 公开接口
>
> **C. 针对 `app/api/v1/endpoints/experts.py` (实用派 API 测试) - [必须生成]**

>   * **说明**：`tests/integration/test_api_experts.py` **不存在**，必须生成完整的测试文件。
>   * **必须包含以下所有测试用例**：
>
> **公开路由（`router`）**：
>
>   * **`@router.get("/")` (get_experts)**:
>       * (成功) `test_api_get_experts_success`: 获取专家列表（分页），验证 `response.status_code == 200` 和分页数据。
>       * (成功) `test_api_get_experts_with_filter_featured`: 获取推荐专家列表（`is_featured=True`），验证过滤功能。
>       * (成功) `test_api_get_experts_pagination`: 验证分页功能（`page=2`, `size=10`）。
>
>   * **`@router.get("/featured")` (get_featured_experts)**:
>       * (成功) `test_api_get_featured_experts_success`: 获取首页推荐专家列表，验证 `response.status_code == 200` 和专家列表。
>
>   * **`@router.get("/{expert_id}")` (get_expert_detail)**:
>       * (成功) `test_api_get_expert_detail_success`: 获取专家详情，验证 `response.status_code == 200` 和专家信息。
>       * (失败 - 未找到) `test_api_get_expert_detail_not_found`: 传入不存在的 `expert_id`，验证 `response.status_code == 404`。
>
>   * **`@router.get("/{expert_id}/sessions")` (get_expert_with_sessions)**:
>       * (成功) `test_api_get_expert_with_sessions_success`: 获取专家及场次信息，验证 `response.status_code == 200` 和场次列表。
>       * (失败 - 未找到) `test_api_get_expert_with_sessions_not_found`: 传入不存在的 `expert_id`，验证 `response.status_code == 404`。
>
>   * **`@router.post("/{expert_id}/follow")` (follow_expert)**:
>       * (成功) `test_api_follow_expert_success`: 关注专家，验证 `response.status_code == 200` 和 `data["followed"] == True`。
>       * (失败 - 未认证) `test_api_follow_expert_unauthorized`: 不带 JWT token，验证 `response.status_code == 401`。
>       * (失败 - 专家不存在) `test_api_follow_expert_not_found`: 传入不存在的 `expert_id`，验证 `response.status_code == 404`。
>
>   * **`@router.delete("/{expert_id}/follow")` (unfollow_expert)**:
>       * (成功) `test_api_unfollow_expert_success`: 取消关注专家，验证 `response.status_code == 200` 和 `data["unfollowed"] == True`。
>       * (失败 - 未认证) `test_api_unfollow_expert_unauthorized`: 不带 JWT token，验证 `response.status_code == 401`。
>       * (失败 - 专家不存在) `test_api_unfollow_expert_not_found`: 传入不存在的 `expert_id`，验证 `response.status_code == 404`。
>
>   * **`@router.get("/followed")` (get_followed_experts)**:
>       * (成功) `test_api_get_followed_experts_success`: 获取关注的专家列表，验证 `response.status_code == 200` 和分页数据。
>       * (失败 - 未认证) `test_api_get_followed_experts_unauthorized`: 不带 JWT token，验证 `response.status_code == 401`。
>
> **管理员路由（`admin_router`）**：
>
>   * **`@admin_router.post("/")` (create_expert)**:
>       * (成功) `test_api_admin_create_expert_success`: 管理员创建专家，验证 `response.status_code == 200` 和专家信息。
>       * (失败 - 权限) `test_api_admin_create_expert_permission_denied`: 普通用户尝试创建专家，验证 `response.status_code == 403`。
>
>   * **`@admin_router.patch("/{expert_id}")` (update_expert)**:
>       * (成功) `test_api_admin_update_expert_success`: 管理员更新专家，验证 `response.status_code == 200` 和更新后的专家信息。
>       * (失败 - 未找到) `test_api_admin_update_expert_not_found`: 更新不存在的专家，验证 `response.status_code == 404`。
>       * (失败 - 权限) `test_api_admin_update_expert_permission_denied`: 普通用户尝试更新专家，验证 `response.status_code == 403`。
>
>   * **`@admin_router.delete("/{expert_id}")` (delete_expert)**:
>       * (成功) `test_api_admin_delete_expert_success`: 管理员删除专家，验证 `response.status_code == 200` 和 `data["deleted"] == True`。
>       * (失败 - 未找到) `test_api_admin_delete_expert_not_found`: 删除不存在的专家，验证 `response.status_code == 404`。
>       * (失败 - 权限) `test_api_admin_delete_expert_permission_denied`: 普通用户尝试删除专家，验证 `response.status_code == 403`。
>
> **API 测试的额外要求**：
> - **必须使用 `async_client` 和 `db_session`** 进行测试
> - **必须遵循 `3.4` 节的规范**：
>   - 从 JWT token 解析 `user_id`（用于创建数据）
>   - 在 headers 中使用原始 JWT token（用于 API 请求）
>   - 使用与后端 API 定义匹配的 URL 参数（如 `/{expert_id}/follow`）
> - **必须遵循 `3.6.2` 节的增量验证规范**：
>   - 对于涉及记录数量或列表查询的测试，记录测试前的基线状态
>   - 测试后只验证增量变化
> - **必须验证 API 响应结构**：
>   - 验证 HTTP 状态码（200, 403, 404, 401, 500）
>   - 验证响应 JSON 中的 `code`、`message`、`data` 字段
>   - 根据 Schema 定义验证业务数据的字段（如 `name`, `title`, `hospital`）

-----

#### 8\. 交付物 (Deliverable - 执行者)

请根据**以上所有规范 (1-7)**，为 `Section 6` 中列出的依赖文件生成**完整的 `pytest` 测试文件**。

  * **交付物**:
      - `tests/integration/test_api_experts.py` (如果提供了 `app/api/v1/endpoints/experts.py`) - **必须生成完整的新文件**
      - `tests/unit/test_crud_experts.py` (可选补充) - **只能在现有基础上补充，不能修改已存在的测试**
      - `tests/unit/test_service_experts.py` (可选补充) - **只能在现有基础上补充，不能修改已存在的测试**
  * **指令**:
    1. **读取依赖文件**: 在生成测试之前，**必须**使用 `read_file()` 工具读取 `Section 6` 中列出的所有依赖文件的完整内容。
    2. **检查模型字段**: **必须**根据实际模型的字段名、类型、约束来生成测试数据，**严禁**猜测。
    3. **检查函数签名**: **必须**根据实际CRUD、Service、API函数的签名来生成测试，**严禁**猜测参数名或类型。
    4. **遵循任务列表**: 严格按照 `Section 7` 中的列表生成所有指定的测试用例。
    5. **遵循所有规范**: 确保生成的代码 100% 遵循 `Section 2-5` (哲学, 语法, 断言, 风格)。
    6. **[增量测试模式]**：
       - **严禁修改**已存在的测试函数（即使发现错误）。
       - **允许修改 `conftest.py`**（仅限添加新内容）：
         - ✅ **只增加**新的Fixture、函数、变量、导入等。
         - ❌ **严禁修改**已有的Fixture、函数、变量等。
         - ❌ **严禁删除**任何现有内容。
         - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
       - **必须使用增量判断方式**验证数据库操作（见 `3.6.2` 节）。
       - 新测试函数必须遵循现有测试的命名约定和代码风格。

**重要说明**：
- **用户角色参数**: 所有Service层方法签名使用 `user_role: str`（字符串），**不使用枚举**。
- **JWT Payload**: API层从JWT中提取的 `role` 字段是字符串，无需转换。
- **权限检查**: 使用字符串比较：`if user_role not in ['ADMIN', 'SUPERADMIN']:`
- **API层提取**: `user_role = current_user.get("role", "REGULAR").upper()`
- **🚨 最重要**: **必须**基于实际读取的代码生成测试，**严禁**猜测任何字段、函数或参数！

**[你生成的 `.md` 文件的内容应在这里结束]**

