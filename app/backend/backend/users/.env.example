# ============================================================================
# 用户服务环境变量配置模板
# ============================================================================
# 说明：
# 1. 将此文件复制为 .env 用于本地开发
# 2. 生产环境请创建独立的 .env.production 文件
# 3. 所有密钥类变量必须在生产环境中使用强密钥（长度>=32字符）
# 4. CORS配置在生产环境中不允许使用通配符"*"
# ============================================================================

# ============================================================================
# 环境配置
# ============================================================================
# 运行环境: development | production
ENVIRONMENT=development

# ============================================================================
# 数据库配置
# ============================================================================
# PostgreSQL 数据库配置（必填）
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=users_service_test
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ============================================================================
# JWT 认证配置
# ============================================================================
# JWT 密钥（必填，生产环境必须>=32字符）
# 生成命令: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_min_32_chars_use_openssl_rand_hex_32
JWT_ALGORITHM=HS256
# JWT 时钟偏差容忍秒数（默认 60）
JWT_LEEWAY_SECONDS=60

# ========== Redis 直连（session revoke / 跨服务标记共用）==========
REDIS_URL=redis://redis:6379/0

# ========== 内部服务间调用（与 live_core 分享相同值）==========
INTERNAL_SERVICE_TOKEN=

# ========== LiveCore 服务地址（注销清理等内部调用）==========
LIVE_CORE_SERVICE_URL=http://live_core_service:8000

# ============================================================================
# Authing SSO 配置
# ============================================================================
# Authing 应用配置（如需使用SSO登录，必须配置）
VITE_CLIENT_ID=your_authing_client_id_here
USER_POOL_SECRET=your_authing_pool_secret_here
APP_HOST=https://your-app-host.authing.cn
REDIRECT_URL=http://localhost:5174/callback
ISSUER=https://your-app-host.authing.cn/oidc

# ============================================================================
# CORS 配置
# ============================================================================
# 允许的前端域名列表（逗号分隔，生产环境不允许使用"*"）
# 开发环境示例:
CORS_ALLOWED_ORIGINS=http://localhost:5174,http://127.0.0.1:5174,http://localhost:5173,http://127.0.0.1:5173

# 生产环境示例（取消注释并修改为实际域名）:
# CORS_ALLOWED_ORIGINS=https://mp.dayilive.com,https://www.mp.dayilive.com

# ============================================================================
# Redis 配置
# ============================================================================
# Redis 连接配置（用于验证码、黑名单等）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ============================================================================
# 调试配置
# ============================================================================
# 是否启用调试模式
DEBUG=true

# ============================================================================
# API 配置
# ============================================================================
# API 路由前缀
API_V1_STR=/api/v1

# ============================================================================
# 密钥生成建议（仅供参考，请勿使用）
# ============================================================================
# 强密钥生成命令：
#   JWT密钥:         openssl rand -hex 32
#   通用密钥:        openssl rand -base64 32
#   PostgreSQL密码:  openssl rand -base64 24
#
# 注意：生成后请妥善保管密钥，不要提交到版本控制系统
# ============================================================================
