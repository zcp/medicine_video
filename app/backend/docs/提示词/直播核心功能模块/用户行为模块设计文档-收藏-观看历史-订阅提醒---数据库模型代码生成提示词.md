# 用户行为模块数据库模型代码生成提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**基于设计文档**: 直播核心功能设计文档_v6_用户行为模块设计文档-收藏-观看历史-订阅提醒.md  
**目标文件**: `backend/live_core_service/app/models/user_behavior.py`

---

## 1. 角色定义 (Role Definition)

你是一名精通 SQLAlchemy 2.0（异步）和 FastAPI 的资深 Python 后端工程师。你的任务是为 `LiveCore Service` 项目创建**用户行为模块**的数据库模型代码，确保与现有代码风格、架构和规范完全一致。

**增量开发模式**（重要）：
- 本模块是对 `live_core_service` 的增量扩展，必须遵守现有工程结构和规范。
- 你可以**新建/重建** `user_behavior.py` 文件；如果旧文件已被删除，请根据设计文档完整生成新的实现。
- 你可以在 `models/__init__.py` 中**追加导入**，但不能破坏现有模块导入。
- 不允许修改其他已有模型文件（如 `live_core.py`、`content_management.py` 等）。

---

## 2. 任务目标 (Task Objective)

生成或完善 `backend/live_core_service/app/models/user_behavior.py` 文件，包含用户行为模块所需的三个核心表对应的 SQLAlchemy 模型类：

1. `UserFavorite` → `user_favorites`（用户收藏直播间关联表）  
2. `WatchHistory` → `watch_history`（用户观看历史表）  
3. `UserSubscription` → `user_subscriptions`（用户订阅提醒表）  

生成的模型必须满足：
- 与设计文档中的 DDL 定义完全一致（字段、类型、约束、索引、外键）。
- 与 v6 主文档的跨服务 user_id/public_id 约定一致：`user_id` 存储 `users.public_id`（UUID），在应用层验证。
- 使用异步 SQLAlchemy 2.0 风格，继承 `app.database.Base`。

---

## 3. 核心上下文信息 (Core Context)

### 3.1 项目结构

模型文件应放在以下结构中：

```text
backend/live_core_service/
├── app/
│   ├── database.py          # 定义 Base, AsyncEngine, AsyncSession
│   ├── models/
│   │   ├── __init__.py
│   │   ├── live_core.py
│   │   ├── content_management.py
│   │   └── user_behavior.py   # ← 本次要生成的目标文件
```

要求：
- `user_behavior.py` 中的模型类需在 `models/__init__.py` 中导出，以便初始化脚本自动创建表。
- 不要在本文件中创建引擎或会话，只定义模型。

### 3.2 设计文档中的数据库表 (Section 2)

根据设计文档 `Section 2` 的 DDL 定义，本模块包含三张表：

#### 3.2.1 `user_favorites`（用户收藏表）

DDL 摘要（来自设计文档）：

```sql
CREATE TABLE user_favorites (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    room_id UUID NOT NULL,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT uq_user_favorites_user_room UNIQUE(user_id, room_id),
    CONSTRAINT fk_user_favorites_room_id 
        FOREIGN KEY (room_id) REFERENCES live_rooms(id) ON DELETE CASCADE
);

CREATE INDEX idx_user_favorites_user_id ON user_favorites(user_id);
CREATE INDEX idx_user_favorites_room_id ON user_favorites(room_id);
CREATE INDEX idx_user_favorites_user_active ON user_favorites(user_id, is_active)
WHERE is_active = true;

COMMENT ON TABLE user_favorites IS '用户收藏直播间关联表';
COMMENT ON COLUMN user_favorites.user_id IS '用户公开ID（users.public_id），应用层验证，JWT Token的user_id字段值';
COMMENT ON COLUMN user_favorites.is_active IS '是否有效：true=已收藏，false=已取消（软删除）';
```

模型要求：
- 使用 `UUID(as_uuid=True)` 存储 `id/user_id/room_id`。
- `user_id` 不加数据库外键，只加注释，含义为 `users.public_id`。
- `room_id` 使用命名外键约束 `fk_user_favorites_room_id` 指向 `live_rooms.id`，`ON DELETE CASCADE`。
- 使用 `UniqueConstraint` 和 `Index` 实现上述约束与索引。
- 使用软删除语义：`is_active=False` 视为取消收藏。

#### 3.2.2 `watch_history`（观看历史表）

（此处在真实生成时，必须从设计文档中完整提取 DDL；提示词中只给摘要要求）

关键字段与要求（根据设计文档 Section 2.2）：
- `id UUID PRIMARY KEY`
- `user_id UUID NOT NULL` → `users.public_id`（跨服务应用层验证）
- `session_id UUID NOT NULL` → 外键 `live_sessions.id ON DELETE CASCADE`
- `progress INT`：观看进度（秒）
- `watched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`
- `is_latest BOOLEAN DEFAULT true`：是否为该用户该场次的最新观看记录
- 必要索引：如 `idx_watch_history_user_session`、`idx_watch_history_is_latest`（参照设计文档）

模型要求：
- 使用命名外键约束将 `session_id` 关联到 `live_sessions.id`。
- 按设计文档中的索引命名创建 `Index`。
- 通过注释或 docstring 说明 `is_latest` 的语义（进度跟踪）。

#### 3.2.3 `user_subscriptions`（订阅提醒表）

关键字段与要求（根据设计文档 Section 2.3）：
- `id UUID PRIMARY KEY`
- `user_id UUID NOT NULL` → `users.public_id`（跨服务应用层验证）
- `target_type`：订阅目标类型，枚举值 `room` / `session`（在模型层使用 Python Enum + SAEnum）
- `room_id UUID NULL`：当 `target_type='room'` 时使用（可为 NULL）
- `session_id UUID NULL`：当 `target_type='session'` 时使用（可为 NULL）
- `is_active BOOLEAN DEFAULT true`：是否仍然订阅（软删除）
- `created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`
- 唯一约束：同一用户对同一目标的订阅唯一（具体组合字段参照设计文档，例如 `(user_id, target_type, room_id)` / `(user_id, target_type, session_id)`）。

模型要求：
- 定义 `SubscriptionTargetType(str, enum.Enum)`，并在模型中使用 `SAEnum(SubscriptionTargetType, name="subscription_target_type")` 存储。
- 正确表达联合唯一约束（可能需要两个 UniqueConstraint，或根据设计文档选择一个复合键方案）。
- 对 `room_id`、`session_id` 根据设计文档添加必要外键（如果有），否则仅通过注释说明。

---

## 4. 代码生成要求 (Implementation Requirements)

请根据上述设计文档摘要，为 `user_behavior.py` 生成完整的 SQLAlchemy 模型代码，遵循以下规则：

1. **基础设置**：
   - `from app.database import Base`
   - 统一使用 `UUID(as_uuid=True)` 存储 UUID 字段。
   - 时间字段使用 `TIMESTAMP(timezone=True)` 或 `TIMESTAMPTZ`，并设置 `server_default=func.now()`。

2. **命名规范**：
   - 表名使用设计文档中的小写下划线命名：`__tablename__ = "user_favorites"` 等。
   - 约束和索引名称与 DDL 保持一致（如 `uq_user_favorites_user_room`）。

3. **注释和 docstring**：
   - 每个模型类需要有简短 docstring，说明业务含义。
   - 关键字段的 `comment` 参数要与设计文档中的注释一致，尤其是 `user_id` 的跨服务说明、软删除字段等。

4. **关系 (relationship)**：
   - 可以为 `UserFavorite.room`、`WatchHistory.session` 等添加 `relationship`，但要遵循现有 `live_core.py` 中的关系定义风格；
   - 如果关系在当前阶段不是必须，可以先保守实现（仅外键，不强制关系）。

5. **兼容现有代码风格**：
   - 参考 `content_management.py`、`live_core.py` 的写法，保持一致的导入顺序、缩进和类型注释风格。

---

## 5. 输出要求

最终生成的 `user_behavior.py` 文件应满足：

- 包含 `SubscriptionTargetType`、`UserFavorite`、`WatchHistory`、`UserSubscription` 四个定义。
- 字段、约束、索引与设计文档 DDL 完全一致。
- 不包含业务逻辑（仅模型定义）。
- 通过项目现有的类型检查与 linter（如有）。

请直接输出完整的 `user_behavior.py` 模型代码实现。以此提示词为唯一规范来源，不要自行发明字段或约束。*** End Patch*** *** Add File: C:\Users\is99z\PycharmProjects\live-streaming-saas\docs\提示词\直播核心功能模块\用户行为模块设计文档-收藏-观看历史-订阅提醒---Pydantic模型代码生成提示词.md
# 用户行为模块 Pydantic 模型代码生成提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**基于设计文档**: 直播核心功能设计文档_v6_用户行为模块设计文档-收藏-观看历史-订阅提醒.md  
**目标文件**: `backend/live_core_service/app/schemas/user_behavior.py`

---

## 1. 角色定义

你是一名精通 Pydantic v2 和 FastAPI 的资深后端工程师。你的任务是为 `LiveCore Service` 的**用户行为模块**生成一组 Pydantic Schema，用于：

- API 入参（Create/Update 请求体）；
- API 出参（单项和列表响应）；
- 统一响应结构中的业务数据部分。

这些 Schema 必须与设计文档和即将生成的 `app/models/user_behavior.py` 模型保持字段和约束上的一致。  

---

## 2. 任务目标

在 `backend/live_core_service/app/schemas/user_behavior.py` 中生成以下 Schema：

1. 收藏相关：
   - `FavoriteCreate`：创建收藏请求体（例如收藏房间）。
   - `FavoriteItem`：单条收藏记录（用于列表返回）。
   - `FavoriteListResponse`：收藏列表分页/集合响应。

2. 观看历史相关：
   - `WatchEventRequest`：记录观看事件的请求体（包含 session_id、progress 等）。
   - `WatchHistoryItem`：单条观看历史记录。
   - `WatchHistoryListResponse`：观看历史列表分页/集合响应。

3. 订阅提醒相关：
   - `SubscriptionCreate`：创建订阅请求体（target_type + room_id/session_id）。
   - `SubscriptionItem`：单条订阅记录。
   - `SubscriptionListResponse`：订阅列表响应。

以及必要的枚举和基础类型：
- `SubscriptionTargetType`：与模型层枚举保持一致（`room` / `session`）。

---

## 3. 核心约束 (根据设计文档摘要)

### 3.1 用户收藏（favorites）

根据 `user_favorites` 表设计：

- `user_id`：在 API 层**不作为显式字段**，从 JWT 的 `user_id` 中提取，不出现在请求体中。
- `room_id`：必填，UUID，指向 `live_rooms.id`。
- `is_active`：在 API 入参中通常不暴露，由后端控制；在出参中可以展示当前收藏状态。
- `created_at`：只读字段，出参中展示。

由此推导 Schema：

- `FavoriteCreate`：
  - 字段：`room_id: UUID`
  - 不包含 `user_id`、`is_active`、`created_at`（后端填充）。

- `FavoriteItem`：
  - 字段：`id: UUID`, `room_id: UUID`, `is_active: bool`, `created_at: datetime`

- `FavoriteListResponse`：
  - 参考全局分页/列表响应规范，可以是：
    - 简单列表：`items: List[FavoriteItem]`
    - 或带分页：`total, page, size, items`（根据你项目统一的 ListResponse 模式选择，建议与其它模块一致）。

### 3.2 观看历史（watch_history）

根据设计文档 `watch_history` 设计要点：

- 仅关联 `session_id`，不直接暴露 `room_id`。  
- 记录字段包括：`progress`（观看进度，秒）、`watched_at`（时间戳）、`is_latest`（是否最新记录）。
- `user_id` 同样由 JWT 提取，不出现在请求体中。

Schema 约束：

- `WatchEventRequest`：
  - 字段：
    - `session_id: UUID`（必填）
    - `progress: int`（可选或必填，>=0）
  - 不包含 `user_id`、`watched_at`、`is_latest`（后端自动维护）。

- `WatchHistoryItem`：
  - 字段示例：`id, session_id, progress, watched_at, is_latest`。

- `WatchHistoryListResponse`：
  - 与收藏列表类似，可采用统一的分页/列表结构。

### 3.3 订阅提醒（user_subscriptions）

设计要点：

- `target_type`：订阅目标类型，`room` 或 `session`。
- `room_id` 和 `session_id` 至少一个不为 NULL，取决于 `target_type`。
- `is_active`：软删除标志。

Schema 约束：

- `SubscriptionCreate`：
  - 字段：
    - `target_type: SubscriptionTargetType`（必填）
    - `room_id: Optional[UUID]`（当 target_type 为 room 时必填）
    - `session_id: Optional[UUID]`（当 target_type 为 session 时必填）
  - 需要在 Pydantic 层做简单校验：根据 `target_type` 检查对应字段非空。

- `SubscriptionItem`：
  - 字段：`id, target_type, room_id, session_id, is_active, created_at` 等。

- `SubscriptionListResponse`：
  - 同样使用统一的列表/分页结构。

---

## 4. 实现规范

请在 `user_behavior.py` Schema 文件中遵循以下规范：

1. 使用 Pydantic v2 风格：
   - `from pydantic import BaseModel, Field`
   - 使用 `ConfigDict(from_attributes=True)` 支持 ORM 模式。

2. 枚举与模型对齐：
   - Schema 层的 `SubscriptionTargetType` 必须与模型层同名枚举保持值一致。

3. 字段约束：
   - 使用 `Field(..., description="...")` 描述关键字段含义（如 `progress`, `target_type`）。
   - 对需要非负的整型字段（如 `progress`）使用 `ge=0` 约束。

4. 列表/分页响应：
   - 推荐复用项目中已有的通用分页结构（如 `PaginatedData`），如果有的话；
   - 否则，在本模块内定义简单的 `total/page/size/items` 结构，并与其它模块保持一致。

5. 只读/写入字段区分：
   - Create 请求体不包含只读字段（如 `id`, `created_at`, `is_active`）。
   - 出参 Item 模型包含这些只读字段，方便前端展示。

---

## 5. 输出要求

最终输出应是完整的 `schemas/user_behavior.py` 文件内容，包含上述所有 Schema 定义，且：

- 字段名、类型与数据库模型和设计文档一致；
- 不包含任何业务逻辑，只做数据结构定义和轻量级验证；
- 可以被 `schemas/__init__.py` 导入使用。*** End Patch*** ** *** End Patch***}"""
