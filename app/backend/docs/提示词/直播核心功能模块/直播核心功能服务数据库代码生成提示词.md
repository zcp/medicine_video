
-----

### **高效 AI 提示词：生成数据库初始化脚本**

#### **1. 角色定义 (Role Definition)**

你是一名精通 SQLAlchemy 和项目脚手架的资深 Python 后端工程师。你的任务是为 `LiveCore Service` 项目创建一个用于初始化数据库的独立脚本。

#### **2. 任务目标 (Task Objective)**

生成一个名为 `create_tables.py` 的 Python 脚本，该脚本位于项目的 `live_core_service/scripts/` 目录下。当执行这个脚本时，它会连接到数据库，并根据 `live_core_service/app/models/live_core.py` 中定义的 SQLAlchemy 模型，自动创建所有对应的数据库表。

#### **3. 核心上下文信息 (Core Context Information)**

这是成功生成代码所必需的背景信息：

**3.1. 项目文件结构 (Project File Structure)**
你的脚本必须基于以下项目结构来正确导入模块：

```
live_core_service/
├── app/
│   ├── __init__.py
│   ├── database.py         # <-- 关键：定义了 SQLAlchemy 的 `engine`
│   ├── models/
│   │   ├── __init__.py
│   │   └── live_core.py    # <-- 关键：定义了所有模型和 `Base`
│   └── scripts/
        └── create_tables.py    # <-- 这是你要生成的目标文件
```

**3.2. 关键文件内容假设 (Key File Content Assumptions)**

  * **`live_core_service/app/database.py`**: 这个文件包含一个已经配置好的 SQLAlchemy `engine` 实例。AI 在生成代码时，应假设可以从这里导入 `engine`，即 `from app.database import engine`。
  * **`live_core_service/app/models/live_core.py`**:
      * 这个文件包含一个声明式基类：`Base = declarative_base()`。
      * 所有的模型（如 `LiveRoom`, `LiveSession`, `SessionStatistics`）都继承自这个 `Base`。
      * **非常重要**：为了让 `Base.metadata` 能够“发现”所有的模型，脚本中必须确保这些模型类所在的模块被导入。

**3.3. 运行环境说明**
这个脚本将从项目的根目录 (`live_core_service/`) 执行。因此，AI 生成的导入语句（如 `from app.database import engine`）是有效的。

#### **4. 代码生成具体要求 (Specific Code Generation Requirements)**

1.  **文件名与位置**: 生成的代码是用于 `live_core_service/scripts/create_tables.py` 文件的。
2.  **核心逻辑**: 必须使用 `Base.metadata.create_all(bind=engine)` 来创建表。
3.  **模块导入**:
      * 必须从 `app.database` 导入 `engine` 对象。
      * 必须从 `app.models.live_core` 导入 `Base`。
      * **必须显式导入所有模型类** (`LiveRoom`, `LiveSession`, `SessionStatistics`)，以确保 SQLAlchemy 的元数据能够注册它们。这是一个关键步骤，否则 `create_all` 可能不会创建任何表。
4.  **健壮性**:
      * 使用标准的 `logging` 模块来输出操作信息（如“开始创建...”、“创建成功”、“创建失败”）。
      * 将 `create_all` 调用包裹在 `try...except` 块中，以捕获并报告任何数据库连接或创建过程中的异常。
5.  **可执行性**:
      * 将核心逻辑封装在一个函数内，例如 `init_db()`。
      * 使用 `if __name__ == "__main__"` 块来调用这个函数，使脚本可以直接通过 `python scripts/create_tables.py` 命令执行。

#### **5. 最终交付 (Final Deliverable)**

请根据以上所有要求，为 `live_core_service/scripts/create_tables.py` 文件生成完整、健壮且可直接运行的 Python 代码。

-----

### **为什么这个提示词会有效？**

  * **提供了精确的上下文**: 它明确了项目结构、关键文件名和它们的内容，AI 不需要猜测 `engine` 和 `Base` 来自哪里。
  * **指出了关键陷阱**: 它特别强调了“必须显式导入所有模型类”这一点。这是 SQLAlchemy 初学者最常遇到的问题。如果不导入模型类，`Base.metadata` 的 `tables` 集合就是空的，`create_all` 也就无事可做。
  * **要求明确且专业**: 它要求使用 `logging`、`try/except` 和 `if __name__ == "__main__"`，这些都是编写高质量、可维护脚本的最佳实践。
  * **目标清晰**: 任务目标直接说明了要生成哪个文件以及它的功能，没有任何歧义。

