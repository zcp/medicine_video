# 专家模块设计文档-专家信息-专家关注 问题修复报告

**生成日期**: 2026-01-18  
**修复范围**: 一致性检测报告中发现的问题1和问题2

---

## 问题1修复：API端点补充说明

### 问题描述

提示词文档中包含设计文档未明确提及的2个API端点：
- `GET /api/v1/experts/{expert_id}` - 获取专家详情
- `GET /api/v1/experts/{expert_id}/is-followed` - 检查是否已关注

### 修复方案

**决策**：在设计文档中补充这两个API接口的说明（符合RESTful最佳实践）

### 修复内容

#### 1. 在设计文档中添加 `GET /api/v1/experts/{expert_id}` 接口

**位置**：`docs/03_系统设计/直播核心功能设计文档_v6_专家模块设计文档-专家信息-专家关注.md`  
**插入位置**：在 `4.1.2` 节（原"获取专家详情及关联内容"之前，现改为 `4.1.3`）

**新增内容**：
- 接口编号：`4.1.2 获取专家详情（公开）`
- Endpoint: `GET /api/v1/experts/{expert_id}`
- 描述：获取专家详细信息（不包含场次信息）
- 认证：公开访问，无需JWT Token（支持Optional Auth）
- 成功响应：`ExpertItem` Schema
- 执行流程：查询专家 -> 验证存在性 -> 序列化 -> 返回响应

#### 2. 在设计文档中添加 `GET /api/v1/experts/{expert_id}/is-followed` 接口

**位置**：`docs/03_系统设计/直播核心功能设计文档_v6_专家模块设计文档-专家信息-专家关注.md`  
**插入位置**：在 `4.2.3` 节（获取关注的专家列表）之后

**新增内容**：
- 接口编号：`4.2.4 检查是否已关注专家`
- Endpoint: `GET /api/v1/experts/{expert_id}/is-followed`
- 描述：检查当前登录用户是否已关注指定专家
- 认证：Strict Auth（强制鉴权）
- 成功响应：`{"is_followed": true/false}`
- 执行流程：JWT验证 -> 参数验证 -> 查询关注记录 -> 返回结果

#### 3. 更新接口编号

由于新增了 `4.1.2` 接口，后续接口编号需要调整：
- 原 `4.1.2` → 现 `4.1.3`（获取专家详情及关联内容）
- 原 `4.1.3` → 现 `4.1.4`（创建专家）
- 原 `4.1.4` → 现 `4.1.5`（获取专家列表）
- 原 `4.1.5` → 现 `4.1.6`（更新专家信息）
- 原 `4.1.6` → 现 `4.1.7`（删除专家）

#### 4. 更新执行流程部分

在 `5. 执行流程详细说明` 部分添加：
- `5.1.3 获取专家详情流程`
- `5.2.1 检查是否已关注流程`

### Service和CRUD层函数检查

**检查结果**：✅ **不需要新增函数**

**原因**：
1. **`GET /api/v1/experts/{expert_id}` 接口**：
   - Service层已有：`get_expert_detail` 方法（方法5）
   - CRUD层已有：`get_expert` 函数（函数2）
   - ✅ 完全对应，无需新增

2. **`GET /api/v1/experts/{expert_id}/is-followed` 接口**：
   - Service层已有：`check_is_followed` 方法（方法14）
   - CRUD层已有：`get_subscription` 函数（函数9）
   - ✅ 完全对应，无需新增

### 修复验证

- ✅ 设计文档已添加两个API接口的完整说明
- ✅ 接口编号已正确更新
- ✅ 执行流程部分已补充
- ✅ Service和CRUD层函数已确认存在，无需新增
- ✅ 无冲突和不一致的地方

---

## 问题2修复：路由路径前缀说明

### 问题描述

提示词文档中未明确说明路由路径前缀的处理方式（但符合项目规范）

### 问题分析

#### 两个提示词母版对比

**1. 通用母版文档**（`crud_service_endpoint代码生成提示词母版.md`）：
- **位置**：第1555-1592行
- **内容**：包含**非常详细**的路由定义说明，包括：
  - APIRouter定义时不指定prefix
  - prefix在api.py中统一指定
  - **避免重复前缀的详细说明**（关键）
  - **路由前缀层级示例**（关键）
  - **路由前缀命名规范**（关键）

**2. 专家模块母版文档**（`专家模块设计文档-专家信息-专家关注---crud_service_endpoint代码生成提示词母版.md`）：
- **位置**：第286-292行
- **内容**：包含**简洁**的路由定义说明，包括：
  - APIRouter定义时不指定prefix
  - prefix在api.py中统一指定
  - 端点路径的说明
  - ❌ **缺少**：避免重复前缀的详细说明
  - ❌ **缺少**：路由前缀层级示例
  - ❌ **缺少**：路由前缀命名规范

**3. 生成的Service/API层提示词文档**（`专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md`）：
- **位置**：第839行
- **内容**：只有一个简单注释：`# prefix 在 api.py 中指定`
- ❌ **缺少**：路由定义的详细说明

### 问题根本原因

**问题出在专家模块母版文档**，而不是生成的提示词文档。

**原因分析**：
1. 专家模块母版文档（步骤4.1生成）在生成时，从通用母版文档中提取了路由定义说明，但**只提取了简洁版本**，没有包含通用母版中的详细说明（避免重复前缀、路由前缀层级示例、路由前缀命名规范）。
2. 生成的Service/API层提示词文档（步骤4.2.2生成）基于专家模块母版文档，因此也**只包含了简洁版本**的路由定义说明。

### 修复方案

**方案1：修复专家模块母版文档**（推荐）
- 在专家模块母版文档中补充详细的路由定义说明
- 优点：修复根本原因，后续生成的提示词文档会自动包含详细说明
- 缺点：需要修改母版文档

**方案2：修复生成的提示词文档**（已执行）
- 在生成的Service/API层提示词文档中直接补充详细的路由定义说明
- 优点：快速修复，立即可用
- 缺点：如果重新生成提示词文档，需要再次修复

### 修复内容

**已执行修复**：在生成的Service/API层提示词文档中补充了详细的路由定义说明

**位置**：`docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md`  
**插入位置**：第839行（`router = APIRouter(tags=["专家管理"])` 之后）

**新增内容**：
1. **路由定义说明**（4个要点）：
   - APIRouter定义时不指定prefix
   - prefix在api.py中统一指定
   - 避免重复前缀的详细说明
   - 端点路径的说明

2. **路由前缀层级示例**：
   - 层级1：main.py（全局前缀）
   - 层级2：api.py（子路由前缀）
   - 正确示例和错误示例对比

### 建议的长期修复方案

**建议修复专家模块母版文档**，确保后续生成的提示词文档自动包含详细说明：

**位置**：`docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---crud_service_endpoint代码生成提示词母版.md`  
**修改位置**：第286-292行（路由定义部分）

**建议修改**：将简洁版本替换为详细版本（参考通用母版文档第1555-1592行）

---

## 修复验证

### 问题1修复验证

- ✅ 设计文档已添加两个API接口的完整说明
- ✅ 接口编号已正确更新（4.1.2-4.1.7, 4.2.1-4.2.4）
- ✅ 执行流程部分已补充（5.1.3, 5.2.1）
- ✅ Service和CRUD层函数已确认存在，无需新增
- ✅ 无冲突和不一致的地方

### 问题2修复验证

- ✅ 生成的Service/API层提示词文档已补充详细的路由定义说明
- ✅ 包含避免重复前缀的说明
- ✅ 包含路由前缀层级示例
- ✅ 包含正确示例和错误示例对比

---

## 总结

### 问题1修复总结

✅ **已修复**：在设计文档中添加了两个API接口的完整说明
- `GET /api/v1/experts/{expert_id}` - 获取专家详情
- `GET /api/v1/experts/{expert_id}/is-followed` - 检查是否已关注

✅ **确认**：Service和CRUD层函数已存在，无需新增

✅ **验证**：无冲突和不一致的地方

### 问题2修复总结

✅ **已修复**：在生成的Service/API层提示词文档中补充了详细的路由定义说明

⚠️ **根本原因**：专家模块母版文档中的路由定义说明过于简洁，缺少通用母版文档中的详细说明（避免重复前缀、路由前缀层级示例、路由前缀命名规范）

💡 **建议**：修复专家模块母版文档，确保后续生成的提示词文档自动包含详细说明

---

**修复完成时间**: 2026-01-18  
**修复状态**: ✅ 已完成

