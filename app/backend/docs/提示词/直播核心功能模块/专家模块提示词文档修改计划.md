# 专家模块提示词文档修改计划

## 📋 需要修改的问题汇总

根据《系统一致性审计报告》，需要修改以下问题：

---

## 🔴 严重问题（2个）

### 问题1：专家详情端点路径不一致

**当前位置**：
- Section 6.1 第324行：`端点：GET /api/v1/experts/{expert_id}`
- API层代码示例（第712行）：`@router.get("/{expert_id}")`

**修改为**：
- Section 6.1 第324行：`端点：GET /api/v1/experts/{expert_id}/sessions`
- API层代码示例（第712行）：`@router.get("/{expert_id}/sessions")`

---

## 🟠 中等问题（1个）

### 问题2：HTTP方法已一致

**当前状态**：
- 第662行已经是 `@router.patch("/experts/{expert_id}")` ✅
- 第314行已经是 `端点：PATCH /api/v1/admin/experts/{expert_id}` ✅

**结论**：这个问题已经解决，无需修改。

---

## 🟡 轻微问题（4个）

### 问题3：提示词文档包含设计文档未定义的API（5个）

需要删除以下5个API：

1. **获取专家详情**（第324-327行）
   - 当前：`GET /api/v1/experts/{expert_id}`
   - 设计文档要求：`GET /api/v1/experts/{expert_id}/sessions`
   - 修改为：获取专家详情及关联内容

2. **设置专家为推荐**（第340-343行，第757-786行）
   - 删除整个功能定义和API实现

3. **取消专家推荐**（第345-348行，第789-830行）
   - 删除整个功能定义和API实现

4. **检查是否已关注**（第890-905行）
   - 删除整个功能定义和API实现

5. **批量获取关注专家的直播状态**（第945-965行）
   - 删除整个功能定义和API实现

---

## 📊 修改总结

| 修改类型 | 数量 | 修改内容 |
|---------|-----|---------|
| 修改专家详情端点路径 | 1 | 添加 `/sessions` 后缀 |
| 删除冗余API | 5 | 删除5个设计文档未定义的API |
| **总计** | **6** | |

---

## 🎯 修改原则

1. **最小幅度修改**：只修改必须修改的部分
2. **确保不引入冲突**：修改后确保所有引用正确
3. **保持代码一致性**：确保修改后的风格一致

---

**生成日期**: 2026-01-18
**下一步**: 执行上述修改

