# 品牌模块设计文档-品牌管理-品牌专题关联-品牌直播间关联 数据库模型一致性验证提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**适用于**: 品牌模块设计文档-品牌管理-品牌专题关联-品牌直播间关联 数据库模型一致性验证  
**基于文档**: docs/03_系统设计/直播核心功能设计文档_v6_品牌模块设计文档-品牌管理-品牌专题关联.md

---

## 1. 角色定义 (Role Definition)

你是一名资深的 Python 后端技术审计员和 SQLAlchemy 2.0 专家。你的核心任务是进行严格的代码审查，确保下文中提供的品牌模块设计文档-品牌管理-品牌专题关联-品牌直播间关联 SQLAlchemy ORM 模型代码 (`brand.py`) **百分之百**地、**严格地**遵循了给定的数据库设计规范 (DDL)。

你必须以**挑剔和精确**的态度找出任何细微的偏差，包括但不限于：
- 字段名称不一致
- 数据类型不匹配
- 默认值设置错误
- 索引定义缺失或错误
- 外键约束不正确
- 关联关系定义不完整

---

## 2. 任务目标 (Task Objective)

你的目标是：

1. **比较**下面提供的【数据库设计规范 (DDL)】和【待审查的 SQLAlchemy 模型代码】
2. **验证**代码是否完全实现了 DDL 中定义的所有表、字段、类型、约束和索引
3. **生成**一份详细的审查报告，明确指出所有一致、不一致或缺失的实现，并提供修正建议

---

## 3. 核心输入 (Core Input)

### 3.1. 权威的数据库设计规范 (Authoritative Database Schema - DDL)

这是唯一的设计标准，所有代码实现都必须与此对齐。

**来源**: `docs/03_系统设计/直播核心功能设计文档_v6_品牌模块设计文档-品牌管理-品牌专题关联.md` Section 2

#### 3.1.1. brands（品牌/合作伙伴表）

```sql
CREATE TABLE brands (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    name VARCHAR(150) NOT NULL UNIQUE,
    slug VARCHAR(150),
    logo_url VARCHAR(512),
    description TEXT,
    website_url VARCHAR(255),
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT true,
    
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_brands_sort_order ON brands(sort_order);
CREATE INDEX idx_brands_is_active ON brands(is_active);
CREATE INDEX idx_brands_active_sort ON brands(is_active, sort_order) 
WHERE is_active = true;

-- 触发器
CREATE TRIGGER set_timestamp_brands 
BEFORE UPDATE ON brands 
FOR EACH ROW EXECUTE PROCEDURE trigger_set_timestamp();

-- 注释
COMMENT ON TABLE brands IS '品牌/合作伙伴信息表（如：迈瑞医疗、威克·微课等）';
COMMENT ON COLUMN brands.id IS '主键UUID，在应用层通过uuid.uuid4()生成';
COMMENT ON COLUMN brands.logo_url IS '品牌Logo图片URL';
COMMENT ON COLUMN brands.website_url IS '品牌官网链接';
COMMENT ON COLUMN brands.sort_order IS '排序权重，用于品牌栏展示顺序';
```

#### 3.1.2. brand_topics（品牌-专题关联表）

```sql
CREATE TABLE brand_topics (
    brand_id UUID NOT NULL,
    topic_id UUID NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    PRIMARY KEY (brand_id, topic_id),
    
    -- 命名的外键约束
    CONSTRAINT fk_brand_topics_brand_id 
        FOREIGN KEY (brand_id) REFERENCES brands(id) ON DELETE CASCADE,
    CONSTRAINT fk_brand_topics_topic_id 
        FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_brand_topics_brand_id ON brand_topics(brand_id);
CREATE INDEX idx_brand_topics_topic_id ON brand_topics(topic_id);

-- 注释
COMMENT ON TABLE brand_topics IS '品牌与专题的多对多关联表，关联brands表和topics表（topics表定义见专题功能文档）';
COMMENT ON COLUMN brand_topics.brand_id IS '品牌ID，关联brands表';
COMMENT ON COLUMN brand_topics.topic_id IS '专题ID，关联topics表（topics表定义见《直播核心功能设计文档_v6_增加专题功能完整设计文档-修正版.md》）';
```

#### 3.1.3. brand_rooms（品牌-直播间关联表）✅ V1.1新增

```sql
CREATE TABLE brand_rooms (
    brand_id UUID NOT NULL,
    room_id UUID NOT NULL,

    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY (brand_id, room_id),

    CONSTRAINT fk_brand_rooms_brand_id
        FOREIGN KEY (brand_id) REFERENCES brands(id) ON DELETE CASCADE,

    CONSTRAINT fk_brand_rooms_room_id
        FOREIGN KEY (room_id) REFERENCES live_rooms(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_brand_rooms_brand_id ON brand_rooms(brand_id);
CREATE INDEX idx_brand_rooms_room_id ON brand_rooms(room_id);

-- 注释
COMMENT ON TABLE brand_rooms IS '品牌与直播间的多对多关联表，用于直播间页品牌Tab展示 room 绑定品牌';
COMMENT ON COLUMN brand_rooms.brand_id IS '品牌ID，关联brands表';
COMMENT ON COLUMN brand_rooms.room_id IS '直播间ID，关联live_rooms表（live_rooms表定义见《直播核心功能设计文档_v6_深度融合最终版.md》）';
```

### 3.2. 待审查的 SQLAlchemy 模型代码

**文件**: `live_core_service/app/models/brand.py`

*请在此处提供 `brand.py` 文件的完整内容*

---

## 4. 审查清单 (Audit Checklist)

### 4.1. 表与模型映射

**检查项列表**:
- [ ] 所有表是否都已正确映射为SQLAlchemy模型类
- [ ] 每个模型类的`__tablename__`是否与DDL表名完全对应
- [ ] 模型类命名是否符合Python规范（大驼峰命名法）
- [ ] 表名到模型名的映射关系（如：`brands` → `Brand`）

**表映射检查表**:

| DDL 表名 | 模型类名 | `__tablename__` | 是否一致 |
|---------|---------|----------------|---------|
| `brands` | `Brand` | `"brands"` | ? |
| `brand_topics` | `BrandTopic` | `"brand_topics"` | ? |
| `brand_rooms` | `BrandRoom` | `"brand_rooms"` | ? |

---

### 4.2. 枚举类型定义

**检查项列表**:
- [ ] 是否定义了所有必需的枚举类
- [ ] 枚举类是否正确继承`str, enum.Enum`
- [ ] 枚举值是否完整且正确
- [ ] 枚举值的字符串形式是否与DDL中的ENUM值一致（通常为小写）

**枚举类型检查表**:

| 枚举类型 | DDL 定义 | 模型定义 | 是否一致 |
|---------|---------|---------|---------|
| N/A | 本模块无枚举类型 | N/A | N/A |

**注意**: 本模块不包含枚举类型，此检查项不适用。

---

### 4.3. 字段与列的完全对应

**检查项列表**:
- [ ] 每个DDL列是否都在模型中定义了对应字段
- [ ] 字段命名是否与DDL列名完全一致

#### 4.3.1. brands 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `name` | [ ] | [ ] | 品牌名称，NOT NULL, UNIQUE |
| `slug` | [ ] | [ ] | URL友好标识符 |
| `logo_url` | [ ] | [ ] | 品牌Logo图片URL |
| `description` | [ ] | [ ] | 品牌描述 |
| `website_url` | [ ] | [ ] | 品牌官网链接 |
| `sort_order` | [ ] | [ ] | 排序权重，DEFAULT 0 |
| `is_active` | [ ] | [ ] | 是否激活，DEFAULT true |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | [ ] | [ ] | 更新时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

#### 4.3.2. brand_topics 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `brand_id` | [ ] | [ ] | 复合主键之一，外键到 brands.id |
| `topic_id` | [ ] | [ ] | 复合主键之一，外键到 topics.id |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

#### 4.3.3. brand_rooms 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `brand_id` | [ ] | [ ] | 复合主键之一，外键到 brands.id |
| `room_id` | [ ] | [ ] | 复合主键之一，外键到 live_rooms.id |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

---

### 4.4. 数据类型精确性

#### 4.4.1. UUID类型检查

**检查项列表**:
- [ ] 是否从`sqlalchemy.dialects.postgresql`导入`UUID`
- [ ] 所有UUID字段是否使用`UUID(as_uuid=True)`
- [ ] 主键`id`是否包含`default=uuid.uuid4`（应用层生成）
- [ ] 是否导入了`import uuid`

**UUID类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `brands.id` | `UUID` | `UUID(as_uuid=True), primary_key=True, default=uuid.uuid4` | [ ] |
| `brand_topics.brand_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |
| `brand_topics.topic_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |
| `brand_rooms.brand_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |
| `brand_rooms.room_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |

#### 4.4.2. 字符串类型检查

**检查项列表**:
- [ ] VARCHAR(n)是否映射为`String(n)`
- [ ] TEXT是否映射为`Text`

**字符串类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `brands.name` | `VARCHAR(150)` | `String(150)` | [ ] |
| `brands.slug` | `VARCHAR(150)` | `String(150)` | [ ] |
| `brands.logo_url` | `VARCHAR(512)` | `String(512)` | [ ] |
| `brands.website_url` | `VARCHAR(255)` | `String(255)` | [ ] |
| `brands.description` | `TEXT` | `Text` | [ ] |

#### 4.4.3. 数值类型检查

**检查项列表**:
- [ ] INT是否映射为`Integer`

**数值类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `brands.sort_order` | `INT` | `Integer` | [ ] |

#### 4.4.4. 布尔类型检查

**检查项列表**:
- [ ] BOOLEAN是否映射为`Boolean`

**布尔类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `brands.is_active` | `BOOLEAN` | `Boolean` | [ ] |

#### 4.4.5. 时间戳类型检查

**检查项列表**:
- [ ] TIMESTAMPTZ是否映射为`TIMESTAMP(timezone=True)`
- [ ] 是否从`sqlalchemy`导入`TIMESTAMP`（不是`sqlalchemy.dialects.postgresql`）
- [ ] 是否使用`server_default=func.now()`设置数据库级默认值

**时间戳类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `brands.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |
| `brands.updated_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now()` | [ ] |
| `brand_topics.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |
| `brand_rooms.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |

---

### 4.5. 主键定义

**检查项列表**:
- [ ] 单表主键是否正确定义（`primary_key=True`）
- [ ] 复合主键是否正确定义（两个字段都设置`primary_key=True`）
- [ ] UUID主键是否使用`default=uuid.uuid4`（应用层生成）

**主键定义检查表**:

| 表 | 主键类型 | DDL 定义 | 模型定义 | 是否一致 |
|---|---------|---------|---------|---------|
| `brands` | 单主键 | `PRIMARY KEY (id)` | `id = Column(..., primary_key=True)` | [ ] |
| `brand_topics` | 复合主键 | `PRIMARY KEY (brand_id, topic_id)` | 两个字段都设置`primary_key=True` | [ ] |
| `brand_rooms` | 复合主键 | `PRIMARY KEY (brand_id, room_id)` | 两个字段都设置`primary_key=True` | [ ] |

---

### 4.6. 外键与级联规则

**检查项列表**:
- [ ] 所有外键是否正确定义`ForeignKey(...)`
- [ ] 外键约束名称是否与DDL一致（如果DDL中指定了约束名称）
- [ ] 级联删除规则是否使用`ondelete="CASCADE"`

**外键定义检查表**:

| 字段 | DDL 外键定义 | 模型外键定义 | 是否一致 |
|------|------------|------------|---------|
| `brand_topics.brand_id` | `FOREIGN KEY (brand_id) REFERENCES brands(id) ON DELETE CASCADE` | `ForeignKey("brands.id", ondelete="CASCADE")` | [ ] |
| `brand_topics.topic_id` | `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE` | `ForeignKey("topics.id", ondelete="CASCADE")` | [ ] |
| `brand_rooms.brand_id` | `FOREIGN KEY (brand_id) REFERENCES brands(id) ON DELETE CASCADE` | `ForeignKey("brands.id", ondelete="CASCADE")` | [ ] |
| `brand_rooms.room_id` | `FOREIGN KEY (room_id) REFERENCES live_rooms(id) ON DELETE CASCADE` | `ForeignKey("live_rooms.id", ondelete="CASCADE")` | [ ] |

---

### 4.7. 约束和默认值

#### 4.7.1. NOT NULL约束检查

**检查项列表**:
- [ ] DDL中`NOT NULL`的字段是否在模型中设置为`nullable=False`
- [ ] DDL中可空的字段是否在模型中设置为`nullable=True`（或不指定，默认True）

**NOT NULL约束检查表**:

| 字段 | DDL 约束 | 模型约束 | 是否一致 |
|------|---------|---------|---------|
| `brands.id` | `NOT NULL` | `nullable=False` | [ ] |
| `brands.name` | `NOT NULL` | `nullable=False` | [ ] |
| `brands.slug` | 可空 | `nullable=True` | [ ] |
| `brands.sort_order` | `NOT NULL` | `nullable=False` | [ ] |
| `brands.is_active` | `NOT NULL` | `nullable=False` | [ ] |
| `brands.created_at` | `NOT NULL` | `nullable=False` | [ ] |
| `brands.updated_at` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_topics.brand_id` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_topics.topic_id` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_topics.created_at` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_rooms.brand_id` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_rooms.room_id` | `NOT NULL` | `nullable=False` | [ ] |
| `brand_rooms.created_at` | `NOT NULL` | `nullable=False` | [ ] |

#### 4.7.2. 默认值检查

**检查项列表**:
- [ ] DDL中`DEFAULT`的值是否与模型中的`default`参数一致
- [ ] 时间戳字段是否使用`server_default=func.now()`（数据库级默认值）

**默认值检查表**:

| 字段 | DDL 默认值 | 模型默认值 | 是否一致 |
|------|-----------|-----------|---------|
| `brands.sort_order` | `DEFAULT 0` | `default=0` | [ ] |
| `brands.is_active` | `DEFAULT true` | `default=True` | [ ] |
| `brands.created_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now()` | [ ] |
| `brands.updated_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now(), onupdate=func.now()` | [ ] |
| `brand_topics.created_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now()` | [ ] |
| `brand_rooms.created_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now()` | [ ] |

---

### 4.8. 唯一约束

**检查项列表**:
- [ ] DDL中`UNIQUE`的字段是否在模型中设置`unique=True`

**唯一约束检查表**:

| 字段 | DDL 约束 | 模型约束 | 是否一致 |
|------|---------|---------|---------|
| `brands.name` | `UNIQUE` | `unique=True` | [ ] |

---

### 4.9. 索引定义

**检查项列表**:
- [ ] 所有DDL中定义的索引是否都在模型的`__table_args__`中定义
- [ ] 索引是否使用`Index`显式命名（不使用`index=True`）
- [ ] 索引名称是否与DDL中的索引名称一致
- [ ] 部分索引（`WHERE`子句）是否使用`postgresql_where`参数

**索引定义检查表**:

#### 4.9.1. brands 表索引检查

| 索引名称 | DDL 定义 | 模型定义 | 是否一致 |
|---------|---------|---------|---------|
| `idx_brands_sort_order` | `CREATE INDEX idx_brands_sort_order ON brands(sort_order)` | `Index('idx_brands_sort_order', 'sort_order')` | [ ] |
| `idx_brands_is_active` | `CREATE INDEX idx_brands_is_active ON brands(is_active)` | `Index('idx_brands_is_active', 'is_active')` | [ ] |
| `idx_brands_active_sort` | `CREATE INDEX idx_brands_active_sort ON brands(is_active, sort_order) WHERE is_active = true` | `Index('idx_brands_active_sort', 'is_active', 'sort_order', postgresql_where=text("is_active = true"))` | [ ] |

#### 4.9.2. brand_topics 表索引检查

| 索引名称 | DDL 定义 | 模型定义 | 是否一致 |
|---------|---------|---------|---------|
| `idx_brand_topics_brand_id` | `CREATE INDEX idx_brand_topics_brand_id ON brand_topics(brand_id)` | `Index('idx_brand_topics_brand_id', 'brand_id')` | [ ] |
| `idx_brand_topics_topic_id` | `CREATE INDEX idx_brand_topics_topic_id ON brand_topics(topic_id)` | `Index('idx_brand_topics_topic_id', 'topic_id')` | [ ] |

#### 4.9.3. brand_rooms 表索引检查

| 索引名称 | DDL 定义 | 模型定义 | 是否一致 |
|---------|---------|---------|---------|
| `idx_brand_rooms_brand_id` | `CREATE INDEX idx_brand_rooms_brand_id ON brand_rooms(brand_id)` | `Index('idx_brand_rooms_brand_id', 'brand_id')` | [ ] |
| `idx_brand_rooms_room_id` | `CREATE INDEX idx_brand_rooms_room_id ON brand_rooms(room_id)` | `Index('idx_brand_rooms_room_id', 'room_id')` | [ ] |

---

### 4.10. 关系完整性

**检查项列表**:
- [ ] 关联关系是否正确定义`relationship(...)`
- [ ] 反向关系（`back_populates`）是否正确定义
- [ ] 级联删除是否使用`cascade="all, delete-orphan"`

**关联关系检查表**:

| 关系 | DDL 约束 | 模型定义 | 是否一致 |
|------|---------|---------|---------|
| `Brand.topics` | N/A（通过外键隐式） | `relationship("BrandTopic", back_populates="brand", cascade="all, delete-orphan")` | [ ] |
| `Brand.rooms` | N/A（通过外键隐式） | `relationship("BrandRoom", back_populates="brand", cascade="all, delete-orphan")` | [ ] |
| `BrandTopic.brand` | N/A（通过外键隐式） | `relationship("Brand", back_populates="topics")` | [ ] |
| `BrandRoom.brand` | N/A（通过外键隐式） | `relationship("Brand", back_populates="rooms")` | [ ] |

**注意**: 
- `BrandTopic`和`BrandRoom`不定义与`Topic`和`LiveRoom`的反向关系，避免循环导入
- `Topic`模型由专题功能模块定义，`LiveRoom`模型由v6主文档定义

---

### 4.11. 导入语句检查

**检查项列表**:
- [ ] 是否从`app.database`导入`Base`（不是`app.models.base`）
- [ ] 是否从`sqlalchemy`导入`TIMESTAMP`（不是`sqlalchemy.dialects.postgresql`）
- [ ] 是否从`sqlalchemy.dialects.postgresql`导入`UUID`
- [ ] 是否导入`func`用于`func.now()`
- [ ] 是否导入`Index`用于显式命名索引
- [ ] 是否导入`text`用于部分索引的`postgresql_where`参数
- [ ] `Enum`是否重命名为`SAEnum`（虽然本模块不使用枚举，但检查导入是否规范）

**导入语句检查表**:

| 导入项 | 来源 | 是否正确 | 备注 |
|--------|------|---------|------|
| `Base` | `app.database` | [ ] | 必须在database.py中 |
| `TIMESTAMP` | `sqlalchemy` | [ ] | 不是dialects.postgresql |
| `UUID` | `sqlalchemy.dialects.postgresql` | [ ] | PostgreSQL特定类型 |
| `func` | `sqlalchemy.sql` | [ ] | 用于func.now() |
| `Index` | `sqlalchemy` | [ ] | 用于显式命名索引 |
| `text` | `sqlalchemy.sql` | [ ] | 用于部分索引 |

---

### 4.12. 代码风格与文档

**检查项列表**:
- [ ] 模型类是否有文档字符串（中文）
- [ ] 关键字段是否有`comment`参数（中文）
- [ ] 是否定义了`__repr__`方法
- [ ] `__repr__`方法是否不暴露敏感信息

**代码风格检查表**:

| 检查项 | 要求 | 是否符合 |
|--------|------|---------|
| 模型类文档字符串 | 每个模型类都有中文文档字符串 | [ ] |
| 字段注释 | 关键字段有`comment`参数 | [ ] |
| `__repr__`方法 | 每个模型类都有`__repr__`方法 | [ ] |
| 安全规范 | `__repr__`方法不暴露敏感信息 | [ ] |

---

## 5. 最终交付 (Final Deliverable)

### 5.1. 审查报告结构

请生成一份完整的数据库模型一致性验证报告，包含以下内容：

#### 5.1.1. 总体评估

- **符合性评分**: `XX/XX (XX%)` - 请给出具体的符合性评分
- **总体结论**: 完全一致 ✅ | 基本一致 ⚠️ | 存在差异 ❌
- **关键发现摘要**: 列出3-5个最重要的发现（问题或亮点）

#### 5.1.2. 详细审查结果表

对审查清单中的每个检查项，提供：

| 检查项编号 | 检查项名称 | 检查结果 | 问题描述 | 优先级 | 修正建议 |
|-----------|-----------|---------|---------|--------|---------|
| 4.1 | 表与模型映射 | ✅/⚠️/❌ | (如有问题) | P0/P1/P2 | (如需要) |
| 4.2 | 枚举类型定义 | ✅/⚠️/❌ | N/A（本模块无枚举） | - | - |
| 4.3 | 字段与列的完全对应 | ✅/⚠️/❌ | (如有问题) | P0/P1/P2 | (如需要) |
| ... | ... | ... | ... | ... | ... |

**优先级说明**:
- **P0 (严重)**: 会导致功能错误或数据库不一致的问题，必须修复
- **P1 (重要)**: 会影响性能或可维护性的问题，建议修复
- **P2 (一般)**: 代码风格或文档的问题，可选修复

#### 5.1.3. 关键问题清单

按优先级分类列出所有发现的问题：

**P0 - 严重问题** (必须修复):
- 问题1：...
- 问题2：...

**P1 - 重要问题** (建议修复):
- 问题1：...
- 问题2：...

**P2 - 一般问题** (可选修复):
- 问题1：...
- 问题2：...

#### 5.1.4. 修正代码示例

对于发现的每个问题，提供修正后的代码示例（如需要）：

```python
# 问题：XXX
# 修正前：
...

# 修正后：
...
```

#### 5.1.5. 一致性总结

**完全一致的方面**:
- 列出所有完全符合要求的检查项

**需要改进的方面**:
- 列出所有需要改进的检查项

**最终建议**:
- 给出整体评估和建议

---

### 5.2. 报告格式要求

- 使用Markdown格式
- 使用表格清晰展示对比结果
- 使用标记（✅、⚠️、❌）标注检查结果
- 代码示例使用代码块格式

---

**版本历史**:
- V1.0 (2026-01-18): 初始版本
