

1.  Write **detailed tests** for the stable `crud` layer.
2.  Write **high-level tests** for the `endpoints` layer to verify the API contract and critical business logic, without delving into implementation details that will change.

-----

### **高效 AI 代码生成提示词 (阶段一测试: CRUD 单元测试与 Endpoint 集成测试)**

#### **1. 角色定义 (Role Definition)**

You are a Senior Python Backend Engineer specializing in automated testing. You are an expert in using `pytest` with `pytest-asyncio` for asynchronous code and FastAPI's `TestClient` for integration testing. Your task is to write a robust test suite for the Phase 1 code of the `users`.

#### **2. 任务目标 (Task Objective)**

Your goal is to **extend** the existing test suite to cover the **Batch 2** APIs. This is an **additive task**: you will **add new test cases** and **new test files**, without modifying the existing Batch 1 tests.

Your tasks are:

1.  **【追加内容】** **Update** `tests/test_crud_users.py` by **appending** new unit tests for the Batch 2 CRUD functions.
2.  **【新增文件】** Create a **new test file**, `tests/test_api_auth_batch2.py`, with integration tests for the new Batch 2 authentication endpoints.
3.  **【新增文件】** Create a **new test file**, `tests/test_api_users_batch2.py`, with integration tests for the new Batch 2 user self-management endpoints.

#### **3. 核心上下文信息 (Core Context Information)**

**3.1. Testing Strategy**

  * **For `crud` layer:** Test thoroughly. This code is stable and foundational.
  * **For `endpoints` layer:** Focus on testing the external contract (request/response) and critical failure paths (e.g., trying to update a live room). We will not unit test the internal logic of the endpoint functions, as this logic will be moved to a service layer later.

**3.2. Testing Environment Setup**

  * Assume a `pytest` environment is set up with `pytest-asyncio`.
  * Assume a testing database is configured. You will need to create a test-specific `AsyncSession` that can be used in the tests. A common pattern is to create a `conftest.py` file to manage this. You should generate the code for this as well.

**3.3. Project Structure**

```
users/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── deps.py          # <-- 【新增】目标文件 1
│   │       └── endpoints/
│   │           └── auth.py       # <-- 【更新】目标文件 2
│   │           └── users.py       # <-- 【更新】目标文件 3
│   ├── core/
│   │   └── redis_client.py         # <-- 已存在 (代码如下)
│   │   └── response.py         # <-- 已存在 (代码如下)
│   ├── crud/
│   │   └── crud_users.py         # <-- 【更新】目标文件 4
│   ├── models/
│   │   └── users.py    # <-- 已存在 (代码如下)
│   ├── schemas/
│   │   └── users.py    # <-- 已存在 (代码如下)
│   └── database.py         # <-- 已存在
|___└── tests/
        ├── conftest.py               # <-- 已存在, 只能读取, 不能修改
        ├── test_crud_users.py     # <-- 目标文件1 (追加内容)
        ├── test_api_auth.py          # <-- 已存在 (批次一测试)
        ├── test_api_users.py         # <-- 已存在 (批次一测试)
        ├── test_api_auth_batch2.py   # <-- 目标文件2 (新文件)
        └── test_api_users_batch2.py  # <-- 目标文件3 (新文件)
```


**3.4. 已存在的代码全文 (Full Text of Existing Code)**

*You must generate tests based on the logic within the following application code.*

**【关键指令】** 以下是项目已存在的、**不可修改**的核心代码。你必须将它们作为上下文。

  * `@app/api/v1/endpoints/users.py` (包含批次一和批次二的代码)
  * `@app/api/v1/endpoints/auth.py` (包含批次一和批次二的代码)
  * `@app/crud/crud_user.py` (包含批次一和批次二的代码)
  * `@tests/conftest.py` (**作为测试环境和 Fixture 的来源**)
  * `@app/models/users.py`
  * `@app/schemas/users.py`

### 被测试函数的详细说明**
批次二：认证生命周期与用户自我管理 (Batch 2: Auth Lifecycle & User Self-Management)
目标: 完善Token管理和用户对自身信息的管理，构建完整的用户个人中心功能。

#### 2.2. 获取当前用户信息

  * **Endpoint**: `GET /api/v1/users/me`
  * **功能描述**: 获取当前已登录用户的详细个人资料。
  * **认证**: 需要提供有效的 `Access Token`。
  * **请求参数**: 无。
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {
        "uuid": "a1b2c3d4-e5f6-4a1b-8c3d-e5f6a1b2c3d4",
        "username": "testuser",
        "email": "testuser@example.com",
        "nickname": "测试用户",
        "avatar_url": null,
        "bio": null,
        "role": "REGULAR",
        "status": "NORMAL",
        "is_email_verified": true
      },
      "timestamp": "2025-07-22T20:05:00Z"
    }
    ```
  * **失败响应示例** (`401 Unauthorized` - Token无效):
    ```json
    {
      "code": 3004,
      "message": "认证凭证无效",
      "data": {
        "error": "Token has expired or is invalid"
      },
      "timestamp": "2025-07-22T20:06:00Z"
    }
    ```
  * **实现流程描述**:
    1.  从 `Authorization` Header 中解析 `Access Token` 并验证其有效性。
    2.  从 Token 的载荷 (payload) 中解析出用户的内部ID (`id`)。
    3.  根据 `id` 查询 `users` 表获取完整的用户记录。
    4.  若用户不存在或状态异常（如 `BANNED`），返回 401 或 403 错误。
    5.  将查询结果序列化为 Pydantic 模型（过滤掉 `password_hash` 等敏感字段），并返回。

#### 2.3. 更新当前用户信息

  * **Endpoint**: `PATCH /api/v1/users/me`
  * **功能描述**: 更新当前用户的公开个人资料，如昵称、简介、头像。
  * **认证**: 需要提供有效的 `Access Token`。
  * **请求体** (`application/json`):
    ```json
    {
      "nickname": "资深测试用户",
      "bio": "更新后的个人简介。"
    }
    ```
  * **成功响应** (`200 OK`): (返回更新后的完整用户对象)
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {
        "uuid": "a1b2c3d4-...", "username": "testuser", "email": "testuser@example.com",
        "nickname": "资深测试用户", "avatar_url": null, "bio": "更新后的个人简介。",
        "role": "REGULAR", "status": "NORMAL", "is_email_verified": true
      },
      "timestamp": "2025-07-22T20:08:00Z"
    }
    ```
  * **失败响应示例** (`400 Bad Request` - 昵称过长):
    ```json
    {
      "code": 4001,
      "message": "参数校验失败",
      "data": {
        "field": "nickname",
        "error": "昵称长度不能超过50个字符"
      },
      "timestamp": "2025-07-23T12:05:00Z"
    }
    ```
  * **实现流程描述**:
    1.  解析 `Access Token` 获取用户ID。
    2.  根据ID查询 `users` 表获取用户对象。
    3.  创建一个仅包含允许用户修改字段 (`nickname`, `avatar_url`, `bio`) 的 Pydantic 模型来解析请求体，以防权限提升攻击。
    4.  遍历 Pydantic 模型中已设置的字段，更新用户对象的相应属性。
    5.  提交数据库事务。
    6.  返回更新后的完整用户信息。

#### 2.4. 用户注销账户

  * **Endpoint**: `DELETE /api/v1/users/me`
  * **功能描述**: 用户软删除自己的账户。
  * **认证**: 需要提供有效的 `Access Token`。
  * **请求参数**: 无。
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": null,
      "timestamp": "2025-07-22T21:10:00Z"
    }
    ```
  * **失败响应示例** (`403 Forbidden` - 账户有未完成的业务):
    ```json
    {
      "code": 2002,
      "message": "业务逻辑错误",
      "data": {
        "error": "账户下存在有效会员，请先处理"
      },
      "timestamp": "2025-07-23T12:10:00Z"
    }
    ```
  * **实现流程描述**:
    1.  解析 `Access Token` 获取用户ID。
    2.  （可选）执行安全校验，如要求用户再输入一次密码进行确认。
    3.  （可选）执行业务检查，如确认用户没有正在进行的订阅或交易。若有，返回 403 错误。
    4.  将该用户的 `status` 字段更新为 `'DELETED'` (或 `'DEACTIVATED'`)。
    5.  将该用户的所有 Token 加入黑名单，强制其下线。
    6.  提交数据库事务，返回成功响应。


### 1.4. 刷新令牌

  * **Endpoint**: `POST /api/v1/auth/refresh`
  * **功能描述**: 使用有效的 `refresh_token` 获取一个新的 `access_token`。
  * **认证**: 无需认证。
  * **请求体** (`application/json`):
    ```json
    {
      "refresh_token": "a1b2c3d4e5f6..."
    }
    ```
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {
        "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9_new...",
        "token_type": "bearer"
      },
      "timestamp": "2025-07-22T21:00:00Z"
    }
    ```
  * **失败响应示例** (`401 Unauthorized` - Refresh Token无效或过期):
    ```json
    {
      "code": 3003,
      "message": "凭证无效或已过期",
      "data": {
        "error": "Invalid or expired refresh token"
      },
      "timestamp": "2025-07-22T21:01:00Z"
    }
    ```
  * **实现流程描述**:
    1.  验证 `refresh_token` 的有效性（是否存在、未过期、未在黑名单中）。
    2.  若有效，为该 Token 关联的用户生成一个新的 `access_token`。
    3.  返回新的 `access_token`。

### 1.5. 用户登出

  * **Endpoint**: `POST /api/v1/auth/logout`
  * **功能描述**: 用户主动登出，使其持有的 Token 失效。
  * **认证**: 需要提供有效的 `Access Token`。
  * **请求参数**: 无请求体，Token 通过 `Authorization` Header 传递。
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": null,
      "timestamp": "2025-07-22T21:05:00Z"
    }
    ```
  * **失败响应示例** (`401 Unauthorized` - Token格式错误或已过期):
    ```json
    {
      "code": 3004,
      "message": "认证凭证格式无效",
      "data": null,
      "timestamp": "2025-07-22T21:06:00Z"
    }
    ```
  * **实现流程描述**:
    1.  解析 `access_token` 获取其唯一标识（`jti`）。
    2.  将该 `access_token` 和关联的 `refresh_token` 标识加入到 Redis 黑名单中，并设置适当的过期时间。
    3.  返回成功响应。
好的，收到您的指示。我将严格按照最详尽的标准，为您完善“资源: 用户 (Users) - 公开接口”这一章节，确保每个接口都包含完整的**请求说明**、**成功响应体**、**失败响应体**和**执行流程**。

#### **(新增) 1.6. 修改密码 (用户已登录)**

  * **Endpoint**: `POST /api/v1/users/me/password`
  * **功能描述**: 当前已登录的用户修改自己的密码。为保证安全，此操作需要用户提供其**当前密码**。
  * **认证**: 需要提供有效的 `Access Token`。
  * **请求体** (`application/json`):
    ```json
    {
      "current_password": "old_password123",
      "new_password": "a_very_strong_new_password"
    }
    ```
      * *前端提示：建议增加一个“确认新密码”的输入框，并在前端校验两次输入的新密码是否一致，后端只需接收一次 `new_password` 即可。*
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "密码修改成功",
      "data": null,
      "timestamp": "2025-07-23T23:00:00Z"
    }
    ```
  * **失败响应示例** (`400 Bad Request` - 当前密码错误):
    ```json
    {
      "code": 4004,
      "message": "参数校验失败",
      "data": {
        "error": "当前密码不正确"
      },
      "timestamp": "2025-07-23T23:01:00Z"
    }
    ```
  * **实现流程描述**:
    1.  从 `Access Token` 中解析出用户ID。
    2.  根据用户ID查询 `users` 表，获取 `password_hash`。
    3.  验证请求体中的 `current_password` 与存储的 `password_hash` 是否匹配。若不匹配，返回 400 错误。
    4.  验证 `new_password` 是否符合密码强度策略（如长度、复杂度等）。
    5.  对 `new_password` 进行加盐哈希，生成新的 `password_hash`。
    6.  更新数据库中该用户的 `password_hash` 字段。
    7.  **安全增强**: 使该用户的所有旧 Token 和 Refresh Token 失效，强制其在其他设备上重新登录。
    8.  提交事务，并返回成功响应。

#### **(新增) 1.7. 忘记密码/重置密码流程 (用户未登录)**

此流程分为两步：请求重置 和 执行重置。

##### **步骤一: 请求密码重置**

  * **Endpoint**: `POST /api/v1/auth/password-reset-request`
  * **功能描述**: 用户提供注册邮箱，系统向该邮箱发送一个包含有时效性重置令牌的链接。此接口受图形验证码保护。
  * **认证**: 无需认证。
  * **请求体** (`application/json`):
    ```json
    {
      "email": "user@example.com",
      "captcha_id": "e1f2a3b4-...",
      "captcha_solution": "kL9mN2"
    }
    ```
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {
        "message": "如果该邮箱已注册，一封密码重置邮件已发送至您的邮箱。"
      },
      "timestamp": "2025-07-23T23:10:00Z"
    }
    ```
      * *注意：为防止泄露用户信息，无论邮箱是否存在，都应返回模糊的成功提示。*
  * **失败响应示例**: (`400 Bad Request` - 图形验证码错误)
  * **实现流程描述**:
    1.  校验图形验证码。
    2.  校验 `email` 格式。
    3.  查询 `users` 表确认该 `email` 是否存在。**如果不存在，直接返回成功响应，不执行后续操作**。
    4.  如果存在，生成一个唯一的、有时效性（如15分钟）的 `reset_token`。
    5.  将 `(reset_token, user_id)` 存入 Redis 并设置过期时间。
    6.  异步发送一封包含密码重置链接（如 `https://yoursite.com/reset-password?token=...`）的邮件给用户。
    7.  返回成功响应。

##### **步骤二: 执行密码重置**

  * **Endpoint**: `POST /api/v1/auth/password-reset`
  * **功能描述**: 用户通过重置令牌设置新密码。
  * **认证**: 无需认证。
  * **请求体** (`application/json`):
    ```json
    {
      "reset_token": "unique_reset_token_from_email",
      "new_password": "a_very_strong_new_password"
    }
    ```
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "密码重置成功",
      "data": null,
      "timestamp": "2025-07-23T23:20:00Z"
    }
    ```
  * **失败响应示例** (`400 Bad Request` - 令牌无效或过期):
    ```json
    {
      "code": 4005,
      "message": "无效的令牌",
      "data": {
        "error": "密码重置链接无效或已过期"
      },
      "timestamp": "2025-07-23T23:21:00Z"
    }
    ```
  * **实现流程描述**:
    1.  从请求体获取 `reset_token` 和 `new_password`。
    2.  在 Redis 中查询 `reset_token` 是否存在。若不存在，返回 400 错误。
    3.  从 Redis 中获取关联的 `user_id`，并立即删除该键。
    4.  验证 `new_password` 的强度。
    5.  对 `new_password` 进行加盐哈希。
    6.  更新对应 `user_id` 的 `password_hash` 字段。
    7.  提交事务，返回成功响应。

#### **4. 代码生成具体要求 (Specific Code Generation Instructions) - REVISED FOR USERS & AUTH**

---
##### **4.2 测试函数生成规范**

**必须严格遵循以下模板生成所有测试函数：**

```python
# CRUD测试模板
@pytest.mark.asyncio
async def test_crud_operation(self, db_session):
    """测试描述"""
    async for db in db_session:
        # 测试逻辑全部在此 async for 块内
        pass

# API测试模板（单fixture）
@pytest.mark.asyncio  
async def test_api_endpoint(self, async_client, mock_redis_captcha_success):
    """API测试描述"""
    async for client in async_client:
        # API测试逻辑全部在此 async for 块内
        pass

# API+数据库测试模板（多fixture）
@pytest.mark.asyncio
async def test_api_with_db(self, async_client, db_session, mock_redis_captcha_success):
    """API和数据库组合测试"""
    async for client in async_client:
        async for db in db_session:
            # 组合测试逻辑全部在此嵌套 async for 块内
            pass
```

**⚠️ 禁止的写法：**
- 不得在 async for 外部调用数据库操作
- 不得直接使用 fixture 参数而不解包
- 不得在函数参数中添加类型提示（会导致混淆）
- 

**Example of a Test Helper Function (Working Example):**
```
@pytest.mark.asyncio
async def test_retry_mp4_task_exceeds_max_retry_count(async_client, db_session):
        """
        Test retrying MP4 task that already exceeded max retry count.
        - Create a FAILED MP4 task with retry_count=3
        - Call retry API
        - Assert 400 response and error message
        """
        async for client in async_client:
            async for db in db_session:
                task = await task_pending_basic_for_invalid_mp4_url_retry(db)

                task.retry_count = 3
                await db.commit()

                response = await client.post(f"/api/v1/download/tasks/{task.id}/retry")
                assert response.status_code == 400
                assert "重试次数已达上限" in response.text or "Retry limit exceeded" in response.text
```

#### **4. 代码生成具体要求 (Specific Code Generation Instructions) - V2.0 深度测试版**

##### **4.1. `tests/conftest.py` - 测试设置**
* **指令**:
    1.  你的任务**不包括**生成或修改 `conftest.py`。
    2.  你**必须假设**一个已存在的 `conftest.py` 文件已经提供了 `db_session` (异步数据库会话) 和 `async_client` (异步HTTP客户端) 这两个 fixtures，并且它们可以正常工作。

---

##### **4.2. `tests/test_crud_users.py` - 追加 CRUD 单元测试**

* **指令**: 在 `tests/test_crud_users.py` 文件**末尾追加**以下新的测试用例。
* **要求**: 所有新测试函数必须 `async def` 并接受 `db_session` fixture。遵循“准备-执行-断言”(Arrange-Act-Assert)模式。

* **需新增的测试用例**:

    1.  **`test_get_user_by_id_found_and_not_found`**:
        * **准备**: 创建一个 `models.User` 实例并存入数据库，获取其 `id`。
        * **执行1 (Found)**: 使用获取的 `id` 调用 `crud_user.get()`。
        * **断言1**: 断言返回的 `User` 对象不为 `None`，且其 `id` 匹配。
        * **执行2 (Not Found)**: 使用一个不存在的 `id` (如 `999`) 调用 `crud_user.get()`。
        * **断言2**: 断言返回结果为 `None`。

    2.  **`test_get_user_by_uuid_found_and_not_found`**:
        * **说明**: 为 `crud_user.get_by_uuid()` 编写两个独立的测试用例，逻辑同上。

    3.  **`test_update_user_nickname_and_status`**:
        * **准备**: 创建一个用户 `original_user`。
        * **执行**: 创建一个 `schemas.UserUpdate` 对象，其中包含新的 `nickname` 和 `status` (`BANNED`)。调用 `crud_user.update(db_obj=original_user, obj_in=update_data)`。
        * **断言数据库状态**:
            * a. 从数据库中重新查询该用户。
            * b. 断言其 `nickname` 字段已被成功更新为新值。
            * c. 断言其 `status` 字段已被成功更新为 `EntityStatus.BANNED`。
            * d. 断言其 `username` 等未被更新的字段保持原样。

    4.  **`test_remove_user_soft_deletes`**:
        * **准备**: 创建一个 `status` 为 `NORMAL` 的用户。
        * **执行**: 调用 `crud_user.remove(user_to_delete=user)`。
        * **断言数据库状态**:
            * a. 重新查询该用户。
            * b. 断言该用户**仍然存在**于数据库中。
            * c. 断言其 `status` 字段的值已变为 `EntityStatus.DELETED`。

---
##### **4.3. `tests/test_api_auth_batch2.py` - 新增认证接口集成测试**

* **文件名**: `tests/test_api_auth_batch2.py`
* **职责**: 对**批次二**的**认证和密码管理**API端点进行集成测试。
* **要求**:
    * **必须**同时验证 **API 响应** (状态码、业务码、数据结构) 和**副作用** (如数据库、Redis状态变化)。
    * 需要时，使用 `mocker` fixture (来自 `pytest-mock`) 来模拟外部依赖，如 Redis 和邮件发送。

* **需实现的测试用例**:

    1.  **`test_refresh_token_success`**:
        * **准备**: 先调用登录接口（可为此创建一个可复用的`login_user`辅助函数）获取一个有效的 `refresh_token`。
        * **执行**: 使用该 `refresh_token` 请求 `POST /api/v1/auth/refresh`。
        * **断言 API 响应**: 断言 HTTP `200`，业务 `code` `200`，且 `data` 中包含一个新的、与旧 `access_token` 不同的 `access_token`。

    2.  **`test_refresh_token_fails_with_invalid_token`**:
        * **执行**: 使用一个伪造的字符串请求 `POST /api/v1/auth/refresh`。
        * **断言 API 响应**: 断言 HTTP `401`，业务 `code` 为 `3003`。

    3.  **`test_logout_success_and_token_is_blacklisted`**:
        * **准备**: 登录获取 `access_token`。
        * **执行**: 使用该 `access_token` 请求 `POST /api/v1/auth/logout`。
        * **断言 API 响应**: 断言 HTTP `200`。
        * **验证副作用**: 再次使用**同一个** `access_token` 访问一个受保护的接口（如`GET /api/v1/users/me`），断言返回 `401 Unauthorized`，以验证Token已被成功加入黑名单。

    4.  **`test_password_reset_flow_success`**:
        * **准备**: 在数据库中创建一个用户 `user_to_reset`。
        * **步骤1: 请求重置**:
            * a. Mock `captcha` 校验成功。
            * b. 请求 `POST /api/v1/auth/password-reset-request` 并传入 `user_to_reset.email`。
            * c. 断言返回 `200 OK`。
            * d. (高级) 从被 Mock 的 Redis `set` 命令中捕获生成的 `reset_token`。
        * **步骤2: 执行重置**:
            * a. 使用上一步获取的 `reset_token` 和一个 `new_password`，请求 `POST /api/v1/auth/password-reset`。
            * b. 断言返回 `200 OK`，`message` 为 "密码重置成功"。
        * **验证数据库状态**:
            * a. 重新从数据库查询 `user_to_reset`。
            * b. 断言其 `password_hash` 字段**已发生改变**。
        * **验证最终效果**:
            * a. 尝试使用该用户的 `username` 和**新密码**调用登录接口，断言**登录成功**。

---
##### **4.4. `tests/test_api_users_batch2.py` - 新增用户接口集成测试**

* **文件名**: `tests/test_api_users_batch2.py`
* **职责**: 对**批次二**的**用户自我管理**API端点进行集成测试。

* **需实现的测试用例**:

    1.  **`test_get_me_success`**:
        * **准备**: 创建一个用户 `test_user` 并为其生成 `access_token`。
        * **执行**: 使用该 `token` 请求 `GET /api/v1/users/me`。
        * **断言 API 响应**: 断言返回 `200 OK`，且 `data` 中的 `uuid` 和 `username` 与 `test_user` 的信息完全一致。

    2.  **`test_patch_me_success`**:
        * **准备**: 创建用户 `test_user` 并为其生成 `access_token`。
        * **执行**: 使用 `token` 请求 `PATCH /api/v1/users/me`，请求体为 `{"nickname": "new_nickname", "bio": "new_bio"}`。
        * **断言 API 响应**: 断言返回 `200 OK`，且 `data` 中 `nickname` 和 `bio` 已更新为新值。
        * **验证数据库状态**: 查询数据库，确认 `test_user` 对应的行，其 `nickname` 和 `bio` 字段已被修改。

    3.  **`test_patch_me_fails_to_change_role`**:
        * **准备**: 创建用户 `test_user` (其 `role` 为 `REGULAR`) 并为其生成 `access_token`。
        * **执行**: 请求 `PATCH /api/v1/users/me` 并恶意尝试传入 `"role": "ADMIN"`。
        * **断言 API 响应**: 断言返回 `200 OK` (因为后端应优雅地忽略该字段)。
        * **验证数据库状态**: 重新查询 `test_user`，**断言其 `role` 字段的值仍然是 `REGULAR`**，并未被篡改。

    4.  **`test_change_password_success`**:
        * **准备**: 创建用户 `test_user` (密码为 'old_password') 并为其生成 `access_token`。
        * **执行**: 使用 `token` 请求 `POST /api/v1/users/me/password`，请求体为 `{"current_password": "old_password", "new_password": "new_strong_password"}`。
        * **断言 API 响应**: 断言返回 `200 OK`。
        * **验证最终效果**: 尝试使用该用户的 `username` 和**新密码** `'new_strong_password'` 调用登录接口，**断言登录成功**。再尝试用**旧密码** `'old_password'` 登录，**断言登录失败** (返回`401`)。

    5.  **`test_delete_me_success`**:
           * **准备**: 创建一个普通用户 `test_user` 并为其生成 `access_token`。
           * **执行**: 使用该 `token` 请求 `DELETE /api/v1/users/me`。
           * **断言 API 响应**: 断言返回 `200 OK`，业务 `code` 为 `200`。
           * **验证数据库状态**:
               * a. 重新从数据库查询 `test_user`。
               * b. 断言该用户**仍然存在**。
               * c. 断言其 `status` 字段的值已变为 `EntityStatus.DELETED`。
           * **验证副作用**: （可选）尝试使用旧的 `access_token` 访问 `GET /me`，断言返回 `401 Unauthorized`，验证Token已失效。

    6.  **`test_delete_me_fails_with_active_membership` `(可选)`**:
      * **准备**: 创建一个用户，并手动为其在 `user_memberships` 表中创建一条状态为 `ACTIVE` 的记录。然后为该用户生成 `access_token`。
      * **执行**: 使用该 `token` 请求 `DELETE /api/v1/users/me`。
      * **断言 API 响应**: 断言返回 `403 Forbidden`，业务 `code` 为 `2002`，`message` 包含“账户下存在有效会员”等关键字。


#### **5. 最终交付 (Final Deliverable)**

Please generate the complete, runnable Python code for the following three files. For `tests/test_crud_users.py`, only provide the **new test cases to be appended**.

1.  `tests/test_crud_users.py` **(仅追加部分)**
2.  `tests/test_api_auth_batch2.py` **(新文件)**
3.  `tests/test_api_users_batch2.py` **(新文件)**