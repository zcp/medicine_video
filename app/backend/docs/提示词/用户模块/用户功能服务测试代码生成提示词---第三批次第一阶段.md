

1.  Write **detailed tests** for the stable `crud` layer.
2.  Write **high-level tests** for the `endpoints` layer to verify the API contract and critical business logic, without delving into implementation details that will change.

-----

### **高效 AI 代码生成提示词 (阶段一测试: CRUD 单元测试与 Endpoint 集成测试)**

#### **1. 角色定义 (Role Definition)**

You are a Senior Python Backend Engineer specializing in automated testing. You are an expert in using `pytest` with `pytest-asyncio` for asynchronous code and FastAPI's `TestClient` for integration testing. Your task is to write a robust test suite for the Phase 1 code of the `users`.

#### **2. 任务目标 (Task Objective)**

Your goal is to **extend** the existing test suite to cover the **Batch 3** APIs. This is an **additive task**: you will **add new test cases** and **new test files**, without modifying the existing Batch 1 tests.

Your tasks are:
1.  **【新增文件】** Create a new test file, `tests/test_crud_membership_product.py`, with detailed unit tests for the new Batch 3 CRUD layer.
2.  **【新增文件】** Create a new test file, `tests/test_api_membership_products.py`, with high-level integration tests for the new Batch 3 public API endpoint.

#### **3. 核心上下文信息 (Core Context Information)**

**3.1. Testing Strategy**

  * **For `crud` layer:** Test thoroughly. This code is stable and foundational.
  * **For `endpoints` layer:** Focus on testing the external contract (request/response) and critical failure paths (e.g., trying to update a live room). We will not unit test the internal logic of the endpoint functions, as this logic will be moved to a service layer later.

**3.2. Testing Environment Setup**

  * Assume a `pytest` environment is set up with `pytest-asyncio`.
  * Assume a testing database is configured. You will need to create a test-specific `AsyncSession` that can be used in the tests. A common pattern is to create a `conftest.py` file to manage this. You should generate the code for this as well.

**3.3. Project Structure & Code Context**

```
users/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── deps.py          # <-- 已存在 (代码如下)
│   │       └── endpoints/
│   │           └── auth.py       # <-- 已存在 (代码如下)
│   │           └── users.py       # <-- 已存在 (代码如下)
│   │           └── membership_products.py # <-- 已存在 (代码如下)
│   ├── core/
│   │   └── redis_client.py         # <-- 已存在 (代码如下)
│   │   └── response.py         # <-- 已存在 (代码如下)
│   ├── crud/
│   │   └── crud_users.py         # <-- 已存在 (代码如下)
│   │   └── crud_membership_product.py     # <-- 已存在 (代码如下)
│   ├── models/
│   │   └── users.py    # <-- 已存在 (代码如下)
│   ├── schemas/
│   │   └── users.py    # <-- 已存在 (代码如下)
│   └── database.py         # <-- 已存在
```

*You must generate tests based on the logic within the following application code.*
* **依赖的上下文 (Dependencies & Context)**:
    * `@tests/conftest.py` (**作为测试环境和 Fixture 的来源，不可修改**)
    * `@app/models/users.py` (需要其中的 `MembershipProduct` 模型和 `MembershipProductStatus` 枚举)
    * `@app/schemas/users.py` (需要其中的 `MembershipProductResponse` 等 Schemas)

### 被测试函数的详细说明**
批次三(Batch 3:Public Membership Product Interfac)
目标: 让所有用户（包括未登录的）能够看到可购买的会员产品，为会员购买流程提供“商品”数据。

* **被测试代码 (Code to be Tested)**:
    * `@app/crud/crud_membership_product.py`
    * `@app/api/v1/endpoints/membership_products.py`
  
#### 3.1. 获取可购买的会员产品列表

  * **Endpoint**: `GET /api/v1/membership-products`
  * **功能描述**: 公开接口，获取所有状态为 `ACTIVE` 的、可供用户购买的会员产品列表，通常用于价格或购买页面。
  * **认证**: 无需认证。
  * **请求参数 (Query)**:
      * `sort` (string, 可选): 排序字段及顺序，默认 `sort_order:asc`。例如 `price:desc`。
  * **成功响应** (`200 OK`):
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {
        "items": [
          {
            "code": "VIDEO_YEARLY",
            "name": "年度影视会员",
            "description": "畅享所有视频内容，为期一年。",
            "price": "199.99",
            "level": 1,
            "duration_unit": "year",
            "duration_value": 1
          },
          {
            "code": "LIVE_FAN_MONTHLY",
            "name": "粉丝月度包",
            "description": "直播间专属粉丝徽章和彩色弹幕。",
            "price": "29.99",
            "level": 3,
            "duration_unit": "month",
            "duration_value": 1
          }
        ]
      },
      "timestamp": "2025-07-22T20:10:00Z"
    }
    ```
  * **失败响应示例** (`500 Internal Server Error` - 数据库异常):
    ```json
    {
      "code": 1002,
      "message": "数据库查询错误",
      "data": {
        "error": "An unexpected database error occurred"
      },
      "timestamp": "2025-07-22T20:11:00Z"
    }
    ```
  * **实现流程描述**:
    1.  构建 `select(membership_products)` 查询。
    2.  添加 `WHERE status = 'ACTIVE'` 的筛选条件。
    3.  根据 `sort` Query 参数（若提供）或默认的 `sort_order` 字段对结果进行排序。
    4.  执行查询，将结果序列化为对象列表并返回。




#### **4. 代码生成具体要求 (Specific Code Generation Instructions) **

---
##### **4.2 测试函数生成规范**

**必须严格遵循以下模板生成所有测试函数：**

```python
# CRUD测试模板
@pytest.mark.asyncio
async def test_crud_operation(self, db_session):
    """测试描述"""
    async for db in db_session:
        # 测试逻辑全部在此 async for 块内
        pass

# API测试模板（单fixture）
@pytest.mark.asyncio  
async def test_api_endpoint(self, async_client, mock_redis_captcha_success):
    """API测试描述"""
    async for client in async_client:
        # API测试逻辑全部在此 async for 块内
        pass

# API+数据库测试模板（多fixture）
@pytest.mark.asyncio
async def test_api_with_db(self, async_client, db_session, mock_redis_captcha_success):
    """API和数据库组合测试"""
    async for client in async_client:
        async for db in db_session:
            # 组合测试逻辑全部在此嵌套 async for 块内
            pass
```

**⚠️ 禁止的写法：**
- 不得在 async for 外部调用数据库操作
- 不得直接使用 fixture 参数而不解包
- 不得在函数参数中添加类型提示（会导致混淆）
- 

**Example of a Test Helper Function (Working Example):**
```
@pytest.mark.asyncio
async def test_retry_mp4_task_exceeds_max_retry_count(async_client, db_session):
        """
        Test retrying MP4 task that already exceeded max retry count.
        - Create a FAILED MP4 task with retry_count=3
        - Call retry API
        - Assert 400 response and error message
        """
        async for client in async_client:
            async for db in db_session:
                task = await task_pending_basic_for_invalid_mp4_url_retry(db)

                task.retry_count = 3
                await db.commit()

                response = await client.post(f"/api/v1/download/tasks/{task.id}/retry")
                assert response.status_code == 400
                assert "重试次数已达上限" in response.text or "Retry limit exceeded" in response.text
```

#### **4. 代码生成具体要求 (Specific Code Generation Instructions) - V2.0 深度测试版**

##### **4.1. `tests/conftest.py` - 测试设置**
* **指令**:
    1.  你的任务**不包括**生成或修改 `conftest.py`。
    2.  你**必须假设**一个已存在的 `conftest.py` 文件已经提供了 `db_session` (异步数据库会话) 和 `async_client` (异步HTTP客户端) 这两个 fixtures，并且它们可以正常工作。


---
##### **4.2. `tests/test_crud_membership_product.py` - CRUD 单元测试**

* **文件名**: `tests/test_crud_membership_product.py`
* **职责**: 对 `app/crud/crud_membership_product.py` 中的 `get_multi_active` 函数进行详细的单元测试。
* **要求**: 所有测试函数必须 `async def` 并接受 `db_session` fixture。

* **需实现的测试用例**:

    1.  **`test_get_multi_active_returns_only_active_products`**:
        * **准备 (Arrange)**:
            * a. 在数据库中创建**多个** `MembershipProduct` 记录，其 `status` 分别为 `ACTIVE`, `DRAFT`, `ARCHIVED`。确保至少有两个 `ACTIVE` 的产品。
        * **执行 (Act)**: 调用 `crud_membership_product.get_multi_active(db)`。
        * **断言 (Assert)**:
            * a. 断言返回的列表**长度**恰好等于你创建的 `ACTIVE` 产品的数量。
            * b. 断言列表中的**每一个**产品的 `status` 字段都等于 `MembershipProductStatus.ACTIVE`。

    2.  **`test_get_multi_active_returns_empty_list_when_no_active`**:
        * **准备**: 在数据库中只创建 `DRAFT` 和 `INACTIVE` 状态的产品，不创建任何 `ACTIVE` 状态的产品。
        * **执行**: 调用 `crud_membership_product.get_multi_active(db)`。
        * **断言**: 断言返回的列表为空 (`len(result) == 0`)。

    3.  **`test_get_multi_active_respects_default_sorting`**:
        * **准备**: 创建两个 `ACTIVE` 状态的产品，`product_A` 的 `sort_order` 为 `10`，`product_B` 的 `sort_order` 为 `5`。
        * **执行**: 调用 `crud_membership_product.get_multi_active(db)` (不带排序参数)。
        * **断言**:
            * a. 断言返回列表的第一个元素的 `code` 是 `product_B.code`。
            * b. 断言返回列表的第二个元素的 `code` 是 `product_A.code`。

    4.  **`test_get_multi_active_respects_custom_sorting`**:
        * **准备**: 创建两个 `ACTIVE` 状态的产品，`product_A` 的 `price` 为 `100.00`，`product_B` 的 `price` 为 `50.00`。
        * **执行**: 调用 `crud_membership_product.get_multi_active(db, sort="price", direction="desc")`。
        * **断言**: 断言返回列表的第一个元素的 `code` 是 `product_A.code` (价格更高)。

---
##### **4.3. `tests/test_api_membership_products.py` - 端点集成测试**

* **文件名**: `tests/test_api_membership_products.py`
* **职责**: 对**批次三**的 `GET /api/v1/membership-products` 端点进行集成测试。
* **要求**: **必须**同时验证 **API 响应** (状态码、业务码、数据结构) 和 **返回数据的正确性**。

* **需实现的测试用-例**:

    1.  **`test_get_membership_products_returns_only_active`**:
        * **准备**: 在 `db_session` 中创建多个产品，状态分别为 `ACTIVE`, `DRAFT`, `ARCHIVED`。
        * **执行**: 向 `GET /api/v1/membership-products` 发送请求。
        * **断言 API 响应**:
            * a. 断言 HTTP 状态码为 `200`。
            * b. 断言响应体 JSON 中的 `code` 为 `200`。
            * c. 断言 `data['items']` 是一个列表，其**长度**等于数据库中 `ACTIVE` 产品的数量。
            * d. 断言列表中**每一个**元素的 `status` 字段的值都是 `'ACTIVE'`。

    2.  **`test_get_membership_products_sorting_works`**:
        * **准备**: 在 `db_session` 中创建两个 `ACTIVE` 状态的产品，`product_A` (`sort_order=10`, `price=100`) 和 `product_B` (`sort_order=5`, `price=200`)。
        * **执行 1 (默认排序)**: 请求 `GET /api/v1/membership-products`。
        * **断言 1**: 断言返回的 `items` 列表中，`product_B` (sort_order=5) 在 `product_A` (sort_order=10) **之前**。
        * **执行 2 (自定义排序)**: 请求 `GET /api/v1/membership-products?sort=price:desc`。
        * **断言 2**: 断言返回的 `items` 列表中，`product_B` (price=200) 在 `product_A` (price=100) **之前**。

#### **5. 最终交付 (Final Deliverable)**

Please generate the complete, runnable Python code for the following **two new files**, placed in clearly marked code blocks:

1.  `tests/test_crud_membership_product.py`
2.  `tests/test_api_membership_products.py`
