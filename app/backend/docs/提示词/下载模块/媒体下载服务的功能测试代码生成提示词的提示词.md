请帮我生成一个测试代码的提示词，需要覆盖更多测试场景，验证必要的执行过程中状态，字段值的变化是否正确，状态码返回是否正确等等
这是我的6个代码。

1. download.py 在endpoint目录中，是api层
2. downloadservice.py 是服务层代码
3. model/download.py 是我定义的模型
   4.schema/download.py 是我的数据操作与验证
   5.conftest.py 定义了一些已经被证明是成功的测试数据
4. test\_download\_1by1.py 使用conftest.py中的数据进行了不同场景下的测试，并且都运行成功了。

请帮我生成一个提示词，用来验证download.py的正确性。在验证中需要满足一些要求：

1. 要验证返回码的一致性，
   2.要验证返回数据结构是否符合预期
   3.要验证执行过程是否正确，比如当我创建一个下载任务时，任务状态应该为pending； 当我开始任务下载时，如果任务的状态应该为completed， 视频的状态也应该为completed。还有很多测试创建，应该需要验证状态变化的正确性
   4.验证字段值的正确性，比如当retry一个下载任务时，retry\_count应该加一，当retry\_count超过3时，应该触发相应的字段错误
2. 还有其他验证，请自行添加。
   6.测试场景请参考test\_download\_1by1.py， 如果场景缺失，请增加
3. 如果mp4下载，在有两种可能：completed和failed。 如果failed，更新下载任务状态为failed， 添加一条记录到download\_failures中，为后续下载。 如果retry成功，这将下载任务状态更新为completed， download\_failures中的对应记录删除。 如果重试超过三次，任务状态标记为abandon， download\_failures表中的记录也标记为abandon
4. hls的 下载比mp4复杂。 1）下载时如果m3u8下载失败，则更新下载任务状态为failed， 添加一条记录到download\_failures中，为后续下载。2）如果m3u8下载成功，但是ts下载成功率的数量小于指定阈值，则仍任务下载任务失败，则更新下载任务状态为failed， 添加一条记录到download\_failures中， 3）如果ts下载成功率的数量大于指定阈值，则认为下载任务部分成功，更改下载任务状态为部分成功，记录下载失败的ts到download\_failures中
5. hls的retry比mp4复杂。 如果下载任务状态为failed，每次retry, retry\_count加一。 在retry时会重新下载, 如果retry\_count大于三次，任务状态标记为abandon， download\_failures表中的记录也标记为abandon； 如果下载任务状态为部分完成(partial\_complete)，则只从download\_failures中读取下载失败的ts， 并retry它们。类似的，如果它们retry\_count超过3次，则将它们标记为abandon。如果它们retry成功，则从download\_failures表中删除。如果所有ts下载成功，则将下载任务的状态更新为completed。
   10.请问mp4和hls的测试生成独立的测试程序。
6. &nbsp;   测试应该包含错误场景测试，边界场景测试，
7. 断言应该包括： 断言应包括但不限于：API响应的状态码、响应体字段，数据库中相关表的状态、字段值、记录数量，业务副作用（如失败记录、重试次数、下载记录等），对于失败/异常场景，还要断言错误码、错误信息和数据库无异常变更。 比如：
      `test\_mp4\_download\_success\_flow`
        \* 断言：
           \* API响应：状态码200，响应体status为"completed"
          \* 数据库：DownloadTask.status为COMPLETED，retry\_count为0
         \* downloaded\_videos表有新记录，status为"completed"

&nbsp;          \* download\_failures表无相关记录

