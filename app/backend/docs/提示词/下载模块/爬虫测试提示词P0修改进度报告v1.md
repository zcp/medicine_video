# 爬虫测试提示词 P0 修改进度报告 v1

**修改日期**: 2025-01-13  
**任务**: 任务4 - 测试提示词文档P0修改  
**文件**: `docs/提示词/爬虫与批量导入集成功能测试提示词v1.md`

---

## 已完成的修改

### ✅ 修改1: 测试配置fixture（第68-78行）

**状态**: 已完成

**修改内容**:
- 移除 `max_retries: 2`
- 修改 `timeout: 30` → `timeout: 10`
- 修改 `temp_dir: '/tmp/test_crawler_csv'` → `temp_dir: '/tmp/vzan_crawler'`
- 添加 `batch_size: 100`

---

### ✅ 修改2: 第一个服务层测试方法调用（第784-798行）

**状态**: 已完成

**修改内容**:
- 移除 `crawl_url` 参数
- 添加新字段验证：`crawl_file`, `incremental_file`, `failed_file`

---

### ✅ 修改3: Mock `crawler.crawl()` 返回值（第762-780行）

**状态**: 已完成

**修改内容**:
- 从返回 `Path` 改为返回 `Dict`
- 添加 `incremental_file`, `failed_file`, `total_rows`, `failed_rows`, `incremental_rows` 字段

---

### ✅ 修改4-5: 第二、第三个服务层测试方法调用

**状态**: 已完成

**修改内容**:
- 移除 `crawl_url` 参数
- 调整断言（移除crawl_time验证，因为失败场景无返回值）

---

## 待完成的修改（批量操作）

### 🔄 剩余 10+ 处需要修改

由于测试文档中大量使用了相同的模式，建议采用批量修改策略：

#### A. 服务层测试方法调用（约8处）

**搜索模式**: `crawl_url='https://api.vzan.com/test'`

**批量修改操作**:
1. 删除所有包含 `crawl_url='...'` 的行
2. 检查相关的断言是否需要更新

**位置**:
- 第931行左右（test_crawl_and_import_import_partial_success）
- 第994行左右（test_crawl_and_import_batch_commit）
- 第1021行左右（test_crawl_and_import_empty_crawl_url - **需要删除整个测试用例**）

#### B. Mock `crawler.crawl()` 返回值（约5处）

**搜索模式**: `mock_crawler.crawl.return_value = csv_path`

**批量修改模板**:
```python
# 原有的csv_path创建保持不变
csv_path = tmp_path / "test_crawl.csv"
csv_path.write_bytes(sample_csv_content)

# 添加增量文件和失败文件
incremental_file = tmp_path / "test_crawl_inc.csv"
incremental_file.write_bytes(sample_csv_content)
failed_file = tmp_path / "failed_urls.txt"
failed_file.write_text("", encoding='utf-8')

# 修改返回值为Dict
mock_crawler.crawl.return_value = {
    'csv_file': str(csv_path),
    'incremental_file': str(incremental_file),
    'failed_file': str(failed_file),
    'total_rows': 2,  # 根据实际测试数据调整
    'failed_rows': 0,
    'incremental_rows': 2
}
```

#### C. API测试请求payload（约3处）

**搜索模式**: `"crawl_url": "https://api.vzan.com/..."`

**批量修改操作**:
1. 删除 `"crawl_url"` 字段
2. 添加 `"crawl_options"` 字段（可选）

**修改示例**:
```python
# 修改前
json={
    "crawler_type": "vzan",
    "crawl_url": "https://api.vzan.com/live/list",
    "skip_duplicates": True,
    "auto_start": False
}

# 修改后
json={
    "crawler_type": "vzan",
    "skip_duplicates": True,
    "auto_start": False,
    "crawl_options": {
        "page_size": 10,
        "max_pages": 5
    }
}
```

#### D. API测试响应验证（约3处）

**添加新字段验证**:
```python
# 原有验证保持
assert response.status_code == 200
response_data = response.json()
assert response_data['code'] == 201

data = response_data['data']
assert data['crawl_status'] == 'success'

# 添加新字段验证
assert data['crawl_file'] is not None
assert data['incremental_file'] is not None
assert data['failed_file'] is not None
```

#### E. 删除整个测试用例

**位置**: 第1011-1033行

**操作**: 完全删除 `test_crawl_and_import_empty_crawl_url` 测试用例（因为 `crawl_url` 参数已不存在）

---

## 需要添加的文档说明（P1）

### 1. 统一响应模式说明

**位置**: 第2节"测试基础规范"之后

**内容**: 添加第2.5节，说明API统一返回HTTP 200，业务状态码在 `code` 字段

### 2. 同步/异步测试区分说明

**位置**: 第2节"测试基础规范"之后

**内容**: 添加第2.6节，明确服务层用同步、API用异步

### 3. 不修改 `conftest.py` 的说明

**位置**: 第2节"测试基础规范"之后

**内容**: 添加第2.7节，说明辅助函数应在测试文件中定义

---

## 修改策略建议

### 方案A: 逐一手动修改（推荐给完整性要求高的场景）

**优点**: 准确、完整、不遗漏  
**缺点**: 耗时较长  
**预计时间**: 2-3小时

### 方案B: 批量搜索替换 + 重点手动修改

**优点**: 快速高效  
**缺点**: 可能需要多次验证  
**预计时间**: 1-2小时  
**操作步骤**:
1. 使用 `grep` 找到所有 `crawl_url` 出现位置
2. 批量删除 `crawl_url` 行
3. 手动修改 Mock 返回值（约5处）
4. 手动添加新字段验证（约8处）
5. 手动删除无效测试用例（1处）
6. 添加文档说明（3处）

---

## 当前进度

| 修改类型 | 总数 | 已完成 | 待完成 |
|:---|:---:|:---:|:---:|
| 测试配置修改 | 1 | 1 | 0 |
| 服务层方法调用修改 | 8+ | 3 | 5+ |
| Mock返回值修改 | 5+ | 1 | 4+ |
| 测试用例删除 | 1 | 0 | 1 |
| 返回值验证添加 | 8+ | 1 | 7+ |
| API请求修改 | 3+ | 0 | 3+ |
| API响应验证修改 | 3+ | 0 | 3+ |
| 文档说明添加 | 3 | 0 | 3 |

**总体进度**: 约20%

---

## 下一步行动

### 选项1: 继续手动修改（当前策略）

继续按照当前节奏逐一修改，确保每处修改都准确无误。

### 选项2: 切换到批量修改策略

使用 `search_replace` 工具批量处理重复性修改，然后集中处理特殊情况。

---

## 建议

**鉴于修改点较多且重复性高，建议：**

1. **立即完成关键修改**（影响测试可用性）:
   - ✅ 测试配置（已完成）
   - ✅ 第一个测试用例（已完成，作为示例）
   - 🔄 删除无效测试用例（`test_crawl_and_import_empty_crawl_url`）
   - 🔄 添加文档说明（统一响应模式、同步/异步区分、不修改conftest.py）

2. **批量完成重复性修改**:
   - 剩余服务层测试的 `crawl_url` 删除
   - 剩余 Mock 返回值修改
   - API测试的 `crawl_url` 删除和 `crawl_options` 添加

3. **生成最终修改完成报告**

---

**报告完成**  
**等待决策：继续当前策略还是切换到批量修改策略？**

