# 爬虫与批量导入集成功能增量开发提示词 v2.0

## 一、角色定义 (Role Definition)

你是一名资深的 Python 后端工程师，精通 FastAPI、SQLAlchemy、Pydantic、爬虫技术和系统集成，并擅长根据详细的设计文档和代码上下文，编写出职责清晰、分层明确、健壮可靠的微服务代码。你熟悉适配器模式、资源管理、异步编程等高级技术。

## 二、任务目标 (Task Objective)

你的任务是**扩展**现有的**媒体下载服务**，通过**最小幅度修改已有文件和新增文件**的方式，实现爬虫与批量导入的自动化集成功能。**你必须尽可能保证已有代码不被修改，或实现最小幅度的修改。**

具体要求：
1. **【新增文件】** 创建爬虫模块目录 `app/crawlers/`，包含基类、适配器、工厂
2. **【新增文件】** 创建Token加密工具 `app/core/encryption.py`
3. **【新增文件】** 创建加密脚本 `scripts/encrypt_token.py`
4. **【追加内容】** 更新 `app/core/config.py`，追加爬虫配置项
5. **【追加内容】** 更新 `app/core/exceptions.py`，追加爬虫异常类
6. **【追加内容】** 更新 `app/services/download_service.py`，追加 `crawl_and_import_tasks` 方法
7. **【追加内容】** 更新 `app/schemas/download.py`，追加爬取与导入相关Schema
8. **【追加内容】** 更新 `app/api/v1/endpoints/download.py`，追加 `POST /tasks/crawl-and-import` 端点

## 三、核心上下文信息 (Core Context Information)

### 3.1 项目结构

```
backend/media_download_service/
├── app/
│   ├── api/
│   │   └── v1/
│   │       └── endpoints/
│   │           └── download.py  # <-- 追加API端点
│   ├── schemas/
│   │   └── download.py  # <-- 追加Schema定义
│   ├── services/
│   │   └── download_service.py  # <-- 追加服务层方法
│   ├── models/
│   │   └── download.py  # <-- 已存在，无需修改
│   ├── crawlers/  # <-- 【新增目录】
│   │   ├── __init__.py  # <-- 【新增】爬虫工厂
│   │   ├── base_crawler.py  # <-- 【新增】爬虫基类
│   │   └── vzan_crawler.py  # <-- 【新增】微赞爬虫适配器
│   └── core/
│       ├── config.py  # <-- 追加爬虫配置
│       ├── exceptions.py  # <-- 追加爬虫异常类
│       ├── encryption.py  # <-- 【新增】Token加密工具
│       └── response.py  # <-- 已存在（create_response, create_error_response）
├── scripts/
│   └── encrypt_token.py  # <-- 【新增】Token加密脚本
└── backend/
    └── dynamicCrawler_vzan.py  # <-- 【已存在】现有爬虫实现，复用此文件
```

### 3.2 已存在的核心依赖

本功能依赖以下已存在的模块和组件：

#### 3.2.1 批量导入功能（已实现）

1. **服务层方法** (`app/services/download_service.py`):
   - `batch_import_tasks_from_csv(file_content, user_id, skip_duplicates, auto_start)`: 批量导入CSV
   - 返回值: `Dict` (包含 total, success, failed, skipped, created_task_ids等字段)

2. **API端点** (`app/api/v1/endpoints/download.py`):
   - `POST /tasks/batch-import`: 批量导入接口
   - 参数: file, skip_duplicates, auto_start

3. **Schema** (`app/schemas/download.py`):
   - `BatchImportResult`: 批量导入结果Schema

#### 3.2.2 核心基础设施（已实现）

1. **响应处理** (`app/core/response.py`):
   - `create_response(data, message, code)`: 统一成功响应
   - `create_error_response(message, code, data=None)`: 统一错误响应

2. **核心异常** (`app/core/exceptions.py`):
   - `DownloadServiceError`: 服务错误基类
   - `DatabaseError`: 数据库错误异常

3. **配置管理** (`app/core/config.py`):
   - `Settings` 类：基于Pydantic的配置管理
   - 环境变量读取：支持 `.env` 文件

4. **认证** (`app/core/deps.py`):
   - `get_current_user()`: JWT认证依赖
   - `get_db()`: 数据库会话依赖

#### 3.2.3 已存在的爬虫实现（核心复用）

**文件**: `backend/dynamicCrawler_vzan.py`

**关键类**: `DuanShuCrawler_vzan`

**关键方法**:
```python
def __init__(self):
    # 初始化Playwright浏览器（headless=False）
    # 设置文件路径：
    #   - self.liveroom_list_savefile: 主CSV文件
    #   - self.liveroom_list_savefile_inc: 增量CSV文件
    #   - self.failed_liveroomlist_url: 失败记录文件

def parse_all_liveroomlist_data(self, config: dict):
    # 核心爬取方法
    # Args:
    #   config: {'token': str, 'username': str, 'password': str}
    # 爬取流程：
    #   1. 读取主文件获取已爬取ID（增量爬取基准）
    #   2. 调用API获取直播间列表
    #   3. 提取enc_tpid和play_url
    #   4. 写入主文件和增量文件
    # 生成的文件：
    #   - 主文件: {temp_dir}/liveroomlist_vzan_api.csv
    #   - 增量文件: {temp_dir}/liveroomlist_inc_vzan_backup.csv
    #   - 失败文件: {temp_dir}/failed_liveroomlist_urls_vzan.txt

def close(self):
    # 关闭Playwright浏览器
    # 必须调用，否则浏览器进程泄漏
```

**文件路径信息**（从 `dynamicCrawler_vzan.py` 获取）:

| 属性名 | 值 | 文件名 | 示例路径 |
|:---|:---|:---|:---|
| `temp_dir` | `tempfile.gettempdir() / 'vzan_crawler'` | - | `/tmp/vzan_crawler` |
| `liveroom_list_savefile` | `{temp_dir}/liveroomlist_vzan.csv` | `liveroomlist_vzan.csv` | `/tmp/vzan_crawler/liveroomlist_vzan.csv` |
| `liveroom_list_savefile_inc` | `{temp_dir}/liveroomlist_inc_vzan.csv` | `liveroomlist_inc_vzan.csv` | `/tmp/vzan_crawler/liveroomlist_inc_vzan.csv` |
| `failed_liveroomlist_url` | `{temp_dir}/failed_liveroomlist_urls_vzan.txt` | `failed_liveroomlist_urls_vzan.txt` | `/tmp/vzan_crawler/failed_liveroomlist_urls_vzan.txt` |

**CSV字段说明**（已有爬虫输出）:

⚠️ **重要**: `dynamicCrawler_vzan.py` 生成的CSV使用**中文列名**，而 `batch_import_tasks_from_csv` 期望**英文列名**。

| 爬虫输出列名（中文） | 批量导入期望列名（英文） | 说明 | 状态 |
|:---|:---|:---|:---:|
| `直播间ID` | `liveroom_id` | 直播间唯一标识 | ⚠️ 列名不匹配 |
| `标题` | `liveroom_title` | 直播间标题 | ⚠️ 列名不匹配 |
| `直播间url` | `liveroom_url` | 直播间URL | ⚠️ 列名不匹配 |
| `播放url` | `resource_url` | 视频资源URL（核心字段）| ⚠️ 列名不匹配 |
| - | `resource_type` | 默认值 `'hls'`（微赞默认HLS格式，如从其他源导入可为mp4或image）| ⚠️ 缺失字段 |

**解决方案（两选一）**:

1. **【推荐】方案A: 修改爬虫CSV输出列名**
   - 在 `backend/dynamicCrawler_vzan.py` 中修改 `save_liveroomlist_to_csv` 方法
   - 将中文列名改为英文列名（`['liveroom_id', 'liveroom_title', 'liveroom_url', 'resource_url', ...]`）
   - 优点: 一劳永逸，无需转换；性能最佳
   - 缺点: 需要修改已有爬虫代码

2. **方案B: 在VzanCrawler中添加CSV转换**
   - 在 `VzanCrawler.crawl()` 方法中，爬取完成后添加转换步骤
   - 读取中文列名CSV → 转换为英文列名CSV → 写入新文件
   - 优点: 不修改已有爬虫
   - 缺点: 需要额外转换步骤，性能略有损失

**本提示词采用方案B**（不修改已有爬虫，在 `VzanCrawler` 中添加CSV转换逻辑）。

### 方案B转换实现要求

在 `VzanCrawler.crawl()` 方法中，**爬虫完成后且在返回前**，执行以下CSV转换步骤：

#### 转换流程
```
1. 检查爬虫输出的CSV文件是否存在
2. 读取中文列名CSV文件（使用csv.DictReader）
3. 构建列名映射字典（只映射批量导入需要的字段）
4. 转换数据为英文列名
5. 添加缺失的 resource_type 列（默认值 'hls'，微赞默认HLS格式，如从其他源导入可为mp4或image）
6. 使用临时文件写入转换后的CSV
7. 原子替换：移动临时文件到原文件位置（覆盖）
8. 记录转换日志
```

#### 字段分析（基于实际代码）

**爬虫输出CSV列名**（来自 `dynamicCrawler_vzan.py` 721-736行）：
```python
爬虫headers = [
    '直播间ID',      # 映射到 liveroom_id (必需)
    '标题',          # 映射到 liveroom_title (可选)
    '直播间url',     # 映射到 liveroom_url (可选)
    '创建时间',      # 不导入
    '开始时间',      # 不导入
    '结束时间',      # 不导入
    '直播类型',      # 不导入
    '直播状态',      # 不导入
    '播放url',       # 映射到 resource_url (必需)
    '封面图片',      # 不导入
    '观看次数',      # 不导入
    'zbID',         # 不导入
    'enc_tpid',     # 不导入
    '直播间总数'     # 不导入
]
```

**批量导入需要的列名**（来自 `download_service.py` 2008-2029行）：
```python
批量导入required_columns = {
    'liveroom_id',      # 必需 - 来自 '直播间ID'
    'resource_url',     # 必需 - 来自 '播放url'
    'resource_type'     # 必需 - 新增默认值为'hls'，微赞默认HLS格式，如从其他源导入可为mp4或image
}

批量导入optional_columns = {
    'liveroom_title',   # 可选 - 来自 '标题'
    'liveroom_url'      # 可选 - 来自 '直播间url'
}
```

#### 列名映射字典（完整版 - 仅映射需要的字段）
```python
COLUMN_MAPPING = {
    # === 必需字段（批量导入必需）===
    '直播间ID': 'liveroom_id',      # 必需 - 直播间唯一标识
    '播放url': 'resource_url',       # 必需 - 视频资源URL
    
    # === 可选字段（批量导入可选）===
    '标题': 'liveroom_title',        # 可选 - 直播间标题
    '直播间url': 'liveroom_url',     # 可选 - 直播间页面URL
}

# 转换后的CSV只包含以下列（顺序与batch_import_tasks_from_csv一致）
OUTPUT_COLUMNS = [
    'liveroom_id',       # 必需
    'liveroom_title',    # 可选
    'liveroom_url',      # 可选
    'resource_url',      # 必需
    'resource_type'      # 必需（默认值'hls'，微赞默认HLS格式，如从其他源导入可为mp4或image）
]
```

#### 字段验证规则
1. **必需字段验证**：转换时必须验证 `'直播间ID'` 和 `'播放url'` 存在且非空
2. **可选字段处理**：`'标题'` 和 `'直播间url'` 可以为空，转换为 `None`
3. **默认字段添加**：`resource_type` 默认为 `'hls'`（微赞平台默认HLS格式，如从其他源导入可为mp4或image）
4. **空值处理**：空字符串转换为 `None`，避免数据库插入空字符串

#### 转换代码实现要点
1. **读取CSV**: 使用 `csv.DictReader` 读取中文列名CSV
2. **验证必需字段**: 确保 `'直播间ID'` 和 `'播放url'` 存在
3. **数据转换**: 
   - 遍历每一行，使用 `COLUMN_MAPPING` 转换列名
   - 添加 `resource_type = 'hls'`（默认值，微赞默认HLS格式，如从其他源导入可为mp4或image）
   - 只保留 `OUTPUT_COLUMNS` 中的字段
4. **错误处理**:
   - 如果原始CSV不存在，记录警告并跳过转换
   - 如果必需字段缺失，抛出 `ValueError`
   - 如果转换失败，记录错误并重新抛出异常
5. **日志记录**:
   - 转换前：记录原始CSV路径和行数
   - 转换后：记录转换后CSV路径和成功转换的行数
   - 跳过的行：记录跳过原因（如缺失必需字段）
6. **文件处理**:
   - **主CSV文件**: 转换后**覆盖原文件**（`self.crawler.liveroom_list_savefile`）
   - **增量CSV文件**: 转换后**覆盖原文件**（`self.crawler.liveroom_list_savefile_inc`）
   - 使用临时文件写入，写入成功后替换原文件（原子操作）

#### 转换代码位置
- **文件**: `backend/media_download_service/app/services/crawler_service.py`
- **方法**: `VzanCrawler.crawl()`
- **位置**: 在 `self.crawler.parse_all_liveroomlist_data(config)` 调用**之后**，返回结果**之前**

#### 转换函数详细实现说明

##### `_convert_csv_columns` 私有方法（新增）

**方法签名**：
```python
def _convert_csv_columns(self, csv_file_path: str) -> int:
    """
    将中文列名CSV转换为英文列名CSV（原地覆盖）
    
    Args:
        csv_file_path: CSV文件路径（主文件或增量文件）
        
    Returns:
        int: 转换成功的行数
        
    Raises:
        ValueError: 如果必需字段缺失或文件不存在
    """
```

**实现步骤**（参考核心编码原则）：

```
1. 变量提取（用于异常处理日志）
   - file_path_for_logging = csv_file_path

2. 文件存在性检查
   - if not os.path.exists(csv_file_path): raise ValueError(...)

3. 初始化统计变量
   - success_count = 0
   - skip_count = 0
   - temp_file = f"{csv_file_path}.tmp"

4. try块:
   4.1 读取中文CSV（使用csv.DictReader）
       - encoding='utf-8-sig'（处理BOM）
       - 验证必需字段：'直播间ID', '播放url' in reader.fieldnames
   
   4.2 打开临时文件写入（使用csv.DictWriter）
       - fieldnames=OUTPUT_COLUMNS
       - writer.writeheader()
   
   4.3 遍历转换每一行
       for row_num, row in enumerate(reader, start=2):
           - 验证必需字段非空：if not row.get('直播间ID') or not row.get('播放url'): skip
           - 构建新行字典：
             new_row = {
                 'liveroom_id': row['直播间ID'].strip(),
                 'resource_url': row['播放url'].strip(),
                 'liveroom_title': row.get('标题', '').strip(),
                 'liveroom_url': row.get('直播间url', '').strip(),
                 'resource_type': 'hls'
             }
           - writer.writerow(new_row)
           - success_count += 1
   
   4.4 记录转换统计
       - logger.info(f"CSV列名转换完成: {file_path_for_logging}, 成功{success_count}行, 跳过{skip_count}行")
   
   4.5 原子替换原文件
       - os.replace(temp_file, csv_file_path)
       - logger.info(f"CSV文件已更新: {file_path_for_logging}")
   
   4.6 返回 success_count

5. except Exception as e:
   5.1 清理临时文件
       - if os.path.exists(temp_file): os.remove(temp_file)
   
   5.2 记录错误日志
       - logger.error(f"CSV列名转换失败: {file_path_for_logging}, 错误: {str(e)}")
   
   5.3 重新抛出异常
       - raise ValueError(f"CSV列名转换失败: {str(e)}")
```

**关键实现点**：
1. **列名映射使用**：直接在代码中使用 `COLUMN_MAPPING` 字典（在方法外定义为类常量或模块常量）
2. **编码处理**：`utf-8-sig` 自动处理BOM，兼容Excel
3. **原子替换**：`os.replace()` 是原子操作，避免并发写入问题
4. **错误隔离**：单行转换失败记录警告，不影响其他行
5. **空值处理**：`row.get('标题', '').strip()` 确保可选字段缺失时为空字符串
6. **必需字段验证**：在转换前验证header存在，在转换每行时验证值非空

##### 在 `VzanCrawler.crawl()` 中调用转换

**调用位置**：在 `self.crawler.parse_all_liveroomlist_data(config)` **之后**，返回结果**之前**

**实现代码**（伪代码）：
```python
def crawl(self, config: Dict, **kwargs) -> Dict:
    # ... 现有爬虫逻辑 ...
    
    # === 新增：CSV列名转换（在返回前执行）===
    csv_path = self.crawler.liveroom_list_savefile
    incremental_file = self.crawler.liveroom_list_savefile_inc
    
    # 转换主文件
    if os.path.exists(csv_path):
        try:
            main_rows = self._convert_csv_columns(csv_path)
            logger.info(f"主CSV文件列名转换成功: {csv_path}, {main_rows}行")
        except Exception as e:
            logger.error(f"主CSV文件列名转换失败: {csv_path}, {str(e)}")
            # 转换失败不中断流程，继续返回
    
    # 转换增量文件
    if os.path.exists(incremental_file):
        try:
            inc_rows = self._convert_csv_columns(incremental_file)
            logger.info(f"增量CSV文件列名转换成功: {incremental_file}, {inc_rows}行")
        except Exception as e:
            logger.error(f"增量CSV文件列名转换失败: {incremental_file}, {str(e)}")
            # 转换失败不中断流程，继续返回
    
    # 返回爬虫结果
    return {
        "csv_file": csv_path,
        "csv_rows": csv_rows,
        "incremental_file": incremental_file,
        "incremental_rows": incremental_rows,
        "failed_file": failed_file
    }
```

**重要注意事项**：
1. **转换失败不中断流程**：即使转换失败，也返回爬虫结果，避免影响整体流程
2. **转换后覆盖原文件**：批量导入直接使用转换后的英文列名CSV
3. **增量文件和主文件都需转换**：确保两个文件的列名都是英文

---

## 四、功能需求详细说明

### 4.1 核心功能特性

#### 4.1.1 爬取与导入集成

**自动化流程**:
```
API请求 → 配置验证 → 创建爬虫 → 执行爬取 → 生成CSV → 
优先增量文件导入 → 批量导入任务 → 关闭浏览器 → 清理临时文件 → 返回结果
```

#### 4.1.2 CSV文件选择策略（关键特性）

**【重要】优先使用增量文件**:
- 优先级1: 增量文件存在且非空 → 导入增量文件（性能最优）
- 优先级2: 增量文件不可用 → 回退到主文件，**强制启用去重**

**理由**:
- 增量文件只包含本次新增数据（如40条），导入速度快
- 主文件包含所有历史数据（如120条），导入慢且需去重
- 主文件**必须保留**，作为下次增量爬取的基准

#### 4.1.3 资源管理（关键特性）

**【重要】浏览器资源**:
- `DuanShuCrawler_vzan` 初始化时启动Playwright浏览器
- 爬取完成后**必须调用** `crawler.close()` 关闭浏览器
- 使用 finally 块确保浏览器关闭，避免进程泄漏

**【重要】临时文件清理**:
- 只删除增量文件和失败文件
- **不删除主文件**（下次增量爬取的基准）
- 如果删除主文件，下次会视为首次爬取，失去增量能力

### 4.2 Token安全管理

#### 4.2.1 Token加密存储

使用 `cryptography.fernet` 对称加密：

**加密流程**:
1. 生成加密密钥: `Fernet.generate_key()`
2. 加密Token: `cipher.encrypt(token.encode()).decode()`
3. 存储到环境变量: `VZAN_TOKEN=<加密后的token>`
4. 运行时解密使用: `cipher.decrypt(encrypted_token.encode()).decode()`

### 4.3 详细结果统计

返回结果包含：
- `crawl_status`: 爬取状态（success/failed）
- `crawl_rows`: 爬取到的总行数
- `crawl_file`: 主CSV文件路径
- `incremental_file`: 增量CSV文件路径
- `failed_file`: 失败记录文件路径
- `import_result`: 批量导入结果（来自 `batch_import_tasks_from_csv`）
  * total, success, failed, skipped
  * created_task_ids, failed_rows, skipped_rows
  * processing_time
- `error_message`: 错误信息（如果有）

---

## 五、具体代码生成指令

### 5.1 配置模块

#### 5.1.1 扩展 `app/core/config.py` （追加内容）

**指令**: 在 `Settings` 类中**追加**以下配置项。

```python
# ============ 爬虫配置 (Crawler Configuration) ============

# 微赞爬虫配置
VZAN_USERNAME: str = Field(default="", description="微赞用户名")
VZAN_PASSWORD: str = Field(default="", description="微赞密码")
VZAN_TOKEN: str = Field(default="", description="微赞API Token（加密后）")

# 爬虫通用配置
CRAWLER_TIMEOUT: int = Field(default=10, ge=1, le=600, description="爬虫单个请求超时时间（秒）")
CRAWLER_TEMP_DIR: str = Field(default="/tmp/vzan_crawler", description="CSV临时文件目录")
CRAWLER_BATCH_SIZE: int = Field(default=100, ge=10, le=1000, description="批量写入CSV的数据条数")

# 集成配置
CRAWL_IMPORT_AUTO_START: bool = Field(default=False, description="导入后是否自动启动")
CRAWL_IMPORT_SKIP_DUPLICATES: bool = Field(default=True, description="是否跳过重复任务组合（resource_url + liveroom_id + liveroom_title）")
CRAWL_IMPORT_DELETE_TEMP_FILE: bool = Field(default=True, description="导入后是否删除增量文件和失败记录（注意：主文件不会删除）")

# Token加密密钥
TOKEN_ENCRYPTION_KEY: str = Field(default="", description="Token加密密钥")
```

**注意事项**:
1. 敏感配置默认为空字符串
2. `CRAWLER_TIMEOUT` 设置为10秒（与 `dynamicCrawler_vzan.py` 一致）
3. `CRAWL_IMPORT_DELETE_TEMP_FILE` 说明明确**不删除主文件**

#### 5.1.2 创建 `app/core/encryption.py` （新增文件）

**指令**: 创建Token加密工具类。

```python
"""
Token加密工具模块

使用Fernet对称加密保护敏感Token
"""
import logging
from cryptography.fernet import Fernet, InvalidToken
from app.core.config import settings

logger = logging.getLogger(__name__)


class TokenEncryption:
    """Token加密工具类（使用Fernet对称加密）"""
    
    def __init__(self):
        """
        初始化加密器
        
        Raises:
            ValueError: 加密密钥未设置或格式错误
        """
        if not settings.TOKEN_ENCRYPTION_KEY:
            raise ValueError("TOKEN_ENCRYPTION_KEY环境变量未设置")
        
        try:
            self.cipher = Fernet(settings.TOKEN_ENCRYPTION_KEY.encode())
        except Exception as e:
            raise ValueError(f"加密密钥格式错误: {str(e)}")
    
    def encrypt(self, token: str) -> str:
        """
        加密token
        
        Args:
            token: 原始token字符串
        
        Returns:
            str: Base64编码的加密字符串
        
        Raises:
            ValueError: token为空或加密失败
        """
        if not token:
            raise ValueError("token不能为空")
        
        try:
            encrypted = self.cipher.encrypt(token.encode())
            return encrypted.decode()
        except Exception as e:
            logger.error(f"Token加密失败: {str(e)}")
            raise ValueError(f"加密失败: {str(e)}")
    
    def decrypt(self, encrypted_token: str) -> str:
        """
        解密token
        
        Args:
            encrypted_token: 加密后的token字符串
        
        Returns:
            str: 原始token字符串
        
        Raises:
            ValueError: encrypted_token为空
            InvalidToken: token无效或已损坏
        """
        if not encrypted_token:
            raise ValueError("encrypted_token不能为空")
        
        try:
            decrypted = self.cipher.decrypt(encrypted_token.encode())
            return decrypted.decode()
        except InvalidToken:
            logger.error("Token解密失败：Token无效或已损坏")
            raise InvalidToken("Token无效或已损坏")
        except Exception as e:
            logger.error(f"Token解密失败: {str(e)}")
            raise ValueError(f"解密失败: {str(e)}")


# 单例模式
_encryption_instance = None


def get_token_encryption() -> TokenEncryption:
    """
    获取Token加密器实例（单例模式）
    
    Returns:
        TokenEncryption: 加密器实例
    """
    global _encryption_instance
    if _encryption_instance is None:
        _encryption_instance = TokenEncryption()
    return _encryption_instance
```

#### 5.1.3 扩展 `app/core/exceptions.py` （追加内容）

**指令**: 在文件**末尾追加**以下爬虫异常类。

```python
# ============ 爬虫异常 (Crawler Exceptions) ============

class CrawlerError(Exception):
    """爬虫基础异常"""
    pass


class CrawlerConfigError(CrawlerError):
    """爬虫配置错误"""
    pass


class CrawlerTimeoutError(CrawlerError):
    """爬虫超时错误"""
    pass


class CrawlerAPIError(CrawlerError):
    """爬虫API调用错误"""
    pass


class CrawlerParseError(CrawlerError):
    """爬虫数据解析错误"""
    pass
```

---

### 5.2 爬虫模块（新增文件）

#### 5.2.1 创建 `app/crawlers/base_crawler.py`

**指令**: 创建爬虫基类，定义统一接口。

**实现要点**:
1. 使用ABC定义抽象基类
2. `crawl()` 方法返回Dict（包含csv_file, incremental_file, failed_file等）
3. `validate_config()` 方法验证配置完整性

```python
"""
爬虫基类模块

定义所有爬虫必须实现的统一接口
"""
from abc import ABC, abstractmethod
from typing import Dict
import logging

logger = logging.getLogger(__name__)


class BaseCrawler(ABC):
    """
    爬虫基类，定义统一接口
    
    所有具体的爬虫实现（如VzanCrawler）必须继承此类并实现抽象方法
    """
    
    def __init__(self, config: Dict):
        """
        初始化爬虫
        
        Args:
            config: 爬虫配置字典，至少包含:
                - timeout: 超时时间（秒），默认10秒
                - temp_dir: 临时文件目录
        """
        self.config = config
        self.timeout = config.get('timeout', 10)
        
        logger.info(f"{self.__class__.__name__} initialized with timeout={self.timeout}s")
    
    @abstractmethod
    def crawl(self, config: Dict, **kwargs) -> Dict:
        """
        执行爬取任务（抽象方法，子类必须实现）
        
        Args:
            config: 爬虫配置字典，包含 username, password, token 等
            **kwargs: 爬虫特定的额外参数，如:
                - page_size: 每页数据量（默认10）
                - max_pages: 最大爬取页数（默认全部）
        
        Returns:
            Dict: 爬取结果摘要
                {
                    "csv_file": str,             # 主 CSV 文件路径
                    "incremental_file": str,     # 增量 CSV 文件路径
                    "failed_file": str,          # 失败记录文件路径
                    "total_rows": int,           # 总爬取行数
                    "failed_rows": int,          # 失败行数
                    "incremental_rows": int      # 本次新增行数
                }
        
        Raises:
            CrawlerError: 爬取失败时抛出
            CrawlerTimeoutError: 爬取超时时抛出
            CrawlerAPIError: API调用失败时抛出
        """
        pass
    
    @abstractmethod
    def validate_config(self) -> bool:
        """
        验证配置是否完整有效（抽象方法，子类必须实现）
        
        Returns:
            bool: 配置是否有效
        """
        pass
```

#### 5.2.2 创建 `app/crawlers/vzan_crawler.py`

**指令**: 创建微赞爬虫适配器，**复用** `backend/dynamicCrawler_vzan.py`。

**设计模式**: 适配器模式

```
VzanCrawler (适配器，约60-80行)
     ↓ 调用
DuanShuCrawler_vzan (已有爬虫，backend/dynamicCrawler_vzan.py)
     ↓ 生成
CSV文件 (liveroomlist_vzan.csv, liveroomlist_inc_vzan.csv)
```

**实现要点**:
1. 导入 `DuanShuCrawler_vzan` 和必要的模块（csv, os, tempfile, shutil）
2. `__init__` 中创建 `self.crawler = DuanShuCrawler_vzan()`
3. `crawl()` 中调用 `self.crawler.parse_all_liveroomlist_data(config)`
4. **爬取完成后立即调用** `_convert_csv_columns()` 转换CSV列名（中文→英文）
5. **在 finally 块中调用** `self.crawler.close()` 关闭浏览器
6. 从 `self.crawler.liveroom_list_savefile` 等属性获取文件路径
7. 使用 `_count_csv_rows()` 统计行数
8. `_convert_csv_columns()` 使用临时文件+原子替换保证安全性

```python
"""
微赞爬虫适配器模块

使用适配器模式复用 backend/dynamicCrawler_vzan.py
"""
import csv
import logging
import os
import tempfile
import shutil
from typing import Dict
from pathlib import Path
from .base_crawler import BaseCrawler
from app.core.exceptions import CrawlerError, CrawlerConfigError

# 导入已有爬虫实现
import sys
from pathlib import Path
# 添加backend目录到sys.path
backend_dir = Path(__file__).parent.parent.parent.parent / 'backend'
sys.path.insert(0, str(backend_dir))
from dynamicCrawler_vzan import DuanShuCrawler_vzan

logger = logging.getLogger(__name__)


class VzanCrawler(BaseCrawler):
    """
    微赞爬虫适配器
    
    将已有的 DuanShuCrawler_vzan 适配到 BaseCrawler 接口
    """
    
    def __init__(self, config: Dict):
        """
        初始化适配器
        
        Args:
            config: 爬虫配置字典
        
        Raises:
            CrawlerConfigError: 配置无效或爬虫初始化失败
        """
        super().__init__(config)
        
        # 提取配置
        self.username = config.get('username')
        self.password = config.get('password')
        self.token = config.get('token')
        
        # 创建已有爬虫实例
        try:
            self.crawler = DuanShuCrawler_vzan()
            logger.info("VzanCrawler: DuanShuCrawler_vzan instance created")
        except Exception as e:
            logger.error(f"VzanCrawler: Failed to create crawler instance - {str(e)}")
            raise CrawlerConfigError(f"爬虫初始化失败: {str(e)}")
    
    def validate_config(self) -> bool:
        """
        验证配置
        
        Returns:
            bool: 配置是否有效
        """
        if not all([self.username, self.password, self.token]):
            logger.error("VzanCrawler: Missing required config (username, password, token)")
            return False
        return True
    
    def crawl(self, config: Dict, **kwargs) -> Dict:
        """
        执行爬取（主入口）
        
        Args:
            config: 爬虫配置字典 (包含 username, password, token)
            **kwargs: 
                - page_size: 每页数据量（可选）
                - max_pages: 最大爬取页数（可选）
        
        Returns:
            Dict: 爬取结果摘要
        """
        try:
            # 验证配置
            if not self.validate_config():
                raise CrawlerConfigError("VzanCrawler配置无效：缺少username, password或token")
            
            logger.info("VzanCrawler: Starting crawl")
            
            # 调用已有爬虫的核心方法
            self.crawler.parse_all_liveroomlist_data(config)
            
            logger.info("VzanCrawler: Crawl completed, starting CSV column conversion")
            
            # ⚠️ 重要：转换CSV列名（中文 → 英文）
            # 必须在返回前完成，确保导入模块能正确读取
            csv_file = str(self.crawler.liveroom_list_savefile)
            incremental_file = str(self.crawler.liveroom_list_savefile_inc)
            
            # 转换主CSV文件
            if os.path.exists(csv_file):
                self._convert_csv_columns(csv_file)
                logger.info(f"VzanCrawler: Main CSV converted - {csv_file}")
            
            # 转换增量CSV文件
            if os.path.exists(incremental_file):
                self._convert_csv_columns(incremental_file)
                logger.info(f"VzanCrawler: Incremental CSV converted - {incremental_file}")
            
            logger.info("VzanCrawler: CSV conversion completed, extracting file paths")
            
            # 构造返回结果（获取已有爬虫生成的文件路径）
            failed_file = str(self.crawler.failed_liveroomlist_url)
            
            # 统计CSV行数
            total_rows = self._count_csv_rows(csv_file)
            incremental_rows = self._count_csv_rows(incremental_file)
            failed_rows = self._count_csv_rows(failed_file)
            
            result = {
                "csv_file": csv_file,
                "incremental_file": incremental_file,
                "failed_file": failed_file,
                "total_rows": total_rows,
                "incremental_rows": incremental_rows,
                "failed_rows": failed_rows
            }
            
            logger.info(
                f"VzanCrawler: Crawl result - "
                f"total: {total_rows}, incremental: {incremental_rows}, failed: {failed_rows}"
            )
            
            return result
            
        except Exception as e:
            logger.error(f"VzanCrawler: Crawl failed - {str(e)}", exc_info=True)
            raise CrawlerError(f"爬取失败: {str(e)}")
        
        finally:
            # ⚠️ 重要：关闭浏览器，释放资源
            try:
                if hasattr(self, 'crawler') and self.crawler:
                    self.crawler.close()
                    logger.info("VzanCrawler: Browser closed")
            except Exception as e:
                logger.warning(f"VzanCrawler: Failed to close browser - {str(e)}")
    
    def _count_csv_rows(self, csv_file: str) -> int:
        """
        统计CSV文件行数（不含表头）
        
        Args:
            csv_file: CSV文件路径
        
        Returns:
            int: 行数（不含表头），异常时返回0
        """
        try:
            with open(csv_file, 'r', encoding='utf-8-sig') as f:
                reader = csv.reader(f)
                next(reader, None)  # 跳过表头
                return sum(1 for _ in reader)
        except Exception as e:
            logger.warning(f"Failed to count rows in {csv_file}: {str(e)}")
            return 0
    
    def _convert_csv_columns(self, csv_file: str) -> None:
        """
        将中文列名CSV转换为英文列名CSV（原地覆盖）
        
        Args:
            csv_file: CSV文件路径
            
        Raises:
            ValueError: 如果必需字段缺失
            Exception: 转换失败
        """
        import tempfile
        import shutil
        
        # 列名映射字典
        COLUMN_MAPPING = {
            '直播间ID': 'liveroom_id',      # 必需
            '播放url': 'resource_url',       # 必需
            '标题': 'liveroom_title',        # 可选
            '直播间url': 'liveroom_url',     # 可选
        }
        
        # 输出列顺序
        OUTPUT_COLUMNS = ['liveroom_id', 'liveroom_title', 'liveroom_url', 'resource_url', 'resource_type']
        
        try:
            logger.info(f"VzanCrawler: Converting CSV columns - {csv_file}")
            
            # 读取原始CSV
            with open(csv_file, 'r', encoding='utf-8-sig') as f:
                reader = csv.DictReader(f)
                rows = list(reader)
                
                # 验证必需字段
                if not rows:
                    logger.warning(f"VzanCrawler: Empty CSV file - {csv_file}")
                    return
                
                first_row = rows[0]
                if '直播间ID' not in first_row or '播放url' not in first_row:
                    raise ValueError(
                        f"CSV文件缺少必需字段。必需: ['直播间ID', '播放url'], "
                        f"实际: {list(first_row.keys())}"
                    )
            
            # 使用临时文件写入转换后的数据
            temp_fd, temp_path = tempfile.mkstemp(suffix='.csv', text=True)
            try:
                with os.fdopen(temp_fd, 'w', encoding='utf-8-sig', newline='') as f:
                    writer = csv.DictWriter(f, fieldnames=OUTPUT_COLUMNS)
                    writer.writeheader()
                    
                    converted_count = 0
                    skipped_count = 0
                    
                    for row in rows:
                        try:
                            # 转换列名并构造新行
                            new_row = {}
                            for cn_col, en_col in COLUMN_MAPPING.items():
                                value = row.get(cn_col, '').strip()
                                new_row[en_col] = value if value else None
                            
                            # 添加固定字段
                            new_row['resource_type'] = 'hls'
                            
                            # 验证必需字段
                            if not new_row.get('liveroom_id') or not new_row.get('resource_url'):
                                logger.warning(
                                    f"VzanCrawler: Skipping row with missing required fields - "
                                    f"liveroom_id: {new_row.get('liveroom_id')}, "
                                    f"resource_url: {new_row.get('resource_url')}"
                                )
                                skipped_count += 1
                                continue
                            
                            writer.writerow(new_row)
                            converted_count += 1
                            
                        except Exception as e:
                            logger.warning(f"VzanCrawler: Failed to convert row - {str(e)}")
                            skipped_count += 1
                            continue
                
                # 原子替换：移动临时文件到目标文件
                shutil.move(temp_path, csv_file)
                logger.info(
                    f"VzanCrawler: CSV conversion successful - {csv_file}, "
                    f"converted: {converted_count}, skipped: {skipped_count}"
                )
                
            except Exception as e:
                # 清理临时文件
                if os.path.exists(temp_path):
                    os.remove(temp_path)
                raise
                
        except Exception as e:
            logger.error(f"VzanCrawler: CSV conversion failed - {csv_file}: {str(e)}", exc_info=True)
            raise Exception(f"CSV列名转换失败: {str(e)}")
```

**关键说明**:
1. ✅ 复用 `DuanShuCrawler_vzan`，不重写爬虫逻辑
2. ✅ 从 `self.crawler.liveroom_list_savefile` 等获取文件路径
3. ✅ **CSV列名转换**：爬取完成后立即转换CSV列名（中文→英文），确保批量导入模块能正确读取
4. ✅ finally块中关闭浏览器，防止进程泄漏
5. ✅ 文件路径来自实际 `dynamicCrawler_vzan.py` 的定义
6. ✅ 使用临时文件和原子替换保证CSV转换的安全性

#### 5.2.3 创建 `app/crawlers/__init__.py`

**指令**: 创建爬虫工厂，根据类型创建爬虫实例。

```python
"""
爬虫模块

提供爬虫工厂和统一的爬虫接口
"""
from typing import Dict
from .base_crawler import BaseCrawler
from .vzan_crawler import VzanCrawler
from app.core.exceptions import CrawlerError
import logging

logger = logging.getLogger(__name__)


class CrawlerFactory:
    """
    爬虫工厂类
    
    根据爬虫类型创建相应的爬虫实例
    """
    
    # 注册的爬虫类型映射
    _crawlers = {
        'vzan': VzanCrawler,
        # 未来可扩展其他爬虫
        # 'douyin': DouyinCrawler,
        # 'kuaishou': KuaishouCrawler,
    }
    
    @classmethod
    def create(cls, crawler_type: str, config: Dict) -> BaseCrawler:
        """
        创建爬虫实例
        
        Args:
            crawler_type: 爬虫类型（vzan, douyin等）
            config: 爬虫配置字典
        
        Returns:
            BaseCrawler: 爬虫实例
        
        Raises:
            CrawlerError: 不支持的爬虫类型
        """
        crawler_class = cls._crawlers.get(crawler_type.lower())
        
        if not crawler_class:
            supported_types = ', '.join(cls._crawlers.keys())
            logger.error(f"不支持的爬虫类型: {crawler_type}，支持的类型: {supported_types}")
            raise CrawlerError(
                f"不支持的爬虫类型: {crawler_type}。"
                f"支持的类型: {supported_types}"
            )
        
        logger.info(f"CrawlerFactory: 创建{crawler_type}爬虫实例")
        return crawler_class(config)
    
    @classmethod
    def get_supported_types(cls) -> list:
        """
        获取支持的爬虫类型列表
        
        Returns:
            list: 支持的爬虫类型
        """
        return list(cls._crawlers.keys())


# 导出公共接口
__all__ = ['BaseCrawler', 'VzanCrawler', 'CrawlerFactory']
```

---

### 5.3 Schema定义（追加内容）

#### 5.3.1 扩展 `app/schemas/download.py`

**指令**: 在文件**末尾追加**以下Schema定义。

```python
# ==================== 爬取与导入相关Schema ====================

from typing import List, Optional
from pydantic import BaseModel, Field, field_validator


class CrawlOptions(BaseModel):
    """爬虫选项"""
    page_size: Optional[int] = Field(
        default=10, 
        ge=1, 
        le=100, 
        description="每页数据量（默认10）"
    )
    max_pages: Optional[int] = Field(
        None, 
        ge=1, 
        description="最大爬取页数（默认全部）"
    )
    
    model_config = ConfigDict(extra='allow')  # 允许扩展字段


class CrawlAndImportRequest(BaseModel):
    """爬取并导入请求模型"""
    crawler_type: str = Field(
        ..., 
        description="爬虫类型（vzan等，微赞爬虫使用固定API地址）"
    )
    skip_duplicates: bool = Field(
        default=True, 
        description="是否跳过重复任务组合（基于resource_url + liveroom_id + liveroom_title）"
    )
    auto_start: bool = Field(
        default=False, 
        description="导入后是否自动启动任务"
    )
    crawl_options: Optional[CrawlOptions] = Field(
        None, 
        description="爬虫特定参数（page_size, max_pages）"
    )
    
    @field_validator('crawler_type')
    def validate_crawler_type(cls, v):
        """验证爬虫类型"""
        from app.crawlers import CrawlerFactory
        supported_types = CrawlerFactory.get_supported_types()
        
        if v.lower() not in supported_types:
            raise ValueError(
                f"不支持的爬虫类型: {v}。"
                f"支持的类型: {', '.join(supported_types)}"
            )
        return v.lower()


class CrawlAndImportResponse(BaseModel):
    """爬取并导入响应模型"""
    crawl_status: str = Field(..., description="爬取状态（success/failed）")
    crawl_rows: int = Field(..., description="爬取到的总行数")
    crawl_file: Optional[str] = Field(None, description="主CSV文件路径")
    incremental_file: Optional[str] = Field(None, description="增量CSV文件路径")
    failed_file: Optional[str] = Field(None, description="失败记录文件路径")
    import_result: Optional[BatchImportResult] = Field(
        None, 
        description="导入结果（仅当爬取成功时存在）"
    )
    error_message: Optional[str] = Field(
        None, 
        description="错误信息（仅当失败时存在）"
    )
```

---

### 5.4 服务层方法（追加内容）

#### 5.4.1 扩展 `app/services/download_service.py`

**指令**: 在 `DownloadService` 类**内部末尾追加**以下方法。

**注意**: 此方法依赖已存在的 `batch_import_tasks_from_csv` 方法。

```python
def crawl_and_import_tasks(
    self,
    crawler_type: str,
    user_id: uuid.UUID,
    skip_duplicates: bool = True,
    auto_start: bool = False,
    **crawl_kwargs
) -> Dict:
    """
    执行爬取并批量导入下载任务
    
    完整流程: 爬取 → 生成CSV → 读取CSV → 批量导入 → 清理临时文件
    
    Args:
        crawler_type: 爬虫类型（vzan等）
        user_id: 当前用户ID
        skip_duplicates: 是否跳过重复任务组合（resource_url + liveroom_id + liveroom_title，默认True）
        auto_start: 导入后是否自动启动任务（默认False）
        **crawl_kwargs: 传递给爬虫的额外参数，如:
            - page_size: 每页数据量（默认10）
            - max_pages: 最大爬取页数（默认全部）
    
    Returns:
        Dict: {
            "crawl_status": "success" | "failed",
            "crawl_rows": int,
            "crawl_file": str,
            "incremental_file": str,
            "failed_file": str,
            "import_result": Dict,  # 来自 batch_import_tasks_from_csv
            "error_message": str
        }
    
    Raises:
        CrawlerConfigError: 爬虫配置无效
        ValueError: 参数错误
    """
    import os
    import time
    
    # 【第一步：变量提取】（安全异步异常处理）
    user_id_for_logging = str(user_id)
    crawler_type_for_logging = str(crawler_type)
    
    # 【第二步：初始化返回结果】
    result = {
        "crawl_status": "failed",
        "crawl_rows": 0,
        "crawl_file": None,
        "incremental_file": None,
        "failed_file": None,
        "import_result": None,
        "error_message": None
    }
    csv_path = None  # 用于finally块清理
    incremental_file = None
    failed_file = None
    start_time = time.time()
    
    try:
        # 【第三步：参数验证】
        if not crawler_type:
            raise ValueError("crawler_type不能为空")
        
        logger.info(
            f"开始爬取并导入 - 用户ID: {user_id_for_logging}, "
            f"爬虫类型: {crawler_type_for_logging}"
        )
        
        # 【第四步：准备爬虫配置】
        from app.core.config import settings
        
        crawler_config = {
            'timeout': settings.CRAWLER_TIMEOUT,
            'temp_dir': settings.CRAWLER_TEMP_DIR,
            'batch_size': settings.CRAWLER_BATCH_SIZE,
            'username': settings.VZAN_USERNAME,
            'password': settings.VZAN_PASSWORD,
            'token': settings.VZAN_TOKEN  # 加密后的token
        }
        
        # 【第五步：创建爬虫实例】
        from app.crawlers import CrawlerFactory
        
        crawler = CrawlerFactory.create(crawler_type, crawler_config)
        
        if not crawler.validate_config():
            raise CrawlerConfigError("爬虫配置验证失败")
        
        # 【第六步：执行爬取】
        crawl_result = crawler.crawl(crawler_config, **crawl_kwargs)
        
        csv_path = crawl_result['csv_file']
        crawl_rows = crawl_result['total_rows']
        incremental_file = crawl_result['incremental_file']
        incremental_rows = crawl_result['incremental_rows']
        failed_file = crawl_result['failed_file']
        
        # 更新结果
        result['crawl_status'] = 'success'
        result['crawl_rows'] = crawl_rows
        result['crawl_file'] = csv_path
        result['incremental_file'] = incremental_file
        result['failed_file'] = failed_file
        
        logger.info(
            f"爬取成功 - 用户ID: {user_id_for_logging}, "
            f"总行数: {crawl_rows}, 增量行数: {incremental_rows}"
        )
        
        # 【第七步：选择导入文件】（重要：优先增量文件）
        csv_to_import = None
        force_skip_duplicates = skip_duplicates  # 保存原始去重设置
        
        if incremental_file and os.path.exists(incremental_file) and incremental_rows > 0:
            # 增量文件存在且非空，优先使用
            csv_to_import = incremental_file
            logger.info(f"使用增量文件导入 - 文件: {incremental_file}, 行数: {incremental_rows}")
        else:
            # 增量文件不可用，回退到主文件
            csv_to_import = csv_path
            force_skip_duplicates = True  # 强制启用去重，防止重复导入
            logger.info(f"增量文件不可用，使用主文件导入（强制启用去重） - 文件: {csv_path}")
        
        # 验证文件存在
        if not csv_to_import or not os.path.exists(csv_to_import):
            raise FileNotFoundError(f"CSV文件不存在: {csv_to_import}")
        
        # 【第八步：读取CSV文件】
        with open(csv_to_import, 'rb') as f:
            file_content = f.read()
        
        logger.info(f"成功读取CSV文件 - 大小: {len(file_content)} 字节")
        
        # 【第九步：调用批量导入】
        import_result = self.batch_import_tasks_from_csv(
            file_content=file_content,
            user_id=user_id,
            skip_duplicates=force_skip_duplicates,  # 注意：使用force_skip_duplicates
            auto_start=auto_start
        )
        
        result['import_result'] = import_result
        
        logger.info(
            f"爬取并导入完成 - 用户ID: {user_id_for_logging}, "
            f"导入成功: {import_result['success']}/{import_result['total']}, "
            f"耗时: {round(time.time() - start_time, 2)}秒"
        )
        
        return result
    
    except CrawlerConfigError as e:
        logger.error(
            f"爬虫配置错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}",
            exc_info=True
        )
        result['error_message'] = str(e)
        return result  # 不抛出，返回结果
    
    except CrawlerError as e:
        logger.error(
            f"爬取失败 - 用户ID: {user_id_for_logging}, 错误: {str(e)}",
            exc_info=True
        )
        result['error_message'] = str(e)
        return result  # 不抛出，返回结果
    
    except ValueError as e:
        logger.error(
            f"参数错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}"
        )
        raise  # 参数错误需要抛出
    
    except Exception as e:
        logger.error(
            f"爬取并导入失败 - 用户ID: {user_id_for_logging}, "
            f"错误类型: {type(e).__name__}, 错误: {str(e)}",
            exc_info=True
        )
        result['error_message'] = str(e)
        return result
    
    finally:
        # 【第十步：清理临时文件】
        from app.core.config import settings
        
        if settings.CRAWL_IMPORT_DELETE_TEMP_FILE:
            # ⚠️ 重要：只删除增量文件和失败文件，不删除主文件
            files_to_delete = [
                incremental_file,  # ✅ 删除增量文件（已导入，无需保留）
                failed_file        # ✅ 删除失败文件（可选）
            ]
            # ❌ 不删除主文件：csv_path (需要保留作为下次增量爬取的基准)
            
            for file_path in files_to_delete:
                try:
                    if file_path and os.path.exists(file_path):
                        os.remove(file_path)
                        logger.info(f"已删除临时文件: {file_path}")
                except Exception as e:
                    logger.warning(f"删除临时文件失败: {file_path}, 错误: {str(e)}")
            
            # 记录主文件保留
            if csv_path and os.path.exists(csv_path):
                logger.info(f"保留主文件用于下次增量爬取: {csv_path}")
```

**关键说明**:
1. ✅ 依赖已存在的 `batch_import_tasks_from_csv` 方法
2. ✅ 优先使用增量文件，回退到主文件时强制去重
3. ✅ finally块清理，**只删除增量文件和失败文件**
4. ✅ **保留主文件**，作为下次增量爬取的基准

---

### 5.5 API端点层（追加内容）

#### 5.5.1 扩展 `app/api/v1/endpoints/download.py`

**指令**: 在文件**末尾追加**以下API端点。

```python
@router.post("/tasks/crawl-and-import")
async def crawl_and_import_tasks(
    request_data: CrawlAndImportRequest,
    current_user: dict = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """
    爬取并批量导入下载任务
    
    完整流程: 爬取 → 生成CSV → 批量导入任务 → 返回结果
    
    **认证**: 需要JWT Token
    """
    # ==================== 【核心规范0.1】变量提取（安全异步异常处理） ====================
    user_id_for_logging = current_user["user_id"]
    crawler_type_for_logging = request_data.crawler_type
    
    logger.info(
        f"API /tasks/crawl-and-import: 请求开始 - "
        f"用户ID={user_id_for_logging}, 爬虫类型={crawler_type_for_logging}"
    )
    
    try:
        # ==================== 业务处理 ====================
        service = DownloadService(db)
        
        # 准备爬虫参数
        crawl_kwargs = {}
        if request_data.crawl_options:
            crawl_kwargs = request_data.crawl_options.model_dump(exclude_unset=True)
        
        # 调用服务层方法
        result = service.crawl_and_import_tasks(
            crawler_type=request_data.crawler_type,
            user_id=user_id_for_logging,
            skip_duplicates=request_data.skip_duplicates,
            auto_start=request_data.auto_start,
            **crawl_kwargs
        )
        
        # ==================== 【核心规范0.2】统一响应处理 ====================
        # 根据结果返回不同响应
        
        # 1. 爬取失败
        if result['crawl_status'] == 'failed':
            return create_error_response(
                message=f"爬取失败: {result.get('error_message', '未知错误')}",
                code=500
            )
        
        # 2. 爬取成功，但导入全部失败
        if result.get('import_result') and result['import_result']['success'] == 0:
            return create_error_response(
                message="爬取成功，但导入全部失败",
                code=400,
                data=result
            )
        
        # 3. 爬取成功，导入部分成功
        if result.get('import_result') and result['import_result']['failed'] > 0:
            return create_response(
                data=result,
                message="爬取成功，导入部分成功",
                code=207
            )
        
        # 4. 爬取和导入全部成功
        return create_response(
            data=result,
            message="爬取并导入成功",
            code=200
        )
    
    # ==================== 【核心规范0.3】异常处理（使用提取的变量记录日志） ====================
    except ValueError as e:
        # 参数错误
        logger.warning(
            f"参数错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}"
        )
        return create_error_response(message=f"参数错误: {str(e)}", code=400)
    
    except CrawlerConfigError as e:
        # 爬虫配置错误
        logger.error(
            f"爬虫配置错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}"
        )
        return create_error_response(message=f"爬虫配置错误: {str(e)}", code=400)
    
    except CrawlerError as e:
        # 爬虫执行错误
        logger.error(
            f"爬虫执行错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}",
            exc_info=True
        )
        return create_error_response(message=f"爬虫执行错误: {str(e)}", code=500)
    
    except SQLAlchemyError as e:
        # 数据库错误（服务层已rollback）
        logger.error(
            f"数据库错误 - 用户ID: {user_id_for_logging}, 错误: {str(e)}",
            exc_info=True
        )
        return create_error_response(message="数据库操作失败", code=1002)
    
    except Exception as e:
        # 未知错误
        logger.error(
            f"未知错误 - 用户ID: {user_id_for_logging}, "
            f"错误类型: {type(e).__name__}, 错误: {str(e)}",
            exc_info=True
        )
        return create_error_response(message="服务器内部错误", code=1000)
```

**关键说明**:
1. ✅ 统一返回HTTP 200，业务状态码在响应体
2. ✅ 使用 `create_response` 和 `create_error_response`
3. ✅ 服务层已处理rollback，API层无需rollback

---

### 5.6 工具脚本（新增文件）

#### 5.6.1 创建 `scripts/encrypt_token.py`

**指令**: 创建Token加密命令行工具。

```python
#!/usr/bin/env python3
"""
Token加密工具

用法:
1. 生成密钥: python scripts/encrypt_token.py --generate-key
2. 加密token: python scripts/encrypt_token.py "your_token_here"
3. 解密验证: python scripts/encrypt_token.py --decrypt "encrypted_token" "encryption_key"
"""
import sys
from cryptography.fernet import Fernet


def generate_key():
    """生成新的加密密钥"""
    key = Fernet.generate_key()
    print("=" * 60)
    print("✅ 生成的加密密钥:")
    print(key.decode())
    print("=" * 60)
    print("\n💡 使用说明:")
    print("1. 将此密钥保存到环境变量 TOKEN_ENCRYPTION_KEY")
    print("2. 使用此密钥加密token: python encrypt_token.py <token> <key>")
    print("=" * 60)
    return key


def encrypt_token(token: str, key: str):
    """加密token"""
    cipher = Fernet(key.encode())
    encrypted = cipher.encrypt(token.encode())
    print("=" * 60)
    print("✅ 加密后的Token:")
    print(encrypted.decode())
    print("=" * 60)
    print("\n💡 使用说明:")
    print("将加密后的Token保存到环境变量 VZAN_TOKEN")
    print("=" * 60)
    return encrypted.decode()


def decrypt_token(encrypted_token: str, key: str):
    """解密token（用于验证）"""
    try:
        cipher = Fernet(key.encode())
        decrypted = cipher.decrypt(encrypted_token.encode())
        print("=" * 60)
        print("✅ 解密成功!")
        print("原始Token:")
        print(decrypted.decode())
        print("=" * 60)
        return decrypted.decode()
    except Exception as e:
        print("=" * 60)
        print(f"❌ 解密失败: {str(e)}")
        print("=" * 60)
        return None


def main():
    """主函数：解析命令行参数"""
    if len(sys.argv) < 2:
        print("用法:")
        print("  1. 生成密钥: python encrypt_token.py --generate-key")
        print("  2. 加密token: python encrypt_token.py <token> [encryption_key]")
        print("  3. 解密验证: python encrypt_token.py --decrypt <encrypted_token> <encryption_key>")
        sys.exit(1)
    
    if sys.argv[1] == '--generate-key':
        generate_key()
    
    elif sys.argv[1] == '--decrypt':
        if len(sys.argv) != 4:
            print("错误: --decrypt 需要2个参数")
            print("用法: python encrypt_token.py --decrypt <encrypted_token> <encryption_key>")
            sys.exit(1)
        decrypt_token(sys.argv[2], sys.argv[3])
    
    else:
        # 加密token
        token = sys.argv[1]
        
        if len(sys.argv) == 3:
            key = sys.argv[2]
        else:
            print("未提供加密密钥，自动生成新密钥...")
            key = generate_key().decode()
            print()
        
        encrypt_token(token, key)


if __name__ == "__main__":
    main()
```

---

## 六、编码规范与约束

### 6.1 核心编码原则（必须遵守）

#### 6.1.1 最小修改原则
- **【禁止修改已有代码】**: 所有代码都是**新增**或**追加**，不修改任何现有功能
- **【复用已有组件】**: 直接导入使用 `backend/dynamicCrawler_vzan.py`，不重写
- **【依赖已有方法】**: 调用 `batch_import_tasks_from_csv` 完成导入，不重复实现

#### 6.1.2 安全异步异常处理
- **【规则】**: 在进入`try`块之前，提取所有需要在`except`块中使用的ORM对象属性
- **【原因】**: 避免在捕获`SQLAlchemyError`后访问失效的ORM对象
- **【实现】**: 使用局部变量保存，如`user_id_for_logging = current_user["user_id"]`

#### 6.1.3 统一响应处理
- **【成功响应】**: 必须使用`create_response(data, message, code)`
- **【错误响应】**: 必须使用`create_error_response(message, code, data=None)`
- **【禁止】**: 服务层不得返回HTTP响应对象

#### 6.1.4 资源管理
- **【浏览器关闭】**: VzanCrawler 必须在 finally 块中调用 `self.crawler.close()`
- **【文件清理】**: 只删除增量文件和失败文件，**不删除主文件**
- **【主文件保留】**: 主文件是下次增量爬取的基准，删除会失去增量能力

#### 6.1.5 日志记录
- **【Info级别】**: 记录开始、进度、完成信息
- **【Warning级别】**: 记录文件清理失败、浏览器关闭失败
- **【Error级别】**: 记录爬取失败、数据库错误，必须包含`exc_info=True`

---

## 七、依赖导入清单

### 7.1 新增文件需导入的模块

**`app/crawlers/base_crawler.py`**:
```python
from abc import ABC, abstractmethod
from typing import Dict
import logging
```

**`app/crawlers/vzan_crawler.py`**:
```python
import csv
import logging
import sys
from typing import Dict
from pathlib import Path
from .base_crawler import BaseCrawler
from app.core.exceptions import CrawlerError, CrawlerConfigError
from dynamicCrawler_vzan import DuanShuCrawler_vzan  # 关键：导入已有爬虫
```

**`app/core/encryption.py`**:
```python
import logging
from cryptography.fernet import Fernet, InvalidToken
from app.core.config import settings
```

**`scripts/encrypt_token.py`**:
```python
import sys
from cryptography.fernet import Fernet
```

### 7.2 已有文件需追加的导入

**`app/services/download_service.py`** (追加):
```python
import os
import time
from app.crawlers import CrawlerFactory
from app.core.exceptions import CrawlerError, CrawlerConfigError
```

**`app/api/v1/endpoints/download.py`** (追加):
```python
from app.schemas.download import CrawlAndImportRequest, CrawlAndImportResponse
from app.core.exceptions import CrawlerError, CrawlerConfigError
```

---

## 八、测试验证要点

### 8.1 正常场景测试
- [ ] 爬取100条数据，全部成功导入
- [ ] 验证增量文件被优先导入
- [ ] 验证主文件被保留，增量文件被删除
- [ ] 验证浏览器正确关闭，无进程泄漏

### 8.2 异常场景测试
- [ ] 爬虫配置缺失（token为空），返回400错误
- [ ] 爬取超时，返回500错误
- [ ] 增量文件不可用，回退到主文件并强制去重

### 8.3 资源管理测试
- [ ] 爬取失败，浏览器仍正确关闭
- [ ] 第二次爬取，主文件存在，增量爬取正常
- [ ] 主文件不被删除，作为下次基准

---

## 九、最终交付清单

### 9.1 文件修改清单

**新增文件** (6个):
1. `app/crawlers/__init__.py`
2. `app/crawlers/base_crawler.py`
3. `app/crawlers/vzan_crawler.py`
4. `app/core/encryption.py`
5. `scripts/encrypt_token.py`

**追加内容** (5个):
1. `app/core/config.py` - 追加爬虫配置项
2. `app/core/exceptions.py` - 追加爬虫异常类
3. `app/schemas/download.py` - 追加爬取与导入Schema
4. `app/services/download_service.py` - 追加 `crawl_and_import_tasks` 方法
5. `app/api/v1/endpoints/download.py` - 追加 `POST /tasks/crawl-and-import` 端点

**复用文件** (1个):
1. `backend/dynamicCrawler_vzan.py` - **不修改**，通过导入复用

### 9.2 验证清单
- [ ] 所有代码都是新增或追加，无修改已有代码
- [ ] 使用适配器模式复用 `dynamicCrawler_vzan.py`
- [ ] 浏览器在 finally 块中正确关闭
- [ ] 只删除增量文件和失败文件，主文件被保留
- [ ] 优先使用增量文件导入
- [ ] 使用统一响应格式（`create_response`/`create_error_response`）
- [ ] 所有异常处理使用提取的局部变量记录日志

---

## 十、注意事项与最佳实践

### 10.1 关键注意事项

1. **【复用已有爬虫】**: VzanCrawler 是适配器，不重写爬虫逻辑
2. **【保留主文件】**: 主文件是增量爬取基准，删除会导致性能下降200%+
3. **【浏览器资源管理】**: 必须在finally块中关闭浏览器
4. **【依赖已有批量导入】**: 调用 `batch_import_tasks_from_csv`，不重复实现
5. **【最小修改原则】**: 所有改动都是追加，不修改任何已有功能

### 10.2 文件路径来源

所有文件路径信息来自 `backend/dynamicCrawler_vzan.py`:
- `self.liveroom_list_savefile`: 主文件
- `self.liveroom_list_savefile_inc`: 增量文件
- `self.failed_liveroomlist_url`: 失败文件
- `self.temp_dir`: `/tmp/vzan_crawler` (Linux) or `C:\Users\<username>\AppData\Local\Temp\vzan_crawler` (Windows)

### 10.3 增量爬取维护

- ✅ 主文件必须保留，作为下次增量爬取的基准
- ✅ 增量文件导入后可删除
- ✅ 失败文件可选删除
- ❌ 不要调用 `crawler.clean_old_files()`（会删除主文件）

---

**文档版本**: v2.0 (增量开发版)  
**创建日期**: 2025-01-13  
**文档状态**: 已完成，待实施  
**适用于**: 最小修改的增量开发场景

