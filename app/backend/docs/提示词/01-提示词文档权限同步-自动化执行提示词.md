# 提示词文档权限同步 - 自动化执行提示词

**版本**: V1.0
**创建日期**: 2026-01-12
**执行类型**: 一次性执行
**目标**: 自动化执行提示词文档权限同步，确保所有模块提示词文档与母版完全一致

---

## ⚠️ 使用说明（重要）

**本提示词用于自动化执行提示词文档的权限同步更新。**

**执行方式**：
在 Cursor 聊天框中先 `@` 引用本提示词文档和所有需要更新的提示词文档：

```
@docs/提示词/01-提示词文档权限同步-自动化执行提示词.md
@docs/提示词/提示词文档权限更新提示词.md
@docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户偏好与通知---Service和API端点代码生成提示词.md
@docs/提示词/直播核心功能模块/用户偏好与通知---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---CRUD层代码生成提示词.md
@docs/提示词/自动化后端代码生成/crud_service_endpoint代码生成提示词母版.md
```

**⚠️ 关键**：AI 需要读取所有 `@` 引用的文档内容，才能正确执行自动化更新。

---

## 1. 角色定义

你是一名精通「学院派架构（Clean Architecture）」的资深 Python 后端架构师，专门负责**自动化执行提示词文档的权限同步更新**。

**你的任务**：
1. **读取上下文**：用户已经在提示词的开头使用了 Cursor 的 `@` 标记引用了**所有的提示词文档**。
2. **理解母版**：读取母版文档（`crud_service_endpoint代码生成提示词母版.md`）的 Section 5.0.5 权限管理策略，确保完全理解核心内容。
3. **理解更新指南**：读取提示词文档权限更新提示词（`提示词文档权限更新提示词.md`），理解详细的更新步骤和检查清单。
4. **顺序执行**：按照指定的顺序，依次更新8个模块提示词文档。
5. **严格检查**：对每个文档的更新，都严格执行所有检查清单。
6. **生成报告**：生成详细的执行报告，记录每个文档的更新情况和检查结果。

---

## 2. 执行目标

**主要目标**：将母版 Section 5.0.5 的权限管理策略**有机融入**到所有8个模块提示词文档中，确保权限设计的一致性。

**需要更新的文档列表**（按顺序执行）：

### 第一阶段：专家模块（2个文档）
1. `专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md`
2. `专家模块设计文档-专家信息-专家关注---CRUD层代码生成提示词.md`

### 第二阶段：用户偏好与通知模块（2个文档）
3. `用户偏好与通知---Service和API端点代码生成提示词.md`
4. `用户偏好与通知---CRUD层代码生成提示词.md`

### 第三阶段：用户行为模块（2个文档）
5. `用户行为模块设计文档-收藏-观看历史-订阅提醒---Service层和API层代码生成提示词.md`
6. `用户行为模块设计文档-收藏-观看历史-订阅提醒---CRUD层代码生成提示词.md`

### 第四阶段：内容管理模块（2个文档）
7. `直播核心功能---内容管理模块设计文档-标签-分类-场次标签关联---Service层和API层代码生成提示词.md`
8. `直播核心功能---内容管理模块设计文档-标签-分类-场次标签关联---CRUD层代码生成提示词.md`

---

## 3. 母版权限管理核心内容（必须完整同步）

在执行更新之前，**必须**确认母版 Section 5.0.5 包含以下核心内容：

### 3.1. 三层权限职责分工（完整版）

**核心内容**：
1. **API 层 (Endpoint)**：负责 Authentication（认证）
   - 使用 `get_current_user` 或 `get_current_user_optional`
   - 提取 `user_id` (UUID) 和 `role` (str)
   - 传递给 Service 层
   - **不做权限判断**

2. **Service 层**：全权负责 Authorization（鉴权）
   - 接收 `user_id: UUID` 和 `user_role: str`
   - 调用权限守卫函数
   - 决定是否允许访问
   - 传递权限参数给 CRUD 层

3. **CRUD 层**：负责 SQL 层面权限过滤
   - 接收 `current_user_id: Optional[UUID]` 和 `role: Optional[str]`
   - 在 WHERE 条件中应用权限过滤
   - Admin查询：无过滤
   - Regular User查询：`WHERE (status='published' OR (status IN ('draft', 'archived') AND user_id=current_user_id))`
   - Anonymous查询：`WHERE status='published'`
   - 业务筛选与权限过滤：AND 关系

### 3.2. 双轨鉴权模式

| 模式 | 行为 | 适用场景 | API 层依赖 |
|-------|------|---------|-------------|
| Strict Auth | 无 Token → 401<br>Token 无效 → 401 | 所有写操作 | `get_current_user` |
| Optional Auth | 无 Token → None<br>Token 无效 → 401 | 所有读操作 | `get_current_user_optional` |

### 3.3. 权限守卫函数

**类型 1：资源可见性检查**
- 通用函数：`_check_resource_visibility(resource, user_id, role, is_published_field, published_value)`
- 模块适配：各模块可基于此实现专用函数（如 `_check_topic_visibility`）

**类型 2：写权限检查**
- 通用函数：`_check_write_permission(resource, user_id, role)`

### 3.4. 404 伪装机制

- 对于无权访问的私有资源，必须抛出 `NotFoundException`（404）
- **严禁**返回 `PermissionDeniedException`（403）
- 原因：防止资源探测攻击

### 3.5. Repository 层权限过滤

**原则**：
- 禁止内存过滤（性能陷阱）
- 在 SQL 查询中应用权限过滤
- 业务筛选与权限过滤使用 AND 关系
- 总数查询和数据查询使用相同的 WHERE 条件

---

## 4. 执行策略与检查机制

### 4.1. 执行策略

**顺序执行**：
- ✅ 必须按照指定的顺序（第一阶段→第二阶段→第三阶段→第四阶段）依次更新8个文档
- ✅ 必须逐个文档完成，确保每个文档的更新和检查都通过后，才能进行下一个文档
- ✅ 如果任何一个文档的更新或检查失败，必须**立即停止**，报告问题并等待确认

**逐文档执行流程**：

```
对于每个文档：
1. 读取文档内容
2. 执行更新前检查清单
3. 如果更新前检查未通过 → 停止，报告问题
4. 执行文档更新（有机融入母版权限管理策略）
5. 执行更新后检查清单
6. 如果更新后检查未通过 → 回滚，报告问题
7. 记录更新情况和检查结果
8. 继续下一个文档
```

### 4.2. 检查机制

**三层检查机制**：

#### 第一层：更新前检查清单
- ✅ 文档结构检查
- ✅ 内容完整性检查
- ✅ 功能权限类型检查

#### 第二层：更新后检查清单
- ✅ 结构完整性检查
- ✅ 内容一致性检查
- ✅ 功能完整性检查
- ✅ 无遗漏检查
- ✅ 无过度更新检查
- ✅ 无冲突检查

#### 第三层：最终检查清单
- ✅ 文档完整性检查
- ✅ 内容一致性检查
- ✅ 功能完整性检查
- ✅ 质量检查

### 4.3. 验证与确认机制

**自动验证**：
- ✅ AI 自身执行所有检查清单
- ✅ AI 自动标记每个检查项的通过/失败状态
- ✅ AI 自动记录每个文档的更新情况

**人工确认**（可选）：
- 在更新完成后，AI 提供完整的执行报告
- 报告包含每个文档的详细更新情况和检查结果
- 用户可以查看报告并确认更新是否满足要求
- 如果用户发现任何问题，可以要求AI回滚或重新执行

---

## 5. 执行步骤

### 5.1. 准备阶段（开始前必须完成）

**Step 1：确认所有文档都已引用**
```
确认以下文档都已通过 @ 标记引用：
✅ 01-提示词文档权限同步-自动化执行提示词.md
✅ 提示词文档权限更新提示词.md
✅ crud_service_endpoint代码生成提示词母版.md
✅ 专家模块 - Service和API层
✅ 专家模块 - CRUD层
✅ 用户偏好与通知 - Service和API层
✅ 用户偏好与通知 - CRUD层
✅ 用户行为模块 - Service和API层
✅ 用户行为模块 - CRUD层
✅ 内容管理模块 - Service和API层
✅ 内容管理模块 - CRUD层
```

**Step 2：读取并理解母版**
```
读取 crud_service_endpoint代码生成提示词母版.md 的 Section 5.0.5，确认包含：
✅ 三层权限职责分工（完整版）
✅ 双轨鉴权模式
✅ 权限守卫函数
✅ 404 伪装机制
✅ Repository 层权限过滤
```

**Step 3：读取并理解更新指南**
```
读取 提示词文档权限更新提示词.md，确认理解：
✅ 更新原则（有机融入原则）
✅ 每个文档的更新指南（Service和API层、CRUD层）
✅ 每个模块的特定更新内容
✅ 所有检查清单（更新前、更新后、最终）
```

**Step 4：创建执行报告**
```
创建一个空的执行报告，包含以下结构：

# 提示词文档权限同步 - 执行报告

**执行日期**: 2026-01-12
**执行人**: AI Assistant
**执行状态**: ⏳ 正在执行

---

## 执行概述

| 阶段 | 模块 | 文档数 | 已完成 | 状态 |
|------|------|---------|--------|------|
| 第一阶段 | 专家模块 | 2 | 0 | ⏳ 待执行 |
| 第二阶段 | 用户偏好与通知 | 2 | 0 | ⏳ 待执行 |
| 第三阶段 | 用户行为模块 | 2 | 0 | ⏳ 待执行 |
| 第四阶段 | 内容管理模块 | 2 | 0 | ⏳ 待执行 |

---

## 详细执行记录

（在执行过程中填充）
```

---

### 5.2. 执行阶段（按顺序执行8个文档）

#### 第一阶段：专家模块

##### 文档 1：专家模块 - Service和API层

**Step 1：读取文档**
```
读取：专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md
```

**Step 2：执行更新前检查清单**
```
按照 提示词文档权限更新提示词.md 的 Section 4.1.1 执行更新前检查清单：

✅ 文档结构检查：
  - [ ] 文档是否包含"权限管理策略"相关的 Section
  - [ ] 如果包含，是否与母版 Section 5.0.5 保持一致
  - [ ] 如果不包含，是否需要新增 Section

✅ 内容完整性检查：
  - [ ] 文档是否包含双轨鉴权模式说明
  - [ ] 文档是否包含用户身份解析规范
  - [ ] 文档是否包含 Service 层用户参数规范
  - [ ] 文档是否包含权限守卫函数说明
  - [ ] 文档是否包含 404 伪装机制说明
  - [ ] 文档是否包含 Repository 层权限过滤说明

✅ 功能权限类型检查：
  - [ ] 每个功能是否明确了权限类型（Strict Auth / Optional Auth）
  - [ ] 每个端点是否明确了权限要求（公开、需登录、仅管理员）
  - [ ] 写操作是否使用 Strict Auth
  - [ ] 读操作是否使用 Optional Auth

检查结果：
- 如果所有检查项都通过 → 继续 Step 3
- 如果有任何一个检查项未通过 → 停止，报告问题
```

**Step 3：执行文档更新**
```
按照 提示词文档权限更新提示词.md 的 Section 4.1.2 执行文档更新：

✅ Step 1：分析现有内容（已完成，在Step 2中）
✅ Step 2：确定更新位置
  - 在适当位置新增或更新 Section：##### 5.x 【新增】权限管理策略（通用权限体系）

✅ Step 3：执行更新
  - 按照母版 Section 5.0.5 的内容，有机融入权限管理策略
  - 确保包含：三层权限职责分工、双轨鉴权模式、权限守卫函数、404 伪装机制、Repository 层权限过滤
  - 根据专家模块的特点，提供具体的代码示例

✅ Step 4：更新功能权限说明
  - 遍历文档中的所有功能描述，确保每个功能都明确了：
    - 权限类型（Strict Auth / Optional Auth）
    - 权限要求（公开、需登录、仅管理员）
    - 对应的 API 层依赖（get_current_user / get_current_user_optional）
```

**Step 4：执行更新后检查清单**
```
按照 提示词文档权限更新提示词.md 的 Section 4.1.3 执行更新后检查清单：

✅ 结构完整性检查：
  - [ ] 文档是否包含完整的权限管理策略 Section
  - [ ] 权限管理策略 Section 是否包含所有 6 个核心子节：
    - [ ] 三层权限职责分工
    - [ ] 双轨鉴权模式
    - [ ] 用户身份解析规范
    - [ ] 权限守卫函数
    - [ ] 404 伪装机制
    - [ ] Repository 层权限过滤

✅ 内容一致性检查：
  - [ ] 权限管理策略是否与母版 Section 5.0.5 保持一致
  - [ ] 代码示例是否与母版保持一致
  - [ ] 字符串比较方式是否一致（`if user_role not in ['ADMIN', 'SUPERADMIN']:`）
  - [ ] 用户身份解析方式是否一致（`uuid.UUID(current_user.get("user_id"))`）

✅ 功能完整性检查：
  - [ ] 每个功能是否明确了权限类型（Strict Auth / Optional Auth）
  - [ ] 每个端点是否明确了权限要求（公开、需登录、仅管理员）
  - [ ] 写操作是否都使用 Strict Auth
  - [ ] 读操作是否都使用 Optional Auth
  - [ ] Service 层方法是否都接收 `user_id` 和 `user_role` 参数

✅ 无遗漏检查：
  - [ ] 是否有任何一个功能没有明确权限类型
  - [ ] 是否有任何一个端点没有明确权限要求
  - [ ] 是否有任何一个 Service 方法没有接收 `user_id` 和 `user_role` 参数

✅ 无过度更新检查：
  - [ ] 是否添加了任何非权限相关的内容
  - [ ] 是否修改了任何非权限相关的 Section
  - [ ] 是否有重复定义的内容

✅ 无冲突检查：
  - [ ] 新增内容是否与现有内容冲突
  - [ ] 代码示例是否与文档其他部分一致
  - [ ] 权限设计是否与模块业务逻辑冲突

检查结果：
- 如果所有检查项都通过 → 继续 Step 5
- 如果有任何一个检查项未通过 → 回滚，报告问题
```

**Step 5：记录更新情况**
```
在执行报告中记录：

### 文档 1：专家模块 - Service和API层

| 检查类型 | 检查项数 | 通过 | 失败 | 状态 |
|---------|---------|------|------|------|
| 更新前检查 | 11 | 11 | 0 | ✅ 通过 |
| 更新后检查 | 21 | 21 | 0 | ✅ 通过 |

更新内容摘要：
- 新增 Section 5.4：【新增】权限管理策略（通用权限体系）
- 包含：三层权限职责分工、双轨鉴权模式、权限守卫函数、404 伪装机制、Repository 层权限过滤
- 根据专家模块特点，提供具体代码示例
- 更新所有功能的权限类型说明（15个接口）

验证状态：✅ 通过
```

---

##### 文档 2：专家模块 - CRUD层

**重复文档 1 的所有步骤**（使用 Section 4.2 的检查清单）

---

#### 第二阶段：用户偏好与通知模块（2个文档）

**重复第一阶段的所有步骤**（文档 3、文档 4）

---

#### 第三阶段：用户行为模块（2个文档）

**重复第一阶段的文档 1 的所有步骤**（文档 5、文档 6）

---

#### 第四阶段：内容管理模块（2个文档）

**重复第一阶段的文档 1 的所有步骤**（文档 7、文档 8）

---

### 5.3. 验证阶段（所有文档更新完成后）

**Step 1：执行最终检查清单**
```
按照 提示词文档权限更新提示词.md 的 Section 7.1 执行最终检查清单：

✅ 文档完整性：
  - [ ] 所有 8 个文档都已更新
  - [ ] 每个文档都包含完整的权限管理策略 Section
  - [ ] 每个文档都包含完整的代码示例

✅ 内容一致性：
  - [ ] 所有文档的权限管理策略都与母版 Section 5.0.5 保持一致
  - [ ] 所有文档的代码示例都与母版保持一致
  - [ ] 所有文档都使用了相同的权限检查逻辑
  - [ ] 所有文档都使用了相同的用户身份解析方式

✅ 功能完整性：
  - [ ] 每个功能都明确了权限类型（Strict Auth / Optional Auth）
  - [ ] 每个端点都明确了权限要求（公开、需登录、仅管理员）
  - [ ] Service 层方法都接收 `user_id` 和 `user_role` 参数
  - [ ] CRUD 层查询方法都接收 `current_user_id` 和 `role` 参数

✅ 质量检查：
  - [ ] 没有遗漏任何应该更新的方法
  - [ ] 没有过度增加不必要的内容
  - [ ] 没有引起任何冲突和不一致
  - [ ] 所有代码示例都是正确和完整的
```

**Step 2：生成完整执行报告**
```
在执行报告中填充：

## 最终验证结果

| 检查类型 | 检查项数 | 通过 | 失败 | 状态 |
|---------|---------|------|------|------|
| 文档完整性 | 3 | 3 | 0 | ✅ 通过 |
| 内容一致性 | 4 | 4 | 0 | ✅ 通过 |
| 功能完整性 | 4 | 4 | 0 | ✅ 通过 |
| 质量检查 | 4 | 4 | 0 | ✅ 通过 |

---

## 总体统计

| 阶段 | 模块 | 文档数 | 检查项数 | 通过 | 失败 | 状态 |
|------|------|---------|---------|------|------|------|
| 第一阶段 | 专家模块 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第二阶段 | 用户偏好与通知 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第三阶段 | 用户行为模块 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第四阶段 | 内容管理模块 | 2 | 64 | 64 | 0 | ✅ 通过 |
| **总计** | **4个模块** | **8** | **256** | **256** | **0** | **✅ 全部通过** |

---

## 执行结论

✅ **所有8个提示词文档都已成功更新**
✅ **所有检查清单都已通过**
✅ **所有文档的权限管理策略都与母版 Section 5.0.5 保持一致**
✅ **没有遗漏任何检查项**
✅ **没有过度更新任何内容**
✅ **没有引起任何冲突和不一致**

**执行状态**: ✅ 全部完成
**执行日期**: 2026-01-12
**执行人**: AI Assistant
```

---

## 6. 错误处理与回滚机制

### 6.1. 错误类型

**类型 1：更新前检查失败**
- 症状：更新前检查清单中任何一个检查项未通过
- 处理：停止执行，报告问题，等待用户确认

**类型 2：更新后检查失败**
- 症状：更新后检查清单中任何一个检查项未通过
- 处理：回滚当前文档的更新，报告问题，等待用户确认

**类型 3：最终检查失败**
- 症状：最终检查清单中任何一个检查项未通过
- 处理：报告所有失败的检查项，提供详细的失败原因，等待用户确认

### 6.2. 回滚机制

**自动回滚**：
- 如果更新后检查失败，AI 自动回滚当前文档的更新
- 回滚后，恢复到更新前的状态

**手动回滚**（可选）：
- 用户可以要求 AI 回滚任何已更新的文档
- AI 提供回滚命令和步骤

---

## 7. 执行记录表格

**在执行过程中，AI 必须实时填充以下表格**：

| 文档序号 | 文档名称 | 更新前检查 | 更新后检查 | 更新内容摘要 | 验证状态 |
|---------|---------|-----------|-----------|------------|---------|
| 1 | 专家模块-Service和API | ✅ 通过 | ✅ 通过 | 新增权限管理策略 Section，15个接口明确权限类型 | ✅ 通过 |
| 2 | 专家模块-CRUD | ✅ 通过 | ✅ 通过 | 新增权限参数到3个查询方法 | ✅ 通过 |
| 3 | 用户偏好与通知-Service和API | ✅ 通过 | ✅ 通过 | 新增权限管理策略 Section，10个接口明确权限类型 | ✅ 通过 |
| 4 | 用户偏好与通知-CRUD | ✅ 通过 | ✅ 通过 | 新增权限参数到3个查询方法 | ✅ 通过 |
| 5 | 用户行为模块-Service和API | ✅ 通过 | ✅ 通过 | 新增权限管理策略 Section，11个接口明确权限类型 | ✅ 通过 |
| 6 | 用户行为模块-CRUD | ✅ 通过 | ✅ 通过 | 新增权限参数到3个查询方法 | ✅ 通过 |
| 7 | 内容管理模块-Service和API | ✅ 通过 | ✅ 通过 | 新增权限管理策略 Section，13个接口明确权限类型 | ✅ 通过 |
| 8 | 内容管理模块-CRUD | ✅ 通过 | ✅ 通过 | 新增权限参数到4个查询方法 | ✅ 通过 |

---

## 8. 成功标准

**执行成功的标准**：

1. ✅ 所有8个文档都已更新
2. ✅ 所有文档的权限管理策略都与母版 Section 5.0.5 保持一致
3. ✅ 所有检查清单都已通过（256个检查项）
4. ✅ 没有遗漏任何应该更新的内容
5. ✅ 没有过度更新任何内容
6. ✅ 没有引起任何冲突和不一致
7. ✅ 所有代码示例都是正确和完整的
8. ✅ 生成了完整的执行报告

**如果任何一项未满足，则认为执行失败，需要回滚或重新执行。**

---

## 9. 输出格式

**AI 在执行过程中的输出格式**：

### 执行开始

```
# 提示词文档权限同步 - 执行开始

**执行日期**: 2026-01-12
**执行人**: AI Assistant

## 准备阶段

✅ Step 1: 确认所有文档都已引用
✅ Step 2: 读取并理解母版
✅ Step 3: 读取并理解更新指南
✅ Step 4: 创建执行报告

准备完成，开始执行...
```

### 每个文档的执行

```
## 第一阶段：专家模块

### 文档 1：专家模块 - Service和API层

✅ Step 1: 读取文档
✅ Step 2: 执行更新前检查清单（11个检查项，11个通过）
✅ Step 3: 执行文档更新
✅ Step 4: 执行更新后检查清单（21个检查项，21个通过）
✅ Step 5: 记录更新情况

文档 1 执行完成，状态：✅ 通过
```

### 执行完成

```
## 执行完成

所有8个文档都已成功更新！

### 总体统计

| 阶段 | 文档数 | 检查项数 | 通过 | 失败 | 状态 |
|------|--------|---------|------|------|------|
| 第一阶段 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第二阶段 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第三阶段 | 2 | 64 | 64 | 0 | ✅ 通过 |
| 第四阶段 | 2 | 64 | 64 | 0 | ✅ 通过 |
| **总计** | **8** | **256** | **256** | **0** | **✅ 全部通过** |

### 执行结论

✅ **所有8个提示词文档都已成功更新**
✅ **所有检查清单都已通过**
✅ **所有文档的权限管理策略都与母版 Section 5.0.5 保持一致**
✅ **没有遗漏任何检查项**
✅ **没有过度更新任何内容**
✅ **没有引起任何冲突和不一致**

**执行状态**: ✅ 全部完成
**执行日期**: 2026-01-12
```

---

## 10. 执行命令示例

**在 Cursor 中执行**：

```
@docs/提示词/01-提示词文档权限同步-自动化执行提示词.md
@docs/提示词/提示词文档权限更新提示词.md
@docs/提示词/自动化后端代码生成/crud_service_endpoint代码生成提示词母版.md
@docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户偏好与通知---Service和API端点代码生成提示词.md
@docs/提示词/直播核心功能模块/用户偏好与通知---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---CRUD层代码生成提示词.md
@docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---Service层和API层代码生成提示词.md
@docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---CRUD层代码生成提示词.md

请按照上述提示词文档的指引，依次更新所有8个模块提示词文档，确保它们与母版 Section 5.0.5 的权限管理策略保持完全一致，并严格执行所有检查清单。
```

---

**文档版本**: V1.0
**创建日期**: 2026-01-12
**执行类型**: 一次性执行
**状态**: 准备就绪

