# 用户模块 - 配置与安全优化实施指南

**文档版本**: v1.0  
**创建日期**: 2025-12-22  
**适用范围**: `live-streaming-saas/backend/users/`  
**基于文档**: 
- `用户模块设计文档---配置与安全优化方案.md`
- `用户模块设计文档authing版+权限设计版.md`

---

## 📋 目录

1. [概述](#1-概述)
2. [实施步骤](#2-实施步骤)
   - [P0: 创建配置管理模块](#p0-创建配置管理模块)
   - [P1: 移除敏感信息硬编码](#p1-移除敏感信息硬编码)
   - [P2: 优化CORS配置](#p2-优化cors配置)
   - [P3: 创建环境变量模板](#p3-创建环境变量模板)
   - [P4: 安全加固](#p4-安全加固)
3. [测试验证](#3-测试验证)
4. [验证清单](#4-验证清单)
5. [回滚方案](#5-回滚方案)

---

## 1. 概述

### 1.1 目标

本实施指南旨在为用户模块实施配置与安全优化，确保：
- 所有敏感信息（数据库密码、JWT密钥、Authing密钥）从代码中移除
- CORS配置可通过环境变量灵活管理
- 配置管理集中统一，支持不同环境
- 日志和错误消息不泄露敏感信息

### 1.2 优先级

| 优先级 | 任务内容 | 风险等级 | 预计时间 |
|-------|---------|---------|----------|
| **P0** | 创建配置管理模块 | 🟢 低 | 15分钟 |
| **P1** | 移除敏感信息硬编码 | 🔴 高 | 30分钟 |
| **P2** | 优化CORS配置 | 🟡 中 | 15分钟 |
| **P3** | 创建环境变量模板 | 🟢 低 | 15分钟 |
| **P4** | 安全加固 | 🟡 中 | 30分钟 |

**总预计时间**: 约 1.5 小时

### 1.3 当前问题清单

| 文件 | 问题 | 风险等级 |
|------|------|---------|
| `app/main.py` | CORS origins 硬编码 | 🟡 中 |
| `app/database.py` | 数据库密码硬编码 "CHANGE_ME" | 🔴 高 |
| `app/api/v1/deps.py` | JWT密钥硬编码 "your-super-secret-key-here" | 🔴 高 |
| `app/services/auth_service.py` | JWT密钥硬编码 "your-super-secret-key-here" | 🔴 高 |
| `app/services/auth_service.py` | Authing配置硬编码 | 🔴 高 |
| 缺失 | 无 `.env.example` 模板 | 🟡 中 |
| 缺失 | 无配置验证机制 | 🟡 中 |
| 缺失 | 无日志脱敏机制 | 🟡 中 |

---

## 2. 实施步骤

### P0: 创建配置管理模块

#### P0-1: 创建 app/core/config.py

**文件**: `backend/users/app/core/config.py`  
**状态**: ⏳ 待创建

**完整代码**:

```python
"""
Users Service - Application Configuration

配置管理模块
"""

import os
import warnings
from typing import List


class Settings:
    """应用配置类"""
    
    # 环境标识
    ENVIRONMENT: str = os.getenv("ENVIRONMENT", "development")
    
    # 项目基本信息
    PROJECT_NAME: str = "Users Service"
    VERSION: str = "1.0.0"
    DESCRIPTION: str = "用户服务API"
    
    # API配置
    API_V1_STR: str = "/api/v1"
    
    # 数据库配置
    POSTGRES_SERVER: str = os.getenv("POSTGRES_SERVER", "localhost")
    POSTGRES_USER: str = os.getenv("POSTGRES_USER", "postgres")
    # ⚠️ 生产环境必须设置此环境变量，不允许使用默认值
    POSTGRES_PASSWORD: str = os.getenv("POSTGRES_PASSWORD", "")
    POSTGRES_DB: str = os.getenv("POSTGRES_DB", "users_service_test")
    POSTGRES_PORT: str = os.getenv("POSTGRES_PORT", "5432")

    @property
    def DATABASE_URL(self) -> str:
        """异步数据库连接URL"""
        return f"postgresql+asyncpg://{self.POSTGRES_USER}:{self.POSTGRES_PASSWORD}@{self.POSTGRES_SERVER}:{self.POSTGRES_PORT}/{self.POSTGRES_DB}"
    
    @property
    def SYNC_DATABASE_URL(self) -> str:
        """同步数据库连接URL"""
        return f"postgresql://{self.POSTGRES_USER}:{self.POSTGRES_PASSWORD}@{self.POSTGRES_SERVER}:{self.POSTGRES_PORT}/{self.POSTGRES_DB}"
    
    # CORS配置
    # 从环境变量读取，支持逗号分隔的多个域名
    # 格式: "http://localhost:5174,https://example.com"
    _CORS_ORIGINS_STR: str = os.getenv("CORS_ORIGINS", "*")
    
    @property
    def BACKEND_CORS_ORIGINS(self) -> List[str]:
        """CORS允许的源列表"""
        return [
            origin.strip() 
            for origin in self._CORS_ORIGINS_STR.split(",") 
            if origin.strip()
        ]
    
    # JWT配置
    JWT_SECRET_KEY: str = os.getenv("JWT_SECRET_KEY", "")
    JWT_ALGORITHM: str = os.getenv("JWT_ALGORITHM", "HS256")
    
    # Authing配置（可选）
    VITE_CLIENT_ID: str = os.getenv("VITE_CLIENT_ID", "")
    USER_POOL_SECRET: str = os.getenv("USER_POOL_SECRET", "")
    APP_HOST: str = os.getenv("APP_HOST", "")
    REDIRECT_URL: str = os.getenv("REDIRECT_URL", "")
    ISSUER: str = os.getenv("ISSUER", "")
    
    # Redis配置
    REDIS_HOST: str = os.getenv("REDIS_HOST", "localhost")
    REDIS_PORT: int = int(os.getenv("REDIS_PORT", "6379"))
    REDIS_PASSWORD: str = os.getenv("REDIS_PASSWORD", "")
    REDIS_DB: int = int(os.getenv("REDIS_DB", "0"))
    
    # 调试模式
    DEBUG: bool = os.getenv("DEBUG", "true").lower() == "true"

    def __init__(self):
        """初始化配置并验证"""
        # 根据环境决定验证严格程度
        if self.ENVIRONMENT == "production":
            self._validate_production_config()
        else:
            self._validate_development_config()
    
    def _validate_production_config(self):
        """生产环境严格验证"""
        errors = []
        
        # 验证数据库配置
        if not self.POSTGRES_PASSWORD:
            errors.append("❌ POSTGRES_PASSWORD 环境变量未设置")
        elif self.POSTGRES_PASSWORD == "CHANGE_ME":
            errors.append("❌ 生产环境不允许使用默认数据库密码 'CHANGE_ME'")
        
        # 验证JWT配置
        if not self.JWT_SECRET_KEY:
            errors.append("❌ JWT_SECRET_KEY 环境变量未设置")
        elif self.JWT_SECRET_KEY == "your-super-secret-key-here":
            errors.append("❌ 生产环境不允许使用默认JWT密钥 'your-super-secret-key-here'")
        elif len(self.JWT_SECRET_KEY) < 32:
            errors.append(f"❌ JWT_SECRET_KEY 长度不足32个字符（当前：{len(self.JWT_SECRET_KEY)}）")
        
        # 验证CORS配置
        if "*" in self.BACKEND_CORS_ORIGINS:
            errors.append("❌ 生产环境CORS不允许使用 '*'，必须指定具体域名")
        
        # 验证DEBUG模式
        if self.DEBUG:
            errors.append("❌ 生产环境必须关闭DEBUG模式（设置 DEBUG=false）")
        
        if errors:
            raise ValueError(
                "\n\n" + "="*60 + "\n" +
                "生产环境配置验证失败：\n" + 
                "\n".join(errors) +
                "\n" + "="*60 + "\n"
            )
    
    def _validate_development_config(self):
        """开发环境基础验证（使用警告而非错误）"""
        warnings_list = []
        
        if not self.POSTGRES_PASSWORD:
            warnings_list.append("⚠️  POSTGRES_PASSWORD 未设置，数据库连接可能失败")
            warnings_list.append("    💡 提示：请复制 .env.example 为 .env 并填写配置")
        elif self.POSTGRES_PASSWORD == "CHANGE_ME":
            warnings_list.append("⚠️  使用默认数据库密码 'CHANGE_ME'，建议在 .env 文件中设置自定义密码")
        
        if not self.JWT_SECRET_KEY:
            warnings_list.append("⚠️  JWT_SECRET_KEY 未设置，JWT认证将无法工作")
            warnings_list.append("    💡 提示：运行 'openssl rand -hex 32' 生成密钥")
        elif self.JWT_SECRET_KEY == "your-super-secret-key-here":
            warnings_list.append("⚠️  使用默认JWT密钥 'your-super-secret-key-here'，建议在 .env 文件中设置自定义密钥")
        elif len(self.JWT_SECRET_KEY) < 32:
            warnings_list.append(f"⚠️  JWT_SECRET_KEY 长度不足32个字符（当前：{len(self.JWT_SECRET_KEY)}），建议使用更长的密钥")
        
        if warnings_list:
            first_run_hint = ""
            if not self.POSTGRES_PASSWORD or not self.JWT_SECRET_KEY:
                first_run_hint = "\n\n💡 首次运行？请执行:\n   1. cp .env.example .env\n   2. 编辑 .env 文件填写配置\n"
            
            warning_message = (
                "\n" + "="*60 + 
                "\n⚠️  开发环境配置警告：\n" + 
                "\n".join(warnings_list) + 
                first_run_hint +
                "="*60
            )
            warnings.warn(warning_message, UserWarning)


# 创建全局配置实例
settings = Settings()
```

**说明**:
- 集中管理所有配置项
- 移除所有敏感信息的默认值
- 支持环境区分验证（生产环境强制验证，开发环境友好警告）
- 提供动态属性（DATABASE_URL, BACKEND_CORS_ORIGINS）

---

### P1: 移除敏感信息硬编码

#### P1-1: 修改 app/database.py

**文件**: `backend/users/app/database.py`  
**状态**: ⏳ 待修改

**修改内容**:

1. 导入settings配置
2. 使用settings替代直接读取环境变量
3. 移除敏感信息的日志输出

**修改后代码**:

```python
"""
数据库配置和连接管理
"""
from sqlalchemy import create_engine
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
import logging

# 导入配置
from app.core.config import settings

# 配置日志
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# 创建数据库引擎
try:
    # 使用配置模块的连接URL
    logger.info("正在初始化数据库连接...")

    # 同步引擎（用于初始化）
    engine = create_engine(
        settings.SYNC_DATABASE_URL,
        echo=settings.DEBUG  # 根据DEBUG模式决定是否打印SQL
    )

    # 异步引擎（用于应用）
    async_engine = create_async_engine(
        settings.DATABASE_URL,
        echo=settings.DEBUG  # 根据DEBUG模式决定是否打印SQL
    )

    # 测试连接
    with engine.connect() as conn:
        logger.info("数据库连接成功！")
except Exception as e:
    logger.error(f"数据库连接失败: {type(e).__name__}")
    raise

# 创建会话工厂
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

# 创建异步会话工厂
AsyncSessionLocal = sessionmaker(
    bind=async_engine,
    class_=AsyncSession,
    autocommit=False,
    autoflush=False
)

# 创建基类
Base = declarative_base()

# 获取数据库会话（同步）
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# 获取异步数据库会话
async def get_async_db() -> AsyncSession:
    async with AsyncSessionLocal() as session:
        try:
            yield session
            if session.is_active:
                await session.commit()  # 显式提交事务
        except Exception as e:
            if session.is_active:
                await session.rollback()
            raise
        finally:
            await session.close()

# 为了保持兼容性，将get_db指向异步版本
get_db = get_async_db
```

**修改对比**:

**原代码**（第18-29行）:
```python
POSTGRES_USER = os.getenv("POSTGRES_USER", "postgres")
POSTGRES_PASSWORD = os.getenv("POSTGRES_PASSWORD", "CHANGE_ME")  # ❌ 硬编码密码
POSTGRES_SERVER = os.getenv("POSTGRES_SERVER", "localhost")
POSTGRES_PORT = os.getenv("POSTGRES_PORT", "5432")
POSTGRES_DB = os.getenv("POSTGRES_DB", "users_service_test")

DATABASE_URL = f"postgresql://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_SERVER}:{POSTGRES_PORT}/{POSTGRES_DB}"
ASYNC_DATABASE_URL = f"postgresql+asyncpg://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_SERVER}:{POSTGRES_PORT}/{POSTGRES_DB}"

logger.info(f"尝试连接数据库: {DATABASE_URL}")  # ❌ 泄露密码
```

**修改后**:
```python
from app.core.config import settings

logger.info("正在初始化数据库连接...")  # ✅ 不泄露密码

engine = create_engine(settings.SYNC_DATABASE_URL, echo=settings.DEBUG)
async_engine = create_async_engine(settings.DATABASE_URL, echo=settings.DEBUG)
```

---

#### P1-2: 修改 app/api/v1/deps.py

**文件**: `backend/users/app/api/v1/deps.py`  
**状态**: ⏳ 待修改

**修改内容**:

1. 导入settings配置
2. 使用settings.JWT_SECRET_KEY替代硬编码

**修改后代码** (第1-30行):

```python
"""
用户功能服务 - 依赖注入层
提供可复用的依赖项，特别是用于用户认证的依赖
"""
import logging
import uuid

import jwt
from typing import Optional
from jwt import ExpiredSignatureError, PyJWTError

from fastapi import Depends, HTTPException
from fastapi.security import OAuth2PasswordBearer
from sqlalchemy.ext.asyncio import AsyncSession

from app.database import get_async_db
from app.core.redis_client import get_redis_client
from app.core.config import settings  # ✅ 导入配置
from app.crud import crud_user
from app.models.users import User, EntityStatus

logger = logging.getLogger(__name__)

# OAuth2方案定义
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")

# JWT配置 - 从配置模块读取
JWT_SECRET_KEY = settings.JWT_SECRET_KEY
JWT_ALGORITHM = settings.JWT_ALGORITHM
```

**修改对比**:

**原代码**（第28-29行）:
```python
JWT_SECRET_KEY = os.getenv("JWT_SECRET_KEY", "your-super-secret-key-here")  # ❌ 硬编码密钥
JWT_ALGORITHM = "HS256"
```

**修改后**:
```python
from app.core.config import settings

JWT_SECRET_KEY = settings.JWT_SECRET_KEY  # ✅ 从配置读取
JWT_ALGORITHM = settings.JWT_ALGORITHM
```

---

#### P1-3: 修改 app/services/auth_service.py

**文件**: `backend/users/app/services/auth_service.py`  
**状态**: ⏳ 待修改

**修改内容**:

1. 导入settings配置
2. 移除JWT密钥硬编码
3. 移除Authing配置硬编码

**修改后代码** (第1-120行):

```python
"""
认证服务层 - AuthService
封装所有认证相关的业务逻辑，包括验证码、登录、JWT管理等
"""
import re
import uuid
import base64
import hashlib
import logging
import random
import string
import os
import secrets
from datetime import datetime, timedelta
from typing import Optional, Dict, Any

import jwt
from jwt import PyJWKClient
from jwt import InvalidTokenError, ExpiredSignatureError

from sqlalchemy.ext.asyncio import AsyncSession

from app.core.redis_client import get_redis_client
from app.core.config import settings  # ✅ 导入配置
from app.crud import crud_user
from app.models.users import EntityStatus
from app import schemas
from app.exceptions import InvalidTokenException, InvalidCredentialsException

# Authing SDK导入
from authing import AuthenticationClient

# 条件导入captcha库
try:
    from captcha.image import ImageCaptcha
    CAPTCHA_AVAILABLE = True
except ImportError:
    CAPTCHA_AVAILABLE = False

logger = logging.getLogger(__name__)


class CaptchaErrorException(Exception):
    """图形验证码错误异常"""
    pass


class RateLimitException(Exception):
    """频率限制异常"""
    pass


class UserAlreadyExistsException(Exception):
    """用户已存在异常"""
    pass


class InvalidCredentialsException(Exception):
    """无效凭证异常"""
    pass


class InvalidTokenException(Exception):
    """无效令牌异常"""
    pass


class InvalidResetTokenException(Exception):
    """无效重置令牌异常"""
    pass


class WeakPasswordException(Exception):
    """密码强度不足异常"""
    pass


class AuthService:
    """认证服务类"""

    def __init__(self, db: AsyncSession, redis_client=None):
        """
        初始化认证服务

        Args:
            db: 数据库会话
            redis_client: Redis客户端（可选，如果不提供会获取默认客户端）
        """
        self.db = db
        self.redis_client = redis_client or get_redis_client()

        # JWT配置 - 从配置模块读取
        self.jwt_secret_key = settings.JWT_SECRET_KEY
        self.jwt_algorithm = settings.JWT_ALGORITHM

        # Authing 客户端配置 - 从配置模块读取
        try:
            app_id = settings.VITE_CLIENT_ID
            app_secret = settings.USER_POOL_SECRET
            app_host = settings.APP_HOST
            redirect_url = settings.REDIRECT_URL
            
            if app_id and app_secret:
                self.auth_client = AuthenticationClient(
                    app_id=app_id,
                    app_secret=app_secret,
                    app_host=app_host,
                    redirect_uri=redirect_url
                )
                logger.info("Authing 客户端初始化成功")
            else:
                logger.warning("Authing 配置缺失（VITE_CLIENT_ID 或 USER_POOL_SECRET 未设置），SSO功能将不可用")
                self.auth_client = None
        except Exception as e:
            logger.error(f"Authing 客户端初始化失败: {e}")
            self.auth_client = None
```

**修改对比**:

**原代码**（第94-116行）:
```python
# JWT配置
self.jwt_secret_key = os.getenv("JWT_SECRET_KEY", "your-super-secret-key-here")  # ❌ 硬编码
self.jwt_algorithm = "HS256"

# Authing 客户端配置
try:
    app_id = os.getenv("VITE_CLIENT_ID") or "689e762956a8a5df2b154759"  # ❌ 硬编码
    app_secret = os.getenv("USER_POOL_SECRET") or "aedd53bf78ac1c2078bf1b2c7dd8ffdd"  # ❌ 硬编码
    app_host= os.getenv("APP_HOST")
    redirect_url = os.getenv("REDIRECT_URL")
```

**修改后**:
```python
from app.core.config import settings

# JWT配置 - 从配置模块读取
self.jwt_secret_key = settings.JWT_SECRET_KEY  # ✅ 从配置读取
self.jwt_algorithm = settings.JWT_ALGORITHM

# Authing 客户端配置 - 从配置模块读取
app_id = settings.VITE_CLIENT_ID  # ✅ 从配置读取
app_secret = settings.USER_POOL_SECRET
app_host = settings.APP_HOST
redirect_url = settings.REDIRECT_URL
```

同时修改 `sso_login` 方法中的配置读取（第550-551行）:

**原代码**:
```python
audience = os.getenv("VITE_CLIENT_ID")
issuer = os.getenv("ISSUER")
```

**修改后**:
```python
audience = settings.VITE_CLIENT_ID
issuer = settings.ISSUER
```

---

### P2: 优化CORS配置

#### P2-1: 修改 app/main.py

**文件**: `backend/users/app/main.py`  
**状态**: ⏳ 待修改

**修改内容**:

1. 导入settings配置
2. 移除硬编码的CORS origins
3. 使用settings.BACKEND_CORS_ORIGINS

**修改后代码**:

```python
import os

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

# 导入配置
from app.core.config import settings

app = FastAPI(
    title=settings.PROJECT_NAME,
    version=settings.VERSION,
    description=settings.DESCRIPTION
)

# 添加CORS中间件
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.BACKEND_CORS_ORIGINS,  # 从配置读取
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
    expose_headers=["*"],
)

# 导入路由
from app.api.v1.api import api_router
app.include_router(api_router, prefix=settings.API_V1_STR)

@app.get("/")
async def root():
    return {
        "message": f"欢迎使用{settings.PROJECT_NAME}",
        "version": settings.VERSION,
        "docs_url": "/docs",
        "redoc_url": "/redoc"
    }
```

**修改对比**:

**原代码**（第10-26行）:
```python
# 本地开发环境允许的源
local_origins = [
    "http://localhost:5174",
    "http://127.0.0.1:5174",
    "http://localhost:5173",
    "http://127.0.0.1:5173",
    "http://localhost:9500",
    "http://127.0.0.1:9500",
]

# 生产环境允许的源（从环境变量读取，如果没有则使用默认值）
production_origins = os.getenv(
    "CORS_ALLOWED_ORIGINS",
    "https://mp.dayilive.com,https://www.mp.dayilive.com,https://mp2.dayilive.com,https://www.mp2.dayilive.com"
).split(",")

# 合并所有允许的源
origins = local_origins + [origin.strip() for origin in production_origins if origin.strip()]
```

**修改后**:
```python
from app.core.config import settings

# 从配置读取CORS origins
allow_origins=settings.BACKEND_CORS_ORIGINS
```

---

### P3: 创建环境变量模板

#### P3-1: 创建 .env.example

**文件**: `backend/users/.env.example`  
**状态**: ⏳ 待创建

**完整内容**:

```bash
# ============================================
# 用户服务环境变量配置模板
# ============================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 填写实际的值
# 3. 确保 .env 文件已添加到 .gitignore

# ============================================
# 环境标识
# ============================================
ENVIRONMENT=development  # development | production

# ============================================
# 数据库配置（必需）
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_database_password_here
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=users_service_test

# ============================================
# JWT配置（必需）
# ============================================
# 生成强密钥：openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_min_32_chars
JWT_ALGORITHM=HS256

# ============================================
# Authing SSO配置（可选，如果不需要SSO功能可以不设置）
# ============================================
VITE_CLIENT_ID=your_authing_app_id
USER_POOL_SECRET=your_authing_app_secret
APP_HOST=https://your-authing-host.com
REDIRECT_URL=https://your-app.com/api/v1/auth/callback
ISSUER=https://your-authing-host.com/oidc

# ============================================
# CORS配置
# ============================================
# CORS允许的源（逗号分隔的多个域名）
# 开发环境示例: CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174,http://localhost:5173,https://mp.dayilive.com
# 生产环境示例: CORS_ORIGINS=https://mp.dayilive.com,https://www.mp.dayilive.com
# ⚠️ 生产环境不允许使用 *，必须设置具体的域名列表
CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174,http://localhost:5173,http://127.0.0.1:5173,http://localhost:9500,http://127.0.0.1:9500

# ============================================
# Redis配置
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# 调试模式
# ============================================
DEBUG=true  # true | false

# ============================================
# API配置
# ============================================
API_V1_STR=/api/v1
```

---

#### P3-2: 更新 .gitignore

**文件**: `live-streaming-saas/.gitignore` (项目根目录)  
**状态**: ⏳ 待检查/更新

**确保包含以下内容**:

```gitignore
# 环境变量文件
.env
.env.local
.env.development
.env.production
.env.*.local

# 但保留模板文件
!.env.example
```

**验证步骤**:
```bash
cd live-streaming-saas
grep -E "^\.env$|^\.env\.\*" .gitignore
```

---

### P4: 安全加固

#### P4-1: 创建日志脱敏工具

**文件**: `backend/users/app/core/logging_utils.py`  
**状态**: ⏳ 待创建

**完整代码**:

```python
"""
日志脱敏工具

提供日志消息脱敏功能，防止敏感信息泄露
"""

import re
import logging


def sanitize_log_message(message: str) -> str:
    """对日志消息进行脱敏处理"""
    
    # 移除数据库连接URL中的密码
    message = re.sub(
        r'postgresql[+a-z]*://([^:]+):([^@]+)@',
        r'postgresql://\1:***@',
        message,
        flags=re.IGNORECASE
    )
    
    # 移除JWT密钥（如果意外出现在日志中）
    message = re.sub(
        r'JWT_SECRET_KEY["\']?\s*[:=]\s*["\']?([^"\'\s]+)',
        r'JWT_SECRET_KEY="***"',
        message,
        flags=re.IGNORECASE
    )
    
    # 移除Authing密钥（如果意外出现在日志中）
    message = re.sub(
        r'(VITE_CLIENT_ID|USER_POOL_SECRET)["\']?\s*[:=]\s*["\']?([^"\'\s]+)',
        r'\1="***"',
        message,
        flags=re.IGNORECASE
    )
    
    # 移除密码字段
    message = re.sub(
        r'(password|passwd|pwd)["\']?\s*[:=]\s*["\']?([^"\'\s]+)',
        r'\1="***"',
        message,
        flags=re.IGNORECASE
    )
    
    return message


class SanitizedFormatter(logging.Formatter):
    """脱敏日志格式化器"""
    
    def format(self, record):
        # 对日志消息进行脱敏
        original_msg = record.msg
        if isinstance(original_msg, str):
            record.msg = sanitize_log_message(original_msg)
        
        # 调用父类格式化
        formatted = super().format(record)
        
        # 恢复原始消息
        record.msg = original_msg
        
        return formatted


def setup_sanitized_logging(
    level=logging.INFO,
    format_string='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
):
    """
    设置带脱敏功能的日志配置
    
    Args:
        level: 日志级别
        format_string: 日志格式字符串
    """
    # 获取根日志记录器
    root_logger = logging.getLogger()
    root_logger.setLevel(level)
    
    # 移除现有的处理器
    for handler in root_logger.handlers[:]:
        root_logger.removeHandler(handler)
    
    # 创建控制台处理器
    console_handler = logging.StreamHandler()
    console_handler.setLevel(level)
    
    # 使用脱敏格式化器
    formatter = SanitizedFormatter(format_string)
    console_handler.setFormatter(formatter)
    
    # 添加处理器
    root_logger.addHandler(console_handler)
    
    return root_logger
```

---

#### P4-2: 创建健康检查端点

**文件**: `backend/users/app/api/v1/endpoints/health.py`  
**状态**: ⏳ 待创建

**完整代码**:

```python
"""
健康检查端点

提供服务健康状态检查、数据库连接检查等功能
"""

from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession
from typing import Dict, Any

from app.database import get_async_db
from app.core.config import settings
from app.core.redis_client import get_redis_client

router = APIRouter()


@router.get("/", response_model=Dict[str, str])
async def health_check():
    """
    基本健康检查
    
    Returns:
        服务状态
    """
    return {
        "status": "healthy",
        "service": settings.PROJECT_NAME,
        "version": settings.VERSION
    }


@router.get("/ready", response_model=Dict[str, Any])
async def readiness_check(db: AsyncSession = Depends(get_async_db)):
    """
    就绪检查（包含数据库和Redis连接检查）
    
    Args:
        db: 数据库会话
        
    Returns:
        详细的就绪状态
        
    Raises:
        HTTPException: 如果服务未就绪
    """
    checks = {
        "database": False,
        "redis": False
    }
    
    # 检查数据库连接
    try:
        await db.execute(text("SELECT 1"))
        checks["database"] = True
    except Exception as e:
        raise HTTPException(
            status_code=503,
            detail=f"数据库连接失败: {type(e).__name__}"
        )
    
    # 检查Redis连接
    try:
        redis_client = get_redis_client()
        await redis_client.ping()
        checks["redis"] = True
    except Exception as e:
        raise HTTPException(
            status_code=503,
            detail=f"Redis连接失败: {type(e).__name__}"
        )
    
    return {
        "status": "ready",
        "checks": checks
    }


@router.get("/config", response_model=Dict[str, Any])
async def config_check():
    """
    配置检查（用于验证关键配置是否正确设置）
    
    ⚠️ 注意：此端点仅用于开发环境调试，生产环境应禁用
    
    Returns:
        配置状态（不包含敏感值）
    """
    if settings.ENVIRONMENT == "production":
        raise HTTPException(
            status_code=403,
            detail="生产环境不允许访问配置检查端点"
        )
    
    return {
        "environment": settings.ENVIRONMENT,
        "debug": settings.DEBUG,
        "database_configured": bool(settings.POSTGRES_PASSWORD),
        "jwt_configured": bool(settings.JWT_SECRET_KEY),
        "jwt_key_length": len(settings.JWT_SECRET_KEY) if settings.JWT_SECRET_KEY else 0,
        "cors_origins_count": len(settings.BACKEND_CORS_ORIGINS),
        "cors_allows_all": "*" in settings.BACKEND_CORS_ORIGINS,
        "authing_configured": bool(settings.VITE_CLIENT_ID and settings.USER_POOL_SECRET),
        "redis_configured": bool(settings.REDIS_HOST)
    }
```

---

#### P4-3: 注册健康检查路由

**文件**: `backend/users/app/api/v1/api.py`  
**状态**: ⏳ 待修改

**在现有路由注册代码中添加**:

```python
from app.api.v1.endpoints import health

# 注册健康检查路由
api_router.include_router(health.router, prefix="/health", tags=["health"])
```

---

#### P4-4: 在应用启动时应用日志脱敏

**文件**: `backend/users/app/main.py`  
**状态**: ⏳ 待修改

**在文件开头添加**:

```python
# 设置日志脱敏
from app.core.logging_utils import setup_sanitized_logging
setup_sanitized_logging()
```

---

## 3. 测试验证

### 3.1 前置条件

确保已安装必要的依赖：

```bash
cd backend/users
pip install pytest pytest-asyncio python-dotenv
```

### 3.2 单元测试

#### 测试配置模块

**文件**: `backend/users/tests/test_config_security.py`  
**状态**: ⏳ 待创建

```python
"""
配置与安全测试
"""

import pytest
import os
import warnings
from unittest.mock import patch

from app.core.config import Settings
from app.core.logging_utils import sanitize_log_message


class TestConfigSecurity:
    """配置安全测试"""
    
    def test_production_config_validation_missing_password(self):
        """测试生产环境配置验证 - 缺少数据库密码"""
        with patch.dict(os.environ, {"ENVIRONMENT": "production", "POSTGRES_PASSWORD": ""}):
            with pytest.raises(ValueError) as exc_info:
                Settings()
            assert "POSTGRES_PASSWORD" in str(exc_info.value)
    
    def test_production_config_validation_default_password(self):
        """测试生产环境配置验证 - 使用默认密码"""
        with patch.dict(os.environ, {
            "ENVIRONMENT": "production",
            "POSTGRES_PASSWORD": "CHANGE_ME"
        }):
            with pytest.raises(ValueError) as exc_info:
                Settings()
            assert "CHANGE_ME" in str(exc_info.value)
    
    def test_production_config_validation_missing_jwt_key(self):
        """测试生产环境配置验证 - 缺少JWT密钥"""
        with patch.dict(os.environ, {
            "ENVIRONMENT": "production",
            "POSTGRES_PASSWORD": "secure_password",
            "JWT_SECRET_KEY": ""
        }):
            with pytest.raises(ValueError) as exc_info:
                Settings()
            assert "JWT_SECRET_KEY" in str(exc_info.value)
    
    def test_production_config_validation_short_jwt_key(self):
        """测试生产环境配置验证 - JWT密钥过短"""
        with patch.dict(os.environ, {
            "ENVIRONMENT": "production",
            "POSTGRES_PASSWORD": "secure_password",
            "JWT_SECRET_KEY": "short_key",
            "CORS_ORIGINS": "https://example.com"
        }):
            with pytest.raises(ValueError) as exc_info:
                Settings()
            assert "长度不足32个字符" in str(exc_info.value)
    
    def test_production_config_validation_wildcard_cors(self):
        """测试生产环境配置验证 - CORS使用通配符"""
        with patch.dict(os.environ, {
            "ENVIRONMENT": "production",
            "POSTGRES_PASSWORD": "secure_password",
            "JWT_SECRET_KEY": "a" * 32,
            "CORS_ORIGINS": "*"
        }):
            with pytest.raises(ValueError) as exc_info:
                Settings()
            assert "CORS" in str(exc_info.value)
    
    def test_development_config_warnings(self):
        """测试开发环境配置警告"""
        with patch.dict(os.environ, {"ENVIRONMENT": "development", "POSTGRES_PASSWORD": ""}):
            with warnings.catch_warnings(record=True) as w:
                warnings.simplefilter("always")
                Settings()
                assert len(w) > 0
                assert "POSTGRES_PASSWORD" in str(w[0].message)


class TestLogSanitization:
    """日志脱敏测试"""
    
    def test_sanitize_database_url(self):
        """测试数据库URL脱敏"""
        message = "连接到数据库: postgresql://user:mypassword123@localhost:5432/db"
        sanitized = sanitize_log_message(message)
        assert "mypassword123" not in sanitized
        assert "***" in sanitized
    
    def test_sanitize_jwt_key(self):
        """测试JWT密钥脱敏"""
        message = 'JWT_SECRET_KEY="my_super_secret_key_12345"'
        sanitized = sanitize_log_message(message)
        assert "my_super_secret_key_12345" not in sanitized
        assert "***" in sanitized
    
    def test_sanitize_authing_keys(self):
        """测试Authing密钥脱敏"""
        message = 'VITE_CLIENT_ID=689e762956a8a5df2b154759 USER_POOL_SECRET=aedd53bf78ac1c2078bf1b2c7dd8ffdd'
        sanitized = sanitize_log_message(message)
        assert "689e762956a8a5df2b154759" not in sanitized
        assert "aedd53bf78ac1c2078bf1b2c7dd8ffdd" not in sanitized


class TestCORSConfiguration:
    """CORS配置测试"""
    
    def test_cors_parse_multiple_origins(self):
        """测试CORS多域名解析"""
        with patch.dict(os.environ, {
            "CORS_ORIGINS": "http://localhost:5174,https://example.com"
        }):
            settings = Settings()
            assert len(settings.BACKEND_CORS_ORIGINS) == 2
            assert "http://localhost:5174" in settings.BACKEND_CORS_ORIGINS
            assert "https://example.com" in settings.BACKEND_CORS_ORIGINS
    
    def test_cors_strip_whitespace(self):
        """测试CORS域名空格清理"""
        with patch.dict(os.environ, {
            "CORS_ORIGINS": "http://localhost:5174 , https://example.com "
        }):
            settings = Settings()
            assert all(" " not in origin for origin in settings.BACKEND_CORS_ORIGINS)
```

### 3.3 集成测试

运行现有测试套件，确保修改后所有测试通过：

```bash
cd backend/users
pytest tests/ -v
```

### 3.4 手动验证

#### 验证1: 配置验证机制

```bash
# 1. 测试生产环境配置验证（应该失败并抛出错误）
cd backend/users
ENVIRONMENT=production python -c "from app.core.config import settings"

# 2. 测试开发环境配置（应该成功并显示警告）
cd backend/users
ENVIRONMENT=development python -c "from app.core.config import settings"
```

#### 验证2: 健康检查端点

```bash
# 启动服务
cd backend/users
python run.py

# 在另一个终端测试健康检查
curl http://localhost:8000/api/v1/health
curl http://localhost:8000/api/v1/health/ready
curl http://localhost:8000/api/v1/health/config
```

#### 验证3: 日志脱敏

```bash
# 检查日志中是否包含敏感信息
cd backend/users
python -c "from app.core.logging_utils import setup_sanitized_logging; import logging; setup_sanitized_logging(); logger = logging.getLogger(); logger.info('Database URL: postgresql://user:mypassword@localhost/db'); logger.info('JWT_SECRET_KEY=my_secret_key')"
```

---

## 4. 验证清单

### 4.1 代码检查

- [ ] `app/core/config.py` 已创建且包含所有配置项
- [ ] `app/database.py` 已移除硬编码密码并使用settings
- [ ] `app/api/v1/deps.py` 已移除硬编码JWT密钥
- [ ] `app/services/auth_service.py` 已移除所有硬编码敏感信息
- [ ] `app/main.py` 已移除硬编码CORS配置
- [ ] `.env.example` 已创建且包含所有必要配置项
- [ ] `.gitignore` 已正确配置忽略 `.env` 文件
- [ ] `app/core/logging_utils.py` 已创建日志脱敏工具
- [ ] `app/api/v1/endpoints/health.py` 已创建健康检查端点

### 4.2 功能测试

- [ ] 应用可以正常启动（开发环境）
- [ ] 应用可以正常启动（生产环境，设置所有必需环境变量后）
- [ ] 数据库连接正常
- [ ] JWT认证功能正常
- [ ] Authing SSO功能正常（如果配置）
- [ ] CORS配置生效
- [ ] 健康检查端点返回正确状态
- [ ] 所有现有测试通过

### 4.3 安全检查

- [ ] 代码中无敏感信息硬编码
- [ ] `.env` 文件未提交到版本控制
- [ ] 日志中不包含数据库密码
- [ ] 日志中不包含JWT密钥
- [ ] 日志中不包含Authing密钥
- [ ] 生产环境配置验证正常工作
- [ ] 开发环境友好警告正常显示

---

## 5. 回滚方案

如果实施过程中遇到问题，可以按以下步骤回滚：

### 5.1 回滚代码修改

```bash
cd backend/users

# 回滚所有修改
git checkout app/database.py
git checkout app/api/v1/deps.py
git checkout app/services/auth_service.py
git checkout app/main.py

# 删除新创建的文件
rm -f app/core/config.py
rm -f app/core/logging_utils.py
rm -f app/api/v1/endpoints/health.py
rm -f .env.example
rm -f tests/test_config_security.py
```

### 5.2 回滚环境变量

```bash
# 恢复原有的环境变量配置（如果有备份）
cd backend/users
cp .env.backup .env  # 如果有备份
```

### 5.3 验证回滚

```bash
# 确保应用可以正常启动
cd backend/users
python run.py

# 运行测试
pytest tests/ -v
```

---

## 附录A: 密钥生成命令

### 生成JWT密钥

```bash
# 方法1: 使用openssl
openssl rand -hex 32

# 方法2: 使用Python
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 生成数据库密码

```bash
# 生成强密码（24字符）
openssl rand -base64 24
```

---

## 附录B: 环境变量部署指南

### Docker部署

在 `docker-compose.yml` 中使用环境变量：

```yaml
services:
  users-service:
    build: ./backend/users
    env_file:
      - .env.production  # 从文件读取
    # 或者直接设置
    environment:
      - ENVIRONMENT=production
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
```

### Kubernetes部署

使用 Secret 资源：

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: users-service-secrets
type: Opaque
stringData:
  POSTGRES_PASSWORD: your_password
  JWT_SECRET_KEY: your_jwt_key
```

---

## 附录C: 故障排查

### 问题1: 应用启动失败

**错误**: `ValueError: 生产环境配置验证失败`

**解决**:
1. 检查 `ENVIRONMENT` 环境变量是否正确设置
2. 确保所有必需的环境变量已设置
3. 检查 `.env` 文件是否存在且格式正确

### 问题2: 数据库连接失败

**错误**: `数据库连接失败: OperationalError`

**解决**:
1. 检查 `POSTGRES_PASSWORD` 是否正确设置
2. 确保数据库服务正在运行
3. 检查数据库连接参数（主机、端口、数据库名）

### 问题3: JWT认证失败

**错误**: `认证凭证无效`

**解决**:
1. 检查 `JWT_SECRET_KEY` 是否正确设置
2. 确保前后端使用相同的JWT密钥
3. 检查JWT密钥长度是否至少32字符

---

**文档完成时间**: 2025-12-22  
**下一步**: 严格按照本实施指南执行配置与安全优化

