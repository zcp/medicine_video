# 媒体下载服务设计文档

## 一、规范遵循说明

本服务模块在开发过程中需要遵循以下通用规范：

### 1.1 代码规范
- 遵循 `rules.md` 中的代码规范
- 使用 Python 3.8+ 开发
- 遵循 PEP 8 规范
- 使用 4 个空格缩进
- 使用 UTF-8 编码

### 1.2 项目结构规范
- 遵循 `开发规范文档` 中的后端项目结构规范
- 模块化设计
- 清晰的目录结构

### 1.3 命名规范
- 遵循 `开发规范文档` 中的命名规范
- 类名：使用大驼峰命名法（PascalCase）
- 函数名：使用下划线命名法（snake_case）
- 变量名：使用下划线命名法（snake_case）
- 常量名：使用全大写下划线命名法（UPPER_SNAKE_CASE）

### 1.4 测试规范
- 遵循 `测试规范文档` 中的测试规范
- 单元测试覆盖率 > 80%
- 接口测试完整性
- 性能测试达标

### 1.5 日志规范
- 遵循 `开发文档规范` 中的日志规范
- 使用统一的日志格式
- 合理的日志级别
- 完整的日志内容

### 1.6 安全规范
- 遵循 `安全规范文档` 中的安全规范
- API 认证授权
- 数据安全保护
- 访问控制

### 1.7 部署规范
- 遵循 `部署文档` 中的部署规范
- 容器化部署
- 环境配置
- 监控告警

## 二、服务概述

### 1.1 服务定位
媒体下载服务是一个独立的微服务，负责处理直播平台中的媒体资源下载任务，包括视频流下载（HLS、MP4）、图片下载等功能。该服务采用异步处理方式，支持高并发下载任务处理，并提供完善的失败处理和重试机制。

### 1.2 核心功能
- HLS视频流下载
- MP4视频文件下载
- 图片资源下载
- 并发下载控制
- 断点续传
- 下载任务管理
- 资源存储管理
- 失败任务重试
- 下载状态检查
- 失败记录管理

### 1.3 技术栈选型
- 开发语言：Python 3.8+
- Web框架：FastAPI
- 任务队列：Celery + Redis
- 存储系统：本地文件系统 + 对象存储
- 数据库：PostgreSQL
- 监控：Prometheus + Grafana

## 三、系统架构

### 2.1 整体架构
```
[API网关] → [下载服务] → [任务队列] → [下载引擎]
   ↑            ↑            ↑            ↑
   |            |            |            |
[业务系统] ← [数据库] ← [缓存系统] ← [存储系统]
```

### 2.2 核心组件
1. API服务层
   - 任务管理接口
   - 下载控制接口
   - 状态查询接口
   - 资源管理接口
   - 失败任务管理接口

2. 任务调度层
   - 任务队列管理
   - 任务优先级控制
   - 任务状态管理
   - 任务重试机制
   - 失败任务恢复

3. 下载引擎层
   - m3u8解析器
   - 并发下载控制器
   - 断点续传管理器
   - 资源验证器
   - 失败检测器

4. 存储管理层
   - 本地存储管理
   - 对象存储管理
   - 文件命名管理
   - 存储空间管理
   - 失败记录管理

## 四、数据库设计

### 4.1 下载任务表（download_tasks）
```sql
CREATE TABLE download_tasks (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    video_id VARCHAR(50) NOT NULL,          -- 视频ID，格式：{liveroom_id}_{uuid4后缀}
    liveroom_id VARCHAR(20) NOT NULL,       -- 直播间ID
    liveroom_title VARCHAR(100),            -- 直播间标题
    liveroom_url VARCHAR(255),              -- 直播间URL
    video_url VARCHAR(255) NOT NULL,        -- 视频播放URL
    video_type VARCHAR(20) NOT NULL,        -- hls, mp4
    storage_path VARCHAR(255) NOT NULL,     -- 存储路径
    status VARCHAR(20) DEFAULT 'pending',   -- pending, processing, completed, failed
    progress FLOAT DEFAULT 0,               -- 下载进度（0-1）
    retry_count INT DEFAULT 0,              -- 重试次数
    last_error TEXT,                        -- 最后错误信息
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    CONSTRAINT video_id_format CHECK (video_id ~ '^[0-9]{10,16}_[a-f0-9]{4}$'),  -- 确保video_id符合命名规范
    CONSTRAINT check_status CHECK (status IN ('pending', 'processing', 'completed', 'partial_completed', 'failed', 'cancelled'))
);

-- 索引设计
CREATE INDEX idx_download_tasks_video_id ON download_tasks(video_id);
CREATE INDEX idx_download_tasks_liveroom_id ON download_tasks(liveroom_id);
CREATE INDEX idx_download_tasks_status ON download_tasks(status);
CREATE INDEX idx_download_tasks_created_at ON download_tasks(created_at);
```

### 4.2 下载分片表（download_segments）
```sql
CREATE TABLE download_segments (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    task_id UUID REFERENCES download_tasks(id) ON DELETE CASCADE,
    segment_url TEXT NOT NULL,              -- 分片URL
    segment_type VARCHAR(20) NOT NULL,      -- ts, mp4, image
    segment_index INT NOT NULL,             -- 分片序号
    status VARCHAR(20) DEFAULT 'pending',   -- pending, processing, completed, failed
    file_path VARCHAR(255),                 -- 存储路径
    file_size BIGINT,
    retry_count INT DEFAULT 0,              -- 重试次数
    error_message TEXT,                     -- 错误信息
    download_time TIMESTAMP,                -- 下载完成时间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT check_segment_type CHECK (segment_type IN ('ts', 'mp4', 'image')),
    CONSTRAINT check_segment_status CHECK (status IN ('pending', 'processing', 'completed', 'failed'))
);

-- 索引设计
CREATE INDEX idx_download_segments_task_id ON download_segments(task_id);
CREATE INDEX idx_download_segments_status ON download_segments(status);
CREATE INDEX idx_download_segments_created_at ON download_segments(created_at);
```

### 4.3 下载失败记录表（download_failures）
```sql
CREATE TABLE download_failures (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    task_id UUID REFERENCES download_tasks(id) ON DELETE CASCADE,
    segment_id UUID REFERENCES download_segments(id) ON DELETE CASCADE,
    resource_type VARCHAR(20) NOT NULL DEFAULT 'video',  -- video, image
    failure_type VARCHAR(50) NOT NULL,      -- network_error, timeout, invalid_content, storage_error, permission_error
    error_message TEXT NOT NULL,
    retry_count INT DEFAULT 0,
    next_retry_time TIMESTAMP,              -- 下次重试时间
    status VARCHAR(20) DEFAULT 'pending',   -- pending, retrying, abandoned
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT check_resource_type CHECK (resource_type IN ('video', 'image')),
    CONSTRAINT check_failure_type CHECK (failure_type IN ('network_error', 'timeout', 'invalid_content', 'storage_error', 'permission_error')),
    CONSTRAINT check_failure_status CHECK (status IN ('pending', 'retrying', 'abandoned'))
);

-- 索引设计
CREATE INDEX idx_download_failures_task_id ON download_failures(task_id);
CREATE INDEX idx_download_failures_status ON download_failures(status);
CREATE INDEX idx_download_failures_next_retry_time ON download_failures(next_retry_time);
```

### 4.4 下载配置表（download_configs）
```sql
CREATE TABLE download_configs (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    task_id UUID REFERENCES download_tasks(id) ON DELETE CASCADE,
    config_key VARCHAR(100) NOT NULL,
    config_value TEXT NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(task_id, config_key)  -- 支持每个任务配置不同参数
);

-- 示例插入语句（默认配置，可被任务覆盖）
-- INSERT INTO download_configs (task_id, config_key, config_value) VALUES (NULL, 'MAX_RETRY_COUNT', '3');
    
-- 索引设计
CREATE INDEX idx_download_configs_config_key ON download_configs(config_key);
CREATE INDEX idx_download_configs_is_active ON download_configs(is_active);
```

### 4.5 下载视频数据表（downloaded_videos）
```sql
CREATE TABLE downloaded_videos (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    video_id VARCHAR(50) NOT NULL,          -- 视频ID，格式：{liveroom_id}_{uuid4后缀}
    liveroom_id VARCHAR(20) NOT NULL,       -- 直播间ID
    liveroom_title VARCHAR(100),            -- 直播间标题
    liveroom_url VARCHAR(255),              -- 直播间URL
    video_type VARCHAR(20) NOT NULL,        -- hls, mp4
    video_url VARCHAR(255) NOT NULL,        -- 原始视频URL
    storage_path VARCHAR(255) NOT NULL,     -- 存储路径，遵循存储目录结构规范
    file_size BIGINT,                       -- 文件大小（字节）
    duration INT,                           -- 视频时长（秒）
    resolution VARCHAR(20),                 -- 视频分辨率
    format VARCHAR(20),                     -- 视频格式
    cover_url VARCHAR(255),                 -- 封面图片URL
    cover_path VARCHAR(255),                -- 封面图片存储路径
    download_task_id UUID REFERENCES download_tasks(id) ON DELETE SET NULL,  -- 关联的下载任务ID
    status VARCHAR(20) NOT NULL,            -- 状态：completed, partial_completed, failed
    download_start_time TIMESTAMP,          -- 开始下载时间
    download_end_time TIMESTAMP,            -- 完成下载时间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(video_id, liveroom_id),          -- 确保同一视频在同一直播间只下载一次
    CONSTRAINT video_id_format CHECK (video_id ~ '^[0-9]{10,16}_[a-f0-9]{4}$'),  -- 确保video_id符合命名规范
    CONSTRAINT check_video_type CHECK (video_type IN ('hls', 'mp4')),
    CONSTRAINT check_video_status CHECK (status IN ('completed', 'partial_completed', 'failed'))
);

-- 索引设计
CREATE INDEX idx_downloaded_videos_video_id ON downloaded_videos(video_id);
CREATE INDEX idx_downloaded_videos_liveroom_id ON downloaded_videos(liveroom_id);
CREATE INDEX idx_downloaded_videos_status ON downloaded_videos(status);
CREATE INDEX idx_downloaded_videos_download_task_id ON downloaded_videos(download_task_id);
CREATE INDEX idx_downloaded_videos_created_at ON downloaded_videos(created_at);
```

### 4.6 下载视频分片数据表（downloaded_video_segments）
```sql
CREATE TABLE downloaded_video_segments (
    id UUID PRIMARY KEY DEFAULT uuid.uuid4,
    video_id UUID REFERENCES downloaded_videos(id) ON DELETE CASCADE,
    segment_type VARCHAR(20) NOT NULL,      -- ts, mp4, image
    segment_url VARCHAR(255) NOT NULL,      -- 分片URL
    segment_index INT NOT NULL,             -- 分片序号
    file_path VARCHAR(255) NOT NULL,        -- 存储路径
    file_size BIGINT,                       -- 文件大小
    duration INT,                           -- 分片时长（秒）
    status VARCHAR(20) NOT NULL,            -- 状态：completed, failed
    download_task_id UUID REFERENCES download_tasks(id)  ON DELETE SET NULL,  -- 关联的下载任务ID
    download_time TIMESTAMP,                -- 下载完成时间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT check_segment_type CHECK (segment_type IN ('ts', 'mp4', 'image')),
    CONSTRAINT check_segment_status CHECK (status IN ('completed', 'failed'))
);

-- 索引设计
CREATE INDEX idx_downloaded_video_segments_video_id ON downloaded_video_segments(video_id);
CREATE INDEX idx_downloaded_video_segments_status ON downloaded_video_segments(status);
CREATE INDEX idx_downloaded_video_segments_download_task_id ON downloaded_video_segments(download_task_id);
CREATE INDEX idx_downloaded_video_segments_created_at ON downloaded_video_segments(created_at);
```

### 4.7 索引设计
```sql
-- download_tasks表索引
CREATE INDEX idx_download_tasks_video_id ON download_tasks(video_id);
CREATE INDEX idx_download_tasks_liveroom_id ON download_tasks(liveroom_id);
CREATE INDEX idx_download_tasks_status ON download_tasks(status);

-- download_segments表索引
CREATE INDEX idx_download_segments_task_id ON download_segments(task_id);
CREATE INDEX idx_download_segments_status ON download_segments(status);

-- download_failures表索引
CREATE INDEX idx_download_failures_task_id ON download_failures(task_id);
CREATE INDEX idx_download_failures_status ON download_failures(status);
```

## 五、API接口设计

### 5.1 响应处理规范
-通用响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    // 实际数据内容
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

- 错误响应必须遵循以下格式：
```json
{
  "code": 400,           // 错误码
  "message": "错误信息",  // 错误描述
  "data": null,         // 错误数据
  "timestamp": "2024-03-20T10:00:00Z"  // 时间戳
}
```

### 5.2 错误码说明
| 错误码 | 说明 | 处理建议 |
|--------|------|----------|
| 200 | 成功 | - |
| 400 | 请求参数错误 | 检查参数格式 |
| 401 | 未授权 | 重新登录获取token |
| 403 | 禁止访问 | 检查权限 |
| 404 | 资源不存在 | 检查资源ID |
| 500 | 服务器错误 | 联系管理员 |

### 5.3 任务管理接口

#### 5.3.1 创建下载任务
- **接口**: `POST /api/v1/download/tasks`
- **描述**: 创建新的下载任务
- **请求参数**:
```json
{
  "video_id": "string",          // 视频ID，格式：{liveroom_id}_{uuid4后缀}
  "liveroom_id": "string",       // 直播间ID
  "liveroom_title": "string",    // 直播间标题
  "liveroom_url": "string",      // 直播间URL
  "video_url": "string",         // 视频播放URL
  "video_type": "string",        // hls, mp4
  "storage_path": "string"       // 存储路径
}
```
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "status": "pending",
    "created_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.3.2 查询下载任务
- **接口**: `GET /api/v1/download/tasks/{task_id}`
- **描述**: 获取下载任务详情
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "video_id": "string",
    "liveroom_id": "string",
    "status": "string",
    "progress": 0.5,
    "failed_segments": [
      {
        "segment_id": "uuid",
        "segment_url": "string",
        "error_message": "string"
      }
    ],
    "created_at": "2024-03-20T10:00:00Z",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.3.3 取消下载任务
- **接口**: `DELETE /api/v1/download/tasks/{task_id}`
- **描述**: 取消指定的下载任务
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "status": "cancelled",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

### 5.4 下载控制接口

#### 5.4.1 暂停下载任务
- **接口**: `POST /api/v1/download/tasks/{task_id}/pause`
- **描述**: 暂停指定的下载任务
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "status": "paused",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.4.2 恢复下载任务
- **接口**: `POST /api/v1/download/tasks/{task_id}/resume`
- **描述**: 恢复暂停的下载任务
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "status": "processing",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.4.3 重试失败任务
- **接口**: `POST /api/v1/download/tasks/{task_id}/retry`
- **描述**: 重试失败的下载任务
- **请求参数**:
```json
{
  "segment_ids": ["uuid"],  // 可选，指定重试的分片
  "resource_type": "string" // 可选，指定重试的资源类型
}
```
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "uuid",
    "status": "retrying",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

### 5.5 失败任务管理接口

#### 5.5.1 查询失败记录
- **接口**: `GET /api/v1/download/tasks/{task_id}/failures`
- **描述**: 获取任务的失败记录
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 10,
    "page": 1,
    "size": 10,
    "items": [
      {
        "failure_id": "uuid",
        "segment_id": "uuid",
        "failure_type": "string",
        "error_message": "string",
        "retry_count": 0,
        "next_retry_time": "2024-03-20T10:00:00Z",
        "created_at": "2024-03-20T10:00:00Z"
      }
    ]
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.5.2 重试失败任务
- **接口**: `POST /api/v1/download/tasks/{task_id}/failures/{failure_id}/retry`
- **描述**: 重试特定的失败记录
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "failure_id": "uuid",
    "status": "retrying",
    "next_retry_time": "2024-03-20T10:00:00Z",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

#### 5.5.3 放弃失败任务
- **接口**: `POST /api/v1/download/tasks/{task_id}/failures/{failure_id}/abandon`
- **描述**: 放弃特定的失败记录
- **响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "failure_id": "uuid",
    "status": "abandoned",
    "updated_at": "2024-03-20T10:00:00Z"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

### 5.4 错误处理规范
- 错误码定义
  - 系统级错误：1xxx
  - 业务级错误：2xxx
  - 权限级错误：3xxx
  - 参数级错误：4xxx
- 错误信息格式
```json
{
  "code": 400,
  "message": "参数错误",
  "details": {
    "field": "video_id",
    "error": "视频ID格式不正确"
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

### 5.6 分页规范
- 分页请求参数
  - page: 页码，从1开始
  - size: 每页数量，默认10
  - sort: 排序字段，格式：field:asc/desc
- 分页响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "size": 10,
    "items": []
  },
  "timestamp": "2024-03-20T10:00:00Z"
}
```

## 六、功能流程设计

### 6.1 下载状态检查流程
```python
def check_download_status(video_id: str, liveroom_id: str) -> Dict:
    """
    检查视频下载状态
    :param video_id: 视频ID
    :param liveroom_id: 直播间ID
    :return: 下载状态信息
    """
    # 1. 检查已下载视频记录
    video = get_downloaded_video(video_id, liveroom_id)
    if video:
        return {
            "status": video.status,
            "task_id": video.download_task_id,
            "storage_path": video.storage_path,
            "download_time": video.download_end_time
        }
    
    # 2. 检查是否有进行中的下载任务
    task = get_task_by_video_id(video_id, liveroom_id)
    if task:
        if task.status == 'processing':
            return {
                "status": "downloading",
                "task_id": task.id,
                "progress": calculate_download_progress(task.id)
            }
        elif task.status == 'failed':
            failures = get_task_failures(task.id)
            return {
                "status": "failed",
                "task_id": task.id,
                "failures": failures
            }
    
    return {"status": "not_found"}
```

### 6.2 视频下载流程
```python
def create_storage_path(video_id: str, liveroom_id: str) -> str:
    """
    创建符合规范的存储路径
    :param video_id: 视频ID，格式：{liveroom_id}_{uuid4后缀}
    :param liveroom_id: 直播间ID
    :return: 存储路径
    """
    timestamp = datetime.now().strftime("%Y%m%dT%H%M%S")
    base_path = f"/media/video_{video_id}"
    
    # 创建目录结构
    paths = {
        'raw': f"{base_path}/raw",
        'hls': f"{base_path}/hls_1080p_4Mbps",
        'hls_ts': f"{base_path}/hls_1080p_4Mbps/ts",
        'mp4': f"{base_path}/mp4",
        'images': f"{base_path}/images"
    }
    
    # 创建目录
    for path in paths.values():
        os.makedirs(path, exist_ok=True)
    
    return base_path

def download_video(task_id: str, video_type: str):
    """
    视频下载主流程
    :param task_id: 下载任务ID
    :param video_type: 视频类型（hls/mp4）
    """
    logger = logging.getLogger('download_service')
    try:
        # 1. 获取任务信息
        task = get_task(task_id)
        if not task:
            logger.error(f"Task {task_id} not found")
            raise TaskNotFoundException(f"Task {task_id} not found")
        
        # 2. 检查是否已下载
        existing_video = check_existing_video(task.video_id, task.liveroom_id)
        if existing_video and existing_video.status == 'completed':
            logger.info(f"Video {task.video_id} already exists")
            return mark_task_as_completed(task_id, "视频已存在")
        
        # 3. 创建存储目录
        storage_path = create_storage_path(task.video_id, task.liveroom_id)
        logger.info(f"Created storage path: {storage_path}")
        
        # 4. 下载视频
        video_success = False
        image_success = True
        timestamp = datetime.now().strftime("%Y%m%dT%H%M%S")
        
        if video_type == 'hls':
            # 4.1 HLS下载流程
            try:
                # 下载m3u8文件
                m3u8_path = f"{storage_path}/hls_1080p_4Mbps/index_{task.video_id}_transcoded_{timestamp}.m3u8"
                m3u8_content = download_m3u8(task.video_url)
                with open(m3u8_path, 'w') as f:
                    f.write(m3u8_content)
                
                segments = parse_m3u8(m3u8_content)
                total_segments = len(segments)
                failed_segments = 0
                
                # 并发下载分片
                with ThreadPoolExecutor(max_workers=settings.MAX_CONCURRENT_DOWNLOADS) as executor:
                    future_to_segment = {
                        executor.submit(
                            download_segment, 
                            segment,
                            f"{storage_path}/hls_1080p_4Mbps/ts/segment_{i:03d}_{task.video_id}_transcoded_{timestamp}.ts"
                        ): segment 
                        for i, segment in enumerate(segments)
                    }
                    
                    for future in as_completed(future_to_segment):
                        segment = future_to_segment[future]
                        try:
                            future.result()
                        except Exception as e:
                            failed_segments += 1
                            logger.error(f"Failed to download segment {segment.id}: {str(e)}")
                            handle_download_failure(task_id, segment.id, e)
                
                # 检查下载结果
                if failed_segments == 0:
                    video_success = True
                    logger.info(f"Successfully downloaded all segments for video {task.video_id}")
                elif failed_segments / total_segments <= 0.05:
                    video_success = True
                    logger.warning(f"Partially downloaded video {task.video_id}: {failed_segments} segments failed")
                    mark_task_as_partial_completed(task_id, f"视频下载基本完成，但有{failed_segments}个分片下载失败")
                else:
                    video_success = False
                    logger.error(f"Failed to download video {task.video_id}: {failed_segments} segments failed")
                    mark_task_as_failed(task_id, f"视频下载失败，{failed_segments}个分片下载失败")
                    
            except Exception as e:
                logger.error(f"Error in HLS download process: {str(e)}")
                handle_download_failure(task_id, None, e)
                video_success = False
                
        elif video_type == 'mp4':
            # 4.2 MP4下载流程
            try:
                mp4_path = f"{storage_path}/mp4/video_{task.video_id}_transcoded_1080p_{timestamp}.mp4"
                download_mp4(task.video_url, mp4_path)
                video_success = True
                logger.info(f"Successfully downloaded MP4 video {task.video_id}")
            except Exception as e:
                logger.error(f"Failed to download MP4 video {task.video_id}: {str(e)}")
                handle_download_failure(task_id, None, e)
                video_success = False
        
        # 5. 下载相关图片
        if task.cover_url:
            try:
                cover_path = f"{storage_path}/images/cover_{task.video_id}_upload_{timestamp}.jpg"
                download_image(task.cover_url, cover_path)
                logger.info(f"Successfully downloaded cover image for video {task.video_id}")
            except Exception as e:
                logger.error(f"Failed to download cover image for video {task.video_id}: {str(e)}")
                handle_download_failure(task_id, None, e, 'image')
                image_success = False
        
        # 6. 更新任务状态
        if video_success:
            if image_success:
                mark_task_as_completed(task_id)
                logger.info(f"Task {task_id} completed successfully")
                # 记录下载成功的视频信息
                record_downloaded_video(
                    video_id=task.video_id,
                    liveroom_id=task.liveroom_id,
                    liveroom_title=task.liveroom_title,
                    liveroom_url=task.liveroom_url,
                    video_type=video_type,
                    video_url=task.video_url,
                    storage_path=storage_path,
                    cover_url=task.cover_url,
                    cover_path=cover_path if task.cover_url else None,
                    download_task_id=task_id
                )
            else:
                mark_task_as_partial_completed(task_id, "视频下载成功，但部分图片下载失败")
                logger.warning(f"Task {task_id} partially completed: video success but image failed")
        else:
            mark_task_as_failed(task_id, "视频下载失败")
            logger.error(f"Task {task_id} failed: video download failed")
            
    except Exception as e:
        logger.error(f"Download task {task_id} failed: {str(e)}", exc_info=True)
        mark_task_as_failed(task_id, f"下载任务异常: {str(e)}")
        raise
```

### 6.3 失败处理流程
```python
def handle_download_failure(task_id: str, segment_id: str, error: Exception, resource_type: str = 'video'):
    """
    处理下载失败
    :param task_id: 任务ID
    :param segment_id: 分片ID（对于HLS视频）
    :param error: 错误信息
    :param resource_type: 资源类型（video/image）
    """
    # 记录失败信息
    failure = create_failure_record(task_id, segment_id, error, resource_type)
    
    # 更新任务状态
    if resource_type == 'video':
        if segment_id:
            # HLS视频分片失败，检查失败比例
            total_segments = get_total_segments(task_id)
            failed_segments = get_failed_segments_count(task_id)
            failure_ratio = failed_segments / total_segments
            
            if failure_ratio <= 0.05:  # 允许5%的分片失败
                update_task_status(task_id, 'partial_completed')
                log_segment_failure(task_id, segment_id, error)
            else:
                update_task_status(task_id, 'failed')
                mark_task_as_failed(task_id, f"视频下载失败: 失败分片比例超过5%")
        else:
            # MP4视频下载失败，直接标记任务失败
            update_task_status(task_id, 'failed')
            mark_task_as_failed(task_id, f"视频下载失败: {str(error)}")
    else:
        # 图片下载失败，记录失败但不影响任务状态
        update_task_status(task_id, 'partial_completed')
        log_image_failure(task_id, error)
```

### 6.4 任务状态定义
```python
class TaskStatus:
    PENDING = 'pending'           # 等待下载
    PROCESSING = 'processing'     # 下载中
    COMPLETED = 'completed'       # 完全成功（视频和图片都下载成功）
    PARTIAL_COMPLETED = 'partial_completed'  # 部分成功（视频基本完成但有少量分片失败，或视频成功但图片失败）
    FAILED = 'failed'            # 失败（视频下载失败或失败分片过多）
    CANCELLED = 'cancelled'      # 已取消
```

### 6.5 重试机制流程
```python
def retry_failed_segments(task_id: str, segment_ids: List[str] = None, resource_type: str = None):
    """
    重试失败的下载
    :param task_id: 任务ID
    :param segment_ids: 要重试的分片ID列表（可选）
    :param resource_type: 要重试的资源类型（video/image，可选）
    """
    # 获取失败记录
    failures = get_failures_to_retry(task_id, segment_ids, resource_type)
    if not failures:
        return
    
    # 记录重试开始
    log_retry_start(task_id, len(failures))
    
    # 重试下载
    success_count = 0
    for failure in failures:
        try:
            if failure.segment_type == 'mp4':
                download_mp4(failure.segment_url, failure.file_path)
            elif failure.segment_type == 'image':
                download_image(failure.segment_url, failure.file_path)
            else:
                download_segment(failure.segment_id)
            
            # 下载成功，删除失败记录
            remove_failure_record(failure.id)
            success_count += 1
            
        except Exception as e:
            # 更新失败记录
            update_failure_record(failure.id, e)
    
    # 检查是否所有失败记录都已处理成功
    remaining_failures = get_failures_to_retry(task_id)
    if not remaining_failures:
        # 所有失败记录都已处理成功
        task = get_task(task_id)
        if task.video_type == 'hls':
            # 检查是否所有分片都已下载成功
            if all_segments_completed(task_id):
                mark_task_as_completed(task_id)
                log_retry_success(task_id, "所有分片下载成功")
        else:
            mark_task_as_completed(task_id)
            log_retry_success(task_id, "下载任务完成")
    else:
        # 仍有失败记录
        task = get_task(task_id)
        if task.video_type == 'hls':
            # 检查失败分片比例
            total_segments = get_total_segments(task_id)
            failed_segments = len(remaining_failures)
            if failed_segments / total_segments <= 0.05:
                mark_task_as_partial_completed(task_id, f"重试后仍有{failed_segments}个分片下载失败")
            else:
                mark_task_as_failed(task_id, f"重试后仍有过多分片下载失败")
        else:
            mark_task_as_failed(task_id, "重试后仍有下载失败")
    
    # 记录重试结果
    log_retry_result(task_id, success_count, len(failures) - success_count)
```

### 6.6 失败记录管理
```python
def get_failures_to_retry(task_id: str, segment_ids: List[str] = None, resource_type: str = None) -> List[Dict]:
    """
    获取需要重试的失败记录
    :param task_id: 任务ID
    :param segment_ids: 要重试的分片ID列表（可选）
    :param resource_type: 要重试的资源类型（可选）
    :return: 失败记录列表
    """
    query = {
        "task_id": task_id,
        "retry_count": {"$lt": MAX_RETRY_COUNT}  # 限制最大重试次数
    }
    
    if segment_ids:
        query["segment_id"] = {"$in": segment_ids}
    if resource_type:
        query["resource_type"] = resource_type
    
    return list(failures_collection.find(query).sort("created_at", 1))

def remove_failure_record(failure_id: str):
    """
    删除失败记录
    :param failure_id: 失败记录ID
    """
    failures_collection.delete_one({"_id": failure_id})

def update_failure_record(failure_id: str, error: Exception):
    """
    更新失败记录
    :param failure_id: 失败记录ID
    :param error: 错误信息
    """
    failures_collection.update_one(
        {"_id": failure_id},
        {
            "$inc": {"retry_count": 1},
            "$set": {
                "last_error": str(error),
                "last_retry_at": datetime.utcnow()
            }
        }
    )
```

## 七、性能设计

### 7.1 性能指标
- 并发下载任务数：100+
- 单个任务最大并发数：10
- 下载速度限制：可配置
- 任务队列容量：1000+
- API响应时间：<200ms

### 7.2 性能优化
1. 并发控制
   - 任务级别并发控制
   - 全局并发控制
   - 动态调整并发数

2. 资源管理
   - 内存使用限制
   - CPU使用限制
   - 磁盘IO限制
   - 网络带宽限制

3. 缓存策略
   - 任务状态缓存
   - 配置信息缓存
   - 临时文件缓存

### 7.3 失败处理机制
1. 失败检测
   - 网络错误检测
   - 超时检测
   - 内容有效性检测
   - 完整性校验

2. 失败记录
   - 详细错误信息记录
   - 失败时间记录
   - 重试次数记录
   - 失败类型分类

3. 重试策略
   - 指数退避重试
   - 最大重试次数限制
   - 重试间隔控制
   - 优先级调整

4. 失败恢复
   - 自动重试机制
   - 手动重试支持
   - 失败任务清理
   - 资源释放

## 八、安全设计

### 8.1 访问控制
- API认证授权
- 任务权限控制
- 资源访问控制
- 操作审计日志

### 8.2 数据安全
- 下载内容验证
- 文件完整性校验
- 敏感信息加密
- 临时文件清理

### 8.3 防护措施
- 请求频率限制
- 并发任务限制
- 下载大小限制
- 异常行为监控

## 九、监控告警

### 9.1 监控指标
- 任务处理状态
- 下载速度统计
- 资源使用情况
- 错误率统计

### 9.2 告警规则
- 任务失败告警
- 性能指标告警
- 资源使用告警
- 异常行为告警

## 十、部署方案

### 10.1 容器化部署
```yaml
version: '3'
services:
  download-service:
    image: media-download-service:latest
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=postgres
      - REDIS_HOST=redis
      - STORAGE_PATH=/data
    volumes:
      - ./data:/data
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:13
    environment:
      - POSTGRES_DB=download_service
      - POSTGRES_USER=download_user
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:6
    volumes:
      - redis_data:/data
```

### 10.2 扩展性设计
- 水平扩展支持
- 负载均衡配置
- 服务发现集成
- 配置中心集成

### 10.3 安全配置
```yaml
security:
  # API认证配置
  jwt_secret: ${JWT_SECRET}
  token_expire_minutes: 60
  
  # 访问控制配置
  rate_limit:
    requests_per_minute: 100
    burst_size: 50
  
  # 下载限制配置
  download_limit:
    max_file_size: 1024MB
    max_concurrent_downloads: 10
    chunk_size: 1MB
```

## 十一、媒体下载服务特定规范

### 11.1 下载任务状态定义
```python
class TaskStatus:
    PENDING = 'pending'           # 等待下载
    PROCESSING = 'processing'     # 下载中
    COMPLETED = 'completed'       # 完全成功（视频和图片都下载成功）
    PARTIAL_COMPLETED = 'partial_completed'  # 部分成功（视频基本完成但有少量分片失败，或视频成功但图片失败）
    FAILED = 'failed'            # 失败（视频下载失败或失败分片过多）
    CANCELLED = 'cancelled'      # 已取消
```

### 11.2 下载失败处理规范
- 失败类型分类：
  - network_error：网络错误
  - timeout：超时错误
  - invalid_content：内容无效
  - storage_error：存储错误
  - permission_error：权限错误

- 重试策略：
  - 最大重试次数：3次
  - 重试间隔：指数退避（1分钟、2分钟、4分钟）
  - HLS分片失败容忍度：5%
  - 失败记录保留时间：7天

### 11.3 并发控制规范
- 全局并发限制：
  - 最大并发下载任务数：100
  - 单个任务最大并发数：10
  - 任务队列容量：1000

- 资源限制：
  - 内存使用限制：2GB/任务
  - CPU使用限制：50%/任务
  - 磁盘IO限制：100MB/s
  - 网络带宽限制：10MB/s

### 11.4 存储路径规范
```
/media/video_{video_id}/
├── images/                    # 图片资源目录
│   ├── cover_{video_id}_{操作类型}_{时间戳}.jpg
│   └── thumbnail_{video_id}_{操作类型}_{时间戳}.webp
├── raw/                      # 原始视频目录
│   └── video_{video_id}_{操作类型}_{时间戳}.mp4
├── hls_{清晰度}_{码率}/      # HLS视频目录
│   ├── index_{video_id}_{操作类型}_{时间戳}.m3u8
│   └── ts/                   # TS分片目录
│       └── segment_{序号}_{video_id}_{操作类型}_{时间戳}.ts
├── mp4/                      # MP4视频目录
│   └── video_{video_id}_{操作类型}_{清晰度}_{时间戳}.mp4
└── metadata.json            # 元数据文件
```

### 11.5 监控指标规范
- 任务监控：
  - 任务处理状态
  - 下载速度
  - 失败率
  - 重试次数

- 资源监控：
  - CPU使用率
  - 内存使用率
  - 磁盘使用率
  - 网络带宽使用率

- 告警规则：
  - 任务失败率 > 10%
  - 重试次数 > 3次
  - 资源使用率 > 80%
  - 下载速度 < 1MB/s