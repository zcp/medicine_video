# 融合后续操作路线图（2026-08-10）

> 融合实施（阶段 0-4）已完成并同步文档。本文档梳理后续全部待办，按优先级分组，供逐步实施参考。
> 状态：**A 组进行中，B/C/D 组待定**。

---

## A 组 · 近期建议做（功能性/必要配套）

| # | 项 | 内容 | 收益 | 工作量 | 风险 |
|---|----|------|------|--------|------|
| A1 | 留言限流 | 照 wechat `check_message_rate_limit`（5 秒 1 条，Redis 记数）接入创建留言链路 | 唯一功能性安全补漏：防刷屏/打 DB | ~30 行 + 2 测试 | 极低（独立小功能） |
| A2 | 前端 2D 提交入库 | saas_app-main 提交 auth.ts + account-security 改动 | 防改动丢失；后端上线后注销功能不坏 | 1 次 commit | 零 |
| A3 | 前端 admin 角色 UI 同步 | 管理端"修改角色"下拉移除 ADMIN 选项（后端已仅 SUPERADMIN 可改） | 消除联调 403 | 前端小改动 | 低 |

## B 组 · 推送（随时可执行，等用户指示）

| # | 项 | 说明 |
|---|----|------|
| B1 | `git push origin main` | 照 `待推送清单-20260810.md`：10 提交 + 69 文件 + 6 项检查 |

## C 组 · 上云前部署准备（非代码或轻代码）

| # | 项 | 内容 | 性质 | 时机 |
|---|----|------|------|------|
| C1 | JWT_SECRET_KEY 强密钥 | 部署时 `openssl rand -hex 32` 生成，两服务同值 | 纯配置 | 上云时 |
| C2 | seed 接入部署脚本 | medical_lexicon_seed 接进 compose/init-db 或部署文档 | ~10 行脚本或文档 1 段 | 上云前（可选） |
| C3 | entrypoint.sh | Docker 启动自动建表+seed | 便利性优化 | 上云前（可选，可跳过） |

## D 组 · 技术债（可集中专项清理，无生产影响）

| # | 项 | 内容 | 收益 | 注意 |
|---|----|------|------|------|
| D1 | live_core 46 测试 | 剩余失败全为测试数据/断言未适配 | 恢复回归保护网 | 修复风险极低（4C 已修 41 个零回归），用户已确认暂缓 |
| D2 | users 94 测试 | 同性质，从未收编/清理过 | 同上 | 测试跑 74 秒/轮，适合集中半天 |
| D3 | 裸词剔除（可选） | 对齐 wechat DROPPED_BARE_KEYWORDS | 降低"最/第一"误伤率 | 白名单已部分兜底 |
| D4 | AdminMessageItem 对齐（可选） | 补 user_nickname/user_role_snapshot | 契约完全一致 | 前端不用，无功能影响 |

## 依赖关系

```
A2（1 分钟）→ 无依赖，现在就能做
A1 → 无依赖，现在就能做
A3 → 依赖前端联调计划
B1（推送）→ 等用户指示；与 A 组互不影响（推送内容已固定）
C 组 → 依赖 B1（上云必然在推送之后）
D 组 → 独立，任何时候可做；建议在 B1 之后做（避免推送内容漂移）
```

## 推荐执行顺序

1. **现在/近期**：A2（提交前端）→ A1（留言限流）
2. **用户确认后**：B1（推送）
3. **上云前**：C1+C2（+可选 C3）
4. **专项期**：D1+D2（同批半天）+ 可选 D3/D4
