# 融合方案 P4 — 数据迁移脚本代码生成提示词

**版本**: V1.0  
**创建日期**: 2026-06-23  
**基于设计文档**: `app修改记录/融合方案最终设计_A2+C.md`、`app修改记录/融合方案_提示词编写规范借鉴.md`  
**依赖**: P1 (Model + Schema) + P2 (CRUD) + P3 (Service + API) 已完成  
**目标文件**: 4 个 SQL + 1 个 Python

---

## 1. 角色定义 (Role Definition)

你是一名精通 PostgreSQL 15 DDL/DML 和 Python 数据迁移脚本编写的数据库运维工程师。你理解本项目的迁移规范——手动 SQL 文件 + 编号前缀保证执行顺序 + `IF NOT EXISTS` 幂等化 + Python 脚本做复杂数据转换。

你的任务是执行**融合方案 P4**：在 P1-P3 代码已生成但数据库尚未迁移的前提下，编写 4 个 SQL 迁移文件和 1 个 Python 数据迁移脚本，按正确的顺序将数据库从"旧结构"变迁到融合方案需要的"新结构"。

**三个必须**：
- 必须保持与已有迁移文件相同的风格（`ADD COLUMN IF NOT EXISTS`、`CREATE INDEX IF NOT EXISTS`）
- 必须标注每个迁移的依赖和前置条件
- 必须在 Python 迁移脚本中输出 Dry-Run 报告（先读不写，人工确认后才执行写操作）

---

## 2. 任务目标 (Task Objective)

### 2.1 迁移文件清单

| # | 文件名 | 内容 | 执行顺序 |
|:--|:------|:-----|:-------:|
| V4 | `migrations/V4_create_expert_departments.sql` | 创建 `expert_departments` 表 + `experts.department_id` 列 | 1 |
| V5 | `migrations/V5_add_category_parent_id.sql` | `categories` 加 `parent_id` + 部分唯一索引 | 2 |
| V6 | `scripts/migrate_expert_departments.py` | Python 迁移脚本：Dry-Run → 正式迁移 `department → department_id` | 3 |
| V7 | `migrations/V7_drop_experts_department.sql` | 删除 `experts.department` 列 + `category_id SET NOT NULL` | 4 |

### 2.2 不能改的文件

- ❌ 不修改已有的 3 个迁移文件（`add_experts_is_active.sql`、`drop_live_rooms_category_id.sql`、`20260531_add_keyword_to_search_keyword_stats.sql`）
- ❌ 不创建 Alembic 配置
- ❌ 不修改 `app/` 下的任何 Python 文件

---

## 3. 核心上下文 (Core Context)

### 3.1 已有迁移文件风格

```
migrations/
├── add_experts_is_active.sql          ← 使用 ALTER TABLE ... ADD COLUMN IF NOT EXISTS
├── drop_live_rooms_category_id.sql     ← 使用 ALTER TABLE ... DROP COLUMN IF EXISTS
└── 20260531_add_keyword_to_search_keyword_stats.sql  ← 有编号前缀, 有回填 DML
```

**关键风格点**：
- 所有 DDL 操作使用 `IF NOT EXISTS` / `IF EXISTS` 确保幂等
- 文件头部有 `-- ` 注释说明背景、前置条件
- 每个操作之间有明确的注释分隔

### 3.2 技术栈与约束

```
PostgreSQL 15 | 数据库名: live_core_test
执行方式: docker exec -i <container> psql -U postgres -d live_core_test -f /path/to/migration.sql
Python 迁移脚本: 使用 SQLAlchemy 异步 session，复用已有 db 连接模式
```

### 3.3 P1 已生成的关键模型信息

```
新增表: expert_departments
  ├── id UUID PK DEFAULT gen_random_uuid()
  ├── name VARCHAR(120) UNIQUE NOT NULL
  ├── category_id UUID NOT NULL REFERENCES categories(id) ON DELETE RESTRICT
  ├── synonyms JSONB NOT NULL DEFAULT '[]'::jsonb
  ├── is_active BOOLEAN NOT NULL DEFAULT true
  ├── is_verified BOOLEAN NOT NULL DEFAULT false
  ├── created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  └── updated_at TIMESTAMPTZ NOT NULL DEFAULT now()

experts 表新增:
  ├── department_id UUID REFERENCES expert_departments(id) ON DELETE SET NULL
  └── idx_experts_department_id

categories 表新增:
  ├── parent_id UUID REFERENCES categories(id) ON DELETE RESTRICT
  ├── 移除 name 的 UNIQUE 约束 (categories_name_key)
  ├── uq_root_category_name 部分唯一索引 (WHERE parent_id IS NULL)
  └── uq_sub_category_name 部分唯一索引 (WHERE parent_id IS NOT NULL)
```

### 3.4 部署顺序死线

```
Step 1: V4 (建表)  → Step 2: V5 (加列) → Step 3: V6 (数据迁移) → Step 4: V7 (删列)
```

**关键约束**：
- V5 中 `DROP CONSTRAINT categories_name_key` 必须在 `CREATE UNIQUE INDEX uq_root_category_name` 之前执行——否则旧约束阻止同名子分类，且旧约束和部分索引冲突
- V7 中 `ALTER COLUMN category_id SET NOT NULL` 之前必须确认所有专家的 `category_id` 非 NULL（V6 保证）
- V7 中 `DROP COLUMN department` 不可逆——在这之前必须确认 V6 的迁移报告已审核通过

---

## 4. 架构约束 (Architecture Constraints)

### 4.1 所有迁移文件必须

- ✅ 使用 `IF NOT EXISTS` / `IF EXISTS` 确保重复执行幂等
- ✅ 使用 `COMMENT ON` 为新建列添加注释
- ✅ 文件头部用 `-- ` 注释说明背景、前置条件
- ✅ 使用 `CREATE INDEX IF NOT EXISTS` 创建索引（不用 `CREATE UNIQUE INDEX` 时例外，需特殊处理）
- ✅ Python 迁移脚本必须支持 `--dry-run` 模式

### 4.2 部分唯一索引的特殊处理

`CREATE UNIQUE INDEX` 不支持 `IF NOT EXISTS` 语法。使用 PL/pgSQL 块包裹：

```sql
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_indexes
        WHERE indexname = 'uq_root_category_name'
    ) THEN
        CREATE UNIQUE INDEX uq_root_category_name
        ON categories (name)
        WHERE parent_id IS NULL;
    END IF;
END $$;
```

### 4.3 V5 中的 DROP CONSTRAINT 幂等处理

```sql
-- 使用 IF EXISTS 确保可重复执行
ALTER TABLE categories DROP CONSTRAINT IF EXISTS categories_name_key;
```

### 4.4 Python 迁移脚本约束

- 使用 `asyncio.run()` 入口
- 复用项目已有的 `from app.database import get_db` 或独立创建 session
- `--dry-run` 模式只读不写，输出 CSV 到 stdout
- 正式模式的每一步写操作之间间隔打印进度

---

## 5. 代码生成要求 (Specific Code Generation Requirements)

### 5.1 V4: `migrations/V4_create_expert_departments.sql`

```sql
-- 融合方案 V4：创建 expert_departments 专家科室受控词表 + experts.department_id 列
-- 背景：将 department 从自由文本升级为受控词表 FK
-- 前置条件：categories 表已存在（seed.py 已创建）
-- 执行顺序：Step 1（第一步，无依赖）
-- 幂等性：所有 DDL 使用 IF NOT EXISTS

-- 1) 创建 expert_departments 表
CREATE TABLE IF NOT EXISTS expert_departments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(120) UNIQUE NOT NULL,
    category_id UUID NOT NULL REFERENCES categories(id) ON DELETE RESTRICT,
    synonyms JSONB NOT NULL DEFAULT '[]'::jsonb,
    is_active BOOLEAN NOT NULL DEFAULT true,
    is_verified BOOLEAN NOT NULL DEFAULT false,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE expert_departments IS '专家科室受控词表。将 department 从自由文本变为受控词表';
COMMENT ON COLUMN expert_departments.name IS '标准科室名称，全局唯一。如"乳腺外科"';
COMMENT ON COLUMN expert_departments.category_id IS '科室所属主分类ID。ON DELETE RESTRICT 防止误删分类';
COMMENT ON COLUMN expert_departments.synonyms IS '同义词列表 JSON 数组。导入时用于容错匹配';
COMMENT ON COLUMN expert_departments.is_verified IS '是否已审核。false=自适应创建待确认，true=已审核标准词';

-- 索引
CREATE INDEX IF NOT EXISTS idx_expert_departments_category_id ON expert_departments(category_id);
CREATE INDEX IF NOT EXISTS idx_expert_departments_is_active ON expert_departments(is_active);
CREATE INDEX IF NOT EXISTS idx_expert_departments_is_verified ON expert_departments(is_verified);

-- 2) experts 表新增 department_id FK
ALTER TABLE experts ADD COLUMN IF NOT EXISTS department_id UUID
    REFERENCES expert_departments(id) ON DELETE SET NULL;

COMMENT ON COLUMN experts.department_id IS '科室ID，关联 expert_departments 受控词表。NULL 表示未映射';

CREATE INDEX IF NOT EXISTS idx_experts_department_id ON experts(department_id);
```

### 5.2 V5: `migrations/V5_add_category_parent_id.sql`

```sql
-- 融合方案 V5：categories 加分层结构（parent_id + 部分唯一索引）
-- 背景：支持子分类（如 心内科→冠脉介入组）
-- 前置条件：categories 表已存在，V4 已执行
-- 执行顺序：Step 2
-- 关键约束：必须先 DROP 旧 UNIQUE 约束，再创建部分索引——顺序不可逆

-- 1) 删除旧全局唯一约束（幂等）
ALTER TABLE categories DROP CONSTRAINT IF EXISTS categories_name_key;

-- 2) 添加 parent_id 自引用外键
ALTER TABLE categories ADD COLUMN IF NOT EXISTS parent_id UUID
    REFERENCES categories(id) ON DELETE RESTRICT;

COMMENT ON COLUMN categories.parent_id IS '父分类ID。NULL=根分类；非NULL=子分类（具体专业方向）';
COMMENT ON COLUMN categories.name IS '分类名称';

-- 3) 创建普通索引
CREATE INDEX IF NOT EXISTS idx_categories_parent_id ON categories(parent_id);

-- 4) 创建部分唯一索引（不支持 IF NOT EXISTS，使用 PL/pgSQL 包裹）
DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_indexes WHERE indexname = 'uq_root_category_name'
    ) THEN
        CREATE UNIQUE INDEX uq_root_category_name
        ON categories (name)
        WHERE parent_id IS NULL;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_indexes WHERE indexname = 'uq_sub_category_name'
    ) THEN
        CREATE UNIQUE INDEX uq_sub_category_name
        ON categories (parent_id, name)
        WHERE parent_id IS NOT NULL;
    END IF;
END $$;
```

### 5.3 V6: `scripts/migrate_expert_departments.py`

```python
#!/usr/bin/env python3
"""
融合方案 V6：专家 department → expert_departments 数据迁移脚本

功能：
  --dry-run: 输出匹配报告 CSV（只读不写）
  正式模式: 遍历 experts 表，将 department 文本匹配到 expert_departments 表
           并设置 department_id 和 category_id

使用方式：
  cd backend/live_core_service
  python scripts/migrate_expert_departments.py --dry-run > migration_report.csv
  python scripts/migrate_expert_departments.py           # 正式执行

前置条件：
  - V4（expert_departments 表）已执行
  - V5（categories.parent_id）已执行
  - expert_departments 中已预置种子科室数据（或至少包含"其他-科室"兜底记录）
"""

import asyncio
import csv
import io
import sys
import uuid
from typing import Optional, Tuple

from sqlalchemy import select, text, update as sa_update
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker

# 配置
DATABASE_URL = "postgresql+asyncpg://postgres:postgres@localhost:5432/live_core_test"

# 科室→根分类别名映射表（从 P3 的 expert_service.py 复制）
DEPARTMENT_CATEGORY_MAP = {
    '乳腺外科': '普通外科', '甲状腺外科': '普通外科',
    '肝胆外科': '普通外科', '胃肠外科一科': '普通外科',
    '胃肠外科二科': '普通外科', '胃肠外科三科': '普通外科',
    '胆胰外科': '普通外科', '肝外科': '普通外科',
    '脊柱外科': '骨科', '关节外科': '骨科',
    '骨肿瘤科': '骨科', '运动医学科': '骨科',
    '显微创伤外手科': '骨科',
    '妇科': '妇产科', '产科': '妇产科',
    '鼻专科': '耳鼻喉科', '耳专科': '耳鼻喉科', '咽喉专科': '耳鼻喉科',
    '肾移植专科': '器官移植科',
    '器官移植科/肾移植专科': '器官移植科',
    '器官移植科/肝移植专科': '器官移植科',
    '泌尿外科/男科': '泌尿外科',
    '生殖医学中心': '生殖医学科', '生殖男科专科': '生殖医学科',
    '男科/生殖医学中心': '生殖医学科',
    '胸外科': '心胸外科',
    '口内修复科': '口腔科', '口腔颌面外科': '口腔科',
    '小儿外科': '儿科',
    '变态反应专科': '其他', '外科门诊': '其他',
    '肝外科/超声医学科/介入超声专科': '其他',
}


def normalize(text_val: str) -> str:
    """清洗科室文本：去除末尾斜杠和首尾空白"""
    return text_val.rstrip('/.。、, ').strip()


def match_department(normalized: str) -> Optional[Tuple[str, str]]:
    """
    匹配科室文本 → (matched_dept_name, matched_category_name)。
    返回 None 表示需要人工确认。

    匹配优先级：
    1. 子串匹配（根分类名包含在科室文本中）
    2. 别名表匹配
    """
    from app.models.content_management import Category  # 不实际导入，只用 category.name

    # 模拟已有分类（实际执行时从 DB 读）
    ROOT_CATEGORIES = ['普通外科','神经外科','心内科','骨科','肿瘤科','妇产科','儿科','眼科',
                       '耳鼻喉科','消化科','内分泌科','泌尿外科','心胸外科','血管外科','皮肤科',
                       '口腔科','整形外科','器官移植科','生殖医学科','烧伤与创面修复科','其他']

    # 1. 子串匹配
    for rc in ROOT_CATEGORIES:
        if rc in normalized:
            return (normalized, rc)

    # 2. 别名表匹配
    mapped = DEPARTMENT_CATEGORY_MAP.get(normalized)
    if mapped:
        return (normalized, mapped)

    return None


async def run_dry_run(session: AsyncSession):
    """Dry-Run：只读不写，输出 CSV 报告到 stdout"""
    writer = csv.writer(sys.stdout)
    writer.writerow(["expert_id", "name", "original_department", "matched_dept", "matched_category", "status"])

    result = await session.execute(
        text("SELECT id, name, department FROM experts WHERE department IS NOT NULL ORDER BY created_at")
    )
    rows = result.fetchall()

    matched_count = 0
    unmatched_count = 0

    for row in rows:
        expert_id = str(row[0])
        expert_name = row[1]
        original = row[2]
        normalized = normalize(original)

        match_result = match_department(normalized)
        if match_result:
            matched_dept, matched_cat = match_result
            writer.writerow([expert_id, expert_name, original, matched_dept, matched_cat, "auto"])
            matched_count += 1
        else:
            writer.writerow([expert_id, expert_name, original, "", "其他", "manual_review"])
            unmatched_count += 1

    print(f"\n# Dry-Run 完成：{matched_count} 条可自动匹配，{unmatched_count} 条需人工审核",
          file=sys.stderr)


async def run_migrate(session: AsyncSession):
    """正式迁移：写数据库"""
    # 1. 查询所有有 department 的专家
    result = await session.execute(
        text("SELECT id, name, department FROM experts WHERE department IS NOT NULL")
    )
    rows = result.fetchall()

    # 2. 加载 expert_departments 查找表
    dept_result = await session.execute(
        text("SELECT id, name, category_id FROM expert_departments WHERE is_active = true")
    )
    dept_lookup = {row[1]: (row[0], row[2]) for row in dept_result.fetchall()}

    # 3. 加载"其他"分类 ID
    other_result = await session.execute(
        text("SELECT id FROM categories WHERE name = '其他' AND parent_id IS NULL AND is_active = true")
    )
    other_cat_id = other_result.scalar_one()

    migrated = 0
    skipped = 0

    for row in rows:
        expert_id = row[0]
        original = row[2]
        normalized = normalize(original)

        if normalized in dept_lookup:
            dept_id, cat_id = dept_lookup[normalized]
        else:
            match_result = match_department(normalized)
            if match_result:
                matched_dept_name, _ = match_result
                if matched_dept_name in dept_lookup:
                    dept_id, cat_id = dept_lookup[matched_dept_name]
                else:
                    # 匹配到了但科室表中不存在 → 归入"其他"
                    dept_id, cat_id = None, other_cat_id
            else:
                dept_id, cat_id = None, other_cat_id

        if dept_id:
            await session.execute(
                sa_update(text("experts"))
                .where(text("id = :eid"))
                .values(department_id=dept_id, category_id=cat_id),
                {"eid": expert_id}
            )
            migrated += 1
        else:
            await session.execute(
                sa_update(text("experts"))
                .where(text("id = :eid"))
                .values(category_id=cat_id),
                {"eid": expert_id}
            )
            skipped += 1

        if migrated % 50 == 0:
            print(f"  已迁移 {migrated} 条...", file=sys.stderr)

    await session.commit()
    print(f"\n# 迁移完成：{migrated} 条成功迁移，{skipped} 条未匹配（归入'其他'）", file=sys.stderr)


async def main():
    dry_run = "--dry-run" in sys.argv

    engine = create_async_engine(DATABASE_URL)
    async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

    async with async_session() as session:
        if dry_run:
            print("# 迁移脚本 Dry-Run 报告", file=sys.stderr)
            await run_dry_run(session)
        else:
            print("# 正式迁移开始...", file=sys.stderr)
            await run_migrate(session)

    await engine.dispose()


if __name__ == "__main__":
    asyncio.run(main())
```

### 5.4 V7: `migrations/V7_drop_experts_department.sql`

```sql
-- 融合方案 V7：删除 experts.department 自由文本列 + category_id SET NOT NULL
-- 背景：department 已迁移到 expert_departments 受控词表（V6 完成）
-- 前置条件：V6 迁移脚本已执行且 Dry-Run 报告已审核通过
-- 执行顺序：Step 4（最后一步，不可逆）
-- 关键：执行前必须确认 experts 表中 category_id IS NULL 的行数为 0

-- 0) 前置验证（如有多行 NULL category_id，RAISE EXCEPTION 阻止 DROP）
DO $$
DECLARE
    null_count INTEGER;
BEGIN
    SELECT COUNT(*) INTO null_count FROM experts WHERE category_id IS NULL;
    IF null_count > 0 THEN
        RAISE EXCEPTION '仍有 % 个专家的 category_id 为 NULL，不能删除 department 列。请重新执行 V6 迁移', null_count;
    END IF;
END $$;

-- 1) 删除 department 列（不可逆）
ALTER TABLE experts DROP COLUMN IF EXISTS department;

COMMENT ON COLUMN experts.category_id IS '专家主专业分类，关联全局 categories.id。强制 NOT NULL，兜底指向"其他"分类';

-- 2) category_id 强制 NOT NULL
ALTER TABLE experts ALTER COLUMN category_id SET NOT NULL;
```

---

## 6. 完整性检查清单 (Completeness Checklist)

### 6.1 V4 (expert_departments 表)

- ✅ `CREATE TABLE IF NOT EXISTS expert_departments` 幂等
- ✅ `name` 有 `UNIQUE NOT NULL`
- ✅ `category_id` 用 `ON DELETE RESTRICT`
- ✅ `synonyms` 用 `DEFAULT '[]'::jsonb`
- ✅ `ALTER TABLE experts ADD COLUMN IF NOT EXISTS department_id` 幂等
- ✅ `ON DELETE SET NULL`（专家不会因科室被删而丢失）
- ✅ 所有索引用 `CREATE INDEX IF NOT EXISTS`

### 6.2 V5 (categories 分层)

- ✅ `DROP CONSTRAINT IF EXISTS categories_name_key` 幂等
- ✅ `ADD COLUMN IF NOT EXISTS parent_id` 幂等
- ✅ `ON DELETE RESTRICT`（防止删除父分类）
- ✅ 两个部分唯一索引用 PL/pgSQL 包裹（不支持 `IF NOT EXISTS`）

### 6.3 V6 (Python 迁移)

- ✅ `--dry-run` 模式只读不写
- ✅ Dry-Run 输出 CSV 含 expert_id / name / original / matched_dept / matched_category / status
- ✅ 正式模式每个 UPDATE 之间间隔打印进度
- ✅ 迁移前加载 expert_departments 查找表
- ✅ 完全无法匹配的 `department_id=NULL, category_id="其他"`
- ✅ `asyncio.run()` 入口 + `engine.dispose()` 清理

### 6.4 V7 (删列 + NOT NULL)

- ✅ 前置验证 `category_id IS NULL` 的行数
- ✅ 有 NULL 时 `RAISE EXCEPTION` 阻止 DROP
- ✅ `DROP COLUMN IF EXISTS department`
- ✅ `ALTER COLUMN category_id SET NOT NULL`

### 6.5 全局约束

- ✅ 执行顺序 V4 → V5 → V6 → V7 不可颠倒
- ✅ 所有 SQL 文件使用 `IF NOT EXISTS` / `IF EXISTS` 幂等
- ✅ 文件头部有 `-- ` 注释说明背景和前置条件
- ✅ 不修改已有 3 个迁移文件
- ✅ Python 脚本不依赖 `app.` 模块导入（独立脚本）

---

## 7. 最终交付 (Final Deliverable)

根据以上要求，生成以下 **4 个 SQL + 1 个 Python 文件**：

1. **新建** `migrations/V4_create_expert_departments.sql`
2. **新建** `migrations/V5_add_category_parent_id.sql`
3. **新建** `scripts/migrate_expert_departments.py`
4. **新建** `migrations/V7_drop_experts_department.sql`

**执行流程**：

```bash
# Step 1: 建表
docker exec -i <pg> psql -U postgres -d live_core_test < migrations/V4_create_expert_departments.sql

# Step 2: 分层结构
docker exec -i <pg> psql -U postgres -d live_core_test < migrations/V5_add_category_parent_id.sql

# Step 3: Dry-Run（先看报告）
cd backend/live_core_service
python scripts/migrate_expert_departments.py --dry-run > migration_report.csv
# 人工审核 CSV，确认匹配正确

# Step 4: 正式迁移（确认后执行）
python scripts/migrate_expert_departments.py

# Step 5: 删列 + NOT NULL
docker exec -i <pg> psql -U postgres -d live_core_test < migrations/V7_drop_experts_department.sql
```

---

## 8. 实施后同步记录（2026-07-19）

> 实际 SQL 迁移文件和 Python 脚本与原 P4 设计存在以下偏差。

### 8.1 V4 SQL 迁移差异

| 设计 (§5.1) | 实际实现 | 判断 |
|---|---|---|
| `id UUID PRIMARY KEY DEFAULT gen_random_uuid()` | `id UUID PRIMARY KEY`（无 DEFAULT） | **实现正确**：项目所有 UUID 主键在应用层（ORM）生成，不依赖数据库默认值 |
| 无种子数据插入 | **包含 30 条种子科室 INSERT**（9 个分类 × 2~8 条），使用 `ON CONFLICT (name) DO NOTHING` 幂等 | **实现更完整**：冷启动时只需运行 V4 即可得到可用的科室数据 |
| 迁移编号：Step 1 | Step **A1** | 编号风格差异，无影响 |

### 8.2 V5 SQL 迁移差异

| 设计 (§5.2) | 实际实现 | 判断 |
|---|---|---|
| 全部 DDL + 部分索引 + COMMENT | ✅ 完全一致 | 无差异 |
| 迁移编号：Step 2 | Step **A2** | 编号风格差异，无影响 |

### 8.3 V6 Python 迁移脚本

| 设计 (§5.3) | 实际实现 | 判断 |
|---|---|---|
| 脚本位置 `scripts/migrate_expert_departments.py` | ✅ 文件存在于 `backend/live_core_service/scripts/` | 一致 |
| `--dry-run` 模式输出 CSV | 待验证（脚本是独立运行模式，路径正确） | — |

### 8.4 V7 SQL 迁移差异

| 设计 (§5.4) | 实际实现 | 判断 |
|---|---|---|
| `ALTER TABLE experts DROP COLUMN IF EXISTS department;` | ✅ 已实现，附加前置验证（`null_count > 0 → RAISE EXCEPTION`） | 一致 |
| `ALTER TABLE experts ALTER COLUMN category_id SET NOT NULL;` | ✅ 已实现（幂等，已设置则跳过） | 一致 |
| `COMMENT ON COLUMN experts.category_id IS ...` | ✅ 已实现 | 一致 |

### 8.5 部署顺序

设计要求的执行顺序 V4→V5→V6→V7 不可颠倒 — **已严格遵守**。
