# 分类实施 — 阶段 0：基线登记与备份记录

> **关联方案**: [分类系统根治方案_语义冻结与基线治理.md](./分类系统根治方案_语义冻结与基线治理.md)（V1.3）
> **执行日期**: 2026-08-12
> **执行原则**: 最小安全幅度——全程只读查询 + 备份，**未修改任何数据**
> **状态**: 🟡 备份完成，待 Gate 0 决策项（3 位专家迁移方向）确认后进入阶段 1A

---

## 一、执行摘要

| 项 | 结果 |
|:---|:---|
| 基线快照 | ✅ 已采集（见第二节） |
| 四表备份 | ✅ 全库 dump + 四表数据 dump + 四表 CSV 明细（见第三节） |
| 测试数据登记 | ✅ 模式统计 + 完整明细以 CSV 附件为准（见第四节） |
| 3 位专家决策 | ⏳ **待用户决策**（见第五节，Gate 0 阻塞项） |
| 数据修改 | ❌ 无（全程只读） |

---

## 二、基线快照（2026-08-12 08:40 实测）

> ⚠️ 并行功能开发仍在进行，数据持续变化（本次快照为查询时刻状态）。

### 2.1 核心统计

| 维度 | 数值 | 说明 |
|:---|:---:|:---|
| **categories 总数** | **193（active ~170）** | 35 条常量 + ~158 条并行开发创建的测试/新增分类 |
| 层级结构 | 一级 160 / 二级 33 | 测试分类几乎全为一级根（与历史模式一致） |
| standard | 191 true / 2 false | 测试分类继承默认 true（历史问题复现中） |
| display_name | 仅 35 条非空 | 只有常量分类有 |
| **experts 专家** | **488** | 全有 category_id（0 NULL） |
| **expert_departments** | **45**（seed_s4 40 + manual 1 + 无 source 4） | is_verified 状态待复核 |
| **live_rooms 直播间** | **252**（seed_ 前缀 6 + 其余 246） | 并行开发创建为主 |
| live_room_categories | 25 条关联，**3 房间有 primary** | 主分类问题复现中 |

### 2.2 脏科室状态（V8 未执行，与 V1.3 一致）

| 科室 | 挂载专家 | 分类 | 处置 |
|:---|:---:|:---|:---|
| 外科门诊 | 2（李强、黄文生） | 其他 | V8 DELETE → department_id 置 NULL；**迁移去向待决策** |
| 肝外科/超声医学科/介入超声专科 | 1（吕明德） | 其他 | V8 DELETE → department_id 置 NULL；**迁移去向待决策** |
| 口内修复科 | 6 人 | 口腔科 | V8 重命名 → 口腔内科，专家自动跟随（无损失） |

---

## 三、备份产物清单

位置：`app修改记录/分类实施文档/阶段0备份/`

| 文件 | 内容 | 大小 |
|:---|:---|:---:|
| `full_backup_categories_20260812.sql` | **全库完整 dump**（结构+数据，恢复首选） | 1.4 MB |
| `backup_data_categories_20260812.sql` | 四表数据 dump（INSERT 语句） | 448 KB |
| `snapshot_categories.csv` | categories 全量 193 行（id/name/slug/parent_id/sort_order/is_active/standard/display_name/created_at） | 20 KB |
| `snapshot_experts.csv` | experts 全量 488 行（id/name/title/hospital/department_id/category_id/is_active/is_featured/created_at） | 95 KB |
| `snapshot_departments.csv` | expert_departments 全量 45 行（id/name/category_id/synonyms/is_active/is_verified/source/created_at） | 6 KB |
| `snapshot_rooms.csv` | live_rooms 全量 252 行（id/user_id/title/stream_key/is_private/external_room_id/created_at） | 43 KB |

**恢复方式**（如需回滚）：
```bash
# 全库恢复
docker exec -i live-streaming-saas-v2-main-postgres-1 psql -U postgres -d live_core_test < full_backup_categories_20260812.sql
```

---

## 四、测试数据登记表

### 4.1 模式统计（实测覆盖 8+ 类，比预设更广）

| 模式 | 数量 | 示例 | 处置建议 |
|:---|:---:|:---|:---|
| `分类_` | 110（含 分类1_/分类2_/分类3_） | 分类_036a8303 | 阶段 4 清理候选 |
| 无前缀测试模式 | ~38 | 二级A_/子_/父_/根_/活跃_/空分类_/重复分类_/新分类_/唯一名_/神经内科_（含同名重复！） | **逐条人工确认**（部分可能为真实数据） |
| `源_` | 4 | 源_xxx | 阶段 4 清理候选 |
| `目标_` | 2 | 目标_xxx | 阶段 4 清理候选 |
| `一级_` | 2 | 一级_xxx | 阶段 4 清理候选 |
| 含"测试" | 2 | 含测试 | 阶段 4 清理候选 |
| **常量 35 条** | 35 | 内科/外科/心内科... | **保留（基线）** |
| 无前缀真实新增 | 待确认 | 无法确定 | **本次不动，登记观察** |

> ⚠️ **关键发现**：`神经内科_430e3cfa`、`神经内科_97f705bb` 等存在**同名重复**（部分唯一索引失效场景），且部分测试分类已 `is_active=false`（禁用_/停用子_/父_ 等）——清理时以 CSV 快照为准逐条核对。

### 4.2 处置规则（R-T1 ~ R-T4 落地）

```
① 阶段 4 清理只针对「登记表 + 前缀匹配」交集
② 无前缀 38 条测试模式分类：清理前逐条人工确认（防误删正确数据）
③ 并行开发创建的直播间/专家（非本次实施创建）：登记观察，本次不清理
④ 本次实施过程中若需创建测试数据 → 先登记到本表 4.3 节，结束后统一清理
```

### 4.3 实施期间测试数据登记区（预留，实施中按行追加）

| 登记时间 | 名称/ID | 类型 | 用途 | 创建人 | 清理状态 |
|:---|:---|:---|:---|:---|:---:|
| — | — | — | — | — | — |

---

## 五、Gate 0 决策项（阻塞，需确认）

### 3 位脏科室专家的迁移方向

V8 执行时 `外科门诊`（2 人）与 `肝外科/超声医学科/介入超声专科`（1 人）将被 DELETE，专家 `department_id` 置 NULL。**迁移去向需你决策**：

| # | 专家 | 科室（现） | 分类（现） | 决策选项 |
|:--|:---|:---|:---|:---|
| 1 | 李强 | 外科门诊 | 其他 | A) 迁移到「普通外科」科室（推荐，语义最近）<br>B) 接受 department_id=NULL，后续补映射 |
| 2 | 黄文生 | 外科门诊 | 其他 | 同上 |
| 3 | 吕明德 | 肝外科/超声医学科/介入超声专科 | 其他 | A) 迁移到「普通外科」或「肝胆外科」科室<br>B) 接受 NULL |

> **推荐方案**：A（3 人全部迁移到对应标准科室，避免产生新的未映射专家）——具体目标科室名称可再确认（如 李强/黄文生 → 普通外科；吕明德 → 普通外科 或新建「肝胆外科」科室）。

---

## 六、Gate 0 状态

| 检查项 | 状态 |
|:---|:---:|
| 基线快照采集 | ✅ |
| 四表备份完成 | ✅ |
| 登记表与实测一致 | ✅ |
| 3 位专家决策已定 | ⏳ **待用户决策（第五节）** |

> **结论**：Gate 0 大部分通过；唯一阻塞项为 3 位专家迁移决策。决策完成后即可进入阶段 1A（常量修正 K1/K2/K8）。

---

> **最后更新**: 2026-08-12 | **阶段 0 执行人**: AI 实施助手 | **全程只读，未修改任何数据**
