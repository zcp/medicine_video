# 分类实施 — 阶段 1A：常量修正执行记录

> **关联方案**: [分类系统根治方案_语义冻结与基线治理.md](./分类系统根治方案_语义冻结与基线治理.md)（V1.3）
> **关联阶段 0**: [分类实施_阶段0_基线登记与备份记录.md](./分类实施_阶段0_基线登记与备份记录.md)
> **执行日期**: 2026-08-12
> **执行原则**: 最小安全幅度——仅代码修改 + 容器同步，**未修改任何数据库数据**
> **状态**: 🟢 完成（Gate 1A 全部通过）

---

## 一、执行内容

### K1：`category_constants.py` 4 条 standard 改 false

| 分类 | 修改前 | 修改后 | 依据 |
|:---|:---:|:---:|:---|
| 心胸外科 | standard: True | **False** | 名录外科二级无此科目，属业务扩展 |
| 血管外科 | standard: True | **False** | 同上 |
| 器官移植科 | standard: True | **False** | 同上 |
| 烧伤与创面修复科 | standard: True | **False** | 同上 |

### K2：`category_constants.py` 别名表修正

| 别名 | 修改前 | 修改后 | 说明 |
|:---|:---|:---|:---|
| `变态反应专科` | '其他' | **'变态反应科'** | 常量已有变态反应科（内科二级），医学上明确 |
| `男科` | 无条目 | **'泌尿外科'**（新增） | 与 D4 数据修正（男科→泌尿外科）一致 |
| `外科门诊` | '其他' | 不变 | V8 将删除该科室数据，别名保留无害 |

### K8：`seed.py` 补 is_primary

```python
# 修改前
db.add(LiveRoomCategory(room_id=room.id, category_id=cat.id))
# 修改后
db.add(LiveRoomCategory(room_id=room.id, category_id=cat.id, is_primary=True))
```

> 目的：线上 seed 房间主分类缺失 → audit 第 4 项（无主分类）必报 → CheckList"0 异常"无法达成。

### D5 准备：V16 迁移 SQL 编写（未执行）

- 文件：`migrations/V16_fix_category_standard.sql`
- 内容：4 条分类 standard 回填（`UPDATE ... WHERE standard = true` 幂等守卫）
- **未执行**——与 K1 同批闭环，属阶段 2

---

## 二、Gate 1A 验证结果

| 检查项 | 结果 |
|:---|:---:|
| `len(CATEGORIES_SEED_DATA) == 35` | ✅ 35 |
| 层级 root 16 / child 19 | ✅ |
| `STANDARD_CATEGORY_NAMES` 派生 | ✅ 29 条（33 → 29） |
| standard=false 分类数 | ✅ 6（生殖医学科/其他/心胸外科/血管外科/器官移植科/烧伤与创面修复科） |
| `DEPARTMENT_CATEGORY_MAP` 条目 | ✅ 33（32 + 男科新增） |
| 男科 → 泌尿外科 | ✅ |
| 变态反应专科 → 变态反应科 | ✅ |
| seed.py / category_constants.py 语法 | ✅ py_compile 通过 |
| 容器内导入验证 | ✅ 通过（docker cp 同步后） |

---

## 三、运行时影响评估

| 影响面 | 结论 |
|:---|:---|
| 运行中服务（uvicorn） | **零影响**——容器 uvicorn 无 `--reload`，模块已加载旧常量；新常量下次重启/部署生效 |
| 数据库 | **零改动**——4 条分类仍 standard=true（预期，D5 阶段 2 执行） |
| 已同步文件 | `category_constants.py` + `seed.py` 已 docker cp 进容器（项目惯例），等待期重启即生效 |
| 并行开发 | 无冲突——改动不改变任何 API 签名/行为（仅匹配结果在重启后更正确） |

---

## 四、⚠️ 窗口期风险（已开启，需尽快闭环）

常量已改（K1）但 DB 未回填（D5 未执行）——**当前编辑这 4 条分类会触发 standard 校验 400**：

```
STANDARD_CATEGORY_NAMES 已不含 心胸外科/血管外科/器官移植科/烧伤与创面修复科
DB 中这 4 条仍 standard=true
→ 管理员编辑这 4 条分类 → _validate_standard_category_name → 400
```

**缓解**：尽快执行阶段 2（D5 回填），窗口期原则上不超过 1 个工作日；期间禁止编辑这 4 条分类。

---

## 五、改动文件清单

| 文件 | 改动 | 同步状态 |
|:---|:---|:---:|
| `app/core/category_constants.py` | K1（4 行）+ K2（3 行） | ✅ 已 docker cp |
| `app/seed.py` | K8（1 行） | ✅ 已 docker cp |
| `migrations/V16_fix_category_standard.sql` | 新增（D5 准备，未执行） | — |

---

> **最后更新**: 2026-08-12 | **阶段 1A 状态**: 🟢 完成 | **下一阶段**: 阶段 2（数据修复 D1-D5，含 D5 窗口期闭环）
