# 后端修改记录文档

> 项目：live-streaming-saas-v2-main
> 最后更新：2026-06-03（新增十五~十八：头像持久化 + 分类标签前端实现 + 播放页标�?+ Tab 权限修正�?
> 说明：本文档记录后端本地运行方式及所有关键代码修改，供后续维护参考�?

---

## 一、后端本地运行方�?

### 1.1 技术栈概览

| 组件 | 版本/镜像 | 说明 |
|------|-----------|------|
| 数据�?| postgres:15-alpine | 主数据库 |
| 缓存队列 | redis:7-alpine | Celery 任务队列 |
| 主服�?| live_core_service | FastAPI，端�?8000 |
| 媒体服务 | media_download_service | 端口 8001 |
| 用户服务 | user_service | 端口 8002 |
| 异步任务 | celery_worker | Celery + Gevent |
| 流媒体服�?| ossrs/srs:5 | SRS 直播服务�?|
| 反向代理 | nginx:alpine | 80/443 端口，SSL |

### 1.2 Docker Compose 启动

> ⚠️ **Windows 本地开发必须使�?`compose-app.windows.yml`**，不要使�?`docker-compose.prod.yml`�?
> 原因：`docker-compose.prod.yml` 挂载生产 nginx 配置（需�?SSL 证书 `/var/www/mp.dayilive.com/fullchain.pem`），本地无此证书，nginx 会崩溃循环导致端�?8080 不通�?
> `compose-app.windows.yml` 挂载 `nginx.windows.conf`（纯 HTTP，端�?8080，无 SSL），适用�?Windows 本地开发�?

**首次启动（构建镜�?+ 启动）：**
```powershell
# 在项目根目录 d:\live-streaming-saas-v2-main 下执�?
docker compose -f compose-app.windows.yml up --build -d
```

**日常启动（已构建过镜像）�?*
```powershell
docker compose -f compose-app.windows.yml up -d
```

**重启单个服务（修改代码后）：**
```powershell
docker compose -f compose-app.windows.yml restart live_core_service
```

**查看运行状态：**
```powershell
docker compose -f compose-app.windows.yml ps
```

**查看服务日志�?*
```powershell
docker logs live-streaming-saas-v2-main-live_core_service-1 --tail 50 -f
```

**停止所有服务：**
```powershell
docker compose -f compose-app.windows.yml down
```

### 1.3 环境配置文件

- 配置文件位置：`d:\live-streaming-saas-v2-main\.env.docker`
- 各服务通过 `env_file: .env.docker` 读取环境变量
- 主要变量包括：数据库连接字符串、JWT 密钥、Redis 地址�?
- 本地测试�?JWT 密钥默认�?`my-key`�?*生产环境必须修改**

### 1.4 数据库说�?

| 服务 | 数据库名 |
|------|---------|
| live_core_service | live_core_test |
| media_download_service | media_download_test |
| user_service | users_service_test |

数据库由 PostgreSQL 容器启动时自动执�?`init-db.sh` 脚本建库�?

---

## 二、种子数据初始化

### 2.1 脚本位置

```
d:\live-streaming-saas-v2-main\backend\live_core_service\app\seed.py
```

### 2.2 脚本内容说明

`seed.py` 负责向本地数据库写入测试所需的初始数据，包括�?

- **15 个医学分�?*（普通外科、神经外科、心内科等）
- **400 名专�?*（从 CSV 文件导入�?
- **5 个直播间 + 对应场次**（含 1 条真实可播放�?HLS 回放链接�?
- **4 条首页焦点图（Banner�?*

脚本使用幂等设计：重复运行不会产生重复数据（按名�?ID 检查是否已存在）�?

### 2.3 执行步骤

**步骤 1：将专家 CSV 文件复制到容器（仅首次执行一次）**
```powershell
docker cp "D:\wechat\xwechat_files\wxid_vuwbstum7hx822_f849\msg\file\2026-03\experts_cleaned_top400.csv" live-streaming-saas-v2-main-live_core_service-1:/app/experts.csv
```

**步骤 2：将 seed.py 复制到容�?*
```powershell
docker cp "D:\live-streaming-saas-v2-main\backend\live_core_service\app\seed.py" live-streaming-saas-v2-main-live_core_service-1:/app/seed.py
```

**步骤 3：在容器内执行脚�?*
```powershell
docker exec live-streaming-saas-v2-main-live_core_service-1 sh -c "cd /app && python seed.py"
```

> **注意**：每次修�?`seed.py` 后都需要重新执行步�?2（重新复制脚本到容器），否则容器内运行的仍是旧版本�?

### 2.4 测试用户 ID

种子数据中使用的测试用户（对应前端登录的账号）：

```
SEED_USER_ID = bc1d319e-480f-48f9-bfd7-69b5d7bea88a
```

### 2.5 PowerShell 注意事项

- `docker cp` �?PowerShell 中会将成功信息输出到 stderr，导致显�?`NativeCommandError`，但实际操作成功，可忽略此提示�?
- PowerShell 控制台若显示中文乱码，是终端编码问题，不影响数据库实际数据，可忽略�?

### 2.6 焦点图与直播间种子数据增�?

**时间**�?026-05-12

**文件**：`backend/live_core_service/app/seed.py`

**修改类型**：数据增�?/ 种子修复

#### 修改目标

1. 让首页焦点图全部使用 `room` 类型，避免外链跳转影响直播首页体验�?
2. 焦点图与数据库中的真实直播间绑定，点击后进入项目内的直播间页�?
3. 增加可用于分类筛选验证的直播间数据，覆盖多个科室，便于验证首页分类功能�?
4. 保持焦点图图�?URL 使用公开可访问的 HTTPS 图片地址，兼容前�?`resolveMediaUrl()` 的解析逻辑�?

#### 关键改动

- 新增 `BRANDS_DATA` 的同时，保留焦点图图片为公开 HTTPS 地址�?
- �?`FEATURED_CONTENTS_DATA` 中所有焦点图统一改为 `target_type = "room"`�?
- 为每条焦点图新增 `target_room_title`，由 `seed_featured_content()` 自动查找对应直播间并写入 `target_id`�?
- `seed_featured_content()` �?`target_type == "room"` 的记录会自动清空 `target_url`，避免外链跳转�?
- �?`LIVE_ROOMS_DATA` 中补充了额外直播间，覆盖：妇产科、儿科、眼科等分类，用于首页分类筛选验证�?

#### 新增直播间（用于分类验证�?

- 妇产科围手术期管�?| 剖宫产与宫腔镜专�?
- 儿科疑难病例讨论 | 新生儿重症管�?
- 眼科显微手术技�?| 白内障与玻璃体切�?

#### 焦点图图�?URL 说明

当前焦点图图片统一使用公开可访问的 HTTPS 图片地址（Unsplash）。前�?`FeaturedCarousel.vue` 会通过 `resolveMediaUrl()` 进行规范化；对于完整 HTTPS 地址，前端不会改写协议，只做原样透传，因此图�?URL 兼容性良好�?

#### 执行效果

- 焦点图点击后进入项目�?`LiveView` 页面
- 首页可通过分类切换验证不同科室直播�?
- 焦点图与直播间数据保持一致，减少外链导致的测试偏�?

---

## 三、后端代码修改记�?

---

### 修改 #1：新增种子数据脚�?

**日期**�?026-04 会话  
**文件**：`backend/live_core_service/app/seed.py`（新建）  
**修改类型**：新增文�? 

**背景**�? 
本地 Docker 环境启动后数据库为空，前端页面无数据可展示，需要初始化测试数据�?

**内容**�? 
创建了完整的数据库种子脚本，初始�?15 个医学分类�?00 名专家�? 个直播间场次�?4 条首�?Banner。该脚本使用同步 SQLAlchemy Session（非异步）运行，支持幂等执行（不产生重复数据）�?

---

### 修改 #2：修�?POST 接口 500 错误（核�?Bug 修复�?

**日期**�?026-04-11  
**文件**：`backend/live_core_service/app/services/user_behavior_service.py`  
**修改类型**：Bug 修复  
**影响接口**�?
- `POST /users/me/favorites`（添加收藏）
- `POST /users/me/watch-history`（记录观看历史）

#### 问题现象

前端调用上述两个 POST 接口均返�?HTTP 500，错误响应体�?
```json
{"code": 1002, "message": "内部服务器错�?}
```

同时�?*数据实际上已成功写入数据�?*（POST 返回 500 后，GET 接口仍能读取到刚写入的数据），说明错误发生在数据写入之后、返回响应之前�?

#### 根本原因

**SQLAlchemy 异步 Session �?`expire_on_commit=True` 默认行为 + Pydantic 延迟属性访问冲突�?*

详细分析�?

1. SQLAlchemy 默认配置 `expire_on_commit=True`，即执行 `await db.commit()` 后，Session 中所有已加载�?ORM 对象会被标记�?过期"（expired）�?
2. 对过期对象的属性访问会触发 SQLAlchemy �?懒加�?（lazy load），向数据库重新查询�?
3. 在异�?SQLAlchemy 中，**不允许隐式的懒加�?*，会抛出 `MissingGreenlet` 异常（即在没有事件循环的情况下触�?I/O 操作）�?
4. 原始代码�?`commit()` **之后**才调�?`model_validate()`，Pydantic 访问已过�?ORM 对象的属性时触发懒加载，引发异常�?
5. 该异常被 `except Exception as e:` 捕获，最终返�?500�?

#### 原始代码（有问题�?

`add_favorite` 方法（`user_behavior_service.py`，第 37-53 行）�?
```python
async def add_favorite(self, current_user_id: UUID, room_id: UUID) -> FavoriteItem:
    try:
        fav = await crud_user_behavior.create_favorite(self.db, current_user_id, room_id)
        await self.db.commit()          # �?commit �?fav 已过�?
    except DatabaseIntegrityException as e:
        ...
    except Exception as e:
        ...
        raise
    return FavoriteItem.model_validate(fav)  # �?访问已过期对象属性，触发懒加载异�?
```

`record_watch_event` 方法（`user_behavior_service.py`，第 103-125 行）�?
```python
async def record_watch_event(self, current_user_id, watch_event) -> WatchHistoryItem:
    try:
        history = await crud_user_behavior.record_watch_history(...)
        await self.db.commit()          # �?commit �?history 已过�?
    except Exception as e:
        ...
        raise
    return WatchHistoryItem.model_validate(history)  # �?同上，触发懒加载异常
```

#### 修复后代�?

�?`model_validate()` 移至 `commit()` **之前**执行（此�?ORM 对象尚未过期，属性已�?CRUD 层的 `db.refresh()` 提前加载完毕）：

`add_favorite`�?
```python
async def add_favorite(self, current_user_id: UUID, room_id: UUID) -> FavoriteItem:
    try:
        fav = await crud_user_behavior.create_favorite(self.db, current_user_id, room_id)
        result = FavoriteItem.model_validate(fav)   # �?移到 commit 之前
        await self.db.commit()
    except DatabaseIntegrityException as e:
        await self.db.rollback()
        raise InvalidParameterException("已收藏该直播�?) from e
    except Exception as e:
        await self.db.rollback()
        self.logger.error(...)
        raise
    return result
```

`record_watch_event`�?
```python
async def record_watch_event(self, current_user_id, watch_event) -> WatchHistoryItem:
    try:
        history = await crud_user_behavior.record_watch_history(...)
        result = WatchHistoryItem.model_validate(history)  # �?移到 commit 之前
        await self.db.commit()
    except Exception as e:
        await self.db.rollback()
        self.logger.error(...)
        raise
    return result
```

#### 补充说明

- 此问题在本地环境和云端环境均存在（本地日志中也有 500 记录，只是手机模拟器界面没有明显提示）�?
- 后端老师提到�?Python 3.9 �?3.10 升级并非此问题的原因，该 Bug �?Python 版本无关，是 SQLAlchemy 异步使用规范问题�?
- 修复后无需 `expire_on_commit=False` 等全局配置变更，最小化改动范围�?

#### 修复后验�?

修复提交后执行重启：
```powershell
docker compose -f docker-compose.prod.yml restart live_core_service
```

预期结果�?
- `POST /users/me/favorites` �?返回 `{"code": 200, "data": {...}}`
- `POST /users/me/watch-history` �?返回 `{"code": 200, "data": {...}}`

---

## 四、修改记录二：Tab 公开接口（解决普通用�?匿名用户 404�?

**修改日期**�?026-04-12  
**问题描述**：前�?App 端请�?`GET /api/v1/rooms/{room_id}/tabs` 返回 404。原因是所�?Tab 路由均注册在 `/admin` 前缀下（`GET /api/v1/admin/rooms/{room_id}/tabs`），普通用户和匿名用户访问的公开路径不存在�?

**设计依据**（来�?`docs/03_系统设计/直播核心功能设计文档_v6_regular在tab和留言上的权限扩充设计.md`）：
> - Tab 内容：对所有用户可见（**继承 Room �?`is_private` 可见�?*�?
> - Tab 管理功能（编�?删除）：仅对创建者和 Admin 可见

**修改文件**�? 个文件，纯新增，零删改�?

---

### 修改1：`app/api/v1/endpoints/live_features.py`

**改动 A**：新�?CRUD 模块导入（第 29 行）

```python
from app.crud import live_features as crud_live_features
```

**改动 B**：在文件末尾新增 `public_tab_router` 及端点函�?

```python
public_tab_router = APIRouter(tags=["Public - Tabs"])

@public_tab_router.get("/rooms/{room_id}/tabs")
async def list_room_tabs_public(
    room_id: uuid.UUID,
    current_user: Optional[Dict] = Depends(get_current_user_optional),
    db: AsyncSession = Depends(get_async_db),
):
    """
    权限规则（继�?Room �?is_private 可见性）�?
    - 公开房间：所有用户可访问，含未登录匿名用�?
    - 私有房间：仅 ADMIN/SUPERADMIN 和房间创建者可访问，其余返�?404
    """
```

权限校验逻辑�?
1. 调用 `TabService._check_room_exists()` 获取 room 对象
2. �?`room.is_private=True`：未登录 �?404；非 Admin 且非创建�?�?404
3. 调用 `crud_live_features.get_active_by_room_id()` 只返�?`is_active=True` �?Tab

---

### 修改2：`app/api/v1/api.py`

�?`admin_tab_router` 注册之后，新增注册（�?70-75 行）�?

```python
# 4.1 Tab公开路由器（普通用�?匿名，继�?Room is_private 可见性）
api_router.include_router(
    live_features.public_tab_router,
    prefix="",
    tags=["public-tabs"]
)
```

最终路径：`GET /api/v1/rooms/{room_id}/tabs`

---

### 权限矩阵

| 场景 | 路径 | 结果 |
|------|------|------|
| 匿名用户 �?公开房间 tab | `GET /rooms/{id}/tabs` | �?返回 tabs |
| 普通用�?�?公开房间 tab | `GET /rooms/{id}/tabs` | �?返回 tabs |
| 普通用�?�?私有房间 tab（非创建者） | `GET /rooms/{id}/tabs` | �?404 |
| 房间创建�?�?自己私有房间 tab | `GET /rooms/{id}/tabs` | �?返回 tabs |
| Admin �?任意房间 tab | `GET /rooms/{id}/tabs` | �?返回 tabs |
| Admin/创建者管�?tab（CRUD�?| `GET/POST /admin/rooms/{id}/tabs` | �?不变（现有接口） |

### 验证方式

修改后重建镜像：
```powershell
docker compose -f docker-compose.prod.yml up --build -d live_core_service
```

---

## 五、待关注事项

1. **JWT 密钥**：本地默认使�?`my-key`，部署到生产/云端时必须在 `.env.docker` 中配置强密钥�?
2. **种子数据重复执行**：每次重置数据库后需重新执行 seed.py；若不重置，重复执行是安全的（幂等）�?
3. **容器内代码同�?*：`restart` 仅重启容器，不重建镜像。如修改了后端代码，需执行 `up --build` 重新构建镜像，或使用 `docker cp` 将修改的单个文件复制进容器（仅适用于临时调试）�?
4. **异步 SQLAlchemy 懒加载限�?*：所有需要在 `commit()` 之后访问 ORM 对象属性的地方，都应提前调�?`db.refresh()` 或在 `commit()` 之前完成 Pydantic 转换，避免类�?Bug 复现�?

---

## 六、种子数据补�?�?直播�?Tab 测试数据

**时间**�?026-04-12
**文件**：`backend/live_core_service/app/seed.py`

### 问题背景

公开 Tab API（`GET /rooms/{room_id}/tabs`）接口已修复�?00 OK），但数据库中无 Tab 记录，导致前端始终回退到硬编码�?直播介绍/互动讨论"两个固定 Tab，无法验证后�?Tab 数据的实际渲染效果�?

### 修改内容

#### 1. 新增 import

```python
from app.models.live_features import LiveRoomTab, LiveRoomTabContentType
```

#### 2. �?`LIVE_ROOMS_DATA` 的三个直播间中添�?`tabs` 字段

| 直播�?| Tab �?| content_type 类型 |
|--------|--------|-------------------|
| 腔腔三人�?\| 普通外科手术分享系�?| 3 | `text` / `text` / `image` |
| 神经外科技术论�?\| 内镜与显微手术专�?| 2 | `mixed` / `text` |
| 心内科学术直�?\| 冠心病与心衰管理新进�?| 3 | `text` / `text` / `mixed` |

其余 2 个直播间（脑血管病外科、中山一院胃肠外科）�?Tab，前端使�?fallback，形成对比�?

#### 3. 新增 `seed_tabs()` 函数

功能：对已存在于数据库中的直播间也能补充插入 Tab，按 `(room_id, tab_key)` 做去重，幂等安全�?

```python
def seed_tabs(db):
    for room_data in LIVE_ROOMS_DATA:
        if not room_data.get("tabs"):
            continue
        room = db.query(LiveRoom).filter_by(title=room_data["title"]).first()
        for tab_data in room_data["tabs"]:
            existing = db.query(LiveRoomTab).filter_by(
                room_id=room.id, tab_key=tab_data["tab_key"]
            ).first()
            if existing:
                continue
            db.add(LiveRoomTab(...))
    db.flush()
```

#### 4. �?`main()` 中调�?

```python
seed_live_rooms(db, categories)
seed_tabs(db)          # �?新增
seed_featured_content(db)
```

### 执行方式

由于 Docker 容器内代码是构建时复制进去的（非实时挂载），需先将修改后的文件复制进容器再执行�?

```powershell
# 将本地修改的 seed.py 复制进容�?
docker cp "backend\live_core_service\app\seed.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/seed.py

# 执行种子脚本
docker exec -e PYTHONPATH=/app live-streaming-saas-v2-main-live_core_service-1 sh -c "cd /app && python app/seed.py"
```

### 执行结果

```
📑 插入直播�?Tab 数据...
  ++ 创建: [腔腔三人�?| 普通外科手...] 直播简�?(text)
  ++ 创建: [腔腔三人�?| 普通外科手...] 本期议程 (text)
  ++ 创建: [腔腔三人�?| 普通外科手...] 会议海报 (image)
  ++ 创建: [神经外科技术论�?| 内镜...] 论坛介绍 (mixed)
  ++ 创建: [神经外科技术论�?| 内镜...] 主讲专家 (text)
  ++ 创建: [心内科学术直�?| 冠心�?..] 课程简�?(text)
  ++ 创建: [心内科学术直�?| 冠心�?..] 课程大纲 (text)
  ++ 创建: [心内科学术直�?| 冠心�?..] 参考资�?(mixed)
```

数据库最�?Tab 记录�?*8 �?*，覆�?3 个直播间，包�?`text` / `image` / `mixed` 三种 `content_type`�?

---

## 七、删除直播间 Bug 修复

**时间**�?026-04-12
**涉及文件**�?
- `backend/live_core_service/app/crud/room.py`
- `backend/live_core_service/app/services/room_service.py`
- `backend/live_core_service/app/api/v1/endpoints/room.py`

### 问题背景

调用 `DELETE /rooms/{room_id}` 后端返回 500，根本原因是 **async SQLAlchemy `expire_on_commit` 导致的过期属性访�?*，与之前 user_behavior 模块的同�?Bug 一致�?

### 修复详情

#### Fix 1：`crud/room.py` �?`remove()` commit 后返回缓存�?

```python
from types import SimpleNamespace

async def remove(db: AsyncSession, db_obj: LiveRoom) -> SimpleNamespace:
    room_id_log = db_obj.id
    title_log = db_obj.title
    await db.delete(db_obj)
    await db.commit()
    return SimpleNamespace(id=room_id_log, title=title_log)
```

#### Fix 2：`room_service.py` �?提前缓存 `room.title`

```python
room_title = room.title
await crud_room.delete_room_related(db=self.db, room_id=room_id)
deleted_room = await crud_room.remove(db=self.db, db_obj=room)
logger.info(f"成功删除房间: room_id={room_id}, title={room_title}")
```

#### Fix 3：`room.py` �?补充通用异常兜底

```python
except Exception as e:
    logger.error(f"删除房间失败（未知错误）: room_id={room_id}, error={e}", exc_info=True)
    return JSONResponse(status_code=500, content=error_response(code=5000, message="服务器内部错�?))
```

---

## 八�?我的直播"列表按创建者过滤修�?

**时间**�?026-04-12
**涉及文件**�?
- 后端（无需修改，逻辑已存在）
- 前端 `D:\saas_app-main\src\store\room.ts`
- 前端 `D:\saas_app-main\src\pages\app\live-manage\list.vue`

### 问题背景

新注册账号进�?我的直播"页面，仍能看到其他用户创建的房间�?

### 根本原因

前端调用 `GET /rooms` 时未传�?`owner_only=true`，后端默认返回所有公开房间�?

### 修复

前端 `store/room.ts` �?`fetchRooms` 新增 `ownerOnly` 参数，`list.vue` 中调用时传入 `ownerOnly: true`�?

---

## 九、修�?Windows 批处理脚本编码问�?

**时间**�?026-05-11
**文件**：`start-dev.bat`、`stop-dev.bat`、`rebuild.bat`、`rebuild-live-core-only.bat`

### 问题

CMD 执行 `.bat` 文件�?UTF-8 中文�?emoji 字符�?GBK 错误解析为乱码�?

### 修复

所�?`.bat` 文件改为�?ASCII 编码，提示信息使用英文，修正 `COMPOSE_FILE` 指向 `compose-app.windows.yml`�?

---

## 十、头像上传与预览功能完整实现

**时间**�?026-05-14
**涉及文件（后�?3 �?+ 前端 5 个）**�?
- `backend/users/app/main.py`
- `nginx/nginx.windows.conf`
- `nginx/nginx.conf`
- 前端 `D:\saas_app-main\src\api\auth.ts`
- 前端 `D:\saas_app-main\src\store\auth.ts`
- 前端 `D:\saas_app-main\src\utils\url.ts`
- 前端 `D:\saas_app-main\src\pages\app\tabbar\my\edit-profile\index.vue`
- 前端 `D:\saas_app-main\src\components\shared\AvatarPreview.vue`

### 问题背景

前端调用裁剪后的头像上传接口返回 404，头像预览无法加载。经排查，存在两个独立问题：

1. **上传路径拼接错误**：前�?`uploadAvatar` 调用 `authUrl('/users/me/avatar')`，但 `AUTH_API_URL` 已包�?`/api/users`，导致最�?URL �?`/api/users/users/me/avatar`（重�?`/users/`�?�?404
2. **上传后的头像文件无法被前端访�?*：用户服务将头像保存到容器内 `uploads/avatar/` 目录，但该目录既没挂�?StaticFiles，Nginx 也无对应代理路由

### 后端现有支持（已有，无需修改�?

用户服务 `backend/users/app/api/v1/endpoints/users.py` 中已实现�?

- **头像上传接口**：`POST /api/users/me/avatar`
  - 接收 `multipart/form-data`，字段名 `file`
  - 校验格式（仅 JPG/PNG/WEBP）、大小限制（5MB�?
  - 保存�?`uploads/avatar/{user_id}_{uuid}.{ext}`
  - 更新用户 `avatar_url` 字段
  - 返回 `{ code: 200, data: { avatar_url: "/uploads/avatar/xxx.jpg" } }`

- **用户信息接口**：`GET /api/users/me`
  - 返回 `UserResponse`，包�?`avatar_url` 字段

### 后端补充修改

#### 修改 1：`backend/users/app/main.py` �?挂载静态文件目�?

```python
from fastapi.staticfiles import StaticFiles
from pathlib import Path

# 确保上传目录存在
UPLOAD_DIR = Path("uploads")
UPLOAD_DIR.mkdir(parents=True, exist_ok=True)

# 挂载静态文件目录：使上传的头像等文件可通过 /uploads/ 访问
app.mount("/uploads", StaticFiles(directory=str(UPLOAD_DIR)), name="uploads")
```

> **注意**：`app.mount()` 建议放在所有路由注册之后，避免路径匹配优先级问题�?

#### 修改 2：`nginx/nginx.windows.conf` �?新增 `/uploads/` 代理

```nginx
# 用户上传文件（头像等�?
location /uploads/ {
    proxy_pass http://users_api/uploads/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    expires 7d;
    add_header Cache-Control "public";
}
```

#### 修改 3：`nginx/nginx.conf` �?生产环境同步新增

�?HTTP (80) �?HTTPS (443) 两个 server 块中均新增了 `/uploads/` 代理配置，与 Windows 开发配置一致�?

### 前端补充修改

#### 修改 4：`src/api/auth.ts` �?修复上传路径

```diff
- url: authUrl('/users/me/avatar'),
+ url: authUrl('/me/avatar'),
```

`AUTH_API_URL` 已包�?`/api/users` 前缀，path 不应再重�?`/users/`。其他所有接口（登录、验证码、获取用户信息等）均使用�?`/users/` 前缀�?path，仅此一处写错�?

#### 修改 5：`src/store/auth.ts` �?头像 URL 解析

`fetchUserProfile()` 中对后端返回�?`avatar_url` 调用 `resolveMediaUrl()` 转换为完整可访问 URL�?

```typescript
const resolvedAvatar = resolveMediaUrl(userInfo.avatar_url);
this.user = {
    ...
    avatar: resolvedAvatar,
    ...
};
```

#### 修改 6：`src/utils/url.ts` �?`resolveMediaUrl()` 函数

将后端返回的相对路径（如 `/uploads/avatar/xxx.jpg`）转换为完整 URL�?

- 优先使用 `VITE_MEDIA_BASE_URL` 环境变量
- 否则�?`VITE_BASE_API_URL` 推导 origin
- IPv4 地址保留 `http://` 协议（不强制升级�?`https://`�?
- 对已是完�?HTTPS URL 的原样透传

#### 修改 7：`src/pages/.../edit-profile/index.vue` �?编辑页头像显�?

```typescript
const displayAvatar = computed(() => {
    const avatar = authStore.user?.avatar;
    return resolveMediaUrl(avatar) || avatar || '/static/tabbar/my.png';
});
```

#### 修改 8：`src/components/shared/AvatarPreview.vue` �?头像预览弹窗

```typescript
const resolvedSrc = computed(() => {
    const src = props.src || '';
    return resolveMediaUrl(src) || src;
});
```

### 完整请求链路（修复后�?

```
�?上传
前端裁剪 �?uni.uploadFile �?POST http://10.60.105.235:8080/api/users/me/avatar
�?Nginx /api/users/ �?user_service:8002/api/v1/users/me/avatar
�?后端保存文件 �?返回 { avatar_url: "/uploads/avatar/xxx.jpg" }

�?预览/展示
前端 resolveMediaUrl("/uploads/avatar/xxx.jpg") �?http://10.60.105.235:8080/uploads/avatar/xxx.jpg
�?Nginx /uploads/ �?user_service:8002/uploads/avatar/xxx.jpg
�?StaticFiles 返回图片二进�?
```

### 环境变量配置

前端 `D:\saas_app-main\.env.development.local` 需配置�?

```env
VITE_AUTH_API_URL=http://10.60.105.235:8080/api/users
VITE_MEDIA_BASE_URL=http://10.60.105.235:8080
```

### 部署方式

后端修改�?`main.py` �?nginx 配置，需重建镜像并重启：

```powershell
# 重新构建 user_service �?nginx 镜像
docker compose -f compose-app.windows.yml up --build -d user_service nginx

# 或完整重建所有服�?
docker compose -f compose-app.windows.yml up --build -d
```

前端修改无需部署，重�?dev server 即可生效�?

---

## 十一、分类筛选逻辑修复 + 模型导入 + 专家字段补全

**时间**�?026-05-21
**涉及文件�? 个）**�?
- `backend/live_core_service/app/crud/homepage_search.py`�? 处修改）
- `backend/live_core_service/app/models/__init__.py`�? 行补导入�?
- `backend/live_core_service/app/api/v1/endpoints/experts.py`�? 行补字段�?

### 问题背景

经分析，分类功能存在 4 个后端问题：

1. **P0-1**：首�?`GET /homepage/rooms?category_id=xxx` 筛选仍�?`live_rooms.category_id` 单字段，不走 `live_room_categories` 多对多表。后台通过 `POST /admin/rooms/{id}/categories` 设置的分类，首页筛选查不到�?
2. **P0-2**：搜索接�?`GET /search?category_id=xxx` 同款问题，room 搜索�?`category_filter` 也是单字段�?
3. **P1-1**：`LiveRoomCategory` 模型未在 `models/__init__.py` 中导入，`create_all()` 不会自动创建该表�?
4. **P1-2**：`featured-experts` 返回字段不含 `department` �?`expertise_areas`，导致前端专家页按科室筛选无效�?

### 修改详情

#### P0-1：首页分类筛选兼容多对多（`crud/homepage_search.py` �?371�?

```python
# 修改�?
if category_id is not None:
    category_where = "WHERE lr.category_id = :category_id"

# 修改后（兼容旧单字段 + 新多对多 live_room_categories�?
if category_id is not None:
    category_where = """
        WHERE (
            lr.category_id = :category_id
            OR EXISTS (
                SELECT 1 FROM live_room_categories lrc
                WHERE lrc.room_id = lr.id AND lrc.category_id = :category_id
            )
        )
    """
```

#### P0-2：搜索分类筛选同款修复（`crud/homepage_search.py` �?574�?

```python
# 修改�?
if category_id is not None:
    category_filter = "AND category_id = :category_id"

# 修改�?
if category_id is not None:
    category_filter = """AND (
        live_rooms.category_id = :category_id
        OR EXISTS (
            SELECT 1 FROM live_room_categories lrc
            WHERE lrc.room_id = live_rooms.id AND lrc.category_id = :category_id
        )
    )"""
```

#### P1-1：补导入 LiveRoomCategory（`models/__init__.py` �?16�?

```python
# 修改�?
from .content_management import Tag, Category, SessionTag

# 修改�?
from .content_management import Tag, Category, SessionTag, LiveRoomCategory
```

#### P1-2：featured-experts 补字段（`api/v1/endpoints/experts.py` �?126�?

```python
# 返回体中增加两行
"department": expert.department,
"expertise_areas": expert.expertise_areas,
```

### 部署方式

```powershell
# 推荐：重建镜像（确保所有修改生效）
docker compose -f compose-app.windows.yml up --build -d live_core_service

# 备选：快�?cp + 重启
docker cp "backend\live_core_service\app\crud\homepage_search.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/crud/homepage_search.py
docker cp "backend\live_core_service\app\models\__init__.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/models/__init__.py
docker cp "backend\live_core_service\app\api\v1\endpoints\experts.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/api/v1/endpoints/experts.py
docker restart live-streaming-saas-v2-main-live_core_service-1
```

> **注意**：`docker cp` 目标路径�?`/app/app/...`（容器内 `WORKDIR /app` + `app/` 子目录）

### 关于种子数据

**不需要重新加载�?* `up --build` 只重建应用镜像和容器，数据库在独立的 `postgres` 容器中，数据�?`pgdata:/var/lib/postgresql/data` 持久化存储。已插入的分类（15 个）、直播间�? 个）、品牌（10 个）、专家（400 名）、焦点图等种子数据完全不受影响�?

仅当执行�?`docker compose down -v`（删除数据卷）后才需要重新运行种子脚本�?

---

## 十二、直播创�?编辑状态收敛修�?

**时间**�?026-05-24

**涉及文件（后�?3 �?+ 前端 4 个）**�?

- 后端 `backend/live_core_service/app/schemas/session_import.py`
- 后端 `backend/live_core_service/app/services/session_import.py`
- 后端 `backend/live_core_service/app/services/session_service.py`
- 前端 `D:\saas_app-main\src\components\app\CreateLiveDrawer.vue`
- 前端 `D:\saas_app-main\src\pages\app\live-manage\create.vue`
- 前端 `D:\saas_app-main\src\pages\app\live-manage\edit.vue`
- 前端 `D:\saas_app-main\src\types\session.ts`

### 问题背景

直播场次当前后端状态枚举包含：

```text
scheduled / live / finished / processing / ready / error
```

其中�?

- `scheduled`：预�?计划中，可由用户创建和维护�?
- `ready`：回放已就绪，用户导入已有回放链接时可创建�?
- `live`：真实推流开始后，由 SRS `on_publish` 回调写入�?
- `finished`：推流结束后，由 SRS `on_unpublish` 回调写入�?
- `processing`：直播结束后后台生成回放时写入�?
- `error`：后台生成回放失败时写入�?

原实现存在状态边界混乱：

1. 创建直播时前端允许选择 `scheduled / live / finished`�?
2. 有回放地址时，前端可能把场次导入为 `live` �?`finished`�?
3. 编辑直播页把 `scheduled / live / finished / ready / processing / error` 全部暴露给用户选择�?
4. 后端 `PATCH /sessions/{session_id}` 的状态限制被注释，前端传入系统态时后端也会接受�?

这会导致用户可以手动制造“没有真实推流但状态是直播中”的假直播，或手动写�?`processing/error` 这类本应由后台任务生成的内部状态�?

### 目标设计

用户侧只管理两类业务态：

| 用户视角 | 后端状�?| 说明 |
|----------|----------|------|
| 预告 | `scheduled` | 创建未来直播，后续由推流回调切为直播�?|
| 回放 | `ready` | 用户直接提供已有视频链接，进入直播间播放回放 |

系统侧状态只读：

| 后端状�?| 来源 |
|----------|------|
| `live` | SRS `on_publish` |
| `finished` | SRS `on_unpublish` |
| `processing` | Celery 后台任务 |
| `error` | Celery 后台任务异常 |

### 后端修改详情

#### 修改 1：导入回放接口只允许 `ready`

文件�?

- `backend/live_core_service/app/schemas/session_import.py`
- `backend/live_core_service/app/services/session_import.py`

修改前：

```python
if v not in ['finished', 'ready', 'live']:
    raise ValueError("导入会话�?status 只能�?'finished', 'live', �?'ready'")
```

修改后：

```python
if v != 'ready':
    raise ValueError("导入回放会话�?status 只能�?'ready'")
```

服务层同步兜底：

```python
if status != "ready":
    raise InvalidParameterException(
        message="导入回放会话�?status 只能�?'ready'",
        code=4001,
    )
```

影响接口�?

```http
POST /api/v1/rooms/{room_id}/sessions/import
```

现在该接口只用于“导入已有回放视频”，不再允许用户通过该接口创�?`live` �?`finished` 场次�?

#### 修改 2：用户编辑接口禁止写入系统�?

文件�?

```text
backend/live_core_service/app/services/session_service.py
```

位置：`SessionService.update_scheduled_session_info()`

新增规则�?

- 用户接口只允许写�?`scheduled` �?`ready`�?
- 禁止手动写入 `live / finished / processing / error`�?
- `ready` 必须存在有效 `playback_url`�?
- `scheduled -> ready` 允许，表示用户给预告场次补充回放链接后转为回放�?
- `ready -> ready` 允许，用于维护回放地址�?
- `error -> ready` 允许，用于后台生成失败后用户手动补回放链接修复�?
- `live / finished / processing` 不允许通过用户编辑接口修改计划时间或回放地址�?

核心逻辑�?

```python
if requested_status is not None:
    if requested_status_value not in (
        LiveSessionStatus.SCHEDULED.value,
        LiveSessionStatus.READY.value,
    ):
        raise SessionActionForbiddenException("不允许手动修改为该场次状�?)

    if requested_status_value == LiveSessionStatus.READY.value:
        effective_playback_url = update_data.get("playback_url", session.playback_url)
        if not effective_playback_url or not str(effective_playback_url).strip():
            raise SessionActionForbiddenException("设置为回放状态必须提供回放地址")
```

同时保留系统流程�?

- SRS 回调仍可写入 `live / finished`�?
- Celery 后台任务仍可写入 `processing / ready / error`�?
- 本次限制只作用于用户编辑接口�?

### 前端修改详情

#### 修改 1：创建抽屉收敛为“预�?回放�?

文件�?

```text
D:\saas_app-main\src\components\app\CreateLiveDrawer.vue
```

修改前：

```typescript
const createModeOptions = [
  { value: 'scheduled', label: '预告' },
  { value: 'live', label: '直播�? },
  { value: 'finished', label: '已结�? }
];
```

修改后：

```typescript
const createModeOptions = [
  { value: 'ready', label: '回放', tip: '使用现有视频链接，用户进入后直接观看回放' }
];
```

当前行为�?

- 未填�?`playback_url`：走普通创建场次接口，后端创建 `scheduled`�?
- 已填�?`playback_url`：走导入接口，前端固定提�?`status: 'ready'`�?

```typescript
status: 'ready' as const
```

#### 修改 2：独立创建页同步修复

文件�?

```text
D:\saas_app-main\src\pages\app\live-manage\create.vue
```

同步调整�?

- 默认状态从 `live` 改为 `scheduled`�?
- 有回放地址时固定创建为 `ready`�?
- 不再允许创建 `live / finished`�?

#### 修改 3：编辑页状态只�?可编辑分�?

文件�?

```text
D:\saas_app-main\src\pages\app\live-manage\edit.vue
```

修改前：

```typescript
const statusOptions = [
  { value: 'scheduled', label: '📅 已计�? },
  { value: 'live', label: '🔴 直播�? },
  { value: 'finished', label: '�?已结�? },
  { value: 'ready', label: '🎬 已就绪（回放�? },
  { value: 'processing', label: '�?处理�? },
  { value: 'error', label: '�?错误' }
];
```

修改后：

```typescript
const statusOptions = [
  { value: 'scheduled', label: '预告' },
  { value: 'ready', label: '回放' }
];
```

新增只读状态映射：

```typescript
const statusLabelMap: Record<string, string> = {
  scheduled: '预告',
  live: '直播�?,
  finished: '已结�?,
  processing: '回放生成�?,
  ready: '回放',
  error: '回放生成失败'
};
```

编辑页行为：

| 当前状�?| 页面行为 |
|----------|----------|
| `scheduled` | 可选择“预�?回放”；填写回放地址后自动转回放 |
| `ready` | 可维护回放地址 |
| `live` | 状态只读，只保存房间基础信息 |
| `finished` | 状态只读，只保存房间基础信息 |
| `processing` | 状态只读，只保存房间基础信息 |
| `error` | 状态只读；若用户填写回放地址，可提交 `ready` 修复 |

保存时新增保护：

```typescript
const editableStatuses: BackendSessionStatus[] = ['scheduled', 'ready'];
const canEditSession = editableStatuses.includes(originalSessionStatus.value as BackendSessionStatus);
const canRecoverError = originalSessionStatus.value === 'error' && !!formData.value.playback_url;

if (canEditSession) {
  sessionUpdateData.start_time = startTime;
  sessionUpdateData.status = formData.value.status;
  ...
} else if (canRecoverError) {
  sessionUpdateData.status = 'ready';
  sessionUpdateData.playback_url = formData.value.playback_url;
}
```

#### 修改 4：前端类型最小兼�?

文件�?

```text
D:\saas_app-main\src\types\session.ts
```

为避免影�?H5 旧页面和现有测试，未全量替换全局 `SessionStatus`�?

仅将导入回放 payload 收窄为：

```typescript
export interface SessionImportPayload {
  title: string;
  description?: string;
  start_time: string;
  playback_url: string;
  status: 'ready';
}
```

同时允许用户编辑接口�?`ready`�?

```typescript
status?: SessionStatus | 'ready';
```

### 验证结果

#### 后端语法验证

执行�?

```powershell
cd backend\live_core_service
python -m py_compile app\schemas\session_import.py app\services\session_import.py app\services\session_service.py
```

结果：通过，无 Python 语法错误�?

#### 前端类型检�?

执行�?

```powershell
cd D:\saas_app-main
npm.cmd exec vue-tsc -- --noEmit
```

结果：项目仍存在一批既有类型错误，主要集中�?H5 页面、通知、首�?mock、测试文件等非本次修改范围�?

本次相关文件�?

- `src/components/app/CreateLiveDrawer.vue`
- `src/pages/app/live-manage/create.vue`
- `src/pages/app/live-manage/edit.vue`
- `src/types/session.ts`

经筛选未发现本次修改文件新增类型报错�?

### 部署方式

后端修改需要重�?`live_core_service`�?

```powershell
docker compose -f compose-app.windows.yml up --build -d live_core_service
```

或临时调试复制文件进容器�?

```powershell
docker cp "backend\live_core_service\app\schemas\session_import.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/schemas/session_import.py
docker cp "backend\live_core_service\app\services\session_import.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/services/session_import.py
docker cp "backend\live_core_service\app\services\session_service.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/services/session_service.py
docker restart live-streaming-saas-v2-main-live_core_service-1
```

前端修改需要重启前端开发服务：

```powershell
cd D:\saas_app-main
npm.cmd run dev:h5
```

### 后续待关�?

1. 当前 `finished -> processing -> ready` 的后台链路已有状态变化，�?`auto_generate_playback_url()` 未确认被后台任务调用。后续若要真正实现“直播结束后自动生成回放链接”，需要补齐回�?URL 生成和写入链路�?
2. H5 旧页面仍使用 `ended / archived` 等旧状态命名，后续建议统一前端状态类型，避免 App 端和 H5 端长期分叉�?
3. 如果未来需要主播手动“开始直�?结束直播”，建议新增独立动作接口，例�?`POST /sessions/{id}/start`、`POST /sessions/{id}/stop`，不要重新开放通用 PATCH 写入 `live/finished`�?

### 复核补充修正�?026-05-24�?

本轮复核前后端状态收敛后，发现并修正 4 个隐性冲突：

1. **前端 ready -> scheduled 误操作入�?*
   - 问题：编辑页原先只要当前表单状态是 `scheduled` �?`ready` 就展示状态选择器，导致已有 `ready` 回放场次可以�?UI 上选择�?`scheduled`�?
   - 后端规则：`ready -> scheduled` 不允许，只有原始 `scheduled` 场次可以保持/切换预告与回放�?
   - 修正：`D:\saas_app-main\src\pages\app\live-manage\edit.vue` �?`isStatusSelectable` 改为仅当 `originalSessionStatus === 'scheduled'` 时可选；已有 `ready` 仅显示只读“回放”，仍可维护回放地址�?

2. **后端 video_id 绕过 UI 手动修改**
   - 问题：`update_scheduled_session_info()` 的可编辑计划字段仍包�?`video_id`，外部客户端可以绕过前端直接 PATCH 系统字段�?
   - 风险：`video_id` 属于录制/转码链路生成字段，用户手动改可能造成回放生成链路、唯一约束或资源引用异常�?
   - 修正：`backend/live_core_service/app/services/session_service.py` 明确拒绝用户接口传入 `video_id`，并�?`schedule_fields` 移除�?

3. **历史脏数�?ready 但无 playback_url 的保存死�?*
   - 问题：如果历史数据或后台任务产生 `ready` 但没�?`playback_url`，编辑页会因“回放必须填写地址”阻止用户保存房间标�?封面等基础信息，同时后端也会拒绝继续提�?`ready`�?
   - 修正：前端记�?`originalPlaybackUrl`。当原始状态就�?`ready` 且原�?当前回放地址都为空时，允许保存房间信息，并跳过场次更新；用户后续补充回放地址后，可正常提交并修复该场次�?

4. **scheduled + playback_url 矛盾组合**（⚠�?二次复核判定为误拦截，已回滚�?
   - 初次分析（误判）：认为外部客户端可直�?PATCH `status=scheduled` 且带非空 `playback_url`，形�?预告场次但已有回放地址"的业务矛盾。初次修正为后端直接拒绝�?
   - **二次复核结论�?026-05-24�?*：上述拦截属�?*误判**。`playback_url` 字段�?`LiveSession` 中具有双重语义：
     - **预告/直播�?*：代表直播流播放地址（SRS 需要此 URL 来写�?分发直播数据，若无此 URL 则推流无法被观众观看�?
     - **回放（ready�?*：代表已录制视频的回放地址
   - 因此 `scheduled` 场次**允许同时携带 `playback_url`** 是完全合法的业务场景，不是矛盾�?
   - **已回�?*：移�?`session_service.py` �?`"预告状态不允许填写回放地址"` 的拒绝逻辑（原�?360-366）�?
   - 保留的合理规则：
     - 若客户端只传 `playback_url` 不传 `status`，且当前状态为 `scheduled`，仍自动转为 `ready`（兼容原�?填写回放→自动转回放"设计�?
     - 若客户端显式�?`status=scheduled` + `playback_url`，后端接受并保持 `scheduled`（用户意图明确：只是给预告配置直播流 URL�?

复核验证�?

```powershell
cd backend\live_core_service
python -m py_compile app\services\session_service.py app\schemas\session_import.py app\services\session_import.py
```

---

## 十五、头像持久化 + 认证路径修正�?026-06-03�?

> 本轮聚焦：修�?user_service 容器重建后头像文件丢�?+ 修正 nginx 代理模式下认�?API 路径双重前缀

### 15.1 头像持久�?�?Docker 卷挂�?

**问题**：`compose-app.windows.yml` �?`user_service` 没有任何 volumes，头像文件存在容器内 `uploads/avatar/`，每�?`docker compose up -d` 重建容器文件全部丢失。数据库�?`avatar_url` 还在但磁盘文件已消失�?

**文件**：`compose-app.windows.yml`

**修改**：为 `user_service` 新增持久化目录挂�?

```yaml
  user_service:
    build: ./backend/users
    restart: unless-stopped
    ports: ["8002:8002"]
    environment:
      POSTGRES_DB: "users_service_test"
    env_file: .env.docker
    volumes:
      # 持久化用户上传文件（头像等），避免容器重建后丢失
      - ./data/user_uploads:/app/uploads
    networks:
      - live-network
```

**部署方式**�?

```powershell
# 1. 创建主机持久化目录（仅首次执行一次）
New-Item -ItemType Directory -Path "d:\live-streaming-saas-v2-main\data\user_uploads" -Force

# 2. 强制重建 user_service 容器使卷挂载生效
docker compose -f compose-app.windows.yml up -d --force-recreate user_service

# 3. 重启 nginx 刷新容器 DNS
docker compose -f compose-app.windows.yml restart nginx
```

> **执行时机与范�?*�?
> - ⚠️ 上述操作**仅适用于本�?Windows Docker 开发环�?*（`compose-app.windows.yml`），不涉及云端�?
> - **建议**：等所有后端修改完善后进行统一部署时一并执行，避免频繁重建容器�?
> - 部署到腾讯云/CVM 时，用户上传文件应改�?*腾讯云对象存�?COS**，不�?Docker 层面依赖卷挂载。COS 方案需后端同学额外开发存储适配层，不在本次修改范围内�?

> **后续部署注意**：这是本�?Docker 开发的持久化方案。部署到腾讯�?CVM 时，应将用户上传文件改为对象存储（腾�?COS），不在 Docker 层面依赖卷挂载�?

### 15.2 认证 API 路径修正

**问题**：切换为 nginx 代理模式后，`AUTH_API_URL` �?`http://10.0.2.2:8002/api/v1`（直连）变为 `http://10.0.2.2:8080/api/users`（nginx）。ngix 已将 `/api/users/` 代理�?`user_service/api/v1/`，前端路径不能再�?`/users/` 前缀，否则产生双重路径�?

```
修改前：AUTH_API_URL(/api/users) + /users/me �?/api/users/users/me �?404
修改后：AUTH_API_URL(/api/users) + /me       �?/api/users/me       �?200
```

**文件**：`src/api/auth.ts`

| 函数 | 修改�?| 修改�?|
|------|--------|--------|
| `getCurrentUser` | `authUrl('/users/me')` | `authUrl('/me')` |
| `updateProfile` | `authUrl('/users/me')` | `authUrl('/me')` |
| `bindPhone` | `authUrl('/users/me/phone')` | `authUrl('/me/phone')` |
| `uploadAvatar` | `authUrl('/users/me/avatar')` | `authUrl('/me/avatar')` |

### 15.3 前端环境变量修正

**文件**：`.env.development.local`

| 配置�?| 修改前（错误，直连模式） | 修改后（nginx 代理模式�?|
|--------|------------------------|------------------------|
| `VITE_BASE_API_URL` | `http://10.0.2.2:8008/api/v1` | `http://10.0.2.2:8080/api/core` |
| `VITE_AUTH_API_URL` | `http://10.0.2.2:8002/api/v1` | `http://10.0.2.2:8080/api/users` |
| `VITE_MEDIA_BASE_URL` | `http://10.0.2.2:8002` | `http://10.0.2.2:8080` |

> **规则**：Docker Compose 端口映射写死�?YAML 中，nginx 永远�?8080。只要不改连接方式（nginx vs 直连），配置永不改变�?

### 15.4 sessionTags API 路径修正

**文件**：`src/api/sessionTags.ts`

| 函数 | 修改�?| 修改�?|
|------|--------|--------|
| `setSessionTags` | `POST /admin/sessions/{id}/tags` | `POST /sessions/{id}/tags` |
| `removeSessionTag` | `DELETE /admin/sessions/{id}/tags/{tagId}` | `DELETE /sessions/{id}/tags/{tagId}` |

同时补充 `mode: 'replace' | 'append'` 参数（后�?schema 要求必填）�?

---

## 十六、分�?标签创建关联前端实现�?026-06-03�?

> 本轮聚焦：在创建直播页面（`create.vue` + `CreateLiveDrawer.vue`）中添加分类/标签选择器，替代占位按钮

### 16.1 后端 API 已就�?

后端分类/标签关联 API 在此前已完整实现（参见十�?+ 附录 A）：

- `GET /categories` / `GET /tags` �?获取可选列�?
- `POST /admin/rooms/{room_id}/categories` �?设置房间分类（ADMIN 或房间创建者）
- `POST /sessions/{session_id}/tags` �?设置场次标签（任意登录用户）

### 16.2 新建文件

**文件**：`src/api/roomCategories.ts`（新建）

封装直播间分类关联的 3 个端点：

| 导出函数 | HTTP | 路径 |
|---------|------|------|
| `getRoomCategories` | GET | `/rooms/{roomId}/categories` |
| `setRoomCategories` | POST | `/admin/rooms/{roomId}/categories` |
| `removeRoomCategory` | DELETE | `/admin/rooms/{roomId}/categories/{categoryId}` |

已在 `src/api/index.ts` 中注册导出�?

### 16.3 前端 UI 实现

**涉及文件�? 个）**�?

| 文件 | 修改 |
|------|------|
| `src/pages/app/live-manage/create.vue` | 占位按钮 �?功能分类/标签选择器（ScrollablePickerSheet�?|
| `src/components/app/CreateLiveDrawer.vue` | 同上 |
| `src/api/index.ts` | 新增 `export * from './roomCategories'` |

#### create.vue 改动详情

**模板**�?更多设置"区域中直播分类和场次标签�?`placeholder-btn + showPlaceholderToast` 替换为：
- 已选分类卡�?+ "+ 选择分类"按钮
- 标签胶囊列表 + "+ 添加标签"按钮

**脚本新增**�?
- 导入：`getCategories`、`getTags`、`setSessionTags`、`setRoomCategories`
- 状态：`selectedCategories`、`selectedTags`、`showCategoryPicker`、`showTagPicker`
- Handler：`handleOpenCategoryPicker`、`handleCategorySelect`、`removeCategory`、`handleOpenTagPicker`、`handleTagSelect`、`removeTag`
- `handleCreate` 新增步骤 5（关联分类）+ 步骤 6（关联标签）

**CSS 新增**：`.tag-chip`、`.tag-list`、`.multi-card-icon`

#### CreateLiveDrawer.vue 同步修改

�?create.vue 模式，在"专家与品�?section 之后新增"分类与标�?section�?

### 16.4 依赖关系说明

- **Categories** �?**Tags** �?ADMIN 在后台管理界面维护（系统元数据）�?
- **Room-Categories** 由房间创建者或 ADMIN 在创�?编辑直播时设置（业务关联）�?
- **Session-Tags** 由任意登录用户在创建/编辑场次时设置（业务关联）�?
- 前端创建直播时：先拿�?`room_id` �?`session_id` �?再分别调用关联接口�?

---

## 十七、播放页标签展示�?026-06-03�?

**文件**：`src/pages/app/live/LiveView.vue`

### 问题

`LiveView.vue` 中标签展示代码仅�?fallback 分支存在，从未被数据填充。需�?Header 区添加标签展示并实际调用 API 获取数据�?

### 实现

**展示位置**：Header 区域 Meta 行下方、Expert 行上�?

```
标题: test-分类标签
元信�? 回放 · 1.2k 查看 · 06/03 16:45
标签:  #手术直播  #腹腔�? #微创手术  #胃癌   �?新增
专家: 万鹏�?| 主任医师 | 博士生导�?
操作: ⭐收�? 📤分享  👍点赞
```

**改动�?*�?

| 层级 | 改动 |
|------|------|
| import | 新增 `import { getSessionTags } from '@/api/sessionTags'` |
| 状�?| 新增 `const sessionTags = ref<any[]>([])` |
| 模板 | �?`.meta-row` 下方新增 `.tag-row` 标签展示 |
| 数据加载 | `loadSessionData()` 中品牌数据之后新增标签获�?|
| CSS | 新增 `.tag-row` `.tag-chip` 样式 |

---

## 十八、Tab 公开接口权限完善�?026-06-03�?

### 问题

后端 Tab 管理接口 GET �?POST 权限不一致：

```
GET  /admin/rooms/{id}/tabs  �? _check_admin_role       �?�?ADMIN
POST /admin/rooms/{id}/tabs  �? _check_tab_management   �?ADMIN + 房间创建�?
```

房间创建者能创建 Tab，但通过 admin GET 读不到自己的 Tab�?03）�?

### 修复

**文件**：`src/pages/app/live-manage/edit.vue`

编辑直播时读�?Tab 改用公开端点（权限完全一致，返回结果相同）：

| 位置 | 修改�?| 修改�?|
|------|--------|--------|
| import | `import { getRoomTabList, ... }` | `import { getPublicRoomTabList, ... }` |
| 调用 | `await getRoomTabList(roomId.value)` | `await getPublicRoomTabList(roomId.value)` |

### 最�?Tab 端点状�?

| 方法 | 路径 | 前端函数 | 谁可以调 |
|------|------|---------|---------|
| `GET` | `/rooms/{id}/tabs` | `getPublicRoomTabList` | 所有人 |
| `POST` | `/admin/rooms/{id}/tabs` | `createRoomTab` | ADMIN + 创建�?|
| `PUT` | `/admin/tabs/{id}` | `updateRoomTab` | ADMIN + 创建�?|
| `DELETE` | `/admin/tabs/{id}` | `deleteRoomTab` | ADMIN + 创建�?|

---

## 十九、品�?Tab 跳转路径修正�?026-06-03�?

**文件**：`src/components/BrandsTab.vue`

**问题**：`goToBrandDetail` 跳转�?`/pages/brand/detail`，但 `pages.json` 中注册的路径�?`/pages/app/brand/detail`（少�?`app/`），点击品牌卡片无法跳转�?

**修复**�?

```diff
- url: `/pages/brand/detail?id=${brandId}`
+ url: `/pages/app/brand/detail?id=${brandId}`
```

结果：通过�?

```powershell
cd D:\saas_app-main
npm.cmd exec vue-tsc -- --noEmit --pretty false
```

结果：前端项目仍存在既有全局类型错误，但本轮相关文件 `src/pages/app/live-manage/edit.vue` 未出现在错误列表中�?

---

## 十三、创建直播完整链路修复（2026-05-24�?

> 本轮聚焦：playback_url 必填化、状态流转完善、权限补全、展示链路修�?

### 13.1 playback_url 对所有场次必�?

**背景**：`playback_url` �?`LiveSession` 中具有双重语义：
- **预告（scheduled�?*：直播流播放地址（SRS 写入/分发直播数据的目�?URL�?
- **回放（ready�?*：已录制视频的回放地址

因此 `scheduled` 场次同样需�?`playback_url`，否则推流无法被观众观看�?

**涉及文件（后�?3 �?+ 前端 3 个）**�?

#### 后端修改

**修改 1**：`schemas/live_core.py` �?`LiveSessionBase` 新增 `playback_url`

```python
class LiveSessionBase(BaseModel):
    # ... 其他字段 ...
    playback_url: Optional[str] = Field(None, max_length=1024, description="播放地址（预�?直播流写入地址，回�?已录制视频地址�?)
```

**修改 2**：`schemas/live_core.py` �?`ScheduledSessionCreate` 新增必填 `playback_url`

```python
class ScheduledSessionCreate(BaseModel):
    start_time: datetime = Field(..., description="计划开始时�?)
    playback_url: str = Field(..., max_length=1024, description="播放地址（必填）")
```

**修改 3**：`services/session_service.py` �?`create_scheduled_session()` 传�?`playback_url`

```python
session_create = LiveSessionCreate(
    # ... 其他字段 ...
    playback_url=session_in.playback_url
)
```

#### 前端修改

**修改 4**：`types/session.ts` �?`SessionCreatePayload` 新增必填 `playback_url`

**修改 5**：`CreateLiveDrawer.vue` �?表单"回放地址（可选）"改为"播放地址 *（必填）"，验证逻辑同步

**修改 6**：`create.vue` �?同上

### 13.2 playback_url_hash 创建时同步计�?

**文件**：`schemas/live_core.py`、`services/session_service.py`

**问题**：`create_scheduled_session` 保存 `playback_url` 但未计算 `playback_url_hash`，与导入回放不一致，可能导致幂等去重失效�?

**修改**�?
- `LiveSessionBase` 新增 `playback_url_hash: Optional[str]`
- `create_scheduled_session()` 在构�?`LiveSessionCreate` 后调�?`calc_playback_url_hash()` 同步计算 hash

### 13.3 创建响应补充 playback_url

**文件**：`api/v1/endpoints/room.py`

**问题**：`POST /rooms/{room_id}/sessions` 响应体缺�?`playback_url`，前端需额外调用 `fetchSessionsByRoomId` 刷新获取�?

**修改**：响应体新增 `"playback_url": new_session.playback_url`

### 13.4 首页 ready 映射修正

**文件**：`services/homepage_search_service.py`

**问题**：`_determine_live_status()` �?`ready` �?`scheduled` 都映射为 `LiveStatusEnum.SCHEDULED`，导致回放直播间在首页显示为"预告�?�?

**修改**�?
```python
# 修改�?
elif session_status in ['ready', 'scheduled']:
    return LiveStatusEnum.SCHEDULED

# 修改�?
elif session_status == 'scheduled':
    return LiveStatusEnum.SCHEDULED
elif session_status == 'ready':
    return LiveStatusEnum.REPLAY  # 回放
```

---

## 十五、小程序端搜索与专家模块合并�?026-05-31 ~ 2026-06-02�?

> 本轮聚焦：从微信小程序端（`live-streaming-saas-v2-main(wechat)`）合并搜索增强功能和专家列表增强到当�?App 端项目。使�?Git 分支 `merge/wechat-search-experts` 进行安全合并，原始代码保留在 `master` 分支�?

### 15.0 合并策略

- **Git 仓库**：`https://github.com/HuangShiYu-a/live-streaming-saas-main-v2-app.git`（备份用�?
- **基线分支**：`master`（原始版本，未改动）
- **合并分支**：`merge/wechat-search-experts`�? 步增量提交）
- **小程序端源路�?*：`D:\live-streaming-saas-v2-main(wechat)\live-streaming-saas-v2-main\backend\`
- **目标路径**：`d:\live-streaming-saas-v2-main\backend\`

### 15.1 搜索模块（后�?4 个新文件 + 4 个修改文件）

**时间**�?026-05-31
**涉及文件�? 个）**�?

#### 新增文件�? 个）

| 文件 | 说明 |
|------|------|
| `backend/live_core_service/app/models/search.py` | `UserSearchHistory` + `SearchKeywordStats` 两个 ORM 模型 |
| `backend/live_core_service/app/schemas/search.py` | `SearchHistoryItem`、`HotKeywordItem`、`SearchSuggestionItem` Pydantic Schema |
| `backend/live_core_service/app/crud/search.py` | `record_search()` 落库 + 搜索历史 CRUD + 热词/建议/推荐查询 |
| `backend/live_core_service/app/api/v1/endpoints/search_extra.py` | 6 �?API 端点：搜索历�?CRUD + 热门搜索 + 搜索建议 + 搜索推荐 |

#### 修改文件�? 个）

| 文件 | 改动 |
|------|------|
| `app/models/__init__.py` | 新增导入 `UserSearchHistory, SearchKeywordStats`（保留现有所有导入） |
| `app/api/v1/api.py` | 新增 `import search_extra` + 注册 `search_history_router`（`/users/me`�? `search_extra_router`（`/search`�?|
| `app/api/v1/endpoints/homepage_search.py` | 搜索端点新增 `current_user` 参数 + `record_search()` 联动（搜索即记录历史�?|
| `app/api/v1/endpoints/room.py` | 新增 `users_me_rooms_router` + `GET /users/me/rooms` 端点 |

#### 新增数据库表�? 张）

```sql
CREATE TABLE user_search_history (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    keyword VARCHAR(255) NOT NULL,
    keyword_norm VARCHAR(255) NOT NULL,
    search_count INT NOT NULL DEFAULT 1,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, keyword_norm)
);

CREATE TABLE search_keyword_stats (
    id UUID PRIMARY KEY,
    keyword_norm VARCHAR(255) NOT NULL UNIQUE,
    keyword VARCHAR(255),           -- 原始关键词（保留大小写）
    total_count BIGINT NOT NULL DEFAULT 0,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

#### 新增 API 端点汇�?

| 方法 | 路径 | 认证 | 功能 |
|------|------|:--:|------|
| `GET` | `/api/v1/search/hot-keywords?limit=10&days=7` | 公开 | 热门搜索词排�?|
| `GET` | `/api/v1/search/suggestions?keyword=xxx&limit=10` | 公开 | 输入前缀联想 |
| `GET` | `/api/v1/search/recommendations?keyword=xxx&limit=8` | 可�?| 搜索推荐（含个性化 boost�?|
| `GET` | `/api/v1/users/me/search-history?limit=10` | 必须 | 我的搜索历史 |
| `DELETE` | `/api/v1/users/me/search-history/{id}` | 必须 | 删除单条历史 |
| `DELETE` | `/api/v1/users/me/search-history` | 必须 | 清空全部历史 |
| `GET` | `/api/v1/users/me/rooms?page=1&size=20` | 必须 | 我创建的直播间列�?|

#### 搜索种子数据

�?`search_keyword_stats` 表中插入 10 条医疗领域热词种子数据：

```sql
INSERT INTO search_keyword_stats (id, keyword_norm, keyword, total_count, last_searched_at) VALUES
  (gen_random_uuid(), '心血�?, '心血�?, 15, now()),
  (gen_random_uuid(), '肝胆外科', '肝胆外科', 12, now()),
  (gen_random_uuid(), '肿瘤', '肿瘤', 10, now()),
  (gen_random_uuid(), '神经内科', '神经内科', 8, now()),
  (gen_random_uuid(), '心脏搭桥', '心脏搭桥', 6, now()),
  (gen_random_uuid(), '肝癌', '肝癌', 5, now()),
  (gen_random_uuid(), '胃癌', '胃癌', 4, now()),
  (gen_random_uuid(), '肺癌', '肺癌', 3, now()),
  (gen_random_uuid(), '肝脏移植', '肝脏移植', 2, now()),
  (gen_random_uuid(), '心内�?, '心内�?, 1, now());
```

#### 搜索全链�?

```
用户搜索 q=xxx
  �?record_search() 自动触发
    �?upsert user_search_history（用户历史）
    �?upsert search_keyword_stats（热词统计）
      �?GET /search/hot-keywords（热门搜索）
      �?GET /search/suggestions（搜索建议）
      �?GET /search/recommendations（搜索推荐）
      �?GET /users/me/search-history（我的历史）
```

---

### 15.2 专家列表筛选增强（后端 3 层联动）

**时间**�?026-06-01
**涉及文件�? 个）**�?

| 文件 | 改动 |
|------|------|
| `app/api/v1/endpoints/experts.py` | `get_experts_public_list` 新增 5 �?Query 参数：`keyword`、`department`、`hospital`、`is_active`、`sort` |
| `app/services/expert_service.py` | `get_experts_public_list` 方法新增 5 个参数透传�?CRUD |
| `app/crud/experts.py` | `get_experts_public_list` 函数新增关键�?ILIKE（姓�?医院/科室/擅长领域/简介）、科室筛选、医院筛选、启用状态过滤、自定义排序解析 |

#### 新增筛选参�?

| 参数 | 类型 | 说明 |
|------|------|------|
| `keyword` | string | 模糊搜索姓名/医院/科室/擅长领域/简�?|
| `department` | string | 按科�?ILIKE 筛�?|
| `hospital` | string | 按医�?ILIKE 筛�?|
| `is_active` | bool | 启用状态筛选（默认仅启用专家） |
| `sort` | string | 自定义排序，格式 `field:direction`（如 `name:asc`�?|

#### 保留现有参数

`category_id` 筛选逻辑完整保留不变（UUID �?分类�?�?科室/擅长领域 ILIKE）�?

---

### 15.3 前端修改�? 个文件）

**时间**�?026-06-01 ~ 2026-06-02
**项目路径**：`D:\saas_app-main\`

#### 搜索 API �?

**文件**：`src/api/search.ts`

| 改动 | 说明 |
|------|------|
| 新增 `getHotKeywords()` | GET /search/hot-keywords |
| 新增 `getSuggestions(keyword)` | GET /search/suggestions |
| 新增 `getRecommendations(keyword?)` | GET /search/recommendations |
| 新增 `getServerSearchHistory()` | GET /users/me/search-history（需登录�?|
| 新增 `deleteServerSearchHistory(id)` | DELETE /users/me/search-history/{id} |
| 新增 `clearServerSearchHistory()` | DELETE /users/me/search-history |

#### 搜索 Store

**文件**：`src/store/search.ts`

| 改动 | 说明 |
|------|------|
| Mock 热词替换�?`loadHotKeywords()` API 调用 | 着陆页展示真实热词 |
| 新增 `suggestions` 状�?+ `fetchSuggestions()` | 输入时实时联�?|
| 新增 `recommendations` 状�?+ `loadRecommendations()` | "猜你想搜"推荐 |
| `loadSearchHistory` 合并服务端历�?| 登录用户自动合并 `GET /users/me/search-history` |
| `clearHistory` 同步清空服务�?| 同时�?`DELETE /users/me/search-history` |

#### 搜索着陆页

**文件**：`src/pages/app/search/index.vue`

| 改动 | 说明 |
|------|------|
| `onMounted` 调用 `loadHotKeywords()` + `loadRecommendations()` | 加载真实热词和推�?|
| 模板新增 `v-if="keyword.trim()"` 条件渲染 | 输入时显示建议列表，清空时显示历�?热词 |
| 新增"猜你想搜"推荐标签区域 | 展示 `getRecommendations()` 结果 |
| `handleSelectKeyword` 直接跳转不设 keyword | 防止导航前闪烁联想视�?|
| `watch(keyword)` 触发 `fetchSuggestions` | 实时加载搜索建议 |
| `onShow` 恢复状�?| 从结果页返回时刷新建�?热词 |

#### 搜索结果�?

**文件**：`src/pages/app/search/results.vue`

| 改动 | 说明 |
|------|------|
| 新增 `showSuggestions` 状�?| 控制建议/结果视图切换 |
| 模板新增建议列表区域 | 输入关键词时显示联想，点击直接搜�?|
| `watch(keyword)` 输入时显示建�?| 清空时回结果视图 |
| `handleSuggestionClick` 点击建议直达搜索 | 不跳页，在当前页执行 `performSearch` |
| `handleSearch` + `handleSuggestionClick` 先关建议再设 keyword | 防止 watch 重新打开建议视图 |
| `isClickingSuggestion` 守卫 | 防止 keyword 变更触发 watch 重新显示建议 |

#### 专家 Store

**文件**：`src/store/expert.ts`

| 改动 | 说明 |
|------|------|
| 删除 `getFeaturedExperts` 降级逻辑 | 不再�?50 条全量前端自己做过滤 |
| `fetchExperts` 直接�?`keyword/department/hospital` �?`getExperts()` | 384 名专家由后端筛�?|
| 删除前端 `filtered` 过滤逻辑 | 后端已支持全部筛选参�?|

#### 认证路径修复

**文件**：`src/api/auth.ts`

| 改动 | 说明 |
|------|------|
| `getCurrentUser` 路径 `/me` �?`/users/me` | 修复直连 Docker（无 nginx 路径重写）时 404 |
| `bindPhone` 路径 `/me/phone` �?`/users/me/phone` | 同上 |

---

### 15.4 Docker 端口配置变更

**时间**�?026-06-02
**文件**：`docker-compose.prod.yml`

| 改动 | 原因 |
|------|------|
| `live_core_service` 端口映射 `8008:8000` | HBuilderX 开发服务器占用 8000 端口冲突 |
| `user_service` 端口映射 `8002:8002` | 前端直连 Docker 需要暴露端�?|

**前端环境变量**（`.env.development.local`）：

```env
VITE_BASE_API_URL=http://10.0.2.2:8008/api/v1
VITE_AUTH_API_URL=http://10.0.2.2:8002/api/v1
VITE_MEDIA_BASE_URL=http://10.0.2.2:8008
```

> `10.0.2.2` �?Android 模拟器中宿主机的映射地址�?

---

### 15.5 SQL 迁移脚本

**文件**：`backend/live_core_service/migrations/20260531_add_keyword_to_search_keyword_stats.sql`

```sql
ALTER TABLE search_keyword_stats ADD COLUMN IF NOT EXISTS keyword VARCHAR(255);
UPDATE search_keyword_stats SET keyword = keyword_norm WHERE keyword IS NULL;
```

---

### 15.6 弃用说明

微信小程序端的独�?`backend/` 目录（`simple_search_service`、`aggregator`、`migrations`）已弃用，功能完全合并到 `live_core_service` 内部，采用简化版 V2 设计�? �?+ 同步 upsert，无 Kafka/Redis 依赖）�?

---

### 15.7 验证方式

**数据库建�?*�?
```powershell
docker compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d live_core_test -c "CREATE TABLE IF NOT EXISTS user_search_history (...); CREATE TABLE IF NOT EXISTS search_keyword_stats (...);"
```

**重建并重启服�?*�?
```powershell
docker compose -f docker-compose.prod.yml build live_core_service
docker compose -f docker-compose.prod.yml up -d
```

**验证 API**�?
```powershell
# 容器�?
docker compose -f docker-compose.prod.yml exec live_core_service python -c "from urllib.request import urlopen; ..."

# 宿主�?
Invoke-WebRequest -Uri "http://localhost:8008/api/v1/search/hot-keywords" -UseBasicParsing
```


# 修改�?
elif session_status in ['ready', 'scheduled']:
    return LiveStatusEnum.SCHEDULED

# 修改�?
elif session_status == 'scheduled':
    return LiveStatusEnum.SCHEDULED
elif session_status == 'ready':
    return LiveStatusEnum.REPLAY  # 回放
```

### 13.5 专家/品牌关联权限补全

**涉及文件�? 个）**�?

#### 专家关联

**文件**：`services/expert_service.py`

**修改�?*：`set_session_experts()` 仅允�?ADMIN/SUPERADMIN

**修改�?*：与 Tab 管理权限一致——Admin 或房间创建者可操作
```python
# 权限检查：Admin 或房间创建者可操作
if role not in ['ADMIN', 'SUPERADMIN']:
    session = await crud_session.get(self.db, session_id)
    if session is None:
        raise NotFoundException("场次不存�?)
    room = await crud_room.get(self.db, room_id=session.room_id)
    if room is None or room.user_id != current_user_id:
        raise PermissionDeniedException("需要管理员权限或房间创建者权�?)
```

**新增导入**：`from app.crud import session as crud_session`、`from app.crud import room as crud_room`

#### 品牌绑定

**文件**：`services/brand_service.py`

**修改�?*：`bind_room_brands()` 仅允�?ADMIN/SUPERADMIN

**修改�?*：Admin 或房间创建者可操作（逻辑同上�?

### 13.6 前端修复汇总（2026-05-24�?

| 文件 | 修改 |
|------|------|
| `LiveView.vue` | `setupPlayerSource()` 播放状态判断增�?`ready`；`updateNavigationTitle()` 导航标题增加 `ready` |
| `CreateLiveDrawer.vue` | ①表单必填化 ②专�?品牌 Tab 创建与关联分离独�?try-catch ③创建逻辑�?status 分支 |
| `create.vue` | 同上 |
| `edit.vue` | `isStatusSelectable` �?`scheduled` 可选；`originalPlaybackUrl` 脏数据兼�?|
| `session.ts` | `SessionStatus` 统一后端枚举；移�?`scheduled_start_time`；`SessionCreatePayload` 新增 `playback_url` |

### 13.7 部署方式

```powershell
# 重建后端镜像
docker compose -f compose-app.windows.yml up --build -d live_core_service

# 重启 nginx 刷新 DNS
docker compose -f compose-app.windows.yml restart nginx

# 前端热更新（无需部署，重�?dev server 即可�?
cd D:\saas_app-main
npm.cmd run dev:h5
```

### 13.8 变更验证

```powershell
cd backend\live_core_service
python -m py_compile app/services/session_service.py app/schemas/live_core.py app/api/v1/endpoints/room.py app/services/homepage_search_service.py app/services/expert_service.py app/services/brand_service.py
```

结果：全部通过，无 Python 语法错误�?

### 13.9 创建页专家列表从精选改为全�?

**文件**：`CreateLiveDrawer.vue`、`create.vue`

**问题**：原实现调用 `GET /featured-experts`（精选推荐专家），仅返回后端标记�?`is_featured=True` 的专家。种子数�?400 名专家中仅少量被标记为精选，用户创建直播时可选的专家极少�?

**修改**：`handleOpenExpertPicker()` 改为调用 `getExperts()`（全量专家列�?`GET /experts`，分页接口）�?

```typescript
// 修改�?
const response = await get('/featured-experts', { limit: 50 });
if (response.data && Array.isArray(response.data)) {
  expertList.value = response.data;
}

// 修改�?
const response = await getExperts({ page: 1, size: 100 });
if (response.data?.items) {
  expertList.value = response.data.items;
}
```

**注意**：`getExperts` 原本已在两个文件�?import 但从未使用，本次修改无需新增导入�?

### 13.10 专家列表分页限制说明

**现状**：后�?`GET /experts` 接口 `size` 参数上限�?`le=100`（单次最多返�?100 条），前端当前传 `size=100`�?

**风险评估**：当前种子数�?400 名专家，`size=100` 仅返回前 100 条。未来若专家数量增长，需考虑以下方案�?
- 方案 A：前端自动分页循环加载（零后端改动）
- 方案 B：后端放开 `le` 上限
- 方案 C：选择器改为搜索模式（长期最优）

**当前状�?*：暂�?`size=100`，等待产品确认全量选择�?vs 搜索选择器后再决定最终方案�?

### 13.11 13.6 前端修复补充

| 文件 | 补充修改 |
|------|---------|
| `CreateLiveDrawer.vue` | `handleOpenExpertPicker()` �?`/featured-experts` 改为 `getExperts()`；`handleOpenBrandPicker()` 复用 `getBrands()`（已有） |
| `create.vue` | 同上 |

### 13.12 变更验证（更新）

```powershell
cd backend\live_core_service
python -m py_compile app/services/session_service.py app/schemas/live_core.py app/api/v1/endpoints/room.py app/services/homepage_search_service.py app/services/expert_service.py app/services/brand_service.py app/api/v1/endpoints/experts.py
```

结果：全部通过�?

---

## 十四、直播间分类权限修复�?026-05-25�?

> 本轮聚焦：`live_room_categories` 关联接口的权限从 ADMIN-only 放宽�?Admin 或房间创建�?

### 14.1 问题背景

`set_live_room_categories()` �?`delete_live_room_category()` 调用 `_check_admin_permission(role)`，仅允许 ADMIN/SUPERADMIN 为直播间设置分类。普通用户即使创建了直播间，也无法给自己房间设置分类�?

这与已修复的专家关联（`expert_service.set_session_experts()`）和品牌绑定（`brand_service.bind_room_brands()`）的问题完全一致�?

### 14.2 修改详情

**文件**：`backend/live_core_service/app/services/content_management_service.py`

#### 修改 1：`set_live_room_categories()` �?�?823

```python
# 修改�?
self._check_admin_permission(role)
room = await crud_room.get(db, room_id)
if room is None:
    raise NotFoundException("直播间不存在")

# 修改�?
# 权限检查：Admin 或房间创建者可操作
room = await crud_room.get(db, room_id)
if room is None:
    raise NotFoundException("直播间不存在")
if role not in ['ADMIN', 'SUPERADMIN'] and room.user_id != current_user_id:
    raise PermissionDeniedException("需要管理员权限或房间创建者权�?)
```

#### 修改 2：`delete_live_room_category()` �?�?861

```python
# 修改�?
self._check_admin_permission(role)
deleted = await crud_content_management.delete_live_room_category(...)

# 修改�?
# 权限检查：Admin 或房间创建者可操作
room = await crud_room.get(db, room_id)
if room is None:
    raise NotFoundException("直播间不存在")
if role not in ['ADMIN', 'SUPERADMIN'] and room.user_id != current_user_id:
    raise PermissionDeniedException("需要管理员权限或房间创建者权�?)
deleted = await crud_content_management.delete_live_room_category(...)
```

### 14.3 不变的部�?

标签/分类本身�?CRUD（创�?编辑/删除标签和分类定义）**保持 ADMIN-only**——这些是系统元数据管理操作，普通用户不应修改�?

### 14.4 部署方式

```powershell
docker compose -f compose-app.windows.yml up --build -d live_core_service
```

### 14.5 变更验证

```powershell
cd backend\live_core_service
python -m py_compile app/services/content_management_service.py
```

结果：通过�?

---

## 附录 A：分�?标签关联 API 接口汇总（供前端参考）

### A.1 标签（Tags）�?系统元数�?

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| `GET` | `/api/v1/tags` | 可选登�?| 获取标签列表；支�?`q`（关键词/ID）、`search_type`（id/keyword）、`include_inactive`（需管理员） |
| `POST` | `/api/v1/admin/tags` | ADMIN/SUPERADMIN | 创建新标�?|
| `PUT` | `/api/v1/admin/tags/{tag_id}` | ADMIN/SUPERADMIN | 更新标签（名称、图标、启用状态等�?|
| `DELETE` | `/api/v1/admin/tags/{tag_id}` | ADMIN/SUPERADMIN | 删除标签 |

### A.2 分类（Categories）�?系统元数�?

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| `GET` | `/api/v1/categories` | 可选登�?| 获取分类列表；支�?`include_disabled`（需管理员） |
| `POST` | `/api/v1/admin/categories` | ADMIN/SUPERADMIN | 创建新分�?|
| `PUT` | `/api/v1/admin/categories/{category_id}` | ADMIN/SUPERADMIN | 更新分类（名称、图标、父分类、启用状态等�?|
| `DELETE` | `/api/v1/admin/categories/{category_id}` | ADMIN/SUPERADMIN | 删除分类 |

### A.3 场次-标签关联（Session Tags�?

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| `GET` | `/api/v1/sessions/{session_id}/tags` | 无需登录（公开�?| 获取某场次已关联的标签列�?|
| `POST` | `/api/v1/sessions/{session_id}/tags` | 任意登录用户 | 为场次设置标签；请求�?`{ tag_ids: UUID[], mode: "replace" | "append" }` |

### A.4 直播�?分类关联（Room Categories�?

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| `GET` | `/api/v1/rooms/{room_id}/categories` | 无需登录（公开�?| 获取某直播间已关联的分类列表 |
| `POST` | `/api/v1/admin/rooms/{room_id}/categories` | ADMIN/SUPERADMIN **或房间创建�?* | 为直播间批量设置分类；请求体 `{ category_ids: UUID[], mode: "replace" | "append" }` |
| `DELETE` | `/api/v1/admin/rooms/{room_id}/categories/{category_id}` | ADMIN/SUPERADMIN **或房间创建�?* | 删除直播间某个分类关�?|

### A.5 创建直播时的推荐调用顺序

```text
�?前端加载可选列�?
  GET /api/v1/categories          �?获取全部分类（供用户多选）
  GET /api/v1/tags                �?获取全部标签（供用户多选）

�?用户创建直播�?+ 场次（现有流程，不变�?
  POST /api/v1/rooms              �?创建直播�?�?得到 room_id
  POST /api/v1/rooms/{room_id}/sessions  �?创建场次 �?得到 session_id

�?关联分类和标签（新增步骤，需要前端补齐）
  POST /api/v1/admin/rooms/{room_id}/categories
    body: { category_ids: [...], mode: "replace" }
  POST /api/v1/sessions/{session_id}/tags
    body: { tag_ids: [...], mode: "replace" }
```

### A.6 依赖关系说明

- **Categories** �?**Tags** �?ADMIN 在后台管理界面维护（系统元数据）�?
- **Room-Categories** 由房间创建者或 ADMIN 在创�?编辑直播时设置（业务关联）�?
- **Session-Tags** 由任意登录用户在创建/编辑场次时设置（业务关联）�?
- 前端创建直播时：先拿�?`room_id` �?`session_id` �?再分别调用关联接口�?

---

## ʮ�š����ݹ���������������2026-08-14��

> ��������app�޸ļ�¼/���ݹ�������������_��״������ʵʩ����.md����V1.3 ���壩
> ״̬���׶� 0-4 ȫ����ɣ��������߽���

### 19.1 �Ķ��ļ��嵥��A1-A6 / B1 / C1-C3��

| ��� | �ļ� | �Ķ� |
|:--:|------|------|
| A6 | pp/crud/homepage_search.py | ���������������� disable_featured_content_by_target(db, target_type, target_id)�������߽���ͼ�����ش���������**V1.5 ��չ��target_id ֧�ֵ��� UUID �� UUID �б���in_ ƥ�䣩** |
| A1 | pp/crud/room.py delete_room_related | ׷�Ӵ��ã�ɾ����ʱ������ target_type=room ��**V1.5 �޸���session_ids ��֧ͬ�������� target_type=session ����ͼ��ɾ���伶��ɾ���Σ��붩������ͬ��**�Ľ���ͼ |
| A2 | pp/services/session_service.py delete_session | ׷�Ӵ��ã�ɾ����ʱ������ target_type=session �Ľ���ͼ |
| A3 | pp/services/topic_service.py delete_topic | ׷�Ӵ��ã�ɾר��ʱ������ target_type=topic �Ľ���ͼ�������һ���ԣ�����ר�� API ��Լ�� |
| A4 | pp/services/brand_service.py + pp/crud/brand.py | ����ɾƷ������������ָ�����Ľ���ͼ��D3=�ǣ�����ӲɾƷ�� check_brand_references ���ӽ���ͼ���ü������ܾ���D2=�ܾ���������ֵ���ͬ�����۴����İ�������ͼ������ |
| A5 | pp/crud/homepage_search.py get_featured_content_list | �����б�׷�� target ��Ч�Թ��ˣ�room/session/topic ������ + brand is_active�������Ҳ����أ������˷�ҳ�б�����ȫ�� |
| B1 | pp/services/session_service.py delete_session���� A2 ͬ������ | ׷�Ӵ��ã�Ӳɾָ��ó��ε� UserSubscription(SESSION)��D5=Ӳɾ����ɾ����·�����룬������������ɾ��ʵ��ͬ���� |
| C1 | pp/crud/experts.py get_session_experts | ���� iewer_role �������ǹ���Ա SQL ��������ͣ��ר�ң�D4=��������������ȫ���� |
| C2 | pp/services/expert_service.py get_session_experts | �� 
ole_user ͸�� CRUD |
| C3 | pp/services/content_management_service.py set_session_tags | ׷�ӱ�ǩ is_active=True У�飬��һͣ�ü� 4001���İ�"���ֱ�ǩID�����ڻ���ͣ��"�� |

### 19.2 ����

| �ļ� | ���� |
|------|------|
| 	ests/unit/test_data_governance.py������ 9 ������ | ���� 1-9��ɾ����/����/ר��/Ʒ����ɾ/Ӳɾ�ܾ�/�����б�����/ר�Ҷ�ȡ����/��ǩд��У��/����Ӳɾ |

### 19.3 �׶� 4 ���ս����2026-08-14��

- ȫ���ع飨gov �⣩��**145 failed / 1220 passed**����ʧ���� 145 ��׶� 0 ������ȫһ�£�����ʧ�ܣ�ר�� category_id �о�ȱʧ��asyncpg ���� InterfaceError������/ר��/������������⣩��**����������ʧ��**��ͨ���� 1220 = ���� 1211 + �������� 9 ����ȫ��
- ��������������TEST_���� ǰ׺ 139 ����59 ����/29 Ʒ��/11 ����/22 ר��/18 ��ǩ�����������˶Ժ�����ɾ������֤ 0 ����������ǰȫ������ Temp\opencode\gov_phase4_cleanup_backup_20260814.sql
- ��ʵ�⣨live_core_test���㴥��������ȫ�̸����� live_core_test_gov ר�ò��Կ⣩
- D1 Ʒ����ɾ����ھ���**�Ѳ�Ʒȷ�ϣ�2026-08-14��������״������**����7.6 ��ʽ�ھ�����ɾƷ�ƹ�������+ǰ̨����=����ȼ۽�󣻻ָ����ù����Զ��ָ�������ͼ�ֶ��ָ���Ӳɾǰ�����ü�飩
- **V1.5 ��©�޸���2026-08-14��**��ɾ����·�� delete_room_related ����ɾ����ʱ session ���ͽ���ͼδ���ã�A2 ���ҵ�ɾ����·������ ��disable_featured_content_by_target ��չ֧�� ID �б����� ID �����ݣ�����delete_room_related session_ids ��֧�����ã������� 1 ��չ���ԣ�����+���ν���ͼ�������ߣ�����ȫ���ع� 145 failed/1220 passed �����һ�£�������ʧ�ܣ������� 9 ����ȫ�������� 0

---

## 二十、标签管理 V2 融合接入（2026-09-05）

> 依据文档：app修改记录/标签管理V2融合接入_现状分析与实施方案.md（V1.8 定稿）
> 状态：阶段 0-6 全部完成；提交 9776548/a416aee/dc9bc5c/c2487ed/8cba664/c27096e + 修复 d1f3b0d 已推送 origin/main
> 口径：数量上限 5（D1）；resolve 自建=公共词表立即可见（D2）；直接 main 顺序提交（D3）；权限收窄接受（D4）

### 20.1 阶段与提交

| 阶段 | 提交 | 内容 |
|------|------|------|
| 0 | 9776548 | 工作区清理（live_features 页内正序 + 焦点图时间区间校验）+ .gitignore 杂项隔离 + pytest 基线 |
| 1 | a416aee | tags 溯源字段模型 + V18 迁移（created_by/source + 索引，dev 已执行并回填 703 行） |
| 2 | dc9bc5c | Schema：SessionTagsSetRequest 0~5、TagItem 溯源字段、TagResolve*（13 schema 单测） |
| 3 | c2487ed | CRUD：create_tag 溯源参数、get_tag_by_name、set_session_tags 空列表守卫（含 service 空守卫配套） |
| 4 | 8cba664 | Service：绑标签权限收紧为房主或 Admin、append 上限 5、resolve_tag、Admin 溯源（行为变更已标注） |
| 5 | c27096e | resolve 端点 + tag_name 内容安全场景（TEXT_FIELD_MATRIX）+ 集成测试收编 |
| 修复 | d1f3b0d | set_session_tags commit 前序列化，规避 MissingGreenlet（dev expire_on_commit 场景 500） |

### 20.2 关键实现点

- 权限复用 main 自有 core.permissions.check_room_owner_or_admin（文案「需要管理员或房间创建者权限」），未复制 wechat 私有实现
- resolve_tag：登录 → check_scene_fields(scene=tag_name) → 精确查重 → 软删 400 不复活 → source=user 自建 → 并发唯一冲突 rollback 再查（幂等）
- 端点内容安全 422 显式 catch（与 live_features 留言端点风格一致）
- main 政治检测默认 placeholder 占位（无外部 API 依赖）
- 空列表守卫防 PG IN ()（replace 空=清空语义）

### 20.3 回归与验收

- 全量回归（gov 专用库，126 存量失败/3 WS 环境 errors 全程一致，零新增失败）；通过数 1277 → 1315（新增 schema 13 + crud 8 + service 13 + resolve API 4）
- dev 真实容器验收（ACCEPTV2 夹具）：21/21 通过（resolve 新建/复用/公开联想/401/软删 400/违禁词 422、Admin 建标签 201、房主 200/非房主 403/Admin 旁路 200、6 个 422、append 超限 4001、空清空、公开读、删关联权限），清理残留 0
- gov 测试库规则策略：基线空规则态 + 仅 tag_name 32 行（备份 backup_csr_gov_20260905.sql）；dev 全量 seed +32（备份 backup_csr_dev_20260905.sql）
- 环境注意：rebuild-live-core-only.bat 为删库重建脚本（DROP live_core_test），保数据部署用 docker-compose -f compose-app.windows.yml build/up -d live_core_service

### 20.4 待跨端同步（S1-S3）

- 小程序前端标签上限 10 → 收回 5（契约不允许单方放宽）
- 绑标签报错文案统一（main 口径「部分标签ID不存在或已停用」）
- 契约：上限 5 / 房主或 Admin / resolve 422(2005) / source 字段语义

---

## �ڶ�ʮһ�� ����״̬�������Ǩ�ƣ�2026-09-06�����������Ķ���ע

> �����ĵ���app�޸ļ�¼/����״̬�������Ǩ�ƣ�����ҳ���༭ҳ��_��״������ʵʩ����.md��V1.2��
> ״̬��**�����Ķ�**������Լ���飻ǰ����ڹ�λ������ҳ �� �༭ҳ��

21.1 �������

- POST /api/v1/sessions/{session_id}/status��app/api/v1/endpoints/session.py:182-261��Ϊ V15 �ύ d1d8707 ���ж˵㣬����δ�����
- switch_external_status��app/services/session_service.py:449-553��״̬���󡢲��ŵ�ַ�������ֹ�����δ������
- source_type ������ҳ����̬����=EXTERNAL / ����ط�����������=PUSH��Ϊ������Ϊ��δ���������� push ���ι��������������ݻ������� ��12 S2��������������

21.2 �޸Ķ���ϸ

- ���룺�ޣ�Ǩ�ƣ��ޣ�seed���ޣ����ã��ޣ����ԣ��ޣ�ǰ�˻ع�Ϊ eslint/����˶ԣ��������ĵ� ��ʮ����

21.3 ˵��

- ������ʵʩ���� source_type ����������S2�����������������ڴ����ǡ�