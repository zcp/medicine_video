# 管理端数据面板 P3 — SQL 迁移 + 种子数据更新

**版本**: V1.0
**创建日期**: 2026-07-23
**基于设计文档**: `app修改记录/管理端后台数据面板_AdminStats设计文档.md`
**依赖**: P1 + P2 已完成（模型、CRUD、API 已就位）
**目标文件**: 新建 1 个 + 修改 1 个

---

## 1. 角色定义 (Role Definition)

你是一名精通 PostgreSQL DDL + 数据迁移 + 种子数据维护的数据库运维工程师。你了解幂等迁移设计——`ALTER TABLE ADD COLUMN IF NOT EXISTS` 防止重复执行报错。你的任务是执行**管理端数据面板 P3**：创建数据库迁移 SQL，修改种子数据适配新字段。

**三个必须**：
- 迁移 SQL 必须幂等（可重复执行不报错）
- 必须使用 `IF NOT EXISTS` / `IF EXISTS` 子句
- 种子数据必须设置 `is_verified=True`，避免存量种子专家变为待审批

---

## 2. 任务目标 (Task Objective)

### 2.1 新建文件（1 个）

| 文件 | 内容 |
|:-----|:-----|
| `backend/live_core_service/migrations/add_experts_is_verified.sql` | `ALTER TABLE` DDL + `UPDATE` 数据回填 |

### 2.2 修改文件（1 个）

| 文件 | 修改内容 |
|:-----|:---------|
| `backend/live_core_service/app/seed.py` | `seed_experts()` 中创建 Expert 时补充 `is_verified=True` |

### 2.3 禁止事项

- ❌ 不修改 `app/models/`、`app/crud/`、`app/api/`（P1 + P2 已完成）
- ❌ 不使用 `DROP COLUMN` 或破坏性 DDL
- ❌ 不在迁移中使用事务外部的非幂等操作

---

## 3. 核心上下文 (Core Context)

### 3.1 SQL 迁移文件

**路径**：`backend/live_core_service/migrations/add_experts_is_verified.sql`

```sql
-- 管理端数据面板: 为 experts 表新增 is_verified 审核状态字段
-- 创建日期: 2026-07-23
-- 执行方式: psql -U postgres -d live_core_test -f migrations/add_experts_is_verified.sql

-- Step 1: 新增 is_verified 列（幂等）
ALTER TABLE experts
  ADD COLUMN IF NOT EXISTS is_verified BOOLEAN NOT NULL DEFAULT FALSE;

COMMENT ON COLUMN experts.is_verified IS '审核状态: False=待审批, True=已审批';

-- Step 2: 将现有已启用(is_active=True)的专家标记为已审批
-- 避免存量专家数据全部变为待审批，影响运营
UPDATE experts SET is_verified = TRUE WHERE is_active = TRUE AND is_verified = FALSE;
```

**关键设计**：
- `ADD COLUMN IF NOT EXISTS` 保证幂等
- `UPDATE` 使用 `AND is_verified = FALSE` 也保证幂等（重复执行不会重复回填）

### 3.2 `app/seed.py` — 种子专家创建

**当前 `seed_experts()` 函数**（约第 300-350 行）：

```python
def seed_experts(db):
    ...
    for expert_data in EXPERTS_DATA:
        existing = db.query(Expert).filter_by(name=expert_data["name"]).first()
        if existing:
            continue
        expert = Expert(
            id=uuid.uuid4(),
            name=expert_data["name"],
            title=expert_data.get("title", ""),
            hospital=expert_data.get("hospital", ""),
            category_id=...,
            department_id=...,
            is_featured=True,
            is_active=True,
            sort_order=0,
            created_at=datetime.utcnow(),
            updated_at=datetime.utcnow(),
        )
        db.add(expert)
    db.commit()
```

**修改**：在 `Expert(...)` 创建处补充 `is_verified=True`：

```python
expert = Expert(
    ...
    is_active=True,
    is_verified=True,  # ← 新增：种子专家默认已审批
    sort_order=0,
    ...
)
```

**为什么是 `True`**：种子数据是开发/测试用的预先准备数据，不应视为"待审批"。如果设 `False`，400 名种子专家全部变成待审批状态，干扰前端管理摘要的待审批计数展示。

---

## 4. 执行说明

### 4.1 迁移执行顺序

```powershell
# Step 1: 复制迁移文件到容器
docker cp "backend\live_core_service\migrations\add_experts_is_verified.sql" live-streaming-saas-v2-main-live_core_service-1:/app/migrations/add_experts_is_verified.sql

# Step 2: 执行迁移
docker exec live-streaming-saas-v2-main-live_core_service-1 sh -c "psql -U postgres -d live_core_test -f /app/migrations/add_experts_is_verified.sql"

# Step 3: 复制更新后的 seed.py 到容器并执行（仅首次或数据库重置时需要）
docker cp "backend\live_core_service\app\seed.py" live-streaming-saas-v2-main-live_core_service-1:/app/app/seed.py
docker exec -e PYTHONPATH=/app live-streaming-saas-v2-main-live_core_service-1 sh -c "cd /app && python app/seed.py"
```

### 4.2 验证 SQL

```sql
-- 确认新增列存在
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_name = 'experts' AND column_name = 'is_verified';

-- 确认已启用专家标记为已审批
SELECT is_verified, COUNT(*) FROM experts GROUP BY is_verified;
```

---

## 5. 输出要求

### 5.1 输出格式

迁移 SQL 输出完整文件内容；seed.py 输出 diff 或 Expert 创建的代码块。

### 5.2 验证清单

- [ ] `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 包含 `IF NOT EXISTS`
- [ ] `COMMENT ON COLUMN` 正确描述了字段语义
- [ ] `UPDATE` 使用 `WHERE is_active = TRUE AND is_verified = FALSE`（幂等安全）
- [ ] `seed.py` 中 `Expert(...)` 创建处补充了 `is_verified=True`
- [ ] 所有种子专家在 seed 执行后 `is_verified=True`
