# 观看历史·订阅·通知功能代码生成指令

**文档版本**: V1.0  
**创建日期**: 2026-02-03  
**适用提示词**: 《观看历史-订阅-通知页面代码生成提示词.md》

---

## 📋 前置准备（已完成）

✅ **清理步骤已完成**：
- 已删除 `src/api/watchHistory.ts`（空文件）
- 已删除 `src/api/subscription.ts`（空文件）

---

## 🎯 分阶段代码生成指令

由于提示词文档较长（4408行），建议分4个阶段生成代码，避免 AI 输出被截断。

---

## 阶段 1：创建类型定义和 API 层

### 📝 **指令 1-1：创建 TypeScript 类型定义**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的"TypeScript 类型定义"章节（第769-889行），为我创建以下3个类型定义文件：

1. src/types/watchHistory.ts - 观看历史相关类型
2. src/types/subscription.ts - 订阅相关类型  
3. src/types/notification.ts - 通知相关类型

要求：
- 严格按照文档中的类型定义实现
- 包含所有接口和类型别名
- 添加完整的 JSDoc 注释
- 确保类型安全

请一次性生成这3个文件的完整代码。
```

---

### 📝 **指令 1-2：创建 API 层**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的"API和Store现状说明"章节（第611-758行），为我创建以下3个 API 文件：

1. src/api/watchHistory.ts - 观看历史 API
   需要实现的方法：
   - recordWatch(sessionId, data) - 记录观看行为
   - getWatchHistory(params) - 获取观看历史列表
   - deleteWatchHistory(historyId) - 删除单条历史
   - clearWatchHistory() - 清空全部历史

2. src/api/subscription.ts - 订阅 API
   需要实现的方法：
   - getSubscriptions(params) - 获取订阅列表
   - addSubscription(data) - 添加订阅
   - removeSubscription(id) - 取消订阅
   - checkSubscription(targetId) - 检查订阅状态
   - clearSubscriptionHistory() - 清空历史订阅

3. src/api/notification.ts - 通知 API
   需要实现的方法：
   - getNotifications(params) - 获取通知列表
   - markNotificationRead(notificationId) - 标记单条已读
   - markAllNotificationsRead() - 标记全部已读
   - deleteNotification(notificationId) - 删除单条通知

要求：
- 使用 @/utils/request 中的 get、post、patch、del 方法
- API 路径只写相对路径（如 /users/me/watch-history）
- 所有接口都需要传 { auth: true } 参数
- 导入正确的类型定义
- 添加完整的 JSDoc 注释

请分别生成这3个文件的完整代码。
```

---

## 阶段 2：创建 Store 层和工具函数

### 📝 **指令 2-1：创建 Store 状态管理**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的"Store实现说明"章节（第912-1026行），为我创建以下3个 Store 文件：

1. src/store/watchHistory.ts - 观看历史 Store
   核心功能：
   - 分组管理观看历史（今天/昨天/更早）
   - 分页加载与无限滚动
   - 左滑删除状态管理
   - 清空全部历史
   
   关键实现：
   - 使用 computed 缓存分组结果
   - 使用 dayjs 实现日期分组算法
   - 参考文档第923-956行的分组算法

2. src/store/subscription.ts - 订阅 Store
   核心功能：
   - 三标签分类（待开播/已通知/已过期）
   - 未读徽章统计
   - 取消订阅与清空历史
   
   关键实现：
   - 待开播：start_time > now && status === 'scheduled'
   - 已通知：is_notified === true || status === 'live'
   - 已过期：end_time < now && status === 'ended'
   - 参考文档第970-993行的分类逻辑

3. src/store/notification.ts - 通知 Store
   核心功能：
   - 三标签列表管理（全部/系统/直播）
   - 30秒轮询机制（使用 PollingManager）
   - 未读统计（独立统计三个类型）
   - 批量已读与清空
   
   关键实现：
   - 使用 PollingManager 管理轮询
   - 参考文档第1009-1022行的轮询管理代码

要求：
- 使用 Pinia 的 defineStore
- 使用 Vue3 Composition API
- 导入对应的 API 方法和类型定义
- 实现完整的错误处理
- 添加 loading 状态管理

请分别生成这3个文件的完整代码。
```

---

### 📝 **指令 2-2：补充工具函数**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的"工具函数说明"章节（第1029-1158行），为我补充以下工具函数：

1. 在 src/utils/security.ts 末尾添加图片占位符功能
   需要添加的内容（参考文档第239-315行）：
   - DEFAULT_COVER_PLACEHOLDER 常量
   - DEFAULT_AVATAR_PLACEHOLDER 常量
   - TRUSTED_IMAGE_DOMAINS 常量
   - getImageUrl(url, type) 函数
   
   要求：
   - 追加到现有文件末尾
   - 复用已有的 validateUrl 函数
   - 添加完整的安全校验逻辑

2. 创建 src/utils/polling.ts - 轮询管理器
   核心功能（参考文档第1132-1146行）：
   - 智能轮询策略
   - 页面可见性检测（H5平台）
   - 网络状态检测
   - 网络恢复时立即轮询
   - 页面重新可见时立即轮询

3. 创建 src/utils/logger.ts - 日志工具（如果不存在）
   核心功能（参考文档第1072-1096行）：
   - 生产环境日志控制
   - 错误上报到监控服务

请分别生成这些工具函数的完整代码。
```

---

## 阶段 3：创建页面组件

### 📝 **指令 3-1：创建观看历史页面**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档，为我创建观看历史页面：

文件路径：src/pages/app/tabbar/my/watch-history/index.vue

页面功能：
- 分组显示历史记录（今天/昨天/更早）
- 左滑删除单条记录
- 清空全部历史
- 下拉刷新和上拉加载
- 空状态展示

UI要求：
- 使用 uni-app 原生组件
- 参考哔哩哔哩移动端UI设计
- 使用 pages.json 配置的原生导航栏
- 实现左滑删除交互（手动实现，不使用第三方组件）

技术要求：
- 使用 Vue3 Composition API
- 使用 TypeScript
- 调用 watchHistoryStore 的方法
- 使用 dayjs 格式化时间
- 使用 @/utils/navigation 进行页面跳转
- 使用 @/utils/security 的 getImageUrl 处理图片

请生成完整的 .vue 文件代码，包括 template、script setup 和 style。
```

---

### 📝 **指令 3-2：创建订阅页面**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档，为我创建订阅页面：

文件路径：src/pages/app/tabbar/my/subscriptions/index.vue

页面功能：
- 三标签切换（待开播/已通知/已过期）
- 未读徽章显示
- 取消订阅功能
- 清空历史订阅
- 下拉刷新和上拉加载

UI要求：
- 使用 uni-app 原生组件
- 参考哔哩哔哩移动端UI设计
- 手动实现 Tab 切换组件
- 手动实现徽章组件

技术要求：
- 使用 Vue3 Composition API
- 使用 TypeScript
- 调用 subscriptionStore 的方法
- 实现三标签分类逻辑
- 显示未读徽章数量

请生成完整的 .vue 文件代码，包括 template、script setup 和 style。
```

---

### 📝 **指令 3-3：创建通知中心页面**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档，为我创建通知中心页面：

文件路径：src/pages/app/notifications/index.vue

页面功能：
- 三标签切换（全部/系统/直播）
- 未读徽章显示
- 标记已读（单条/全部）
- 30秒自动轮询
- 下拉刷新

UI要求：
- 使用 uni-app 原生组件
- 参考哔哩哔哩移动端UI设计
- 顶部右侧显示"全部已读"按钮
- 手动实现 Tab 切换组件

技术要求：
- 使用 Vue3 Composition API
- 使用 TypeScript
- 调用 notificationStore 的方法
- 实现30秒轮询（页面显示时启动，隐藏时停止）
- 实现标记已读功能

请生成完整的 .vue 文件代码，包括 template、script setup 和 style。
```

---

### 📝 **指令 3-4：补充"我的"页面按钮**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的""我的"页面入口按钮补充"章节（第135-260行），为我修改"我的"页面：

文件路径：src/pages/app/tabbar/my/index.vue

需要修改的内容：
1. 在"收藏"和"关注"按钮后面添加两个新按钮：
   - 观看历史按钮：图标 📺，跳转到 /pages/app/tabbar/my/watch-history/index
   - 订阅按钮：图标 🔔，跳转到 /pages/app/tabbar/my/subscriptions/index

2. 样式调整：
   - 按钮数量从 2 个增加到 4 个
   - 使用 flex: 1 确保按钮均分空间
   - 图标大小：48rpx
   - 文字大小：24rpx

参考代码：文档第161-248行

请提供修改后的完整代码片段（只需要修改的部分）。
```

---

## 阶段 4：配置和测试

### 📝 **指令 4-1：配置 pages.json**

```
请帮我在 src/pages.json 中添加以下3个页面的路由配置：

1. 观看历史页面：pages/app/tabbar/my/watch-history/index
2. 订阅页面：pages/app/tabbar/my/subscriptions/index
3. 通知中心页面：pages/app/notifications/index

配置要求（参考文档第555-607行）：
- navigationBarTitleText：对应的页面标题
- navigationBarBackgroundColor：#ffffff
- navigationBarTextStyle：black
- enablePullDownRefresh：true
- backgroundTextStyle：dark

请提供完整的配置代码。
```

---

### 📝 **指令 4-2：创建全局样式变量（可选）**

```
请根据《观看历史-订阅-通知页面代码生成提示词.md》文档中的"全局样式变量"章节（第1162-1199行），为我创建全局样式文件：

文件路径：src/styles/variables.scss

内容要求：
- B站风格配色方案
- 包含主题色、功能色、文字颜色、背景颜色
- 包含间距和圆角变量

请生成完整的 SCSS 文件代码。
```

---

## 📋 实施检查清单

完成所有代码生成后，请按照以下清单验证：

### 阶段 1 检查
- [ ] 已创建 src/types/watchHistory.ts
- [ ] 已创建 src/types/subscription.ts
- [ ] 已创建 src/types/notification.ts
- [ ] 已创建 src/api/watchHistory.ts（包含4个方法）
- [ ] 已创建 src/api/subscription.ts（包含5个方法）
- [ ] 已创建 src/api/notification.ts（包含4个方法）

### 阶段 2 检查
- [ ] 已创建 src/store/watchHistory.ts
- [ ] 已创建 src/store/subscription.ts
- [ ] 已创建 src/store/notification.ts
- [ ] 已补充 src/utils/security.ts（图片占位符功能）
- [ ] 已创建 src/utils/polling.ts
- [ ] 已创建 src/utils/logger.ts

### 阶段 3 检查
- [ ] 已创建 src/pages/app/tabbar/my/watch-history/index.vue
- [ ] 已创建 src/pages/app/tabbar/my/subscriptions/index.vue
- [ ] 已创建 src/pages/app/notifications/index.vue
- [ ] 已修改 src/pages/app/tabbar/my/index.vue（添加按钮）

### 阶段 4 检查
- [ ] 已配置 src/pages.json（3个页面路由）
- [ ] 已创建 src/styles/variables.scss（可选）

---

## 🚀 使用说明

1. **按顺序执行**：严格按照阶段1→2→3→4的顺序执行指令
2. **逐个文件生成**：每次只生成1-3个文件，避免输出被截断
3. **验证后继续**：每完成一个阶段，验证代码无误后再进入下一阶段
4. **参考文档行号**：如果 AI 需要更多细节，可以指定文档的具体行号范围

---

## 💡 常见问题

**Q1: AI 输出被截断怎么办？**  
A: 要求 AI 只生成单个文件，或者要求"继续生成剩余代码"

**Q2: 代码有错误怎么办？**  
A: 指定具体的错误信息和文件，要求 AI 修正

**Q3: 需要查看文档详细内容怎么办？**  
A: 告诉 AI 查看文档的具体行号范围（如"请查看第769-889行"）

---

**最后更新**: 2026-02-03  
**文档版本**: V1.0
