# 前端接入后端优化 · 阶段执行记录

> 本文件记录各阶段实施与 Gate 审查结果，与《前端接入后端优化实施文档.md》配套。
> 执行分支：`feature/backend-integration`（基于 main @ 9289f0d）

---

## 阶段 0：准备与基线（2026-08-10）

### 0.1 现状核查

| 项 | 现状 |
|----|------|
| 分支 | main @ 9289f0d（`ea7ffae` 注销验证码 / `9289f0d` SUPERADMIN 守卫 之后） |
| 工作区 | 干净（仅 1 个未跟踪文档：`docs/前端接入后端优化实施文档.md`） |
| 工具链 | pnpm 11.18.0 / node v24.14.0 / node_modules 就绪 |
| 后端契约 | 前序多轮已静态核验（分类 409 / 专家 4001 / 留言 422 形态均已源码确认，见 0.3） |

### 0.2 本阶段执行动作

| # | 动作 | 结果 |
|---|------|------|
| 1 | 新建分支 `feature/backend-integration` | ✅ |
| 2 | 提取 wechat 分支参照代码 → `docs/ref-wechat/`（8 份，UTF-8 无 BOM） | ✅ |
| 3 | 基线检查：lint / test:run / build:h5 | ✅ 记录（见 0.4） |

**参照代码清单**（来自 wechat 分支，仅供功能逻辑参考，后续阶段使用）：

| 文件 | 行数 | 用途 |
|------|:---:|------|
| ContentSafetyRuleList.vue | 400 | N3 内容安全规则列表（阶段 3） |
| ContentSafetyRuleFormDialog.vue | 560 | N3 规则表单弹窗（阶段 3） |
| ContentSafetyLogList.vue | 307 | N3 日志列表（阶段 3） |
| NotificationDetail.vue | 386 | N2 通知详情（阶段 3，视决策） |
| contentSafety.ts | 139 | P2-1 2004/2005 文案映射（阶段 5） |
| contentSafetyDisplay.ts | 332 | P2-1 展示辅助（阶段 5） |
| roomMessageNormalize.ts | 137 | F1 留言 user 嵌套归一化（阶段 2） |
| roomMessageSearch.ts | 62 | F1 留言搜索辅助（阶段 2） |

### 0.3 关键契约现状（静态核验结论，引用前序实测）

| 端点 | 形态 | 证据（后端） |
|------|------|-------------|
| `DELETE /admin/categories/{id}` | 非 force+有引用 → **409** + `data.references`{expert_all/expert_active/department_count/room_count/active_child_count} + `data.warnings[]`；force=确认级联；禁删 slug=="other"→400 | content_management_service.py:660-698 |
| `GET /experts?category_id=<停用>` | → **400/4001** "分类不存在或已停用" | crud/experts.py:251-253 |
| 留言违规 | → **422/2005**（全局 handler） | main.py:65-74 |
| `POST /admin/categories/merge`、`POST /admin/categories/{id}/migrate` | dry_run 预览 / scope 迁移 | content_management.py:523-619 |

### 0.4 基线记录（既有状态，本阶段不修复）

| 检查 | 结果 | 说明 |
|------|------|------|
| `pnpm lint` | **194 problems（46 errors / 148 warnings）** | 既有失败，集中于基建文件：`uni.promisify.adaptor.js`（no-undef）、`uniCloud-aliyun/cloudfunctions/get-phone-number/index.js`（no-undef）、`vite.config.ts`（未用变量警告）等，与本次实施无关 |
| `pnpm test:run` | **16 files failed / 3 passed；10 tests failed / 60 passed** | 既有失败（如 tests/store_session.spec.ts updateSession toast 断言失配等），均为存量测试与代码状态不一致，非本次改动引入 |
| `pnpm build:h5` | ✅ **DONE Build complete** | 成功（platform.ts 动态导入警告为既有项，不影响产物） |

> 基线策略：lint/test 既有失败**不在本实施范围内修复**（与实施无耦合），但每阶段须确认"未新增失败"——即以基线 46E/148W、10F/60P 为参照，阶段门禁要求 lint/test 失败数**不增加**。

### 0.5 Gate 0 验收结果

```
□ 分支已建（feature/backend-integration）                      ✅
□ 工作区仅含实施文档 + 参照代码（无其他改动）                   ✅
□ 参照代码已提取（8 份文件可用）                                ✅
□ 后端关键契约形态已静态确认（409/4001/422）                    ✅
□ 基线已记录（lint 46E/148W、test 10F/60P、build:h5 成功）      ✅
□ 构建基线通过（build:h5 成功）                                 ✅
```

**审查问题核对**：
- 是否引入项目冲突？→ 无：未修改任何 src/ 代码；仅新增分支、docs/ref-wechat/ 参照文件、本记录文档
- 是否可进入阶段 1？→ ✅ 可以（分类契约适配 C1-C6，全部为现有页面低风险改动）

---

## 阶段 1：分类契约适配（C1-C6）（2026-08-10）

### 1.1 实施内容（10 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| C6 | 类型补字段 | types/category.ts | Category/CreatePayload/UpdatePayload 补 `display_name?`、`standard?` |
| C1a | AllCategories 展示+搜索 | AllCategories.vue:46,81,111 | 展示 `display_name \|\| name`；搜索匹配加 display_name |
| C1b | 首页分类 Tab | CategoryTabs.vue:25 | 展示切换 |
| C1c | 专家页科室 Tab | tabbar/expert/index.vue:212-216 | Tab 用 display_name；map **双 key**（口语名+标准名→id）防御 |
| C1d | create 分类卡片+picker | create.vue:275,494 | 展示切换 |
| C1e | edit 分类卡片+picker | edit.vue:193,460 | 展示切换（picker 为补漏项） |
| C1f | departments 分组+picker | departments/index.vue:353,382-386 | 分组名与科室表单 picker/回显切换 |
| C2 | 分类表单补 display_name | departments/index.vue:281-291,375,705-744 | 表单加"口语名"输入（标准名/口语名分离）；编辑回填 name 用标准名；payload 带 display_name |
| C3 | 专家筛选 4001 文案 | store/expert.ts:137-143 | catch 区分 `code===4001` → "该分类已停用或不存在，请更换筛选条件" |
| C4 | 专家表单一致性 | expert-list/index.vue:626-641 | 提交前本地校验 category_id 与科室一致，不一致拦截提示；onAssignDeptChange 补分类同步 |
| C5 | deleteCategory 参数 | api/category.ts:70-88 | force/target_category_id 拼 URL query |

**补漏项（grep 复核发现）**：CreateLiveDrawer.vue:300,513（分类卡片+picker）、expert-list:310-314（筛选 Tab 标签）、detail.vue:98（房间详情分类标签）、edit.vue:460（picker）——共 4 处原清单未覆盖的展示点，一并切换。

**实施前确认（风险 1）**：store/expert.ts 全文件无客户端 specialty 过滤（specialty 为冗余状态，列表完全由后端返回）→ display_name 切换无失配风险；map 双 key 已做防御。

### 1.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ **DONE Build complete** | 无编译错误 |
| `pnpm lint` | 194（46E/148W） | 194（46E/148W） | **不增加** ✅ |
| `pnpm test:run` | 10F/60P（16 files） | 10F/60P（16 files） | **不增加** ✅ |
| grep 复核 | — | App 端无遗漏 `cat.name` 展示点（仅 H5 页面与专题分类保留） | ✅ |

### 1.3 Gate 1 验收结果

```
□ 展示点切换完成（6 处主清单 + 4 处补漏）                ✅
□ 分类表单支持口语名（创建/编辑/payload）                 ✅
□ 专家筛选 4001 专门文案                                 ✅
□ 专家表单一致性本地校验 + assign 同步                    ✅
□ deleteCategory URL query 参数                          ✅
□ lint/test/build 门禁通过（不新增失败）                  ✅
```

**审查问题核对**：
- 是否引入新样式 token？→ 无（仅表单新增一个 input，复用现有 form-group/form-input 样式）
- display_name 切换是否有遗漏？→ 全库 grep 复核，App 端无遗漏（H5 页面不在实施范围）
- 是否影响 H5？→ category.ts 类型为纯增量（新增可选字段），H5 编译已通过
- Mock 数据兼容？→ display_name 为可选字段 + `\|\| name` 兜底，Mock 数据零改动

**遗留说明**：
- departments 页科室的 `dept.category_name` 标签（:149）为后端组装字段（返回标准名），前端无法切换，记为已知差异（待后端 category_name 输出 display_name 或后续统一）
- standard 标记展示按规划留至 P2-3

---

## 阶段 2：融合契约修复（F1-F9）（2026-08-10）

### 2.1 实施内容（9 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| F1 | 留言字段对齐 | types/message.ts、ChatTab.vue:39,45 | Message 补 `user_display_name`/`user{nickname,avatar_url}`；渲染改 `msg.user_display_name \|\| msg.user?.nickname` + `msg.user?.avatar_url`（空头像兜底默认图） |
| F2 | 留言分页参数 | ChatTab.vue:152,185 | `page_size`→`size`（响应字段 size 已对齐） |
| F3 | bind-phone 路径 | api/auth.ts:186 | `/me/bind-phone`→`/me/phone`（后端唯一路由） |
| F4 | updateRoom/updateSubVenue 动词 | api/room.ts:54-57,123-125 | PUT→PATCH（删除误导注释）；清理 create.vue/CreateLiveDrawer 死 import；**连带发现 store/room.ts 的 updateSubVenue 同样 405，一并修复** |
| F5 | 创建场次 playback_url | detail.vue | sessionForm 加 playback_url；弹窗加输入（复用 form 样式）；必填校验 + 传参 + 开关重置 |
| F6 | 编辑/开播交互 | detail.vue:918-920,951-953 | 编辑 `scheduled_start_time`→`start_time`；**开播改为跳播放页+推流提示**（后端无显式 start 端点，SRS 回调驱动，去掉虚假"开播成功"） |
| F7 | 通知批量（方案修正） | types/notification.ts、api/notification.ts、notification-push/index.vue | **实测后端 POST /admin/notifications 本身就是 user_ids 批量端点、不支持全站**：CreateNotificationRequest 补 user_ids；删除 batchCreateNotifications（/batch 404）；specific 走 user_ids 批量；all 分支明确提示"暂未开放"（修复隐藏 400） |
| F8 | 移除 today/live 死模式 | api/admin.ts（删除）、list.vue 8 处 | pageMode 收窄 manage/admin；删 today 状态/displayItems 分支/5 个空态 computed/loadTodaySessions/onLoad filter/下拉刷新/loadMore 分支；保留 isTodaySession 判断（恒 false 无害） |
| F9 | 移除 clearWatchHistory 死代码 | api/watchHistory.ts、store/watchHistory.ts | 删除 API 函数 + store import/clearAllHistory（页面无调用） |

### 2.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ **DONE Build complete** | 期间发现并修复 list.vue 下拉刷新残留 `};` 语法错误 |
| `pnpm lint` | 194（46E/148W） | 192（46E/146W） | errors 持平、warnings -2（死代码清理）✅ |
| `pnpm test:run` | 10F/60P | **9F/61P** | **净 -1**：更新 tests/api_room.spec.ts（mock 补 patch + updateRoom/updateSubVenue 断言 PUT→PATCH + 顺手修 getSubVenues 存量断言失配）✅ |
| grep 复核 | — | list.vue 仅剩 isTodaySession 判断与注释；无 getAdminSessionsToday/batchCreateNotifications/clearWatchHistory 引用 | ✅ |

### 2.3 Gate 2 验收结果

```
□ 留言字段/分页对齐（类型+渲染+size）                ✅
□ 绑定手机号 /me/phone                               ✅
□ 房间编辑 PATCH（含 updateSubVenue 连带修复）        ✅
□ 创建场次 playback_url 必填 + 编辑 start_time + 开播跳播放页 ✅
□ 通知：specify 批量（user_ids）/ 全站明确提示 / 无 /batch 残留 ✅
□ today/live 死模式移除（仅 manage/admin）            ✅
□ clear 死代码移除                                   ✅
□ lint/test/build 门禁通过（净改善）                  ✅
```

**审查问题核对**：
- F7 方案修正依据？→ 实测后端 `NotificationCreateRequest.user_ids` 必填数组（schema:128-133）、service 明确"不支持全站"（:331-342）、无 /batch 路由 → 前端改走唯一批量端点，并修复 all 分支隐藏 400
- F6 开播语义？→ 后端无显式 start 端点（room.py POST 仅 3 个），状态由 SRS on-publish 回调驱动 → 前端"确认开播→跳播放页+提示推流"
- F8 是否误伤？→ isTodaySession 判断保留（manage 模式数据无此字段，恒 false）；注释保留

**遗留说明**：
- 通知推送的 user_ids 输入为用户 ID 文本（与后端 public_id 契约一致，联调时验证）
- F6 开播交互与后端回调语义的最终确认留待联调

---

## 阶段 3：新增功能（N1/N3/N4/N5/N6 + N2）（2026-08-10）

### 3.1 实施内容（6 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| N6 | 创建私密房间 | types/room.ts、live-manage/create.vue | RoomCreatePayload 补 `is_private?`；创建页补"房间状态"开关 UI（对齐 edit.vue 样式）+ payload 传 is_private |
| N5 | 专家"已下架"Tab | admin/expert-list/index.vue | tabs 加 inactive；loadExperts/loadMore 按 Tab 传 is_active=false；空态文案区分；恢复走已有编辑表单 is_active 开关 |
| N4 | 用户筛选 3 参数 | types/adminUser.ts、admin/users/index.vue | 类型补 phone_number/nickname/can_stream；**nostream Tab 改服务端 can_stream=false**（移除客户端过滤，保留 ADMIN/SUPERADMIN 排除双保险）；新增手机号/昵称输入框（filter-search--narrow 样式） |
| N1 | 留言删除 | api/message.ts、ChatTab.vue、admin/messages/index.vue | 新增 deleteRoomMessage；ChatTab 长按删除（本人/管理员，403 文案透传）；管理端行内"删除"入口 + meta-text--danger 样式 |
| N3 | **内容安全规则管理页（新增页面）** | 新建 types/contentSafety.ts + api/contentSafety.ts + utils/contentSafetyDisplay.ts + pages/app/admin/content-safety/index.vue；pages.json + my 入口 + permission.config.ts | 规则列表（场景筛选/启停开关/徽标/编辑）+ 新建/编辑弹窗（10 字段）+ 处理记录 Tab（场景/决策筛选+日志列表）；App 范式（useAdminGuard/useSwiperTabs/ModalDialog/上拉加载） |
| N2 | 通知详情 | api/notification.ts、notifications/index.vue | 新增 getNotificationDetail；点击通知→标已读→详情弹层（详情接口刷新，404 回退列表项）→"去观看"跳转 |

### 3.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ DONE Build complete | 通过 |
| `pnpm lint` | 192（46E/146W） | 192（46E/146W） | **不增加** ✅（期间修复 content-safety 页 v-else 链歧义 1 error） |
| `pnpm test:run` | 9F/61P | 9F/61P | **不增加** ✅ |
| grep 复核 | — | 无遗留死代码/引用；新页面注册三件齐（pages.json/入口/权限） | ✅ |

### 3.3 Gate 3 验收结果

```
□ 创建私密房间（开关 UI + payload）                      ✅
□ 专家"已下架"Tab + 恢复                                 ✅
□ 用户筛选：手机号/昵称/服务端 nostream                  ✅
□ 留言：ChatTab 长按自删 + 管理端单删                    ✅
□ 内容安全页：列表/场景筛选/新建/编辑/启停/日志 + 三件注册  ✅
□ 通知详情弹层（接口 + 404 回退 + 去观看）               ✅
□ lint/test/build 门禁通过                               ✅
```

**审查问题核对**：
- 新页面样式是否一致？→ 复用现有管理页范式（top-bar/tab-bar/filter-bar/consumer-layout）+ 页面内自建规则卡片样式（参照 tags/brands 卡片风格，主色 #0F766E）
- 守卫是否齐？→ 页面级 useAdminGuard + 路由级 PAGE_PERMISSIONS（content-safety 已补）+ 「我的」入口 + pages.json 注册
- N4 nostream 行为变化？→ 服务端过滤（分页正确），客户端仅保留角色排除双保险
- N1 权限语义？→ 与后端一致：本人留言/管理员可删；他人长按无反应（canDelete 前置判断）

**遗留说明**：
- N3 表单 PATCH 仅更新后端允许字段（pattern/action/severity/priority/enabled/remark）；规则名称/场景等创建后不可改（后端契约限制）
- 内容安全页联调需后端环境（规则 CRUD + 日志）

---

## 阶段 4：分类治理工具（M1-M4）（2026-08-10）

### 4.1 实施内容（4 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| M4 | API 层封装 | api/category.ts | 类型：CategoryReferences（五类计数）/CategoryMigrateStats/CategoryMergeResult；新增 migrateCategoryReferences（scope）、mergeCategories（attach_children/dry_run） |
| M1 | 分类删除闭环 | departments/index.vue | 组头加"治理"actionSheet（编辑/迁移/合并/删除）；删除流程：非 force 调用 → 无引用直接停用 / **409 → 引用清单弹窗**（五类计数 + warnings 完整文案 + 迁移目标 picker 缺省"其他"）→ "确认级联停用"（force=true 带 target）；400 透传（禁删 other） |
| M2 | 迁移工具 | departments/index.vue | "治理→迁移引用"弹窗：源静态 + 目标 picker + scope picker（全部/仅专家/仅科室/仅房间）→ 确认后展示 migrated 统计 |
| M3 | 合并工具 | departments/index.vue | "治理→合并分类"弹窗：源静态 + 目标 picker + attach_children 开关 + **dry_run 两段式**（先预览统计 → 确认执行）；与科室合并（"合并科室"弹窗）明确区分 |

**样式**：新增 ref-row/ref-label/ref-value（引用统计行）、form-note--warn（警告文案）、cat-edit--govern（治理入口）；复用现有 form-picker/form-static/form-note。

### 4.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ DONE Build complete | 通过 |
| `pnpm lint` | 192（46E/146W） | 192（46E/146W） | **不增加** ✅ |
| `pnpm test:run` | 9F/61P | 9F/61P | **不增加** ✅ |

### 4.3 Gate 4 验收结果

```
□ 删除含引用分类 → 409 弹窗五类计数 + warnings + 目标选择 + force 确认      ✅（代码级，联调验证）
□ 删除无引用分类 → 直接停用；禁删"其他"400 透传                            ✅（代码级）
□ 迁移：scope 参数 + 统计展示                                             ✅（代码级）
□ 合并：dry_run 预览 → 确认执行两段式；与科室合并入口区分                   ✅（代码级）
□ 门禁通过（lint/test/build 不新增失败）                                   ✅
```

**审查问题核对**：
- force 语义是否杜绝静默？→ 删除流程强制"409 弹窗 → force 确认"，actionSheet 无直接 force 路径 ✅
- 409 解析稳定性？→ request.ts "其他"分支透传 error.data（含 references/warnings/message），页面用完整 message 展示（避开 toast 截断）
- 目标选择校验？→ 前端 picker 排除逻辑由后端兜底（≠source/∉子树/启用/非 other），400 透传
- 与科室合并混淆？→ 弹窗标题/说明均标注"分类"层级；科室合并入口保持不变

**遗留说明**：
- 治理功能为代码级完成，**409/迁移/合并需后端环境联调验证**（测试库造引用数据）
- 弹窗在 dry_run 预览后切换确认文案（"预览统计"→"确认合并"），交互闭环

---

## 阶段 5：体验与防御（P2-1~P2-5）（2026-08-10）

### 5.1 实施内容（5 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| P2-1 | 内容安全错误统一文案（2004/2005） | 新建 utils/contentSafety.ts；utils/request.ts | 精简 wechat 文案映射（2005→"内容未通过审核"含图片细分；2004→透传/固定文案）；**request.ts 422 分支与"其他"分支识别 2004/2005**：error.message 覆盖为统一文案 + toast 完整显示（不截断）+ 挂 `__toasted` 标记 |
| P2-4 | 5 场景统一文案（合并入 P2-1） | 同上 | request 层统一处理 → 房间标题/描述/Tab/留言/搜索词 5 场景输入页 catch 自动生效，无需逐页改 |
| P2-5 | 双重 toast 统一 | ChatTab.vue、departments/index.vue（治理 4 处 catch）、notification-push/index.vue | 高频/新增路径 catch 检查 `e?.__toasted` 避免重复提示；其余页面既有行为记录 |
| P2-2 | 管理端留言头像 | admin/messages/index.vue | 行内加头像 image（resolveMediaUrl + 默认兜底 + onAvatarError 变异地字段防重复渲染）+ message-avatar 样式 |
| P2-3 | LiveView 降级聊天提示 | live/LiveView.vue | 文案强化："消息仅本地显示，不会发送到服务器" |

### 5.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ DONE Build complete | 通过 |
| `pnpm lint` | 192（46E/146W） | 192（46E/146W） | **不增加** ✅ |
| `pnpm test:run` | 9F/61P | 9F/61P | **不增加** ✅ |

### 5.3 Gate 5 验收结果

```
□ 内容安全码（2004/2005）request 层统一文案 + 完整 toast（无截断）     ✅（代码级）
□ 双重 toast：ChatTab/治理弹窗/通知推送 catch 检查 __toasted         ✅（代码级）
□ 管理端留言头像（含兜底与防重复渲染）                                ✅
□ LiveView 降级提示强化                                             ✅
□ lint/test/build 门禁通过                                          ✅
```

**审查问题核对**：
- request.ts 全局改动影响面？→ 仅 3 个点：内容安全码文案覆盖、内容安全码 toast 不截断、__toasted 标记；401/403/404 分支与登录特例未动
- 2005 图片细分？→ 仅当后端 message 含"图片/头像/avatar/image"时用图片文案
- P2-4 覆盖范围？→ request 层统一 = 5 场景自动生效（页面无需改）

**遗留说明**：
- 非本次路径的既有双重 toast（其他页面 catch）记录为全局已知行为，未扩大修复
- 内容安全文案联调验证需后端触发 422/2005 场景

---

## 阶段 6：清理与收尾（P3 选取项 + 全量回归）（2026-08-10）

### 6.1 实施内容（6 项全部完成）

| # | 内容 | 文件 | 改动 |
|:--|------|------|------|
| P3-1 | 死代码清理 | api/expert.ts（删 getExpertContent/associateSessionExperts/batchImportExperts）、api/notification.ts（删 deleteNotification + 移除 del import）、api/roomCategories.ts（删 setRoomCategories + RoomCategoriesPayload；**getRoomCategories 有调用方保留**；文件乱码注释重写为 UTF-8） | 5 处潜伏 404/契约错位函数清除 |
| P3-2 | 类型清理 | types/room.ts | 删除 `category_id` 残留（后端已移除该字段、全库无引用） |
| N9 | avatar-crop 简化 | tabbar/my/avatar-crop/index.vue | **实测后端 POST /me/avatar 已自动写库**（users.py:404-407）→ 移除冗余 updateProfile 第二步 + 清理 import |
| P3-6 | 守卫映射补 5 页 | config/permission.config.ts | 补 users/tags/brands/messages/room-tabs 5 页（ADMIN/SUPERADMIN），路由级双保险补齐 |
| P3-7 | 业务码校验 | utils/request.ts | 2xx 分支校验 body.code：对象 + number + 非 200/0 → 构造业务错误 reject（code/message/data 透传）；**超时统一（5000→30000）按分析不做**（记录待单独评估） |
| — | 全量回归 | — | build:h5 ✅ / lint ✅ / test ✅ |

### 6.2 验证结果

| 检查 | 基线 | 本次 | 结论 |
|------|------|------|------|
| `pnpm build:h5` | 成功 | ✅ DONE Build complete | 通过（含业务码校验改动） |
| `pnpm lint` | 192（46E/146W） | 192（46E/146W） | **不增加** ✅ |
| `pnpm test:run` | 9F/61P | 9F/61P | **不增加** ✅（业务码校验无测试回归） |
| grep 复核 | — | 5 处死代码无残留引用；room.category_id 无引用 | ✅ |

### 6.3 Gate 6 验收结果

```
□ 死代码/类型清理完成（无引用报错）                        ✅
□ avatar-crop 上传→刷新头像链路（移除冗余写入）              ✅
□ 管理端 5 页路由级守卫补齐                                ✅
□ 业务码校验生效（2xx+非 200/0 不再静默成功）               ✅（代码级）
□ 全量回归：build/lint/test 全过（不新增失败）              ✅
□ 实施文档/执行记录同步                                    ✅
```

**审查问题核对**：
- 业务码校验误伤风险？→ 校验条件严格（对象 + number code + 非 200/0）；接受 200/0 双值；全量测试无回归；联调期继续观察
- N9 依据？→ 后端 users.py:404-407 实测 avatar 上传即 update_profile 写库
- getRoomCategories 为何保留？→ detail.vue:761 / edit.vue:577 实际调用

**遗留说明**：
- 超时统一（request 5000ms vs API_TIMEOUT 30000）记录为未实施项（全局行为变更风险，单独评估）
- 本分支 6 个阶段全部完成，待联调验证项汇总见下

---

## 附：全阶段待联调验证清单（后端环境）

```
□ 分类：409 引用弹窗 / force 级联 / 迁移统计 / 合并 dry_run（阶段 4）
□ 内容安全：规则 CRUD / 日志 / 2004-2005 文案（阶段 3/5）
□ 留言：C 端长按自删 / 管理端单删 / 头像昵称三段式（阶段 2/3）
□ 通知：详情弹层 / user_ids 批量推送（阶段 3）
□ 场次：创建 playback_url / 编辑 start_time / 开播跳转（阶段 2）
□ 用户管理：phone/nickname/can_stream 服务端筛选（阶段 3）
□ 专家：已下架 Tab / 分类一致性校验 400 文案（阶段 3/1）
□ 头像：上传即写库链路（阶段 6）
□ 业务码校验：2xx+非 200/0 响应形态观察（阶段 6）
```

---

> 更新：2026-08-10 | 状态：✅ 阶段 6 完成（全部 6 阶段实施完毕，待提交）

---

> 更新：2026-08-10 | 状态：✅ 阶段 1 完成（待提交）

---

> 更新：2026-08-10 | 状态：✅ 阶段 0 完成
