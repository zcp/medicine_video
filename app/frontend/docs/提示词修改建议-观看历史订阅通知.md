# 提示词修改建议 - 观看历史·订阅·通知功能

**生成日期**: 2026-02-03  
**基于项目状态扫描**: d:\live_core_service_frontend-main2\src

---

## 📊 项目当前状态总结

### ✅ 保留的文件（需要重新实现）
- `src/api/watchHistory.ts` - 空文件，注释"已回滚"
- `src/api/subscription.ts` - 空文件，注释"已回滚"
- `src/pages/app/notifications/` - 空目录
- `src/pages/app/tabbar/my/subscriptions/` - 空目录

### ❌ 不存在的文件（需要新建）
- `src/pages/app/tabbar/my/watch-history/` - 目录不存在
- `src/store/watchHistory.ts` - 文件不存在
- `src/store/subscription.ts` - 文件不存在
- `src/store/notification.ts` - 文件不存在
- `src/types/notification.ts` - 文件不存在
- `src/types/subscription.ts` - 文件不存在
- `src/types/watchHistory.ts` - 文件不存在

### ✅ 已存在的相关文件（可复用）
- `src/store/favorite.ts` - 收藏功能 Store（可参考）
- `src/store/follow.ts` - 关注功能 Store（可参考）
- `src/api/favorite.ts` - 收藏 API（可参考）
- `src/api/expertFollow.ts` - 关注 API（可参考）
- `src/utils/security.ts` - 223行完整安全工具
- `src/utils/navigation.ts` - 61行导航工具
- `src/utils/errorHandler.ts` - 113行错误处理
- `src/utils/analytics.ts` - 141行埋点工具
- `src/utils/platform.ts` - 385行平台判断工具

---

## 🔧 提示词需要修改的内容

### 1. 更新"文件状态映射表"章节

#### 原文（错误）：
```markdown
| 文件路径 | 状态 | 处理方案 | 优先级 |
|---------|------|---------|-------|
| **API层** |
| `src/api/notification.ts` | ✅ 空文件 | ✅ 完整实现（附录10.3.1） | P0 |
| `src/api/watchHistory.ts` | ⚠️ 已有3个方法 | ✅ 补充clearWatchHistory方法（见下文） | P0 |
| `src/api/subscription.ts` | ⚠️ 已有4个方法 | ✅ 补充clearSubscriptionHistory方法（见下文） | P0 |
| **Store层** |
| `src/store/watchHistory.ts` | ✅ 空文件 | ✅ 完整实现（附录10.4.1） | P0 |
| `src/store/subscription.ts` | ✅ 空文件 | ✅ 完整实现（附录10.4.2） | P0 |
| `src/store/notification.ts` | ✅ 空文件 | ✅ 完整实现（附录10.4.3） | P0 |
| **类型定义** |
| `src/types/notification.ts` | ✅ 空文件 | ✅ 完整实现（附录10.3.2） | P0 |
| `src/types/subscription.ts` | ⚠️ 已有基础类型 | ✅ 补充字段（如需要） | P1 |
| `src/types/watchHistory.ts` | ✅ 已完整 | ✅ 无需修改 | - |
```

#### 修改后（正确）：
```markdown
| 文件路径 | 状态 | 处理方案 | 优先级 |
|---------|------|---------|-------|
| **API层** |
| `src/api/notification.ts` | ❌ 不存在 | ✅ 新建并完整实现（附录10.3.1） | P0 |
| `src/api/watchHistory.ts` | ⚠️ 空文件（已回滚） | ✅ 删除后重新创建并完整实现 | P0 |
| `src/api/subscription.ts` | ⚠️ 空文件（已回滚） | ✅ 删除后重新创建并完整实现 | P0 |
| **Store层** |
| `src/store/watchHistory.ts` | ❌ 不存在 | ✅ 新建并完整实现（附录10.4.1） | P0 |
| `src/store/subscription.ts` | ❌ 不存在 | ✅ 新建并完整实现（附录10.4.2） | P0 |
| `src/store/notification.ts` | ❌ 不存在 | ✅ 新建并完整实现（附录10.4.3） | P0 |
| **类型定义** |
| `src/types/notification.ts` | ❌ 不存在 | ✅ 新建并完整实现（附录10.3.2） | P0 |
| `src/types/subscription.ts` | ❌ 不存在 | ✅ 新建并完整实现 | P0 |
| `src/types/watchHistory.ts` | ❌ 不存在 | ✅ 新建并完整实现 | P0 |
| **页面文件** |
| `src/pages/app/tabbar/my/watch-history/` | ❌ 目录不存在 | ✅ 创建目录和index.vue | P0 |
| `src/pages/app/tabbar/my/subscriptions/` | ✅ 空目录 | ✅ 创建index.vue | P0 |
| `src/pages/app/notifications/` | ✅ 空目录 | ✅ 创建index.vue | P0 |
```

---

### 2. 添加"清理与准备步骤"章节

在提示词开头添加：

```markdown
## 🧹 实施前的清理步骤（重要！）

### 步骤 1：清理空的占位符文件

删除以下文件（它们只包含"已回滚"注释，没有实际代码）：
```bash
# 删除空的 API 文件
rm src/api/watchHistory.ts
rm src/api/subscription.ts
```

### 步骤 2：确认目录结构

检查并创建必要的目录：
```bash
# 检查是否存在，不存在则创建
mkdir -p src/pages/app/tabbar/my/watch-history
mkdir -p src/pages/app/tabbar/my/subscriptions
mkdir -p src/pages/app/notifications
mkdir -p src/store
mkdir -p src/types
```

### 步骤 3：确认 pages.json 配置

在 `src/pages.json` 中添加以下路由配置（如果不存在）：

```json
{
  "pages": [
    // ... 其他页面配置
    {
      "path": "pages/app/tabbar/my/watch-history/index",
      "style": {
        "navigationBarTitleText": "观看历史",
        "enablePullDownRefresh": true
      }
    },
    {
      "path": "pages/app/tabbar/my/subscriptions/index",
      "style": {
        "navigationBarTitleText": "我的订阅",
        "enablePullDownRefresh": true
      }
    },
    {
      "path": "pages/app/notifications/index",
      "style": {
        "navigationBarTitleText": "通知中心",
        "enablePullDownRefresh": true
      }
    }
  ]
}
```

### 步骤 4：检查依赖文件

确认以下文件存在且可用：
- ✅ `src/utils/security.ts` (223行)
- ✅ `src/utils/navigation.ts` (61行)
- ✅ `src/utils/errorHandler.ts` (113行)
- ✅ `src/utils/analytics.ts` (141行)
- ✅ `src/utils/platform.ts` (385行)
- ✅ `src/utils/auth.ts` (309行)

如果缺少，需要先创建这些工具文件。
```

---

### 3. 更新"核心原则"章节

#### 原文：
```markdown
### 2. 逻辑复用，视图构建

- **Store复用**: 所有Store（watchHistoryStore、subscriptionStore、notificationStore）已在设计文档中定义，直接复用
- **API复用**: 所有API接口（api/watchHistory.ts、api/subscription.ts、api/notification.ts）已定义，直接调用
```

#### 修改后：
```markdown
### 2. 逻辑复用，视图构建

- **Store新建**: 所有Store（watchHistoryStore、subscriptionStore、notificationStore）需要新建，按照设计文档完整实现
- **API新建**: 所有API接口（api/watchHistory.ts、api/subscription.ts、api/notification.ts）需要新建，按照设计文档完整实现
- **工具函数复用**: 所有工具函数（navigation.ts、analytics.ts、security.ts等）已存在，直接导入使用
- **你的职责**: 
  1. 新建所有 API、Store、Types 文件
  2. 构建页面的 `<template>` 和 `<script setup>`
  3. 确保所有代码可直接运行
```

---

### 4. 添加"文件创建顺序"章节

```markdown
## 📋 推荐的实施顺序

### 阶段 1：基础设施（P0）

1. **创建类型定义**：
   - `src/types/watchHistory.ts`
   - `src/types/subscription.ts`
   - `src/types/notification.ts`

2. **创建 API 层**：
   - `src/api/watchHistory.ts`
   - `src/api/subscription.ts`
   - `src/api/notification.ts`

3. **创建 Store 层**：
   - `src/store/watchHistory.ts`
   - `src/store/subscription.ts`
   - `src/store/notification.ts`

### 阶段 2：工具函数补充（P1）

4. **补充工具函数**：
   - 在 `src/utils/security.ts` 末尾添加图片占位符功能
   - 创建 `src/utils/logger.ts`（如果不存在）
   - 创建 `src/utils/polling.ts`（如果不存在）

### 阶段 3：页面实现（P0）

5. **创建页面文件**：
   - `src/pages/app/tabbar/my/watch-history/index.vue`
   - `src/pages/app/tabbar/my/subscriptions/index.vue`
   - `src/pages/app/notifications/index.vue`

6. **配置路由**：
   - 在 `src/pages.json` 中添加3个页面的路由配置

### 阶段 4：测试验证（P2）

7. **功能测试**：
   - 测试观看历史记录、删除、清空
   - 测试订阅功能、Tab切换、状态判断
   - 测试通知列表、已读标记、轮询更新
```

---

### 5. 更新"禁止从零开始"章节

#### 原文：
```markdown
### 3. 禁止从零开始

- 不要创建新的业务逻辑
- 不要创建新的API接口
- 不要创建新的工具函数
- 所有需要的代码都已在设计文档中定义，你只需复用
```

#### 修改后：
```markdown
### 3. 按照设计文档实现

- ✅ **需要创建**: API接口、Store、Types、页面文件（按照设计文档完整实现）
- ✅ **可以复用**: 已有的工具函数（security.ts、navigation.ts、errorHandler.ts等）
- ✅ **参考实现**: 可参考已有的 favorite.ts、follow.ts 等类似功能的实现方式
- ❌ **禁止随意修改**: 不要修改已有的工具函数和全局配置（除非设计文档明确要求）
```

---

## 📌 关键提醒

### 给 AI 的明确指令

在提示词开头添加：

```markdown
## ⚠️ 重要提醒

### 当前项目状态（2026-02-03 扫描结果）

1. **之前的实现已被完全回滚**：
   - API 文件只剩空壳（watchHistory.ts、subscription.ts）
   - Store 文件不存在
   - Types 文件不存在
   - 页面文件不存在或为空目录

2. **你需要从头开始实现**：
   - 不要假设任何文件已经存在
   - 不要假设任何方法已经实现
   - 严格按照设计文档创建所有文件

3. **实施前必须先清理**：
   - 删除 `src/api/watchHistory.ts`（空文件）
   - 删除 `src/api/subscription.ts`（空文件）
   - 然后重新创建这些文件

4. **可以复用的内容**：
   - ✅ `src/utils/` 下的工具函数（security、navigation、errorHandler等）
   - ✅ `src/store/favorite.ts`、`src/store/follow.ts` 作为参考
   - ✅ 项目已有的 UI 组件和样式规范
```

---

## 📝 完整的修改清单

### 需要在提示词中修改的章节

1. ✅ **添加"实施前的清理步骤"章节**
2. ✅ **更新"文件状态映射表"** - 所有文件状态改为"不存在"或"空文件"
3. ✅ **更新"核心原则"** - 明确说明需要新建而非复用
4. ✅ **添加"文件创建顺序"章节**
5. ✅ **更新"禁止从零开始"** - 改为"按照设计文档实现"
6. ✅ **添加"重要提醒"章节** - 说明项目已回滚状态
7. ✅ **更新 pages.json 配置示例** - 给出完整的路由路径

---

## 🎯 总结

**核心问题**：
- 提示词假设很多文件已存在，但实际上都已被删除或回滚
- 需要明确告诉 AI：这是一个"从零开始"的实现，而不是"补充完善"

**解决方案**：
- 在提示词开头添加"当前项目状态"说明
- 更新所有文件状态为"不存在"或"需要新建"
- 添加清理步骤，确保旧文件不会干扰新实现
- 明确实施顺序，避免依赖问题

**预期效果**：
- AI 会按照设计文档完整实现所有功能
- 不会因为假设文件已存在而跳过关键代码
- 实施前会先清理旧文件，避免冲突
