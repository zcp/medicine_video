# 专家/品牌页面实时搜索与拼音排序代码生成提示词

> **版本**: V3.0（安全修正版）  
> **最后更新**: 2026-03-08  
> **技术栈**: uni-app + Vue3 + TypeScript + Pinia  
> **适用平台**: H5/Android/iOS（App优先，可选小程序）  
> **重大更新**:  
> - 🔧 **V3.0**: 采用安全的混合模式，最小化修改风险
> - 🔧 **V2.0**: 统一使用pinyin-pro、完善回滚方案、修正9个核心问题
> - 🎯 **核心策略**: 渐进式增强，不破坏现有架构

---

## 📌 V3.0 修正说明（基于项目实际代码分析）

**修正问题**：
1. ✅ **包管理器**: 修正为npm（不是pnpm）
2. ✅ **组件路径**: 删除矛盾指令，明确优化现有组件
3. ✅ **Expert Store**: 采用混合模式（保留action筛选 + 升级排序算法）
4. ✅ **Brand Store**: 改为可选创建，不强制迁移页面
5. ✅ **页面修改**: 最小化改动，保持现有调用方式

**安全性提升**：
- 🟢 **风险等级**: 从高风险降至低风险
- 🟢 **修改行数**: 从200+行降至10-15行
- 🟢 **回归测试**: 从10+文件降至2-3文件
- 🟢 **实施时间**: 从3-4天降至0.5-1天

**核心原则**：
- ✅ 保留现有架构（不改数据流）
- ✅ 只升级排序算法（插件替换）
- ✅ 渐进式实施（工具→Store→页面）
- ✅ 可回滚设计（每步独立）

---

## 🎯 V3.0 执行摘要（30秒速览）

**本次修正的关键变化**：

| 维度 | V2.0（原方案） | V3.0（安全方案） | 改进 |
|------|---------------|----------------|------|
| **Store重构** | action只加载，getter筛选 | 保留action筛选，升级排序 | ✅ 兼容现有调用 |
| **修改行数** | 200+行 | 10-18行 | ✅ 降低91% |
| **风险等级** | 高（3个🔴） | 低（全🟢） | ✅ 零高风险项 |
| **实施时间** | 3-4天 | 0.5-1天 | ✅ 节省75% |
| **影响文件** | 8个文件大改 | 3个文件小改 | ✅ 降低63% |
| **品牌页面** | 强制迁移 | 可选创建 | ✅ 灵活可控 |

**核心修改**（仅3个文件）：
1. ✅ `expert.ts`: 只改1行排序代码
2. ✅ `expert/index.vue`: 改3-5行（firstLetter函数）
3. ✅ `LetterIndex.vue`: 加5-10行（悬浮提示）

**跳过的操作**：
- ⏭️ expert.ts 架构重构（保持现有）
- ⏭️ brand/index.vue 迁移（未来优化）
- ⏭️ ExpertSearch组件修改（已完善）  

---

## 📋 角色定义与任务目标

### 角色定位

你是一名**资深移动端前端工程师**，具备以下专业技能：

- **精通技术栈**：uni-app、Vue 3 Composition API、TypeScript、移动端H5、小程序等多端开发
- **性能优化专家**：熟悉移动端性能优化、内存管理、列表优化、搜索防抖等技术
- **交互设计能力**：精通移动端交互设计（字母索引、滑动定位、悬浮提示）
- **安全意识强**：熟悉数据脱敏、日志记录、隐私保护等安全规范

### 核心目标

基于已有项目基础，为**专家页面**和**品牌页面**实现以下功能：

1. **拼音排序功能**（专家页面）：按姓名拼音首字母A-Z排序，支持字母索引和悬浮提示
2. **实时搜索功能**（专家+品牌）：支持汉字、拼音、拼音首字母三种搜索方式
3. **字母分组显示**（专家页面）：按首字母分组，吸顶分组头，参考微信通讯录
4. **性能优化**：缓存管理、防抖搜索、内存优化、虚拟滚动
5. **安全合规**：日志记录脱敏、隐私保护

---

## ⚠️ 重要声明与前置要求

### 开发策略

**本提示词采用增量开发策略：在现有页面基础上进行功能增强，不破坏现有代码。**

- ✅ **技术栈**：纯Vue 3 Composition API + TypeScript + Pinia
- ✅ **组件规范**：统一使用uni-app原生组件 + iconfont图标
- ✅ **性能优先**：首屏加载 < 3秒，搜索响应 < 200ms，内存占用 < 200MB
- ✅ **安全合规**：遵循《直播SaaS平台移动端前端设计文档》第3.5节日志规范
- ✅ **移动优化**：流量优化、电量优化、低端设备适配

### 必读文档

**在开始开发前，你必须先阅读以下文档：**

📖 **设计文档**：
- `docs/专家品牌实时搜索和拼音排序设计文档.md`（本功能完整设计文档）
- `docs/直播SaaS平台移动端前端设计文档.md`（第3.5节日志规范、第4.2节脱敏规范、第6章性能优化）
- `src/pages/app/tabbar/expert/index.vue`（现有专家页面代码）
- `src/pages/app/tabbar/brand/index.vue`（现有品牌页面代码）
- `src/store/expert.ts`（现有专家Store）
- `src/store/brand.ts`（现有品牌Store）

---

## 第0章：强制性前置检查 ⚡（必须先执行）

### ⚠️ 强制执行流程（禁止跳过）

**AI在生成代码前，必须按顺序执行以下步骤，禁止跳过任何步骤：**

#### 步骤A：声明（必须输出）
在聊天窗口输出：
```
我将执行前置检查，禁止在用户确认前生成代码。
我承诺遵守以下规则：
1. 不覆盖已有代码
2. 不创建重复组件
3. 不破坏现有字段
4. 等待用户确认后才生成代码
```

### 0.1 检查目的

在生成任何代码之前，必须全面了解项目现状，避免：
- ❌ 覆盖已有的完善代码
- ❌ 与现有类型定义冲突
- ❌ 创建重复的工具函数
- ❌ 破坏现有的导入依赖关系
- ❌ 创建重复的组件文件

### 0.2 必须执行的检查步骤

#### 步骤1：读取现有核心文件（强制）

你必须先读取以下文件，了解现有实现：

```bash
必须读取的文件清单：
✅ src/pages/app/tabbar/expert/index.vue - 现有专家页面（重点：字母索引实现）
✅ src/pages/app/tabbar/brand/index.vue - 现有品牌页面
✅ src/store/expert.ts - 专家Store（重点：现有字段清单）
✅ src/store/brand.ts - 品牌Store
✅ src/components/expert/ExpertSearch.vue - 现有搜索组件（检查是否需要优化）
✅ src/components/expert/LetterIndex.vue - 现有字母索引组件（检查是否需要优化）
✅ src/components/expert/ExpertCard.vue - 专家卡片组件（检查依赖字段）
✅ src/api/expert.ts - 专家API
✅ src/api/brand.ts - 品牌API
✅ src/types/models.ts - 数据模型定义
✅ src/utils/logger.ts - 日志工具（确认导入路径）
✅ src/static/fonts/iconfont.css - 可用图标列表
✅ package.json - 依赖清单（检查lodash-es是否已安装）
```

**执行命令**：使用 `read_file` 工具逐个读取以上文件。

#### 步骤2：扫描组件目录（强制）

```bash
必须扫描的目录：
✅ src/components/expert/ - 专家相关组件（检查是否已有LetterIndex、ExpertSearch）
✅ src/components/brand/ - 品牌相关组件
✅ src/components/business/ - 业务组件目录
✅ src/components/common/ - 通用组件目录
✅ src/utils/ - 工具函数目录（检查是否已有拼音工具）
✅ src/config/ - 配置文件目录
```

**执行命令**：使用 `list_dir` 工具扫描目录。

#### 步骤3：在聊天窗口输出项目现状分析（强制）

**重要**：❗ 不要生成单独的分析报告文件，直接在聊天窗口中展示分析结果！

基于步骤1和步骤2的检查结果，在聊天窗口中输出以下结构化分析：

---

**📊 专家/品牌页面功能开发现状分析**

**一、已存在且可直接复用的文件（✅ 直接使用）**
- 列出所有可复用的API、Store、组件
- 注明文件路径、核心功能
- 给出"直接导入使用"的结论
**必须明确列出现有字段清单**（包括字段名、类型、用途）
- **必须明确列出缺失字段**（避免字段冲突）
- **必须说明是否有组件路径冲突**（如LetterIndex、ExpertSearch）
- 说明操作方式（multi_replace_string_in_file，保留现有字段）
- **特别注意**：Store中的list、keyword、specialty等字段如已存在，不要重复创建ue的字母索引功能）
- 明确列出现有字段清单
- 明确列出缺失字段（如字母索引组件、拼音排序逻辑）
- 说明操作方式（multi_edit，保留现有字段）

**三、需要新建的文件（➕ 需新建）**
- 列出所有需要新建的工具文件
- 列出需要新建的组件文件
- 列出需要新建的配置文件

**四、功能需求差异分析（📍 专家vs品牌）**
- 专家页面需求：完整功能（拼音排序+字母索引+实时搜索）
- 品牌页面需求：精简功能（仅实时搜索，无拼音排序）

**五、安全性分析**
- ⚠️ 列出潜在风险点
- ✅ 给出安全操作策略

---

#### 步骤4：等待用户确认（强制）

在聊天窗口输出分析结果后，**必须等待用户确认**才能继续生成代码。

向用户提问：
```
⚠️ 发现的潜在冲突点：
- [列出在分析中发现的所有冲突，如组件重复、字段冲突等]

请确认：
1. 分析结果是否准确？
2. 是否有遗漏或需要调整的地方？
3. 对于上述冲突点，是优化现有文件还是创建新文件？
4. 如果确认无误，请输入"确认继续"开始代码生成。
```

**❌ 禁止事项（违反将导致代码错误）**：
- ❌ 禁止在用户确认前开始生成任何代码
- ❌ 禁止假设用户已确认
- ❌ 禁止跳过冲突点分析
- ❌ 禁止创建与现有组件路径冲突的文件

**禁止**在用户确认前开始生成任何代码！

---

## 第1章：核心技术规范

### 1.1 技术栈与组件规范

#### 1.1.1 基础技术栈
- **框架**：uni-app (Vue 3 + Composition API + TypeScript)
- **状态管理**：Pinia
- **拼音处理库**：pinyin-pro (~50KB，gzip后约15KB)
- **工具库**：项目自带 `src/utils/debounce.ts`（防抖函数）

#### 1.1.2 UI组件规范
**所有UI元素必须使用uni-app原生组件 + iconfont图标**

```vue
<!-- ✅ 正确：使用uni-app原生组件 -->
<view class="search-container">
  <input 
    v-model="keyword" 
    type="text" 
    placeholder="搜索专家（支持拼音）"
    @input="handleSearchInput"
    class="search-input"
  />
  <text class="iconfont icon-search"></text>
</view>

<!-- ❌ 错误：使用Element Plus组件 -->
<el-input v-model="keyword" placeholder="搜索专家" />
```

#### 1.1.3 可用图标速查表

| 图标类名 | 用途 | 图标类名 | 用途 |
|---------|------|---------|------|
| `icon-search` | 搜索 | `icon-close` | 清空/关闭 |
| `icon-star` | 收藏 | `icon-star-filled` | 已收藏 |
| `icon-heart` | 关注 | `icon-heart-filled` | 已关注 |
| `icon-arrow-right` | 右箭头 | `icon-arrow-down` | 下箭头 |

### 1.2 拼音工具函数核心接口

#### 1.2.1 功能函数清单

| 函数名 | 功能 | 性能指标 |
|--------|------|----------|
| `getFirstLetter(text)` | 获取拼音首字母（A-Z/#） | < 1ms |
| `getPinyin(text)` | 获取完整拼音 | < 5ms |
| `sortByPinyin(items, getNameFn)` | 拼音排序（三级排序） | < 50ms（1000条） |
| `groupByFirstLetter(items, getNameFn)` | 按首字母分组 | < 10ms（1000条） |
| `getSortedLetters(groups)` | 获取排序后的字母列表 | < 1ms |
| `searchByPinyin(items, keyword, getNameFn)` | 实时搜索过滤 | < 20ms（1000条） |
| `loadPinyinWhitelist(configUrl?)` | 动态加载多音字白名单 | 异步 |

#### 1.2.2 三级排序规则

```typescript
// 排序优先级：拼音字典序 > 原始汉字Unicode > 长度
sortByPinyin(items, (item) => item.name)

// 示例结果：
// "丁一" < "丁之明" (拼音字典序：dingyi < dingzhiming)
// "李芳" < "李方" (拼音相同时，按汉字Unicode)
// "张" < "张三" (拼音相同时，较短排前)
```

### 1.3 字母索引组件交互规范

#### 1.3.1 字母索引设计

```
┌─────────────────────────────────┐
│  ┌─────┐                        │
│  │搜索框│                        │  A
│  └─────┘                        │  B
│                                 │  C
│  === A组分组头（吸顶） ===       │  D ← 字母索引（右侧固定）
│  [专家卡片] 丁一                 │  L
│  [专家卡片] 丁之明                │  Z
│                                 │  #
│  === L组分组头（吸顶） ===       │
│  [专家卡片] 李芳                 │      ┌───────┐
│  [专家卡片] 刘德华                │      │   D   │ ← 悬浮提示
│                                 │      └───────┘
└─────────────────────────────────┘
```

#### 1.3.2 交互规则

| 交互行为 | 触发条件 | 响应效果 |
|---------|---------|---------|
| **点击字母** | 用户点击字母索引 | 列表滚动到对应字母分组 |
| **显示悬浮提示** | 点击字母或滑动 | 屏幕中央显示大号字母（如：D） |
| **隐藏悬浮提示** | 1秒后自动隐藏 | 淡出动画 |
| **吸顶效果** | 滚动列表 | 分组头固定在顶部 |

### 1.4 性能优化规范

#### 1.4.1 缓存策略

| 缓存项 | 有效期 | 清理时机 |
|--------|--------|---------|
| **专家列表** | 5分钟 | 下拉刷新/页面卸载 |
| **品牌列表** | 5分钟 | 下拉刷新/页面卸载 |
| **拼音索引** | 无限期 | 数据重新加载时 |

#### 1.4.2 搜索防抖

```typescript
import { debounce } from '@/utils/debounce';

// 统一使用400ms防抖（与现有代码保持一致）
const handleSearchInput = debounce((keyword: string) => {
  expertStore.setKeyword(keyword);
}, 400);
```

#### 1.4.3 内存优化

```typescript
// ⚠️ 注意：项目已有完整的 Expert 接口，包含所有必需字段
// 不需要创建单独的 ExpertBrief 接口
// 对于1000条数据，完整对象和精简对象的内存差异可忽略（<10KB）

// 内存优化策略：
// 1. 图片懒加载：减少首屏图片加载
// 2. 虚拟滚动：数据量 > 1000时启用
// 3. 缓存拼音结果：避免重复计算
```

### 1.5 安全与日志规范

#### 1.5.1 日志记录规范（遵循设计文档第3.5节）

```typescript
import { logger } from '@/utils/logger';

// ✅ 正确：记录性能指标（脱敏）
logger.info('拼音排序完成', {
  dataCount: items.length,
  duration: sortDuration,
  level: 'INFO'
});

// ✅ 正确：记录搜索行为（只记录长度）
logger.info('搜索过滤完成', {
  keywordLength: keyword.length,  // 不记录具体内容
  resultCount: filteredItems.length,
  duration: searchDuration,
  level: 'INFO'
});

// ❌ 错误：记录敏感信息
logger.info('用户搜索', {
  keyword: keyword,  // 禁止记录具体搜索内容
  userName: userName  // 禁止记录用户姓名原文
});
```

#### 1.5.2 敏感信息脱敏（遵循设计文档第4.2节）

```typescript
// ✅ 正确：专家姓名脱敏
const maskName = (name: string) => {
  return name.charAt(0) + '**';  // "张三" → "张**"
};

// 日志中使用脱敏后的数据
logger.info('专家详情查看', {
  expertId: expertId,  // ID可记录
  expertName: maskName(expert.name),  // 姓名脱敏
  level: 'INFO'
});
```

---

## 第2章：文件清单与生成指令

### 2.1 P0级任务（必须完成）⚠️

**⚠️ 重要提示**：基于实际项目代码分析，采用安全的渐进式方案。

| 序号 | 文件路径 | 操作类型 | 说明 | 修改量 | 风险等级 |
|-----|---------|---------|------|--------|----------|
| 1 | `src/utils/pinyin.ts` | ✨ **新建** | 拼音工具函数（已测试，100%通过） | 新增 | 🟢 极低 |
| 2 | `src/config/pinyin-whitelist.json` | ✨ **新建** | 多音字白名单配置 | 新增 | 🟢 极低 |
| 3 | `src/components/expert/LetterIndex.vue` | 🔧 **优化** | 添加悬浮提示功能（保持现有） | 5-10行 | 🟢 低 |
| 4 | `src/components/expert/ExpertSearch.vue` | ⏭️ **跳过** | 已完善（400ms防抖），无需修改 | 0行 | 🟢 无风险 |
| 5 | `src/store/expert.ts` | 🔧 **优化** | 只升级排序算法（保留现有架构） | 2-3行 | 🟢 低 |
| 6 | `src/store/brand.ts` | ✨ **新建** | 创建Store但不迁移页面（可选） | 新增 | 🟢 低 |
| 7 | `src/pages/app/tabbar/expert/index.vue` | 🔧 **优化** | 修改firstLetter函数（保持调用） | 3-5行 | 🟢 低 |
| 8 | `src/pages/app/tabbar/brand/index.vue` | ⏭️ **跳过** | 保持现有实现（未来优化） | 0行 | 🟢 无风险 |

**修改总计**: 10-18行代码（V3.0安全方案 vs V2.0的200+行）

**📋 前置检查后的调整规则**：
1. 如果 `src/components/expert/LetterIndex.vue` 已存在：改为优化该文件，**不创建** `src/components/business/LetterIndex.vue`
2. 如果 `src/components/expert/ExpertSearch.vue` 已存在：改为优化该文件，**不创建** `src/components/business/ExpertSearch.vue`
3. 如果 Store 中 `keyword`、`specialty` 字段已存在：**不重复创建**，只添加缺失的 `cacheTimestamp`
4. 如果专家页面已有字母索引逻辑：**增强现有功能**而非全部重写

### 2.2 依赖安装

**⚠️ 先检查依赖是否已安装**：执行前置检查时，查看 `package.json` 中是否已有 `pinyin-pro`。

```bash
# 必须安装：拼音处理库（使用npm，不是pnpm）
npm install pinyin-pro

# 说明：
# - 项目使用npm作为包管理器（根据package.json和终端历史确认）
# - lodash-es 已在项目中安装，无需重复安装
# - 项目已有 src/utils/debounce.ts，使用项目自带工具
# - 因此只需安装 pinyin-pro 即可
```

---

## 第3章：分步生成与验证流程

### 步骤1：生成拼音工具函数（核心基础设施）

**📁 文件：`src/utils/pinyin.ts`**

**关键要求**：
1. 使用 `pinyin-pro` 库进行拼音转换
2. 支持多音字白名单机制
3. 实现三级排序规则（拼音→汉字→长度）
4. 支持汉字、拼音、拼音首字母三种搜索
5. 完整的JSDoc注释和性能指标标注
6. **排序和搜索统一使用 pinyin-pro**（保证一致性）

**核心函数接口**：

```typescript
// 获取拼音首字母（大写）
export function getFirstLetter(text: string): string

// 获取完整拼音（小写）
export function getPinyin(text: string): string

// 拼音排序（三级排序）
export function sortByPinyin<T>(
  items: T[], 
  getNameFn: (item: T) => string
): T[]

// 按首字母分组
export function groupByFirstLetter<T>(
  items: T[],
  getNameFn: (item: T) => string
): Record<string, T[]>

// 获取排序后的字母列表
export function getSortedLetters(
  groups: Record<string, any[]>
): string[]

// 实时搜索过滤
export function searchByPinyin<T>(
  items: T[],
  keyword: string,
  getNameFn: (item: T) => string
): T[]

// 动态加载白名单
export async function loadPinyinWhitelist(
  configUrl?: string
): Promise<void>
```

**性能标准**：
- `getFirstLetter`: < 1ms
- `getPinyin`: < 5ms
- `sortByPinyin`: < 50ms（1000条数据）
- `searchByPinyin`: < 20ms（1000条数据）

**实现策略**：
```typescript
// 拼音缓存：避免重复计算
const pinyinCache = new Map<string, string>();

function getPinyin(text: string): string {
  if (pinyinCache.has(text)) return pinyinCache.get(text)!;
  
  const result = pinyin(text, { toneType: 'none', type: 'array' }).join('');
  pinyinCache.set(text, result);
  return result;
}
```

**关键边界情况处理**:
```typescript
// 1. 空字符串：返回 '#'
// 2. 非汉字（英文、数字）：返回首字符大写
// 3. 特殊字符：返回 '#'
```

**完整实现代码**：

```typescript
// src/utils/pinyin.ts

import { pinyin } from 'pinyin-pro';

/**
 * 多音字/特殊读音白名单（医疗领域常见姓名修正）
 * 优先级：白名单 > pinyin-pro自动识别
 */
const PINYIN_WHITELIST: Record<string, string> = {};

/**
 * 获取汉字的拼音首字母（大写）
 * @param text 汉字文本
 * @returns 拼音首字母，如 "丁" → "D"
 */
export function getFirstLetter(text: string): string {
  if (!text) return '#';
  
  const firstChar = text.charAt(0);
  
  // 1. 处理英文字母（优先级最高）
  if (/^[a-zA-Z]$/.test(firstChar)) {
    return firstChar.toUpperCase();
  }
  
  // 2. 处理中文字符
  if (/[\u4e00-\u9fa5]/.test(firstChar)) {
    // 检查白名单（完整名字优先匹配）
    if (PINYIN_WHITELIST[text]) {
      return PINYIN_WHITELIST[text].charAt(0).toUpperCase();
    }
    
    // 获取拼音首字母
    const py = pinyin(firstChar, { 
      pattern: 'first',  // 只取首字母
      toneType: 'none'   // 无音调
    }).toUpperCase();
    
    return py || '#';
  }
  
  // 3. 其他字符（数字、符号等）归类到 #
  return '#';
}

/**
 * 获取完整拼音（用于排序和搜索）
 * @param text 汉字文本
 * @returns 完整拼音，如 "丁之明" → "dingzhiming"
 */
export function getPinyin(text: string): string {
  if (!text) return '';
  
  // 优先检查白名单（完整名字匹配）
  if (PINYIN_WHITELIST[text]) {
    return PINYIN_WHITELIST[text].toLowerCase();
  }
  
  // 使用 pinyin-pro 转换
  return pinyin(text, { 
    pattern: 'pinyin',
    toneType: 'none',
    type: 'array'
  }).join('').toLowerCase();
}

/**
 * 按拼音排序（三级排序规则）
 * @param items 要排序的数组
 * @param getNameFn 获取姓名的函数
 * @returns 排序后的数组（新数组，不修改原数组）
 */
export function sortByPinyin<T>(
  items: T[], 
  getNameFn: (item: T) => string
): T[] {
  return items.slice().sort((a, b) => {
    const nameA = getNameFn(a);
    const nameB = getNameFn(b);
    
    // 1. 获取完整拼音序列
    const pinyinA = getPinyin(nameA);
    const pinyinB = getPinyin(nameB);
    
    // 2. 拼音比较（使用中文locale，区分重音）
    const pinyinCompare = pinyinA.localeCompare(pinyinB, 'zh-Hans-CN', { 
      sensitivity: 'accent' 
    });
    
    // 3. 拼音相同时，回退到原始汉字比较（解决同音字排序）
    if (pinyinCompare === 0) {
      return nameA.localeCompare(nameB, 'zh-Hans-CN');
    }
    
    return pinyinCompare;
  });
}

/**
 * 按首字母分组（参考iOS通讯录）
 * @param items 要分组的数组
 * @param getNameFn 获取姓名的函数
 * @returns 分组后的对象，键为首字母，值为该组的数据
 */
export function groupByFirstLetter<T>(
  items: T[],
  getNameFn: (item: T) => string
): Record<string, T[]> {
  const groups: Record<string, T[]> = {};
  
  items.forEach(item => {
    const name = getNameFn(item);
    const letter = getFirstLetter(name);
    
    if (!groups[letter]) {
      groups[letter] = [];
    }
    groups[letter].push(item);
  });
  
  return groups;
}

/**
 * 获取排序后的字母列表（包含 #）
 * @param groups 分组后的数据
 * @returns 排序后的字母数组
 */
export function getSortedLetters(groups: Record<string, any[]>): string[] {
  const letters = Object.keys(groups);
  
  // 分离普通字母和特殊符号
  const normalLetters = letters.filter(l => l !== '#').sort();
  const hasSpecial = letters.includes('#');
  
  // # 排在最后
  return hasSpecial ? [...normalLetters, '#'] : normalLetters;
}

/**
 * 实时搜索过滤（支持拼音和汉字）
 * @param items 要搜索的数组
 * @param keyword 搜索关键词
 * @param getNameFn 获取姓名的函数
 * @returns 过滤后的数组
 */
export function searchByPinyin<T>(
  items: T[],
  keyword: string,
  getNameFn: (item: T) => string
): T[] {
  if (!keyword.trim()) return items;
  
  const kw = keyword.toLowerCase().trim();
  
  return items.filter(item => {
    const name = getNameFn(item);
    const nameLower = name.toLowerCase();
    const fullPinyin = getPinyin(name);
    
    // 1. 汉字匹配
    if (nameLower.includes(kw)) return true;
    
    // 2. 完整拼音匹配
    if (fullPinyin.includes(kw)) return true;
    
    // 3. 拼音首字母匹配
    const initials = pinyin(name, { pattern: 'first', toneType: 'none', type: 'array' })
      .join('').toLowerCase();
    if (initials.includes(kw)) return true;
    
    return false;
  });
}

/**
 * 动态加载多音字白名单配置
 * @param configUrl 配置文件URL或本地路径
 */
export async function loadPinyinWhitelist(configUrl?: string): Promise<void> {
  try {
    const config = configUrl 
      ? await fetch(configUrl).then(r => r.json())
      : await import('@/config/pinyin-whitelist.json');
    
    Object.assign(PINYIN_WHITELIST, config);
    console.log('[Pinyin] 白名单加载成功:', Object.keys(PINYIN_WHITELIST).length, '条');
  } catch (e) {
    console.warn('[Pinyin] 白名单加载失败，使用默认配置:', e);
  }
}
```

**完整代码参考**：参见《专家品牌实时搜索和拼音排序设计文档.md》第4.1节

---

### 步骤2：创建多音字白名单配置

**📁 文件：`src/config/pinyin-whitelist.json`**

**格式说明**：
```json
{
  "解文": "xiewen",
  "单医生": "shanys",
  "仇医生": "qiuys"
}
```

**维护方式**：
1. 通过后台管优化或创建字母索引组件

**📁 文件：`src/components/expert/LetterIndex.vue`**（✅ 已存在，只需优化）

**⚠️ 重要**：该文件已存在且功能完善，**不要创建新文件**，只需添加以下功能：

---

### 步骤3：优化字母索引组件（添加悬浮提示）

**📁 文件：`src/components/expert/LetterIndex.vue`**（使用 `replace_string_in_file` 修改）

**新增功能**：
1. 显示悬浮字母提示（屏幕中央，1秒后自动隐藏）
2. 保持现有的右侧固定定位、点击功能
3. 保持现有样式和交互

**Props接口**：
```typescript
interface Props {
  letters: string[];      // 字母列表（如：['A', 'D', 'L', 'Z', '#']）
  active: string;         // 当前激活的字母
}

// Emits
const emit = defineEmits<{
  (e: 'pick', letter: string): void  // 用户选择字母
}>();
```

**样式要求**：
- 右侧固定定位：`position: fixed; right: 10rpx;`
- 垂直居中：`top: 50%; transform: translateY(-50%);`
- 字母间距：`padding: 6rpx 0;`
- 激活状态：蓝色高亮（`color: #0F766E;`）
- 悬浮提示：屏幕中央，大号字体（80rpx），半透明背景

**交互行为**：
```typescript
// 点击字母
const handleLetterClick = (letter: string) => {
  emit('pick', letter);
  showFloatingLetter.value = true;
  activeLetter.value = letter;
  
  // 1秒后隐藏悬浮提示
  if (floatingTimer) clearTimeout(floatingTimer);
  floatingTimer = setTimeout(() => {
    showFloatingLetter.value = false;
  }, 1000);
};

// 触摸滑动快速定位
const handleTouchMove = (e: any) => {
  // 计算触摸位置对应的字母索引
  // 触发pick事件
};优化或创建搜索框组件

**📁 文件：`src/components/expert/ExpertSearch.vue`**（✅ 已存在且功能完善）

**⚠️ 重要**：该文件已实现400ms防抖搜索，**无需修改**
---

### 步骤4：搜索框组件（无需操作）

**📁 文件：`src/components/expert/ExpertSearch.vue`**（✅ 已完善，跳过）

**组件功能**：
1. 支持实时输入搜索
2. 显示清空按钮（输入内容时）
3. 显示搜索图标
4. 显示placeholder提示"搜索专家（支持拼音）"

**Props接口**：
```typescript
interface Props {
  value: string;              // 搜索关键词（v-model）
  placeholder?: string;       // 占位符文本
}

// Emits
const emit = defineEmits<{
  (e: 'input', value: string): void  // 用户输入
  (e: 'clear'): void                 // 清空按钮点击
}>();
```

**样式要求**：
- 圆角搜索框：`border-radius: 32rpx;`
- 背景色：`#F5F5F5`
- 左侧搜索图标：`icon-search`
---

### 步骤5：优化专家Store（混合模式 - 安全方案）

**📁 文件：`src/store/expert.ts`**

**操作类型**：🔧 **渐进优化**（保留现有架构，只升级排序算法）

**🎯 核心变更理念**：

**现状分析：**
当前 `fetchExperts()` 在action中筛选和排序，这是**合理的**架构，只需升级排序算法即可。

```typescript
// ✅ 现有架构（保留）
async fetchExperts(params) {
  const filtered = data.filter(item => item.name.includes(keyword)); // ✅ 保留
  const sorted = filtered.sort(...); // 🔄 升级为拼音排序
  this.list = sorted;
}
```

**优化目标**：
- ✅ **保留现有架构**：action继续筛选排序（兼容现有调用）
- ✅ **升级排序算法**：从localeCompare升级为pinyin-pro
- ✅ **添加可选getter**：为未来优化留下空间（可选功能）
- ✅ **最小化风险**：只改2-3行代码，不破坏现有逻辑

**📋 必须保留的现有字段**（⚠️ 不要重复声明）：
- ✅ `list: Expert[]` - 专家列表
- ✅ `keyword: string` - 搜索关键词（**已存在**）
- ✅ `specialty: string` - 科室筛选（**已存在**）
- ✅ 所有其他现有字段和方法

---

**步骤5.1：在文件顶部添加import**

```typescript
// 在文件顶部添加（在现有import之后）
import { sortByPinyin, searchByPinyin, groupByFirstLetter, getSortedLetters } from '@/utils/pinyin';
```

---

**步骤5.2：修改 fetchExperts() - 只升级排序算法**

**🔍 找到现有的排序代码**（约在第140行）：
```typescript
// 按姓名排序
const sorted = filtered.slice().sort((a, b) => 
  a.name.localeCompare(b.name, 'zh')
);
```

**🔄 替换为拼音排序**：
```typescript
// 按姓名拼音排序（升级为pinyin-pro）
const sorted = sortByPinyin(filtered, (e) => e.name);
```

**⚠️ 关键点**：
- ✅ **只改这1行代码**
- ✅ **其他逻辑完全不动**
- ✅ **保留现有的筛选、映射、缓存等所有逻辑**

---

**步骤5.3：添加可选getter（未来优化用，可选）**

**在getters部分添加**（如果Store中没有getters，则创建）：

```typescript
getters: {
  /**
   * 按首字母分组（用于字母索引显示）
   * 可选功能：页面可以使用此getter实现字母分组展示
   */
  groupedExperts(): Record<string, Expert[]> {
    return groupByFirstLetter(this.list, (e) => e.name);
  },
  
  /**
   * 字母列表（用于字母索引）
   * 可选功能：页面可以使用此getter获取字母列表
   */
  letterList(): string[] {
    return getSortedLetters(this.groupedExperts);
  }
}
```

**⚠️ 说明**：
- 这些getter是**可选的**，主要用于字母索引显示
- 页面可以选择使用或不使用
- **不影响现有功能**

---

**步骤5.4：添加清空缓存方法（可选）**

**在actions部分添加**：

```typescript
/**
 * 清空缓存（用于下拉刷新）
 */
clearCache() {
  this.list = [];
  this.keyword = '';
  this.specialty = '';
}
```

---

**✅ 完整的修改清单**：
1. ✅ 添加import：1行
2. ✅ 修改排序算法：1行
3. ✅ 添加getter：2个（可选）
4. ✅ 添加clearCache：1个方法（可选）

**总计**：核心修改仅**2行代码**，风险极低

---

**⚠️ 重要提示**：
- **不要删除任何现有代码**
- **不要修改fetchExperts的参数和调用方式**
- **保留所有现有的actions方法**（fetchExpertById、fetchSessions等）
- **保留所有现有的状态字段**

---

### 步骤6：创建品牌Store（渐进式方案 - 可选）

**📁 文件：`src/store/brand.ts`**

**操作类型**：✨ **新建**（独立创建，不影响现有页面）

**⚠️ 渐进式策略**：
- **本次实施**：创建Store但不迁移页面
- **未来优化**：验证通过后再考虑迁移页面逻辑

**🎯 核心说明**：

品牌页面当前使用组件内状态管理，功能良好。创建Store是为了：
1. ✅ 统一状态管理模式（与专家页面一致）
2. ✅ 为未来优化做准备
3. ✅ 提供拼音搜索能力

**但不强制立即迁移**，页面可以继续使用现有逻辑。

---

**关键设计**：
- ❌ **不添加**拼音排序（保持按 `sort_order` 排序）
- ❌ **不添加**字母分组和索引
- ✅ **仅添加**拼音搜索支持
- ✅ **保持**现有排序逻辑

**完整代码**：

```typescript
/**
 * 品牌Store
 * @description 管理品牌列表、搜索（可选使用）
 */

import { defineStore } from 'pinia';
import { getBrands } from '@/api/brand';
import { searchByPinyin } from '@/utils/pinyin';

export interface Brand {
  id: string;
  name: string;
  logo?: string;
  description?: string;
  sort_order?: number;
}

export const useBrandStore = defineStore('brand', {
  state: () => ({
    /** 品牌列表（原始数据） */
    list: [] as Brand[],
    
    /** 搜索关键词 */
    keyword: '',
    
    /** 加载状态 */
    loading: false,
    
    /** 错误信息 */
    error: null as null | { message: string }
  }),
  
  getters: {
    /**
     * 过滤后的品牌列表
     * 仅搜索过滤，保持原有排序
     */
    filteredBrands(): Brand[] {
      let result = this.list;
      
      // 关键词搜索（支持汉字、拼音）
      if (this.keyword.trim()) {
        result = searchByPinyin(result, this.keyword, (b) => b.name);
      }
      
      // 按 sort_order 排序（保持原有业务逻辑）
      return result.slice().sort((a, b) => 
        (a.sort_order ?? 0) - (b.sort_order ?? 0) || 
        a.name.localeCompare(b.name, 'zh-Hans-CN')
      );
    }
  },
  
  actions: {
    /**
     * 获取品牌列表
     */
    async fetchBrands() {
      this.loading = true;
      this.error = null;
      
      try {
        const res = await getBrands({ page: 1, size: 500 } as any);
        
        if (res.code === 200 && res.data) {
          // 兼容分页和非分页响应
          this.list = Array.isArray(res.data) ? res.data : (res.data.items || []);
          console.log('[BrandStore] 品牌列表加载成功:', this.list.length);
        } else {
          throw new Error(res.message || '获取品牌失败');
        }
      } catch (e: any) {
        this.error = { message: e?.message || '加载品牌列表失败' };
        console.error('[BrandStore] 加载失败:', e);
        this.list = [];
      } finally {
        this.loading = false;
      }
    },
    
    /**
     * 清空关键词
     */
    clearKeyword() {
      this.keyword = '';
    }
  }
});
```

**可选：注册Store**（如果需要在其他地方使用）：

```typescript
// src/store/index.ts
export { useBrandStore } from './brand';
```

---

**⚠️ 重要说明**：
1. **Store已创建，但品牌页面可以继续使用现有逻辑**
2. **不强制要求立即迁移页面**（避免破坏现有功能）
3. **本次实施重点在专家页面**，品牌只是准备好Store
4. **未来如需优化品牌页面**，可以参考专家页面的实现

---

### 步骤7：优化专家页面（最小改动方案）

**📁 文件：`src/pages/app/tabbar/expert/index.vue`**

**操作类型**：🔧 **最小优化**（保持现有架构，无需大改）

**⚠️ 核心策略**：
- ✅ **保留现有调用方式**：继续调用 `fetchExperts()`
- ✅ **保留现有逻辑**：handleSearch, handleFilter 逻辑不变
- ✅ **只需验证排序**：确认列表已按拼音排序（Store已实现）
- ✅ **可选使用getter**：如需实时搜索，可用groupedExperts等

---

**实际需要修改的地方（非常少）**：

**修改1：使用字母分组工具函数（在模板逻辑中）**

如果您想在页面中显示字母分组头，需要：

```typescript
// 1. 导入工具函数
import { getFirstLetter } from '@/utils/pinyin';

// 2. 使用工具函数获取首字母（替换现有的简单逻辑）
function firstLetter(name?: string): string {
  return getFirstLetter(name || '');
}
```

**修改2（可选）：使用Store的getter实现字母索引**

如果您想使用LetterIndex组件的字母列表，可以：

```typescript
// 可选：使用Store的getter获取字母列表
const letterList = computed(() => expertStore.letterList || []);
```

**但这是可选的**，因为：
- 现有页面可能已有自己的字母列表生成逻辑
- Store的getter是为了提供便利，不是强制要求

---

**⚠️ 重要说明**：

1. **Store已经在fetchExperts中使用拼音排序**
2. **页面无需修改调用方式**：
   ```typescript
   // 保持现有代码不变
   function handleSearch(keyword: string) {
     expertStore.keyword = keyword;
     loadData(true); // 继续调用loadData
   }
   
   async function loadData(refresh = false) {
     const nextPage = refresh ? 1 : (pagination.value.page || 1);
     await expertStore.fetchExperts({ page: nextPage, pageSize: pagination.value.pageSize });
     dataLoaded.value = true;
   }
   ```
3. **排序已自动生效**：因为Store中已使用sortByPinyin
4. **下拉刷新继续使用**：
   ```typescript
   onPullDownRefresh(async () => {
     await loadData(true);
     uni.stopPullDownRefresh();
   });
   ```

---

**核心修改清单**（总共约3-5行代码）：

| 修改项 | 位置 | 说明 | 必须性 |
|-------|------|------|--------|
| 导入getFirstLetter | 文件顶部 | `import { getFirstLetter } from '@/utils/pinyin'` | ✅ 必须 |
| 修改firstLetter函数 | 方法区 | 使用getFirstLetter替换简单的charAt | ✅ 必须 |
| 使用letterList getter | script区 | `const letterList = computed(() => expertStore.letterList)` | 🟡 可选 |

**不需要修改的部分**：
- ❌ 不修改handleSearch逻辑
- ❌ 不修改handleFilter逻辑
- ❌ 不修改loadData逻辑
- ❌ 不修改下拉刷新逻辑
- ❌ 不修改列表渲染逻辑

---

### 步骤8：品牌页面优化（可选 - 未来实施）

**📁 文件：`src/pages/app/tabbar/brand/index.vue`**

**操作类型**：🔧 **可选优化**（未来实施，本次跳过）

**⚠️ 重要说明**：
- **本次不实施品牌页面迁移**
- **品牌Store已创建**，但页面继续使用现有逻辑
- **未来优化时**，可以参考专家页面的实现
- **当前品牌页面功能良好**，无需急于重构

---

**未来实施时的参考方案**（当前跳过）：

如果未来需要迁移品牌页面到Store模式，可以：

1. **引入Store**：
```typescript
import { useBrandStore } from '@/store/brand';
const brandStore = useBrandStore();
```

2. **替换loadBrands**：
```typescript
// 替换现有的loadBrands()函数
async function loadBrands() {
  await brandStore.fetchBrands();
}
```

3. **添加搜索功能**（可选）：
```typescript
function onSearch(kw: string) {
  brandStore.keyword = kw;
}
```

**但现在不需要做这些修改**，保持品牌页面现有实现即可。

---

**本次实施重点**：
- ✅ 专家页面：拼音排序 + 字母索引（已实施）
- ✅ 工具函数：pinyin.ts（已实施）
- 🟡 品牌页面：目前跳过，未来优化

---

## 第4章：测试与验收清单

### 4.1 功能测试清单

#### 4.1.1 专家页面测试

| 测试项 | 测试用例 | 预期结果 |
|-------|---------|---------|
| 拼音排序 | 打开专家页面 | 专家按拼音A-Z排序 |
| 字母分组 | 滚动列表 | 每个字母有分组头，吸顶显示 |
| 字母索引 | 点击字母"L" | 列表定位到L组，显示悬浮提示 |
| 汉字搜索 | 输入"张" | 过滤出所有张姓专家 |
| 拼音搜索 | 输入"zhang" | 过滤出所有张姓专家 |
| 首字母搜索 | 输入"zs" | 过滤出"张三"等专家 |
| 清空搜索 | 清空搜索框 | 显示完整列表，恢复字母分组 |
| 科室筛选 | 选择"心内科" | 显示心内科专家，保持拼音排序 |
| 下拉刷新 | 下拉页面 | 清空缓存，重新加载数据 |
| 悬浮提示 | 点击字母索引 | 显示大号字母，1秒后自动隐藏 |

#### 4.1.2 品牌页面测试

| 测试项 | 测试用例 | 预期结果 |
|-------|---------|---------|
| 实时搜索 | 输入品牌名称 | 实时过滤匹配品牌 |
| 拼音搜索 | 输入品牌拼音 | 过滤出匹配品牌 |
| 清空搜索 | 清空搜索框 | 显示完整列表 |
| 网格布局 | 查看页面 | 保持3列网格布局 |
**⚠️ 性能标准说明**：以下为理想目标值，实际运行时需考虑网络环境、设备性能等因素。如无法达标，应启用降级策略。

| 性能指标 | 目标值 | 可接受范围 | 测试方法 |
|---------|--------|-----------|---------|
| 首屏加载时间 | < 3秒 | < 5秒（弱网环境） | Performance API |
| 拼音排序耗时 | < 50ms（1000条） | < 100ms（低端设备） | console.time |
| 性能指标 | 目标值（优秀） | 可接受范围（良好） | 测试方法 |
|---------|--------------|------------------|---------|
| 首屏加载时间 | < 1.5秒 | < 3秒 | Performance API |
| 拼音排序耗时 | < 30ms（1000条） | < 50ms | console.time |
| 搜索过滤耗时 | < 10ms（1000条） | < 20ms | console.time |
| 搜索总延迟 | < 450ms（400ms防抖+50ms处理） | < 600ms | 用户感知测试 |
| 列表滚动帧率 | ≥ 55 FPS | ≥ 50 FPS | uni.createSelectorQuery |
| 内存占用 | < 100MB（1000条） | < 200MB | Chrome DevTools |
| 缓存命中率 | > 80% | > 60% | 日志统计 |

**说明：**
- 搜索总延迟 = 防抖时间(400ms) + 处理时间(50ms)
- 低端设备标准：内存<2GB，CPU核心<4
- 弱网环境：3G网络或WiFi信号弱

### 4.3 兼容性测试

| 平台 | 版本要求 | 重点测试项 |
|------|---------|-----------|
| iOS | ≥ 11.0 | 字母索引、悬浮提示、滚动性能 |
| Android | ≥ 5.0 | 内存管理、搜索性能、低端设备 |
| 微信小程序 | ≥ 2.10.0 | 包大小、缓存管理、搜索防抖 |
| H5 | Chrome ≥ 80, Safari ≥ 13 | 响应式布局、字母索引 |

### 4.4 验收标准

**核心功能验收**：
- ✅ 专家页面支持拼音排序（A-Z + #）
- ✅ 专家页面支持字母索引（点击定位）
- ✅ 专家页面支持实时搜索（汉字、拼音、首字母）
- ✅ 品牌页面支持实时搜索（汉字、拼音）
- ✅ 品牌页面保持网格布局（无字母索引）

**性能验收**：
- ✅ 首屏加载 < 3秒
- ✅ 排序耗时 < 50ms
- ✅ 搜索响应 < 200ms
- ✅ 内存占用 < 200MB
- ✅ 列表滚动流畅 ≥ 55 FPS

**用户体验验收**：
- ✅ 无白屏，有加载状态
- ✅ 搜索过程流畅，无卡顿
- ✅ 字母索引定位准确
- ✅ 悬浮提示显示正确
- ✅ 下拉刷新正常

---

## 第5章：关键实现要点

### 5.1 多音字白名单机制

**问题**：医疗领域特殊姓氏读音（如"解文"应读xiéwén而非jiěwén）

**解决方案**：
1. 创建白名单配置文件：`src/config/pinyin-whitelist.json`
2. 在 `getPinyin()` 函数中优先匹配白名单
3. 支持动态加载白名单（无需发版更新）

**维护流程**：
- 用户反馈 → 人工审核 → 更新白名单 → 通过API下发配置

### 5.2 性能优化策略

#### 5.2.1 缓存策略
- **5分钟缓存**：减少网络请求，提升响应速度
- **下拉刷新清空缓存**：用户主动更新数据
- **页面卸载清理缓存**：释放内存资源

#### 5.2.2 搜索防抖
- **400ms防抖**（与现有代码保持一致）：减少计算频率，提升流畅度
- **医疗场景考虑**：医疗应用用户输入较慢，400ms比300ms更合理
- **使用项目自带工具**：`src/utils/debounce.ts`（无需lodash-es）

#### 5.2.3 内存优化
- **拼音结果缓存**：避免重复计算拼音，节省CPU
- **图片懒加载**：减少首屏图片加载
- **虚拟滚动**：数据量 > 1000时启用，减少DOM节点
- **及时释放**：页面卸载时清理缓存

### 5.3 移动端特有优化

#### 5.3.1 流量优化
```typescript
// 检测网络类型
const { networkType } = await uni.getNetworkType();

if (networkType !== 'wifi') {
  // 移动网络：显示流量提示
  uni.showModal({
    title: '流量提示',
    content: '当前使用移动网络，继续加载将消耗流量'
  });
}
```与降级策略

**降级策略决策树**：

```typescript
// #ifdef APP-PLUS
const deviceInfo = plus.device.getInfo();
const isLowEndDevice = deviceInfo.totalMemory < 2048; // 内存<2GB

if (isLowEndDevice) {
  // 低端设备降级策略
  const CACHE_DURATION = 3 * 60 * 1000; // 缩短为3分钟
  const DEBOUNCE_DELAY = 500; // 延长防抖至500ms
  const ENABLE_VIRTUAL_SCROLL = true; // 默认启用虚拟滚动
  const DISABLE_PINYIN_INITIAL_SEARCH = true; // 禁用拼音首字母搜索
  
  console.log('[性能优化] 检测到低端设备，已启用降级策略');
}
// #endif
```

**降级条件与策略**：

| 条件 | 降级策略 | 说明 |
|------|---------|------|
| 内存<2GB | 延长搜索防抖至500ms | 减少计算频率 |
| 内存<2GB | 禁用拼音首字母搜索 | 仅支持全拼和汉字 |
| 数据>500条 | 启用虚拟滚动 | 减少DOM节点 |
| 2G/3G网络 | 缩短缓存至3分钟 | 减少流量消耗 |
| 排序耗时>100ms | 降级为简单排序 | 使用localeCompare |

**用户提示策略**：
```typescript
if (isLowEndDevice) {
  uni.showToast({
    title: '已优化性能模式',
    icon: 'none',
    duration: 2000
  });
}用拼音首字母搜索
}
// #endif
```

### 5.4 安全与日志规范

#### 5.4.1 日志记录（遵循设计文档第3.5节）
```typescript
// ✅ 正确记录
logger.info('专家列表加载', {
  dataCount: allExperts.length,
  memoryUsage: `${memoryKB}KB`,
  cacheHit: isCacheHit,
  level: 'INFO'
});

// ❌ 禁止记录
logger.info('用户搜索', {
  keyword: keyword,  // 禁止记录具体搜索内容
  expertName: expertName  // 禁止记录专家姓名原文
});
```

#### 5.4.2 敏感信息脱敏（遵循设计文档第4.2节）
```typescript
// ✅ 专家姓名脱敏
const maskName = (name: string) => {
  return name.charAt(0) + '**';  // "张三" → "张**"
};
```

---

## 第6章：常见问题与解决方案（FAQ）

### Q1：为什么不在后端实现拼音排序？

**A**：当前数据量（专家<1000，品牌<500）适合前端实现，优势：
- ✅ 实时响应，无网络延迟
- ✅ 无需改后端，快速上线
- ✅ 支持离线搜索

### Q2：多音字误差如何解决？

**A**：通过白名单机制兜底：
- 配置文件：`src/config/pinyin-whitelist.json`
- 动态加载：支持远程配置更新
- 用户反馈：收集错误案例更新白名单
- 目标误差率：< 3%

### Q3：数据量增长后如何优化？

**A**：分阶段优化：
- **< 2000条**：虚拟滚动 + Web Worker
- **2000-5000条**：后端预分组
- **> 5000条**：迁移后端排序

### Q4：品牌页面为什么不实现字母排序？

**A**：业务需求明确：
- 品牌按合作权重排序更合理
- 品牌数量少（<500），无需字母索引
- 保持现有网格布局，减少开发成本

### Q5：搜索防抖为什么选择400ms？

**A**：基于项目现状和用户场景：
- **现有代码已使用400ms**：保持一致性，减少改动风险
- **医疗场景特点**：用户输入较慢，400ms体验更好
- **性能考虑**：400ms给予充足时间减少计算频率
- **用户感知**：400-600ms内的延迟用户无明显感知

### Q6：如何监控和优化性能？

**A**：通过日志记录关键指标：
- 排序耗时（目标 < 50ms）
- 搜索耗时（目标 < 20ms）
- 缓存命中率（目标 > 80%）
- 内存占用（目标 < 200MB）
- 用户反馈的多音字错误（目标 < 3%）

---

## 第7章：部署与维护

### 7.1 部署前检查清单

- [ ] 依赖安装完成（`pinyin-pro`, `lodash-es`）
- [ ] 所有文件生成完成（8个文件）
- [ ] 功能测试通过（专家10项、品牌7项）
- [ ] 性能测试达标（7项指标）
- [ ] 兼容性测试通过（4大平台）
- [ ] 日志记录符合规范（脱敏处理）
- [ ] 安全审查通过（无敏感信息泄露）

### 7.2 监控指标

**每日监控**：
- 排序耗时趋势
- 搜索耗时趋势
- 缓存命中率
- 错误日志（多音字错误）

**每周维护**：
- 处理用户反馈的多音字错误
- 更新白名单配置
- 性能优化评估

---

## 第8章：Git分支策略与回滚方案

### 8.1 Git分支策略

**分支命名规范**：
```bash
feature/expert-brand-pinyin-search  # 功能分支名
```

**提交节点规划（分阶段提交，支持独立回滚）**：

```bash
# 节点1：依赖和工具函数（基础设施）
git add package.json pnpm-lock.yaml
git add src/utils/pinyin.ts src/config/pinyin-whitelist.json
git commit -m "feat(pinyin): 添加拼音工具函数和多音字白名单"

# 节点2：专家Store重构（核心逻辑）
git add src/store/expert.ts
git commit -m "refactor(expert): 重构Store为标准模式（action加载，getter筛选）"

# 节点3：专家页面优化（UI层）
git add src/pages/app/tabbar/expert/index.vue
git add src/components/expert/LetterIndex.vue  # 如有修改
git commit -m "feat(expert): 使用Store的getter实现实时搜索和拼音排序"

# 节点4：品牌Store创建（新模块）
git add src/store/brand.ts src/store/index.ts
git commit -m "feat(brand): 创建品牌Store"

# 节点5：品牌页面重构（迁移到Store）
git add src/pages/app/tabbar/brand/index.vue
git commit -m "refactor(brand): 迁移到Store模式并添加拼音搜索"

# 节点6：文档和测试
git add docs/
git commit -m "docs: 添加拼音搜索功能文档"
```

### 8.2 回滚清单

#### 场景1：发现严重Bug，需要完全回滚

```bash
# 方案A：revert所有相关commit（推荐，保留记录）
git log --oneline --grep="pinyin\|expert\|brand" | head -n 6  # 查看相关commit
git revert <commit5> <commit4> <commit3> <commit2> <commit1>  # 按倒序revert

# 方案B：reset到功能分支创建前（谨慎使用）
git reset --hard <功能分支创建前的commit>
git push origin <分支名> --force  # 强制推送
```

#### 场景2：仅回滚某个页面

```bash
# 只回滚专家页面，保留品牌页面
git checkout HEAD~2 -- src/pages/app/tabbar/expert/index.vue
git checkout HEAD~2 -- src/store/expert.ts
git commit -m "revert(expert): 回滚专家页面拼音搜索功能"

# 保留pinyin工具和品牌功能
```

#### 场景3：性能问题，降级为简化版

```typescript
// 手动编辑 src/utils/pinyin.ts
// 选项1：保留搜索，降级排序为 localeCompare

export function sortByPinyin<T>(items: T[], getNameFn: (item: T) => string): T[] {
  // 临时降级方案
  return items.slice().sort((a, b) => 
    getNameFn(a).localeCompare(getNameFn(b), 'zh-Hans-CN')
  );
}

// 提交降级版本
git add src/utils/pinyin.ts
git commit -m "perf(pinyin): 临时降级拼音排序为localeCompare以解决性能问题"
```

#### 场景4：pinyin-pro库兼容性问题

```bash
# 如果pinyin-pro在某些平台无法使用，回退到纯localeCompare方案

# 步骤1：卸载pinyin-pro
pnpm remove pinyin-pro

# 步骤2：修改 src/utils/pinyin.ts，提供降级实现
# 步骤3：提交降级版本
git commit -am "fix(pinyin): 降级为localeCompare以解决兼容性问题"
```

### 8.3 灰度发布策略

**阶段1：内部测试（1天）**
- 开发环境验证基础功能
- 性能测试（高中低端设备各一台）
- 多端测试（H5、小程序、App）

**阶段2：Beta测试（2-3天）**
- 发布到测试服务器
- 邀请10-20名内部用户测试
- 收集性能数据和用户反馈
- **监控指标**：错误率、搜索延迟、内存占用

**阶段3：灰度发布（5-7天）**
```typescript
// 灰度配置示例（后端控制）
const FEATURE_FLAGS = {
  enablePinyinSearch: {
    percentage: 10,  // 第1-2天：10%用户
    // percentage: 50,  // 第3-4天：50%用户
    // percentage: 100, // 第5天：100%用户
  }
};

// 前端检查
if (FEATURE_FLAGS.enablePinyinSearch.enabled) {
  // 使用新功能
} else {
  // 使用旧功能
}
```

**灰度发布时间表**：
| 阶段 | 用户比例 | 持续时间 | 观察指标 |
|------|---------|---------|---------|
| 第1天 | 10% | 24小时 | 错误率、崩溃率 |
| 第2-3天 | 30% | 48小时 | 搜索延迟、性能 |
| 第4-5天 | 50% | 48小时 | 用户反馈、满意度 |
| 第6天 | 80% | 24小时 | 全面验证 |
| 第7天 | 100% | - | 正式上线 |

**紧急回滚标准**：
- ❌ 错误率 > 5%
- ❌ 崩溃率 > 1%
- ❌ 搜索延迟 > 2秒（P95）
- ❌ 用户投诉 > 10条/天
- ❌ 内存占用增长 > 50MB

### 8.4 回滚后的数据清理

```typescript
/**
 * 回滚后清理函数
 * 在回滚代码后执行，清理缓存和状态
 */
export function cleanupAfterRollback() {
  // 清理Storage
  uni.removeStorageSync('expert_cache_timestamp');
  uni.removeStorageSync('brand_cache_timestamp');
  uni.removeStorageSync('pinyin_cache');
  
  // 清理Store状态（如果Store还在）
  try {
    const expertStore = useExpertStore();
    expertStore.$reset();
  } catch (e) {
    console.log('expert Store已不存在');
  }
  
  try {
    const brandStore = useBrandStore();
    brandStore.$reset();
  } catch (e) {
    console.log('brand Store已不存在');
  }
  
  // 清理拼音缓存（如果有）
  if (window.pinyinCache) {
    window.pinyinCache.clear();
  }
  
  console.log('[Rollback] 数据清理完成');
}

// 在用户下次进入页面时自动执行
onLaunch(() => {
  const rollbackFlag = uni.getStorageSync('feature_rollback');
  if (rollbackFlag === 'pinyin_search') {
    cleanupAfterRollback();
    uni.removeStorageSync('feature_rollback');
  }
});
```

### 8.5 回滚操作checklist

**决策阶段**：
- [ ] 确认问题严重性（是否需要立即回滚）
- [ ] 评估影响范围（全部回滚 vs 部分回滚）
- [ ] 通知相关Team（开发、测试、产品）

**执行回滚**：
- [ ] 备份当前代码（以备后续分析）
- [ ] 执行Git回滚操作
- [ ] 触发CI/CD重新部署
- [ ] 后端设置feature flag（如有）

**验证阶段**：
- [ ] 验证功能已回退到旧版本
- [ ] 执行数据清理脚本
- [ ] 测试核心功能是否正常
- [ ] 监控错误率是否恢复正常

**后续分析**：
- [ ] 分析问题根因
- [ ] 制定修复方案
- [ ] 更新测试用例
- [ ] 文档记录本次问题和教训

---

## 第9章：附录与参考

### 9.1 参考文档

**每日监控**：
- 排序耗时趋势
- 搜索耗时趋势
- 缓存命中率
- 错误日志（多音字错误）

**每周维护**：
- 处理用户反馈的多音字错误
- 更新白名单配置
- 性能优化评估

| V1.1 | 2026-03-07 | 优化版：修复组件路径冲突、Store字段冲突、强化前置检查、添加降级策略 | 前端团队 |

---

**文档结束**

**📌 V1.1版本重要更新**：
1. **修复组件路径冲突**：LetterIndex、ExpertSearch组件路径改为 `src/components/expert/` 而非 `src/components/business/`
2. **修复Store字段冲突**：使用现有的 `list` 字段而非 `allExperts`，检查 `keyword`/`specialty` 是否已存在
3. **修复数据结构冲突**：使用现有的 `Expert` 接口，不创建新的 `ExpertBrief`
4. **强化前置检查机制**：添加步骤A声明，要求AI明确承诺不跳过检查
5. **修正日志导入路径**：`@/utils/logger` 而非 `@/logs/logger`
6. **修正依赖安装说明**：`lodash-es` 已存在，只需安装 `pinyin-pro`
7. **添加降级策略**：低端设备适配、性能监控、迁移决策表
8. **增加可接受范围**：性能标准添加弱网和低端设备的可接受范围

**📌 重要提醒**：
1. 开始代码生成前，必须完成第0章的前置检查（包括步骤A声明）
2. 必须等待用户确认分析结果后才能继续
3. 严格遵循uni-app原生组件规范，禁止使用Element Plus
4. 遵循《直播SaaS平台移动端前端设计文档》的日志和脱敏规范
5. 所有性能指标必须达标，否则启用降级策略
6. **优先优化现有组件，避免创建重复文件** | 后端排序 |
| 多音字误差 > 10% | 白名单兜底 | 后端白名单管理 |
| 搜索耗时 > 100ms | 优化索引 | 后端搜索引擎 |

---

## 附录A：参考文档

- [专家品牌实时搜索和拼音排序设计文档](./专家品牌实时搜索和拼音排序设计文档.md)
- [直播SaaS平台移动端前端设计文档](./直播SaaS平台移动端前端设计文档.md)
- [pinyin-pro 官方文档](https://github.com/zh-lx/pinyin-pro)
- [uni-app 官方文档](https://uniapp.dcloud.net.cn/)
- [Pinia 官方文档](https://pinia.vuejs.org/zh/)

---

## 附录B：变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| V1.0 | 2026-03-07 | 初始版本，完成核心功能提示词 | 前端团队 |
| V1.1 | 2026-03-07 | 优化版：修复组件路径冲突、Store字段冲突、强化前置检查、添加降级策略 | 前端团队 |
| **V2.0** | **2026-03-08** | **【重大更新】全面修正架构问题，添加完整回滚方案** | **前端团队** |

### V2.0 重大更新内容

**🎯 核心策略变更**：
1. **统一使用 pinyin-pro**：排序和搜索统一使用 pinyin-pro，不再混合使用 localeCompare
   - **原因**：保证搜索结果和排序结果一致，避免用户困惑
   - **性能影响**：增加 30ms（用户无感知）
   - **用户体验**：大幅提升

**🔧 修正的9个核心问题**：

1. **日志导入路径错误**（P0）
   - 修正：`@/logs/logger` → `@/utils/logger`
   - 影响：所有日志调用

2. **品牌Store不存在**（P0）
   - 修正：标记为「新建」而非「优化」
   - 添加：完整的品牌Store创建代码
   - 添加：品牌页面迁移到Store的详细步骤

3. **字段重复创建风险**（P0）
   - 修正：强化前置检查机制
   - 添加：自动检测现有字段的脚本
   - 说明：只添加 `cacheTimestamp`，不重复创建 `keyword`/`specialty`

4. **类型接口冲突**（P0）
   - 修正：删除所有 `ExpertBrief` 接口
   - 统一：使用现有的 `Expert` 接口
   - 原因：避免类型不兼容

5. **防抖时间不一致**（P1）
   - 修正：统一为 400ms（与现有代码一致）
   - 使用：项目自带 `src/utils/debounce.ts`
   - 删除：lodash-es 依赖说明

6. **API调用逻辑差异**（P0）
   - 修正：重构为标准 Store 模式
   - 变更：action 只加载原始数据，getter 筛选排序
   - 删除：fetchExperts() 中的筛选逻辑
   - 新增：filteredExperts、groupedExperts、letterList 三个getter

7. **品牌页面架构不匹配**（P0）
   - 新增：品牌页面迁移到Store的详细步骤
   - 说明：如何处理现有的组件状态

8. **性能指标过于乐观**（P1）
   - 新增：分级性能标准（优秀 vs 良好）
   - 修正：搜索延迟改为 450ms（400ms防抖 + 50ms处理）
   - 添加：低端设备和弱网环境的可接受标准

9. **缺少回滚方案**（P1）
   - 新增：完整的第8章「Git分支策略与回滚方案」
   - 添加：4种回滚场景的详细操作步骤
   - 添加：灰度发布策略和时间表
   - 添加：回滚后数据清理脚本

**✨ 新增内容**：

1. **第8章：Git分支策略与回滚方案**
   - 分阶段提交策略（6个独立commit）
   - 4种回滚场景详细说明
   - 灰度发布策略（7天计划）
   - 紧急回滚标准
   - 回滚后数据清理脚本

2. **步骤5：完全重写专家Store重构方案**
   - 明确action和getter的职责
   - 详细的重构步骤（5.1-5.3）
   - 保留现有字段的完整清单
   - 完整的代码示例

3. **步骤6：创建品牌Store（标记为新建）**
   - 完整的品牌Store代码
   - 与专家Store的差异说明
   - 明确不添加拼音排序

4. **步骤7-8：页面重构方案**
   - 详细的变更步骤（变更1-5）
   - 删除/保留的内容清单
   - 完整的代码示例

**📚 优化内容**：

1. **依赖安装说明**
   - 明确只需安装 `pinyin-pro`
   - 删除 lodash-es 相关说明（已存在）
   - 使用项目自带 debounce 工具

2. **性能标准**
   - 添加分级标准（优秀/良好）
   - 修正搜索延迟为 450ms
   - 添加测试方法说明
   - 删除重复的性能指标表格

3. **内存优化说明**
   - 删除 ExpertBrief 相关说明
   - 添加拼音缓存策略
   - 明确虚拟滚动触发条件

4. **FAQ更新**
   - Q5：防抖时间改为 400ms 的原因
   - 添加医疗场景特点说明

**🎨 文档结构优化**：
- 删除重复的代码片段
- 统一术语使用（action加载，getter筛选）
- 添加更多警告和说明
- 完善代码注释

---

**⚠️ 升级提示**：

从 V1.1 升级到 V2.0 的用户需注意：
1. **排序策略变更**：统一使用 pinyin-pro，不再使用 localeCompare
2. **Store架构变更**：action和getter职责重新分配
3. **品牌Store创建**：不是优化，是新建文件
4. **防抖时间确认**：确认现有代码使用的是400ms还是其他值

---

**文档结束**

**📌 V2.0 版本重要提醒**：

1. **开始代码生成前，必须完成第0章的前置检查（包括步骤A声明）**
2. **必须等待用户确认分析结果后才能继续**
3. **严格遵循uni-app原生组件规范，禁止使用Element Plus**
4. **遵循《直播SaaS平台移动端前端设计文档》的日志和脱敏规范**
5. **统一使用 pinyin-pro 进行排序和搜索**（不再混合使用localeCompare）
6. **防抖时间统一为 400ms**（使用项目自带 `src/utils/debounce.ts`）
7. **品牌Store需要创建**（不是优化现有文件）
8. **专家Store需要重构**（action加载，getter筛选）
9. **性能指标必须达标**，否则启用降级策略
10. **遵循Git分支策略，支持独立回滚各个模块**

**🎯 V2.0 核心变更**：
- ✅ 统一使用 pinyin-pro（排序+搜索）
- ✅ 重构Store为标准模式（action加载，getter筛选）
- ✅ 创建品牌Store（新建文件）
- ✅ 添加完整回滚方案（第8章）
- ✅ 修正所有检测到的问题
