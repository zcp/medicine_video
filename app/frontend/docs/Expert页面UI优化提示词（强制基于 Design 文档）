# Expert 页面 UI 优化提示词（强制基于 Design 文档）

本文档与 **当前实现代码** 保持一致；所有数值与行为以代码为准。

---

## 一、角色与最高优先级

你现在是本项目的 **Design System Architect（UI-UX-PRO-MAX-SKILLS）**。

========================================================
【最高优先级：必须优先遵守 Design 文档（不可违背）】
========================================================

必须优先阅读并严格遵守以下两个文档：

1) Design-System-Rules_UI-UX-PRO-MAX.md  
2) 《登录注册找回-统一设计语言与自适配方案.md》  
   - 路径（本仓库）：docs/登录注册找回-统一设计语言与自适配方案.md

该基准文档是本项目唯一权威的 Design Language 与响应式策略来源（Highest Priority）。

强制要求：
- 在进行任何页面重构、tokens/components/layout 抽象前，必须先阅读并理解该文档。
- 所有 Design Tokens / Components / Layout Patterns 必须基于文档总结提炼，不允许凭空创造。
- 若本提示词与基准文档冲突，以基准文档为最高优先级。

---

## 二、任务与原则

========================================================
【任务：优化 Expert 页面（专家专题页）】
========================================================

- **目标页面**：pages/Expert（专家专题页）
- 当前页面已完成品牌色统一；部分优化已实施（见下文「已实施优化与实现对照」），后续迭代须保持与本文档及代码一致。

必须做到：
- 不修改业务逻辑
- 不修改接口/数据结构
- 不修改功能流程
- 仅优化 UI/UX 结构表现与视觉一致性
- 所有样式必须来自 tokens + components + layout pattern
- 禁止硬编码颜色、圆角、阴影、字体、间距

---

## 三、问题与优化目标（当前版本强制要求）

========================================================
【问题】
========================================================

- 当前列表右侧 **Follow CTA（关注按钮）** 与右侧 **AlphabetIndex 字母索引** 发生视觉冲突。
- Follow 按钮视觉权重过强，破坏 ConsumerLayout 的轻列表节奏。

========================================================
【优化目标（必须执行）】
========================================================

1) 将 Follow CTA 从大按钮降级为 **icon-only 操作（IconButton）**  
2) 使用 **Star 图标** 表示「关注/收藏」（不使用 heart，避免社交化）  
3) IconButton 必须位于 **AlphabetIndex 左侧**，禁止重叠  
4) 必须保证 **44×44px 点击热区**  
5) 必须使用 **tokens** 推衍颜色/圆角/动效，禁止硬编码  
6) **未关注**：Neutral icon；**已关注**：品牌主色 icon（Color.Primary）  
7) **点击列表行** 仍进入详情；**点击 Star** 仅切换关注状态，不跳转  
8) 必须补齐 **hover / active / focus-visible / disabled** 状态，动效 **180–220ms**  
9) **AlphabetIndex** 默认弱化（Neutral + 低对比），仅交互时增强  

---

## 四、Layout Pattern 与声明格式

========================================================
【Layout Pattern 选择（必须明确）】
========================================================

该页面属于「内容型列表页」，必须使用：**ConsumerLayout**

核心目标：
- 结构轻
- 留白高级
- 列表节奏清晰
- 不要卡片堆叠感

========================================================
【强制执行：页面改动前必须先输出声明】
========================================================

在输出任何页面代码前，必须先输出：

- **【Design Tokens Used】**  
  - 逐条列出使用的 tokens（Color / Radius / Shadow / Spacing / Typography / Motion）

- **【Components Used】**  
  - 列出复用的组件（如 AppContainer / PageTitle / ListItem / Avatar / ExpertCard / LetterIndex / FilterTabs / ExpertSearch / CustomTabBar 等）

- **【Layout Pattern】**  
  - ConsumerLayout

- **【UI-UX-PRO-MAX 优化点】**  
  - 必须列出将优化的点（且不得改变视觉基因）

只有完成以上声明后，才能输出代码。

---

## 五、必须解决的 UI/UX 问题（针对当前页面）

当前 Expert 页面仍存在或已部分解决的问题，需严格按 Design 文档原则保持或优化：

1) **顶部 Search + Tabs 区域的层级**
   - 搜索框和 Tabs 之间分隔不宜过硬
   - Tabs 选中态不宜过强，未选中态对齐一致
   - Tabs 需为「轻导航」而非组件堆叠

2) **列表项 Follow 与右侧索引关系**（✅ 已实施）
   - 已降级为 Star IconButton，位于 AlphabetIndex 左侧，不重叠
   - 详见「已实施优化与实现对照」

3) **右侧 A-Z 索引栏**（✅ 已实施）
   - 已弱化为辅助导航：默认 opacity 0.45 + Neutral-500，仅激活/交互时主色
   - 详见「已实施优化与实现对照」

4) **列表整体节奏**
   - Divider 极浅（5%~10% Neutral）
   - 行间距用 spacing 建立「呼吸感」，去油腻

5) **视觉层级统一**
   - 头像、姓名、科室、统计信息使用 Typography tokens 与 Spacing scale 建立层级

---

## 六、必须执行的高级感规则（来自 Design 文档）

- **去油腻**：优先 Spacing 分组；Divider 若必须存在则极浅（5%~10% Neutral）；禁止粗分割线、厚边框。  
- **去卡片化**：专家列表为通栏列表；禁止每项独立强卡片（阴影/边框/独立圆角）。  
- **Follow 为 IconButton（Star）**：未关注 Neutral icon；已关注品牌主色；hover/active/focus-visible/disabled 齐全；动效 180–220ms；禁止默认蓝色。  
- **色彩清洗**：禁止亮蓝主交互色；active/selected/CTA 使用 tokens 品牌主色；Neutral 来自 tokens。  
- **点击热区**：tab、list item、Star、字母索引均 ≥44px。  
- **避免 layout shift**：列表加载、tab 切换、关注状态变化不得造成布局跳动。

---

## 七、必须使用或复用的组件（与实现一致）

- **ExpertListItem（ExpertCard）**
  - 左 Avatar；中 Content（**name-row** 含姓名+职称+**Star**，科室，stats 弱层级）
  - **Action Column**：ListItem `padding-right: var(--expert-action-col-width)`；Star 位于 **name-row 内**与姓名同行，**flex align-items: center** 对齐姓名行视觉中心（禁止固定 top px）；热区 `min-width/min-height: var(--expert-icon-tap-size)`（≥44px），图标 20px；未关注 Neutral-400、已关注主色；点击行进详情、点击 Star 仅切换关注

- **ExpertTabs（FilterTabs）**
  - 选中态品牌主色，未选中态 Neutral，无突兀边框；轻导航风格

- **AlphabetIndex（LetterIndex）**
  - 默认 **opacity: 0.45**，**color: var(--home-text2)**（Neutral-500）；**active 时主色 + opacity: 1**
  - 定位 **right: var(--expert-index-inset)**（Index Safe Area 贴边），与 Star 永不重叠

- **SearchBar（ExpertSearch）**
  - 圆角、focus ring、padding/height 来自 tokens

---

## 八、已实施优化与实现对照（与代码一致）

以下与 `src/pages/app/tabbar/expert/index.vue`、`ExpertCard.vue`、`LetterIndex.vue` 保持一致。

### 8.1 布局：Action Column 与 Index Safe Area（tokens 驱动）

| 区域           | 规范                    | Token / 实现位置 |
|----------------|-------------------------|------------------|
| ListItem 右侧栏位 | Action Column，由每项自己预留 | `.expert-list-item` 的 `padding-right: var(--expert-action-col-width)`（64px）；`.list` 不再设 padding-right |
| Star 位置与对齐 | 属内容区，在 name-row 内与姓名同行 | `.name-row` 内 `.name-block` + `.follow-icon-wrap`；`align-items: center` 对齐姓名行视觉中心；`.follow-icon-wrap` 的 `margin-right: var(--expert-action-col-inset)`（8px） |
| 字母索引       | 贴右、弱化（Index Safe Area） | `right: var(--expert-index-inset)`（8px），LetterIndex.vue 中 `.letter-index` |

**Token 定义**（`src/common/uni.scss`，集中管理）：  
- `--expert-action-col-width`: 64px（Action Column 宽度，ListItem padding-right）  
- `--expert-action-col-inset`: 8px（Star 距 ListItem 右缘的 inset，形成与 Index 的 gap）  
- `--expert-index-inset`: 8px（索引距视口右侧，6~10px）  
- `--expert-icon-tap-size`: 88rpx（Star 触控热区 ≥44px）  
- 兼容旧引用：`--home-expert-index-inset`、`--home-expert-list-padding-right` 仍存在，其他页面不受影响  

### 8.2 ExpertCard（Star 在 name-row 内 + Action Column）

- **模板**：行根 `@tap="$emit('click')"` 进详情。`.content` 内首行为 `.name-row`（flex），内含 `.name-block`（name+title）与 `.follow-icon-wrap`（Star）；`.follow-icon-wrap` 上 `@tap.stop="onToggle"`，内为 `<text class="follow-star">★</text>`，`:class="{ active: isFollowing, disabled: pending }"`。
- **样式**：
  - ListItem：`position: relative`；`padding-right: var(--expert-action-col-width)`。
  - name-row：`display: flex`；`align-items: center`；`margin-right: calc(-1 * var(--expert-action-col-width))` 伸入 Action Column。
  - name-block：`flex: 1`；`padding-right: var(--expert-action-col-width)` 防止文字压住 Star。
  - 热区：`min-width / min-height: var(--expert-icon-tap-size)`（≥44px）
  - 图标尺寸：20px；默认 color: var(--home-text2)，opacity: 0.65（Neutral-400）；已关注 color: var(--home-primary)，opacity: 1；Hover 主色。
  - 与姓名行对齐：Star 在 name-row 内，**无固定 top px**，靠 flex `align-items: center` 对齐姓名行视觉中心。
  - 圆角：--home-r-md；动效：0.2s ease

### 8.3 LetterIndex

- 定位：position: fixed; **right: var(--expert-index-inset)**
- 默认：color: var(--home-text2)，opacity: 0.45
- .letter.active / .letter:active：color: var(--home-primary)，opacity: 1；其余字母保持 0.45

### 8.4 Design Tokens 使用（专家页）

- Color：--home-primary、--home-primary-hover、--home-text1、--home-text2、--home-bg、--home-border  
- Radius：--home-r-md、--home-r-pill  
- Spacing：--home-spacing-page、--home-spacing-module、--home-spacing-inner  
- **Action Column（专家列表）**：--expert-action-col-width、--expert-action-col-inset、--expert-index-inset、--expert-icon-tap-size  
- Typography：--home-fs-section、--home-fs-meta  
- Motion：0.2s ease（180–220ms）

---

## 八附、强制工作流程与 Action Column 规则（改造时必遵）

### Step 1：现状分析（结构摘要）

- **DOM/组件结构**：`expert-page` > `scroll-view.list`（无 padding-right）> 每项 `ExpertCard`（`.expert-list-item`：`.left` 头像 + `.content`）；`.content` 内首行为 `.name-row`（含 `.name-block` 姓名+职称 与 `.follow-icon-wrap` Star），其次 dept、stats；页面级 `LetterIndex`（`.letter-index` > `.letters` > `.letter`）。
- **关键 CSS**：ListItem `position: relative`、`padding-right: var(--expert-action-col-width)` 形成 Action Column；name-row `margin-right: calc(-1 * var(--expert-action-col-width))` 伸入栏位，name-block `padding-right` 防压字；Star 热区 `var(--expert-icon-tap-size)`，`margin-right: var(--expert-action-col-inset)`；LetterIndex `right: var(--expert-index-inset)`。
- **冲突原因**：若 Action Column 未预留或 Star 与 Index 共用同一竖向通道，会产生重叠；通过「ListItem 自留 Action Column + Index 贴边 token」保证永不重叠。

### Step 2：Action Column 与安全区规则（落地方式）

- **AlphabetIndex 贴右**：`right: var(--expert-index-inset)`（6~10px），不写魔法数。
- **Star 属内容区、在 Action Column 内**：Star 放在 **name-row 内**与姓名同行，`align-items: center` 对齐姓名行视觉中心（禁止固定 top px）；ListItem `padding-right: var(--expert-action-col-width)`；Star 的 `margin-right: var(--expert-action-col-inset)` 与右缘留 gap。
- **列表**：`.list` 不再设 padding-right；由每项 ListItem 的 padding-right 承担 Action Column，保证 Star 与 Index 永不重叠。
- **z-index**：LetterIndex 保持适度 z-index（如 100），不提高列表项。

### Step 3：页面改动前声明（必须输出）

【Design Tokens Used】见 8.4，含 --expert-action-col-width、--expert-action-col-inset、--expert-index-inset、--expert-icon-tap-size。  
【Components Used】ExpertCard、LetterIndex、ExpertSearch、FilterTabs、CustomTabBar 等。  
【Layout Pattern】ConsumerLayout。  
【UI-UX-PRO-MAX 优化点】Action Column 体系、Star 与 name-row 对齐、与 Index 不重叠、全部用 tokens 无硬编码。

### Step 4–5：最小 diff 原则

- 仅改与布局/结构相关的 CSS 与模板：ListItem padding-right、name-row/name-block、follow-icon-wrap 热区与 margin-right、letter-index 的 right，均用上述 tokens；Star 与姓名行对齐仅允许 name-row 内 flex 或 transform 锚定，禁止固定 top px。
- 不改业务逻辑、不增新视觉风格；新增 token 仅在 uni.scss 中增加并保持向后兼容。

### Step 6：自检清单

- [ ] Star 与 name-row 视觉中心对齐（无固定 top px）  
- [ ] Star 与 AlphabetIndex 在所有断点下完全不重叠  
- [ ] Star、字母项触控热区 ≥ 44×44px（热区用 --expert-icon-tap-size）  
- [ ] 未引入 token 之外的硬编码数值（Action Column / Index 相关）  
- [ ] 未改动其他页面使用的 token 语义，保持向后兼容

---

## 九、输出要求与验收目标

**输出必须包含**：  
1) 对 tokens / components / layout pattern 的声明（按规则格式）  
2) 本次 Expert 页面变更说明（若涉及结构）  
3) 关键代码修改（layout、list item、tabs、index 等）  
4) 若新增 tokens 或 variants，须说明原因并保持向后兼容  
5) 自检 checklist  

**最终验收**：  
- 与登录页同一 Design Language；更轻、更高级、更克制  
- 列表通栏化、去卡片化、去油腻  
- Follow 为 Star IconButton，融入列表结构；Tabs 为轻导航；A-Z 索引弱化且可用  
- 全局一致、可扩展、不影响其他页面  

---

## 十、精简版提示词（修改后，可直接复制）

以下为可直接用于生成或迭代的「修改后」提示词，与上文无冲突，且与实现一致。

```text
你现在是本项目的 Design System Architect（UI-UX-PRO-MAX-SKILLS）。

必须优先遵守：
Design-System-Rules_UI-UX-PRO-MAX.md
以及《登录注册找回-统一设计语言与自适配方案.md》

目标页面：pages/Expert（专家专题页）
不改业务逻辑，仅优化 UI。

========================================================
【问题】
========================================================
当前列表右侧 Follow CTA（关注按钮）与右侧 AlphabetIndex 字母索引发生视觉冲突。
同时 Follow 按钮视觉权重过强，破坏 ConsumerLayout 的轻列表节奏。

========================================================
【优化目标（必须执行）】
========================================================
1) 将 Follow CTA 从大按钮降级为 icon-only 操作（IconButton）
2) 使用 Star 图标表示「关注/收藏」（不使用 heart，避免社交化）
3) IconButton 必须位于 AlphabetIndex 左侧，禁止重叠
4) 必须保证 44x44px 点击热区
5) 必须使用 tokens 推衍颜色/圆角/动效，禁止硬编码
6) 未关注：Neutral icon；已关注：品牌主色 icon（Color.Primary）
7) 点击列表行仍进入详情；点击 Star 仅切换关注状态，不跳转
8) 必须补齐 hover/active/focus-visible/disabled 状态，动效 180–220ms
9) AlphabetIndex 默认弱化（Neutral + 低对比），仅交互时增强

========================================================
【强制输出格式（输出代码前必须先写）】
========================================================
【Design Tokens Used】
【Components Used】
【Layout Pattern】ConsumerLayout
【UI-UX-PRO-MAX 优化点】

然后输出修改后的关键代码（不改业务逻辑）。
```

---

**文档与实现代码一致；后续改动请同步更新本文档。**
