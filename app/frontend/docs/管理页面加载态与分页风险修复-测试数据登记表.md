# 管理页面加载态与分页风险修复-测试数据登记表（F1 执行表）

> 依据《管理页面加载态与分页风险修复-实施规划文档.md》第六章"测试数据管理规范"执行。
> 规则：所有测试数据标题必须带 `[TEST]` 前缀；每创建一条立即登记；每阶段退出前完成清理并更新状态；删除前执行 6.5 三查（ID / 前缀 / 归属）。
>
> **重要说明**：本次修复以防御性逻辑为主，预期多数阶段**无需创建测试数据**；仅在实际创建数据时填写本表。若未创建任何数据，各阶段清理核查记录填 0 即可，但终检的基线比对（列表数量）仍必须执行。

## 一、环境与基线（阶段 0 记录）

| 项目 | 内容 |
|------|------|
| 联调环境地址 | `http://10.105.136.235:8080`（即 `.env.development.local` 已配置：`VITE_BASE_API_URL=http://10.105.136.235:8080/api/core`、`VITE_AUTH_API_URL=http://10.105.136.235:8080/api/users`、`VITE_USE_MOCK=false`） |
| 环境连通性（2026-08-31 实测） | 公开接口 200：`/api/core/featured-content`、`/api/core/rooms`、`/api/core/experts`、`/api/core/content/categories`；管理端接口 `/admin/rooms`、`/api/users/admin/users`、`/admin/experts`、`/admin/expert-departments` 无凭证均返回 401（可达、鉴权正确，需登录走查） |
| 不可用环境 | `https://mp.dayilive.com` 本机实测 HTTP 000 不可达（与既有记录"非 Python 后端"一致，联调禁用）；`.env.development`（无 local 覆盖时）指向 mp.dayilive.com 且 `VITE_USE_MOCK=true`，**验证时必须依赖 `.env.development.local` 覆盖** |
| 基线时间 | 2026-08-31 08:11 (UTC+8)（curl 实测） |
| 基线：焦点图 | 公开 active = **3**（医疗直播1 / 医疗直播2 / 医疗直播3，2026-08-31 创建，含图片与跳转）；admin 全量总数**待登录走查** |
| 基线：用户列表（admin） | **待登录走查**（`/api/users/admin/users` 需 JWT，无凭证不可取数） |
| 基线：全站房间 | 公开 total = **10**（代表：TEST_回放 / TEST_直播中 / TEST_预告 / 医疗直播1 / 腔腔三人行｜普通外科手术分享系列）；admin 全量（含私密）**待登录走查** |
| 基线：专家 | 公开 total = **384**（代表：丁之*·副主任医师·中山大学附属第一医院·神经外科）；admin 全量**待登录走查** |
| 基线：科室（admin） | **待登录走查**（`/admin/expert-departments` 需 JWT）；公开分类（categories，非科室）实测 = **35**（内科/外科/妇产/儿科/眼科/耳鼻喉/口腔/皮肤/精神/生殖医学/中医/肿瘤/心内/康复/麻醉/消化/医学影像/内分泌/呼吸内科/肾内/血液/风湿免疫/普通外科/神经外科/骨科/泌尿外科/心胸外科/神经内科/血管外科/整形外科/变态反应科/老年病科/器官移植/烧伤与创面修复/其他） |
| 基线核对方式 | ① curl 复核公开接口数量（命令见下）；② admin 列表（用户/房间/专家/科室）在联调环境登录后走查记录数量；③ 快照代表条目标题 3-5 条/列表 |

> ⚠️ 基线内数据为既有数据，**不得删除、不得修改**；终检时基线与实施后数量比对，确认无既有数据被误删/误改。其中 `TEST_回放 / TEST_直播中 / TEST_预告` 三条房间为既往测试残留（2026-08-12 创建，非本实施创建），同样**不得删除**。

**基线复核命令**（公开接口，均返回 200）：

```
curl http://10.105.136.235:8080/api/core/featured-content
curl "http://10.105.136.235:8080/api/core/rooms?page=1&size=1"
curl "http://10.105.136.235:8080/api/core/experts?page=1&size=1"
curl http://10.105.136.235:8080/api/core/content/categories
```

## 二、测试数据登记

| 记录ID | 数据ID | 标题（含`[TEST]`前缀） | 关联资源（仅引用） | 创建时间 | 用途(实施项) | 清理状态 |
|--------|--------|----------------------|--------------------|----------|--------------|----------|
| （示例） | xxx-uuid | [TEST]E1分页验证-0831 | 不关联 | 2026-08-31 14:00 | E1 页码推进验证 | 已清理(2026-08-31 14:30) |
| | | | | | | |

## 三、阶段清理核查记录

| 阶段 | 清理日期 | 清理数量 | 三查确认（ID/前缀/归属） | 执行人 |
|------|----------|----------|--------------------------|--------|
| 阶段 0 | —（未创建测试数据） | 0 | — | — |
| 阶段 1 | | | | |
| 阶段 2 | | | | |
| 阶段 3 | | | | |
| 阶段 4 | | | | |
| 阶段 5 | | | | |
| 阶段 6 终检 | | | | |

## 四、终检清单（阶段 6 填写）✅ 完成（2026-08-31）

- [x] 登记表无"待清理"项（**各阶段均未创建任何测试数据**——本次修复为纯防御性逻辑，未造数；阶段清理核查记录已核对）
- [x] 焦点图列表数量与阶段 0 基线一致（公开 active = **3**，curl 复核）
- [x] 全站房间列表数量与阶段 0 基线一致（公开 total = **10**，curl 复核）
- [x] 专家列表数量与阶段 0 基线一致（公开 total = **384**，curl 复核）
- [x] 分类数量与阶段 0 基线一致（**35**，curl 复核）
- [x] 基线快照逐条比对，无既有数据被误删/误改（本次实施仅 GET 只读访问 + 前端代码改动）
- [x] 管理端检索 `[TEST]` 0 条（未创建）
- [ ] 用户/科室/admin 全量房间与焦点图 admin 总数——**待需求方登录联调环境补记**（阶段 0 遗留，不阻塞交付）
