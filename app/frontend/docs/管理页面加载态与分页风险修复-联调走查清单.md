# 管理页面加载态与分页风险修复-联调走查清单

> **文档版本**：v1.0
> **创建日期**：2026-08-31
> **依据文档**：《管理页面加载态与分页风险修复-实施规划文档.md》v1.7 阶段 6 遗留清单
> **用途**：阶段 6 验收门禁——联调环境人工走查，逐项勾选；全部通过后关闭发布门禁，结论回填主文档 v1.7 遗留清单
> **执行人**：需求方/验证方

---

## 一、走查环境与准备

| 项 | 要求 |
|----|------|
| 后端联调环境 | `http://10.105.136.235:8080`（`.env.development.local` 覆盖生效，`VITE_USE_MOCK=false`） |
| 前端运行方式 | 微信开发者工具导入 `dist/dev/mp-weixin`（先 `pnpm dev:mp-weixin`）；**App 真机（Android 优先）** 覆盖下拉/滚动相关项 |
| 登录账号 | 联调环境 admin 账号（具备管理功能入口） |
| 基线对照 | 走查前确认登记表"环境与基线"已记录（公开基线：焦点图 3 / 房间 10 / 专家 384 / 分类 35） |
| 注意事项 | ① 走查期间如发现异常，先判定是否属于"已知遗留项"（见文档 v1.7 遗留清单）；② 如确属缺陷，记录现象+复现步骤后回填主文档，评估是否追加修复 |

---

## 二、走查检查项

### A 组：全站房间（admin 模式）

入口：我的 → 管理功能 → 全站房间

| # | 操作步骤 | 通过标准 | 结果 |
|:-:|----------|----------|:---:|
| A1 | 进入页面，观察首屏加载 | 无全局"加载中"遮罩；列表正常渲染；专家信息分批出现（无 40 并发风暴感） | ☐ |
| A2 | 下拉刷新（页面顶部下拉） | 指示器出现并**正常收回**；无页面级原生下拉与 scroll-view refresher 双重冲突；列表数据刷新 | ☐ |
| A3 | 连续切换「全部/公开/私密」Tab | 每次切换后列表与 Tab 条件一致；切换期间快速连续点击，最终列表与最后选中的 Tab 一致（无旧数据残留） | ☐ |
| A4 | 触底分页加载 | 列表逐页追加**无重复行**；滚到最底出现"没有更多了"，请求终止 | ☐ |
| A5 | 断网或后端异常时触底 | 请求失败后**页码不漂移**；恢复网络后再次触底可正常继续加载（不跳页） | ☐ |
| A6 | 加载中切 Tab/搜索 | 加载期间点击 Tab 或搜索，本轮结束后自动补跑，最终列表与当前条件一致 | ☐ |

### B 组：专家管理

入口：我的 → 管理功能 → 专家管理

| # | 操作步骤 | 通过标准 | 结果 |
|:-:|----------|----------|:---:|
| B1 | 下拉刷新 / 快速切 Tab（全部/已下架/未分配） | 刷新期间显示**骨架屏而非空态占位**（无整页白闪）；最终列表与 Tab 一致 | ☐ |
| B2 | 快速连续点击三个 Tab | 最终列表与最后选中 Tab 一致（补跑不被冷却挡掉） | ☐ |
| B3 | 进入页面 onShow | 页面加载与分类下拉加载期间**无全局遮罩** | ☐ |
| B4 | 断网/后端异常时触底加载 | 失败后 3 秒内重复触底**不再重发请求**（无请求风暴）；恢复后正常 | ☐ |
| B5 | 搜索/分类/科室筛选 | 切换条件后列表与条件一致，无旧数据混搭 | ☐ |

### C 组：焦点图（轮播图管理）

入口：我的 → 管理功能 → 轮播图管理

| # | 操作步骤 | 通过标准 | 结果 |
|:-:|----------|----------|:---:|
| C1 | 三个 Tab（正在展示/已下线/全部）逐一滚到最底 | 请求终止，无连环自动加载；"已到底部"与列表实际长度一致 | ☐ |
| C2 | 搜索后列表较短时 | 无自激触底循环；列表与搜索条件一致 | ☐ |
| C3 | 某 Tab 下服务端有数据但全部被过滤（如状态口径不一致） | 自动续拉探测（最多 5 页）后稳定停在空态或正确列表，**不卡死、不无限请求** | ☐ |
| C4 | 页面加载/搜索/切 Tab | 无全局遮罩；骨架与空态切换正确 | ☐ |

### D 组：用户管理 + 专家分类

入口：我的 → 管理功能 → 用户管理 / 专家分类管理

| # | 操作步骤 | 通过标准 | 结果 |
|:-:|----------|----------|:---:|
| D1 | 切 Tab（全部/已封禁/无开播；全部分类/待审核） | 加载中快速切换后，最终列表与当前 Tab 一致（无吞刷新、无旧数据混搭） | ☐ |
| D2 | 改角色/状态筛选、搜索 | 每次变更列表与条件一致；连续快速变更后为最后条件 | ☐ |
| D3 | 触底分页 + 立即下拉刷新 | loadMore 后立即下拉/切 Tab **有响应**（2s 黑名单已移除）；失败不跳页 | ☐ |
| D4 | 页面加载/翻页/搜索期间 | 无全局遮罩（分类页含 getCategories 加载） | ☐ |

### E 组：非 admin 调用点（防误伤确认）

| # | 操作步骤 | 通过标准 | 结果 |
|:-:|----------|----------|:---:|
| E1 | 直播间播放页 LiveView（进入房间、切换场次） | 行为与修复前一致；原依赖的"加载中"提示若消失属预期（页面自带加载态），无报错、无卡死 | ☐ |
| E2 | 直播间 Tab 管理（TabManager）、房间详情（detail） | 场次/专家数据正常加载，无异常遮罩行为 | ☐ |
| E3 | 我的直播（manage 模式）下拉刷新 | 指示器正常收回；已知遗留：加载中再下拉/搜索可能偶发无刷新（manage 吞刷新，另行评估，**不阻塞本门禁**） | ☐ |

### F 组：后端契约观察（走查顺带确认，非缺陷项）

| # | 观察项 | 记录 |
|:-:|--------|------|
| F1 | 焦点图「已下线」Tab：服务端 `status=inactive` 是否包含 expired 记录 | 记录观察到的标题/状态分布 |
| F2 | 焦点图「正在展示」Tab：upcoming（待上线）记录是否被服务端排除或包含 | 同上 |
| F3 | `/admin/rooms` 分页：连续翻页是否稳定（无重复页、无跳页） | 记录 A4 走查结果即可 |
| F4 | 用户管理「无开播权限」Tab：服务端 `can_stream=false` 过滤是否准确（管理员是否被返回） | 记录观察到的角色分布 |

---

## 三、走查结论

| 项 | 内容 |
|----|------|
| 走查日期 |  |
| 执行环境 | 微信开发者工具 ☐ / App 真机（型号： ）☐ |
| A 组 | ☐ 通过 ☐ 有问题（记录： ） |
| B 组 | ☐ 通过 ☐ 有问题（记录： ） |
| C 组 | ☐ 通过 ☐ 有问题（记录： ） |
| D 组 | ☐ 通过 ☐ 有问题（记录： ） |
| E 组 | ☐ 通过 ☐ 有问题（记录： ） |
| F 组契约观察 | 记录见上表 |
| 发现缺陷（如有） | 现象 / 复现步骤 / 是否已知遗留 / 建议处理 |
| **门禁结论** | ☐ 通过，可发布 / ☐ 有条件通过（遗留项见上）/ ☐ 未通过 |

> 走查完成后：① 本清单存档；② 结论回填《管理页面加载态与分页风险修复-实施规划文档.md》v1.7 阶段 6 遗留清单；③ 如有新缺陷，评估追加修复或另立任务。
