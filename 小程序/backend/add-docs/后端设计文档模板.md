# {模块名称}模块设计文档（{英文名}）

**版本**: V1.0  
**日期**: {YYYY-MM-DD}  
**状态**: ✅ 设计完成，可直接开发  
**基于**: 《{依赖的主设计文档名称}》

---

## 📌 核心定位说明

### 1. 本文档的定位

本文档专注于**{模块中文名称}（{英文名}）**的完整后端实现设计，包括：

**包含模块**:
- **{子模块1}（{英文名}）**: {一句话说明}
- **{子模块2}（{英文名}）**: {一句话说明}

**模块范围**:
- {N}张数据表（`{表1}`、`{表2}`）
- {M}个API接口（{子模块1} {X}个、{子模块2} {Y}个）
- 完整的Pydantic Schemas定义
- 完整的数据库DDL及索引设计
- 详细的执行流程说明（8-10步）

### 2. 本文档的核心特点

- ✅ **内容完整性**: 包含{模块名}的所有后端设计内容
- ✅ **独立可开发**: 可作为独立的后端开发文档，开发团队可直接基于本文档进行开发
- ✅ **与v6主文档保持一致**: 遵循v6主文档的所有设计规范和约定
- ✅ **前端可对接**: API接口设计完整覆盖前端管理端和用户端的全部需求

### 3. 业务价值说明

**{子模块1}（{英文名}）**:
- {业务价值1}
- {业务价值2}

**{子模块2}（{英文名}）**:
- {业务价值1}
- {业务价值2}

---

## 📚 依赖文档清单

本文档依赖以下文档，开发时需参考：

| 序号 | 文档名称 | 依赖关系 | 影响范围 |
|------|---------|---------|---------|
| 1 | 《{主设计文档}》 | 📋 **主设计文档** | {影响的表、字段、规范} |
| 2 | 《{模块设计文档}》 | 📋 **模块设计文档** | {影响的DDL、API、流程} |
| 3 | 《{参考文档}》 | 📖 **参考文档** | {参考的规范、约定} |

**⚠️ 重要说明**:
- {依赖关系说明}
- {数据关联说明}
- 所有时间字段使用 `TIMESTAMPTZ`（带时区）
- 所有ID字段使用 `UUID` 类型

---

## 📖 文档规范说明

### 统一响应结构

所有API接口遵循统一的JSON响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-06-07T10:00:00Z"
}
```

### 统一分页格式

分页接口使用统一的分页响应结构：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "size": 20,
    "items": [ ... ]
  },
  "timestamp": "2026-06-07T10:00:00Z"
}
```

### 统一认证规范

本模块API接口使用以下认证级别：

- **公开访问（Public）**: 无需认证（如：{公开接口示例}）
- **JWT认证（JWT）**: 需要有效的JWT Token（如：{用户接口示例}）
- **管理员权限（ADMIN）**: 需要JWT Token且角色为admin（如：{管理端接口示例}）

### JWT Token格式说明

```json
{
  "user_id": "uuid-string",
  "username": "admin",
  "role": "admin",
  "exp": 1717756800
}
```

> **重要说明**: JWT Payload中用户标识字段为 `user_id`（非 `sub`），所有需要获取当前用户的接口均通过 `user_id` 字段提取用户身份。

### 业务状态码

| 状态码 | 说明 | 使用场景 |
|--------|------|----------|
| `200` | 成功 | 所有成功响应 |
| `1002` | 数据库错误 | 数据库操作异常 |
| `2001` | 资源不存在 | {资源}ID不存在 |
| `2002` | 资源已存在 | {唯一字段}重复 |
| `2004` | 业务逻辑错误 | {业务规则}冲突 |
| `3001` | 未授权 | JWT Token缺失或无效 |
| `3002` | 权限不足 | 非管理员访问管理端接口 |
| `4001` | 参数校验失败 | 请求参数不符合规范 |

---

## 🎯 设计要点与约定

### 1.1 数据库设计规范

1. **UUID主键**: 所有表使用 `UUID` 类型作为主键，不使用自增ID
2. **时间戳**: 所有时间字段使用 `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`
3. **外键约束**: 所有外键使用 `CONSTRAINT fk_<table>_<column>` 命名规范
4. **软删除**: {表名}表使用 {软删除策略} 实现软删除
5. **索引策略**: 高频查询字段建立索引，命名规范 `idx_<table>_<column>`
6. **字段注释**: 所有表和关键字段使用 `COMMENT ON` 添加中文注释

### 1.2 API设计规范

1. **HTTP方法**: GET（查询）、POST（创建）、PUT/PATCH（更新）、DELETE（删除）
2. **URL路径**: 使用 `/api/v1/{resource}` 格式，复数名词
3. **认证策略**: {公开读取 + 管理员写入 / 全部需要认证}
4. **执行流程**: 每个API包含8-10步详细执行流程说明

### 1.3 软删除策略

| 表名 | 策略 | 说明 |
|------|------|------|
| `{表1}` | {is_active字段 / 物理删除 / status字段} | {说明} |
| `{表2}` | {策略} | {说明} |

### 1.4 安全与配置规范

#### 架构分层原则

- **API 层**: 仅负责认证（`Depends(get_current_user)` 或 `Depends(get_current_user_optional)`）
- **Service 层**: 负责授权（权限守卫函数）
- **CRUD 层**: 负责SQL级别的权限过滤（不使用内存过滤）

#### 双轨鉴权模式

- **严格鉴权（Strict Auth）**: 所有CUD操作，需要有效的JWT Token
- **可选鉴权（Optional Auth）**: 公开读取操作，允许匿名访问

#### 权限守卫函数

```python
def _check_admin_permission(user_role: str) -> None:
    """检查管理员权限，非管理员抛出403异常"""
    if user_role != "admin":
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={"code": 3002, "message": "权限不足：需要管理员权限"}
        )
```

#### CRUD层权限过滤

```python
# 公开查询：默认只返回 is_active = true 的记录
async def get_{resource}(db: AsyncSession, include_inactive: bool = False):
    query = select({Model})
    if not include_inactive:
        query = query.where({Model}.is_active == True)
    return await db.execute(query)
```

#### 用户身份提取规范

```python
async def get_current_user(token: str = Depends(oauth2_scheme)) -> dict:
    """从JWT Token中提取用户身份"""
    payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    user_id = payload.get("user_id")  # 注意：是 user_id，非 sub
    if user_id is None:
        raise HTTPException(status_code=401, detail={"code": 3001, "message": "无效的Token"})
    return {"user_id": user_id, "role": payload.get("role")}
```

---

## 1. 数据库Schema设计（DDL）

### 1.0 前置准备：触发器函数

```sql
-- 自动更新 updated_at 触发器函数（如已创建则跳过）
CREATE OR REPLACE FUNCTION trigger_set_timestamp()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

### 1.1 {table_name}（{中文描述}）

```sql
-- ==========================================================
-- 表：{table_name}（{中文描述}）
-- 说明：{表的用途说明}
-- ==========================================================

CREATE TABLE {table_name} (
    id UUID PRIMARY KEY,
    {字段名} {类型} {约束},
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_{table}_{column} FOREIGN KEY ({column})
        REFERENCES {ref_table}(id) ON DELETE CASCADE
);

-- 唯一索引
CREATE UNIQUE INDEX idx_{table}_{column} ON {table_name}({column});

-- 普通索引
CREATE INDEX idx_{table}_{column} ON {table_name}({column});

-- 复合索引
CREATE INDEX idx_{table}_{col1}_{col2} ON {table_name}({col1}, {col2});

-- 触发器：自动更新 updated_at
CREATE TRIGGER set_timestamp_{table_name}
    BEFORE UPDATE ON {table_name}
    FOR EACH ROW
    EXECUTE FUNCTION trigger_set_timestamp();

-- 表注释
COMMENT ON TABLE {table_name} IS '{中文表描述}';
COMMENT ON COLUMN {table_name}.id IS '{字段中文说明}';
COMMENT ON COLUMN {table_name}.{field} IS '{字段中文说明}';
```

---

## 2. Pydantic Schemas定义

### 2.1 {Entity} Schemas

```python
from pydantic import BaseModel, Field, ConfigDict, field_validator
from typing import Optional, List
from uuid import UUID
from datetime import datetime
import re


class {Entity}Base(BaseModel):
    """{实体}基础Schema"""
    {field}: {type} = Field(..., {约束}, description="{描述}")

    @field_validator("{field}")
    @classmethod
    def validate_{field}(cls, v: {type}) -> {type}:
        """{校验说明}"""
        {校验逻辑}
        return v


class {Entity}Create({Entity}Base):
    """创建{实体}请求Schema"""
    pass


class {Entity}Update(BaseModel):
    """更新{实体}请求Schema"""
    {field}: Optional[{type}] = Field(None, {约束})

    model_config = ConfigDict(from_attributes=True)


class {Entity}Item({Entity}Base):
    """{实体}响应Schema"""
    id: UUID
    created_at: datetime
    updated_at: datetime

    model_config = ConfigDict(from_attributes=True)


class {Entity}ListResponse(BaseModel):
    """{实体}列表响应Schema（统一分页格式）"""
    code: int = 200
    message: str = "success"
    data: dict  # {total, page, size, items: List[{Entity}Item]}
    timestamp: datetime
```

---

## 3. API接口设计（完整CRUD）

### 3.1 {Module} 模块API

#### 3.1.1 {接口中文名称}（{认证级别}）

**Endpoint**: `{METHOD} /api/v1/{path}`

**描述**: {一句话描述}

**认证**: {公开访问/JWT认证/管理员权限}

**请求参数 (Query)**:
- `{param}` ({type}, {可选/required}, 默认{value}): {描述}

**请求体**:
```json
{
  "{field}": "{value}"
}
```

**成功响应** (`200 OK`):
```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-06-07T10:00:00Z"
}
```

**失败响应** (`{statusCode} {statusText}`):
```json
{
  "code": {errorCode},
  "message": "{errorMessage}",
  "data": null,
  "timestamp": "2026-06-07T10:00:00Z"
}
```

**执行流程**:

1. **接收请求**: API路由接收{METHOD}请求，解析{参数类型}
2. **JWT验证**: 通过 `Depends(get_current_user)` 提取当前用户
3. **权限检查**: 调用 `_check_admin_permission(user.role)` 校验管理员权限
4. **Pydantic校验**: 使用 `{Schema}` Schema校验请求体
5. **存在性检查**: 查询{资源}是否存在，若不存在抛出 `404` 异常
6. **唯一性检查**: 查询{唯一字段}是否已存在，若存在抛出 `409` 异常
7. **{业务操作}**: {具体业务逻辑}
8. **插入/更新数据库**: {SQL操作}
9. **提交事务**: `await db.commit()`
10. **返回响应**: 返回{成功/创建的资源信息}

**HTTP状态码**:
- `200 OK` / `201 Created`: {操作}成功
- `401 Unauthorized`: JWT Token无效（code=3001）
- `403 Forbidden`: 非管理员（code=3002）
- `404 Not Found`: {资源}不存在（code=2001）
- `409 Conflict`: {唯一字段}已存在（code=2002）
- `422 Unprocessable Entity`: 参数校验失败（code=4001）
- `500 Internal Server Error`: 数据库异常

---

## 4. 执行流程详细说明

### 4.1 {操作名称}完整流程

```
1. API路由接收{METHOD}请求
   → 2. JWT验证（Depends(get_current_user)）
   → 3. 权限检查（_check_admin_permission）
   → 4. Pydantic校验（{Schema}）
   → 5. {前置检查}
   → 6. {业务逻辑}
   → 7. {数据库操作}
   → 8. 提交事务（db.commit()）
   → 9. 序列化响应（{Schema}）
   → 10. 返回JSON响应（{statusCode}）
```

---

## 5. 错误处理与事务管理

### 5.1 错误处理策略

**业务异常**:
- 资源不存在（code=2001）→ HTTP 404
- 资源冲突（code=2002）→ HTTP 409
- 业务逻辑错误（code=2004）→ HTTP 422

**系统异常**:
- 数据库连接失败 → HTTP 503
- 文件存储失败 → HTTP 500

**参数校验异常**:
- Pydantic校验失败 → HTTP 422（code=4001）

### 5.2 事务管理规范

**单表操作**: 使用单个事务，操作失败自动回滚

**多表操作**:
```python
async with db.begin():
    # 操作1
    await db.execute(...)
    # 操作2
    db.add_all(...)
# 自动提交或回滚
```

**并发控制**: 使用数据库唯一约束防止并发冲突

---

## 6. 性能优化建议

### 6.1 数据库优化

- **索引优化**: {高频查询字段}建立索引
- **查询优化**: {优化策略}
- **连接池**: 使用SQLAlchemy的 `async_session` 连接池管理

### 6.2 缓存策略

- **Redis缓存**: {可缓存的数据}可缓存{TTL}分钟
- **缓存键**: `{module}:{resource}:{id}`
- **缓存失效**: {触发缓存失效的操作}

### 6.3 应用层优化

- **批量操作**: {批量操作场景}
- **异步处理**: 所有数据库操作使用 `async/await`

---

## 7. 测试建议

### 7.1 单元测试

- {核心业务逻辑测试}
- {数据校验测试}
- {权限检查测试}

### 7.2 集成测试

- API端点完整流程测试
- 权限验证测试（管理员 vs 普通用户 vs 匿名）
- {模块特有测试场景}

### 7.3 性能测试

- {查询性能测试}
- {并发测试}

### 7.4 安全测试

- SQL注入测试
- XSS测试
- 权限绕过测试

---

## 8. 错误码对照表

| 错误码 | 说明 | 使用场景 |
|--------|------|----------|
| `200` | 成功 | 所有成功响应 |
| `1002` | 数据库错误 | 数据库操作异常 |
| `2001` | 资源不存在 | {资源}ID不存在 |
| `2002` | 资源已存在 | {唯一字段}重复 |
| `2004` | 业务逻辑错误 | {业务规则}冲突 |
| `3001` | 未授权 | JWT Token缺失或无效 |
| `3002` | 权限不足 | 非管理员访问管理端接口 |
| `4001` | 参数校验失败 | 请求参数不符合Schema规范 |

---

## 9. 部署与监控建议

### 9.1 部署检查清单

- [ ] 数据库迁移脚本已执行（{表名}表）
- [ ] 触发器函数 `trigger_set_timestamp()` 已创建
- [ ] 上传目录 `{path}` 已创建且有写入权限
- [ ] 环境变量已配置（数据库连接、JWT密钥）
- [ ] CORS配置已添加{模块}相关路由

### 9.2 监控指标

**API监控**:
- {核心接口}响应时间（目标 < {N}ms）
- {操作}成功率（目标 > {N}%）
- 管理端接口错误率（目标 < 1%）

**数据库监控**:
- {表名}表行数
- 慢查询监控（> 1s）

### 9.3 告警规则

**严重告警**:
- 数据库连接失败
- {关键服务}不可用

**警告告警**:
- API响应时间 > {N}s
- {异常指标}

---

## 10. 后续开发建议

### 10.1 优先级P0（必须完成/第一批实现）

- [ ] {核心表DDL创建}
- [ ] {核心CRUD API实现}

### 10.2 优先级P1（重要功能/第二批实现）

- [ ] {重要功能}
- [ ] {缓存集成}

### 10.3 优先级P2（增强功能/优化阶段）

- [ ] {增强功能}
- [ ] {性能优化}

---

## 📝 文档修订历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|----------|------|
| V1.0 | {YYYY-MM-DD} | 初始版本 | {作者} |

---

## 最终路由表

> **说明**: 路由表按实现状态分为三类。编写文档时，将接口归入对应分区；若无某类接口，该分区可省略。

### ✅ 已实现（后端代码已存在）

| Domain | Endpoint | Method | Auth | Roles | Request Schema | Response Schema | Error Codes | Notes |
|--------|----------|--------|------|-------|----------------|-----------------|-------------|-------|
| {domain} | /api/v1/{path} | GET | Public | - | Query: {params} | {Schema} | 200, 1002 | {说明} |
| {domain} | /api/v1/{path} | POST | JWT | admin | Body: {Schema} | {Schema} | 200, 2002, 3001, 3002, 4001, 1002 | {说明} |

### 📋 待实现（设计已完成，后端未开发）

> 若无待实现接口，可省略此分区。

| Domain | Endpoint | Method | Auth | Roles | Request Schema | Response Schema | Error Codes | Notes | 优先级 |
|--------|----------|--------|------|-------|----------------|-----------------|-------------|-------|--------|
| {domain} | /api/v1/{path} | PATCH | JWT | admin | Body: {Schema} | null | 200, 3001, 3002, 4001, 1002 | {说明} | P1 |

### 🔧 代码已有但文档未收录

> 后续补充正式设计文档。若无此类接口，可省略此分区。

| Domain | Endpoint | Method | Auth | Roles | Notes |
|--------|----------|--------|------|-------|-------|
| {domain} | /api/v1/{path} | POST | JWT | admin | {简要说明} |

---

**文档结束** ✅
