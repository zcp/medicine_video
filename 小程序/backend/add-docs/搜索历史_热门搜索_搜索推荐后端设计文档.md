# 搜索历史、热门搜索与搜索建议 - 简化落地设计文档

**版本**: V2.0（简化版）
**创建日期**: 2026-05-23
**最后修订**: 2026-05-27
**状态**: 可执行

---

## 0. 设计原则

> **能跑通再说，跑通了再优化。**

| 原则 | 说明 |
|------|------|
| 最少表 | 只用 2 张表，不建日志表 |
| 最少依赖 | 不引入 Redis 缓存，直接查 PostgreSQL |
| 最少接口 | 搜索写历史在搜索接口内部完成，前端不感知 |
| 最少算法 | 热词 = 按搜索次数排序，建议 = 前缀匹配 |
| 复用已有 | 完全复用 live_core_service 的分层架构和响应格式 |

---

## 1. 功能范围（MVP）

只做三件事：

1. **搜索历史**：用户搜索后自动记录，可查看、可删除、可清空
2. **热门搜索**：展示平台搜索次数最多的关键词
3. **搜索建议**：输入时返回前缀匹配的联想词

不做：
- ~~Redis 缓存~~（后期加）
- ~~异步队列聚合~~（后期加）
- ~~个性化推荐算法~~（后期加）
- ~~独立的日志表~~（后期加）
- ~~search_type / source / scope 多维度~~（后期加）

---

## 2. 数据库设计（2 张表）

### 2.1 user_search_history（用户搜索历史）

```sql
CREATE TABLE user_search_history (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    keyword VARCHAR(255) NOT NULL,
    keyword_norm VARCHAR(255) NOT NULL,
    search_count INT NOT NULL DEFAULT 1,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, keyword_norm)
);

CREATE INDEX idx_user_search_history_user_time 
    ON user_search_history(user_id, last_searched_at DESC);
```

**字段说明**：
- `keyword_norm`：keyword 做 trim + 转小写后的值，用于去重
- `search_count`：同一用户搜同一词的累计次数
- `UNIQUE(user_id, keyword_norm)`：同一用户同一词只保留一条，重复搜时更新 count 和时间

**历史条数控制**：业务层限制每个用户最多保留 20 条，超出时删除最早的。

### 2.2 search_keyword_stats（热词统计）

```sql
CREATE TABLE search_keyword_stats (
    id UUID PRIMARY KEY,
    keyword_norm VARCHAR(255) NOT NULL UNIQUE,
    total_count BIGINT NOT NULL DEFAULT 0,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_search_keyword_stats_count 
    ON search_keyword_stats(total_count DESC);
```

**字段说明**：
- `keyword_norm`：标准化后的关键词，全局唯一
- `total_count`：所有用户搜索该词的总次数
- `last_searched_at`：最近一次搜索时间，用于过滤冷词

**为什么没有 `recent_count_7d` / `recent_count_24h`**：
这些字段需要定时任务刷新，MVP 阶段不需要。如果要过滤"最近热词"，直接用 `WHERE last_searched_at > now() - interval '7 days'` 即可。

---

## 3. SQLAlchemy 模型

在 `backend/live_core_service/app/models/` 下新建 `search.py`：

```python
"""
搜索历史与热词统计模型
"""
import uuid
from sqlalchemy import Column, String, Integer, BigInteger, Index, UniqueConstraint, TIMESTAMP
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.sql import func

from app.database import Base


class UserSearchHistory(Base):
    """用户搜索历史表"""
    __tablename__ = "user_search_history"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), nullable=False, index=True)
    keyword = Column(String(255), nullable=False)
    keyword_norm = Column(String(255), nullable=False)
    search_count = Column(Integer, nullable=False, default=1)
    last_searched_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        UniqueConstraint('user_id', 'keyword_norm', name='uq_user_search_history_user_keyword'),
        Index('idx_user_search_history_user_time', 'user_id', 'last_searched_at'),
    )


class SearchKeywordStats(Base):
    """搜索关键词统计表"""
    __tablename__ = "search_keyword_stats"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    keyword_norm = Column(String(255), nullable=False, unique=True)
    total_count = Column(BigInteger, nullable=False, default=0)
    last_searched_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index('idx_search_keyword_stats_count', 'total_count'),
    )
```

---

## 4. Pydantic Schemas

在 `backend/live_core_service/app/schemas/` 下新建 `search.py`：

```python
"""
搜索历史与热词 Schemas
"""
import uuid
import datetime
from typing import Optional, List
from pydantic import BaseModel, Field, ConfigDict


# ==================== 搜索历史 ====================

class SearchHistoryItem(BaseModel):
    """搜索历史项"""
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    keyword: str
    search_count: int
    last_searched_at: datetime.datetime


class SearchHistoryListResponse(BaseModel):
    """搜索历史列表响应"""
    code: int = 200
    message: str = "success"
    data: List[SearchHistoryItem]
    timestamp: datetime.datetime


# ==================== 热门搜索 ====================

class HotKeywordItem(BaseModel):
    """热门搜索项"""
    model_config = ConfigDict(from_attributes=True)

    keyword: str
    search_count: int
    last_searched_at: datetime.datetime


class HotKeywordsResponse(BaseModel):
    """热门搜索响应"""
    code: int = 200
    message: str = "success"
    data: List[HotKeywordItem]
    timestamp: datetime.datetime


# ==================== 搜索建议 ====================

class SearchSuggestionItem(BaseModel):
    """搜索建议项"""
    keyword: str


class SearchSuggestionsResponse(BaseModel):
    """搜索建议响应"""
    code: int = 200
    message: str = "success"
    data: List[SearchSuggestionItem]
    timestamp: datetime.datetime
```

---

## 5. CRUD 层

在 `backend/live_core_service/app/crud/` 下新建 `search.py`：

```python
"""
搜索历史与热词 CRUD
"""
import uuid
from typing import List, Optional, Tuple
from datetime import datetime

from sqlalchemy import select, func, delete, text
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.dialects.postgresql import insert as pg_insert

from app.models.search import UserSearchHistory, SearchKeywordStats
import logging

logger = logging.getLogger(__name__)

MAX_HISTORY_PER_USER = 20


def _normalize_keyword(keyword: str) -> str:
    """标准化关键词：trim + 转小写 + 压缩空白"""
    return " ".join(keyword.strip().lower().split())


# ==================== 搜索落库（搜索接口内部调用） ====================

async def record_search(
    db: AsyncSession,
    user_id: Optional[uuid.UUID],
    keyword: str
) -> None:
    """
    记录一次搜索行为（同步调用，不阻塞搜索结果返回）

    做两件事：
    1. upsert 用户历史表
    2. upsert 热词统计表
    """
    keyword_norm = _normalize_keyword(keyword)
    if not keyword_norm or len(keyword_norm) < 2:
        return

    # 1. upsert 用户历史（仅登录用户）
    if user_id:
        stmt = pg_insert(UserSearchHistory).values(
            id=uuid.uuid4(),
            user_id=user_id,
            keyword=keyword.strip(),
            keyword_norm=keyword_norm,
            search_count=1,
            last_searched_at=func.now(),
        )
        stmt = stmt.on_conflict_do_update(
            index_elements=['user_id', 'keyword_norm'],
            set_={
                'search_count': UserSearchHistory.search_count + 1,
                'last_searched_at': func.now(),
                'updated_at': func.now(),
            }
        )
        await db.execute(stmt)

        # 控制历史条数：删除超出的旧记录
        await _trim_user_history(db, user_id)

    # 2. upsert 热词统计
    stmt = pg_insert(SearchKeywordStats).values(
        id=uuid.uuid4(),
        keyword_norm=keyword_norm,
        total_count=1,
        last_searched_at=func.now(),
    )
    stmt = stmt.on_conflict_do_update(
        index_elements=['keyword_norm'],
        set_={
            'total_count': SearchKeywordStats.total_count + 1,
            'last_searched_at': func.now(),
            'updated_at': func.now(),
        }
    )
    await db.execute(stmt)

    await db.flush()
    logger.info(f"记录搜索: user={str(user_id)[:8] if user_id else 'anon'}, keyword={keyword_norm}")


async def _trim_user_history(db: AsyncSession, user_id: uuid.UUID) -> None:
    """保留最近 MAX_HISTORY_PER_USER 条历史，删除多余的"""
    # 查找需要删除的旧记录ID
    subq = (
        select(UserSearchHistory.id)
        .where(UserSearchHistory.user_id == user_id)
        .order_by(UserSearchHistory.last_searched_at.desc())
        .offset(MAX_HISTORY_PER_USER)
        .subquery()
    )
    stmt = delete(UserSearchHistory).where(UserSearchHistory.id.in_(select(subq.c.id)))
    result = await db.execute(stmt)
    if result.rowcount > 0:
        logger.info(f"清理用户历史: user={str(user_id)[:8]}, 删除{result.rowcount}条")


# ==================== 搜索历史查询 ====================

async def get_user_history(
    db: AsyncSession,
    user_id: uuid.UUID,
    limit: int = 10
) -> List[UserSearchHistory]:
    """获取用户搜索历史，按最近搜索时间倒序"""
    stmt = (
        select(UserSearchHistory)
        .where(UserSearchHistory.user_id == user_id)
        .order_by(UserSearchHistory.last_searched_at.desc())
        .limit(limit)
    )
    result = await db.execute(stmt)
    return list(result.scalars().all())


async def delete_user_history_item(
    db: AsyncSession,
    user_id: uuid.UUID,
    history_id: uuid.UUID
) -> bool:
    """删除单条搜索历史"""
    stmt = delete(UserSearchHistory).where(
        UserSearchHistory.id == history_id,
        UserSearchHistory.user_id == user_id
    )
    result = await db.execute(stmt)
    return result.rowcount > 0


async def clear_user_history(
    db: AsyncSession,
    user_id: uuid.UUID
) -> int:
    """清空用户全部搜索历史，返回删除条数"""
    stmt = delete(UserSearchHistory).where(UserSearchHistory.user_id == user_id)
    result = await db.execute(stmt)
    return result.rowcount


# ==================== 热门搜索查询 ====================

async def get_hot_keywords(
    db: AsyncSession,
    limit: int = 10,
    days: int = 7
) -> List[SearchKeywordStats]:
    """
    获取热门搜索词

    简单逻辑：最近 N 天内有搜索的词，按总搜索次数降序排列
    """
    from datetime import timedelta
    cutoff = datetime.utcnow() - timedelta(days=days)

    stmt = (
        select(SearchKeywordStats)
        .where(SearchKeywordStats.last_searched_at >= cutoff)
        .where(SearchKeywordStats.total_count >= 2)  # 至少被搜过2次
        .order_by(SearchKeywordStats.total_count.desc())
        .limit(limit)
    )
    result = await db.execute(stmt)
    return list(result.scalars().all())


# ==================== 搜索建议查询 ====================

async def get_suggestions(
    db: AsyncSession,
    keyword: str,
    limit: int = 10
) -> List[str]:
    """
    获取搜索建议（前缀匹配）

    优先级：热词表中以 keyword 开头的词，按搜索次数降序
    """
    keyword_norm = _normalize_keyword(keyword)
    if not keyword_norm:
        return []

    pattern = f"{keyword_norm}%"

    stmt = (
        select(SearchKeywordStats.keyword_norm)
        .where(SearchKeywordStats.keyword_norm.like(pattern))
        .where(SearchKeywordStats.keyword_norm != keyword_norm)  # 排除完全相同的
        .order_by(SearchKeywordStats.total_count.desc())
        .limit(limit)
    )
    result = await db.execute(stmt)
    return [row[0] for row in result.all()]
```

---

## 6. API 端点

在 `backend/live_core_service/app/api/v1/endpoints/` 下新建 `search_extra.py`：

```python
"""
搜索历史、热门搜索、搜索建议 API 端点

这些端点是对现有 GET /api/v1/search 的补充，不修改原有搜索契约。
"""
import uuid
from typing import Optional
from datetime import datetime

from fastapi import APIRouter, Depends, Query
from fastapi.responses import JSONResponse
from sqlalchemy.ext.asyncio import AsyncSession

from app.database import get_db
from app.core.deps import get_current_user, get_current_user_optional
from app.crud import search as search_crud
from app.schemas.search import (
    SearchHistoryItem,
    HotKeywordItem,
    SearchSuggestionItem,
)
from app.core.response import success_response, error_response
from app.exceptions import NotFoundException
import logging

logger = logging.getLogger(__name__)

# 搜索历史路由（用户相关）
search_history_router = APIRouter()

# 热门搜索和搜索建议路由（公开）
search_extra_router = APIRouter()


# ==================== 搜索历史 ====================

@search_history_router.get("/search-history", response_model=None, tags=["搜索历史"])
async def get_search_history(
    limit: int = Query(10, ge=1, le=20, description="返回条数"),
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """获取当前用户的搜索历史"""
    user_id = uuid.UUID(current_user.get("user_id") or current_user.get("sub"))

    try:
        items = await search_crud.get_user_history(db, user_id, limit)
        data = [SearchHistoryItem.model_validate(item).model_dump(mode='json') for item in items]
        return JSONResponse(status_code=200, content=success_response(data=data))
    except Exception as e:
        logger.error(f"获取搜索历史失败: {e}")
        return JSONResponse(status_code=500, content=error_response(code=5001, message="服务器内部错误"))


@search_history_router.delete("/search-history/{history_id}", response_model=None, tags=["搜索历史"])
async def delete_search_history(
    history_id: uuid.UUID,
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """删除单条搜索历史"""
    user_id = uuid.UUID(current_user.get("user_id") or current_user.get("sub"))

    try:
        deleted = await search_crud.delete_user_history_item(db, user_id, history_id)
        if not deleted:
            return JSONResponse(status_code=404, content=error_response(code=2001, message="历史记录不存在"))
        return JSONResponse(status_code=200, content=success_response(data=None, message="删除成功"))
    except Exception as e:
        logger.error(f"删除搜索历史失败: {e}")
        return JSONResponse(status_code=500, content=error_response(code=5001, message="服务器内部错误"))


@search_history_router.delete("/search-history", response_model=None, tags=["搜索历史"])
async def clear_search_history(
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """清空当前用户的全部搜索历史"""
    user_id = uuid.UUID(current_user.get("user_id") or current_user.get("sub"))

    try:
        count = await search_crud.clear_user_history(db, user_id)
        return JSONResponse(status_code=200, content=success_response(
            data={"deleted_count": count}, message="清空成功"
        ))
    except Exception as e:
        logger.error(f"清空搜索历史失败: {e}")
        return JSONResponse(status_code=500, content=error_response(code=5001, message="服务器内部错误"))


# ==================== 热门搜索 ====================

@search_extra_router.get("/hot-keywords", response_model=None, tags=["热门搜索"])
async def get_hot_keywords(
    limit: int = Query(10, ge=1, le=50, description="返回条数"),
    days: int = Query(7, ge=1, le=30, description="统计天数窗口"),
    db: AsyncSession = Depends(get_db)
):
    """获取平台热门搜索词（公开接口）"""
    try:
        items = await search_crud.get_hot_keywords(db, limit, days)
        data = [HotKeywordItem.model_validate(item).model_dump(mode='json') for item in items]
        return JSONResponse(status_code=200, content=success_response(data=data))
    except Exception as e:
        logger.error(f"获取热门搜索失败: {e}")
        return JSONResponse(status_code=500, content=error_response(code=5001, message="服务器内部错误"))


# ==================== 搜索建议 ====================

@search_extra_router.get("/suggestions", response_model=None, tags=["搜索建议"])
async def get_suggestions(
    keyword: str = Query(..., min_length=1, max_length=255, description="输入关键词"),
    limit: int = Query(10, ge=1, le=20, description="返回条数"),
    db: AsyncSession = Depends(get_db)
):
    """获取搜索建议（公开接口，前缀匹配）"""
    try:
        keywords = await search_crud.get_suggestions(db, keyword, limit)
        data = [SearchSuggestionItem(keyword=kw).model_dump() for kw in keywords]
        return JSONResponse(status_code=200, content=success_response(data=data))
    except Exception as e:
        logger.error(f"获取搜索建议失败: {e}")
        return JSONResponse(status_code=500, content=error_response(code=5001, message="服务器内部错误"))
```

---

## 7. 路由注册

在 `backend/live_core_service/app/api/v1/api.py` 中添加（约第 195 行后）：

```python
# 搜索模块补充路由
from app.api.v1.endpoints import search_extra

# 用户搜索历史：/api/v1/users/me/search-history
api_router.include_router(
    search_extra.search_history_router,
    prefix="/users/me",
    tags=["搜索历史"]
)

# 热门搜索和搜索建议：/api/v1/search/hot-keywords, /api/v1/search/suggestions
api_router.include_router(
    search_extra.search_extra_router,
    prefix="/search",
    tags=["搜索扩展"]
)
```

---

## 8. 搜索接口联动（写历史）

在现有的搜索接口 `backend/live_core_service/app/api/v1/endpoints/homepage_search.py` 的 `search_resources_endpoint` 中，搜索成功后调用 `record_search`：

```python
# 在 search_resources_endpoint 函数中，搜索成功后添加：
from app.crud.search import record_search

# ... 现有搜索逻辑 ...

# 搜索成功后记录（异步刷盘，不阻塞响应）
try:
    user_id = None
    if current_user:
        user_id = uuid.UUID(current_user.get("user_id") or current_user.get("sub"))
    await record_search(db, user_id, q)
except Exception as e:
    logger.warning(f"记录搜索行为失败（不影响搜索结果）: {e}")
```

同时需要把 `search_resources_endpoint` 的认证从"无认证"改为"可选认证"：

```python
# 修改前：
async def search_resources_endpoint(
    q: str = Query(...),
    ...
    db: AsyncSession = Depends(get_db)
):

# 修改后：
async def search_resources_endpoint(
    q: str = Query(...),
    ...
    current_user: Optional[dict] = Depends(get_current_user_optional),
    db: AsyncSession = Depends(get_db)
):
```

---

## 9. API 接口总览

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/v1/search` | 可选 | 现有综合搜索（改造后自动记录历史） |
| GET | `/api/v1/users/me/search-history` | 必须 | 获取当前用户搜索历史 |
| DELETE | `/api/v1/users/me/search-history/{id}` | 必须 | 删除单条历史 |
| DELETE | `/api/v1/users/me/search-history` | 必须 | 清空全部历史 |
| GET | `/api/v1/search/hot-keywords` | 公开 | 热门搜索词 |
| GET | `/api/v1/search/suggestions` | 公开 | 搜索建议（前缀匹配） |

**前端调用时序**：
1. 页面加载 → `GET /search/hot-keywords`（展示热门搜索）
2. 用户输入 → `GET /search/suggestions?keyword=xxx`（联想词）
3. 用户回车 → `GET /search?q=xxx`（搜索结果 + 自动记录历史）
4. 点击搜索框 → `GET /users/me/search-history`（展示历史）

---

## 10. Docker 开发执行步骤

### 步骤 1：启动开发环境

```bash
# 进入项目根目录
cd d:\live-streaming-saas-v2-main\live-streaming-saas-v2-main

# 启动所有服务
docker-compose -f compose-app.windows.yml up -d

# 确认服务运行正常
docker-compose -f compose-app.windows.yml ps
```

### 步骤 2：创建数据库表

```bash
# 进入 live_core_service 容器
docker-compose -f compose-app.windows.yml exec live_core_service bash

# 在容器内用 Python 创建表（利用 SQLAlchemy 的 create_all）
python -c "
from app.database import engine, Base
from app.models.search import UserSearchHistory, SearchKeywordStats
import asyncio

async def create_tables():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    print('表创建成功')

asyncio.run(create_tables())
"

# 退出容器
exit
```

或者直接用 SQL：

```bash
# 进入 PostgreSQL 容器
docker-compose -f compose-app.windows.yml exec postgres psql -U postgres -d live_core_test

# 执行建表 SQL
CREATE TABLE IF NOT EXISTS user_search_history (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    keyword VARCHAR(255) NOT NULL,
    keyword_norm VARCHAR(255) NOT NULL,
    search_count INT NOT NULL DEFAULT 1,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, keyword_norm)
);

CREATE INDEX IF NOT EXISTS idx_user_search_history_user_time 
    ON user_search_history(user_id, last_searched_at DESC);

CREATE TABLE IF NOT EXISTS search_keyword_stats (
    id UUID PRIMARY KEY,
    keyword_norm VARCHAR(255) NOT NULL UNIQUE,
    total_count BIGINT NOT NULL DEFAULT 0,
    last_searched_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_search_keyword_stats_count 
    ON search_keyword_stats(total_count DESC);

-- 退出
\q
```

### 步骤 3：重建服务（代码修改后）

```bash
# 仅重建代码（保留数据库数据）
docker-compose -f compose-app.windows.yml up -d --build live_core_service

# 查看日志确认启动正常
docker-compose -f compose-app.windows.yml logs -f live_core_service
```

### 步骤 4：测试接口

```bash
# 热门搜索（公开，无需登录）
curl http://localhost:8000/api/v1/search/hot-keywords

# 搜索建议（公开）
curl "http://localhost:8000/api/v1/search/suggestions?keyword=心血"

# 综合搜索（会自动记录历史）
curl "http://localhost:8000/api/v1/search?q=心血管"

# 搜索历史（需要登录 token）
curl -H "Authorization: Bearer <your_token>" \
     http://localhost:8000/api/v1/users/me/search-history

# 删除单条历史
curl -X DELETE \
     -H "Authorization: Bearer <your_token>" \
     http://localhost:8000/api/v1/users/me/search-history/<history_id>

# 清空全部历史
curl -X DELETE \
     -H "Authorization: Bearer <your_token>" \
     http://localhost:8000/api/v1/users/me/search-history
```

### 步骤 5：验证数据落库

```bash
# 进入 PostgreSQL 容器
docker-compose -f compose-app.windows.yml exec postgres psql -U postgres -d live_core_test

# 查看热词统计
SELECT * FROM search_keyword_stats ORDER BY total_count DESC LIMIT 10;

# 查看用户历史
SELECT * FROM user_search_history ORDER BY last_searched_at DESC LIMIT 10;

\q
```

---

## 11. 文件清单

需要新建的文件（共 4 个）：

| 文件路径 | 说明 |
|----------|------|
| `backend/live_core_service/app/models/search.py` | SQLAlchemy 模型 |
| `backend/live_core_service/app/schemas/search.py` | Pydantic Schemas |
| `backend/live_core_service/app/crud/search.py` | CRUD 层 |
| `backend/live_core_service/app/api/v1/endpoints/search_extra.py` | API 端点 |

需要修改的文件（共 2 个）：

| 文件路径 | 修改内容 |
|----------|----------|
| `backend/live_core_service/app/api/v1/api.py` | 注册新路由（加 8 行） |
| `backend/live_core_service/app/api/v1/endpoints/homepage_search.py` | 搜索接口加可选认证 + 调用 record_search（改 3 行） |

---

## 12. 后期优化路径（当前不做）

当 MVP 跑通、有真实用户数据后，按需添加：

| 优化项 | 时机 | 改动量 |
|--------|------|--------|
| Redis 缓存热词 | 热词接口 p99 > 200ms | 加一个缓存层，改 CRUD |
| 异步记录搜索 | 搜索接口延迟明显 | 改为 Celery task，改 endpoint |
| 个性化推荐 | 有足够用户行为数据 | 加推荐算法 service |
| 独立日志表 | 需要做运营分析 | 加 search_query_logs 表 |
| 搜索类型筛选 | 有多种搜索入口 | 加 search_type 字段 |

每一步都是独立的、向后兼容的增量改动，不需要推翻现有设计。

---

## 13. 与原版文档的差异

| 维度 | 原版文档 (V1.0) | 本文档 (V2.0) |
|------|-----------------|---------------|
| 表数量 | 3 张 | 2 张 |
| 缓存 | Redis + DB 双层 | 直接查 DB |
| 热词聚合 | 异步队列 + 定时任务 | 同步 UPDATE |
| 热度公式 | 4 项加权公式 | 搜索次数 |
| 推荐算法 | 前缀/包含/近似/同义词 | 前缀匹配 |
| 历史写入 | 独立 POST 接口 | 搜索接口内部自动写 |
| 建议接口 | 与推荐分开 | 合并为一个 |
| 执行文件数 | 约 8 个 | 4 个新建 + 2 个修改 |
| 预计开发时间 | 3-5 天 | 0.5-1 天 |
