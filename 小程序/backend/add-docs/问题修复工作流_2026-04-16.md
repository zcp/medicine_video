# 问题修复工作流 - 2026年4月16日

## 📋 问题分类与优先级

### 🔴 高优先级 - 核心功能异常
1. **用户行为相关API 500错误** (影响用户体验)
   - 收藏功能 (`/users/me/favorites`)
   - 观看历史 (`/users/me/watch-history`) 
   - 订阅功能 (`/users/me/subscriptions`)
   - 删除直播间 (`/rooms/{id}`)

### 🟡 中优先级 - 功能缺失
2. **分类API 404错误** (基础数据缺失)
3. **Tab权限问题** (用户体验降级)
4. **我的直播页面权限筛选** (功能不完整)

### 🟢 低优先级 - 功能增强
5. **个人信息头像上传** (用户体验优化)

---

## 🔧 修复工作流程

### 阶段一：问题诊断与定位 (预计2小时)

#### 1.1 后端日志分析
```bash
# 查看各服务的错误日志
docker-compose -f compose-app.windows.yml logs live_core_service --tail=50
docker-compose -f compose-app.windows.yml logs user_service --tail=50

# 重点关注500错误的具体原因
grep -i "error\|exception\|500" logs/
```

#### 1.2 数据库状态检查
```bash
# 检查相关表是否存在
docker-compose -f compose-app.windows.yml exec postgres psql -U postgres -d live_core_service_test -c "\dt"

# 检查用户行为相关表
docker-compose -f compose-app.windows.yml exec postgres psql -U postgres -d live_core_service_test -c "
SELECT table_name FROM information_schema.tables 
WHERE table_name IN ('user_favorites', 'watch_history', 'user_subscriptions', 'categories');
"
```

#### 1.3 API端点验证
```bash
# 检查路由是否正确注册
curl -X GET http://localhost:8000/docs  # 查看API文档
curl -X GET http://localhost:8002/docs  # 查看用户服务API文档
```

---

### 阶段二：数据模型修复 (预计3小时)

#### 2.1 检查并创建缺失的数据模型

**优先处理的模型**：
- `UserFavorite` (用户收藏)
- `WatchHistory` (观看历史) 
- `UserSubscription` (用户订阅)
- `Category` (分类)

**检查步骤**：
```python
# 创建检查脚本: add-docs/check_models.py
import sys
sys.path.append('/app')
from app.models import *

# 检查模型是否存在
models_to_check = [
    'UserFavorite', 'WatchHistory', 'UserSubscription', 
    'Category', 'LiveRoom'
]

for model_name in models_to_check:
    try:
        model = globals()[model_name]
        print(f"✅ {model_name} 模型存在")
    except KeyError:
        print(f"❌ {model_name} 模型缺失")
```

#### 2.2 数据库迁移
```bash
# 如果模型存在但表缺失，执行建表
docker-compose -f compose-app.windows.yml exec live_core_service python -m app.scripts.create_tables

# 检查表创建结果
docker-compose -f compose-app.windows.yml exec postgres psql -U postgres -d live_core_service_test -c "\dt"
```

---

### 阶段三：API端点实现 (预计4小时)

#### 3.1 用户行为API实现优先级

**第一批 - 基础CRUD**：
```python
# 1. 收藏功能
GET    /api/v1/users/me/favorites      # 获取收藏列表
POST   /api/v1/users/me/favorites      # 添加收藏
DELETE /api/v1/users/me/favorites/{id} # 取消收藏

# 2. 观看历史
GET    /api/v1/users/me/watch-history  # 获取观看历史
POST   /api/v1/users/me/watch-history  # 记录观看历史

# 3. 订阅功能  
GET    /api/v1/users/me/subscriptions  # 获取订阅列表
POST   /api/v1/users/me/subscriptions  # 添加订阅
DELETE /api/v1/users/me/subscriptions/{id} # 取消订阅
```

**第二批 - 权限相关**：
```python
# 4. 分类API (公开)
GET    /api/v1/categories              # 获取分类列表 (公开)

# 5. Tab API (公开)  
GET    /api/v1/tabs                    # 获取Tab列表 (公开)

# 6. 我的直播间 (带权限筛选)
GET    /api/v1/users/me/rooms          # 获取当前用户的直播间
```

#### 3.2 实现策略

**Step 1: 创建服务层**
```python
# backend/live_core_service/app/services/user_behavior_service.py
class UserBehaviorService:
    async def get_user_favorites(self, user_id: int) -> List[UserFavorite]:
        # 实现收藏列表获取
        pass
    
    async def add_favorite(self, user_id: int, room_id: str) -> UserFavorite:
        # 实现添加收藏
        pass
```

**Step 2: 创建API端点**
```python
# backend/live_core_service/app/api/v1/endpoints/user_behavior.py
@router.get("/users/me/favorites")
async def get_my_favorites(current_user: User = Depends(get_current_user)):
    # 实现API端点
    pass
```

**Step 3: 注册路由**
```python
# backend/live_core_service/app/api/v1/api.py
from .endpoints import user_behavior
api_router.include_router(user_behavior.router, tags=["user-behavior"])
```

---

### 阶段四：权限与安全 (预计2小时)

#### 4.1 权限分级实现
```python
# 创建权限装饰器
def require_permission(permission: str):
    def decorator(func):
        async def wrapper(*args, **kwargs):
            # 检查用户权限
            pass
        return wrapper
    return decorator

# 应用到API
@require_permission("admin")
async def admin_only_api():
    pass

@require_permission("user")  
async def user_api():
    pass

# 公开API (无需权限)
async def public_api():
    pass
```

#### 4.2 数据访问控制
```python
# 确保用户只能访问自己的数据
async def get_user_favorites(user_id: int, current_user: User):
    if user_id != current_user.id and not current_user.is_admin:
        raise HTTPException(403, "无权访问")
    # 继续处理
```

---

### 阶段五：测试与验证 (预计2小时)

#### 5.1 API测试脚本
```python
# add-docs/test_apis.py
import requests
import json

BASE_URL = "http://localhost:8000/api/v1"
AUTH_TOKEN = "your_jwt_token"

def test_user_favorites():
    headers = {"Authorization": f"Bearer {AUTH_TOKEN}"}
    
    # 测试获取收藏
    response = requests.get(f"{BASE_URL}/users/me/favorites", headers=headers)
    print(f"获取收藏: {response.status_code}")
    
    # 测试添加收藏
    data = {"room_id": "test-room-id"}
    response = requests.post(f"{BASE_URL}/users/me/favorites", json=data, headers=headers)
    print(f"添加收藏: {response.status_code}")

if __name__ == "__main__":
    test_user_favorites()
```

#### 5.2 前端集成测试
```javascript
// 在前端测试修复后的API
const testAPIs = async () => {
  try {
    // 测试收藏API
    const favorites = await request.get('/users/me/favorites');
    console.log('收藏API正常:', favorites);
    
    // 测试观看历史API  
    const history = await request.get('/users/me/watch-history');
    console.log('观看历史API正常:', history);
    
  } catch (error) {
    console.error('API测试失败:', error);
  }
};
```

---

## 📅 时间规划

### 第1天 (8小时)
- **上午 (4小时)**: 阶段一 + 阶段二
  - 问题诊断与定位 (2小时)
  - 数据模型检查与修复 (2小时)

- **下午 (4小时)**: 阶段三 (第一批API)
  - 实现收藏、观看历史、订阅API (4小时)

### 第2天 (5小时)  
- **上午 (3小时)**: 阶段三 (第二批API)
  - 实现分类、Tab、我的直播间API (3小时)

- **下午 (2小时)**: 阶段四 + 阶段五
  - 权限与安全 (1小时)
  - 测试与验证 (1小时)

---

## 🔍 问题定位指南

### 500错误排查步骤
1. **查看具体错误信息**
   ```bash
   docker-compose -f compose-app.windows.yml logs live_core_service | grep -A 5 -B 5 "500\|ERROR"
   ```

2. **检查数据库连接**
   ```bash
   docker-compose -f compose-app.windows.yml exec live_core_service python -c "
   from app.database import engine
   print('数据库连接正常' if engine else '数据库连接失败')
   "
   ```

3. **检查模型导入**
   ```bash
   docker-compose -f compose-app.windows.yml exec live_core_service python -c "
   from app.models import UserFavorite
   print('模型导入成功')
   "
   ```

### 404错误排查步骤
1. **检查路由注册**
   ```python
   # 查看 app/api/v1/api.py 中是否正确注册了路由
   ```

2. **检查URL路径**
   ```bash
   curl -I http://localhost:8000/api/v1/categories
   ```

3. **检查nginx代理配置**
   ```bash
   # 检查nginx是否正确代理到后端服务
   ```

---

## 📝 实施检查清单

### 开始前检查
- [ ] 备份当前代码 (`git branch backup-$(date +%Y%m%d)`)
- [ ] 确认开发环境正常运行
- [ ] 准备测试账号和数据

### 阶段一完成检查
- [ ] 后端日志已分析，找到具体错误原因
- [ ] 数据库状态已检查，确认缺失的表
- [ ] API文档已查看，确认缺失的端点

### 阶段二完成检查  
- [ ] 所有必需的数据模型已存在
- [ ] 数据库表已正确创建
- [ ] 表结构符合业务需求

### 阶段三完成检查
- [ ] 用户行为API已实现并可正常调用
- [ ] 分类和Tab API已实现
- [ ] 权限筛选功能已实现

### 阶段四完成检查
- [ ] 权限控制已正确实现
- [ ] 数据访问安全已验证
- [ ] 无权限漏洞

### 阶段五完成检查
- [ ] 所有API测试通过
- [ ] 前端集成测试正常
- [ ] 用户体验符合预期

---

## 🚨 风险控制

### 数据安全
- 在生产环境操作前，先在开发环境验证
- 重要数据操作前先备份
- 权限控制务必严格测试

### 代码质量
- 每个阶段完成后进行代码审查
- 确保错误处理完整
- 添加适当的日志记录

### 回滚计划
- 每个阶段完成后创建Git标签
- 准备快速回滚脚本
- 记录所有配置变更

---

**工作流创建时间**: 2026年4月16日 16:20  
**预计完成时间**: 2天 (13小时)  
**负责人**: 开发团队  
**审查人**: 技术负责人
