# {模块名称}增量设计文档（V{N}）

**版本**: V{N}.0
**日期**: {YYYY-MM-DD}
**状态**: ✅ 设计完成，可直接开发
**基于**: 《{V1文档名称}》

---

## ⚠️ 重要声明

- **本文档是对 V{N-1} 的增量扩展，不是替代或覆盖**
- **原 V{N-1} 中的所有设计保持不变，本次仅新增/修改指定内容**
- **变更内容已明确标注【新增】【修改】【废弃】标识，便于区分**
- **未标注的内容以 V{N-1} 文档为准**

---

## 📌 核心定位说明

### 1. 本文档的定位

本文档是 `{模块中文名称}` 模块的**增量设计**，在 V{N-1} 基础上扩展以下能力：

**本次变更类型**:
- 【新增】{新增内容概述}
- 【修改】{修改内容概述}
- 【废弃】{废弃内容概述（如无则删除此行）}

**变更范围**:
- {X}张数据表变更（`{表1}` 新增字段、`{表2}` 新增索引 等）
- {Y}个API接口变更（新增 {A} 个、修改 {B} 个）
- {Z}项数据修正（种子数据/迁移脚本 等）

### 2. 本文档的核心原则

- ✅ **增量为主，不替代原有设计**: V{N-1} 中所有表、API、规范保持不变
- ✅ **向后兼容**: 现有功能和数据不受影响
- ✅ **风格一致**: 遵循 V{N-1} 的设计规范和命名约定
- ✅ **可独立开发**: 本文档的变更可作为独立开发任务执行

### 3. 业务价值说明

- {业务价值1：解决什么问题}
- {业务价值2：带来什么能力}
- {业务价值3：对用户体验的提升}

---

## 📚 依赖文档清单

本文档依赖以下文档，开发时需参考：

| 序号 | 文档名称 | 依赖关系 | 影响范围 |
|------|---------|---------|---------|
| 1 | 《{V1文档名称}》 | 📋 **基线文档** | 本文档在此基础上增量扩展 |
| 2 | 《{关联模块文档}》 | 📖 **参考文档** | {引用的规范、共享的表} |
| 3 | 《{主设计文档}》 | 📖 **规范来源** | {遵循的设计规范} |

**依赖关系图**:

```
                    本文档（V{N}增量设计）
                         ↓
            ┌────────────┴────────────┐
            ↓                         ↓
   【基线文档】                  【规范来源】
            ↓                         ↓
  V{N-1}设计文档              v6主设计文档
            ↓
   【关联模块】
            ↓
   {关联模块文档}
```

---

## 🔍 变更驱动力与问题分析

### 1. 变更背景

> 说明为什么需要这次变更，是什么触发了这次设计迭代。

| 时间 | 事件 | 影响 |
|------|------|------|
| {日期} | {V1发布/上线} | {基线状态} |
| {日期} | {触发事件} | {发现的问题} |
| {日期} | 本次变更 | {预期解决} |

### 2. 现有问题分析

#### 问题 1：{问题标题}（严重程度：P{N}）

**现象**:
- {问题的具体表现}

**影响**:
- **业务影响**: {对业务的影响}
- **技术影响**: {对系统的影响}
- **用户体验**: {对用户的影响}

**修改目标**:
- ✅ {修改目标1}
- ✅ {修改目标2}

#### 问题 2：{问题标题}（严重程度：P{N}）

**现象**:
- {问题描述}

**影响**:
- {影响分析}

**修改目标**:
- ✅ {修改目标}

### 3. 变更决策记录

> 记录本次变更中的关键设计决策，便于后续追溯。

#### 决策 1：{决策标题}

**背景**: {为什么需要做这个决策}

**方案对比**:

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 方案A：{描述} | {优点} | {缺点} | ✅ 采用 / ❌ 放弃 |
| 方案B：{描述} | {优点} | {缺点} | ✅ 采用 / ❌ 放弃 |

**最终决策**: {选择的方案及理由}

**影响范围**: {这个决策影响哪些表/API/流程}

#### 决策 2：{决策标题}

**背景**: {背景}

**最终决策**: {决策及理由}

---

## 📐 变更范围总览

### 1. 数据库变更清单

| 变更类型 | 表名 | 变更内容 | 影响的现有功能 |
|----------|------|----------|---------------|
| 【新增字段】 | `{table}` | `{column} {type}` | {影响说明} |
| 【新增索引】 | `{table}` | `idx_{table}_{column}` | {影响说明} |
| 【新增表】 | `{table}` | 新建表 | {影响说明} |
| 【修改字段】 | `{table}` | {修改说明} | {影响说明} |

### 2. API 变更清单

| 变更类型 | 方法 | 路径 | 说明 |
|----------|------|------|------|
| 【新增】 | {METHOD} | `/api/v1/{path}` | {功能说明} |
| 【修改】 | {METHOD} | `/api/v1/{path}` | {修改说明} |
| 【废弃】 | {METHOD} | `/api/v1/{path}` | {废弃说明，替代方案} |

### 3. 数据修正清单

| 变更类型 | 数据对象 | 变更内容 | 影响范围 |
|----------|----------|----------|----------|
| 【种子数据】 | {数据名} | {修正说明} | {影响的表/功能} |
| 【迁移脚本】 | {表名} | {迁移内容} | {影响的表/功能} |

---

## 1. 数据库变更设计（DDL）

### 1.1 【新增字段】`{table_name}` 表新增 `{column_name}`

**变更原因**: {为什么需要新增这个字段}

```sql
-- ==========================================================
-- 变更：{table_name} 新增 {column_name} 字段
-- 说明：{字段用途}
-- ==========================================================

ALTER TABLE {table_name}
    ADD COLUMN {column_name} {type} {约束};

-- 新增索引（如需要）
CREATE INDEX idx_{table}_{column} ON {table_name}({column_name});

-- 字段注释
COMMENT ON COLUMN {table_name}.{column_name} IS '{字段中文说明}';
```

**向后兼容说明**:
- 现有数据: {默认值说明，现有记录如何处理}
- 现有查询: {现有查询是否受影响}
- 现有API: {现有API是否需要修改}

### 1.2 【新增表】`{table_name}`（{中文描述}）

```sql
-- ==========================================================
-- 表：{table_name}（{中文描述}）
-- 说明：{表的用途说明}
-- ==========================================================

CREATE TABLE {table_name} (
    id UUID PRIMARY KEY,
    {字段定义},
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_{table}_{column} FOREIGN KEY ({column})
        REFERENCES {ref_table}(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_{table}_{column} ON {table_name}({column});

-- 表注释
COMMENT ON TABLE {table_name} IS '{中文表描述}';
COMMENT ON COLUMN {table_name}.id IS '{字段中文说明}';
```

### 1.3 【修改字段】`{table_name}` 表修改 `{column_name}`

**变更原因**: {为什么需要修改}

**变更前**:
```sql
-- 原定义
{column_name} {old_type} {old_constraint}
```

**变更后**:
```sql
-- 新定义
ALTER TABLE {table_name}
    ALTER COLUMN {column_name} TYPE {new_type};
```

**向后兼容说明**:
- {兼容性分析}

---

## 2. Pydantic Schemas 变更

### 2.1 【新增 Schema】

```python
class {Entity}{NewSchema}(BaseModel):
    """{说明}"""
    {field}: {type} = Field(..., description="{描述}")
```

### 2.2 【修改 Schema】

```python
# 变更前
class {Entity}Item(BaseModel):
    {existing_field}: {type}

# 变更后（标注新增字段）
class {Entity}Item(BaseModel):
    {existing_field}: {type}
    {new_field}: {type}  # 【V{N}新增】{说明}
```

---

## 3. API 接口变更设计

### 3.1 【新增】{接口中文名称}

**Endpoint**: `{METHOD} /api/v1/{path}`

**描述**: {功能描述}

**认证**: {公开访问/JWT认证/管理员权限}

**请求参数**:
- `{param}` ({type}, {required/可选}): {描述}

**成功响应** (`{statusCode}`):
```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-06-07T10:00:00Z"
}
```

**执行流程**:

1. **接收请求**: {说明}
2. **JWT验证**: {说明}
3. **权限检查**: {说明}
4. **参数校验**: {说明}
5. **业务逻辑**: {说明}
6. **数据库操作**: {说明}
7. **返回响应**: {说明}

**HTTP状态码**:
- `200 OK`: {成功说明}
- `401 Unauthorized`: JWT Token无效（code=3001）
- `404 Not Found`: {资源}不存在（code=2001）

### 3.2 【修改】{接口中文名称}

**Endpoint**: `{METHOD} /api/v1/{path}`

**变更说明**: {修改了什么，为什么修改}

**变更前**:
- {原行为描述}

**变更后**:
- {新行为描述}

**向后兼容**:
- {现有调用是否受影响}

---

## 4. 执行流程变更说明

### 4.1 {操作名称}变更后的完整流程

```
1. API路由接收{METHOD}请求
   → 2. JWT验证（Depends(get_current_user)）
   → 3. 权限检查（_check_admin_permission）
   → 4. Pydantic校验（{Schema}）
   → 5. {原有步骤}
   → 6. 【V{N}新增】{新增步骤}
   → 7. {原有步骤}
   → 8. 提交事务（db.commit()）
   → 9. 返回JSON响应
```

---

## 5. 向后兼容性分析

### 5.1 数据兼容性

| 兼容项 | 状态 | 说明 |
|--------|------|------|
| 现有数据 | ✅ 兼容 | {说明现有数据如何处理} |
| 现有查询 | ✅ 兼容 | {说明现有查询是否受影响} |
| 现有外键 | ✅ 兼容 | {说明外键关系是否受影响} |

### 5.2 API 兼容性

| 兼容项 | 状态 | 说明 |
|--------|------|------|
| 现有端点 | ✅ 兼容 | {说明现有端点是否修改行为} |
| 现有请求格式 | ✅ 兼容 | {说明请求格式是否变化} |
| 现有响应格式 | ✅ 兼容 | {说明响应格式是否变化} |

### 5.3 不兼容变更（如有）

> ⚠️ 如存在不兼容变更，在此列出并提供迁移指南。

| 变更项 | 不兼容说明 | 迁移方案 |
|--------|-----------|----------|
| {变更项} | {为什么不兼容} | {如何迁移} |

---

## 6. 数据迁移与种子数据修正

### 6.1 迁移脚本

```sql
-- ==========================================================
-- 迁移脚本：{迁移名称}
-- 说明：{迁移目的}
-- 执行方式：docker exec {container} psql -U {user} -d {db} -f /app/migrations/{filename}.sql
-- ==========================================================

-- 步骤1：{说明}
{SQL语句}

-- 步骤2：{说明}
{SQL语句}

-- 验证
SELECT COUNT(*) FROM {table} WHERE {condition};
```

### 6.2 种子数据修正

**修正原因**: {为什么需要修正种子数据}

**数据来源**: {权威数据来源说明}

**修正方案**:

| 原数据 | 修正后 | 修正原因 |
|--------|--------|----------|
| {原值} | {新值} | {原因} |

**种子数据变更清单**:
- {变更1}
- {变更2}

---

## 7. 错误码变更（如有新增）

| 错误码 | 说明 | 使用场景 |
|--------|------|----------|
| `{code}` | {说明} | {场景} |

---

## 8. 部署检查清单

### 8.1 部署前准备

- [ ] 数据库迁移脚本已执行（{变更的表名}）
- [ ] 种子数据已更新（如涉及）
- [ ] 新增索引已创建
- [ ] 环境变量已配置（如有新增）

### 8.2 部署步骤

```bash
# 步骤1：执行数据库迁移
docker exec {container} psql -U {user} -d {db} -f /app/migrations/{filename}.sql

# 步骤2：更新种子数据（如需要）
docker exec {container} sh -c "cd /app && python -m app.seed"

# 步骤3：重启服务
docker compose restart {service}
```

### 8.3 部署后验证

- [ ] 新增 API 端点可正常访问
- [ ] 现有 API 端点行为未变化
- [ ] 数据库迁移执行成功，无报错
- [ ] 种子数据已正确更新

### 8.4 回滚方案

> 如部署出现问题，如何回滚到变更前状态。

```sql
-- 回滚步骤1：{说明}
{回滚SQL}

-- 回滚步骤2：{说明}
{回滚SQL}
```

---

## 9. 测试建议

### 9.1 新增功能测试

- {测试场景1}
- {测试场景2}

### 9.2 回归测试

- {现有功能回归验证1}
- {现有功能回归验证2}

### 9.3 数据验证

- {数据一致性验证}
- {迁移脚本验证}

---

## 📝 文档修订历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|----------|------|
| V{N}.0 | {YYYY-MM-DD} | 基于 V{N-1} 增量扩展：{概述变更内容} | {作者} |

---

## 最终变更路由表

| 变更类型 | Domain | Endpoint | Method | Auth | 说明 |
|----------|--------|----------|--------|------|------|
| 【新增】 | {domain} | /api/v1/{path} | {METHOD} | {Auth} | {说明} |
| 【修改】 | {domain} | /api/v1/{path} | {METHOD} | {Auth} | {说明} |

---

**文档结束** ✅
