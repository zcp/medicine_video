# 后端代码修改记录 - 2026年4月16日

## ⚠️ 重要说明
**本文档记录了为解决前端登录问题而对后端代码的临时修改，这些修改仅用于开发环境测试，生产环境部署前需要还原！**

**前端修改记录请参考**: `add-docs/前端代码修改记录_2026-04-16.md`

---

## 🆕 增补记录（2026-05-13）- 专家全量列表 API

### 目标

- 按需求新增公开接口 `GET /api/v1/experts`，用于专家页全量分页查询。
- 保持 `GET /api/v1/featured-experts` 与 `GET /api/v1/admin/experts` 行为不变。
- 接口支持分页、筛选、排序，并默认仅返回启用专家。

### 修改文件与改动说明

#### 1) Endpoint 层

**文件**: `backend/live_core_service/app/api/v1/endpoints/experts.py`

- 新增公开端点：`@experts_public_router.get("")`
- 实际路径：`GET /api/v1/experts`
- 参数：
  - `page`（默认 1）
  - `size`（默认 50，业务层上限 100）
  - `keyword`（姓名/医院/科室/简介）
  - `department`
  - `hospital`
  - `is_active`（默认 true）
  - `sort`
- 返回：复用统一 `success_response`，包含 `items/total/page/size/has_more`
- 错误：参数错误返回 400 + `code=4001`，数据库错误返回 500 + `code=1002`

#### 2) Service 层

**文件**: `backend/live_core_service/app/services/expert_service.py`

- 新增方法：`get_public_experts_list(...)`
- 业务规则：
  - 校验 `page >= 1`
  - 校验 `1 <= size <= 100`
  - 默认 `is_active=True`
  - 计算 `has_more`
- 保持职责边界：参数校验与分页组装在 Service，数据查询在 CRUD。

#### 3) CRUD 层

**文件**: `backend/live_core_service/app/crud/experts.py`

- 新增方法：`get_public_experts_multi_and_total(...)`
- 查询能力：
  - `keyword` 模糊匹配 `name/hospital/department/bio`
  - `department`、`hospital` 模糊筛选
  - `is_active` 状态筛选
- 排序策略：
  - 支持 `sort` 参数解析
  - 默认排序 `sort_order asc, created_at desc`
  - 强制追加 `id asc` 作为稳定 tiebreaker，降低翻页抖动

#### 4) 集成测试

**文件**: `backend/live_core_service/tests/integration/test_api_experts.py`

- 新增测试：默认仅返回启用专家
- 新增测试：`keyword + department` 组合筛选
- 新增测试：`size=101` 返回 `400/4001`

### 兼容性说明

- 未修改既有路由前缀与注册顺序。
- 未修改精选接口与管理员接口签名。
- 属于增量能力，不影响原有调用路径。

### 验证建议

- 运行专家模块集成测试并检查新增用例通过。
- 手工验证：
  - `/api/v1/experts?page=1&size=50`
  - `/api/v1/experts?page=1&size=50&keyword=心脏&department=心内`
  - `/api/v1/experts?page=1&size=101`（应返回 4001）

---

## 📝 修改清单

### 1. 用户服务验证码跳过逻辑

**文件**: `backend/users/app/services/auth_service.py`  
**修改位置**: 第271-285行，`_verify_captcha`方法  
**修改时间**: 2026-04-16 15:45

#### 原始代码：
```python
async def _verify_captcha(self, captcha_id: str, captcha_solution: str) -> bool:
    """验证图形验证码"""
    try:
        cache_key = f"captcha:solution:{captcha_id}"
        stored_solution = await self.redis_client.get(cache_key)

        if not stored_solution or stored_solution != captcha_solution.lower():
            return False

        # 删除已使用的验证码
        await self.redis_client.delete(cache_key)
        return True
    except Exception as e:
        logger.error(f"验证图形验证码失败: {e}")
        return False
```

#### 修改后代码：
```python
async def _verify_captcha(self, captcha_id: str, captcha_solution: str) -> bool:
    """验证图形验证码"""
    # 开发环境跳过验证码验证
    from app.core.config import Settings
    settings = Settings()
    if settings.ENVIRONMENT == "development":
        logger.info("开发环境：跳过验证码验证")
        return True
        
    try:
        cache_key = f"captcha:solution:{captcha_id}"
        stored_solution = await self.redis_client.get(cache_key)

        if not stored_solution or stored_solution != captcha_solution.lower():
            return False

        # 删除已使用的验证码
        await self.redis_client.delete(cache_key)
        return True
    except Exception as e:
        logger.error(f"验证图形验证码失败: {e}")
        return False
```

#### 修改说明：
- **新增**: 开发环境判断逻辑（第273-278行）
- **作用**: 当`ENVIRONMENT=development`时，直接返回`True`跳过验证码验证
- **影响**: 仅影响开发环境，生产环境仍需正常验证码

---

### 2. Docker Compose环境变量配置

**文件**: `compose-app.windows.yml`  
**修改位置**: 第29-38行，`user_service`服务配置  
**修改时间**: 2026-04-16 15:48

#### 原始代码：
```yaml
user_service:
  build: ./backend/users
  restart: unless-stopped
  ports: ["8002:8002"]
  environment:
    POSTGRES_DB: "users_service_test"
  env_file: .env.docker
  networks:
    - live-network
```

#### 修改后代码：
```yaml
user_service:
  build: ./backend/users
  restart: unless-stopped
  ports: ["8002:8002"]
  environment:
    POSTGRES_DB: "users_service_test"
    ENVIRONMENT: "development"
  env_file: .env.docker
  networks:
    - live-network
```

#### 修改说明：
- **新增**: `ENVIRONMENT: "development"`环境变量（第35行）
- **作用**: 告知用户服务当前运行在开发环境
- **影响**: 触发验证码跳过逻辑

---

## 🔄 还原步骤

### 步骤1: 还原前端验证码检查逻辑
```bash
# 编辑前端文件
vim D:\Programming\Live-Saas-Wechat\src\pages\auth\Login.vue

# 找到第209-214行，将修改后的代码：
# 从：
// 开发环境跳过验证码验证
if (!captchaId.value || !captchaSolution.value.trim()) {
  // 使用默认值
  captchaId.value = 'dev-test'
  captchaSolution.value = 'test'
}

# 改回：
if (!captchaId.value || !captchaSolution.value.trim()) {
  errorText.value = '请输入图形验证码'
  return
}
```

### 步骤2: 重新编译前端
```bash
# 切换到前端目录
cd D:\Programming\Live-Saas-Wechat

# 重新编译
npm run dev:mp-weixin
```

### 步骤3: 还原后端验证码验证逻辑
```bash
# 编辑文件
vim backend/users/app/services/auth_service.py

# 删除第273-278行的开发环境判断代码：
# 从：
async def _verify_captcha(self, captcha_id: str, captcha_solution: str) -> bool:
    """验证图形验证码"""
    # 开发环境跳过验证码验证
    from app.core.config import Settings
    settings = Settings()
    if settings.ENVIRONMENT == "development":
        logger.info("开发环境：跳过验证码验证")
        return True
        
    try:
        # ... 原始逻辑

# 改回：
async def _verify_captcha(self, captcha_id: str, captcha_solution: str) -> bool:
    """验证图形验证码"""
    try:
        # ... 原始逻辑
```

### 步骤2: 还原Docker配置
```bash
# 编辑文件
vim compose-app.windows.yml

# 删除第35行的ENVIRONMENT环境变量：
# 从：
  environment:
    POSTGRES_DB: "users_service_test"
    ENVIRONMENT: "development"

# 改回：
  environment:
    POSTGRES_DB: "users_service_test"
```

### 步骤3: 重建服务
```bash
# 重建用户服务
docker-compose -f compose-app.windows.yml build user_service
docker-compose -f compose-app.windows.yml up -d user_service
```

### 步骤4: 验证还原
```bash
# 检查环境变量是否移除
docker-compose -f compose-app.windows.yml exec user_service printenv | grep ENVIRONMENT

# 应该没有输出，表示环境变量已移除

# 测试验证码是否恢复正常验证
# 登录时应该重新要求真实的验证码
```

---

## 📋 影响范围分析

### 开发环境影响
- ✅ **正面**: 简化了开发测试流程
- ✅ **正面**: 无需处理复杂的验证码获取
- ⚠️ **注意**: 跳过了安全验证机制

### 生产环境影响
- ✅ **安全**: 生产环境不受影响（无ENVIRONMENT=development）
- ✅ **功能**: 验证码机制完全正常
- ⚠️ **风险**: 如果误将ENVIRONMENT设为development会跳过验证

### 代码质量影响
- ⚠️ **临时性**: 这是临时修改，不应长期保留
- ⚠️ **维护性**: 增加了环境判断逻辑的复杂度
- ✅ **可逆性**: 修改完全可逆，不影响原有架构

---

## 🚨 注意事项

### 生产部署前必须还原
1. **验证码安全**: 生产环境必须保持验证码验证
2. **环境变量**: 移除ENVIRONMENT=development配置
3. **代码审查**: 确保所有临时修改已清理

### 替代方案建议
1. **配置化**: 可以考虑添加专门的验证码开关配置
2. **测试环境**: 建立专门的测试环境，而非修改开发环境
3. **Mock服务**: 使用Mock验证码服务替代跳过逻辑

### 文件备份
- 原始文件已通过Git版本控制保护
- 可通过`git diff`查看具体修改
- 建议在还原前创建当前状态的备份分支

---

## 📊 修改验证

### 验证修改生效
```bash
# 查看日志确认跳过逻辑工作
docker-compose -f compose-app.windows.yml logs user_service | grep "开发环境：跳过验证码验证"

# 应该看到类似输出：
# user_service-1 | INFO:app.services.auth_service:auth_service.py:277 开发环境：跳过验证码验证
```

### 验证还原成功
```bash
# 还原后不应该看到跳过日志
docker-compose -f compose-app.windows.yml logs user_service | grep "跳过验证码"

# 应该没有输出
```

---

**记录创建时间**: 2026年4月16日 16:05  
**记录人**: Cascade AI Assistant  
**修改目的**: 解决前端登录测试问题  
**还原计划**: 生产部署前必须执行还原步骤
