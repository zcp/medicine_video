# 健康检查模块设计文档

**版本**: V2.1  
**修订日期**: 2026-01-06  
**状态**: 设计完成  
**基于**: 《直播核心功能设计文档_v6_后端新增api接口和模块设计文档-v2.md》Section 4.19

**⚠️ 重要声明**：
- **本文档是相对于已有后端设计文档的增量设计**
- 本文档定义的所有API接口都是**新增**的，是对《直播核心功能设计文档_v6_深度融合最终版.md》的**补充和扩展**
- 本文档**不修改**v6主文档中已定义的任何API接口、数据表结构或业务逻辑
- 本文档遵循v6主文档的所有设计规范（JWT字段、HTTP方法、响应格式、错误码体系等），确保与已有设计**完全兼容**

---

## 📋 核心定位说明

**本文档是相对于已有后端设计文档的增量设计**：
- 本文档定义的所有API接口都是**新增**的，是对《直播核心功能设计文档_v6_深度融合最终版.md》的**补充和扩展**
- 本文档**不修改**v6主文档中已定义的任何API接口、数据表结构或业务逻辑
- 本文档**不重复定义**已在v6主文档中定义的数据表，仅新增API接口
- 本文档遵循v6主文档的所有设计规范（JWT字段、HTTP方法、响应格式、错误码体系等），确保与已有设计**完全兼容**
- 本文档采用**最小幅度增量修改**原则：只新增必要的API，不破坏现有功能

---

## 📚 依赖文档清单

| 序号 | 文档名称 | 依赖关系 | 影响范围 |
|------|---------|---------|---------|
| 1 | 《直播核心功能设计文档_v6_深度融合最终版.md》 | 📋 **主设计文档** | JWT字段、API规范、响应格式、错误码体系 |
| 2 | 《配置与安全优化方案-实施指南.md》 | 🔒 **安全规范** | 健康检查端点要求、日志脱敏、配置验证 |
| 3 | 《直播核心功能设计文档_v6_增加权限设计版.md》 | 🔐 **权限规范** | Strict Auth / Optional Auth模式 |

**⚠️ 重要说明**：
- Tab与留言功能的完整设计在《直播核心功能设计文档v3-增加tab和留言.md》中
- Tab与留言的API设计在《直播核心功能设计文档_v6_深度融合最终版.md》Section 5中
- 本文档不涉及Tab与留言功能

---

## 📖 文档规范说明

### 统一响应结构

所有API接口的响应都遵循以下统一结构：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... } | null,
  "timestamp": "2025-10-23T10:00:00Z"
}
```

### 认证规范

- **公开接口**: 无需JWT Token
- **用户接口**: 需要JWT Token（`Authorization: Bearer <token>`）
- **Admin接口**: 需要JWT Token + `ADMIN` 或 `SUPERADMIN` 角色

### JWT Token格式说明

**Access Token Payload**:
```json
{
  "user_id": "user_uuid",        // 用户公开ID (users.public_id)，注意：与JWT标准的sub字段不同
  "role": "ADMIN",               // 用户角色（REGULAR/MODERATOR/ADMIN/SUPERADMIN）
  "type": "access",              // Token类型
  "exp": 1757364000,             // 过期时间
  "iat": 1757363100,             // 签发时间
  "jti": "token_unique_id"       // Token唯一ID
}
```

**⚠️ 重要说明**：
- **为什么不用`sub`**：虽然JWT RFC 7519标准推荐使用`sub`字段，但本系统为保持代码语义清晰，统一使用`user_id`字段
- **何时做的决策**：2025-12-19权限设计文档定稿时
- **影响范围**：所有Service层方法、CRUD层方法、日志记录等都使用`user_id`命名

**详见**: 
- 《用户模块设计文档authing版+权限设计版.md》Section 7.1.3（JWT Token格式参考）
- 《用户模块---配置与安全优化-实施指南.md》（用户服务的JWT配置要求）
- 《直播核心功能设计文档_v6_增加权限设计版.md》Section 0（字段标准化说明）

### 业务状态码体系

| 业务状态码 | 含义 | 使用场景 |
|:---------|:-----|:---------|
| `200` | 成功 | 操作成功完成 |
| `2001` | 资源不存在 | 查询的资源不存在 |
| `2002` | 资源已存在 | 创建时名称/唯一键冲突 |
| `2003` | 操作被禁止 | 删除被引用的资源等 |
| `2004` | 业务逻辑错误 | 其他业务规则违反 |
| `3001` | 未认证 | Token缺失或无效 |
| `3002` | 权限不足 | 用户角色权限不足 |
| `4001` | 参数校验失败 | 请求参数格式/类型错误 |
| `1002` | 数据库错误 | 系统级数据库异常 |

---

## 1. 设计要点与约定

### 1.1 API设计规范

**遵循《直播核心功能设计文档_v6_深度融合最终版.md》Section 4规范**：

1. **HTTP方法**：
   - `GET`: 查询（幂等）

2. **路径设计**：
   - 公开接口: `/api/v1/<resource>`

3. **认证要求**：
   - 健康检查端点无需认证，公开访问

4. **执行流程规范**：
   - 必须包含详细说明
   - 包含参数校验、业务逻辑、异常处理、日志记录

### 1.2 安全与配置规范

本节内容基于《配置与安全优化方案-实施指南.md》，定义API设计中的安全要求。

#### 1.2.1 日志脱敏规范

所有API接口在记录日志时，必须对以下敏感信息进行脱敏：
- JWT Token完整字符串
- Authorization请求头
- 数据库连接URL中的密码
- 用户密码字段（如果涉及）

**实现方式**：
- 使用统一的日志脱敏工具函数 `sanitize_log_message()`
- JWT Token仅记录前8个字符：`token[:8] + "..."`
- 数据库URL仅记录服务器、端口、数据库名

#### 1.2.2 配置验证要求

**生产环境**：
- 所有敏感配置（数据库密码、JWT密钥）必须设置，否则启动失败
- JWT密钥长度必须至少32字符
- CORS配置必须为具体域名（禁止使用`*`）
- DEBUG模式必须关闭

**开发环境**：
- 缺失配置时发出警告，但不阻止启动
- 提供友好的配置指引信息
- 允许使用CORS通配符`*`

#### 1.2.3 健康检查端点

所有API模块都应提供以下健康检查端点（无需认证）：

| 端点 | 用途 | 返回内容 |
|-----|------|---------|
| `GET /api/v1/health` | 基础健康检查 | 服务状态、版本号 |
| `GET /api/v1/health/ready` | 就绪检查 | 服务状态 + 数据库连接测试 |
| `GET /api/v1/health/config` | 配置检查 | 非敏感配置信息 |

---

## 2. 数据库Schema设计（DDL）

**⚠️ 说明**：健康检查模块不涉及数据库表，无需定义DDL。

---

## 3. Pydantic Schemas定义

**遵循Pydantic V2语法规范**

### 3.1 Health Check Schemas

**新增说明**：
- **新增原因**：集成健康检查端点
- **新增依据**：Section 1.2.3 健康检查端点要求、《配置与安全优化方案-实施指南.md》
- **新增日期**：2026-01-06

```python
from pydantic import BaseModel
from typing import Optional, Dict

class HealthCheckResponse(BaseModel):
    """基础健康检查响应Schema"""
    status: str
    service: str
    version: str
    timestamp: str

class ReadinessCheckResponse(BaseModel):
    """就绪检查响应Schema"""
    status: str
    service: str
    database: str
    error: Optional[str] = None
    timestamp: str

class ConfigCheckResponse(BaseModel):
    """配置检查响应Schema"""
    status: str
    service: str
    config: Dict
    timestamp: str
```

---

## 4. API接口设计（完整CRUD）

### 4.0 通用错误响应格式

所有接口的错误响应遵循统一格式：

```json
{
  "code": 2001,
  "message": "资源不存在",
  "data": {
    "reason": "详细错误原因"
  },
  "timestamp": "2025-10-23T10:00:00Z"
}
```

### 4.1 【新增】Health Check 模块API（健康检查）【V2.1新增】

**新增说明**：
- **新增原因**：集成配置与安全优化方案，支持运维监控和Kubernetes/Docker健康探针
- **新增依据**：《配置与安全优化方案-实施指南.md》、Section 1.2.3健康检查要求
- **新增日期**：2026-01-06

#### 4.1.1 基础健康检查

**Endpoint**: `GET /api/v1/health`

**描述**: 基础健康检查端点，返回服务状态和版本信息

**认证**: 无需认证【V2.1新增权限标注】  
**权限**: 公开访问

**请求参数**: 无

**成功响应** (`200 OK`):
```json
{
  "status": "healthy",
  "service": "live-core-service",
  "version": "2.1.0",
  "timestamp": "2026-01-06T10:00:00Z"
}
```

**执行流程**：
1. **直接返回**：返回硬编码的服务状态信息
2. **无数据库查询**：此端点不执行任何数据库操作，确保快速响应

**HTTP状态码**:
- `200 OK`: 服务正常运行

#### 4.1.2 就绪检查

**Endpoint**: `GET /api/v1/health/ready`

**描述**: 就绪检查端点，包含数据库连接测试，用于Kubernetes/Docker健康探针

**认证**: 无需认证【V2.1新增权限标注】  
**权限**: 公开访问

**请求参数**: 无

**成功响应** (`200 OK`):
```json
{
  "status": "ready",
  "service": "live-core-service",
  "database": "connected",
  "timestamp": "2026-01-06T10:00:00Z"
}
```

**失败响应** (`503 Service Unavailable`):
```json
{
  "status": "not_ready",
  "service": "live-core-service",
  "database": "disconnected",
  "error": "连接数据库失败",
  "timestamp": "2026-01-06T10:00:00Z"
}
```

**执行流程**：
1. **数据库连接测试**：执行 `SELECT 1` 查询
2. **状态判断**：查询成功返回200，失败返回503
3. **返回结果**：返回状态信息

**HTTP状态码**:
- `200 OK`: 服务就绪（数据库连接正常）
- `503 Service Unavailable`: 服务未就绪（数据库连接失败）

#### 4.1.3 配置检查

**Endpoint**: `GET /api/v1/health/config`

**描述**: 配置检查端点，返回非敏感的配置信息，用于运维快速检查配置是否正确加载

**认证**: 无需认证【V2.1新增权限标注】  
**权限**: 公开访问

**请求参数**: 无

**成功响应** (`200 OK`):
```json
{
  "status": "configured",
  "service": "live-core-service",
  "config": {
    "environment": "production",
    "debug": false,
    "database": {
      "server": "localhost",
      "port": 5432,
      "database": "live_streaming_saas"
    },
    "cors_origins": ["https://yourdomain.com"],
    "jwt_algorithm": "HS256"
  },
  "timestamp": "2026-01-06T10:00:00Z"
}
```

**执行流程**：
1. **读取配置**：从 `settings` 对象读取非敏感配置
2. **脱敏处理**：移除所有敏感信息（密码、密钥、Token）
3. **返回结果**：返回安全的配置信息

**HTTP状态码**:
- `200 OK`: 配置正常加载

**安全要求**【V2.1新增】:
- ⚠️ 不得暴露数据库密码
- ⚠️ 不得暴露JWT密钥
- ⚠️ 不得暴露任何Token或API密钥

---

## 5. 执行流程详细说明

### 5.1 基础健康检查执行流程

**API层（router）**：
1. 接收GET请求，无需认证
2. 直接构造响应数据：
   - `status`: "healthy"
   - `service`: "live-core-service"
   - `version`: 从配置读取或硬编码
   - `timestamp`: 当前UTC时间
3. 返回200状态码和JSON响应

**注意**：此端点不涉及Service层和CRUD层，直接返回。

### 5.2 就绪检查执行流程

**API层（router）**：
1. 接收GET请求，无需认证
2. 注入数据库连接 `db: Session = Depends(get_db)`
3. 进入 `try...except` 块

**Service层（可选）**：
1. 调用 `crud.health.check_database_connection(db)`

**CRUD层**：
1. 执行 `SELECT 1` 查询
2. 查询成功：返回True
3. 查询失败：捕获异常，记录ERROR日志，返回False

**API层（继续）**：
4. 根据CRUD层返回结果：
   - 成功：返回200状态码，`status: "ready"`, `database: "connected"`
   - 失败：返回503状态码，`status: "not_ready"`, `database: "disconnected"`, `error: "连接数据库失败"`
5. 记录INFO日志（成功）或ERROR日志（失败）

### 5.3 配置检查执行流程

**API层（router）**：
1. 接收GET请求，无需认证
2. 从 `settings` 对象读取配置
3. 构造配置字典，移除敏感字段：
   - 移除 `database.password`
   - 移除 `jwt_secret_key`
   - 移除 `api_keys`
   - 保留 `database.server`, `database.port`, `database.database`
   - 保留 `environment`, `debug`, `cors_origins`, `jwt_algorithm`
4. 返回200状态码和JSON响应

**注意**：此端点不涉及Service层和CRUD层，直接返回。

---

## 6. 错误处理与事务管理

### 6.1 错误处理

**健康检查模块的错误处理**：

1. **基础健康检查**：
   - 无错误情况，始终返回200

2. **就绪检查**：
   - 数据库连接失败：返回503状态码，记录ERROR日志
   - 异常捕获：捕获所有数据库异常，统一返回503

3. **配置检查**：
   - 配置读取失败：返回500状态码，记录ERROR日志
   - 异常捕获：捕获所有配置读取异常，统一返回500

### 6.2 事务管理

**健康检查模块不涉及事务**：
- 基础健康检查：无数据库操作
- 就绪检查：只读查询（`SELECT 1`），无需事务
- 配置检查：无数据库操作

---

## 7. 性能优化建议

### 7.1 健康检查端点性能优化

1. **基础健康检查**：
   - 无数据库查询，响应时间应 < 1ms
   - 可考虑使用缓存（如Redis）存储版本信息

2. **就绪检查**：
   - 使用连接池，避免创建新连接
   - 查询超时设置：建议5秒
   - 可考虑使用轻量级查询（如`SELECT 1`）

3. **配置检查**：
   - 配置信息可缓存，避免频繁读取
   - 响应时间应 < 10ms

---

## 8. 测试建议

### 8.1 单元测试

1. **基础健康检查**：
   - 测试返回状态码200
   - 测试响应格式正确
   - 测试版本号正确

2. **就绪检查**：
   - 测试数据库连接正常时返回200
   - 测试数据库连接失败时返回503
   - 测试响应格式正确

3. **配置检查**：
   - 测试返回状态码200
   - 测试响应格式正确
   - 测试敏感信息已脱敏

### 8.2 集成测试

1. **健康检查端点集成测试**：
   - 测试所有端点可访问
   - 测试响应格式符合规范
   - 测试数据库连接状态正确反映

### 8.3 性能测试

1. **响应时间测试**：
   - 基础健康检查：< 1ms
   - 就绪检查：< 100ms（包含数据库查询）
   - 配置检查：< 10ms

---

## 9. 错误码对照表

| 业务场景 | 错误码 | 错误消息 | 使用接口 |
|---------|-------|---------|---------|
| 操作成功 | 200 | success | 所有接口 |
| 服务未就绪 | 503 | Service Unavailable | 就绪检查（数据库连接失败） |

---

## 10. 后续开发建议

### 10.1 优先级P0（第一批实现）

1. **基础健康检查端点**：`GET /api/v1/health`
2. **就绪检查端点**：`GET /api/v1/health/ready`
3. **配置检查端点**：`GET /api/v1/health/config`

### 10.2 优先级P1（第二批实现）

1. **配置缓存**：缓存配置信息，提升响应速度
2. **监控集成**：集成Prometheus/Grafana监控

### 10.3 优先级P2（优化阶段）

1. **性能优化**：响应时间优化
2. **日志完善**：完善日志记录

---

**文档结束**

