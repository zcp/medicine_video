# 首页与搜索模块增量开发设计文档 - 焦点图管理端分页列表接口

**版本**: 增量版  
**基于**: 《直播核心功能设计文档_v6_首页与搜索模块设计文档-焦点图-首页API-搜索API.md》  
**状态**: 增量设计（与主设计文档配套使用，冲突时以主文档为准，本文档仅描述增量）

---

## 1. 核心定位说明

### 1.1 阅读顺序

**请先阅读主设计文档全文**（尤其 Section 1 文档规范说明、Section 2.2 URL 路径规范、Section 2.3 查询参数规范、Section 2 统一分页格式、Section 4.1 Featured Content 模块 API、Section 6 权限设计规范），再阅读本文档。本文档仅描述在上述基础上的**增量变更**，不重复主文档已有内容。

### 1.2 增量范围

- **目标**：新增管理端焦点图分页列表接口，供管理页面展示全部焦点图（含已禁用、未上线、已过期），支持分页。
- **原则**：最小幅度修改；不修改既有公开接口 `GET /api/v1/featured-content`；不涉及数据库、Model、Schema 变更；遵循主文档统一分页格式与 Admin 鉴权规范。

### 1.3 与主文档关系

- 主文档 Section 4.1 已定义焦点图公开列表（4.1.1）、创建（4.1.2）、更新（4.1.3）、删除（4.1.4）；本节在 **4.1 下新增 4.1.2 获取焦点图列表（Admin，分页）**，原 4.1.2～4.1.4 顺延为 4.1.3～4.1.5（本文档仅描述新增接口，不修改主文档章节编号，由主文档吸纳时统一调整）。

---

## 2. 依赖与参考

| 序号 | 文档名称 | 依赖关系 | 影响范围 |
|------|----------|----------|----------|
| 1 | 《直播核心功能设计文档_v6_首页与搜索模块设计文档-焦点图-首页API-搜索API.md》 | 主设计文档 | 响应结构、分页格式、Admin 鉴权、API 路径规范 |
| 2 | 《首页与搜索模块设计文档-焦点图-首页API-搜索API---CRUD层代码生成提示词-Phase1-焦点图.md》 | CRUD 基准 | 函数命名、签名风格、执行流程 |
| 3 | 《首页与搜索模块设计文档-焦点图-首页API-搜索API---Service层和API层代码生成提示词-Phase1-焦点图.md》 | Service/API 基准 | 权限守卫、方法/端点格式 |

---

## 3. 修改点总览

| 序号 | 修改位置 | 变更内容 |
|------|----------|----------|
| 1 | 主设计文档 Section 4.1 | 新增「4.1.2 获取焦点图列表（Admin，分页）」；Endpoint `GET /api/v1/admin/featured-content`，Query `page`、`size`，响应为统一分页格式 |
| 2 | CRUD `app/crud/homepage_search.py` | 新增函数 `get_featured_content_list_paginated(db, page, size)`，不施加 is_active/start_at/end_at 过滤，按 sort_order 排序，返回 `Tuple[List[FeaturedContent], int]` |
| 3 | Service `app/services/homepage_search_service.py` | 新增方法 `get_featured_content_list_admin_paginated(db, page, size, current_user_id, role)`，调用 `_check_admin_permission(role)` 后调用 CRUD 分页函数，返回统一分页响应 |
| 4 | API `app/api/v1/endpoints/homepage_search.py` | 新增管理员分页列表端点；需在 `/api/v1/admin/featured-content` 暴露，故新增 `featured_content_admin_router`，挂载路径 `GET /featured-content`，在 `api.py` 中以 prefix `/admin` 注册 |
| 5 | API `app/api/v1/api.py` | 注册 `featured_content_admin_router`，prefix=`/admin`，使 `GET /api/v1/admin/featured-content` 可用 |

---

## 4. API 变更（新增端点）

### 4.1.2 获取焦点图列表（Admin，分页）

**Endpoint**: `GET /api/v1/admin/featured-content`

**描述**: 管理员分页查询焦点图列表，用于管理端焦点图配置页。返回**全部**焦点图（含已禁用、未上线、已过期），不做 is_active 与定时上下线时间过滤。

**认证**: Admin Auth（需 `ADMIN` 或 `SUPERADMIN` 角色）；使用 `Depends(get_current_user)`，Service 层调用 `_check_admin_permission(role)`。

**请求参数 (Query)**:

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `page` | int | 否 | 1 | 页码 |
| `size` | int | 否 | 10 | 每页数量（建议最大 100） |

**成功响应** (`200 OK`): 遵循主文档「统一分页格式」：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 25,
    "page": 1,
    "size": 10,
    "items": [
      {
        "id": "featured_uuid_1",
        "title": "全国骨科学术研讨会",
        "subtitle": "2026年1月10-12日",
        "image_url": "/media/featured/banner1.jpg",
        "target_type": "session",
        "target_id": "session_uuid_123",
        "target_url": null,
        "sort_order": 0,
        "is_active": true,
        "start_at": "2025-10-25T00:00:00Z",
        "end_at": "2025-11-25T23:59:59Z",
        "created_at": "2025-10-23T14:00:00Z",
        "updated_at": "2025-10-23T14:00:00Z"
      }
    ]
  },
  "timestamp": "2026-01-06T10:00:00Z"
}
```

**说明**: 每条 `items` 元素为完整的 `FeaturedContentItem`（与主文档 Section 3.1 一致），与公开接口差异：本接口需鉴权、返回分页结构、不做启用/有效期过滤。

**执行流程**:

1. **鉴权**: API 层 `Depends(get_current_user)`；Service 层 `_check_admin_permission(role)`，非管理员返回 403（业务码 `3002`）。
2. **解析查询参数**: 读取 `page`、`size`，校验合法性（如 `page >= 1`，`size` 在 1～100）。
3. **CRUD 查询**: 调用 `get_featured_content_list_paginated(db, page, size)`，不添加 is_active、start_at、end_at 条件，按 sort_order 升序，OFFSET/LIMIT 分页，并查总数。
4. **序列化**: 将当前页记录序列化为 `FeaturedContentItem` 列表，放入 `data.items`。
5. **返回**: 按主文档统一分页格式返回 `data: { total, page, size, items }`。

**错误响应**:

- `401`: 未提供 Token 或 Token 无效。
- `403` / 业务码 `3002`: 非 ADMIN/SUPERADMIN。
- `400` / `4001` 或 `4002`: `page`/`size` 非法（若实现参数校验）。

---

## 5. 数据库变更

无。本次增量不涉及表结构、索引、触发器变更。

---

## 6. Schema 变更

无。复用主文档 Section 3.1 的 `FeaturedContentItem`。

---

## 7. 执行流程与错误处理

- 执行流程见上文「4. API 变更」中执行流程。
- 错误码与主文档 Section 5 业务状态码、Section 6 错误码一致；日志脱敏遵循主文档 Section 4（如 UUID 仅记录前 8 位）。

---

## 8. 测试建议

- 管理员分页列表：使用 ADMIN/SUPERADMIN Token 调用 `GET /api/v1/admin/featured-content?page=1&size=10`，断言 200、`data.total`、`data.items` 长度、`data.page`/`data.size`。
- 权限：无 Token 或 REGULAR Token 调用同一 URL，断言 401 或 403。
- 分页边界：`page=1&size=1`、`size` 超过 100（若实现校验）的响应符合预期。

---

## 9. 文档结束语

本文档为主设计文档的补充，须与主设计文档配套使用；冲突时以主文档为准，本文档仅描述增量。功能增强的完整设计详见主文档 Section 2（文档规范说明）、Section 4.1（Featured Content 模块 API）、Section 6（权限设计规范）。
