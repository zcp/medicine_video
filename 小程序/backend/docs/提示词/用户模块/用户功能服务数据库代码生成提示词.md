
-----

### **高效 AI 提示词：生成数据库初始化脚本**

#### **1. 角色定义 (Role Definition)**

你是一名精通 SQLAlchemy 和项目脚手架的资深 Python 后端工程师。你的任务是为 `users` 项目创建一个用于初始化数据库的独立脚本。

#### **2. 任务目标 (Task Objective)**

生成一个名为 `create_tables.py` 的 Python 脚本，该脚本位于项目的 `users/scripts/` 目录下。当执行这个脚本时，它会连接到数据库，
并根据 `users/app/models/users.py` 中定义的 SQLAlchemy 模型，自动创建所有对应的数据库表。

#### **3. 核心上下文信息 (Core Context Information)**

这是成功生成代码所必需的背景信息：

**3.1. 项目文件结构 (Project File Structure)**
你的脚本必须基于以下项目结构来正确导入模块：

```
users/
├── app/
│   ├── __init__.py
│   ├── database.py         # <-- 关键：定义了 SQLAlchemy 的 `engine`
│   ├── models/
│   │   ├── __init__.py
│   │   └── users.py    # <-- 关键：定义了所有模型和 `Base`
│   └── scripts/
        └── create_tables.py    # <-- 这是你要生成的目标文件
```

下面是一个 database..py的一个成功案例
``` python
from sqlalchemy import create_engine
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
import logging
import os

# 配置日志
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# 创建数据库引擎
try:
    # 使用环境变量或默认值
    POSTGRES_USER = os.getenv("POSTGRES_USER", "postgres")
    POSTGRES_PASSWORD = os.getenv("POSTGRES_PASSWORD", "324zq999")
    POSTGRES_SERVER = os.getenv("POSTGRES_SERVER", "localhost")
    POSTGRES_PORT = os.getenv("POSTGRES_PORT", "5432")
    POSTGRES_DB = os.getenv("POSTGRES_DB", "live_core_test")

    # 同步连接字符串（用于初始化）
    DATABASE_URL = f"postgresql://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_SERVER}:{POSTGRES_PORT}/{POSTGRES_DB}"
    # 异步连接字符串（用于应用）
    ASYNC_DATABASE_URL = f"postgresql+asyncpg://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_SERVER}:{POSTGRES_PORT}/{POSTGRES_DB}"
    
    logger.info(f"尝试连接数据库: {DATABASE_URL}")

    # 同步引擎（用于初始化）
    engine = create_engine(
        DATABASE_URL,
        echo=True  # 打印 SQL 语句，方便调试
    )

    # 异步引擎（用于应用）
    async_engine = create_async_engine(
        ASYNC_DATABASE_URL,
        echo=True  # 打印 SQL 语句，方便调试
    )

    # 测试连接
    with engine.connect() as conn:
        logger.info("数据库连接成功！")
except Exception as e:
    logger.error(f"数据库连接失败: {str(e)}")
    raise

# 创建会话工厂
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

# 创建异步会话工厂
AsyncSessionLocal = sessionmaker(
    bind=async_engine,
    class_=AsyncSession,
    autocommit=False,
    autoflush=False
)

# 创建基类
Base = declarative_base()

# 获取数据库会话（同步）
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# 获取异步数据库会话
#async def get_async_db() -> AsyncSession:
#    async with AsyncSessionLocal() as session:
#        try:
#            yield session
#        finally:
#            await session.close()

async def get_async_db() -> AsyncSession:
    async with AsyncSessionLocal() as session:
        try:
            yield session
            if session.is_active:
                await session.commit()  # 显式提交事务
        except Exception as e:
            if session.is_active:
                await session.rollback()
            raise
        finally:
            await session.close()

# 为了保持兼容性，将get_db指向异步版本
get_db = get_async_db 
```

**3.2. 关键文件内容假设 (Key File Content Assumptions)**

  * **`users/app/database.py`**: 这个文件包含一个已经配置好的 SQLAlchemy `engine` 实例。AI 在生成代码时，应假设可以从这里导入 `engine`，即 `from app.database import engine`。
  * **`users/app/models/users.py`**:
      * 这个文件从 app.database 导入了共享的 Base。
      * 所有的模型（如 `User`, `MembershipProduct`, `UserMembership`）都继承自这个 `Base`。
      * **非常重要**：为了让 `Base.metadata` 能够“发现”所有的模型，脚本中必须确保这些模型类所在的模块被导入。

**3.3. 运行环境说明**
这个脚本将从项目的根目录 (`users/`) 执行。因此，AI 生成的导入语句（如 `from app.database import engine`）是有效的。

#### **4. 代码生成具体要求 (Specific Code Generation Requirements)**

1.  **文件名与位置**: 生成的代码是用于 `users/scripts/create_tables.py` 文件的。
2.  **核心逻辑**: 必须使用 `Base.metadata.create_all(bind=engine)` 来创建表。
3.  **模块导入**:
      * 必须从 `app.database` 导入 `engine` 对象 和` Base`对象。
      * **必须显式导入所有模型类** 必须从 app.models.users 显式导入所有模型类 (User, MembershipProduct, UserMembership)，以确保 SQLAlchemy 的元数据能够注册它们。 这是一个关键步骤，否则 `create_all` 可能不会创建任何表。
4.  **健壮性**:
      * 使用标准的 `logging` 模块来输出操作信息（如“开始创建...”、“创建成功”、“创建失败”）。
      * 将 `create_all` 调用包裹在 `try...except` 块中，以捕获并报告任何数据库连接或创建过程中的异常。
5.  **可执行性**:
      * 将核心逻辑封装在一个函数内，例如 `init_db()`。
      * 使用 `if __name__ == "__main__"` 块来调用这个函数，使脚本可以直接通过 python -m app.scripts.create_tables 命令执行 (从 users/ 根目录运行)。



#### **5. 最终交付 (Final Deliverable)**

请根据以上所有要求，为 `users/scripts/create_tables.py` 文件生成完整、健壮且可直接运行的 Python 代码。

-----
