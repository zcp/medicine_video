# 专题功能数据库模型一致性验证提示词

## **高效 AI 代码审计提示词：验证 `topic.py` 与设计文档的一致性**

---

### **1. 角色定义 (Role Definition)**

你是一名资深的 Python 后端技术审计员和 SQLAlchemy 2.0 专家。你的核心任务是进行严格的代码审查，确保下文中提供的专题功能 SQLAlchemy ORM 模型代码 (`topic.py`) **百分之百**地、**严格地**遵循了给定的数据库设计规范 (DDL)。

你必须以**挑剔和精确**的态度找出任何细微的偏差，包括但不限于：
- 字段名称不一致
- 数据类型不匹配
- 默认值设置错误
- 索引定义缺失或错误
- 外键约束不正确
- 关联关系定义不完整

---

### **2. 任务目标 (Task Objective)**

你的目标是：

1. **比较**下面提供的【数据库设计规范 (DDL)】和【待审查的 SQLAlchemy 模型代码】
2. **验证**代码是否完全实现了 DDL 中定义的所有表、字段、类型、约束和索引
3. **生成**一份详细的审查报告，明确指出所有一致、不一致或缺失的实现，并提供修正建议

---

### **3. 核心输入 (Core Inputs)**

#### **3.1. 权威的数据库设计规范 (Authoritative Database Schema - DDL)**

这是唯一的设计标准，所有代码实现都必须与此对齐。

**来源**: `docs/03_系统设计/专题功能完整设计文档-修正版.md` Section 7.3.1

```sql
-- LiveCore Service - 专题聚合模型 Schema (V3.2 新增 - 修正版)

-- 枚举类型定义
CREATE TYPE topic_status AS ENUM ('draft', 'published', 'archived');

-- 表 4: topics (专题表 - 顶层实体)
CREATE TABLE topics (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    -- 用户ID，通过JWT Token应用层验证保证引用完整性
    -- 由于采用微服务架构，user_id无法跨数据库外键关联，通过JWT Token验证用户存在性
    user_id UUID NOT NULL,
    
    title VARCHAR(100) NOT NULL,
    description TEXT,
    banner_url VARCHAR(255),
    
    -- 专题状态: draft(草稿), published(已发布), archived(已归档)
    status topic_status NOT NULL DEFAULT 'draft',

    -- 时间戳字段：使用数据库级默认值（对应 SQLAlchemy 的 server_default=func.now()）
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_topics_user_id ON topics(user_id);
CREATE INDEX idx_topics_status ON topics(status);
CREATE INDEX idx_topics_created_at ON topics(created_at DESC);

-- 表 5: topic_categories (专题内分类表 - 中层实体)
CREATE TABLE topic_categories (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    topic_id UUID NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
    name VARCHAR(50) NOT NULL,
    sort_order INT DEFAULT 0,

    -- 时间戳字段：使用数据库级默认值（对应 SQLAlchemy 的 server_default=func.now()）
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_topic_categories_on_topic_id ON topic_categories(topic_id);
CREATE INDEX idx_topic_categories_on_sort_order ON topic_categories(topic_id, sort_order);

-- 表 6: topic_category_rooms (关联表 - 关系层)
CREATE TABLE topic_category_rooms (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    category_id UUID NOT NULL REFERENCES topic_categories(id) ON DELETE CASCADE,
    room_id UUID NOT NULL REFERENCES live_rooms(id) ON DELETE CASCADE,
    sort_order INT DEFAULT 0,
    
    -- 时间戳字段：使用数据库级默认值（对应 SQLAlchemy 的 server_default=func.now()）
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,

    -- 确保同一个直播间在一个分类下只关联一次
    UNIQUE (category_id, room_id)
);

-- 索引
CREATE INDEX idx_tcr_on_category_id ON topic_category_rooms(category_id);
CREATE INDEX idx_tcr_on_room_id ON topic_category_rooms(room_id);
CREATE INDEX idx_tcr_on_category_sort ON topic_category_rooms(category_id, sort_order);
```

---

#### **3.2. 待审查的 SQLAlchemy 模型代码**

**文件**: `backend/live_core_service/app/models/topic.py`

*请在此处提供 `topic.py` 文件的完整内容*
"""
专题聚合功能的数据库模型

本模块包含专题聚合功能所需的三个 SQLAlchemy 模型类：
- Topic: 专题表（顶层实体）
- TopicCategory: 专题内分类表（中层实体）
- TopicCategoryRoom: 专题分类与直播间的关联表（关系层）
"""
import uuid
from datetime import datetime
import enum

# SQLAlchemy 核心导入
from sqlalchemy import Column, String, Text, Integer, ForeignKey, Enum as SAEnum, UniqueConstraint, Index, TIMESTAMP
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func

from app.database import Base


# ==================== 枚举类型定义 ====================

class TopicStatus(str, enum.Enum):
    """专题状态枚举"""
    DRAFT = "draft"          # 草稿
    PUBLISHED = "published"  # 已发布
    ARCHIVED = "archived"    # 已归档


# ==================== 模型类定义 ====================

class Topic(Base):
    """
    专题活动表
    
    用于聚合多个分类和直播间，实现类似聚合页面的功能。
    一个专题可以包含多个分类，每个分类下可以关联多个直播间。
    """
    __tablename__ = "topics"
    
    # 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    
    # 用户ID，对应 users 表的 public_id（通过JWT Token应用层验证保证引用完整性）
    # 注意：不使用 ForeignKey，因为 User 服务可能独立部署
    user_id = Column(
        UUID(as_uuid=True), 
        nullable=False, 
        comment='创建者用户ID，对应 users.public_id'
    )
    
    # 业务字段
    title = Column(String(100), nullable=False, comment='专题标题')
    description = Column(Text, nullable=True, comment='专题描述')
    banner_url = Column(String(255), nullable=True, comment='横幅图URL')
    
    # 专题状态
    status = Column(
        SAEnum(TopicStatus, values_callable=lambda obj: [e.value for e in obj]), 
        nullable=False, 
        default=TopicStatus.DRAFT,
        comment='专题状态: draft(草稿), published(已发布), archived(已归档)'
    )
    
    # 时间戳 - 使用 server_default 和 func.now()
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
    
    # 索引定义
    __table_args__ = (
        Index('idx_topics_user_id', 'user_id'),
        Index('idx_topics_status', 'status'),
        Index('idx_topics_created_at', 'created_at'),
    )
    
    # 关联关系
    categories = relationship(
        "TopicCategory", 
        back_populates="topic", 
        cascade="all, delete-orphan",
        lazy="selectin"
    )
    
    def __repr__(self):
        return f"<Topic(id={self.id}, title={self.title})>"


class TopicCategory(Base):
    """
    专题内分类表
    
    专题下的分类，如按省份、按科室等。
    一个专题可以包含多个分类，每个分类可以关联多个直播间。
    """
    __tablename__ = "topic_categories"
    
    # 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    
    # 外键 - 关联到专题
    topic_id = Column(
        UUID(as_uuid=True), 
        ForeignKey("topics.id", ondelete="CASCADE"), 
        nullable=False,
        comment='所属专题ID'
    )
    
    # 业务字段
    name = Column(String(50), nullable=False, comment='分类名称')
    sort_order = Column(Integer, default=0, nullable=False, comment='排序顺序，数值越小越靠前')
    
    # 时间戳 - 使用 server_default 和 func.now()
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
    
    # 索引和约束定义
    __table_args__ = (
        Index('idx_topic_categories_topic_id', 'topic_id'),
        Index('idx_topic_categories_topic_sort', 'topic_id', 'sort_order'),
        UniqueConstraint('topic_id', 'name', name='uq_topic_category_name'),
    )
    
    # 关联关系
    topic = relationship("Topic", back_populates="categories")
    rooms = relationship(
        "TopicCategoryRoom", 
        back_populates="category", 
        cascade="all, delete-orphan",
        lazy="selectin"
    )
    
    def __repr__(self):
        return f"<TopicCategory(id={self.id}, name={self.name}, sort_order={self.sort_order})>"


class TopicCategoryRoom(Base):
    """
    专题分类与直播间的关联表
    
    用于定义专题分类与直播间的多对多关联关系。
    同一个直播间可以被添加到多个不同的专题分类中。
    """
    __tablename__ = "topic_category_rooms"
    
    # 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    
    # 外键 - 关联到分类
    category_id = Column(
        UUID(as_uuid=True), 
        ForeignKey("topic_categories.id", ondelete="CASCADE"), 
        nullable=False,
        comment='所属分类ID'
    )
    
    # 外键 - 关联到直播间
    room_id = Column(
        UUID(as_uuid=True), 
        ForeignKey("live_rooms.id", ondelete="CASCADE"), 
        nullable=False,
        comment='关联的直播间ID'
    )
    
    # 业务字段
    sort_order = Column(Integer, default=0, nullable=False, comment='在分类下的排序顺序，数值越小越靠前')
    
    # 时间戳 - 使用 server_default 和 func.now()
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
    
    # 索引和约束定义
    __table_args__ = (
        Index('idx_tcr_category_id', 'category_id'),
        Index('idx_tcr_room_id', 'room_id'),
        Index('idx_tcr_category_sort', 'category_id', 'sort_order'),
        UniqueConstraint('category_id', 'room_id', name='uq_category_room'),
    )
    
    # 关联关系
    category = relationship("TopicCategory", back_populates="rooms")
    room = relationship("LiveRoom")
    
    def __repr__(self):
        return f"<TopicCategoryRoom(category_id={self.category_id}, room_id={self.room_id}, sort_order={self.sort_order})>"


---

### **4. 审查清单与验证规则 (Audit Checklist & Validation Rules)**

请根据以下清单逐项进行严格审查：

#### **4.1. 表与模型映射 (Table-to-Model Mapping)**

- [ ] `topics`, `topic_categories`, `topic_category_rooms` 三个表是否都已正确映射为 SQLAlchemy 模型类？
- [ ] 每个模型类的 `__tablename__` 是否与 DDL 表名完全对应？
- [ ] 模型类命名是否符合 Python 规范（大驼峰命名法）？
  - `topics` → `Topic`
  - `topic_categories` → `TopicCategory`
  - `topic_category_rooms` → `TopicCategoryRoom`

---

#### **4.2. 枚举类型定义 (Enum Type Definition)**

- [ ] 是否定义了 `TopicStatus` 枚举类？
- [ ] 枚举类是否正确继承 `str, enum.Enum`？
- [ ] 枚举值是否完整且正确？
  - `DRAFT = "draft"`
  - `PUBLISHED = "published"`
  - `ARCHIVED = "archived"`
- [ ] 枚举值的字符串形式是否与 DDL 中的 ENUM 值一致（小写）？

---

#### **4.3. 字段与列的完全对应 (Field-to-Column Correspondence)**

**⚠️ [强制] 字段名称必须严格对照设计文档**

**关键要求**：
1. **必须从设计文档的 DDL 中提取字段名称**，而不是从已生成的代码中推断。
2. **逐字段对比**：对设计文档 DDL 中的每个字段，检查 ORM 模型中是否存在同名字段。
3. **禁止假设**：如果设计文档中定义的是 `target_id`，而 ORM 模型中使用了 `room_id` 和 `session_id`，这**必须**被标记为不一致，即使逻辑上看似合理。
4. **检查方法**：
   - 第一步：从设计文档 DDL 中提取所有字段名称，形成"设计文档字段清单"
   - 第二步：从 ORM 模型中提取所有字段名称，形成"ORM 模型字段清单"
   - 第三步：逐字段对比，标记差异
   - 第四步：对于任何差异，必须明确说明是"设计文档有但 ORM 没有"还是"ORM 有但设计文档没有"

**示例检查表**（必须包含）：
| 设计文档字段 | ORM 模型字段 | 是否一致 | 备注 |
|------------|------------|---------|------|
| `target_id` | `room_id`, `session_id` | ❌ **不一致** | 设计文档使用 `target_id`，ORM 使用了不同的字段结构 |
| `target_type` | `target_type` | ✅ 一致 | |
| `user_id` | `user_id` | ✅ 一致 | |

**如果发现字段名称不一致，必须标记为"严重问题"，并在报告中明确说明需要修改 ORM 模型以匹配设计文档。**

##### **4.3.1. Topic 模型字段检查**

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `user_id` | [ ] | [ ] | 用户ID，无外键 |
| `title` | [ ] | [ ] | 标题 |
| `description` | [ ] | [ ] | 描述 |
| `banner_url` | [ ] | [ ] | 横幅图URL |
| `status` | [ ] | [ ] | 枚举类型 |
| `created_at` | [ ] | [ ] | 创建时间 |
| `updated_at` | [ ] | [ ] | 更新时间 |

##### **4.3.2. TopicCategory 模型字段检查**

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `topic_id` | [ ] | [ ] | 外键到 topics |
| `name` | [ ] | [ ] | 分类名称 |
| `sort_order` | [ ] | [ ] | 排序 |
| `created_at` | [ ] | [ ] | 创建时间 |
| `updated_at` | [ ] | [ ] | 更新时间 |

##### **4.3.3. TopicCategoryRoom 模型字段检查**

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `category_id` | [ ] | [ ] | 外键到 topic_categories |
| `room_id` | [ ] | [ ] | 外键到 live_rooms |
| `sort_order` | [ ] | [ ] | 排序 |
| `created_at` | [ ] | [ ] | 创建时间 |
| `updated_at` | [ ] | [ ] | 更新时间 |

---

#### **4.4. 数据类型精确性 (Data Type Accuracy)**

##### **4.4.1. UUID 类型检查**

- [ ] 是否从 `sqlalchemy.dialects.postgresql` 导入 `UUID`？
- [ ] 所有 UUID 字段是否使用 `UUID(as_uuid=True)`？
- [ ] 主键 `id` 是否包含 `default=uuid.uuid4`？
- [ ] 是否导入了 `import uuid`？

##### **4.4.2. 字符串类型检查**

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `topics.title` | `VARCHAR(100)` | `String(100)` | [ ] |
| `topics.description` | `TEXT` | `Text` | [ ] |
| `topics.banner_url` | `VARCHAR(255)` | `String(255)` | [ ] |
| `topic_categories.name` | `VARCHAR(50)` | `String(50)` | [ ] |

##### **4.4.3. 数值类型检查**

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `topic_categories.sort_order` | `INT` | `Integer` | [ ] |
| `topic_category_rooms.sort_order` | `INT` | `Integer` | [ ] |

##### **4.4.4. 时间戳类型检查 (关键)**

- [ ] 是否从 `sqlalchemy` 导入 `TIMESTAMP`（不是 `DateTime`）？
- [ ] 是否从 `sqlalchemy.sql` 导入 `func`？
- [ ] 所有时间戳字段是否使用 `TIMESTAMP(timezone=True)`？
- [ ] `created_at` 是否使用 `server_default=func.now()`？
- [ ] `updated_at` 是否使用 `server_default=func.now(), onupdate=func.now()`？
- [ ] ⚠️ **严禁使用** `default=datetime.utcnow`（这是应用层默认值，不符合设计要求）

**正确示例**:
```python
from sqlalchemy import TIMESTAMP
from sqlalchemy.sql import func

created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
```

##### **4.4.5. 枚举类型检查**

- [ ] `status` 字段是否使用 `SAEnum(TopicStatus, values_callable=lambda obj: [e.value for e in obj])`？
- [ ] 是否从 `sqlalchemy` 导入并重命名 `Enum as SAEnum`？
- [ ] 是否避免与 Python 内置 `enum` 模块冲突？

---

#### **4.5. 主键定义 (Primary Keys)**

- [ ] 所有 `id` 字段是否标记为 `primary_key=True`？
- [ ] 所有 `id` 字段是否包含 `default=uuid.uuid4`？
- [ ] 是否在应用层生成 UUID（而非数据库生成）？

---

#### **4.6. 外键与级联规则 (Foreign Keys & Cascade Rules)**

##### **4.6.1. topic_categories.topic_id**

- [ ] 是否定义了 `ForeignKey("topics.id", ondelete="CASCADE")`？
- [ ] `ondelete` 是否正确设置为 `"CASCADE"`（大写）？
- [ ] 是否标记为 `nullable=False`？

##### **4.6.2. topic_category_rooms.category_id**

- [ ] 是否定义了 `ForeignKey("topic_categories.id", ondelete="CASCADE")`？
- [ ] `ondelete` 是否正确设置为 `"CASCADE"`？
- [ ] 是否标记为 `nullable=False`？

##### **4.6.3. topic_category_rooms.room_id**

- [ ] 是否定义了 `ForeignKey("live_rooms.id", ondelete="CASCADE")`？
- [ ] `ondelete` 是否正确设置为 `"CASCADE"`？
- [ ] 是否标记为 `nullable=False`？

##### **4.6.4. topics.user_id (特殊情况)**

- [ ] `user_id` 字段**不应该**定义 `ForeignKey`（因为跨服务）？
- [ ] 是否在注释中说明"通过JWT Token应用层验证"？
- [ ] 是否标记为 `nullable=False`？

---

#### **4.7. 约束和默认值 (Constraints and Defaults)**

##### **4.7.1. NOT NULL 约束**

检查以下字段是否正确设置 `nullable=False`：

**Topic**:
- [ ] `id`
- [ ] `user_id`
- [ ] `title`
- [ ] `status`
- [ ] `created_at`
- [ ] `updated_at`

**TopicCategory**:
- [ ] `id`
- [ ] `topic_id`
- [ ] `name`
- [ ] `sort_order`
- [ ] `created_at`
- [ ] `updated_at`

**TopicCategoryRoom**:
- [ ] `id`
- [ ] `category_id`
- [ ] `room_id`
- [ ] `sort_order`
- [ ] `created_at`
- [ ] `updated_at`

##### **4.7.2. NULL 约束**

检查以下字段是否正确设置 `nullable=True`（或默认）：

**Topic**:
- [ ] `description`
- [ ] `banner_url`

##### **4.7.3. 默认值 (Defaults)**

| 字段 | DDL 默认值 | 应设置为 | 是否正确 |
|------|----------|---------|---------|
| `topics.status` | `'draft'` | `default=TopicStatus.DRAFT` | [ ] |
| `topic_categories.sort_order` | `0` | `default=0` | [ ] |
| `topic_category_rooms.sort_order` | `0` | `default=0` | [ ] |
| 所有 `created_at` | `CURRENT_TIMESTAMP` | `server_default=func.now()` | [ ] |
| 所有 `updated_at` | `CURRENT_TIMESTAMP` | `server_default=func.now(), onupdate=func.now()` | [ ] |

---

#### **4.8. 唯一约束 (Unique Constraints)**

##### **4.8.1. topic_category_rooms 表**

- [ ] 是否在 `__table_args__` 中定义了 `UniqueConstraint('category_id', 'room_id', name='uq_category_room')`？
- [ ] 约束名称是否正确命名？

##### **4.8.2. 可选：topic_categories 表**

- [ ] 是否定义了 `UniqueConstraint('topic_id', 'name', name='uq_topic_category_name')`？
- [ ] （注：DDL 中未明确要求，但为数据一致性建议添加）

---

#### **4.9. 索引定义 (Index Definitions)**

**⚠️ 关键要求**: 所有索引必须使用 `__table_args__` 显式命名，**不能使用** `index=True`

##### **4.9.1. Topic 模型索引**

- [ ] 是否定义了 `Index('idx_topics_user_id', 'user_id')`？
- [ ] 是否定义了 `Index('idx_topics_status', 'status')`？
- [ ] 是否定义了 `Index('idx_topics_created_at', 'created_at')`？
- [ ] 索引名称是否完全匹配 DDL？

##### **4.9.2. TopicCategory 模型索引**

- [ ] 是否定义了 `Index('idx_topic_categories_topic_id', 'topic_id')` 或类似名称？
- [ ] 是否定义了组合索引 `Index('idx_topic_categories_topic_sort', 'topic_id', 'sort_order')` 或类似？
- [ ] 组合索引的字段顺序是否正确？

**DDL 要求对比**:
```sql
CREATE INDEX idx_topic_categories_on_topic_id ON topic_categories(topic_id);
CREATE INDEX idx_topic_categories_on_sort_order ON topic_categories(topic_id, sort_order);
```

##### **4.9.3. TopicCategoryRoom 模型索引**

- [ ] 是否定义了 `Index('idx_tcr_category_id', 'category_id')` 或类似名称？
- [ ] 是否定义了 `Index('idx_tcr_room_id', 'room_id')` 或类似名称？
- [ ] 是否定义了组合索引 `Index('idx_tcr_category_sort', 'category_id', 'sort_order')` 或类似？

**DDL 要求对比**:
```sql
CREATE INDEX idx_tcr_on_category_id ON topic_category_rooms(category_id);
CREATE INDEX idx_tcr_on_room_id ON topic_category_rooms(room_id);
CREATE INDEX idx_tcr_on_category_sort ON topic_category_rooms(category_id, sort_order);
```

##### **4.9.4. 索引定义位置**

- [ ] 所有索引是否都在 `__table_args__` 中定义？
- [ ] 是否从 `sqlalchemy` 导入了 `Index`？

**正确示例**:
```python
from sqlalchemy import Index

__table_args__ = (
    Index('idx_topics_user_id', 'user_id'),
    Index('idx_topics_status', 'status'),
    Index('idx_topics_created_at', 'created_at'),
)
```

---

#### **4.10. 关系完整性 (Relationship Integrity)**

##### **4.10.1. Topic ↔ TopicCategory 关系**

**在 Topic 模型中**:
- [ ] 是否定义了 `relationship("TopicCategory", back_populates="topic", cascade="all, delete-orphan")`？
- [ ] 是否设置了 `cascade="all, delete-orphan"`？
- [ ] 关系名称是否为 `categories`？

**在 TopicCategory 模型中**:
- [ ] 是否定义了 `relationship("Topic", back_populates="categories")`？
- [ ] 关系名称是否为 `topic`？
- [ ] `back_populates` 是否正确指向 `"categories"`？

##### **4.10.2. TopicCategory ↔ TopicCategoryRoom 关系**

**在 TopicCategory 模型中**:
- [ ] 是否定义了 `relationship("TopicCategoryRoom", back_populates="category", cascade="all, delete-orphan")`？
- [ ] 是否设置了 `cascade="all, delete-orphan"`？
- [ ] 关系名称是否为 `rooms`？

**在 TopicCategoryRoom 模型中**:
- [ ] 是否定义了 `relationship("TopicCategory", back_populates="rooms")`？
- [ ] 关系名称是否为 `category`？
- [ ] `back_populates` 是否正确指向 `"rooms"`？

##### **4.10.3. TopicCategoryRoom → LiveRoom 关系**

**在 TopicCategoryRoom 模型中**:
- [ ] 是否定义了 `relationship("LiveRoom")`？
- [ ] 关系名称是否为 `room`？
- [ ] （注：LiveRoom 在另一个模型文件中，不需要 `back_populates`）

##### **4.10.4. 懒加载策略**

- [ ] Topic.categories 是否设置了 `lazy="selectin"`（可选，但推荐）？
- [ ] TopicCategory.rooms 是否设置了 `lazy="selectin"`（可选，但推荐）？

---

#### **4.11. 导入语句检查 (Import Statements)**

- [ ] 是否导入了 `import uuid`?
- [ ] 是否导入了 `from datetime import datetime`?
- [ ] 是否导入了 `import enum`?
- [ ] 是否导入了 `from sqlalchemy import Column, String, Text, Integer, ForeignKey, Enum as SAEnum, UniqueConstraint, Index, TIMESTAMP`?
- [ ] 是否导入了 `from sqlalchemy.dialects.postgresql import UUID`?
- [ ] 是否导入了 `from sqlalchemy.orm import relationship`?
- [ ] 是否导入了 `from sqlalchemy.sql import func`?
- [ ] 是否导入了 `from app.database import Base`?
- [ ] 导入顺序是否正确（标准库 → 第三方库 → 本地模块）？

---

#### **4.12. 代码风格与文档 (Code Style & Documentation)**

- [ ] 每个模型类是否包含文档字符串？
- [ ] 关键字段是否包含中文注释或 `comment` 参数？
- [ ] 是否遵循 PEP 8 代码风格？
- [ ] 类名是否使用大驼峰命名法？
- [ ] 字段名是否使用下划线命名法？
- [ ] 是否包含 `__repr__` 方法（可选，但推荐）？

---

### **5. 最终交付 (Final Deliverable)**

请生成一份 **Markdown 格式**的详细审查报告，包含以下部分：

#### **5.1. 总体结论 (Overall Conclusion)**

一句话总结代码与设计规范的符合程度：
- ✅ **完全一致** - 所有检查项均通过
- ⚠️ **基本一致，存在小问题** - 大部分通过，存在少量非关键问题
- ❌ **存在严重偏差** - 存在关键问题需要修复

#### **5.2. 符合性评分 (Compliance Score)**

| 类别 | 通过项 | 总检查项 | 通过率 |
|------|-------|---------|--------|
| 表与模型映射 | x/3 | 3 | xx% |
| 枚举类型定义 | x/4 | 4 | xx% |
| 字段对应性 | x/20 | 20 | xx% |
| 数据类型精确性 | x/15 | 15 | xx% |
| 外键与级联规则 | x/4 | 4 | xx% |
| 约束和默认值 | x/12 | 12 | xx% |
| 索引定义 | x/9 | 9 | xx% |
| 关系完整性 | x/8 | 8 | xx% |
| **总计** | **x/75** | **75** | **xx%** |

#### **5.3. 详细审查结果表 (Detailed Audit Results)**

使用表格形式，逐项列出审查结果：

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 表映射: topics | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 字段: topics.title | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 数据类型: TIMESTAMPTZ | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 索引: idx_topics_user_id | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| ... | ... | ... | ... |

#### **5.4. 关键问题清单 (Critical Issues)**

列出所有 ❌ 失败的检查项，按优先级排序：

**高优先级（必须修复）**:
1. 问题描述 + 修正代码

**中优先级（建议修复）**:
1. 问题描述 + 修正代码

**低优先级（可选优化）**:
1. 问题描述 + 修正代码

#### **5.5. 修正代码示例 (Corrected Code Examples)**

对于每个失败的检查项，提供完整的修正代码片段。

**示例格式**:
```python
# ❌ 错误示例
created_at = Column(DateTime(timezone=True), default=datetime.utcnow)

# ✅ 正确示例
from sqlalchemy import TIMESTAMP
from sqlalchemy.sql import func

created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
```

#### **5.6. 一致性总结 (Consistency Summary)**

总结模型代码与设计文档的一致性程度，包括：
- 完全一致的部分（值得肯定）
- 存在偏差的部分（需要修正）
- 改进建议（如有）

---

### **6. 审查标准 (Audit Standards)**

审查时请遵循以下标准：

#### **6.1. 严格标准 (Strict Standards)**

以下项目必须100%符合DDL规范，任何偏差都算作**失败**：
- 表名、字段名
- 数据类型（特别是 TIMESTAMP vs DateTime）
- 外键约束和级联规则
- NOT NULL 约束
- UNIQUE 约束

#### **6.2. 宽松标准 (Flexible Standards)**

以下项目允许合理的实现差异：
- 索引名称（只要能清晰表达含义即可）
- 注释内容（只要能说明用途即可）
- 关系名称（只要符合Python命名规范即可）
- `__repr__` 方法（可选）

#### **6.3. 推荐实践 (Best Practices)**

以下项目不影响功能，但建议遵循：
- 添加 `__repr__` 方法便于调试
- 使用 `lazy="selectin"` 避免 N+1 查询
- 添加详细的中文注释
- 使用 `comment` 参数为字段添加数据库注释

---

### **7. 使用说明 (Usage Instructions)**

#### **7.1. 使用步骤**

1. 将 `backend/live_core_service/app/models/topic.py` 的完整内容粘贴到 Section 3.2
2. 运行此提示词，让 AI 生成详细的审查报告
3. 根据报告中的"关键问题清单"和"修正代码示例"进行修复
4. 重新运行审查，直到所有检查项通过

#### **7.2. 审查重点**

特别关注以下容易出错的地方：
1. ⚠️ **时间戳字段** - 必须使用 `TIMESTAMP` + `server_default=func.now()`
2. ⚠️ **索引定义** - 必须使用 `__table_args__` 显式命名
3. ⚠️ **枚举类型** - 必须使用 `SAEnum` 避免命名冲突
4. ⚠️ **外键级联** - `ondelete="CASCADE"` 大写

---

### **8. 示例报告格式 (Example Report Format)**

#### **示例 1: 时间戳字段错误**

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 数据类型: topics.created_at | ❌ **失败** | 使用了 `DateTime` 而非 `TIMESTAMP`；使用了 `default=datetime.utcnow` 而非 `server_default=func.now()` | 见下方修正代码 |

**修正代码**:
```python
# ❌ 当前代码（错误）
from sqlalchemy import DateTime
created_at = Column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)

# ✅ 修正后代码
from sqlalchemy import TIMESTAMP
from sqlalchemy.sql import func
created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
```

#### **示例 2: 索引定义方式错误**

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 索引: topics.user_id | ❌ **失败** | 使用了 `index=True` 而非 `__table_args__` 中的显式索引定义 | 见下方修正代码 |

**修正代码**:
```python
# ❌ 当前代码（错误）
user_id = Column(UUID(as_uuid=True), nullable=False, index=True)

# ✅ 修正后代码
user_id = Column(UUID(as_uuid=True), nullable=False)

__table_args__ = (
    Index('idx_topics_user_id', 'user_id'),
    # ... 其他索引
)
```

---

### **9. 验证完成标准 (Completion Criteria)**

审查报告完成后，必须明确回答以下问题：

1. ✅ / ❌ 所有表是否正确映射？
2. ✅ / ❌ 所有字段是否完整对应？
3. ✅ / ❌ 所有数据类型是否精确匹配？
4. ✅ / ❌ 所有索引是否正确定义？
5. ✅ / ❌ 所有外键和级联规则是否正确？
6. ✅ / ❌ 所有约束和默认值是否正确？
7. ✅ / ❌ 所有关系是否完整定义？
8. ✅ / ❌ 代码是否可以直接运行而不报错？

**只有当以上8个问题全部回答"✅"时，才认为模型代码与设计文档完全一致。**

---

**文档版本**: V1.0  
**创建日期**: 2025-10-16  
**适用于**: 专题功能数据库模型 (`topic.py`)  
**基于规范**: `专题功能完整设计文档-修正版.md` Section 7.3.1

