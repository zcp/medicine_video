请根据以下设计文档和规范,生成媒体下载服务的代码实现:

1. 代码规范要求:
- 严格遵循 @开发规范文档.md 中的:
  - 通用规范（第1.1节）
  - 命名规范（第1.2节）
  - 注释规范（第1.3节）
  - 代码质量指标（第1.4节）
  - 项目结构规范（第4.1节）
  - 异常处理规范（第10.1-10.4节）
  - 日志规范（第9.1-9.4节）

2. 数据库设计规范:
- 严格遵循 @媒体下载服务设计文档.md 中的:
  - 数据库表结构（四、数据库设计 4.1-4.7节）
  - 字段类型和约束
  - 索引设计

3. API 规范:
- 严格遵循  @媒体下载服务设计文档.md 中的:
  - API 接口定义（五、API接口设计 5.1-5.6节）
  - 错误处理规范（五、API接口设计 5.4节）
  - 安全规范（八、安全设计）
  - 响应格式规范（五、API接口设计 5.1节）
  - 禁止直接使用HTTPException抛出异常
  - 必须使用统一的响应处理函数

4. 媒体下载服务规范:
- 严格遵循 @媒体下载服务设计文档.md 中的:
  - 下载任务状态定义（十一、媒体下载服务特定规范 11.1节）
  - 下载失败处理规范（十一、媒体下载服务特定规范 11.2节）
  - 并发控制规范（十一、媒体下载服务特定规范 11.3节）
  - 存储路径规范（十一、媒体下载服务特定规范 11.4节）
  - 监控指标规范（十一、媒体下载服务特定规范 11.5节）
  - 错误码规范（五、API接口设计 5.2节）

5. 具体实现要求:

a) 数据库模型定义规范：
请严格按照 @媒体下载服务设计文档.md 中的数据库设计部分（第4节）生成代码，确保：
- 完全匹配所有表结构定义
- 完全匹配所有字段定义（包括类型、长度、约束）
- 完全匹配所有索引定义
- 完全匹配所有约束条件
- 完全匹配所有关系定义

b)生成代码后，请进行以下检查：
1. 字段完整性检查：
   - 检查每个表是否包含设计文档中定义的所有字段
   - 检查每个字段的类型和长度是否完全匹配
   - 检查每个字段的约束条件（nullable、unique等）是否完全匹配

2. 约束条件检查：
   - 检查每个表的 CHECK 约束是否完全匹配
   - 检查每个表的唯一约束是否完全匹配
   - 检查每个表的外键约束是否完全匹配

3. 索引检查：
   - 检查每个表的索引定义是否完全匹配
   - 检查索引的字段组合是否完全匹配
   - 检查索引的命名是否完全匹配

4. 关系检查：
   - 检查表之间的关系定义是否完全匹配
   - 检查级联删除设置是否完全匹配
   - 检查关系属性（如 back_populates）是否完全匹配
   - 
5. 验证步骤： 
 - 对比 SQL 定义和模型定义
 - 检查约束条件语法
 - 验证关系定义
 - 确认字段类型和长度
 - 确认索引定义

c) UUID字段特殊要求
  - 必须使用Python的uuid4作为默认值
  - 禁止使用数据库的gen_random_uuid()函数
  - 必须使用PGUUID类型
  - 必须设置primary_key=True
  - 必须设置as_uuid=True

示例：
```python
from uuid import uuid4
from sqlalchemy.dialects.postgresql import UUID as PGUUID

class YourModel(Base):
    __tablename__ = 'your_table'

    id = Column(PGUUID(as_uuid=True), primary_key=True, default=uuid4)
    name = Column(String(100), nullable=False, unique=True, index=True)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow)
    status = Column(String(20), nullable=False, default='pending')

    __table_args__ = (
        CheckConstraint("status IN ('pending', 'processing', 'completed', 'failed')"),
    )
```

b) 服务层实现规范：
- 数据创建要求
  - 必须使用 model_dump() 获取数据
  - 必须使用 ** 解包操作符创建实例
  - 必须使用 db.add() 添加实例
  - 必须使用 db.commit() 提交事务
  - 必须使用 db.refresh() 刷新实例

示例：
```python
def create_item(self, item_data: ItemCreate) -> ItemData:
    item_dict = item_data.model_dump()
    item = YourModel(**item_dict)
    self.db.add(item)
    self.db.commit()
    self.db.refresh(item)
    return ItemData.model_validate(item)
```

c) 错误处理规范：
- 数据库操作错误
  - 必须处理 IntegrityError
  - 必须处理 OperationalError
  - 必须处理 SQLAlchemyError
  - 必须记录错误日志
  - 必须回滚事务

示例：
```python
try:
    self.db.add(item)
    self.db.commit()
except SQLAlchemyError as e:
    self.db.rollback()
    logger.error(f"Database error: {str(e)}")
    raise DatabaseError("Failed to create item")
```

d) 日志记录规范：
- 数据操作日志
  - 必须记录创建操作
  - 必须记录更新操作
  - 必须记录删除操作
  - 必须记录错误信息
  - 必须记录关键参数

示例：
```python
logger.info(f"Creating item with data: {item_data.model_dump()}")
logger.info(f"Added item to session: {item.id}")
logger.info(f"Committed item to database: {item.id}")
logger.error(f"Failed to create item: {str(e)}")
```
e) 核心功能实现：
- 必须实现以下功能：
  - HLS视频流下载
  - MP4视频文件下载
  - 图片资源下载
  - 并发下载控制
  - 断点续传
  - 下载任务管理
  - 资源存储管理
  - 失败任务重试
  - 下载状态检查
  - 失败记录管理

f) 下载状态管理：
- 必须实现以下状态：
  - pending（等待下载）
  - processing（下载中）
  - completed（完全成功）
  - partial_completed（部分成功）
  - failed（失败）
  - cancelled（已取消）

- 状态转换逻辑：
  - 必须实现状态转换的合法性检查
  - 必须记录状态转换历史
  - 必须处理异常状态转换
  - 必须更新相关任务状态

g) 失败处理机制：
- 必须实现以下失败类型：
  - network_error（网络错误）
  - timeout（超时错误）
  - invalid_content（内容无效）
  - storage_error（存储错误）
  - permission_error（权限错误）

- 重试策略：
  - 最大重试次数：3次
  - 重试间隔：指数退避（1分钟、2分钟、4分钟）
  - HLS分片失败容忍度：5%
  - 失败记录保留时间：7天

h) 并发控制：
- 必须实现以下限制：
  - 全局并发限制：最大100个并发下载任务
  - 单个任务最大并发数：10
  - 任务队列容量：1000
  - 资源限制：
    - 内存使用限制：2GB/任务
    - CPU使用限制：50%/任务
    - 磁盘IO限制：100MB/s
    - 网络带宽限制：10MB/s

i) 存储路径规范：
- 必须严格遵循 @媒体下载服务设计文档.md 中的十一、媒体下载服务特定规范 11.4节

6. 模型一致性要求：

a) 数据库模型与API响应模型一致性：
- 字段映射一致性
  - 数据库模型字段必须与API响应模型字段完全对应
  - 字段名称必须保持一致
  - 字段类型必须正确映射
  - 字段约束必须正确映射
  - 字段默认值必须正确映射

- 类型转换一致性
  - 必须正确处理UUID类型转换
  - 必须正确处理日期时间类型转换
  - 必须正确处理枚举类型转换
  - 必须正确处理JSON类型转换
  - 必须正确处理自定义类型转换

- 验证规则一致性
  - 数据库约束必须与API验证规则一致
  - 字段长度限制必须一致
  - 字段格式要求必须一致
  - 字段枚举值必须一致
  - 字段依赖关系必须一致

b) 服务层实现规范：
- 数据转换要求
  - 必须将数据库模型转换为API响应模型
  - 必须使用model_validate方法进行转换
  - 禁止直接返回数据库模型
  - 禁止直接返回字典
  - 必须处理所有字段的转换

- 返回类型要求
  - 必须明确指定返回类型注解
  - 必须使用API响应模型作为返回类型
  - 必须处理可选字段的默认值
  - 必须处理日期时间字段的转换
  - 必须处理枚举字段的转换

- 错误处理要求
  - 必须处理数据库操作异常
  - 必须处理数据转换异常
  - 必须处理业务逻辑异常
  - 必须返回统一的错误响应
  - 必须记录错误日志

c) API端点实现规范：
- 响应模型要求
  - 必须使用response_model装饰器
  - 必须使用正确的响应模型类
  - 必须使用统一的响应处理函数
  - 禁止直接返回字典
  - 禁止直接返回数据库模型

- 响应处理要求
  - 必须使用create_response函数
  - 必须包含所有必需字段
  - 必须使用正确的时间格式
  - 必须处理空值情况
  - 必须处理异常情况

- 错误处理要求
  - 必须使用create_error_response函数
  - 必须包含错误码和错误信息
  - 必须包含错误详情（如果有）
  - 必须使用正确的时间格式
  - 必须记录错误日志

7. 响应模型定义要求：
- 基础模型要求
  - 必须继承BaseModel
  - 必须包含Config类
  - 必须启用ORM模型自动转换
  - 必须定义字段验证规则
  - 必须定义字段默认值

- 字段定义要求
  - 必须包含类型注解
  - 必须包含字段描述
  - 必须包含字段验证规则
  - 必须包含字段默认值
  - 必须包含字段长度限制

- 时间字段要求
  - 必须使用datetime类型
  - 必须使用ISO 8601格式
  - 必须设置默认值
  - 必须处理时区问题
  - 必须处理格式化问题

8. 统一响应处理规范：
- 响应函数要求
  - 必须返回响应模型而不是字典
  - 必须包含所有必需字段
  - 必须使用正确的时间格式
  - 必须处理空值情况
  - 必须处理异常情况

- 错误响应要求
  - 必须返回错误响应模型
  - 必须包含错误码和错误信息
  - 必须包含错误详情（如果有）
  - 必须使用正确的时间格式
  - 必须记录错误日志

- 日志记录要求
  - 必须记录请求信息
  - 必须记录响应信息
  - 必须记录错误信息
  - 必须记录处理时间
  - 必须记录关键参数

9. 测试要求:
- 严格遵循 @开发规范文档.md 中的:
  - 单元测试规范（第5.1节）
  - 接口测试规范（第5.2节）
  - 性能测试规范（第5.3节）
  - 安全测试规范（第5.4节）
  - 自动化测试规范（第5.5节）

请生成以下文件的代码:
1. app/models/download.py - 数据模型定义
2. app/schemas/download.py - 请求响应模型
3. app/services/download_service.py - 业务逻辑实现
4. tests/conftest.py - 测试数据库配置
5. tests/utils/test_db.py - 测试数据库工具函数