# 爬虫代码生成指令 P0 修改完成报告 v1

**报告日期**: 2025-01-13  
**任务**: 任务3 - 一致性检查与修改（爬虫代码生成指令）  
**修改文件**: `docs/提示词/爬虫与批量导入集成功能代码生成指令v1.md`  
**对照文档**:
- `docs/03_系统设计/爬虫与批量导入集成设计文档v1.md` (已在任务1和任务2中修改)
- `docs/提示词/媒体下载服务功能代码生成指令v4-已修正.md`
- `docs/提示词/媒体下载服务功能代码生成指令v4-已修正---服务层方法详细说明-完整版.md`

---

## 一、修改概览

### 1.1 修改范围

本次修改完成了**爬虫代码生成指令**文档的**P0优先级修改**，确保关键配置、方法签名、参数和返回值与修改后的设计文档及核心编码规范保持一致。

**已完成修改数量**: 10处关键修改  
**修改行数**: 约80行  
**涉及节**: 5个主要节（配置模块、爬虫基类、服务层方法、Pydantic Schema）

---

## 二、已完成的 P0 修改

### 2.1 配置模块（第3.1节）

**位置**: 第96-123行  
**修改状态**: ✅ 已完成

#### 修改1: 调整 `CRAWLER_TIMEOUT` 配置
- **修改前**: `default=300, ge=60, le=600`
- **修改后**: `default=10, ge=1, le=600`
- **说明**: 与实际crawler实现对齐（单个请求超时，非整体执行超时）

#### 修改2: 移除 `CRAWLER_MAX_RETRIES` 配置
- **修改前**: 存在 `CRAWLER_MAX_RETRIES: int = Field(default=3, ...)`
- **修改后**: 已移除整个字段定义
- **说明**: `dynamicCrawler_vzan.py` 中不使用此配置

#### 修改3: 调整 `CRAWLER_TEMP_DIR` 默认路径
- **修改前**: `default="/tmp/crawler_csv"`
- **修改后**: `default="/tmp/vzan_crawler"`
- **说明**: 与实际crawler实现的路径保持一致

#### 修改4: 重命名并调整 `CRAWLER_MAX_ROWS` → `CRAWLER_BATCH_SIZE`
- **修改前**: `CRAWLER_MAX_ROWS: int = Field(default=10000, ge=1, le=50000, ...)`
- **修改后**: `CRAWLER_BATCH_SIZE: int = Field(default=100, ge=10, le=1000, ...)`
- **说明**: 控制批量写入CSV的数据条数，优化I/O性能

#### 修改5: 更新 `CRAWL_IMPORT_SKIP_DUPLICATES` 描述
- **修改前**: `description="是否跳过重复任务"`
- **修改后**: `description="是否跳过重复任务组合（resource_url + liveroom_id + liveroom_title）"`
- **说明**: 明确去重逻辑的依据

#### 修改6: 添加配置说明注释
- **新增内容**:
  ```markdown
  4. `CRAWLER_TIMEOUT` 设置为10秒，与实际crawler实现保持一致（单个请求超时，非整体执行超时）
  5. `CRAWLER_BATCH_SIZE` 控制批量写入CSV的数据条数，优化I/O性能
  ```

---

### 2.2 爬虫基类（第4.1节）

**位置**: 第303-344行  
**修改状态**: ✅ 已完成

#### 修改7: 简化 `__init__` 方法
- **修改前**:
  ```python
  self.timeout = config.get('timeout', 300)
  self.max_retries = config.get('max_retries', 3)
  self.temp_dir = Path(config.get('temp_dir', '/tmp/crawler_csv'))
  ...
  logger.info(f"...timeout={self.timeout}s, max_retries={self.max_retries}")
  ```
- **修改后**:
  ```python
  self.timeout = config.get('timeout', 10)
  # max_retries 已移除
  # temp_dir 在子类中处理
  ...
  logger.info(f"...timeout={self.timeout}s")
  ```
- **说明**: 移除不必要的配置字段，简化基类

#### 修改8: 修改 `crawl()` 方法签名和返回值
- **修改前**:
  ```python
  @abstractmethod
  def crawl(self, url: str, **kwargs) -> Path:
      """
      Returns:
          Path: 生成的CSV文件路径
      """
  ```
- **修改后**:
  ```python
  @abstractmethod
  def crawl(self, config: Dict, **kwargs) -> Dict:
      """
      Args:
          config: 爬虫配置字典，包含 username, password, token 等
          **kwargs: 
              - page_size: 每页数据量（默认10）
              - max_pages: 最大爬取页数（默认全部）
      
      Returns:
          Dict: 爬取结果摘要
              {
                  "csv_file": str,             # 主 CSV 文件路径
                  "incremental_file": str,     # 增量 CSV 文件路径
                  "failed_file": str,          # 失败记录文件路径
                  "total_rows": int,
                  "failed_rows": int,
                  "incremental_rows": int
              }
      """
  ```
- **说明**: 与实际crawler实现对齐，支持多文件输出和统计信息

---

### 2.3 服务层方法（第5.1节）

**位置**: 第796-940行  
**修改状态**: ✅ 已完成

#### 修改9: 移除 `crawl_url` 参数
- **修改前**: `def crawl_and_import_tasks(self, crawler_type: str, crawl_url: str, ...)`
- **修改后**: `def crawl_and_import_tasks(self, crawler_type: str, user_id: uuid.UUID, ...)`
- **说明**: 微赞爬虫使用固定API地址，不需要用户提供URL

#### 修改10: 更新参数和返回值描述
- **修改前**:
  ```python
  Args:
      crawl_url: 爬取的目标URL
      **crawl_kwargs: 
          - max_rows: 最大爬取行数
          - start_date/end_date: 日期范围
  
  Returns:
      Dict: {
          "crawl_file": str,
          "crawl_time": float,
          ...
      }
  ```
- **修改后**:
  ```python
  Args:
      skip_duplicates: 是否跳过重复任务组合（resource_url + liveroom_id + liveroom_title）
      **crawl_kwargs: 
          - page_size: 每页数据量（默认10）
          - max_pages: 最大爬取页数（默认全部）
  
  Returns:
      Dict: {
          "crawl_file": str,             # 主CSV文件路径
          "incremental_file": str,       # 增量CSV文件路径
          "failed_file": str,            # 失败记录文件路径
          ...
      }
  ```

#### 修改11: 移除参数验证中的 `crawl_url` 检查
- **修改前**: 包含 `if not crawl_url: raise ValueError(...)`
- **修改后**: 已移除

#### 修改12: 更新爬虫配置
- **修改前**:
  ```python
  crawler_config = {
      'timeout': settings.CRAWLER_TIMEOUT,
      'max_retries': settings.CRAWLER_MAX_RETRIES,  # ❌
      'temp_dir': settings.CRAWLER_TEMP_DIR,
  }
  ```
- **修改后**:
  ```python
  crawler_config = {
      'timeout': settings.CRAWLER_TIMEOUT,
      'temp_dir': settings.CRAWLER_TEMP_DIR,
      'batch_size': settings.CRAWLER_BATCH_SIZE  # ✅ 新增
  }
  ```

#### 修改13: 更新爬虫调用
- **修改前**:
  ```python
  csv_path = crawler.crawl(crawl_url, **crawl_kwargs)
  
  # 计算爬取行数（读取CSV文件）
  with open(csv_path, 'r', encoding='utf-8') as f:
      ...
  
  result["crawl_file"] = str(csv_path)
  ```
- **修改后**:
  ```python
  crawl_result = crawler.crawl(crawler_config, **crawl_kwargs)
  
  # 提取爬取结果
  csv_path = crawl_result['csv_file']
  crawl_rows = crawl_result['total_rows']
  incremental_file = crawl_result.get('incremental_file')
  failed_file = crawl_result.get('failed_file')
  
  result["crawl_file"] = str(csv_path)
  result["incremental_file"] = str(incremental_file) if incremental_file else None
  result["failed_file"] = str(failed_file) if failed_file else None
  ```

#### 修改14: 移除变量提取中的 `crawl_url_for_logging`
- **修改前**: `crawl_url_for_logging = str(crawl_url)`
- **修改后**: 已移除

---

### 2.4 Pydantic Schema（第6.1节）

**位置**: 第1083-1148行  
**修改状态**: ✅ 已完成

#### 修改15: 修改 `CrawlOptions` 字段定义
- **修改前**:
  ```python
  class CrawlOptions(BaseModel):
      max_rows: Optional[int] = Field(default=10000, ge=1, le=50000, ...)
      start_date: Optional[str] = Field(None, ...)
      end_date: Optional[str] = Field(None, ...)
      
      @field_validator('start_date', 'end_date')
      def validate_date_format(cls, v): ...
  ```
- **修改后**:
  ```python
  class CrawlOptions(BaseModel):
      page_size: Optional[int] = Field(default=10, ge=1, le=100, ...)
      max_pages: Optional[int] = Field(None, ge=1, ...)
      
      model_config = ConfigDict(extra='allow')
  ```
- **说明**: 字段与实际crawler实现对齐

#### 修改16: 移除 `CrawlAndImportRequest.crawl_url` 字段
- **修改前**:
  ```python
  class CrawlAndImportRequest(BaseModel):
      crawler_type: str = ...
      crawl_url: HttpUrl = ...  # ❌
      skip_duplicates: bool = ...
      ...
  ```
- **修改后**:
  ```python
  class CrawlAndImportRequest(BaseModel):
      crawler_type: str = Field(..., description="爬虫类型（vzan等，微赞爬虫使用固定API地址）")
      skip_duplicates: bool = Field(
          default=True, 
          description="是否跳过重复任务组合（基于resource_url + liveroom_id + liveroom_title）"
      )
      ...
  ```

#### 修改17: 添加 `CrawlAndImportResponse` 字段
- **修改前**:
  ```python
  class CrawlAndImportResponse(BaseModel):
      crawl_status: str = ...
      crawl_rows: int = ...
      crawl_file: Optional[str] = ...
      crawl_time: float = ...
      import_result: Optional[BatchImportResponse] = ...
      error_message: Optional[str] = ...
  ```
- **修改后**:
  ```python
  class CrawlAndImportResponse(BaseModel):
      crawl_status: str = ...
      crawl_rows: int = Field(..., description="爬取到的总行数")
      crawl_file: Optional[str] = Field(None, description="主CSV文件路径")
      incremental_file: Optional[str] = Field(None, description="增量CSV文件路径")  # ✅ 新增
      failed_file: Optional[str] = Field(None, description="失败记录文件路径")      # ✅ 新增
      import_result: Optional[BatchImportResult] = ...
      error_message: Optional[str] = ...
  ```
- **说明**: 支持多文件输出，与crawler返回值对齐

---

## 三、未完成的 P1 修改

### 3.1 微赞爬虫实现（第4.2节）

**位置**: 第378-701行  
**修改状态**: ⏸️ 未完成（需要大规模重写）

**原因**: 该部分需要根据 `backend/dynamicCrawler_vzan.py` 的实际实现进行**完整重写**，包括：

1. **API端点常量定义**（需要添加）:
   ```python
   API_LIVEROOM_LIST = "https://live-liveapi.vzan.com/api/v1/topic/get_topicdatas"
   API_LIVEROOM_URL_PREFIX = "https://inter.dayilive.com/live/page/"
   API_TOPIC_CONFIG_PREFIX = "https://live-play.vzan.com/api/topic/topic_config?..."
   API_VIDEO_CONFIG_PREFIX = "https://live-play.vzan.com/api/topic/video_config?..."
   ```

2. **CSV字段定义**（需要扩展）:
   - **修改前**: 仅5个字段 `['liveroom_id', 'liveroom_title', 'liveroom_url', 'resource_url', 'resource_type']`
   - **修改后**: 需要13个字段，包括 `created_at`, `start_time`, `live_type`, `status`, `cover_url`, `view_count`, `zb_id`, `enc_tpid`

3. **HTTP请求头**（需要更新）:
   - **修改前**: 简化的请求头（仅4个字段）
   - **修改后**: 完整的微赞API必需请求头（12个字段，包括 `lid`, `zbid`, `admin-token`, `appcode` 等）

4. **多步骤数据提取流程**（需要实现）:
   - **步骤1**: 调用 `get_topicdatas` API 获取直播间基础信息
   - **步骤2**: 对每个直播间，调用 `topic_config` API 提取 `enc_tpid`
   - **步骤3**: 对每个直播间，调用 `video_config` API 提取视频 URL 和封面图

5. **增量爬取支持**（需要实现）:
   - `_load_existing_ids()` 方法：从主CSV文件加载已爬取的直播间ID
   - 增量文件生成逻辑：`liveroomlist_inc_vzan.csv`

6. **多输出文件**（需要实现）:
   - 主文件: `liveroomlist_vzan.csv`
   - 增量文件: `liveroomlist_inc_vzan.csv`
   - 失败记录文件: `failed_liveroomlist_urls_vzan.txt`

7. **批量写入优化**（需要实现）:
   - 使用 `CRAWLER_BATCH_SIZE` 控制批量写入数据条数

**建议**: 参考已修改的 `docs/03_系统设计/爬虫与批量导入集成设计文档v1.md` 中的 `VzanCrawler` 实现细节（第5.3节），该部分已在任务1中对齐实际代码。

---

### 3.2 API接口实现（第7.1节）

**位置**: 第1176-1420行  
**修改状态**: ⏸️ 部分未完成

**需要修改**:
1. ✅ API签名已通过Schema修改间接修正（移除了 `crawl_url`）
2. ⏸️ 请求示例和响应示例（需要更新JSON示例，移除 `crawl_url`，添加 `incremental_file`, `failed_file`）
3. ⏸️ 错误处理示例（需要确认符合统一响应规范：HTTP 200 + 业务码）

**建议**: 在VzanCrawler实现完成后，同步更新API接口文档的示例部分。

---

### 3.3 测试代码生成指令（第8节）

**位置**: 第1422-1791行  
**修改状态**: ⏸️ 未评估

**需要修改**:
- 测试函数需要对齐新的API签名（移除 `crawl_url` 相关测试）
- 测试断言需要验证新的返回值字段（`incremental_file`, `failed_file`）
- 测试数据需要对齐新的 `CrawlOptions` 字段（`page_size`, `max_pages`）

**建议**: 在VzanCrawler实现和API接口文档完成后，进行测试代码生成指令的更新。

---

## 四、一致性验证

### 4.1 与设计文档的一致性

| 检查项 | 一致性状态 | 说明 |
|:---|:---:|:---|
| 配置模块 | ✅ 一致 | 所有配置字段与设计文档对齐 |
| 爬虫基类 | ✅ 一致 | 方法签名和返回值与设计文档对齐 |
| 服务层方法 | ✅ 一致 | 参数、返回值、配置与设计文档对齐 |
| Pydantic Schema | ✅ 一致 | 所有字段与设计文档对齐 |
| 微赞爬虫实现 | ⏸️ 待更新 | 需要完整重写以对齐设计文档和实际代码 |
| API接口 | 🟡 部分一致 | 签名一致，示例待更新 |

### 4.2 与核心编码规范的一致性

| 检查项 | 一致性状态 | 说明 |
|:---|:---:|:---|
| 安全异步异常处理 | ✅ 一致 | 服务层方法已包含变量提取步骤 |
| 环境变量驱动配置 | ✅ 一致 | 所有敏感配置通过环境变量读取 |
| 响应处理规范 | ⏸️ 待验证 | API接口需要验证是否符合HTTP 200 + 业务码规范 |
| 日志规范 | ✅ 一致 | 日志包含 `exc_info=True`（服务层方法） |

---

## 五、修改验证清单

### 5.1 已验证的一致性

- [x] 配置模块的所有字段名、默认值、约束与设计文档一致
- [x] 爬虫基类的 `crawl()` 方法签名与设计文档一致
- [x] 服务层方法移除了 `crawl_url` 参数
- [x] 服务层方法添加了 `incremental_file` 和 `failed_file` 返回字段
- [x] `CrawlOptions` 字段从 `max_rows` 改为 `page_size` 和 `max_pages`
- [x] `CrawlAndImportRequest` 移除了 `crawl_url` 字段
- [x] `CrawlAndImportResponse` 添加了 `incremental_file` 和 `failed_file` 字段
- [x] 爬虫配置移除了 `max_retries`，添加了 `batch_size`
- [x] 服务层方法的爬虫调用从 `crawler.crawl(crawl_url)` 改为 `crawler.crawl(crawler_config)`
- [x] 服务层方法正确提取和使用了 `crawl_result` 的多个字段

### 5.2 待验证的一致性

- [ ] 微赞爬虫实现的完整重写（P1优先级）
- [ ] API接口的请求/响应示例更新（P1优先级）
- [ ] API接口的错误处理示例符合统一响应规范（P1优先级）
- [ ] 测试代码生成指令的更新（P1优先级）

---

## 六、后续工作建议

### 6.1 优先级1：完成微赞爬虫实现的重写

**工作量**: 约500行代码生成指令  
**依赖**: 已修改的设计文档（任务1完成）  
**方法**: 直接从设计文档中提取 `VzanCrawler` 的完整实现细节，替换当前的简化版本

### 6.2 优先级2：更新API接口文档的示例部分

**工作量**: 约100行  
**依赖**: 微赞爬虫实现完成  
**方法**: 
1. 更新请求示例（移除 `crawl_url`）
2. 更新响应示例（添加 `incremental_file`, `failed_file`）
3. 验证错误处理示例符合HTTP 200 + 业务码规范

### 6.3 优先级3：更新测试代码生成指令

**工作量**: 约200行  
**依赖**: API接口文档完成  
**方法**: 
1. 更新测试用例（移除 `crawl_url` 相关测试）
2. 添加新字段的测试断言
3. 对齐新的 `CrawlOptions` 字段

---

## 七、总结

### 7.1 完成情况

- **P0修改**: ✅ 100%完成（17处修改全部完成）
- **P1修改**: ⏸️ 0%完成（微赞爬虫实现、API示例、测试指令待更新）
- **文档一致性**: ✅ P0部分与设计文档和核心编码规范完全一致

### 7.2 核心成果

1. ✅ **配置模块**已完全对齐实际实现和设计文档
2. ✅ **爬虫基类**的接口定义已标准化，支持多文件输出和统计信息
3. ✅ **服务层方法**移除了不必要的 `crawl_url` 参数，支持固定API端点的爬虫类型
4. ✅ **Pydantic Schema**已更新，支持新的爬虫选项和返回值结构

### 7.3 使用建议

**当前状态**: 代码生成指令的**主要框架和接口定义**已完成P0修改，可以用于生成配置模块、爬虫基类、服务层方法和Pydantic Schema的代码。

**待完成后**: 在完成P1修改（特别是VzanCrawler实现）后，即可用于生成完整的爬虫与批量导入集成功能的全部代码。

---

**报告完成**  
**下一步**: 继续任务3的第4步（测试提示词文档一致性检查与修改）

