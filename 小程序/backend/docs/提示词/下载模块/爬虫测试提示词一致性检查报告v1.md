# 爬虫测试提示词一致性检查报告 v1

**检查日期**: 2025-01-13  
**任务**: 任务4 - 测试提示词文档一致性检查与修改  
**检查文件**: `docs/提示词/爬虫与批量导入集成功能测试提示词v1.md`  
**对照文件**:
- `docs/提示词/爬虫与批量导入集成功能代码生成指令v1.md` (已在任务3中修改)
- `docs/03_系统设计/爬虫与批量导入集成设计文档v1.md` (已在任务1、2中修改)
- `docs/提示词/媒体下载服务功能代码生成指令v4-已修正---batch_import_csv功能测试提示词.md` (参考测试风格)

---

## 一、检查概述

### 1.1 检查目标

确保测试提示词文档与已修改的代码生成指令和设计文档保持一致，特别是：
1. API签名的变化（移除 `crawl_url` 参数）
2. 配置的变化（移除 `max_retries`，调整超时值和路径）
3. 返回值的变化（添加 `incremental_file`, `failed_file`）
4. 测试风格的一致性（同步/异步，不修改 `conftest.py`）
5. Mock返回值的变化（从 `Path` 改为 `Dict`）

### 1.2 检查方法

- 搜索关键字（`crawl_url`, `max_retries`, `timeout`, `temp_dir`）
- 对比测试配置与实际配置的一致性
- 对比测试断言与实际返回值的一致性
- 对比测试风格与参考文档的一致性

---

## 二、发现的不一致性

### 2.1 测试配置不一致

#### 问题1: `test_crawler_config` fixture 包含已移除的配置

**位置**: 第68-78行

**当前内容**:
```python
@pytest.fixture
def test_crawler_config():
    """测试用爬虫配置"""
    return {
        'username': 'test_user',
        'password': 'test_pass',
        'token': 'test_encrypted_token',
        'timeout': 30,           # ❌ 应为 10
        'max_retries': 2,        # ❌ 已移除
        'temp_dir': '/tmp/test_crawler_csv'  # ❌ 应为 /tmp/vzan_crawler
    }
```

**应修改为**:
```python
@pytest.fixture
def test_crawler_config():
    """测试用爬虫配置"""
    return {
        'username': 'test_user',
        'password': 'test_pass',
        'token': 'test_encrypted_token',
        'timeout': 10,           # ✅ 修正为 10
        'temp_dir': '/tmp/vzan_crawler',  # ✅ 修正路径
        'batch_size': 100        # ✅ 新增配置
    }
```

**影响范围**: 所有使用此fixture的测试用例

---

### 2.2 服务层方法签名不一致

#### 问题2: 服务层测试中使用了已移除的 `crawl_url` 参数

**位置**: 13处（第786, 833, 872, 931, 994, 1021, 1029行等）

**示例 - 第786行**:
```python
result = download_service.crawl_and_import_tasks(
    crawler_type='vzan',
    crawl_url='https://api.vzan.com/test',  # ❌ 已移除
    user_id=test_user_id,
    skip_duplicates=True,
    auto_start=False
)
```

**应修改为**:
```python
result = download_service.crawl_and_import_tasks(
    crawler_type='vzan',
    # crawl_url 已移除
    user_id=test_user_id,
    skip_duplicates=True,
    auto_start=False
)
```

**影响范围**: 所有服务层测试用例（第5.2节 - 第5.6节）

---

#### 问题3: 测试用例包含验证空 `crawl_url` 的测试

**位置**: 第1011-1033行

**当前内容**:
```python
def test_crawl_and_import_empty_crawl_url(
    self,
    download_service,
    test_user_id
):
    """
    测试用例6: 测试空crawl_url（边界条件）
    
    准备:
    - 使用空的crawl_url调用
    
    执行:
    - 调用crawl_and_import_tasks()
    
    验证点:
    - 抛出ValueError
    - 错误信息包含"crawl_url不能为空"
    """
    # 测试空crawl_url
    with pytest.raises(ValueError, match="crawl_url不能为空"):
        download_service.crawl_and_import_tasks(
            crawler_type='vzan',
            crawl_url='',
            user_id=test_user_id
        )
```

**应修改为**:
❌ **应删除此测试用例**（因为 `crawl_url` 参数已移除）

---

### 2.3 Mock返回值不一致

#### 问题4: Mock的 `crawler.crawl()` 返回值类型错误

**位置**: 第743-745, 801, 913行等

**当前内容**:
```python
csv_path = tmp_path / "test_crawl.csv"
csv_path.write_bytes(sample_csv_content)

mock_crawler.crawl.return_value = csv_path  # ❌ 返回Path对象
```

**应修改为**:
```python
csv_path = tmp_path / "test_crawl.csv"
csv_path.write_bytes(sample_csv_content)

# 创建增量文件和失败文件
incremental_file = tmp_path / "test_crawl_inc.csv"
incremental_file.write_bytes(sample_csv_content)
failed_file = tmp_path / "failed_urls.txt"
failed_file.write_text("", encoding='utf-8')

mock_crawler.crawl.return_value = {  # ✅ 返回Dict对象
    'csv_file': str(csv_path),
    'incremental_file': str(incremental_file),
    'failed_file': str(failed_file),
    'total_rows': 2,
    'failed_rows': 0,
    'incremental_rows': 2
}
```

**影响范围**: 所有Mock `crawler.crawl()` 的测试用例

---

### 2.4 测试断言不一致

#### 问题5: 测试断言缺少新返回值字段的验证

**位置**: 第793-796行等

**当前内容**:
```python
# 验证
assert result['crawl_status'] == 'success'
assert result['crawl_rows'] == 2
assert result['import_result'] is not None
assert result['import_result']['success'] == 2
```

**应修改为**:
```python
# 验证
assert result['crawl_status'] == 'success'
assert result['crawl_rows'] == 2
assert result['crawl_file'] is not None           # ✅ 验证主文件
assert result['incremental_file'] is not None     # ✅ 验证增量文件
assert result['failed_file'] is not None          # ✅ 验证失败文件
assert result['import_result'] is not None
assert result['import_result']['success'] == 2
```

**影响范围**: 所有验证成功返回结果的测试用例

---

### 2.5 API测试不一致

#### 问题6: API测试中使用了已移除的 `crawl_url` 字段

**位置**: 第1081, 1184, 1214行

**示例 - 第1081行**:
```python
response = client.post(
    "/api/v1/download/tasks/crawl-and-import",
    json={
        "crawler_type": "vzan",
        "crawl_url": "https://api.vzan.com/live/list",  # ❌ 已移除
        "skip_duplicates": True,
        "auto_start": False
    },
    headers=auth_headers
)
```

**应修改为**:
```python
response = client.post(
    "/api/v1/download/tasks/crawl-and-import",
    json={
        "crawler_type": "vzan",
        # crawl_url 已移除
        "skip_duplicates": True,
        "auto_start": False,
        "crawl_options": {
            "page_size": 10,
            "max_pages": 5
        }
    },
    headers=auth_headers
)
```

**影响范围**: 所有API测试用例（第6节）

---

#### 问题7: API测试断言缺少新字段验证

**位置**: 第1090-1095行等

**当前内容**:
```python
# 验证响应
assert response.status_code == 200
data = response.json()['data']
assert data['crawl_status'] == 'success'
assert data['crawl_rows'] > 0
assert data['crawl_file'] is not None
```

**应修改为**:
```python
# 验证响应（统一响应模式）
assert response.status_code == 200
response_data = response.json()
assert response_data['code'] == 201  # 业务状态码

data = response_data['data']
assert data['crawl_status'] == 'success'
assert data['crawl_rows'] > 0
assert data['crawl_file'] is not None
assert data['incremental_file'] is not None     # ✅ 验证增量文件
assert data['failed_file'] is not None          # ✅ 验证失败文件
```

**影响范围**: 所有验证API响应的测试用例

---

### 2.6 测试风格不一致

#### 问题8: 缺少统一响应模式的说明

**位置**: 测试基础规范部分（第2节）

**当前内容**: 无统一响应模式的说明

**应添加**:
```markdown
### 2.5 响应状态码（统一响应模式）

**重要**：根据统一响应规范，所有API接口都返回 **HTTP 200**，业务状态码在响应体的 `code` 字段中。

| HTTP状态码 | 响应体code字段 | 场景 |
|:---|:---|:---|
| 200 | 201 | 爬取和导入全部成功 |
| 200 | 207 | 爬取成功，导入部分成功 |
| 200 | 400 | 爬虫配置错误、参数错误 |
| 200 | 500 | 数据库错误、系统错误 |

**测试断言示例**：
```python
assert response.status_code == 200  # HTTP状态码固定为200
response_data = response.json()
assert response_data["code"] == 201  # 业务状态码在body的code字段
```
```

---

#### 问题9: 同步/异步测试函数的正确性

**位置**: 服务层测试部分（第5节）

**检查结果**: ⚠️ 需要确认

**当前状态**: 文档中服务层测试使用了 `def test_...`（同步函数），这是正确的。但需要确认：
1. 是否使用 `sync_db_session` fixture
2. 是否正确说明不使用 `@pytest.mark.asyncio`

**建议**: 在文档开头的"测试基础规范"中明确说明同步/异步测试的区分，参考 `batch_import_csv功能测试提示词.md` 的第1节"角色定义"。

---

#### 问题10: `conftest.py` 修改的说明

**位置**: 整个文档

**检查结果**: ⚠️ 需要添加说明

**当前状态**: 文档中未明确说明不应修改 `conftest.py`

**建议**: 在测试基础规范中添加：
```markdown
### 2.6 测试辅助函数规范

**重要**：所有辅助函数（如生成测试数据的函数）应直接定义在测试文件中，**不要修改 `conftest.py`**，因为其他测试文件依赖它。

**示例**：
```python
# ✅ 正确：在测试文件中定义辅助函数
def generate_sample_csv_data():
    """生成示例CSV数据"""
    return [...]

# ❌ 错误：不要在 conftest.py 中添加新的辅助函数
```
```

---

### 2.7 测试数据不一致

#### 问题11: 示例爬取数据缺少新的CSV字段

**位置**: 第89-108行

**当前内容**:
```python
@pytest.fixture
def sample_crawl_data():
    """示例爬取数据"""
    return [
        {
            'liveroom_id': '1234567890',
            'liveroom_title': '测试直播间1',
            'liveroom_url': 'https://example.com/live/1',
            'resource_url': 'https://example.com/video1.m3u8',
            'resource_type': 'hls'
        },
        ...
    ]
```

**应添加新字段**（根据设计文档，CSV应包含13个字段）:
```python
@pytest.fixture
def sample_crawl_data():
    """示例爬取数据"""
    return [
        {
            'liveroom_id': '1234567890',
            'liveroom_title': '测试直播间1',
            'liveroom_url': 'https://example.com/live/1',
            'resource_url': 'https://example.com/video1.m3u8',
            'resource_type': 'hls',
            'created_at': '2025-01-13 10:00:00',  # ✅ 新增
            'start_time': '2025-01-13 09:00:00',  # ✅ 新增
            'live_type': '1',                      # ✅ 新增
            'status': '2',                         # ✅ 新增
            'cover_url': 'https://example.com/cover1.jpg',  # ✅ 新增
            'view_count': '1000',                  # ✅ 新增
            'zb_id': '11749549',                   # ✅ 新增
            'enc_tpid': 'abc123'                   # ✅ 新增
        },
        ...
    ]
```

**影响范围**: 所有使用此fixture的测试用例

---

## 三、修改优先级

### P0（必须修改）

1. ✅ **移除所有 `crawl_url` 参数**（13处）
2. ✅ **修改 `test_crawler_config` fixture**（移除 `max_retries`，调整 `timeout` 和 `temp_dir`）
3. ✅ **修改 Mock `crawler.crawl()` 返回值**（从 `Path` 改为 `Dict`）
4. ✅ **删除 `test_crawl_and_import_empty_crawl_url` 测试用例**
5. ✅ **添加返回值字段验证**（`incremental_file`, `failed_file`）
6. ✅ **修改API请求payload**（移除 `crawl_url`，添加 `crawl_options`）

### P1（建议修改）

7. ⚠️ **添加统一响应模式说明**（HTTP 200 + 业务码）
8. ⚠️ **添加同步/异步测试区分说明**
9. ⚠️ **添加不修改 `conftest.py` 的说明**
10. ⚠️ **扩展示例爬取数据字段**（13个字段）

---

## 四、详细修改清单

### 修改1: 测试配置fixture（第68-78行）

**操作**: 替换整个fixture

**修改前**:
```python
@pytest.fixture
def test_crawler_config():
    """测试用爬虫配置"""
    return {
        'username': 'test_user',
        'password': 'test_pass',
        'token': 'test_encrypted_token',
        'timeout': 30,
        'max_retries': 2,
        'temp_dir': '/tmp/test_crawler_csv'
    }
```

**修改后**:
```python
@pytest.fixture
def test_crawler_config():
    """测试用爬虫配置"""
    return {
        'username': 'test_user',
        'password': 'test_pass',
        'token': 'test_encrypted_token',
        'timeout': 10,
        'temp_dir': '/tmp/vzan_crawler',
        'batch_size': 100
    }
```

---

### 修改2: 服务层测试方法调用（13处）

**位置**: 第786, 833, 872, 931, 994, 1021, 1029行等

**操作**: 删除 `crawl_url` 参数

**示例修改**:
```python
# 修改前
result = download_service.crawl_and_import_tasks(
    crawler_type='vzan',
    crawl_url='https://api.vzan.com/test',  # ❌ 删除
    user_id=test_user_id,
    skip_duplicates=True,
    auto_start=False
)

# 修改后
result = download_service.crawl_and_import_tasks(
    crawler_type='vzan',
    user_id=test_user_id,
    skip_duplicates=True,
    auto_start=False
)
```

---

### 修改3: Mock `crawler.crawl()` 返回值（多处）

**位置**: 所有Mock爬虫的测试用例

**操作**: 将返回值从 `Path` 改为 `Dict`

**示例修改**:
```python
# 修改前
csv_path = tmp_path / "test_crawl.csv"
csv_path.write_bytes(sample_csv_content)
mock_crawler.crawl.return_value = csv_path

# 修改后
csv_path = tmp_path / "test_crawl.csv"
csv_path.write_bytes(sample_csv_content)
incremental_file = tmp_path / "test_crawl_inc.csv"
incremental_file.write_bytes(sample_csv_content)
failed_file = tmp_path / "failed_urls.txt"
failed_file.write_text("", encoding='utf-8')

mock_crawler.crawl.return_value = {
    'csv_file': str(csv_path),
    'incremental_file': str(incremental_file),
    'failed_file': str(failed_file),
    'total_rows': 2,
    'failed_rows': 0,
    'incremental_rows': 2
}
```

---

### 修改4: 删除 `test_crawl_and_import_empty_crawl_url` 测试用例

**位置**: 第1011-1033行

**操作**: 完全删除此测试用例

---

### 修改5: 添加返回值字段验证（多处）

**位置**: 所有验证成功返回结果的测试用例

**操作**: 添加新字段验证

**示例修改**:
```python
# 修改前
assert result['crawl_status'] == 'success'
assert result['crawl_rows'] == 2
assert result['import_result'] is not None

# 修改后
assert result['crawl_status'] == 'success'
assert result['crawl_rows'] == 2
assert result['crawl_file'] is not None
assert result['incremental_file'] is not None  # ✅ 新增
assert result['failed_file'] is not None       # ✅ 新增
assert result['import_result'] is not None
```

---

### 修改6: 修改API测试请求payload（多处）

**位置**: 第1081, 1184, 1214行等

**操作**: 移除 `crawl_url`，添加 `crawl_options`

**示例修改**:
```python
# 修改前
response = client.post(
    "/api/v1/download/tasks/crawl-and-import",
    json={
        "crawler_type": "vzan",
        "crawl_url": "https://api.vzan.com/live/list",  # ❌ 删除
        "skip_duplicates": True,
        "auto_start": False
    },
    headers=auth_headers
)

# 修改后
response = client.post(
    "/api/v1/download/tasks/crawl-and-import",
    json={
        "crawler_type": "vzan",
        "skip_duplicates": True,
        "auto_start": False,
        "crawl_options": {  # ✅ 新增
            "page_size": 10,
            "max_pages": 5
        }
    },
    headers=auth_headers
)
```

---

### 修改7: 添加统一响应模式说明（第2节）

**位置**: 第2节"测试基础规范"

**操作**: 在第2.4节"断言原则"之后添加新的第2.5节

**添加内容**:
```markdown
### 2.5 响应状态码（统一响应模式）

**重要**：根据统一响应规范，所有API接口都返回 **HTTP 200**，业务状态码在响应体的 `code` 字段中。

| HTTP状态码 | 响应体code字段 | 场景 |
|:---|:---|:---|
| 200 | 201 | 爬取和导入全部成功 |
| 200 | 207 | 爬取成功，导入部分成功 |
| 200 | 400 | 爬虫配置错误、参数错误 |
| 200 | 500 | 数据库错误、系统错误 |

**测试断言示例**：
```python
assert response.status_code == 200  # HTTP状态码固定为200
response_data = response.json()
assert response_data["code"] == 201  # 业务状态码在body的code字段
```

### 2.6 同步/异步测试区分

**重要说明**：
1. **服务层测试**：`download_service.py` 中的所有方法都是**同步方法**，因此服务层测试必须使用**同步测试函数**（`def test_...`），使用同步数据库会话（`sync_db_session`）。
2. **API接口测试**：API 接口是异步的，必须使用**异步测试函数**（`async def test_...`）和 `@pytest.mark.asyncio` 装饰器。

### 2.7 测试辅助函数规范

**重要**：所有辅助函数（如生成测试数据的函数）应直接定义在测试文件中，**不要修改 `conftest.py`**，因为其他测试文件依赖它。
```

---

### 修改8: 更新API测试断言（多处）

**位置**: 所有API测试用例的断言部分

**操作**: 更新为统一响应模式

**示例修改**:
```python
# 修改前
assert response.status_code == 200
data = response.json()['data']
assert data['crawl_status'] == 'success'

# 修改后
assert response.status_code == 200
response_data = response.json()
assert response_data['code'] == 201  # ✅ 验证业务状态码

data = response_data['data']
assert data['crawl_status'] == 'success'
assert data['incremental_file'] is not None  # ✅ 验证新字段
assert data['failed_file'] is not None       # ✅ 验证新字段
```

---

## 五、修改总结

### 5.1 修改统计

| 修改类型 | 数量 | 优先级 |
|:---|:---:|:---:|
| 测试配置修改 | 1 | P0 |
| 服务层方法调用修改 | 13+ | P0 |
| Mock返回值修改 | 5+ | P0 |
| 测试用例删除 | 1 | P0 |
| 返回值验证添加 | 10+ | P0 |
| API请求修改 | 3+ | P0 |
| 文档说明添加 | 3 | P1 |
| 测试数据扩展 | 1 | P1 |

**预计修改行数**: 约100-150行

---

### 5.2 一致性评分

| 检查项 | 一致性等级 | 说明 |
|:---|:---:|:---|
| 测试配置 | 🔴 不一致 | 包含已移除的配置，值不正确 |
| 方法签名 | 🔴 不一致 | 使用了已移除的 `crawl_url` 参数 |
| Mock返回值 | 🔴 不一致 | 返回类型错误（Path vs Dict） |
| 测试断言 | 🟡 部分一致 | 缺少新字段验证 |
| 测试风格 | 🟡 部分一致 | 缺少统一响应模式说明 |
| 测试数据 | 🟡 部分一致 | CSV字段不完整 |

---

### 5.3 修改策略

**建议策略**: 逐节修改，先完成P0修改（必须），再完成P1修改（建议）

**修改顺序**:
1. 修改测试配置（第2节）
2. 修改服务层测试（第5节）- 移除 `crawl_url`，修改Mock返回值
3. 删除无效测试用例
4. 修改API测试（第6节）- 移除 `crawl_url`，添加 `crawl_options`
5. 添加文档说明（第2节）
6. 扩展测试数据（可选）

---

**报告完成**  
**下一步**: 开始进行P0修改

