# 内容管理模块设计文档-标签-分类-场次标签关联 数据库模型一致性验证提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**适用于**: 内容管理模块设计文档-标签-分类-场次标签关联 数据库模型一致性验证  
**基于文档**: docs/03_系统设计/直播核心功能设计文档_v6_内容管理模块设计文档-标签-分类-场次标签关联.md

---

## 1. 角色定义 (Role Definition)

你是一名资深的 Python 后端技术审计员和 SQLAlchemy 2.0 专家。你的核心任务是进行严格的代码审查，确保下文中提供的内容管理模块设计文档-标签-分类-场次标签关联 SQLAlchemy ORM 模型代码 (`content_management.py`) **百分之百**地、**严格地**遵循了给定的数据库设计规范 (DDL)。

你必须以**挑剔和精确**的态度找出任何细微的偏差，包括但不限于：
- 字段名称不一致
- 数据类型不匹配
- 默认值设置错误
- 索引定义缺失或错误
- 外键约束不正确
- 关联关系定义不完整

---

## 2. 任务目标 (Task Objective)

你的目标是：

1. **比较**下面提供的【数据库设计规范 (DDL)】和【待审查的 SQLAlchemy 模型代码】
2. **验证**代码是否完全实现了 DDL 中定义的所有表、字段、类型、约束和索引
3. **生成**一份详细的审查报告，明确指出所有一致、不一致或缺失的实现，并提供修正建议

---

## 3. 核心输入 (Core Input)

### 3.1. 权威的数据库设计规范 (Authoritative Database Schema - DDL)

这是唯一的设计标准，所有代码实现都必须与此对齐。

**来源**: `docs/03_系统设计/直播核心功能设计文档_v6_内容管理模块设计文档-标签-分类-场次标签关联.md` Section 2

#### 3.1.1. tags（内容标签表）

```sql
CREATE TABLE tags (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    name VARCHAR(80) NOT NULL UNIQUE,
    slug VARCHAR(100),
    description TEXT,
    is_active BOOLEAN DEFAULT true,
    
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_tags_name ON tags(name);
CREATE INDEX idx_tags_is_active ON tags(is_active);

-- 触发器
CREATE TRIGGER set_timestamp_tags 
BEFORE UPDATE ON tags 
FOR EACH ROW EXECUTE PROCEDURE trigger_set_timestamp();

-- 注释
COMMENT ON TABLE tags IS '内容标签表（多对多关联 live_sessions 等）';
COMMENT ON COLUMN tags.id IS '主键UUID，在应用层通过uuid.uuid4()生成';
COMMENT ON COLUMN tags.name IS '标签名称，全局唯一';
COMMENT ON COLUMN tags.slug IS 'URL友好的标识符，用于前端路由';
COMMENT ON COLUMN tags.is_active IS '是否启用：true=可用，false=已隐藏（软删除）';
```

#### 3.1.2. categories（全局分类表）

```sql
CREATE TABLE categories (
    -- 核心标识 (在应用层通过 uuid.uuid4() 生成)
    id UUID PRIMARY KEY,
    
    name VARCHAR(100) NOT NULL UNIQUE,
    slug VARCHAR(120),
    icon VARCHAR(255),
    description TEXT,
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT true,
    
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_categories_sort_order ON categories(sort_order);
CREATE INDEX idx_categories_is_active ON categories(is_active);
CREATE INDEX idx_categories_active_sort ON categories(is_active, sort_order) 
WHERE is_active = true;

-- 触发器
CREATE TRIGGER set_timestamp_categories 
BEFORE UPDATE ON categories 
FOR EACH ROW EXECUTE PROCEDURE trigger_set_timestamp();

-- 注释
COMMENT ON TABLE categories IS '全局医学内容分类表（如：肝胆胰外科、胃肠外科等）';
COMMENT ON COLUMN categories.id IS '主键UUID，在应用层通过uuid.uuid4()生成';
COMMENT ON COLUMN categories.sort_order IS '排序权重，数字越小越靠前，用于前端展示顺序';
COMMENT ON COLUMN categories.icon IS '分类图标名称或URL';
COMMENT ON COLUMN categories.is_active IS '是否启用：true=前端可见，false=已下线';
```

#### 3.1.3. session_tags（场次-标签关联表）

```sql
CREATE TABLE session_tags (
    session_id UUID NOT NULL,
    tag_id UUID NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    PRIMARY KEY (session_id, tag_id),
    
    -- 命名的外键约束
    CONSTRAINT fk_session_tags_session_id 
        FOREIGN KEY (session_id) REFERENCES live_sessions(id) ON DELETE CASCADE,
    CONSTRAINT fk_session_tags_tag_id 
        FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_session_tags_session_id ON session_tags(session_id);
CREATE INDEX idx_session_tags_tag_id ON session_tags(tag_id);

-- 注释
COMMENT ON TABLE session_tags IS '直播场次与标签的多对多关联表';
COMMENT ON COLUMN session_tags.session_id IS '场次ID，关联live_sessions表';
COMMENT ON COLUMN session_tags.tag_id IS '标签ID，关联tags表';
```

### 3.2. 待审查的 SQLAlchemy 模型代码

**文件**: `live_core_service/app/models/content_management.py`

*请在此处提供 `content_management.py` 文件的完整内容*

---

## 4. 审查清单 (Audit Checklist)

### 4.1. 表与模型映射

**检查项列表**:
- [ ] 所有表是否都已正确映射为SQLAlchemy模型类
- [ ] 每个模型类的`__tablename__`是否与DDL表名完全对应
- [ ] 模型类命名是否符合Python规范（大驼峰命名法）
- [ ] 表名到模型名的映射关系（如：`tags` → `Tag`）

**表映射检查表**:

| DDL 表名 | 模型类名 | `__tablename__` | 是否一致 |
|---------|---------|----------------|---------|
| `tags` | `Tag` | `"tags"` | ? |
| `categories` | `Category` | `"categories"` | ? |
| `session_tags` | `SessionTag` | `"session_tags"` | ? |

---

### 4.2. 枚举类型定义

**检查项列表**:
- [ ] 是否定义了所有必需的枚举类
- [ ] 枚举类是否正确继承`str, enum.Enum`
- [ ] 枚举值是否完整且正确
- [ ] 枚举值的字符串形式是否与DDL中的ENUM值一致（通常为小写）

**枚举类型检查表**:

| 枚举类型 | DDL 定义 | 模型定义 | 是否一致 |
|---------|---------|---------|---------|
| N/A | 本模块无枚举类型 | N/A | N/A |

**注意**: 本模块不包含枚举类型，此检查项不适用。

---

### 4.3. 字段与列的完全对应

**检查项列表**:
- [ ] 每个DDL列是否都在模型中定义了对应字段
- [ ] 字段命名是否与DDL列名完全一致

#### 4.3.1. tags 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `name` | [ ] | [ ] | 标签名称，NOT NULL, UNIQUE |
| `slug` | [ ] | [ ] | URL友好标识符 |
| `description` | [ ] | [ ] | 标签描述 |
| `is_active` | [ ] | [ ] | 是否启用，DEFAULT true |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | [ ] | [ ] | 更新时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

#### 4.3.2. categories 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `id` | [ ] | [ ] | 主键 UUID |
| `name` | [ ] | [ ] | 分类名称，NOT NULL, UNIQUE |
| `slug` | [ ] | [ ] | URL友好标识符 |
| `icon` | [ ] | [ ] | 分类图标 |
| `description` | [ ] | [ ] | 分类描述 |
| `sort_order` | [ ] | [ ] | 排序权重，DEFAULT 0 |
| `is_active` | [ ] | [ ] | 是否启用，DEFAULT true |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |
| `updated_at` | [ ] | [ ] | 更新时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

#### 4.3.3. session_tags 表字段检查

| DDL 列名 | 是否存在 | 字段命名正确 | 备注 |
|---------|---------|------------|------|
| `session_id` | [ ] | [ ] | 复合主键之一，外键到 live_sessions.id |
| `tag_id` | [ ] | [ ] | 复合主键之一，外键到 tags.id |
| `created_at` | [ ] | [ ] | 创建时间，NOT NULL, DEFAULT CURRENT_TIMESTAMP |

---

### 4.4. 数据类型精确性

#### 4.4.1. UUID类型检查

**检查项列表**:
- [ ] 是否从`sqlalchemy.dialects.postgresql`导入`UUID`
- [ ] 所有UUID字段是否使用`UUID(as_uuid=True)`
- [ ] 主键`id`是否包含`default=uuid.uuid4`
- [ ] 是否导入了`import uuid`

**UUID类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `tags.id` | `UUID` | `UUID(as_uuid=True), primary_key=True, default=uuid.uuid4` | [ ] |
| `categories.id` | `UUID` | `UUID(as_uuid=True), primary_key=True, default=uuid.uuid4` | [ ] |
| `session_tags.session_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |
| `session_tags.tag_id` | `UUID` | `UUID(as_uuid=True), ForeignKey(...), primary_key=True` | [ ] |

#### 4.4.2. 字符串类型检查

**检查项列表**:
- [ ] VARCHAR(n)是否映射为`String(n)`
- [ ] TEXT是否映射为`Text`

**类型映射表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `tags.name` | `VARCHAR(80)` | `String(80)` | [ ] |
| `tags.slug` | `VARCHAR(100)` | `String(100)` | [ ] |
| `tags.description` | `TEXT` | `Text` | [ ] |
| `categories.name` | `VARCHAR(100)` | `String(100)` | [ ] |
| `categories.slug` | `VARCHAR(120)` | `String(120)` | [ ] |
| `categories.icon` | `VARCHAR(255)` | `String(255)` | [ ] |
| `categories.description` | `TEXT` | `Text` | [ ] |

#### 4.4.3. 数值类型检查

**检查项列表**:
- [ ] INT是否映射为`Integer`

**类型映射表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `categories.sort_order` | `INT` | `Integer` | [ ] |

#### 4.4.4. 布尔类型检查

**检查项列表**:
- [ ] BOOLEAN是否映射为`Boolean`

**类型映射表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `tags.is_active` | `BOOLEAN` | `Boolean` | [ ] |
| `categories.is_active` | `BOOLEAN` | `Boolean` | [ ] |

#### 4.4.5. 时间戳类型检查（关键）

**检查项列表**:
- [ ] 是否从`sqlalchemy`导入`TIMESTAMP`（不是`DateTime`）
- [ ] 是否从`sqlalchemy.sql`导入`func`
- [ ] 所有时间戳字段是否使用`TIMESTAMP(timezone=True)`
- [ ] `created_at`是否使用`server_default=func.now()`
- [ ] `updated_at`是否使用`server_default=func.now(), onupdate=func.now()`
- [ ] ⚠️ **严禁使用** `default=datetime.utcnow`（这是应用层默认值，不符合设计要求）

**正确示例代码**:

```python
from sqlalchemy import TIMESTAMP
from sqlalchemy.sql import func

created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
```

**时间戳类型检查表**:

| 字段 | DDL 类型 | 应映射为 | 是否正确 |
|------|---------|---------|---------|
| `tags.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |
| `tags.updated_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now()` | [ ] |
| `categories.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |
| `categories.updated_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now()` | [ ] |
| `session_tags.created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | [ ] |

---

### 4.5. 主键定义

**检查项列表**:
- [ ] 所有`id`字段是否标记为`primary_key=True`
- [ ] 所有`id`字段是否包含`default=uuid.uuid4`
- [ ] 是否在应用层生成UUID（而非数据库生成）
- [ ] 复合主键是否正确定义

**主键检查表**:

| 表名 | 主键类型 | 主键字段 | 是否正确定义 |
|------|---------|---------|------------|
| `tags` | 单字段主键 | `id` | [ ] |
| `categories` | 单字段主键 | `id` | [ ] |
| `session_tags` | 复合主键 | `(session_id, tag_id)` | [ ] |

---

### 4.6. 外键与级联规则

**为每个外键提供单独的检查项**:

**检查项列表**:
- [ ] 是否定义了`ForeignKey("[表名].id", ondelete="CASCADE")`
- [ ] `ondelete`是否正确设置为`"CASCADE"`（大写）
- [ ] 是否标记为`nullable=False`

**外键检查表**:

| 字段 | 外键到 | DDL 级联规则 | 应设置为 | 是否正确 |
|------|--------|------------|---------|---------|
| `session_tags.session_id` | `live_sessions.id` | `ON DELETE CASCADE` | `ForeignKey("live_sessions.id", ondelete="CASCADE")` | [ ] |
| `session_tags.tag_id` | `tags.id` | `ON DELETE CASCADE` | `ForeignKey("tags.id", ondelete="CASCADE")` | [ ] |

**特殊情况说明**:
- 跨服务字段（如`user_id`）**不应该**定义`ForeignKey`（因为跨服务）
- 是否在注释中说明"通过JWT Token应用层验证"或类似说明

---

### 4.7. 约束和默认值

#### 4.7.1. NOT NULL约束

**检查项列表**:
- [ ] 为每个模型分别列出需要检查的字段
- [ ] 检查是否正确设置`nullable=False`

**NOT NULL约束检查表**:

| 表名 | 字段 | DDL 约束 | 应设置为 | 是否正确 |
|------|------|---------|---------|---------|
| `tags` | `id` | `NOT NULL` | `nullable=False` | [ ] |
| `tags` | `name` | `NOT NULL` | `nullable=False` | [ ] |
| `tags` | `is_active` | `NOT NULL` | `nullable=False` | [ ] |
| `tags` | `created_at` | `NOT NULL` | `nullable=False` | [ ] |
| `tags` | `updated_at` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `id` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `name` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `sort_order` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `is_active` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `created_at` | `NOT NULL` | `nullable=False` | [ ] |
| `categories` | `updated_at` | `NOT NULL` | `nullable=False` | [ ] |
| `session_tags` | `session_id` | `NOT NULL` | `nullable=False` | [ ] |
| `session_tags` | `tag_id` | `NOT NULL` | `nullable=False` | [ ] |
| `session_tags` | `created_at` | `NOT NULL` | `nullable=False` | [ ] |

#### 4.7.2. NULL约束

**检查项列表**:
- [ ] 为每个模型分别列出需要检查的字段
- [ ] 检查是否正确设置`nullable=True`（或默认）

**NULL约束检查表**:

| 表名 | 字段 | DDL 约束 | 应设置为 | 是否正确 |
|------|------|---------|---------|---------|
| `tags` | `slug` | `NULL` | `nullable=True` | [ ] |
| `tags` | `description` | `NULL` | `nullable=True` | [ ] |
| `categories` | `slug` | `NULL` | `nullable=True` | [ ] |
| `categories` | `icon` | `NULL` | `nullable=True` | [ ] |
| `categories` | `description` | `NULL` | `nullable=True` | [ ] |

#### 4.7.3. 默认值

**检查项列表**:
- [ ] 检查所有有默认值的字段

**默认值对比表**:

| 字段 | DDL 默认值 | 应设置为 | 是否正确 |
|------|----------|---------|---------|
| `tags.is_active` | `DEFAULT true` | `default=True` | [ ] |
| `categories.sort_order` | `DEFAULT 0` | `default=0` | [ ] |
| `categories.is_active` | `DEFAULT true` | `default=True` | [ ] |
| 所有 `created_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now()` | [ ] |
| 所有 `updated_at` | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now(), onupdate=func.now()` | [ ] |

---

### 4.8. 唯一约束

**检查项列表**:
- [ ] 是否在字段定义中正确设置了`unique=True`
- [ ] 约束字段组合是否正确

**唯一约束检查表**:

| 表名 | 字段 | DDL 约束 | 应设置为 | 是否正确 |
|------|------|---------|---------|---------|
| `tags` | `name` | `UNIQUE` | `unique=True` | [ ] |
| `categories` | `name` | `UNIQUE` | `unique=True` | [ ] |
| `session_tags` | `(session_id, tag_id)` | `PRIMARY KEY` | `primary_key=True` (复合主键) | [ ] |

---

### 4.9. 索引定义

**检查项列表**:
- [ ] 是否定义了所有DDL中要求的索引
- [ ] 索引名称是否与DDL一致（或清晰表达含义）
- [ ] 组合索引的字段顺序是否正确
- [ ] ⚠️ **关键要求**: 所有索引必须使用`__table_args__`显式命名，**不能使用**`index=True`

**为每个模型分别检查索引定义**:

#### 4.9.1. tags 表索引检查

**DDL要求**:
```sql
CREATE INDEX idx_tags_name ON tags(name);
CREATE INDEX idx_tags_is_active ON tags(is_active);
```

**应定义为**:
```python
from sqlalchemy import Index

__table_args__ = (
    Index('idx_tags_name', 'name'),
    Index('idx_tags_is_active', 'is_active'),
)
```

**索引检查表**:

| 索引名 | DDL 定义 | 是否定义 | 是否正确 |
|--------|---------|---------|---------|
| `idx_tags_name` | `CREATE INDEX idx_tags_name ON tags(name)` | [ ] | [ ] |
| `idx_tags_is_active` | `CREATE INDEX idx_tags_is_active ON tags(is_active)` | [ ] | [ ] |

#### 4.9.2. categories 表索引检查

**DDL要求**:
```sql
CREATE INDEX idx_categories_sort_order ON categories(sort_order);
CREATE INDEX idx_categories_is_active ON categories(is_active);
CREATE INDEX idx_categories_active_sort ON categories(is_active, sort_order) 
WHERE is_active = true;
```

**应定义为**:
```python
from sqlalchemy import Index

__table_args__ = (
    Index('idx_categories_sort_order', 'sort_order'),
    Index('idx_categories_is_active', 'is_active'),
    # 注意：部分索引 idx_categories_active_sort 需要在DDL中定义
    # SQLAlchemy的Index不支持直接定义部分索引，可以在数据库迁移脚本中定义
)
```

**索引检查表**:

| 索引名 | DDL 定义 | 是否定义 | 是否正确 | 备注 |
|--------|---------|---------|---------|------|
| `idx_categories_sort_order` | `CREATE INDEX idx_categories_sort_order ON categories(sort_order)` | [ ] | [ ] | - |
| `idx_categories_is_active` | `CREATE INDEX idx_categories_is_active ON categories(is_active)` | [ ] | [ ] | - |
| `idx_categories_active_sort` | `CREATE INDEX idx_categories_active_sort ON categories(is_active, sort_order) WHERE is_active = true` | [ ] | [ ] | 部分索引，需在DDL中定义 |

#### 4.9.3. session_tags 表索引检查

**DDL要求**:
```sql
CREATE INDEX idx_session_tags_session_id ON session_tags(session_id);
CREATE INDEX idx_session_tags_tag_id ON session_tags(tag_id);
```

**应定义为**:
```python
from sqlalchemy import Index

__table_args__ = (
    Index('idx_session_tags_session_id', 'session_id'),
    Index('idx_session_tags_tag_id', 'tag_id'),
)
```

**索引检查表**:

| 索引名 | DDL 定义 | 是否定义 | 是否正确 |
|--------|---------|---------|---------|
| `idx_session_tags_session_id` | `CREATE INDEX idx_session_tags_session_id ON session_tags(session_id)` | [ ] | [ ] |
| `idx_session_tags_tag_id` | `CREATE INDEX idx_session_tags_tag_id ON session_tags(tag_id)` | [ ] | [ ] |

---

### 4.10. 关系完整性

**为每个关系提供单独的检查项**:

**检查项列表**:
- [ ] 是否定义了`relationship("[模型名]", back_populates="[反向关系名]")`
- [ ] `back_populates`是否正确指向反向关系
- [ ] 是否设置了`cascade="all, delete-orphan"`（如适用）
- [ ] 关系名称是否符合Python命名规范

**关系检查表**:

| 模型 | 关系名 | 目标模型 | `back_populates` | `cascade` | 是否正确 |
|------|--------|---------|----------------|-----------|---------|
| `Tag` | `session_tags` | `SessionTag` | `"tag"` | `"all, delete-orphan"` | [ ] |
| `SessionTag` | `tag` | `Tag` | `"session_tags"` | - | [ ] |
| `SessionTag` | `session` | `LiveSession` | - | - | [ ] |

**双向关系检查**:
- [ ] 检查两个模型中的关系定义是否相互对应
- [ ] `back_populates`是否在两端都正确设置

**懒加载策略检查**:
- [ ] 是否使用`lazy="selectin"`避免N+1查询（可选，但推荐）
- [ ] 是否根据业务需求选择合适的懒加载策略

---

### 4.11. 导入语句检查

**检查项列表**（必须包含所有必需的导入）:
- [ ] 是否导入了`import uuid`
- [ ] 是否导入了`from datetime import datetime`
- [ ] 是否导入了`from sqlalchemy import Column, String, Text, Integer, ForeignKey, Boolean, Index, TIMESTAMP`
- [ ] 是否导入了`from sqlalchemy.dialects.postgresql import UUID`
- [ ] 是否导入了`from sqlalchemy.orm import relationship`
- [ ] 是否导入了`from sqlalchemy.sql import func`
- [ ] 是否导入了`from app.database import Base`
- [ ] 导入顺序是否正确（标准库 → 第三方库 → 本地模块）

**导入语句检查表**:

| 导入语句 | 是否导入 | 是否正确 |
|---------|---------|---------|
| `import uuid` | [ ] | [ ] |
| `from datetime import datetime` | [ ] | [ ] |
| `from sqlalchemy import Column, String, Text, Integer, ForeignKey, Boolean, Index, TIMESTAMP` | [ ] | [ ] |
| `from sqlalchemy.dialects.postgresql import UUID` | [ ] | [ ] |
| `from sqlalchemy.orm import relationship` | [ ] | [ ] |
| `from sqlalchemy.sql import func` | [ ] | [ ] |
| `from app.database import Base` | [ ] | [ ] |

---

### 4.12. 代码风格与文档

**检查项列表**:
- [ ] 每个模型类是否包含文档字符串
- [ ] 关键字段是否包含中文注释或`comment`参数
- [ ] 是否遵循PEP 8代码风格
- [ ] 类名是否使用大驼峰命名法
- [ ] 字段名是否使用下划线命名法
- [ ] 是否包含`__repr__`方法（可选，但推荐）

**代码风格检查表**:

| 检查项 | 是否满足 | 备注 |
|--------|---------|------|
| 模型类文档字符串 | [ ] | 每个模型类应包含中文文档字符串 |
| 字段注释 | [ ] | 关键字段应包含`comment`参数 |
| PEP 8规范 | [ ] | 遵循Python代码风格 |
| 类名命名 | [ ] | 大驼峰命名法（Tag, Category, SessionTag） |
| 字段名命名 | [ ] | 下划线命名法（session_id, tag_id） |
| `__repr__`方法 | [ ] | 可选，但推荐添加 |

---

## 5. 审查标准 (Audit Standards)

审查时请遵循以下标准：

### 5.1. 严格标准 (Strict Standards)

以下项目必须100%符合DDL规范，任何偏差都算作**失败**：
- 表名、字段名
- 数据类型（特别是 TIMESTAMP vs DateTime）
- 外键约束和级联规则
- NOT NULL 约束
- UNIQUE 约束

### 5.2. 宽松标准 (Flexible Standards)

以下项目允许合理的实现差异：
- 索引名称（只要能清晰表达含义即可）
- 注释内容（只要能说明用途即可）
- 关系名称（只要符合Python命名规范即可）
- `__repr__` 方法（可选）

### 5.3. 推荐实践 (Best Practices)

以下项目不影响功能，但建议遵循：
- 添加 `__repr__` 方法便于调试
- 使用 `lazy="selectin"` 避免 N+1 查询
- 添加详细的中文注释
- 使用 `comment` 参数为字段添加数据库注释

---

## 6. 使用说明 (Usage Instructions)

### 6.1. 使用步骤

1. 将 `live_core_service/app/models/content_management.py` 的完整内容粘贴到 Section 3.2
2. 运行此提示词，让 AI 生成详细的审查报告
3. 根据报告中的"关键问题清单"和"修正代码示例"进行修复
4. 重新运行审查，直到所有检查项通过

### 6.2. 审查重点

特别关注以下容易出错的地方：
1. ⚠️ **时间戳字段** - 必须使用 `TIMESTAMP` + `server_default=func.now()`
2. ⚠️ **索引定义** - 必须使用 `__table_args__` 显式命名
3. ⚠️ **外键级联** - `ondelete="CASCADE"` 大写
4. ⚠️ **UUID主键** - 必须包含 `default=uuid.uuid4`

---

## 7. 示例报告格式 (Example Report Format)

### 示例 1: 时间戳字段错误

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 数据类型: tags.created_at | ❌ **失败** | 使用了 `DateTime` 而非 `TIMESTAMP`；使用了 `default=datetime.utcnow` 而非 `server_default=func.now()` | 见下方修正代码 |

**修正代码**:
```python
# ❌ 当前代码（错误）
from sqlalchemy import DateTime
created_at = Column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)

# ✅ 修正后代码
from sqlalchemy import TIMESTAMP
from sqlalchemy.sql import func
created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now())
```

### 示例 2: 索引定义方式错误

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 索引: tags.name | ❌ **失败** | 使用了 `index=True` 而非 `__table_args__` 中的显式索引定义 | 见下方修正代码 |

**修正代码**:
```python
# ❌ 当前代码（错误）
name = Column(String(80), nullable=False, unique=True, index=True)

# ✅ 修正后代码
name = Column(String(80), nullable=False, unique=True)

__table_args__ = (
    Index('idx_tags_name', 'name'),
    # ... 其他索引
)
```

---

## 8. 验证完成标准 (Completion Criteria)

审查报告完成后，必须明确回答以下问题：

1. ✅ / ❌ 所有表是否正确映射？
2. ✅ / ❌ 所有字段是否完整对应？
3. ✅ / ❌ 所有数据类型是否精确匹配？
4. ✅ / ❌ 所有索引是否正确定义？
5. ✅ / ❌ 所有外键和级联规则是否正确？
6. ✅ / ❌ 所有约束和默认值是否正确？
7. ✅ / ❌ 所有关系是否完整定义？
8. ✅ / ❌ 代码是否可以直接运行而不报错？

**只有当以上8个问题全部回答"✅"时，才认为模型代码与设计文档完全一致。**

---

## 9. 最终交付 (Final Deliverable)

请生成一份 **Markdown 格式**的详细审查报告，包含以下部分：

### 9.1. 总体结论 (Overall Conclusion)

一句话总结代码与设计规范的符合程度：
- ✅ **完全一致** - 所有检查项均通过
- ⚠️ **基本一致，存在小问题** - 大部分通过，存在少量非关键问题
- ❌ **存在严重偏差** - 存在关键问题需要修复

### 9.2. 符合性评分 (Compliance Score)

| 类别 | 通过项 | 总检查项 | 通过率 |
|------|-------|---------|--------|
| 表与模型映射 | x/3 | 3 | xx% |
| 枚举类型定义 | x/0 | 0 | N/A |
| 字段对应性 | x/20 | 20 | xx% |
| 数据类型精确性 | x/15 | 15 | xx% |
| 主键定义 | x/3 | 3 | xx% |
| 外键与级联规则 | x/2 | 2 | xx% |
| 约束和默认值 | x/15 | 15 | xx% |
| 唯一约束 | x/2 | 2 | xx% |
| 索引定义 | x/7 | 7 | xx% |
| 关系完整性 | x/3 | 3 | xx% |
| 导入语句检查 | x/7 | 7 | xx% |
| 代码风格与文档 | x/6 | 6 | xx% |
| **总计** | **x/83** | **83** | **xx%** |

### 9.3. 详细审查结果表 (Detailed Audit Results)

使用表格形式，逐项列出审查结果：

| 审查项 | 审查结果 | 问题描述 | 修正建议 |
|--------|---------|---------|---------|
| 表映射: tags | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 字段: tags.name | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 数据类型: TIMESTAMPTZ | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| 索引: idx_tags_name | ✅ 通过 / ❌ 失败 | 具体问题 | 修正代码 |
| ... | ... | ... | ... |

### 9.4. 关键问题清单 (Critical Issues)

列出所有 ❌ 失败的检查项，按优先级排序：

**高优先级（必须修复）**:
1. 问题描述 + 修正代码

**中优先级（建议修复）**:
1. 问题描述 + 修正代码

**低优先级（可选优化）**:
1. 问题描述 + 修正代码

### 9.5. 修正代码示例 (Corrected Code Examples)

对于每个失败的检查项，提供完整的修正代码片段。

**示例格式**:
```python
# ❌ 错误示例
[错误代码]

# ✅ 正确示例
[正确代码]
```

### 9.6. 一致性总结 (Consistency Summary)

总结模型代码与设计文档的一致性程度，包括：
- 完全一致的部分（值得肯定）
- 存在偏差的部分（需要修正）
- 改进建议（如有）

---

**文档版本**: V1.0  
**创建日期**: 2026-01-18  
**适用于**: 内容管理模块设计文档-标签-分类-场次标签关联 数据库模型一致性验证

