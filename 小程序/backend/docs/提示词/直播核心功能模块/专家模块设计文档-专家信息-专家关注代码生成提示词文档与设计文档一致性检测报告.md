# 专家模块设计文档-专家信息-专家关注 代码生成提示词文档与设计文档一致性检测报告

**生成日期**: 2026-01-18  
**审查范围**: 专家模块设计文档-专家信息-专家关注  
**审查文件**: 
- `docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---CRUD层代码生成提示词.md`
- `docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md`
- `docs/03_系统设计/直播核心功能设计文档_v6_专家模块设计文档-专家信息-专家关注.md`

---

## 1. 总体评估

- **一致性评分**: 98/100
- **发现问题数量**: 2
  - **严重问题**: 0
  - **中等问题**: 1
  - **轻微问题**: 1

**总体结论**: ⚠️ **基本一致，存在小问题** - 大部分检查项通过，存在少量非关键问题

---

## 2. 详细检测结果

### 2.1. 功能覆盖度 (Function Coverage)

* **检测点 1.1 (功能完整性)**：**[一致]**
  - **说明**: 设计文档中提及的所有功能点、API接口、业务逻辑都在提示词文档中被完整覆盖：
    - ✅ 专家信息管理功能（创建、更新、删除、获取详情、获取列表、获取推荐列表）
    - ✅ 专家关注功能（关注、取消关注、获取关注列表）
    - ✅ 场次专家关联功能（设置场次专家、获取场次专家列表）

* **检测点 1.2 (功能冗余)**：**[不一致]**
  - **说明**: 提示词文档中存在设计文档未明确提及的API端点：
    - `GET /api/v1/experts/{expert_id}` - 获取专家详情（设计文档中只有`GET /api/v1/experts/{expert_id}/sessions`）
    - `GET /api/v1/experts/{expert_id}/is-followed` - 检查是否已关注（设计文档中未明确提及）
  - **评估**: 这些端点是合理的补充，符合RESTful最佳实践，但应在设计文档中明确说明

---

### 2.2. 业务逻辑与执行流程 (Business Logic & Flow)

* **检测点 2.1 (步骤一致性)**：**[一致]**
  - **说明**: 提示词文档中描述的函数执行流程与设计文档中描述的业务步骤完全一致：
    - ✅ 创建专家流程：验证JWT Token -> 检查管理员权限 -> 验证user_id（如果提供）-> 检查user_id是否已被绑定 -> 创建专家记录
    - ✅ 关注专家流程：验证JWT Token -> 检查专家是否存在 -> 检查是否已关注 -> 创建关注记录
    - ✅ 获取关注列表流程：验证JWT Token -> 查询关注记录 -> 查询专家信息 -> 查询直播状态（如果include_live_status=true）

* **检测点 2.2 (数据流转)**：**[一致]**
  - **说明**: 设计文档中描述的数据处理逻辑在提示词文档中得到准确体现：
    - ✅ Service层需要验证user_id是否已被绑定
    - ✅ CRUD层负责软删除：设置is_featured=false
    - ✅ Service层负责构造响应（将CRUD结果转换为Schema响应）

* **检测点 2.3 (事务边界)**：**[一致]**
  - **说明**: 设计文档中（明示或暗示）的事务处理范围在提示词文档中被正确实现：
    - ✅ 创建专家和验证user_id必须是原子的（在Service层统一处理事务）
    - ✅ CRUD层写操作包含完整的异常处理（try/except IntegrityError/finally）

---

### 2.3. 数据库规范 (Database Specifications)

* **检测点 3.1 (字段名称)**：**[一致]**
  - **说明**: CRUD层提示词文档中提到的数据库Model字段名与设计文档中的字段名完全一致：
    - ✅ `expert_id`, `user_id`, `is_featured`, `sort_order`, `role`, `session_id` 等字段名完全匹配

* **检测点 3.2 (字段类型)**：**[一致]**
  - **说明**: CRUD层提示词文档中提到的数据库Model字段类型与设计文档中的类型定义完全一致：
    - ✅ `UUID`, `String(120)`, `String(200)`, `String(512)`, `Text`, `Boolean`, `Integer`, `JSONB`, `TIMESTAMP(timezone=True)` 等类型完全匹配

* **检测点 3.3 (字段约束)**：**[一致]**
  - **说明**: CRUD层提示词文档中提到的约束与设计文档中的约束要求完全一致：
    - ✅ `experts.user_id` 具有 `UNIQUE` 约束
    - ✅ `user_expert_subscriptions` 表：`UNIQUE(user_id, expert_id)` 约束
    - ✅ `live_session_experts` 表：`UNIQUE(session_id, expert_id, role)` 约束
    - ✅ 所有外键使用 `ON DELETE CASCADE`
    - ✅ 时间戳字段使用 `TIMESTAMP(timezone=True)` 和 `server_default=func.now()`

---

### 2.4. API 结构规范 (API Structure)

* **检测点 4.1 (路由 URL)**：**[基本一致]**
  - **说明**: 提示词文档中的API路由与设计文档基本一致，但存在路径前缀差异：
    - ✅ 设计文档：`GET /api/v1/featured-experts` → 提示词文档：`GET /featured-experts`（前缀在api.py中统一指定，符合规范）
    - ✅ 设计文档：`POST /api/v1/admin/experts` → 提示词文档：`POST /admin/experts`（前缀在api.py中统一指定，符合规范）
    - ✅ 设计文档：`POST /api/v1/users/me/followed-experts` → 提示词文档：`POST /users/me/followed-experts`（前缀在api.py中统一指定，符合规范）
    - ⚠️ 提示词文档中额外包含：`GET /{expert_id}` 和 `GET /{expert_id}/is-followed`（设计文档未明确提及，但符合RESTful最佳实践）

* **检测点 4.2 (HTTP 方法)**：**[一致]**
  - **说明**: 提示词文档中的HTTP方法与设计文档完全一致：
    - ✅ `GET`, `POST`, `PATCH`, `DELETE` 方法完全匹配

* **检测点 4.3 (请求体 Schema)**：**[一致]**
  - **说明**: 提示词文档中的请求体结构、字段名、类型、是否必填与设计文档完全一致：
    - ✅ `ExpertCreate` Schema：包含所有设计文档中要求的字段（user_id, name, title, hospital, department, expertise_areas, bio, avatar_url, is_featured, sort_order, contact_info）
    - ✅ `ExpertUpdate` Schema：所有字段Optional，支持部分更新
    - ✅ `ExpertFollowRequest` Schema：包含expert_id字段

* **检测点 4.4 (查询参数)**：**[一致]**
  - **说明**: 提示词文档中的查询参数名称、类型、默认值与设计文档完全一致：
    - ✅ `GET /featured-experts`：`limit` (int, 可选, 默认10, 最大50)
    - ✅ `GET /{expert_id}/sessions`：`page` (int, 可选, 默认1), `size` (int, 可选, 默认10), `role` (string, 可选)
    - ✅ `GET /admin/experts`：`page`, `size`, `name`, `is_featured`, `hospital`, `sort`
    - ✅ `GET /users/me/followed-experts`：`include_live_status` (bool, 可选, 默认true)

---

### 2.5. 响应与异常处理 (Response & Exception Handling)

* **检测点 5.1 (成功响应)**：**[一致]**
  - **说明**: 提示词文档中定义的成功响应结构、字段名、和success_response规范与设计文档完全一致：
    - ✅ 成功响应格式：`{"code": 200, "message": "success", "data": {...}, "timestamp": "..."}`
    - ✅ 使用 `success_response(data=...)` 函数构建响应

* **检测点 5.2 (业务异常)**：**[一致]**
  - **说明**: 设计文档中提及的业务异常情况在提示词文档中有对应的异常抛出：
    - ✅ "专家不存在" → `NotFoundException` (code=2001)
    - ✅ "该用户已绑定到其他专家档案" → `InvalidParameterException` (code=2004)
    - ✅ "您已关注该专家" → `InvalidParameterException` (code=2002)
    - ✅ "未关注该专家" → `NotFoundException` (code=2001)

* **检测点 5.3 (错误码 Code)**：**[一致]**
  - **说明**: 提示词文档中返回的错误码与设计文档中定义的消息码规范完全一致：
    - ✅ `2001` - 资源不存在
    - ✅ `2002` - 资源已存在
    - ✅ `2004` - 业务逻辑错误
    - ✅ `4001` - 参数校验失败
    - ✅ `3003` - 权限不足

* **检测点 5.4 (HTTP 状态码)**：**[一致]**
  - **说明**: 提示词文档中返回的HTTP状态码与设计文档中定义的完全一致：
    - ✅ `200` - 成功
    - ✅ `400` - 参数错误
    - ✅ `404` - 资源不存在
    - ✅ `409` - 资源已存在（用于已关注的情况）
    - ✅ `403` - 权限不足
    - ✅ `500` - 服务器错误

---

### 2.6. 跨层规范 (Cross-Cutting Concerns)

* **检测点 6.1 (安全规范)**：**[一致]**
  - **说明**: 设计文档中提及的安全要求在提示词文档中得到体现：
    - ✅ 管理员接口使用 `Depends(get_current_user)` 和 `_check_admin_permission(role)`
    - ✅ 登录用户接口使用 `Depends(get_current_user)` 和 `_check_write_permission(role)`
    - ✅ 公开接口使用 `Depends(get_current_user_optional)`（Optional Auth）
    - ✅ 权限检查在Service层执行（符合"学院派"架构规范）

* **检测点 6.2 (日志规范)**：**[一致]**
  - **说明**: 设计文档中提及的日志记录点在提示词文档中有明确的日志记录指令：
    - ✅ 创建、更新、删除操作记录INFO日志
    - ✅ UUID脱敏（只记录前8位）
    - ✅ 异常情况记录ERROR或WARNING日志

* **检测点 6.3 (隐式规范)**：**[一致]**
  - **说明**: 设计文档中所有隐式的规范在提示词文档中得到了严格遵守：
    - ✅ 使用 `expert_id` 而非 `id`（在路径参数中）
    - ✅ 使用 `user_id` 而非 `user_public_id`（符合设计文档的命名约定）
    - ✅ 软删除使用 `is_featured=false` 而非硬删除
    - ✅ 敏感字段 `contact_info` 不在公开API响应中暴露

---

### 2.7. API-Service对应关系检测 (API-Service Mapping Verification)

* **检测点 7.1 (API端点 → Service方法)**：**[一致]**
  - **说明**: 提示词文档中列出的所有API端点都有对应的Service方法：
  
  **对应关系表**:
  
  | API端点 | Service方法 | 是否对应 |
  |---------|------------|---------|
  | `GET /featured-experts` | `get_featured_experts` | ✅ |
  | `GET /{expert_id}` | `get_expert_detail` | ✅ |
  | `GET /{expert_id}/sessions` | `get_expert_sessions` | ✅ |
  | `POST /admin/experts` | `create_expert` | ✅ |
  | `GET /admin/experts` | `get_experts_list` | ✅ |
  | `PATCH /admin/experts/{expert_id}` | `update_expert` | ✅ |
  | `DELETE /admin/experts/{expert_id}` | `delete_expert` | ✅ |
  | `POST /users/me/followed-experts` | `follow_expert` | ✅ |
  | `DELETE /users/me/followed-experts/{expert_id}` | `unfollow_expert` | ✅ |
  | `GET /users/me/followed-experts` | `get_followed_experts` | ✅ |
  | `GET /{expert_id}/is-followed` | `check_is_followed` | ✅ |
  | `POST /sessions/{session_id}/experts` | `set_session_experts` | ✅ |
  | `GET /sessions/{session_id}/experts` | `get_session_experts` | ✅ |

* **检测点 7.2 (Service方法 → API端点)**：**[一致]**
  - **说明**: 提示词文档中列出的所有Service方法（排除权限守卫函数）都有对应的API端点：
  
  **对应关系表**:
  
  | Service方法 | API端点 | 是否对应 |
  |------------|---------|---------|
  | `get_featured_experts` | `GET /featured-experts` | ✅ |
  | `get_expert_detail` | `GET /{expert_id}` | ✅ |
  | `get_expert_sessions` | `GET /{expert_id}/sessions` | ✅ |
  | `create_expert` | `POST /admin/experts` | ✅ |
  | `get_experts_list` | `GET /admin/experts` | ✅ |
  | `update_expert` | `PATCH /admin/experts/{expert_id}` | ✅ |
  | `delete_expert` | `DELETE /admin/experts/{expert_id}` | ✅ |
  | `follow_expert` | `POST /users/me/followed-experts` | ✅ |
  | `unfollow_expert` | `DELETE /users/me/followed-experts/{expert_id}` | ✅ |
  | `get_followed_experts` | `GET /users/me/followed-experts` | ✅ |
  | `check_is_followed` | `GET /{expert_id}/is-followed` | ✅ |
  | `set_session_experts` | `POST /sessions/{session_id}/experts` | ✅ |
  | `get_session_experts` | `GET /sessions/{session_id}/experts` | ✅ |
  
  **权限守卫函数**（不需要对应的API端点）：
  - `_check_admin_permission` - 权限守卫函数
  - `_check_write_permission` - 权限守卫函数
  - `_check_expert_visibility` - 权限守卫函数

---

## 3. 问题清单

### 3.1. 严重问题（必须修复）

无

### 3.2. 中等问题（建议修复）

1. **API端点补充说明**
   - **问题描述**: 提示词文档中包含设计文档未明确提及的API端点：
     - `GET /api/v1/experts/{expert_id}` - 获取专家详情
     - `GET /api/v1/experts/{expert_id}/is-followed` - 检查是否已关注
   - **影响**: 这些端点是合理的补充，符合RESTful最佳实践，但应在设计文档中明确说明，或在提示词文档中标注为"补充功能"
   - **修正建议**: 
     - 方案1：在设计文档中补充这两个API端点的说明
     - 方案2：在提示词文档中标注这些端点为"补充功能"，说明其设计意图

### 3.3. 轻微问题（可选优化）

1. **路由路径前缀说明**
   - **问题描述**: 提示词文档中的API路由路径不包含 `/api/v1` 前缀，这是符合规范的（前缀在api.py中统一指定），但应在文档中明确说明这一点
   - **影响**: 轻微，不影响功能实现
   - **修正建议**: 在提示词文档的API层代码生成部分明确说明："路由路径不包含 `/api/v1` 前缀，前缀在 `app/api/v1/api.py` 中统一指定"

---

## 4. 总体建议

### 4.1. 优先修复项

1. **API端点补充说明**（中等问题）
   - 建议在设计文档中补充 `GET /api/v1/experts/{expert_id}` 和 `GET /api/v1/experts/{expert_id}/is-followed` 的说明
   - 或者在提示词文档中标注这些端点为"补充功能"

### 4.2. 可选优化项

1. **路由路径前缀说明**（轻微问题）
   - 在提示词文档中明确说明路由路径前缀的处理方式

### 4.3. 架构改进建议

无 - 架构设计合理，符合"学院派"架构规范和RESTful最佳实践

---

## 5. 一致性总结

### 5.1. 完全一致的部分（值得肯定）

1. ✅ **功能覆盖完整** - 所有设计文档中提及的功能都在提示词文档中被完整覆盖
2. ✅ **业务逻辑一致** - 执行流程、数据流转、事务边界与设计文档完全一致
3. ✅ **数据库规范一致** - 字段名、类型、约束与设计文档完全一致
4. ✅ **API结构规范一致** - HTTP方法、请求体Schema、查询参数与设计文档完全一致
5. ✅ **响应与异常处理一致** - 成功响应、业务异常、错误码、HTTP状态码与设计文档完全一致
6. ✅ **跨层规范一致** - 安全规范、日志规范、隐式规范与设计文档完全一致
7. ✅ **API-Service对应关系完整** - 所有API端点都有对应的Service方法，所有Service方法都有对应的API端点

### 5.2. 存在偏差的部分

1. ⚠️ **API端点补充** - 提示词文档中包含设计文档未明确提及的2个API端点（但符合RESTful最佳实践）
2. ⚠️ **路由路径前缀说明** - 提示词文档中未明确说明路由路径前缀的处理方式（但符合项目规范）

### 5.3. 改进建议

1. **建议在设计文档中补充**：
   - `GET /api/v1/experts/{expert_id}` - 获取专家详情（独立端点，不包含场次信息）
   - `GET /api/v1/experts/{expert_id}/is-followed` - 检查当前用户是否已关注该专家

2. **建议在提示词文档中补充说明**：
   - 路由路径前缀的处理方式（前缀在api.py中统一指定）

---

## 6. 最终结论

✅ **提示词文档与设计文档基本一致，存在少量非关键问题**

所有核心功能、业务逻辑、数据库规范、API结构、响应与异常处理、跨层规范、API-Service对应关系都与设计文档完全一致。存在的2个问题都是非关键问题，不影响功能实现，但建议在设计文档或提示词文档中补充说明。

**可以继续执行步骤6（生成CRUD/Service/API代码）**

---

**报告生成时间**: 2026-01-18  
**审查人**: AI自动化检测系统  
**审查状态**: ⚠️ 基本一致，存在小问题  
**一致性评分**: 98/100
