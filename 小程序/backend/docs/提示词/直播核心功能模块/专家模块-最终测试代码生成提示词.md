# 专家模块 - 最终测试代码生成提示词

**文档说明**：本文档为专家模块测试代码生成的**主用提示词**。`专家模块_测试代码生成提示词.md` 为历史版（全新生成模式、部分约定已过时），已由本文档取代，新生成请使用本文档。

**测试模式**：增量测试生成模式（为专家模块新增测试，已有测试文件不能修改）

**说明**：
- 项目已存在测试文件（tests/conftest.py、tests/unit/、tests/integration/等）
- 为专家模块（Expert Info、Expert Follow、Session Expert）**新增**测试代码
- **严禁修改**已存在的测试文件，即使发现错误也不行
- **允许修改** `tests/conftest.py`（**仅限添加新内容**）

---

## 1. 角色定义与测试模式 (Role Definition & Test Mode - 执行者)

你是一名资深的 Python 测试架构师。你精通使用 `pytest`、`pytest-asyncio`、`httpx` 和 `pytest-mock`，并且是"混合测试策略"的专家。

**⚠️ 测试模式说明**：
- **测试模式**：**增量测试生成模式**
- **说明**：为专家模块（Expert Info、Expert Follow、Session Expert）**新增**测试代码，在已有测试基础上增量生成。**严禁修改**已存在的测试文件。

**⚠️ 增量测试模式的特殊要求**：
- **严禁修改**已存在的测试函数（即使是修复错误也不行，除非用户明确要求）。
- **允许修改 `conftest.py`**（仅限添加新内容）：
  - ✅ **只增加**新的Fixture、函数、变量、导入等。
  - ❌ **严禁修改**已有的Fixture、函数、变量等。
  - ❌ **严禁删除**任何现有内容。
  - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
- **必须使用增量判断方式**验证数据库操作（记录测试前状态，只验证增量变化）。

你的任务是**读取**下面 `Section 6` 中提供的应用程序源代码（以及已存在的测试文件），并**严格按照** `Section 2-5` 的"核心规则"和 `Section 7` 的"具体任务列表"，为它们生成一个**完整、健壮、可运行的 `pytest` 测试文件**。

---

## 2. 核心测试哲学 (The Hybrid Testing Strategy - MANDATORY)

你**必须**根据被测试代码的层级，严格遵循以下混合测试策略。这是最高优先级指令。

| 测试目标层级 | 强制测试风格 | 核心工具 | 测试目标 (你必须验证什么？) |
| :--- | :--- | :--- | :--- |
| **API / Endpoint 层** | **实用派 (Pragmatic)** | `httpx.AsyncClient` + `db_session` + `async_session_factory` | **验证完整链路**：HTTP 状态码 + JSON 响应结构（`code`, `message`, `data`）以及*真实*的数据库状态变化（**注意**：必须使用**新会话**查询以避免缓存陷阱）。 |
| **Service (服务) 层** | **学院派 (Academic)** | `mocker` (Mock 掉 CRUD/I/O) | **验证业务逻辑**：*隔离地*测试权限检查、数据编排、以及自定义异常（`pytest.raises`）的抛出。 |
| **CRUD (数据) 层** | **实用派 (Pragmatic)** | `db_session` (真实数据库) | **验证数据库状态**：*必须*通过 `db.refresh()` 或**再次查询**来"证明"数据已被正确创建、更新或删除。 |

---

## 3. 核心实现规范 (Core Implementation Requirements - MANDATORY)

所有生成的测试代码**必须**无条件遵守以下规范。

### 3.1. 测试环境配置 (`conftest.py`)

你必须假设 `conftest.py`（或 `app.db.session`）已经按如下方式配置好：
1.  `setup_database` (scope="session", autouse=True): 自动创建和删除表。
2.  `db_session` (scope="function"): 提供用于**测试准备 (Arrange)** 和 **CRUD 测试**的会话。
3.  `async_client` (scope="function"): 提供 `httpx.AsyncClient`。
4.  **`async_session_factory`**: (来自 `app.db.session` 或 `tests.conftest`) 提供了创建**全新独立会话**的能力，专用于在 API 测试的断言阶段验证数据库状态，以规避身份映射缓存。

**⚠️ 增量测试模式特殊约束**：
- **允许修改 `conftest.py`**（仅限添加新内容）：可以添加新的Fixture、函数或变量，但**必须严格遵守以下规则**：
  - ✅ **只增加新的内容**：只能添加新的Fixture、函数、变量、导入等。
  - ❌ **严禁修改已有的内容**：不能修改、删除或重命名现有的Fixture、函数、变量等。
  - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
  - ✅ **保持代码风格一致**：新增的内容必须遵循现有代码的格式、命名约定和代码风格。
- **必须复用现有Fixture**：优先使用 `conftest.py` 中已定义的Fixture，避免重复创建。
- **新增Fixture的位置**：
  - **优先**：在 `conftest.py` 中添加新Fixture（如果没有命名冲突或功能重复）。
  - **备选**：如果存在命名冲突或功能重复，在测试文件内部定义（使用 `@pytest.fixture`）。

### 3.2. 结构化测试 (Arrange, Act, Assert)

所有测试用例**必须**遵循"准备 (Arrange)"、"执行 (Act)"、"断言 (Assert)"的逻辑结构，并使用注释将其清晰分开。

### 3.3. 严格的异步测试语法 (MANDATORY `async for` Syntax)

所有使用异步 `pytest` Fixture (如 `db_session`, `async_client`) 的测试函数**必须**使用 `async for` 语法来解包。

**⚠️ 禁止的写法**：
- 不得在 `async for` 外部调用数据库操作。
- 不得直接使用 fixture 参数而不解包。
- 不得在函数参数中添加类型提示（`db_session: AsyncSession`）。

```python
# ✅ 正确的 CRUD 测试模板
@pytest.mark.asyncio
async def test_crud_operation(db_session):
    """测试描述"""
    async for db in db_session:
        # ...所有数据库逻辑必须在此块内...
        pass

# ✅ 正确的 API + DB 组合测试模板
@pytest.mark.asyncio
async def test_api_with_db(async_client, db_session):
    """API 和数据库组合测试"""
    async for client in async_client:
        async for db in db_session:
            # ...所有组合测试逻辑必须在此嵌套块内...
            pass
```

### 3.4. 测试数据隔离 (MANDATORY for Pragmatic Tests)

**指令**: 为了防止测试之间因违反数据库 `UNIQUE` 约束而产生冲突，所有唯一性字段（如 `name`, `email`）**必须**是随机生成的。

**实现**: **必须**导入 `Faker` 和 `uuid` (以及 `random` 和 `string`，如果需要)。

**具体要求 (Faker 范例)**:
- **`name`**: `f"{fake.word()}_{uuid.uuid4().hex[:6]}"`
- **`email`**: `fake.email()`
- **`slug`**: `f"{fake.word()}_{uuid.uuid4().hex[:6]}"`

**禁止硬编码**: **严禁**使用硬编码的字符串（如 `'test-tag'`）作为唯一性字段的值，除非测试目的就是为了验证重复性。

### 3.5. [关键] 统一的用户身份解析规范 (MANDATORY - V6.0 修复)

**🚨（新增）API 层用户提取规范（JWT→UUID+String）**
**`current_user` 永远是一个 JWT payload `dict`，而不是 ORM `User` 模型**。

API 层必须使用以下模式解析：

```python
current_user: Dict = Depends(get_current_user)
```

```python
user_id = uuid.UUID(current_user.get("user_id") or current_user.get("sub"))
user_role = current_user.get("role", "REGULAR").upper()
```

**⚠️ 重要说明**：
1. JWT Payload 中的 `role` 字段已经是 **string** 类型，无需转换为枚举
2. 本项目**统一使用字符串**进行角色判断（参考用户模块设计文档）
3. 只有 `live_features` 模块使用 `LiveRoomMessageUserRole` 枚举（这是该模块的特殊需求）
4. 其他模块（topic, room, content_management, experts 等）都应该使用字符串

**🚨（新增）Service 层用户参数规范**
Service 层方法**不得**接受实体类 `User`，**必须**统一为：

```python
async def some_action(
    self,
    user_id: UUID,
    user_role: str,  # ← 使用字符串类型（来自 JWT role 字段）
    ...
):
```

**权限检查实现**：
```python
# 使用字符串比较
if user_role not in ['ADMIN', 'SUPERADMIN']:
    raise PermissionDeniedException("权限不足，需要管理员权限")
```

---

### 3.6. [关键] 增量测试模式规范 (MANDATORY)

**🚨 重要**：只有在**增量测试生成模式**下才需要遵循本节规范。全新测试生成模式可以忽略本节。

#### 3.6.1. 最小幅度修改原则 (MANDATORY)

- ✅ **只增加**新的测试函数，不修改、不删除、不重命名已存在的测试函数。
- ✅ **保持现有测试的命名约定**：新测试函数必须遵循现有测试的命名风格。
- ✅ **复用现有Fixture**：优先使用 `conftest.py` 中已定义的Fixture。
- ✅ **允许修改 `conftest.py`**（仅限添加新内容）：
  - ✅ **只增加**新的Fixture、函数、变量、导入等。
  - ❌ **严禁修改**已有的Fixture、函数、变量等。
  - ❌ **严禁删除**任何现有内容。
  - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
  - ✅ **保持代码风格一致**：新增的内容必须遵循现有代码的格式、命名约定和代码风格。
- ❌ **禁止修改**已存在的测试函数（即使是修复错误也不行）。
- ❌ **禁止删除**任何现有测试函数或代码。

#### 3.6.1.1. conftest.py 修改的冲突检查规范 (MANDATORY)

**⚠️ 关键**：在增量测试模式下，如果要修改 `conftest.py`，**必须**进行冲突检查。

**✅ 冲突检查步骤**：

1. **读取现有 `conftest.py`**：
   - **必须**完整读取 `conftest.py` 的所有内容。
   - **必须**识别所有已定义的Fixture、函数、变量、导入等。

2. **检查命名冲突**：
   - **检查Fixture名称**：新增的Fixture名称不能与现有的Fixture名称重复。
   - **检查函数名称**：新增的函数名称不能与现有的函数名称重复。
   - **检查变量名称**：新增的变量名称不能与现有的变量名称重复（除非是模块级常量且用途相同）。
   - **检查导入冲突**：新增的导入不能与现有的导入冲突（如导入相同的模块使用不同的别名）。

3. **检查功能重复**：
   - **检查Fixture功能**：如果已有Fixture提供相同功能，优先复用现有Fixture，而不是创建新的。
   - **检查辅助函数功能**：如果已有辅助函数提供相同功能，优先复用现有函数，而不是创建新的。

4. **验证代码风格一致性**：
   - **命名约定**：新增内容的命名必须遵循现有代码的命名风格（如 `snake_case`、`camelCase` 等）。
   - **代码格式**：新增内容的格式（缩进、空行、注释风格等）必须与现有代码一致。
   - **导入顺序**：新增的导入必须按照现有代码的导入顺序添加（标准库、第三方库、本地模块）。

#### 3.6.2. 增量数据库验证规范 (MANDATORY)

**🚨 关键原则**：在增量测试模式下，数据库中可能已经包含数据，因此**必须使用增量判断方式**验证数据库操作，而不是基于数据库总数判断。

**✅ 正确的增量验证模式**：

```python
@pytest.mark.asyncio
async def test_create_expert_success(db_session):
    """测试创建专家（增量验证）"""
    async for db in db_session:
        # ===== Arrange (准备) =====
        # [关键] 记录测试前的记录数量（基线）
        from sqlalchemy import select, func
        from app.models.experts import Expert
        
        # 查询测试前的记录总数
        count_stmt = select(func.count(Expert.id))
        result = await db.execute(count_stmt)
        initial_count = result.scalar()
        
        # 准备测试数据
        expert_data = ExpertCreate(
            user_id=uuid.uuid4(),
            name=f"测试专家_{uuid.uuid4().hex[:6]}",
            title="主任医师"
        )
        
        # ===== Act (执行) =====
        expert = await crud_experts.create_expert(db, expert_create=expert_data)
        await db.commit()
        await db.refresh(expert)
        
        # ===== Assert (断言) =====
        # [关键] 验证增量：记录数应该增加1
        final_count_stmt = select(func.count(Expert.id))
        final_result = await db.execute(final_count_stmt)
        final_count = final_result.scalar()
        
        assert final_count == initial_count + 1, f"记录数应该增加1，但实际增加了{final_count - initial_count}"
        assert expert.name == expert_data.name
        assert expert.id is not None
```

**✅ 删除操作的增量验证模式**：

```python
@pytest.mark.asyncio
async def test_delete_expert_success(db_session, created_expert):
    """测试删除专家（增量验证）"""
    async for db in db_session:
        # ===== Arrange (准备) =====
        # [关键] 记录测试前的记录数量（基线）
        from sqlalchemy import select, func
        from app.models.experts import Expert
        
        count_stmt = select(func.count(Expert.id))
        result = await db.execute(count_stmt)
        initial_count = result.scalar()
        
        expert_id = created_expert.id
        
        # ===== Act (执行) =====
        await crud_experts.delete_expert(db, expert_id=expert_id)
        await db.commit()
        
        # ===== Assert (断言) =====
        # [关键] 验证增量：记录数应该减少1
        final_count_stmt = select(func.count(Expert.id))
        final_result = await db.execute(final_count_stmt)
        final_count = final_result.scalar()
        
        assert final_count == initial_count - 1, f"记录数应该减少1，但实际减少了{initial_count - final_count}"
        
        # 验证对象已被删除
        deleted_expert = await crud_experts.get_expert(db, expert_id=expert_id)
        assert deleted_expert is None
```

#### 3.6.3. Service 层方法签名与 Mock 完整性规范 (MANDATORY - 避免签名/可见性/await 错误)

**🚨 方法签名必须从源码读取**：
- 生成 Service 层测试前，**必须**从 `app/services/expert_service.py` 读取被测方法的**完整签名**（参数名、类型、顺序），**禁止**仅凭方法名或旧用例推断。
- 示例：`_check_user_resource_permission(self, resource_owner_id, current_user_id, role)` 为**三参**（resource_owner_id, current_user_id, role），测试调用须传三参，例如 `service._check_user_resource_permission(user_id, user_id, role)`；测「未登录」时传 `(user_id, user_id, None)`。

**🚨 软删除/可见性字段以设计文档为准**：
- 专家模块软删除为 **is_active=False**；**is_featured 仅表示首页推荐**，不再表示软删除。
- 构造「已下架/软删除」场景须使用 `create_mock_expert(is_active=False)`；断言软删除后须断言 `db_expert.is_active is False`（CRUD 层）；可见性测试中「普通用户不可见」须用 `is_active=False` 的专家。

**🚨 调用链上的 CRUD 须全部 Mock**：
- 若 Service 方法内部会依次调用多个 CRUD（如 `check_is_followed` 先 `get_expert` 再 `get_subscription`），**必须 mock 该调用链上的所有 CRUD**，避免真实 CRUD 对 Mock 的 `db` 执行 `await` 导致 `TypeError: object Mock can't be used in 'await' expression`。
- 示例：`check_is_followed` 测试须同时 mock `app.crud.experts.get_expert`（AsyncMock，返回 `create_mock_expert(expert_id=expert_id, is_active=True)`）与 `app.crud.experts.get_subscription`。

---

## 4. 按层级生成的断言规范 (Assertion Rules by Layer)

你必须根据"核心测试哲学"（第 2 节）生成对应的断言逻辑。

### 4.1. 实用派测试 (Pragmatic: CRUD & API)

**A. API 响应验证 (针对 API 测试)**:

- **必须**同时验证 HTTP 状态码和完整的 JSON 响应结构（`code`, `message`, `data`）。
- **必须**验证业务状态码（`response.json()["code"]`）和 `"data"` 内部的关键字段。
- 对于失败响应，**必须**验证业务错误码（例如 `assert response.json()["code"] == 2001`）。

**B. 数据库状态验证 [关键修改]**：

**(针对 CRUD 层测试)**:
- 在**同一个** `db_session` 中执行的 `create()`, `update()`, `remove()` 操作，**必须**在操作后立即使用 `await db.refresh(obj)` 或**再次 `get(db, obj.id)`**，来验证数据已正确持久化。
- **[增量测试模式]**：对于涉及记录数量或列表查询的测试，**必须使用增量判断方式**（见 `3.6.2` 节）。在测试前记录基线状态（记录数、ID集合等），测试后只验证增量变化。

**(针对 API / Endpoint 层测试)**:
- **[关键禁令]**：在 `await client.patch(...)` 或 `client.post(...)`（它们使用*独立会话*）之后，**严禁**使用*原始的* `db_session`（用于 Arrange 阶段）来 `get()` 或查询对象。这样做**必定**会读取到 SQLAlchemy 身份映射（Identity Map）中的**陈旧缓存**，导致测试失败。

**[API 层正确验证方案]**: 要验证 API 调用后的数据库*真实*状态，**必须**选择以下两种方法之一：
  1.  **(首选 - 验证响应)**: **只断言** API 返回的 `response.json()["data"]` 中的内容是否正确（例如 `assert response.json()["data"]["title"] == "新标题"`）。
  2.  **(次选 - 验证数据库)**: **必须**导入 `async_session_factory`（见 3.1 节），创建一个**全新的、独立的会话**（`async with async_session_factory() as new_db:`），并使用这个 `new_db` 会话执行显式的 `select()` 语句来查询数据库，以获取**最新**数据。
    - **[增量测试模式]**：如果涉及记录数量验证，**必须使用增量判断方式**。在API调用前记录基线状态，调用后验证增量变化。

### 4.2. 学院派测试 (Academic: Service & I/O)

**A. 异常逻辑验证**:

- **必须**使用 `with pytest.raises(PermissionDeniedException):` 来验证在特定条件下是否正确抛出了**自定义业务异常**。

**B. 行为验证 (Mocks)**:

- **必须**使用 `mock_crud.assert_called_once_with(...)` 来验证依赖是否被正确调用。
- **[关键] Mock 路径规范 (MANDATORY)**:
  - 假设 Service 层使用别名导入 (`from app.crud import experts as crud_experts`)。
  - 测试代码在 `mocker.patch()` 中**必须**使用**原始模块路径** (`"app.crud.experts.create_expert"`)。
  - **必须**为异步函数使用 `new_callable=AsyncMock`。

- **[关键] Mock 断言参数格式规范 (MANDATORY)**:
  - **断言参数必须与实际调用方式完全一致**：
    - 如果 Service 层调用时使用**位置参数**（如 `await crud_experts.create_expert(db, expert_create)`），则断言必须使用**位置参数**（`mock_create.assert_called_once_with(db, expert_create)`）。
    - 如果 Service 层调用时使用**关键字参数**（如 `await crud_experts.create_expert(db=db, expert_create=expert_create)`），则断言可以使用**关键字参数**（`mock_create.assert_called_once_with(db=db, expert_create=expert_create)`）。
  - **⚠️ 常见错误**：使用关键字参数断言（`db=db`），但实际调用使用位置参数（`db`），会导致断言失败。
  - **正确做法**：**必须**先检查实际Service层代码的调用方式，然后使用相同格式的断言。

**示例**:
```python
# Service 层 (app/services/expert_service.py)
# from app.crud import experts as crud_experts
# await crud_experts.create_expert(...)

# 测试层 (tests/unit/test_service_expert.py)
@pytest.mark.asyncio
async def test_service_create(mocker):
    # 1. 准备 (Arrange)
    # [关键] Mock 原始路径 "app.crud.experts.create_expert"
    mock_create = mocker.patch(
        "app.crud.experts.create_expert",
        new_callable=AsyncMock,
        return_value=mock_expert_object
    )
    # ...
    # 3. 断言 (Assert)
    mock_create.assert_called_once_with(...)
```

### 4.3. [关键] 异步 SQLAlchemy Session 的测试规范 (V5.2 修复)

**🚨（新增）异步 SQLAlchemy Session 的禁止操作**
在所有测试中：

**严禁使用**：
- `db.expire_all()`
- `db.expire(obj)`

因为这会触发 lazy loading，并在异步环境中引发：

`sqlalchemy.exc.MissingGreenlet`

**🚨（新增）避免 Identity Map 缓存污染**
若测试创建了对象（会话 A），但 API 在另一会话（B）修改了该对象：
- 会话 A 再查询该 ID → 会直接命中缓存，无法看到最新值

**⭐ 正确验证最新数据库值的两种模式**

- **最推荐方式**：完全依赖 API 返回 JSON，测试不访问数据库（见 `4.1.A`）。
- **若必须访问数据库**：
  - 必须使用新的独立会话（见 `4.1.B`）。
  - `async with async_session_factory() as new_db:`
  - `stmt = select(Model).where(Model.id == id)`
  - `result = await new_db.execute(stmt)`
  - `obj = result.scalar_one_or_none()`
- **⚠️ 警告**：不能在**同一个**测试会话中二次查询同一主键，否则会命中身份映射缓存。

---

## 5. 工程与风格规范 (Engineering & Style Norms)

**RESTful 路径规范 (MANDATORY)**:
- 在 `APIRouter` 中定义 `prefix` 时，**严禁**在末尾添加斜杠 (例如：`prefix="/experts"`)。
- 对于集合端点（`GET /experts`, `POST /experts`），**必须**在装饰器中使用 `path="/"`。

---

## 6. [动态生成] 核心上下文与被测试代码 (Context & Code to Test)

### 6.1 已存在的测试文件

**⚠️ 增量测试模式说明**：
- **已存在的测试文件列表**：
  - `tests/conftest.py`（Fixture定义）
  - `tests/unit/test_crud_content_management.py`（CRUD测试参考）
  - `tests/unit/test_service_content_management.py`（Service测试参考）
  - `tests/integration/test_api_content_management.py`（API测试参考）
  - 其他现有测试文件...

- **需要新增的测试文件列表**：
  - `tests/unit/test_crud_experts.py`（CRUD层测试 - 新增）
  - `tests/unit/test_service_experts.py`（Service层测试 - 新增）
  - `tests/integration/test_api_experts.py`（API层测试 - 新增）

**⚠️ 严禁修改已存在的测试文件！** 即使发现错误也不修改，只增加新的测试文件。

### 6.2 核心依赖文件

```python
# 【文件路径 1: app/core/deps.py】
from app.core.deps import get_current_user, get_current_user_optional
```

```python
# 【文件路径 2: app/models/experts.py】
from app.models.experts import Expert, UserExpertSubscription, LiveSessionExpert, SessionExpertRole
```

```python
# 【文件路径 3: app/models/live_core.py】
from app.models.live_core import LiveSession, LiveRoom
```

```python
# 【文件路径 4: app/schemas/experts.py】
from app.schemas.experts import (
    ExpertCreate, ExpertUpdate, ExpertItem, FeaturedExpertItem,
    ExpertFollowRequest, ExpertFollowResponse,
    FollowedExpertItem, FollowedExpertsResponse, SessionExpertItem
)
```

```python
# 【文件路径 5: app/crud/experts.py】
# （内容已读取，包含所有CRUD函数定义）
```

```python
# 【文件路径 6: app/services/expert_service.py】
# （内容已读取，包含所有Service方法定义）
# ⚠️ 生成权限/可见性/关注相关测试前，必须读取以下方法的完整定义与调用关系：
#    _check_user_resource_permission(resource_owner_id, current_user_id, role)、
#    _check_expert_visibility(expert, current_user_id, role)、
#    check_is_followed(expert_id, current_user_id, role) 的调用顺序（get_expert → _check_expert_visibility → get_subscription）
```

```python
# 【文件路径 7: app/api/v1/endpoints/experts.py】
# （内容已读取，包含所有API端点定义）
```

```python
# 【文件路径 8: tests/conftest.py】（现有Fixture定义）
# （内容已读取，包含所有现有Fixture）
```

---

## 7. [动态生成] 具体测试任务列表 (Dynamic-Generated Specific Test Tasks)

你必须为 `Section 6` 中的代码严格生成以下所有测试用例：

### ⚠️ 增量测试模式说明

**已存在的测试文件**：
- `tests/conftest.py`
- `tests/unit/test_crud_content_management.py`
- `tests/integration/test_api_content_management.py`
- 其他现有测试...

**需要新增的测试文件**：
- `tests/unit/test_crud_experts.py`（CRUD层测试 - 新增）
- `tests/unit/test_service_experts.py`（Service层测试 - 新增）
- `tests/integration/test_api_experts.py`（API层测试 - 新增）

**⚠️ 重要**：
- **严禁修改**已存在的测试文件（即使发现错误也不行）
- **允许修改 `tests/conftest.py`**（仅限添加新内容，必须检查冲突）
- **必须使用增量判断方式**验证数据库操作（见 `3.6.2` 节）
- 新测试函数必须遵循现有测试的命名约定和代码风格
- **复用现有Fixture**：优先使用 `conftest.py` 中已定义的Fixture（如 `db_session`, `async_client`, `regular_user_id`, `admin_user_id`, `regular_user_token`, `admin_user_token` 等）

---

### A. 针对 `app/crud/experts.py` (实用派 CRUD 测试)

**`async def create_expert(db, expert_create)`**:
- (成功) `test_create_expert_success`: 调用 `create_expert()`，然后**必须**使用 `db.refresh()` 或再次 `get_expert()`，断言数据库中对象的字段与 `expert_create` 匹配。
- **[增量测试模式]**：**必须**在测试前记录记录总数（基线），测试后验证记录数增加了1。

**`async def get_expert(db, expert_id)`**:
- (成功) `test_get_expert_success`: 创建一个 `Expert` 对象，调用 `get_expert()`，断言返回的对象字段正确。
- (失败) `test_get_expert_not_found`: 调用 `get_expert()` 传入一个不存在的 `id`，断言结果为 `None`。

**`async def get_experts_multi(db, skip, limit, filters)`**:
- (成功) `test_get_experts_multi_success`: 创建多个 `Expert` 对象，调用 `get_experts_multi()`，断言返回的列表包含正确的专家。
- (成功 - 推荐) `test_get_experts_multi_featured`: 创建多个 `Expert` 对象，调用 `get_experts_multi(filters={"is_featured": True})`，断言返回的列表包含推荐专家。

**`async def get_experts_multi_and_total(db, skip, limit, filters)`**:
- (成功) `test_get_experts_multi_and_total_success`: 创建多个 `Expert` 对象，调用 `get_experts_multi_and_total()`，断言返回的分页数据正确。

**`async def get_featured_experts(db)`**:
- (成功) `test_get_featured_experts_success`: 创建多个推荐专家，调用 `get_featured_experts()`，断言返回的列表包含推荐专家。

**`async def update_expert(db, expert_id, expert_update)`**:
- (成功) `test_update_expert_success`: 创建一个 `Expert` 对象，调用 `update_expert()`，然后**必须**再次 `get_expert()`，断言数据库中对象的字段已更新。

**`async def delete_expert(db, expert_id)`**（软删除：设置 is_active=False）:
- (成功) `test_delete_expert_success`: 创建一个 `Expert` 对象，调用 `delete_expert()`，然后**必须**再次 `get_expert()`，断言 `db_expert is not None` 且 **`db_expert.is_active is False`**（软删除不断开记录，仅下架）。
- **[增量测试模式]**：**必须**在测试前记录记录总数（基线），测试后验证记录数**不减少**（软删除不删行）；或仅验证 is_active 变更。

**`async def create_expert_follow(db, user_id, expert_id)`**:
- (成功) `test_create_expert_follow_success`: 调用 `create_expert_follow()`，然后**必须**再次查询，断言关联已创建。
- **[增量测试模式]**：**必须**在测试前记录记录总数（基线），测试后验证记录数增加了1。

**`async def delete_expert_follow(db, user_id, expert_id)`**:
- (成功) `test_delete_expert_follow_success`: 创建一个关注关联，调用 `delete_expert_follow()`，然后**必须**再次查询，断言关联已删除。

**`async def is_expert_followed(db, user_id, expert_id)`**:
- (成功) `test_is_expert_followed_true`: 创建一个关注关联，调用 `is_expert_followed()`，断言返回 `True`。
- (失败) `test_is_expert_followed_false`: 调用 `is_expert_followed()` 传入不存在的关联，断言返回 `False`。

**`async def get_followed_experts_multi(db, user_id, skip, limit)`**:
- (成功) `test_get_followed_experts_multi_success`: 创建多个关注关联，调用 `get_followed_experts_multi()`，断言返回的列表包含正确的专家。

**`async def get_followed_experts_multi_and_total(db, user_id, skip, limit)`**:
- (成功) `test_get_followed_experts_multi_and_total_success`: 创建多个关注关联，调用 `get_followed_experts_multi_and_total()`，断言返回的分页数据正确。

**`async def set_session_experts(db, session_id, expert_items)`**:
- (成功) `test_set_session_experts_success`: 调用 `set_session_experts()`，断言批量创建成功。

**`async def get_session_experts(db, session_id)`**:
- (成功) `test_get_session_experts_success`: 创建多个 `SessionExpert` 关联，调用 `get_session_experts()`，断言返回的列表包含正确的关联。

---

### B. 针对 `app/services/expert_service.py` (学院派 Service 测试) [V6.0 修复]

**⚠️ 签名与 Mock 约定（必须遵守）**：
- **权限守卫**：`_check_user_resource_permission(self, resource_owner_id, current_user_id, role)` 为**三参**，测试调用须传三参（如 `service._check_user_resource_permission(user_id, user_id, role)`）；测未登录传 `(user_id, user_id, None)`。
- **可见性**：`_check_expert_visibility` 以 **is_active** 判断下架；构造「软删除/不可见」须用 `create_mock_expert(is_active=False)`，禁止用 is_featured。
- **check_is_followed**：实现先 `get_expert` 再 `get_subscription`，须**同时 mock** `get_expert` 与 `get_subscription`，避免真实 CRUD 对 Mock 的 db 做 await 导致 TypeError。

**(示例: 检查签名后发现是 `async def create_expert(self, expert_create: ExpertCreate, user_id: UUID, user_role: str)`)`**

**Expert Info 相关测试**：

- (成功) `test_service_create_expert_success`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `test_user_role` (字符串，如 "ADMIN")。Mock `crud_experts.create_expert` (使用 `new_callable=AsyncMock`) 并使其返回一个模拟的 `Expert` 对象。
  - 执行 (Act): 调用 `service.create_expert(..., user_id=test_user_id, user_role=test_user_role, ...)`。
  - 断言 (Assert): **必须**断言 `crud_experts.create_expert.assert_called_once_with(db=db, expert_create=...)`，确保 `expert_create` 中已正确包含数据。

- (失败 - 权限) `test_service_create_expert_permission_denied`:
  - 准备 (Arrange): 定义 `regular_user_id` 和 `regular_role` (字符串，如 "REGULAR")。Mock `crud_experts.create_expert`。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(PermissionDeniedException):` 来包裹 `service.create_expert(..., user_id=regular_user_id, user_role=regular_role, ...)` 的调用。

- (成功) `test_service_update_expert_success`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `test_user_role`。Mock `crud_experts.get_expert` (返回一个模拟的 `Expert`) 和 `crud_experts.update_expert`。
  - 执行 (Act): 调用 `service.update_expert(..., user_id=test_user_id, user_role=test_user_role, ...)`。
  - 断言 (Assert): **必须**断言 `crud_experts.update_expert.assert_called_once_with(...)`。

- (失败 - 权限) `test_service_update_expert_permission_denied`:
  - 准备 (Arrange): 定义 `regular_user_id` 和 `regular_role`。Mock `crud_experts.get_expert`。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(PermissionDeniedException):` 来包裹 `service.update_expert(..., user_id=regular_user_id, user_role=regular_role, ...)` 的调用。

- (失败 - 未找到) `test_service_update_expert_not_found`:
  - 准备 (Arrange): Mock `crud_experts.get_expert` (返回 `None`)。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(NotFoundException):` 来包裹调用。

- (成功) `test_service_delete_expert_success`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `test_user_role`。Mock `crud_experts.get_expert` (返回一个模拟的 `Expert`)、`crud_experts.delete_expert`。
  - 执行 (Act): 调用 `service.delete_expert(..., user_id=test_user_id, user_role=test_user_role, ...)`。
  - 断言 (Assert): **必须**断言 `crud_experts.delete_expert.assert_called_once_with(...)`。

- (失败 - 权限) `test_service_delete_expert_permission_denied`:
  - 准备 (Arrange): 定义 `regular_user_id` 和 `regular_role`。Mock `crud_experts.get_expert`。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(PermissionDeniedException):` 来包裹调用。

- (成功) `test_service_get_expert_detail_success`:
  - 准备 (Arrange): 定义 `test_user_id`。Mock `crud_experts.get_expert` (返回一个模拟的 `Expert`)。
  - 执行 (Act): 调用 `service.get_expert_detail(..., user_id=test_user_id, ...)`。
  - 断言 (Assert): 验证返回的专家数据正确。

- (失败 - 未找到) `test_service_get_expert_detail_not_found`:
  - 准备 (Arrange): Mock `crud_experts.get_expert` (返回 `None`)。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(NotFoundException):` 来包裹调用。

- (成功) `test_service_get_experts_multi_success`:
  - 准备 (Arrange): Mock `crud_experts.get_experts_multi_and_total` (返回专家列表和总数)。
  - 执行 (Act): 调用 `service.get_experts_multi(...)`。
  - 断言 (Assert): 验证返回的分页数据正确。

- (成功) `test_service_get_featured_experts_success`:
  - 准备 (Arrange): Mock `crud_experts.get_featured_experts` (返回专家列表)。
  - 执行 (Act): 调用 `service.get_featured_experts(...)`。
  - 断言 (Assert): 验证返回的专家列表正确。

**Expert Follow 相关测试**：

- (成功) `test_service_follow_expert_success`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `expert_id`。Mock `crud_experts.create_expert_follow` (使用 `new_callable=AsyncMock`) 并使其返回一个模拟的 `UserExpertSubscription` 对象。
  - 执行 (Act): 调用 `service.follow_expert(..., user_id=test_user_id, expert_id=expert_id)`。
  - 断言 (Assert): **必须**断言 `crud_experts.create_expert_follow.assert_called_once_with(db=db, user_id=test_user_id, expert_id=expert_id)`。

- (成功) `test_service_unfollow_expert_success`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `expert_id`。Mock `crud_experts.delete_expert_follow` (使用 `new_callable=AsyncMock`) 并使其返回 `True`。
  - 执行 (Act): 调用 `service.unfollow_expert(..., user_id=test_user_id, expert_id=expert_id)`。
  - 断言 (Assert): **必须**断言 `crud_experts.delete_expert_follow.assert_called_once_with(db=db, user_id=test_user_id, expert_id=expert_id)`。

- (成功) `test_service_is_expert_followed_true`:
  - 准备 (Arrange): 定义 `test_user_id` 和 `expert_id`。Mock `crud_experts.is_expert_followed` (使用 `new_callable=AsyncMock`) 并使其返回 `True`。
  - 执行 (Act): 调用 `service.is_expert_followed(..., user_id=test_user_id, expert_id=expert_id)`。
  - 断言 (Assert): 验证返回 `True`。

- (成功) `test_service_get_followed_experts_multi_success`:
  - 准备 (Arrange): 定义 `test_user_id`。Mock `crud_experts.get_followed_experts_multi_and_total` (返回关注列表和总数)。
  - 执行 (Act): 调用 `service.get_followed_experts_multi(..., user_id=test_user_id)`。
  - 断言 (Assert): 验证返回的分页数据正确。

**Session Expert 相关测试**：

- (成功) `test_service_set_session_experts_success`:
  - 准备 (Arrange): 定义 `test_user_id`、`test_user_role` 和 `session_id`。Mock `crud_experts.get_expert` (返回一个模拟的 `LiveSession`)、`crud_experts.set_session_experts`。
  - 执行 (Act): 调用 `service.set_session_experts(..., user_id=test_user_id, user_role=test_user_role, ...)`。
  - 断言 (Assert): **必须**断言 `crud_experts.set_session_experts.assert_called_once_with(...)`。

- (失败 - 权限) `test_service_set_session_experts_permission_denied`:
  - 准备 (Arrange): 定义 `regular_user_id` 和 `regular_role`。
  - 执行 & 断言 (Act & Assert): **必须**使用 `with pytest.raises(PermissionDeniedException):` 来包裹调用。

- (成功) `test_service_get_session_experts_success`:
  - 准备 (Arrange): 定义 `session_id`。Mock `crud_experts.get_session_experts` (返回关联列表)。
  - 执行 (Act): 调用 `service.get_session_experts(..., session_id=session_id)`。
  - 断言 (Assert): 验证返回的关联列表正确。

---

### C. 针对 `app/api/v1/endpoints/experts.py` (实用派 API 测试) [V6.0 修复]

**Expert Info API 测试**：

**`@router.get("/experts")` (get_experts)**:

- (成功) `test_api_get_experts_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。准备测试数据（可选）。**必须**使用 `async for db in db_session:` 解包 `db_session` fixture。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言返回的专家列表不为空（如果有数据）。

- (成功 - 推荐) `test_api_get_experts_featured`: 带推荐参数的成功测试。
- (成功 - 管理员查看禁用) `test_api_get_experts_admin`: 管理员查看专家的成功测试（如果有相关参数）。

**`@router.get("/experts/featured")` (get_featured_experts)**:

- (成功) `test_api_get_featured_experts_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。准备测试数据（推荐专家）。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/featured"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言返回的推荐专家列表不为空。

**`@router.get("/experts/{expert_id}")` (get_expert_detail)**:

- (成功) `test_api_get_expert_detail_success`:
  - 准备 (Arrange): 在 `db_session` 中创建一个 `Expert`。**必须**使用 `async for db in db_session:` 解包 `db_session` fixture。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/{expert_id}"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言返回的专家名称正确。

- (失败 - 未找到) `test_api_get_expert_detail_not_found`:
  - 准备 (Arrange): 准备一个不存在的 `expert_id`。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/{expert_id}"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 404` 且 `response.json()["code"] == 4004`。

**`@router.get("/experts/{expert_id}/sessions")` (get_expert_with_sessions)**:

- (成功) `test_api_get_expert_with_sessions_success`:
  - 准备 (Arrange): 在 `db_session` 中创建一个 `Expert` 和相关的 `SessionExpert` 关联。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/{expert_id}/sessions"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言返回的数据包含专家信息和场次列表。

- (失败 - 未找到) `test_api_get_expert_with_sessions_not_found`:
  - 准备 (Arrange): 准备一个不存在的 `expert_id`。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/{expert_id}/sessions"`。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 404` 且 `response.json()["code"] == 4004`。

**`@admin_router.post("/admin/experts")` (create_expert)**:

- (成功) `test_api_create_expert_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。准备一个有效的 `json` payload（唯一字段使用 Faker）。**必须**使用 `async for db in db_session:` 解包 `db_session` fixture。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `POST "/admin/experts"`（如 `response = await client.post("/api/v1/admin/experts", json=payload, headers=headers)`）。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` (或 201) 且 `response.json()["code"] == 0`。
    - 断言返回的专家名称与输入一致。

- (失败 - 权限) `test_api_create_expert_permission_denied`: 使用 `regular_user_token`，断言 `response.status_code == 403` 且 `response.json()["code"] == 3003`。

**`@admin_router.patch("/admin/experts/{expert_id}")` (update_expert)**:

- (成功) `test_api_update_expert_success`: 更新专家的成功测试。
- (失败 - 权限) `test_api_update_expert_permission_denied`: 权限测试。
- (失败 - 未找到) `test_api_update_expert_not_found`: 专家不存在测试，断言 `response.status_code == 404`。

**`@admin_router.delete("/admin/experts/{expert_id}")` (delete_expert)**:

- (成功) `test_api_delete_expert_success`: 删除专家的成功测试。
- (失败 - 权限) `test_api_delete_expert_permission_denied`: 权限测试。
- (失败 - 未找到) `test_api_delete_expert_not_found`: 专家不存在测试，断言 `response.status_code == 404`。

**Expert Follow API 测试**：

**`@router.post("/experts/{expert_id}/follow")` (follow_expert)**:

- (成功) `test_api_follow_expert_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。在 `db_session` 中创建一个 `Expert`。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `POST "/experts/{expert_id}/follow"`（使用认证头）。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言 `response.json()["data"]["followed"] == True`。

- (失败 - 未认证) `test_api_follow_expert_unauthorized`: 不提供认证头的测试，断言 `response.status_code == 401`。

**`@router.delete("/experts/{expert_id}/follow")` (unfollow_expert)**:

- (成功) `test_api_unfollow_expert_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。在 `db_session` 中创建一个 `Expert` 和关注关联。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `DELETE "/experts/{expert_id}/follow"`（使用认证头）。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言 `response.json()["data"]["unfollowed"] == True`。

- (失败 - 未认证) `test_api_unfollow_expert_unauthorized`: 不提供认证头的测试，断言 `response.status_code == 401`。

**`@router.get("/experts/followed")` (get_followed_experts)**:

- (成功) `test_api_get_followed_experts_success`:
  - 准备 (Arrange): 使用 `async_client` 和 `db_session`。在 `db_session` 中创建多个关注关联。
  - 执行 (Act): **必须**使用 `async for client in async_client:` 解包 `async_client` fixture，然后在 `async for` 块内执行 `GET "/experts/followed"`（使用认证头）。
  - 断言 (Assert): **所有断言必须在 `async for` 块内**：
    - 断言 `response.status_code == 200` 且 `response.json()["code"] == 0`。
    - 断言返回的关注专家列表不为空。

- (失败 - 未认证) `test_api_get_followed_experts_unauthorized`: 不提供认证头的测试，断言 `response.status_code == 401`。

---

## 8. 交付物 (Deliverable - 执行者)

请根据**以上所有规范 (1-7)**，为 `Section 6` 中提供的代码生成**完整的 `pytest` 测试文件**。

**交付物**:
- `tests/unit/test_crud_experts.py` (CRUD层测试)
- `tests/unit/test_service_experts.py` (Service层测试)
- `tests/integration/test_api_experts.py` (API层测试)

**生成后自检（避免签名/可见性/Mock 错误）**：
- [ ] Service 层被测方法调用与源码签名一致（如 `_check_user_resource_permission(user_id, user_id, role)` 三参）。
- [ ] 软删除/可见性使用 **is_active**（构造 `is_active=False`、断言 `is_active is False`），未沿用 is_featured。
- [ ] 多步调用的 Service 方法（如 check_is_followed）已 mock 调用链上的所有 CRUD（get_expert + get_subscription），未出现对 Mock db 的 await。

**指令**:
1. **遵循任务列表**: 严格按照 `Section 7` 中的列表生成所有指定的测试用例。
2. **遵循所有规范**: 确保生成的代码 100% 遵循 `Section 2-5` (哲学, 语法, 断言, 风格)。
3. **[增量测试模式]**：
   - **严禁修改**已存在的测试文件（即使发现错误也行）。
   - **允许修改 `conftest.py`**（仅限添加新内容）：
     - ✅ **只增加**新的Fixture、函数、变量、导入等。
     - ❌ **严禁修改**已有的Fixture、函数、变量等。
     - ❌ **严禁删除**任何现有内容。
     - ✅ **必须检查冲突**：新增的内容必须与已有的内容没有冲突（函数名、变量名、Fixture名等不重复）。
   - **必须使用增量判断方式**验证数据库操作（见 `3.6.2` 节）。
   - 新测试函数必须遵循现有测试的命名约定和代码风格。

**重要说明**：
- **用户角色参数**: 所有Service层方法签名使用 `user_role: str`（字符串），**不使用枚举**。
- **JWT Payload**: API层从JWT中提取的 `role` 字段是字符串，无需转换。
- **权限检查**: 使用字符串比较：`if user_role not in ['ADMIN', 'SUPERADMIN']:`
- **API层提取**: `user_role = current_user.get("role", "REGULAR").upper()`

**开始生成测试代码！**

