# 用户偏好与通知模块 - SQLAlchemy数据库模型代码生成提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**模块名**: user_preference_notification  
**功能模块**: 用户偏好与通知  
**开发模式**: 增量开发（新增模型文件）

---

## 1. 角色定义

你是一名精通SQLAlchemy 2.0和PostgreSQL数据库设计的资深Python后端工程师。你的任务是根据本文档的要求，生成符合项目规范的SQLAlchemy ORM模型代码。

**核心要求**:
- 严格遵循SQLAlchemy 2.0最佳实践
- 遵循Clean Architecture（学院派）架构风格
- 确保代码与设计文档完全一致
- 所有UUID主键在应用层生成（使用`uuid.uuid4()`）
- 使用`TIMESTAMPTZ`类型并设置`server_default=func.now()`
- 所有外键必须命名（格式：`fk_{table}_{column}`）
- 使用`__table_args__`定义索引和约束
- 实现`__repr__`方法（不暴露敏感信息）

---

## 2. 项目结构信息

### 2.1 目标文件路径

**新增文件**: `backend/live_core_service/app/models/user_preference_notification.py`

**更新文件**: `backend/live_core_service/app/models/__init__.py`（增量模式：添加新模型的导入）

### 2.2 项目技术栈

- **Python版本**: 3.9+
- **SQLAlchemy版本**: 2.0+
- **数据库**: PostgreSQL 14+
- **异步支持**: `asyncpg` + `AsyncSession`

---

## 3. 数据库模型定义

### 3.1 UserPreferences（用户偏好设置表）

**表名**: `user_preferences`

**字段定义**:

```python
from sqlalchemy import Column, String, Boolean, Time, TIMESTAMP, Index, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID, JSONB
from sqlalchemy.sql import func
from app.models.base import Base
import uuid

class UserPreferences(Base):
    """用户个性化偏好设置模型"""
    __tablename__ = "user_preferences"
    
    # 核心标识（在应用层生成UUID）
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="主键ID")
    
    # 用户关联（一对一关系）
    user_id = Column(
        UUID(as_uuid=True), 
        nullable=False, 
        unique=True,
        comment="用户公开ID（users.public_id），应用层验证，JWT Token的user_id字段值"
    )
    
    # 昼夜模式设置
    theme_mode = Column(
        String(20), 
        nullable=False, 
        default="auto",
        comment="昼夜模式：auto=跟随系统, light=浅色, dark=深色, scheduled=定时切换"
    )
    theme_scheduled_dark_time = Column(
        Time, 
        nullable=True,
        comment="定时深色模式开始时间（定时切换时使用）"
    )
    theme_scheduled_light_time = Column(
        Time, 
        nullable=True,
        comment="定时浅色模式开始时间（定时切换时使用）"
    )
    
    # 科室星标固定
    pinned_categories = Column(
        JSONB, 
        nullable=True,
        comment="JSONB数组，存储用户固定的科室ID（最多5个），格式：[\"uuid1\", \"uuid2\", ...]"
    )
    
    # 视图模式
    homepage_view_mode = Column(
        String(20), 
        nullable=False, 
        default="double",
        comment="首页视图模式：double=双列瀑布流, single=单列列表"
    )
    
    # 网络与流量设置
    cellular_warning_enabled = Column(
        Boolean, 
        nullable=False, 
        default=True,
        comment="是否启用流量提醒"
    )
    auto_reduce_quality = Column(
        Boolean, 
        nullable=False, 
        default=True,
        comment="流量下自动降画质"
    )
    auto_play_on_wifi = Column(
        Boolean, 
        nullable=False, 
        default=False,
        comment="WiFi下自动播放"
    )
    
    # 其他偏好设置（可扩展）
    extra = Column(
        JSONB, 
        nullable=True,
        comment="JSONB格式存储其他扩展偏好设置"
    )
    
    # 时间戳（使用TIMESTAMPTZ + server_default）
    created_at = Column(
        TIMESTAMP(timezone=True), 
        nullable=False, 
        server_default=func.now(),
        comment="创建时间"
    )
    updated_at = Column(
        TIMESTAMP(timezone=True), 
        nullable=False, 
        server_default=func.now(),
        onupdate=func.now(),
        comment="更新时间"
    )
    
    # 索引和约束
    __table_args__ = (
        Index("idx_user_preferences_user_id", "user_id"),
        {"comment": "用户个性化偏好设置表，存储用户的前端偏好配置"}
    )
    
    def __repr__(self) -> str:
        """字符串表示（不暴露敏感信息）"""
        return f"<UserPreferences(id={str(self.id)[:8]}, user_id={str(self.user_id)[:8]}, theme_mode={self.theme_mode})>"
```

**关键说明**:
1. **UUID主键**: 使用`default=uuid.uuid4`在应用层生成
2. **user_id字段**: 设置`unique=True`确保一对一关系
3. **JSONB字段**: `pinned_categories`和`extra`使用JSONB类型存储复杂数据
4. **时间戳**: 使用`TIMESTAMP(timezone=True)`和`server_default=func.now()`
5. **索引**: 在`user_id`上创建索引以优化查询性能
6. **__repr__方法**: 只显示非敏感信息，使用`[:8]`截断UUID

---

### 3.2 Notification（用户通知表）

**表名**: `notifications`

**字段定义**:

```python
from sqlalchemy import Column, String, Boolean, Text, TIMESTAMP, Index
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.sql import func
from app.models.base import Base
import uuid

class Notification(Base):
    """用户通知模型"""
    __tablename__ = "notifications"
    
    # 核心标识（在应用层生成UUID）
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="主键ID")
    
    # 用户关联
    user_id = Column(
        UUID(as_uuid=True), 
        nullable=False,
        comment="接收通知的用户ID（users.public_id），应用层验证"
    )
    
    # 通知内容
    title = Column(
        String(255), 
        nullable=False,
        comment="通知标题"
    )
    content = Column(
        Text, 
        nullable=True,
        comment="通知内容"
    )
    notification_type = Column(
        String(50), 
        nullable=False, 
        default="system",
        comment="通知类型：system=系统通知, subscription=订阅通知, interaction=互动通知"
    )
    
    # 关联资源（可选）
    related_id = Column(
        UUID(as_uuid=True), 
        nullable=True,
        comment="关联资源ID（如：room_id, session_id, expert_id等）"
    )
    related_type = Column(
        String(50), 
        nullable=True,
        comment="关联资源类型（如：room, session, expert, brand等）"
    )
    
    # 已读状态
    is_read = Column(
        Boolean, 
        nullable=False, 
        default=False,
        comment="是否已读：false=未读, true=已读"
    )
    
    # 时间戳（仅创建时间，通知不可修改）
    created_at = Column(
        TIMESTAMP(timezone=True), 
        nullable=False, 
        server_default=func.now(),
        comment="创建时间"
    )
    
    # 索引和约束
    __table_args__ = (
        Index("idx_notifications_user_id", "user_id"),
        Index("idx_notifications_user_is_read", "user_id", "is_read"),
        Index("idx_notifications_created_at", "created_at"),
        {"comment": "用户通知表，存储系统通知、订阅提醒、互动通知等"}
    )
    
    def __repr__(self) -> str:
        """字符串表示（不暴露敏感信息）"""
        return f"<Notification(id={str(self.id)[:8]}, user_id={str(self.user_id)[:8]}, type={self.notification_type}, is_read={self.is_read})>"
```

**关键说明**:
1. **UUID主键**: 使用`default=uuid.uuid4`在应用层生成
2. **user_id字段**: 不设置`unique=True`，一个用户可以有多条通知
3. **复合索引**: `(user_id, is_read)`用于快速查询未读通知
4. **时间戳索引**: `created_at`用于按时间排序和筛选
5. **关联资源**: `related_id`和`related_type`支持关联不同类型的资源
6. **__repr__方法**: 显示关键信息但不暴露敏感内容

---

## 4. 导入和依赖

### 4.1 必需导入

```python
import uuid
from sqlalchemy import Column, String, Boolean, Text, Time, TIMESTAMP, Index, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID, JSONB
from sqlalchemy.sql import func
from app.models.base import Base
```

### 4.2 Base类说明

所有模型必须继承自`app.models.base.Base`，这是项目的统一基类。

---

## 5. 代码生成要求

### 5.1 文件结构

```python
"""
用户偏好与通知模块 - SQLAlchemy ORM模型

本模块包含用户偏好设置和通知系统的数据库模型定义。
"""

import uuid
from sqlalchemy import Column, String, Boolean, Text, Time, TIMESTAMP, Index, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID, JSONB
from sqlalchemy.sql import func
from app.models.base import Base


class UserPreferences(Base):
    """用户个性化偏好设置模型"""
    # ... (完整定义见3.1节)


class Notification(Base):
    """用户通知模型"""
    # ... (完整定义见3.2节)
```

### 5.2 代码规范

1. **导入顺序**: 标准库 → 第三方库 → 项目内部模块
2. **类文档字符串**: 每个模型类必须有docstring
3. **字段注释**: 使用`comment`参数为每个字段添加注释
4. **表注释**: 在`__table_args__`中添加表注释
5. **__repr__方法**: 必须实现，且不暴露敏感信息

### 5.3 命名规范

- **表名**: 小写+下划线（如`user_preferences`）
- **字段名**: 小写+下划线（如`theme_mode`）
- **类名**: 大驼峰（如`UserPreferences`）
- **索引名**: `idx_{table}_{column}` 或 `idx_{table}_{col1}_{col2}`

---

## 6. 更新__init__.py文件

在生成模型代码后，必须更新`backend/live_core_service/app/models/__init__.py`文件：

```python
# 在文件末尾添加以下导入
from app.models.user_preference_notification import UserPreferences, Notification

# 更新__all__列表
__all__ = [
    # ... 现有模型 ...
    "UserPreferences",
    "Notification",
]
```

---

## 7. 验证清单

生成代码后，请验证以下内容：

- [ ] 所有UUID字段使用`UUID(as_uuid=True)`和`default=uuid.uuid4`
- [ ] 所有时间戳字段使用`TIMESTAMP(timezone=True)`和`server_default=func.now()`
- [ ] 所有索引在`__table_args__`中定义
- [ ] `user_id`字段在`UserPreferences`表中设置了`unique=True`
- [ ] `user_id`字段在`Notification`表中未设置`unique=True`
- [ ] 所有字段都有`comment`参数
- [ ] 表都有注释（在`__table_args__`中）
- [ ] `__repr__`方法不暴露敏感信息
- [ ] JSONB字段使用`JSONB`类型
- [ ] 代码通过linter检查（无语法错误）

---

## 8. 注意事项

1. **不使用外键约束**: 本模块的`user_id`字段引用`users.public_id`，但不在数据库层面创建外键约束，仅在应用层验证
2. **JSONB字段验证**: `pinned_categories`和`extra`字段的数据验证在Pydantic Schema层完成
3. **时间戳更新**: `UserPreferences`表的`updated_at`使用`onupdate=func.now()`自动更新
4. **通知不可修改**: `Notification`表没有`updated_at`字段，通知创建后不可修改
5. **索引优化**: 复合索引`(user_id, is_read)`用于快速查询未读通知

---

**生成指令**: 请严格按照本文档的要求生成SQLAlchemy模型代码，保存到`backend/live_core_service/app/models/user_preference_notification.py`，并更新`__init__.py`文件。
