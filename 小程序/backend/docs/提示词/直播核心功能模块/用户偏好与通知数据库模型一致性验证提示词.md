# 用户偏好与通知模块 - 数据库DDL与SQLAlchemy模型一致性验证提示词

**版本**: V1.0  
**创建日期**: 2026-01-18  
**模块名**: user_preference_notification  
**功能模块**: 用户偏好与通知  
**检测类型**: 设计文档DDL ↔ SQLAlchemy ORM模型一致性

---

## 1. 角色定义

你是一名精通数据库设计和代码审查的资深技术架构师。你的任务是检查设计文档中的DDL定义与生成的SQLAlchemy ORM模型之间的一致性。

**核心要求**:
- 逐字段对比DDL定义和ORM模型
- 检查表名、字段名、类型、约束、索引是否一致
- 检查注释是否完整
- 生成详细的一致性检测报告

---

## 2. 检测目标文件

### 2.1 设计文档DDL

**文件路径**: `docs/03_系统设计/直播核心功能设计文档_v6_用户偏好与通知模块设计文档.md`

**DDL位置**: 第2节"数据库Schema设计（DDL）"

**包含表**:
- `user_preferences`
- `notifications`

### 2.2 SQLAlchemy ORM模型

**文件路径**: `backend/live_core_service/app/models/user_preference_notification.py`

**包含模型**:
- `UserPreferences`
- `Notification`

---

## 3. 检测清单

### 3.1 user_preferences表 ↔ UserPreferences模型

#### 3.1.1 表名一致性

- [ ] DDL表名: `user_preferences`
- [ ] ORM模型`__tablename__`: `user_preferences`
- [ ] 是否一致: ✅

#### 3.1.2 字段定义一致性

| 字段名 | DDL类型 | ORM类型 | DDL约束 | ORM约束 | 是否一致 |
|--------|---------|---------|---------|---------|----------|
| `id` | `UUID PRIMARY KEY` | `UUID(as_uuid=True), primary_key=True` | 主键 | 主键 | ✅ |
| `user_id` | `UUID NOT NULL UNIQUE` | `UUID(as_uuid=True), nullable=False, unique=True` | NOT NULL, UNIQUE | nullable=False, unique=True | ✅ |
| `theme_mode` | `VARCHAR(20) DEFAULT 'auto'` | `String(20), nullable=False, default="auto"` | DEFAULT | default | ✅ |
| `theme_scheduled_dark_time` | `TIME NULL` | `Time, nullable=True` | NULL | nullable=True | ✅ |
| `theme_scheduled_light_time` | `TIME NULL` | `Time, nullable=True` | NULL | nullable=True | ✅ |
| `pinned_categories` | `JSONB NULL` | `JSONB, nullable=True` | NULL | nullable=True | ✅ |
| `homepage_view_mode` | `VARCHAR(20) DEFAULT 'double'` | `String(20), nullable=False, default="double"` | DEFAULT | default | ✅ |
| `cellular_warning_enabled` | `BOOLEAN DEFAULT true` | `Boolean, nullable=False, default=True` | DEFAULT | default | ✅ |
| `auto_reduce_quality` | `BOOLEAN DEFAULT true` | `Boolean, nullable=False, default=True` | DEFAULT | default | ✅ |
| `auto_play_on_wifi` | `BOOLEAN DEFAULT false` | `Boolean, nullable=False, default=False` | DEFAULT | default | ✅ |
| `extra` | `JSONB NULL` | `JSONB, nullable=True` | NULL | nullable=True | ✅ |
| `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | DEFAULT | server_default | ✅ |
| `updated_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now()` | DEFAULT + 触发器 | server_default + onupdate | ✅ |

#### 3.1.3 索引一致性

| 索引名 | DDL定义 | ORM定义 | 是否一致 |
|--------|---------|---------|----------|
| `idx_user_preferences_user_id` | `CREATE INDEX idx_user_preferences_user_id ON user_preferences(user_id)` | `Index("idx_user_preferences_user_id", "user_id")` | ✅ |

#### 3.1.4 注释一致性

- [ ] 表注释存在且内容匹配
- [ ] 所有字段注释存在且内容匹配

---

### 3.2 notifications表 ↔ Notification模型

#### 3.2.1 表名一致性

- [ ] DDL表名: `notifications`
- [ ] ORM模型`__tablename__`: `notifications`
- [ ] 是否一致: ✅

#### 3.2.2 字段定义一致性

| 字段名 | DDL类型 | ORM类型 | DDL约束 | ORM约束 | 是否一致 |
|--------|---------|---------|---------|---------|----------|
| `id` | `UUID PRIMARY KEY` | `UUID(as_uuid=True), primary_key=True` | 主键 | 主键 | ✅ |
| `user_id` | `UUID NOT NULL` | `UUID(as_uuid=True), nullable=False` | NOT NULL | nullable=False | ✅ |
| `title` | `VARCHAR(255) NOT NULL` | `String(255), nullable=False` | NOT NULL | nullable=False | ✅ |
| `content` | `TEXT` | `Text, nullable=True` | NULL | nullable=True | ✅ |
| `notification_type` | `VARCHAR(50) DEFAULT 'system'` | `String(50), nullable=False, default="system"` | DEFAULT | default | ✅ |
| `related_id` | `UUID NULL` | `UUID(as_uuid=True), nullable=True` | NULL | nullable=True | ✅ |
| `related_type` | `VARCHAR(50) NULL` | `String(50), nullable=True` | NULL | nullable=True | ✅ |
| `is_read` | `BOOLEAN DEFAULT false` | `Boolean, nullable=False, default=False` | DEFAULT | default | ✅ |
| `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP(timezone=True), nullable=False, server_default=func.now()` | DEFAULT | server_default | ✅ |

#### 3.2.3 索引一致性

| 索引名 | DDL定义 | ORM定义 | 是否一致 |
|--------|---------|---------|----------|
| `idx_notifications_user_id` | `CREATE INDEX idx_notifications_user_id ON notifications(user_id)` | `Index("idx_notifications_user_id", "user_id")` | ✅ |
| `idx_notifications_user_is_read` | `CREATE INDEX idx_notifications_user_is_read ON notifications(user_id, is_read)` | `Index("idx_notifications_user_is_read", "user_id", "is_read")` | ✅ |
| `idx_notifications_created_at` | `CREATE INDEX idx_notifications_created_at ON notifications(created_at)` | `Index("idx_notifications_created_at", "created_at")` | ✅ |

#### 3.2.4 注释一致性

- [ ] 表注释存在且内容匹配
- [ ] 所有字段注释存在且内容匹配

---

## 4. 特殊检查项

### 4.1 UUID生成方式

- [ ] DDL注释说明"在应用层通过 uuid.uuid4() 生成"
- [ ] ORM模型使用`default=uuid.uuid4`
- [ ] 是否一致: ✅

### 4.2 时间戳处理

- [ ] DDL使用`DEFAULT CURRENT_TIMESTAMP`
- [ ] ORM使用`server_default=func.now()`
- [ ] `updated_at`字段在DDL中有触发器，ORM中有`onupdate=func.now()`
- [ ] 是否一致: ✅

### 4.3 JSONB字段

- [ ] `pinned_categories`在DDL和ORM中都是JSONB类型
- [ ] `extra`在DDL和ORM中都是JSONB类型
- [ ] 是否一致: ✅

### 4.4 外键约束

- [ ] DDL注释说明不创建数据库层面的外键约束
- [ ] ORM模型不定义`ForeignKey`
- [ ] 是否一致: ✅

---

## 5. 输出格式

请生成以下格式的检测报告：

```markdown
# 用户偏好与通知模块 - 数据库DDL与SQLAlchemy模型一致性检测报告

**检测日期**: 2026-01-18  
**检测结果**: ✅ 完全一致 / ⚠️ 发现问题

---

## 1. user_preferences表 ↔ UserPreferences模型

### 1.1 表名一致性
- ✅ 表名完全一致

### 1.2 字段定义一致性
- ✅ 所有字段名称、类型、约束完全一致

### 1.3 索引一致性
- ✅ 所有索引定义完全一致

### 1.4 注释一致性
- ✅ 表注释和字段注释完全一致

---

## 2. notifications表 ↔ Notification模型

### 2.1 表名一致性
- ✅ 表名完全一致

### 2.2 字段定义一致性
- ✅ 所有字段名称、类型、约束完全一致

### 2.3 索引一致性
- ✅ 所有索引定义完全一致

### 2.4 注释一致性
- ✅ 表注释和字段注释完全一致

---

## 3. 特殊检查项

### 3.1 UUID生成方式
- ✅ 应用层生成方式一致

### 3.2 时间戳处理
- ✅ 时间戳生成和更新机制一致

### 3.3 JSONB字段
- ✅ JSONB字段定义一致

### 3.4 外键约束
- ✅ 不创建数据库层面外键的设计一致

---

## 4. 总体结论

✅ 数据库DDL与SQLAlchemy ORM模型完全一致，无需修复。

或

⚠️ 发现以下问题需要修复：
1. [问题描述]
2. [问题描述]

---

## 5. 建议修复方案

如果发现问题，提供详细的修复建议。
```

---

**执行指令**: 请严格按照本文档的检测清单，逐项检查设计文档DDL与SQLAlchemy ORM模型的一致性，并生成详细的检测报告。
