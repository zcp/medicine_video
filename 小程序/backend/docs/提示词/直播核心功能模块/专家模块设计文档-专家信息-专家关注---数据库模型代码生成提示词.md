# 专家模块设计文档-专家信息-专家关注 - 数据库模型代码生成提示词文档

**版本**: V1.0  
**创建日期**: 2026-01-18  
**生成器**: AI 自动化生成系统  
**基于设计文档**: @docs/03_系统设计/直播核心功能设计文档_v6_专家模块设计文档-专家信息-专家关注.md

---

## 📌 文档用途

本文档是用于指导 AI 代码生成器根据设计文档生成 SQLAlchemy 模型代码的详细提示词文档。

**生成目标**：
- 生成完整的 SQLAlchemy 模型代码（Experts、UserExpertSubscriptions、LiveSessionExperts）
- 确保生成的代码与设计文档完全一致
- 遵循项目架构规范和最佳实践

---

## 🎯 核心设计原则

### 1. 不修改 live_sessions 表

**关键要求**：
- ❌ **禁止修改** `live_sessions` 表的任何字段
- ❌ **禁止添加** `live_sessions.featured_expert_id` 字段
- ✅ **必须使用** `live_session_experts` 中间表实现多对多关系
- ✅ **必须完全依赖**中间表的外键约束和级联删除规则

### 2. 多对多关系实现

**设计方式**：
- 使用独立的中间表 `live_session_experts`
- 支持一个场次有多个专家（主讲、主持、嘉宾）
- 支持一个专家参与多个场次

**外键约束**：
- `session_id` → `live_sessions.id`（`ON DELETE CASCADE`）
- `expert_id` → `experts.id`（`ON DELETE CASCADE`）

---

## 🗄️ 表定义提取

### 表1: experts（专家信息表）

**核心字段**：
- `id` (UUID PRIMARY KEY)
- `user_id` (UUID UNIQUE NULL) - 关联平台用户公开ID
- `name` (VARCHAR(120) NOT NULL)
- `title` (VARCHAR(120))
- `hospital` (VARCHAR(200))
- `department` (VARCHAR(120))
- `expertise_areas` (TEXT)
- `bio` (TEXT)
- `avatar_url` (VARCHAR(512))
- `is_featured` (BOOLEAN DEFAULT false)
- `sort_order` (INT DEFAULT 0)
- `contact_info` (JSONB NULL)
- `created_at` (TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)
- `updated_at` (TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)

**索引**：
- `idx_experts_is_featured` (is_featured, sort_order)
- `idx_experts_user_id` (user_id)

**约束**：
- `user_id` UNIQUE

---

### 表2: user_expert_subscriptions（用户专家订阅表）

**核心字段**：
- `id` (UUID PRIMARY KEY)
- `user_id` (UUID NOT NULL) - 用户公开ID
- `expert_id` (UUID NOT NULL REFERENCES experts(id) ON DELETE CASCADE)
- `created_at` (TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)

**索引**：
- `idx_user_expert_subscriptions_user_id` (user_id)
- `idx_user_expert_subscriptions_expert_id` (expert_id)
- `idx_user_expert_subscriptions_created_at` (created_at DESC)

**约束**：
- `UNIQUE(user_id, expert_id)`

---

### 表3: live_session_experts（直播场次专家关联表）

**核心字段**：
- `id` (UUID PRIMARY KEY)
- `session_id` (UUID NOT NULL REFERENCES live_sessions(id) ON DELETE CASCADE)
- `expert_id` (UUID NOT NULL REFERENCES experts(id) ON DELETE CASCADE)
- `role` (VARCHAR(50) NOT NULL DEFAULT '主讲')
- `sort_order` (INT DEFAULT 0)
- `created_at` (TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP)

**索引**：
- `idx_live_session_experts_session` (session_id)
- `idx_live_session_experts_expert` (expert_id)
- `idx_live_session_experts_role` (session_id, role)

**约束**：
- `UNIQUE(session_id, expert_id, role)`

**外键级联删除**：
- 删除场次时，自动删除该场次的所有专家关联记录
- 删除专家时，自动删除该专家的所有场次关联记录

---

## 🔧 SQLAlchemy 模型生成要求

### 1. 通用要求

**遵循项目架构**：
- 模型文件路径：`backend/live_core_service/app/models/experts.py`
- 基类：`app.database.Base`
- 导入路径：`from app.database import Base`

**UUID 字段处理**：
- 所有 UUID 字段使用 `UUID(as_uuid=True)`
- 主键：`primary_key=True, default=uuid.uuid4`
- ❌ 禁止使用 `server_default=gen_random_uuid()`

**时间戳字段**：
- 类型：`TIMESTAMP(timezone=True)`
- `created_at`: `server_default=func.now()`
- `updated_at`: `server_default=func.now(), onupdate=func.now()`

**外键关系**：
- 所有外键使用 `ForeignKey(...)`
- 必须定义 `ondelete` 参数
- 必须使用 `backref` 定义反向关系

**表和约束定义**：
- `__tablename__`: 表名（小写，下划线分隔）
- `__table_args__`: 定义约束和索引
- `UniqueConstraint`: 定义唯一约束
- `Index`: 定义索引

**枚举类型**：
- 使用 Python `enum.Enum` 定义角色枚举

**注释**：
- 所有字段必须添加 `comment` 参数
- 所有表必须添加 `__doc__` 属性

**关系定义**：
- 使用 `relationship()` 定义表间关系
- 必须配置 `back_populates` 或 `backref`
- 使用 `lazy='selectin'` 或 `lazy='joined'` 优化查询

---

### 2. Expert 模型

**模型类名**: `Expert`

**必须定义的字段**：
```python
id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
user_id = Column(UUID(as_uuid=True), nullable=True, unique=True, comment='关联平台用户公开ID')
name = Column(String(120), nullable=False, comment='专家姓名')
title = Column(String(120), nullable=True, comment='职称')
hospital = Column(String(200), nullable=True, comment='所属医院')
department = Column(String(120), nullable=True, comment='科室')
expertise_areas = Column(Text, nullable=True, comment='擅长领域')
bio = Column(Text, nullable=True, comment='个人简介')
avatar_url = Column(String(512), nullable=True, comment='头像URL')
is_featured = Column(Boolean, default=False, nullable=False, comment='是否为首页推荐专家')
sort_order = Column(Integer, default=0, nullable=False, comment='排序顺序')
contact_info = Column(JSONB, nullable=True, comment='联系方式JSONB')
created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='创建时间')
updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now(), comment='更新时间')
```

**必须定义的约束和索引**：
```python
__table_args__ = (
    Index('idx_experts_is_featured', 'is_featured', 'sort_order'),
    Index('idx_experts_user_id', 'user_id'),
)
```

**表注释**：
```python
__doc__ = '平台专家信息表（外部专家+内部用户专家）'
```

---

### 3. UserExpertSubscription 模型

**模型类名**: `UserExpertSubscription`

**必须定义的字段**：
```python
id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
user_id = Column(UUID(as_uuid=True), nullable=False, comment='用户公开ID')
expert_id = Column(UUID(as_uuid=True), ForeignKey("experts.id", ondelete="CASCADE"), nullable=False, comment='专家ID')
created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='关注时间')
```

**必须定义的关系**：
```python
expert = relationship("Expert", backref="user_expert_subscriptions")
```

**必须定义的约束和索引**：
```python
__table_args__ = (
    UniqueConstraint('user_id', 'expert_id', name='uq_user_expert_subscriptions_user_expert'),
    Index('idx_user_expert_subscriptions_user_id', 'user_id'),
    Index('idx_user_expert_subscriptions_expert_id', 'expert_id'),
    Index('idx_user_expert_subscriptions_created_at', 'created_at'),
)
```

**表注释**：
```python
__doc__ = '用户关注专家表（支持"我的关注"功能）'
```

---

### 4. LiveSessionExpert 模型

**模型类名**: `LiveSessionExpert`

**必须定义的字段**：
```python
id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
session_id = Column(UUID(as_uuid=True), ForeignKey("live_sessions.id", ondelete="CASCADE"), nullable=False, comment='直播场次ID')
expert_id = Column(UUID(as_uuid=True), ForeignKey("experts.id", ondelete="CASCADE"), nullable=False, comment='专家ID')
role = Column(String(50), nullable=False, default='主讲', comment='专家角色')
sort_order = Column(Integer, nullable=False, default=0, comment='显示顺序')
created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='关联创建时间')
```

**必须定义的关系**：
```python
session = relationship("LiveSession", backref="live_session_experts")
expert = relationship("Expert", backref="live_session_experts")
```

**必须定义的枚举类型**：
```python
class SessionExpertRole(str, enum.Enum):
    MAIN_SPEAKER = "主讲"
    HOST = "主持"
    GUEST = "嘉宾"
```

**必须定义的约束和索引**：
```python
__table_args__ = (
    UniqueConstraint('session_id', 'expert_id', 'role', name='uq_live_session_experts_session_expert_role'),
    Index('idx_live_session_experts_session', 'session_id'),
    Index('idx_live_session_experts_expert', 'expert_id'),
    Index('idx_live_session_experts_role', 'session_id', 'role'),
)
```

**表注释**：
```python
__doc__ = '直播场次专家关联表（多对多关系），支持一个场次有多个专家'
```

---

## 🚫 禁止事项

- ❌ **禁止修改** `live_sessions` 表的任何字段
- ❌ **禁止添加** `live_sessions.featured_expert_id` 字段
- ❌ **禁止使用** `featured_expert_id` 字段
- ❌ **禁止直接关联** `live_sessions` 和 `experts`（不使用中间表）
- ✅ **必须使用** `live_session_experts` 中间表

---

## 📊 输出文件规范

**文件路径**: `backend/live_core_service/app/models/experts.py`

**文件结构**:
```python
"""
专家模块模型定义
包含专家信息、用户关注、场次专家关联的模型
"""

from sqlalchemy import Column, String, Integer, Boolean, Text, ForeignKey, UniqueConstraint, Index, TIMESTAMP
from sqlalchemy.dialects.postgresql import UUID, JSONB
from sqlalchemy.orm import relationship
from sqlalchemy import func
import uuid
import enum

from app.database import Base


class SessionExpertRole(str, enum.Enum):
    """专家角色枚举"""
    MAIN_SPEAKER = "主讲"
    HOST = "主持"
    GUEST = "嘉宾"


class Expert(Base):
    """
    平台专家信息表（外部专家+内部用户专家）
    """
    __tablename__ = "experts"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment='主键ID')
    user_id = Column(UUID(as_uuid=True), nullable=True, unique=True, comment='关联平台用户公开ID')
    name = Column(String(120), nullable=False, comment='专家姓名')
    title = Column(String(120), nullable=True, comment='职称')
    hospital = Column(String(200), nullable=True, comment='所属医院')
    department = Column(String(120), nullable=True, comment='科室')
    expertise_areas = Column(Text, nullable=True, comment='擅长领域')
    bio = Column(Text, nullable=True, comment='个人简介')
    avatar_url = Column(String(512), nullable=True, comment='头像URL')
    is_featured = Column(Boolean, default=False, nullable=False, comment='是否为首页推荐专家')
    sort_order = Column(Integer, default=0, nullable=False, comment='排序顺序')
    contact_info = Column(JSONB, nullable=True, comment='联系方式JSONB')
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='创建时间')
    updated_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now(), comment='更新时间')
    
    __table_args__ = (
        Index('idx_experts_is_featured', 'is_featured', 'sort_order'),
        Index('idx_experts_user_id', 'user_id'),
    )
    
    def __repr__(self):
        return f"<Expert(id={self.id}, name={self.name})>"


class UserExpertSubscription(Base):
    """
    用户关注专家表（支持"我的关注"功能）
    """
    __tablename__ = "user_expert_subscriptions"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment='主键ID')
    user_id = Column(UUID(as_uuid=True), nullable=False, comment='用户公开ID')
    expert_id = Column(UUID(as_uuid=True), ForeignKey("experts.id", ondelete="CASCADE"), nullable=False, comment='专家ID')
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='关注时间')
    
    expert = relationship("Expert", backref="user_expert_subscriptions")
    
    __table_args__ = (
        UniqueConstraint('user_id', 'expert_id', name='uq_user_expert_subscriptions_user_expert'),
        Index('idx_user_expert_subscriptions_user_id', 'user_id'),
        Index('idx_user_expert_subscriptions_expert_id', 'expert_id'),
        Index('idx_user_expert_subscriptions_created_at', 'created_at'),
    )
    
    def __repr__(self):
        return f"<UserExpertSubscription(id={self.id}, user_id={self.user_id}, expert_id={self.expert_id})>"


class LiveSessionExpert(Base):
    """
    直播场次专家关联表（多对多关系）
    """
    __tablename__ = "live_session_experts"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment='主键ID')
    session_id = Column(UUID(as_uuid=True), ForeignKey("live_sessions.id", ondelete="CASCADE"), nullable=False, comment='直播场次ID')
    expert_id = Column(UUID(as_uuid=True), ForeignKey("experts.id", ondelete="CASCADE"), nullable=False, comment='专家ID')
    role = Column(String(50), nullable=False, default=SessionExpertRole.MAIN_SPEAKER, comment='专家角色')
    sort_order = Column(Integer, nullable=False, default=0, comment='显示顺序')
    created_at = Column(TIMESTAMP(timezone=True), nullable=False, server_default=func.now(), comment='关联创建时间')
    
    session = relationship("LiveSession", backref="live_session_experts")
    expert = relationship("Expert", backref="live_session_experts")
    
    __table_args__ = (
        UniqueConstraint('session_id', 'expert_id', 'role', name='uq_live_session_experts_session_expert_role'),
        Index('idx_live_session_experts_session', 'session_id'),
        Index('idx_live_session_experts_expert', 'expert_id'),
        Index('idx_live_session_experts_role', 'session_id', 'role'),
    )
    
    def __repr__(self):
        return f"<LiveSessionExpert(id={self.id}, session_id={self.session_id}, expert_id={self.expert_id}, role={self.role})>"
```

---

## 📝 代码生成指令

**请根据以下要求和设计文档生成 SQLAlchemy 模型代码**：

**核心要求**：
1. **严格遵循设计文档**：所有字段、约束、索引、关系必须与设计文档完全一致
2. **不修改 live_sessions 表**：禁止生成任何修改 live_sessions 表的代码
3. **使用中间表实现多对多**：必须生成 `live_session_experts` 模型
4. **遵循项目规范**：使用正确的导入路径、基类、类型定义
5. **完整注释**：所有字段和表必须有详细注释

**设计文档路径**：`@docs/03_系统设计/直播核心功能设计文档_v6_专家模块设计文档-专家信息-专家关注.md`

**必须生成的文件**：
- `backend/live_core_service/app/models/experts.py`

**禁止事项**：
- ❌ 禁止生成 `featured_expert_id` 字段
- ❌ 禁止修改 `live_sessions` 表
- ❌ 禁止直接关联 `live_sessions` 和 `experts`

---

## ✅ 验收标准

- [ ] 所有模型都继承自 `Base` 基类
- [ ] 所有主键都使用 `UUID(as_uuid=True), primary_key=True, default=uuid.uuid4`
- [ ] 所有外键都定义了 `ondelete` 参数
- [ ] 所有外键都使用了 `ForeignKey()` 正确引用其他表
- [ ] 所有字段都有详细的 `comment` 参数
- [ ] 所有表都定义了 `__tablename__`
- [ ] 所有约束和索引都定义在 `__table_args__` 中
- [ ] 使用了 `SessionExpertRole` 枚举定义角色
- [ ] 关系定义使用了 `relationship()` 和 `backref`

---

**文档结束**

