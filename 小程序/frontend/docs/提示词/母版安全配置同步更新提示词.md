# 母版安全配置内容同步更新提示词

**版本**: V1.0
**创建日期**: 2026-01-12
**目标**: 将母版 Section 5.2.1-5.2.5 的安全配置内容**有机融入**到所有模块的提示词文档中

---

## 1. 任务概述

**母版文档**: `docs/提示词/自动化后端代码生成/crud_service_endpoint代码生成提示词母版.md`

**目标**: 将母版 Section 5.2.1 - 5.2.5 的安全配置内容**有机融入**到以下所有提示词文档中，确保安全配置的一致性。

**需要更新的文档列表**:

### A. 专家模块
1. `docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---CRUD层代码生成提示词.md`
2. `docs/提示词/直播核心功能模块/专家模块设计文档-专家信息-专家关注---Service层和API层代码生成提示词.md`

### B. 用户偏好与通知模块
3. `docs/提示词/直播核心功能模块/用户偏好与通知---CRUD层代码生成提示词.md`
4. `docs/提示词/直播核心功能模块/用户偏好与通知---Service和API端点代码生成提示词.md`

### C. 用户行为模块
5. `docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---CRUD层代码生成提示词.md`
6. `docs/提示词/直播核心功能模块/用户行为模块设计文档-收藏-观看历史-订阅提醒---Service层和API层代码生成提示词.md`

### D. 内容管理模块
7. `docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---CRUD层代码生成提示词.md`
8. `docs/提示词/直播核心功能模块/内容管理模块设计文档-标签-分类-场次标签关联---Service层和API层代码生成提示词.md`

---

## 2. 母版安全配置核心内容（必须完整同步）

### 2.1. 环境变量配置安全规范（适用于所有模块）

#### 2.1.1. CORS 配置规范

**核心内容**：
- **禁止**在代码中硬编码 CORS 配置（如 `allow_origins=["*"]`）
- **必须**从环境变量读取 CORS 配置
- **配置格式**: 支持逗号分隔的多个域名（如 `http://localhost:5175,https://example.com`）

**实现示例**：
```python
# config.py
CORS_ORIGINS_STR: str = os.getenv("CORS_ORIGINS", "*")
BACKEND_CORS_ORIGINS: List[str] = [
    origin.strip() 
    for origin in CORS_ORIGINS_STR.split(",") 
    if origin.strip()
]

# main.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.BACKEND_CORS_ORIGINS,  # 从配置读取
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**生产环境要求**: CORS 配置**必须**为具体域名，**严禁**使用 `["*"]`

#### 2.1.2. 敏感信息配置规范

**核心内容**：
- **禁止**在代码中硬编码以下敏感信息:
  - 数据库密码、用户名
  - JWT 密钥（`JWT_SECRET_KEY`）
  - 第三方服务密钥（如 Redis、Celery broker）
- **必须**通过环境变量读取所有敏感配置
- **禁止**为敏感信息设置不安全的默认值

**正确和错误的示例对比**：
- ❌ 错误: `POSTGRES_PASSWORD = os.getenv("POSTGRES_PASSWORD", "324zq999")`
- ❌ 错误: `JWT_SECRET_KEY = os.getenv("JWT_SECRET_KEY", "my-key")`
- ✅ 正确: `POSTGRES_PASSWORD = os.getenv("POSTGRES_PASSWORD", "")`

#### 2.1.3. 配置验证规范

**核心内容**：
- **必须**在应用启动时验证必需的敏感配置
- **缺失配置**必须抛出明确的异常，而非使用不安全的默认值
- **数据库密码验证**: 检查密码是否为空
- **JWT 密钥验证**: 检查密钥长度（≥32字符）

**实现示例**：
```python
class Settings(BaseSettings):
    # 数据库配置
    POSTGRES_PASSWORD: str = os.getenv("POSTGRES_PASSWORD", "")
    
    # JWT 配置
    JWT_SECRET_KEY: str = os.getenv("JWT_SECRET_KEY", "")
    
    def __init__(self, **kwargs):
        super().__init__(**kwargs)
        self._validate_required_configs()
    
    def _validate_required_configs(self):
        """验证必需的敏感配置"""
        if not self.POSTGRES_PASSWORD:
            raise ValueError(
                "POSTGRES_PASSWORD 环境变量未设置。"
                "生产环境必须设置此变量，不允许使用默认值。"
            )
        if not self.JWT_SECRET_KEY:
            raise ValueError(
                "JWT_SECRET_KEY 环境变量未设置。"
                "生产环境必须设置此变量，且长度应至少32个字符。"
            )
        if len(self.JWT_SECRET_KEY) < 32:
            raise ValueError(
                "JWT_SECRET_KEY 长度不足32个字符，存在安全风险。"
                "请使用更长的密钥（推荐64字符以上）。"
            )
```

### 2.2. 日志安全规范（适用于所有模块）

#### 2.2.1. 禁止在日志中打印敏感信息

**核心内容**：
- ❌ **禁止**: 打印完整的数据库连接 URL（包含密码）
- ✅ **必须**: 只打印非敏感的连接信息

**正确和错误的示例对比**：
```python
# 错误示例
logger.info(f"尝试连接数据库: {DATABASE_URL}")

# 正确示例
logger.info(
    f"尝试连接数据库: "
    f"server={settings.POSTGRES_SERVER}, "
    f"port={settings.POSTGRES_PORT}, "
    f"database={settings.POSTGRES_DB}, "
    f"user={settings.POSTGRES_USER}"
    # 不打印密码
)
```

#### 2.2.2. 日志脱敏规范（推荐实现）

**核心内容**：
- 创建日志脱敏工具函数 `app/core/logging_utils.py`
- 实现脱敏日志格式化器 `SanitizedFormatter`
- 自动脱敏数据库连接 URL 和 JWT 密钥

**实现示例**：
```python
import re
import logging

def sanitize_log_message(message: str) -> str:
    """对日志消息进行脱敏处理"""
    # 移除数据库连接 URL 中的密码
    message = re.sub(
        r'postgresql[+a-z]*://[^:]+:([^@]+)@',
        r'postgresql://***:***@',
        message,
        flags=re.IGNORECASE
    )
    
    # 移除 JWT 密钥（如果意外出现在日志中）
    message = re.sub(
        r'JWT_SECRET_KEY["\']?\s*[:=]\s*["\']?([^"\'\s]+)',
        r'JWT_SECRET_KEY="***"',
        message,
        flags=re.IGNORECASE
    )
    
    return message

class SanitizedFormatter(logging.Formatter):
    """脱敏日志格式化器"""
    def format(self, record):
        record.msg = sanitize_log_message(str(record.msg))
        return super().format(record)
```

#### 2.2.3. 错误消息脱敏

**核心内容**：
- **禁止**在异常处理中暴露敏感信息
- **必须**只返回通用的错误消息给客户端

**实现示例**：
```python
try:
    # 数据库操作
    pass
except Exception as e:
    logger.error(f"数据库操作失败: {type(e).__name__}")
    # 不要打印完整的异常信息（可能包含连接字符串）
    raise HTTPException(status_code=500, detail="内部服务器错误")
```

### 2.3. 环境变量文件管理规范（适用于所有模块）

#### 2.3.1. `.env.example` 模板文件

**核心内容**：
- **必须**提供 `.env.example` 模板文件
- **必须**包含所有必需的环境变量（包括说明和示例）

**模板文件结构示例**：
```bash
# ============================================
# LiveCore Service 环境变量配置模板
# ============================================
# 
# 使用说明:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际环境填写配置值
# 3. 生产环境必须设置所有必需变量
# 4. 不要将 .env 文件提交到版本控制系统
#
# ============================================

# ========== 数据库配置 ==========
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=live_core_test
POSTGRES_USER=postgres
# ⚠️ 生产环境必须设置强密码
POSTGRES_PASSWORD=your_secure_password_here

# ========== CORS配置 ==========
# 开发环境示例: CORS_ORIGINS=http://localhost:5175,http://localhost:3000
# 生产环境示例: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=*

# ========== JWT配置 ==========
# ⚠️ 生产环境必须设置强密钥（推荐64字符以上）
# 生成方式: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_min_32_chars
JWT_ALGORITHM=HS256

# ========== Celery配置 ==========
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
```

#### 2.3.2. `.gitignore` 规范

**核心内容**：
- **必须**在 `.gitignore` 中忽略所有环境变量文件
- **必须**保留 `.env.example` 模板文件

**配置示例**：
```gitignore
# 环境变量文件（包含敏感信息）
.env
.env.local
.env.production
.env.staging
.env.*.local

# 但保留模板文件
!.env.example
```

### 2.4. 生产环境安全规范（适用于所有模块）

#### 2.4.1. 密钥管理服务（推荐）

**核心内容**：
- 生产环境**强烈建议**使用密钥管理服务，而非环境变量
- **支持的密钥管理服务**:
  - AWS: AWS Secrets Manager / Parameter Store
  - Azure: Azure Key Vault
  - GCP: Secret Manager
  - HashiCorp: Vault

**AWS Secrets Manager 集成示例**：
```python
# app/core/secrets_manager.py (可选)
import boto3
import json

def get_secret(secret_name: str) -> dict:
    """从 AWS Secrets Manager 获取密钥"""
    client = boto3.client('secretsmanager')
    response = client.get_secret_value(SecretId=secret_name)
    return json.loads(response['SecretString'])

# 在 config.py 中使用
# secrets = get_secret("livecore/production")
# POSTGRES_PASSWORD = secrets.get("POSTGRES_PASSWORD")
```

#### 2.4.2. 环境变量注入（Docker/K8s）

**核心内容**：
- 使用环境变量注入或密钥管理服务，而非在镜像中包含 `.env` 文件

**Docker Compose 示例**：
```yaml
services:
  live_core_service:
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}  # 从宿主机环境变量读取
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS}
```

#### 2.4.3. 文件权限控制（Linux/Unix）

**核心内容**：
- **必须**设置环境变量文件的权限，防止未授权访问

**命令示例**：
```bash
# 设置文件权限（仅所有者可读写）
chmod 600 .env

# 设置目录权限（防止其他用户访问）
chmod 700 .
```

### 2.5. 密钥生成最佳实践（适用于所有模块）

#### 2.5.1. JWT 密钥生成

**核心内容**：
- **推荐长度**: 64 字符（128位加密强度）
- **生成方式**:
  ```bash
  # 使用 openssl（推荐）
  openssl rand -hex 32
  
  # 使用 Python
  python -c "import secrets; print(secrets.token_hex(32))"
  ```

#### 2.5.2. 数据库密码生成

**核心内容**：
- **推荐长度**: 16-32 字符
- **生成方式**:
  ```bash
  # 使用 openssl
  openssl rand -base64 16
  
  # 使用密码生成工具
  pwgen -s 16 1
  ```

#### 2.5.3. 密钥命名规范

**核心内容**：
- 使用大写字母和下划线：`POSTGRES_PASSWORD`, `JWT_SECRET_KEY`
- 使用有意义的名称：`CORS_ORIGINS` 而非 `CORS`
- 分组前缀：`POSTGRES_*`, `JWT_*`, `CELERY_*`

---

## 3. 更新原则（严格遵循）

### 3.1. 有机融入原则

**✅ 必须遵守**：
1. **不破坏现有结构**：在现有 Section 中增加或修改内容
2. **不重复定义**：避免在多个 Section 中重复相同内容
3. **优先级明确**：母版 Section 5.2.1-5.2.5 是最高优先级
4. **模块适配**：根据模块特点调整配置示例
5. **示例驱动**：为每个模块提供具体配置示例

**✅ 最小幅度修改**：
1. 只增加或修改安全配置相关内容
2. 不修改其他非安全配置相关内容
3. 保持文档整体结构和风格

### 3.2. 更新内容范围

**必须包含的内容**（不得遗漏）：
- [ ] CORS 配置规范（禁止硬编码、从环境变量读取）
- [ ] 敏感信息配置规范（禁止硬编码、禁止不安全默认值）
- [ ] 配置验证规范（启动时验证必需配置）
- [ ] 日志安全规范（禁止打印敏感信息）
- [ ] 日志脱敏规范（推荐实现）
- [ ] 错误消息脱敏
- [ ] `.env.example` 模板文件规范
- [ ] `.gitignore` 规范
- [ ] 生产环境安全规范（密钥管理、Docker 注入、文件权限）
- [ ] 密钥生成最佳实践（JWT、数据库密码、命名规范）

**禁止添加的内容**（避免过度更新）：
- [ ] 任何非安全配置相关的功能描述
- [ ] 任何与母版 Section 5.2.1-5.2.5 不一致的内容
- [ ] 任何重复定义的内容

---

## 4. 每个文档的更新指南

### 4.1. 更新检查清单（更新前使用）

**Step 1：文档结构检查**
- [ ] 文档是否包含"配置"或"安全"相关的 Section
- [ ] 如果包含，是否与母版 Section 5.2.1-5.2.5 保持一致
- [ ] 如果不包含，是否需要新增 Section

**Step 2：内容完整性检查**
- [ ] 文档是否包含 CORS 配置规范
- [ ] 文档是否包含敏感信息配置规范
- [ ] 文档是否包含配置验证规范
- [ ] 文档是否包含日志安全规范
- [ ] 文档是否包含环境变量文件管理规范
- [ ] 文档是否包含生产环境安全规范
- [ ] 文档是否包含密钥生成最佳实践

**Step 3：通用性检查**
- [ ] 安全配置是否标注为"适用于所有模块"
- [ ] 配置示例是否与模块无关
- [ ] 是否不依赖特定模块的业务逻辑

### 4.2. 具体更新步骤

**Step 1：读取文档**
```
请使用 read_file() 工具读取目标文档的完整内容
```

**Step 2：分析现有内容**
```
请检查文档中是否已经包含安全配置相关的内容：
- 如果包含，检查是否与母版 Section 5.2.1-5.2.5 保持一致
- 如果不包含，需要在适当位置新增一个 Section
```

**Step 3：确定更新位置**
```
根据文档结构，确定在哪里添加安全配置内容：
- 通常在 Section 5（核心架构约束）之后
- 或者作为 Section 5.x 的一个子 Section
- 或者作为 Section 6（交付物）之前的新 Section
```

**Step 4：执行更新**
```
按照以下结构，有机融入安全配置内容：

【新增或更新 Section】
##### 5.x 【新增】全项目安全配置规范（适用于所有模块）

内容参考母版 Section 5.2.1 - 5.2.5 的完整内容
```

**Step 5：调整示例**
```
根据模块特点调整配置示例：
- 保持通用性，不依赖特定模块的业务逻辑
- 确保示例代码与模块无关
- 保持与母版 Section 5.2.1-5.2.5 的内容一致
```

### 4.3. 更新后检查清单（更新后必须执行）

**✅ 结构完整性检查**：
- [ ] 文档是否包含完整的安全配置 Section
- [ ] 安全配置 Section 是否包含所有 5 个核心子节：
  - [ ] 环境变量配置安全规范
  - [ ] 日志安全规范
  - [ ] 环境变量文件管理规范
  - [ ] 生产环境安全规范
  - [ ] 密钥生成最佳实践

**✅ 内容一致性检查**：
- [ ] 安全配置是否与母版 Section 5.2.1-5.2.5 保持一致
- [ ] 代码示例是否与母版保持一致
- [ ] 配置规范是否与母版保持一致
- [ ] 最佳实践是否与母版保持一致

**✅ 通用性检查**：
- [ ] 安全配置是否标注为"适用于所有模块"
- [ ] 配置示例是否与模块无关
- [ ] 是否不依赖特定模块的业务逻辑

**✅ 无遗漏检查**：
- [ ] 是否有任何一个安全配置子节缺失
- [ ] 是否有任何一个代码示例缺失
- [ ] 是否有任何一个最佳实践缺失

**✅ 无过度更新检查**：
- [ ] 是否添加了任何非安全配置相关的内容
- [ ] 是否修改了任何非安全配置相关的 Section
- [ ] 是否有重复定义的内容

**✅ 无冲突检查**：
- [ ] 新增内容是否与现有内容冲突
- [ ] 代码示例是否与文档其他部分一致
- [ ] 安全配置是否与文档其他配置冲突

---

## 5. 每个模块的特定更新内容

### 5.1. 专家模块

**通用性保证**：
- 安全配置适用于所有模块，不依赖专家模块的业务逻辑
- 配置示例与专家模块无关

**需要添加的内容**：
1. Section 5.2.1 - 环境变量配置安全规范
2. Section 5.2.2 - 日志安全规范
3. Section 5.2.3 - 环境变量文件管理规范
4. Section 5.2.4 - 生产环境安全规范
5. Section 5.2.5 - 密钥生成最佳实践

**更新位置**：
- Service 和 API 层文档：在 Section 5（核心架构约束）之后添加
- CRUD 层文档：在 Section 5（核心架构约束）之后添加

### 5.2. 用户偏好与通知模块

**通用性保证**：
- 安全配置适用于所有模块，不依赖用户偏好与通知模块的业务逻辑
- 配置示例与用户偏好与通知模块无关

**需要添加的内容**：
1. Section 5.2.1 - 环境变量配置安全规范
2. Section 5.2.2 - 日志安全规范
3. Section 5.2.3 - 环境变量文件管理规范
4. Section 5.2.4 - 生产环境安全规范
5. Section 5.2.5 - 密钥生成最佳实践

**更新位置**：
- Service 和 API 层文档：在 Section 5（核心架构约束）之后添加
- CRUD 层文档：在 Section 5（核心架构约束）之后添加

### 5.3. 用户行为模块

**通用性保证**：
- 安全配置适用于所有模块，不依赖用户行为模块的业务逻辑
- 配置示例与用户行为模块无关

**需要添加的内容**：
1. Section 5.2.1 - 环境变量配置安全规范
2. Section 5.2.2 - 日志安全规范
3. Section 5.2.3 - 环境变量文件管理规范
4. Section 5.2.4 - 生产环境安全规范
5. Section 5.2.5 - 密钥生成最佳实践

**更新位置**：
- Service 和 API 层文档：在 Section 5（核心架构约束）之后添加
- CRUD 层文档：在 Section 5（核心架构约束）之后添加

### 5.4. 内容管理模块

**通用性保证**：
- 安全配置适用于所有模块，不依赖内容管理模块的业务逻辑
- 配置示例与内容管理模块无关

**需要添加的内容**：
1. Section 5.2.1 - 环境变量配置安全规范
2. Section 5.2.2 - 日志安全规范
3. Section 5.2.3 - 环境变量文件管理规范
4. Section 5.2.4 - 生产环境安全规范
5. Section 5.2.5 - 密钥生成最佳实践

**更新位置**：
- Service 和 API 层文档：在 Section 5（核心架构约束）之后添加
- CRUD 层文档：在 Section 5（核心架构约束）之后添加

---

## 6. 执行顺序建议

为了确保更新的准确性和一致性，建议按照以下顺序执行：

1. **第一步**：仔细阅读母版 Section 5.2.1 - 5.2.5 的内容，确保完全理解安全配置规范
2. **第二步**：从专家模块开始更新，使用更新前检查清单
3. **第三步**：执行专家模块 Service 和 API 层文档的更新
4. **第四步**：使用更新后检查清单验证专家模块 Service 和 API 层文档
5. **第五步**：执行专家模块 CRUD 层文档的更新
6. **第六步**：使用更新后检查清单验证专家模块 CRUD 层文档
7. **第七步**：按照相同的方法更新用户偏好与通知模块
8. **第八步**：按照相同的方法更新用户行为模块
9. **第九步**：按照相同的方法更新内容管理模块
10. **第十步**：使用最终检查清单验证所有文档

---

## 7. 最终检查清单（所有文档更新完成后）

### 7.1. 通用检查清单

**✅ 文档完整性**：
- [ ] 所有 8 个文档都已更新
- [ ] 每个文档都包含完整的安全配置 Section
- [ ] 每个文档都包含完整的代码示例

**✅ 内容一致性**：
- [ ] 所有文档的安全配置都与母版 Section 5.2.1-5.2.5 保持一致
- [ ] 所有文档的代码示例都与母版保持一致
- [ ] 所有文档都使用了相同的配置规范
- [ ] 所有文档都标注了"适用于所有模块"

**✅ 通用性检查**：
- [ ] 所有文档的安全配置都不依赖特定模块的业务逻辑
- [ ] 所有文档的配置示例都与模块无关
- [ ] 所有文档都保持了通用性

**✅ 质量检查**：
- [ ] 没有遗漏任何应该更新的安全配置子节
- [ ] 没有过度增加不必要的内容
- [ ] 没有引起任何冲突和不一致
- [ ] 所有代码示例都是正确和完整的

### 7.2. 安全配置专用检查清单

**✅ 环境变量配置检查**：
- [ ] 是否包含 CORS 配置规范（禁止硬编码）
- [ ] 是否包含敏感信息配置规范（禁止不安全默认值）
- [ ] 是否包含配置验证规范（启动时验证）

**✅ 日志安全检查**：
- [ ] 是否包含禁止打印敏感信息的规范
- [ ] 是否包含日志脱敏规范
- [ ] 是否包含错误消息脱敏规范

**✅ 环境变量文件管理检查**：
- [ ] 是否包含 `.env.example` 模板文件规范
- [ ] 是否包含 `.gitignore` 规范

**✅ 生产环境安全检查**：
- [ ] 是否包含密钥管理服务推荐
- [ ] 是否包含 Docker/K8s 环境变量注入示例
- [ ] 是否包含文件权限控制说明

**✅ 密钥生成最佳实践检查**：
- [ ] 是否包含 JWT 密钥生成规范
- [ ] 是否包含数据库密码生成规范
- [ ] 是否包含密钥命名规范

---

## 8. 更新记录表格

在完成每个文档的更新后，请填写以下表格，记录更新情况：

|| 文档名称 | 更新前版本 | 更新后版本 | 更新日期 | 更新内容摘要 | 验证状态 |
||---------|-----------|-----------|---------|------------|---------|
|| 专家模块-Service和API | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 专家模块-CRUD | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 用户偏好与通知-Service和API | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 用户偏好与通知-CRUD | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 用户行为模块-Service和API | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 用户行为模块-CRUD | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 内容管理模块-Service和API | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |
|| 内容管理模块-CRUD | V1.1 | V1.2 | YYYY-MM-DD | 添加安全配置 Section | ✅ 通过 |

---

## 9. 与权限更新提示词的区别

| 方面 | 权限更新提示词 | 安全配置更新提示词 |
|------|--------------|----------------|
| **目标** | 同步权限管理策略（Section 5.0.5） | 同步安全配置内容（Section 5.2.1-5.2.5） |
| **内容** | 双轨鉴权、权限守卫函数、404伪装、Repository过滤 | CORS配置、敏感信息、日志安全、环境变量管理、密钥生成 |
| **适用范围** | API、Service、CRUD 层的权限逻辑 | 所有模块的全局配置和安全规范 |
| **通用性** | 适用于所有模块的权限设计 | 适用于所有模块的配置和安全规范 |
| **更新方式** | 需要根据模块特点调整权限逻辑 | 保持通用性，不依赖模块业务逻辑 |

---

**文档版本**: V1.0
**创建日期**: 2026-01-12
**状态**: 准备就绪

