
@专题功能完整设计文档-修正版.md  这是我的设计文档，其中数据库的 代码已经生成，
即Pynatic模型和 SQLAichemym模型已经生成。 现在需要为它生成一个代码生成提示词文档， 
用于指导AI生成crud层，service层和endpoint层等相关的代码。 
@直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第一阶段.md  
@直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第三阶段.md 
@直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第二阶段.md  
这是我的另一个功能模块对应代码生成的提示词文档。 
你能帮我生成一个代码生成提示词文档吗，用于 指导AI生成    
@专题功能完整设计文档-修正版.md  中的crud层，service层和endpoint层的代码和一些必要的辅助代码。 
其中 1）代码的编程规范和要求必须和后面“第一阶段，第二阶段，第三阶段”文档中的要求保持一致，
比如 异常处理要求，命名规范， 数据操作规范，返回结构规范等等。
2）该提示词文档是否覆盖 @专题功能完整设计文档-修正版.md 文档中所有涉及service和endpoint操作，包含api接口等等，**请仔细逐一检测，不要遗漏**，
3）该提示词文档指导AI生成代码的规范和要求是否也满足 @专题功能完整设计文档-修正版.md 里面的相关要求，
4）该提示词文档指导AI生成代码的规范和要求是否与 @直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第二阶段.md  @直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第三阶段.md  中对service 和 endpoint操作的代码要求和规范保持一致。请逐一检测，不要遗漏。 
5）│   │   └── live_core.py          # <-- 已存在（包含Topic相关模型） 这个有问题，在models文件夹下，有两个文件 live_core.py 和topic.py 前者定义了直播间相关表格， 后者定义了topic相关表格。
6）@专题功能---代码生成提示词---第二阶段-Service与Endpoint层.md 文档中对函数生成的指令是否于@专题功能完整设计文档-修正版.md 文档规定的对应操作流程保持一致；
7）@专题功能---代码生成提示词---第二阶段-Service与Endpoint层.md 文档中对函数生成的指令风格和规范 是否与@直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第二阶段.md  @直播核心功能---直播流的数据访问层与基础端点代码生成提示词---第三阶段.md 中生成函数指令的风格，要求和规范保持一致。 
8) @专题功能---代码生成提示词---第二阶段-Service与Endpoint层.md  中对代码的规范和要求是否已经有机的融入了下面的要求。
# ✅ 代码生成提示词核心条目（完整版）

以下是代码生成提示词中**必须包含的内容**，用于确保代码风格统一、健壮性高、可维护性强，适用于 FastAPI 等异步 Web 框架下的高风险接口开发。

---

## 1. 核心编码原则 (Core Coding Principles)

### 🛡️ 安全异步异常处理 (Safe Async Exception Handling)

- **规则**：在任何 `try...except` 块中，如果需要使用来自数据库 ORM 对象（如 `current_user`）的属性（如 `current_user.id`）进行日志记录或错误处理，这些属性**必须在进入 `try` 块之前**被提取并存储到局部变量中。
- **指令**：**严禁**在捕获了数据库相关异常（如 `IntegrityError`）的 `except` 块中直接访问可能已与失效会话关联的 ORM 对象的属性。

---

## 2. 接口实现规范 (Endpoint Implementation)

### 🧱 主动变量提取 (Proactive Variable Extraction)

- **实现流程**：
  - 在进入 `try` 块之前，主动提取所有需要在异常处理中使用的变量（如：`user_id_for_logging = current_user.id`）。
  - 在 `except` 块中，**必须使用这些局部变量**进行日志记录或错误处理，避免访问失效对象。

### 🧪 异常处理 (Exception Handling)

- 将整个接口流程包裹在 `try...except` 块中。
- 若发生数据库查询等未知异常，应：
  - 记录错误日志；
  - 返回统一错误响应：
    ```python
    JSONResponse(
        status_code=500,
        content=error_response(code=1002, message='数据库查询错误')
    )
    ```

---

## 3. 响应处理规范 (Response Handling)

### ✅ 成功响应

- 所有成功返回**必须**调用 `success_response(data=...)` 函数构建响应内容。
- 示例：
  ```python
  return success_response(data=user_info)
  ```

### ❌ 错误响应

- 所有业务错误必须返回 `fastapi.responses.JSONResponse`，其 `content` 由 `error_response(...)` 构建。
- 示例：
  ```python
  return JSONResponse(
      status_code=400,
      content=error_response(code=1001, message='参数错误')
  )
  ```

> 假设 `success_response()` 和 `error_response()` 已定义在 `app/core/responses.py` 中。

---

## 4. 外部服务连接规范 (External Service Connections)

### 🔐 环境变量驱动配置

- 所有外部服务（如 Redis、数据库、第三方 API）的连接信息**必须**通过环境变量读取。
- 示例：
  ```python
  import os
  redis_host = os.getenv("REDIS_HOST", "localhost")
  redis_port = os.getenv("REDIS_PORT", "6379")
  ```
- 目的：确保代码在本地、Docker、测试、生产等不同环境下**无需修改即可运行**。

---

## 5. 接口设计规范 (API Design Principles)

### 🌐 RESTful 风格

- 所有接口必须遵循 RESTful 设计风格，包括但不限于：
  - 使用合适的 HTTP 方法（GET、POST、PUT、DELETE）；
  - URL 路径使用名词复数形式（如 `/users` 而非 `/user`）；
  - 使用嵌套路径表示资源关系（如 `/users/{user_id}/roles`）；
  - URL 中不包含动词，动作通过 HTTP 方法表达；
  - 返回统一结构的 JSON 响应（通过 `success_response` / `error_response`）。


此外 