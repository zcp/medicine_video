-----

# AI代码生成API接口实现提示词的修改模板

## 概述

本文档是一个用于迭代开发AI代码生成提示词的模板。当从一个开发批次（例如，第一批次）推进到下一个批次（例如，第二批次）时，请遵循以下步骤来更新您的提示词文档。
我已经将提示词文档中的旧批次API接口详细说明完整地替换为新批次的API接口详细说明， 更新了目录结构，并附上了必要的源代码。请在此基础上完成下面的步骤。

## 修改步骤

1.  **修改任务目标**: 根据新批次的开发任务（例如，要新增/修改的文件、要实现的核心功能），清晰、明确地重写“任务目标 (Task Objective)”部分。
2.  **修改具体代码生成指令**:
      * 根据新批次API接口的“输入”、“输出”和“执行流程”，重写“具体代码生成指令 (Specific Code Generation Instructions)”部分。
      * 确保每个API接口的实现流程指令，都明确指出了如何调用`response.py`中的`success_response`和`error_response`，以保证所有接口的响应体结构一致。
3.  **确保RESTful风格**: 检查并确保所有路由 (`APIRouter`) 的`prefix`末尾没有`/`符号。
4.  **交叉验证**: 再次逐一确认“具体代码生成指令”中的每一个API接口生成指令，都与对应的“API接口详细说明”在输入数据、输出数据和执行流程上完全一致。
5.  **代码修改风险评估**:
      * 确认本次代码生成是否会修改之前批次的代码。
      * 如果**不应修改**，请在提示词中明确加入指令，如：“**严禁修改**任何已存在的代码”、“在文件**末尾追加**新函数”等。
      * 如果**需要修改**，请在提示词中清晰地指出需要修改哪些文件和函数。
6.  **集成核心编码原则**: 确保提示词中包含了以下所有核心编码原则，以指导AI生成健壮、安全的代码。

-----

## 核心编码原则 (Core Coding Principles)

### 🛡️ 安全异步异常处理 (Safe Async Exception Handling)

  - **规则**：在任何 `try...except` 块中，如果需要使用来自数据库 ORM 对象（如 `current_user`）的属性（如 `current_user.id`）进行日志记录或错误处理，这些属性**必须在进入 `try` 块之前**被提取并存储到局部变量中。
  - **指令**：**严禁**在捕获了数据库相关异常（如 `IntegrityError`）的 `except` 块中直接访问可能已与失效会话关联的 ORM 对象的属性。

### 🧱 主动变量提取 (Proactive Variable Extraction)

  - **实现流程**：
      - 在进入 `try` 块之前，主动提取所有需要在异常处理中使用的变量（如：`user_id_for_logging = current_user.id`）。
      - 在 `except` 块中，**必须使用这些局部变量**进行日志记录或错误处理，避免访问失效对象。
  - **范例**:
    ```python
    async def service_method(self, user: User, request: SomeRequest) -> SomeResult:
        # 🔴 第一步：立即提取变量（安全红线）
        user_id_for_logging = user.id
        request_field_for_logging = request.field

        logger.info(f"开始处理: user_id={user_id_for_logging}")

        try:
            # 业务逻辑
            pass
        except SpecificException as e:
            # 🔴 只使用局部变量（安全红线）
            logger.warning(f"特定错误: user_id={user_id_for_logging}")
            raise
        except Exception as e:
            # 🔴 只使用局部变量（安全红线）
            logger.error(f"通用错误: user_id={user_id_for_logging}, error={e}")
            raise
    ```
  - **绝对禁止的反模式**:
    ```python
    # ❌ 错误：在异常处理中访问ORM对象
    except IntegrityError:
        logger.warning(f"错误: user_id={user.id}")  # ← 会导致greenlet错误
    ```

### 🧪 异常处理 (Exception Handling)

  - 将整个接口流程包裹在 `try...except` 块中。
  - 若发生数据库查询等未知异常，应：
      - 记录错误日志；
      - 返回统一错误响应：
        ```python
        JSONResponse(
            status_code=500,
            content=error_response(code=1002, message='数据库查询错误')
        )
        ```

-----

## 响应处理规范 (Response Handling)

### ✅ 成功响应

  - 所有成功返回**必须**调用 `success_response(data=...)` 函数构建响应内容。
  - **示例**：
    ```python
    return success_response(data=user_info)
    ```

### ❌ 错误响应

  - 所有业务错误必须返回 `fastapi.responses.JSONResponse`，其 `content` 由 `error_response(...)` 构建。
  - **示例**：
    ```python
    return JSONResponse(
        status_code=400,
        content=error_response(code=1001, message='参数错误')
    )
    ```

> 假设 `success_response()` 和 `error_response()` 已定义在 `app/core/responses.py` 中。

-----

## 外部服务连接规范 (External Service Connections)

### 🔐 环境变量驱动配置

  - 所有外部服务（如 Redis、数据库、第三方 API）的连接信息**必须**通过环境变量读取。
  - **示例**：
    ```python
    import os
    redis_host = os.getenv("REDIS_HOST", "localhost")
    redis_port = os.getenv("REDIS_PORT", "6379")
    ```
  - **目的**：确保代码在本地、Docker、测试、生产等不同环境下**无需修改即可运行**。



好的，完全没有问题。您希望我将您提供的、用于修改AI提示词的多个规范和指令，整理成一个清晰、结构化、可复用的**Markdown模板**。

这是一个非常好的想法，它可以将您的最佳实践固化下来，方便您在后续批次中高效地创建新的提示词。

下面就是为您精心整理和格式化的\*\*“AI提示词修改模板”\*\*。

-----

# AI接口测试代码生成提示词 - 修改模板

## 概述

本文档是一个用于迭代开发AI测试代码生成提示词的模板。当从一个测试批次（例如，第一批次）推进到下一个测试批次（例如，第二批次）时，请遵循以下步骤来更新您的提示词文档。

我已经将提示词文档中的旧批次API接口详细说明，完整地替换为新批次的API接口详细说明，审查并更新项目目录结构图，以反映当前批次的最新状态。
附上了所有新批次需要被测试的源代码全文。
请帮我做如下事情：
1.  **修改任务目标**: 根据新批次的开发任务（例如，要新增/修改的文件、要实现的核心功能），清晰、明确地重写“任务目标 (Task Objective)”部分。
2.  **修改具体代码生成指令**:
      * 根据新批次API接口的“输入”、“输出”和“执行流程”，重写“具体代码生成指令 (Specific Code Generation Instructions)”部分。
      * 确保所有测试都已覆盖新批次的所有API接口。
3.  **确保一致性与隔离性**:
      * 检查并确保提示词前后内容一致，没有引用错误批次的API或文件。
      * 确保指令是**非破坏性的**，即不会修改已有的测试程序（特别是 `conftest.py`）。
4.  **集成核心测试规范**: 确保提示词中包含了以下所有核心测试规范，以指导AI生成健壮、可靠的代码。



## 核心测试规范 (必须包含在提示词中)

### 1\. 测试数据隔离 (Test Data Isolation)

  * **指令**: 为了防止测试之间因违反数据库 `UNIQUE` 约束而产生冲突，所有唯一性字段（如 `username`, `email`, `product_code`, `transaction_id`）**必须**是随机生成的，以确保每次测试运行时的数据都是独一无二的。
  * **实现**:
      * 推荐安装并使用 `faker` 库 (`pip install Faker`)，因为它可以生成符合特定格式和长度要求的、更真实的数据。
      * 所有生成的随机数据**必须**严格遵守 `app/schemas/users.py` 中 Pydantic 模型的验证规则。
  * **具体要求 (Requirements)**:
      * **`username`**:
          * **规则**: 必须符合 `schemas.UserBase` 中定义的 `min_length=1` 和 `max_length=50`。
          * **Faker 范例**: `fake.user_name()` 结合一个随机后缀，例如 `f"{fake.user_name()}_{uuid.uuid4().hex[:6]}"` 以确保唯一性。
      * **`email`**:
          * **规则**: 必须是一个有效的邮箱格式。
          * **Faker 范例**: `fake.email()`。
      * **`phone_number` (已补充)**:
          * **规则**: 长度不能超过20个字符。
          * **Faker 范例**: `fake.phone_number()` (可能需要根据具体格式进行调整)。
      * **`product_code`**:
          * **规则**: 必须符合 `schemas.MembershipProductCreate` 中定义的 `min_length=1` 和 `max_length=50`。
          * **Faker 范例**: 使用大写字母和数字的组合，例如 `f"PROD_{''.join(random.choices(string.ascii_uppercase + string.digits, k=10))}"`。
      * **`transaction_id`**:
          * **规则**: 长度不能超过255个字符。
          * **Faker 范例**: `f"txn_{uuid.uuid4().hex}"`。
      * **禁止硬编码**: 在测试的“准备 (Arrange)”阶段，**严禁**使用硬编码的字符串（如 `'usera'`) 作为唯一性字段的值，除非测试目的就是为了验证重复性约束。

-----

### 2\. 测试函数生成规范

**必须严格遵循以下模板生成所有测试函数：**

```python
# CRUD测试模板
@pytest.mark.asyncio
async def test_crud_operation(self, db_session):
    """测试描述"""
    async for db in db_session:
        # 测试逻辑全部在此 async for 块内
        pass

# API测试模板（单fixture）
@pytest.mark.asyncio
async def test_api_endpoint(self, async_client, mock_redis_captcha_success):
    """API测试描述"""
    async for client in async_client:
        # API测试逻辑全部在此 async for 块内
        pass

# API+数据库测试模板（多fixture）
@pytest.mark.asyncio
async def test_api_with_db(self, async_client, db_session, mock_redis_captcha_success):
    """API和数据库组合测试"""
    async for client in async_client:
        async for db in db_session:
            # 组合测试逻辑全部在此嵌套 async for 块内
            pass
```

> **⚠️ 禁止的写法：**
>
>   * 不得在 `async for` 外部调用数据库操作
>   * 不得直接使用 fixture 参数而不解包
>   * 不得在函数参数中添加类型提示（会导致混淆）

-----

### 3\. 深度测试要求

所有测试用例必须采用**准备 (Arrange)**、\*\*执行 (Act)**和**断言 (Assert)\*\*模式，并包含更多细节的测试，而不仅仅是检查`status_code`。

  * **必须同时验证 API 响应 (状态码、业务码、数据) 和 数据库状态变化**。

#### **深度测试流程**

1.  **准备 (Arrange)**:
      * 在 Arrange 阶段之前，先清空相关表数据，确保测试环境干净。
      * 创建必要的测试数据（如 `User` 和 `MembershipProduct`），并构造合法的对象（如 `schemas.UserMembershipCreate`）。
2.  **执行 (Act)**:
      * 调用相应的业务逻辑函数（如 `crud_user_membership.create()`）或API端点来执行操作。
3.  **断言 (Assert)**:
      * 验证返回结果的有效性（如对象不为 `None`，或API响应码正确）。
4.  **状态验证 (State Verification)**:
      * 使用 `db.refresh()` 方法刷新数据库对象以确保获取最新的状态。
      * 通过数据库查询验证数据是否正确插入/更新/删除。
      * 验证关键属性是否符合预期（如 `user_id` 和 `product_code`）。

#### **深度测试范例**

> ```
> 2.  test_login_success:
>     * 准备:
>         * a. 模拟 Redis: Mock `captcha` 校验使其总是成功。
>         * b. 创建用户: 在 `db_session` 中预先创建一个测试用户，并获取其 `username` 和明文 `password`。
>     * 执行: 使用正确的用户名、密码和模拟的验证码信息，向 `POST /api/v1/auth/login` 发送请求。
>     * 断言 API 响应:
>         * a. 断言 HTTP 状态码为 `200`，业务 `code` 为 `200`。
>         * b. 断言 `data` 中包含 `access_token`, `refresh_token`, `token_type`。
>     * 验证数据库状态:
>         * a. 重新从数据库中查询该用户。
>         * b. 断言其 `last_login_at` 字段不再为 `None`，且是一个最近的时间。
>         * c. 断言其 `last_login_ip` 字段已被记录。
> ```





# AI接口代码重构提示词 - 修改模板

这是一个对app/api/v/admin/products.py进行改写的提示词文档。从endpoint->crud， 改变为endpoint->service->crud，
即需要将app/api/v1/admin/products.py中的相关逻辑代码抽取出来，放到对应service中。 我只更新了项目的目录结构，并附上app/api/v1/admin/products.py源代码，其他都需要更新。
请仔细分析文档中的app/api/v1/admin/products.py源代码，对每一个api接口实现进行重构。重构包括：
1）生成对应products.py文件，对每个api接口的实现，包括给出详细地重构流程，具体调用什么方法，各种异常处理等等
2）对app/api/v1/admin/products.py中每个api接口进行重构， 包括给出详细地重构流程，
比如，如何调用admin_products_service.py中的哪些方法， 各种异常处理等等
3）下面给出一个重构指令案例，请参考它对每一个api接口实现的重构
`login_user(self, login_request: schemas.LoginRequest) -> dict`**
* **业务逻辑流程**:
    1.  **图形验证码校验**: 调用 `self.verify_captcha()` 校验 `login_request.captcha_id` 和 `login_request.captcha_solution`。如果失败，`raise CaptchaErrorException()`。
    2.  **用户查询**: 调用 `crud_user.get_by_username()` 查询用户。
    3.  **状态与密码校验**:
        * 如果用户不存在，或者 `user.status` 不为 `NORMAL`，或者 `verify_password()` 校验密码失败，`raise InvalidCredentialsException("用户名或密码错误")`。
    4.  **Token 生成**: 调用内部的JWT工具函数，为该 `user.id` 生成 `access_token` 和 `refresh_token`。
    5.  **信息更新**: 更新用户的 `last_login_at` 和 `last_login_ip` 字段。
    6.  调用 `db.commit()` 和 `db.refresh(user)` 提交更改。
    7.  返回包含 `access_token`, `refresh_token`, `token_type` 的字典。

4）在重构完之后，检查是否存在前后矛盾的地方。是否每一个api接口都进行必要的重构，在重构中是否存在调用不存在方法的情况， 重构之后的实现是否与重构前的实现是等价和一致的。



-----

# AI接口测试代码生成提示词 - 修改模板   for authing集成和其他api接口增加

## 概述

本文档是一个用于迭代开发AI测试代码生成提示词的模板。由于代码增加了authing集成和其他新的api接口，因此需要做增量测试 ，请遵循以下步骤来更新您的提示词文档。

我已经将提示词文档中的旧批次API接口详细说明，完整地替换为新批次的API接口详细说明，审查并更新项目目录结构图，以反映当前批次的最新状态。
附上了所有新批次需要被测试的源代码全文。
请帮我做如下事情：
1.  **修改任务目标**: 根据新批次的开发任务（例如，要新增/修改的文件、要实现的核心功能），清晰、明确地重写“任务目标 (Task Objective)”部分。
2.  **修改具体代码生成指令**:
      * 根据新批次API接口的“输入”、“输出”和“执行流程”，重写“具体代码生成指令 (Specific Code Generation Instructions)”部分。
      * 确保所有测试都已覆盖新批次的所有API接口。
3.  **确保一致性与隔离性**:
      * 检查并确保提示词前后内容一致，没有引用错误批次的API或文件。
      * 确保指令是**非破坏性的**，即不会修改已有的测试程序（特别是 `conftest.py`）。
4.  **集成核心测试规范**: 确保提示词中包含了以下所有核心测试规范，以指导AI生成健壮、可靠的代码。



## 核心测试规范 (必须包含在提示词中)

### 1\. 测试数据隔离 (Test Data Isolation)

  * **指令**: 为了防止测试之间因违反数据库 `UNIQUE` 约束而产生冲突，所有唯一性字段（如 `username`, `email`, `product_code`, `transaction_id`）**必须**是随机生成的，以确保每次测试运行时的数据都是独一无二的。
  * **实现**:
      * 推荐安装并使用 `faker` 库 (`pip install Faker`)，因为它可以生成符合特定格式和长度要求的、更真实的数据。
      * 所有生成的随机数据**必须**严格遵守 `app/schemas/users.py` 中 Pydantic 模型的验证规则。
  * **具体要求 (Requirements)**:
      * **`username`**:
          * **规则**: 必须符合 `schemas.UserBase` 中定义的 `min_length=1` 和 `max_length=50`。
          * **Faker 范例**: `fake.user_name()` 结合一个随机后缀，例如 `f"{fake.user_name()}_{uuid.uuid4().hex[:6]}"` 以确保唯一性。
      * **`email`**:
          * **规则**: 必须是一个有效的邮箱格式。
          * **Faker 范例**: `fake.email()`。
      * **`phone_number` (已补充)**:
          * **规则**: 长度不能超过20个字符。
          * **Faker 范例**: `fake.phone_number()` (可能需要根据具体格式进行调整)。
      * **`product_code`**:
          * **规则**: 必须符合 `schemas.MembershipProductCreate` 中定义的 `min_length=1` 和 `max_length=50`。
          * **Faker 范例**: 使用大写字母和数字的组合，例如 `f"PROD_{''.join(random.choices(string.ascii_uppercase + string.digits, k=10))}"`。
      * **`transaction_id`**:
          * **规则**: 长度不能超过255个字符。
          * **Faker 范例**: `f"txn_{uuid.uuid4().hex}"`。
      * **禁止硬编码**: 在测试的“准备 (Arrange)”阶段，**严禁**使用硬编码的字符串（如 `'usera'`) 作为唯一性字段的值，除非测试目的就是为了验证重复性约束。

-----

### 2\. 测试函数生成规范

**必须严格遵循以下模板生成所有测试函数：**

```python
# CRUD测试模板
@pytest.mark.asyncio
async def test_crud_operation(self, db_session):
    """测试描述"""
    async for db in db_session:
        # 测试逻辑全部在此 async for 块内
        pass

# API测试模板（单fixture）
@pytest.mark.asyncio
async def test_api_endpoint(self, async_client, mock_redis_captcha_success):
    """API测试描述"""
    async for client in async_client:
        # API测试逻辑全部在此 async for 块内
        pass

# API+数据库测试模板（多fixture）
@pytest.mark.asyncio
async def test_api_with_db(self, async_client, db_session, mock_redis_captcha_success):
    """API和数据库组合测试"""
    async for client in async_client:
        async for db in db_session:
            # 组合测试逻辑全部在此嵌套 async for 块内
            pass
```

> **⚠️ 禁止的写法：**
>
>   * 不得在 `async for` 外部调用数据库操作
>   * 不得直接使用 fixture 参数而不解包
>   * 不得在函数参数中添加类型提示（会导致混淆）

-----

### 3\. 深度测试要求

所有测试用例必须采用**准备 (Arrange)**、\*\*执行 (Act)**和**断言 (Assert)\*\*模式，并包含更多细节的测试，而不仅仅是检查`status_code`。

  * **必须同时验证 API 响应 (状态码、业务码、数据) 和 数据库状态变化**。

#### **深度测试流程**

1.  **准备 (Arrange)**:
      * 在 Arrange 阶段之前，先清空相关表数据，确保测试环境干净。
      * 创建必要的测试数据（如 `User` 和 `MembershipProduct`），并构造合法的对象（如 `schemas.UserMembershipCreate`）。
2.  **执行 (Act)**:
      * 调用相应的业务逻辑函数（如 `crud_user_membership.create()`）或API端点来执行操作。
3.  **断言 (Assert)**:
      * 验证返回结果的有效性（如对象不为 `None`，或API响应码正确）。
4.  **状态验证 (State Verification)**:
      * 使用 `db.refresh()` 方法刷新数据库对象以确保获取最新的状态。
      * 通过数据库查询验证数据是否正确插入/更新/删除。
      * 验证关键属性是否符合预期（如 `user_id` 和 `product_code`）。

#### **深度测试范例**

> ```
> 2.  test_login_success:
>     * 准备:
>         * a. 模拟 Redis: Mock `captcha` 校验使其总是成功。
>         * b. 创建用户: 在 `db_session` 中预先创建一个测试用户，并获取其 `username` 和明文 `password`。
>     * 执行: 使用正确的用户名、密码和模拟的验证码信息，向 `POST /api/v1/auth/login` 发送请求。
>     * 断言 API 响应:
>         * a. 断言 HTTP 状态码为 `200`，业务 `code` 为 `200`。
>         * b. 断言 `data` 中包含 `access_token`, `refresh_token`, `token_type`。
>     * 验证数据库状态:
>         * a. 重新从数据库中查询该用户。
>         * b. 断言其 `last_login_at` 字段不再为 `None`，且是一个最近的时间。
>         * c. 断言其 `last_login_ip` 字段已被记录。
> ```
